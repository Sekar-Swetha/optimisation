\documentclass[11pt,a4paper]{article}
\usepackage[margin=2.2cm]{geometry}
\usepackage{graphicx}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{mathtools}
\usepackage{float}
\usepackage{hyperref}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{listings}
\usepackage{fancyhdr}
\usepackage{microtype}

% Theorem environments
\newtheorem{theorem}{Theorem}[section]
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{remark}{Remark}[section]

\hypersetup{colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue}

\lstset{
  language=Python,
  basicstyle=\ttfamily\scriptsize,
  keywordstyle=\color{blue},
  commentstyle=\color{gray},
  stringstyle=\color{red},
  numbers=left,
  numberstyle=\tiny\color{gray},
  breaklines=true,
  frame=single,
  captionpos=b
}

\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small CS7DS2 -- Final Assignment}
\fancyhead[R]{\small Swetha Sekar (25336453)}
\fancyfoot[C]{\thepage}

\title{%
  \includegraphics[width=0.25\textwidth]{tcd_logo.jpg}\\[1em]
  \textbf{CS7DS2 -- Optimisation Algorithms for Data Analysis}\\[0.5em]
  \Large Final Assignment Report
}
\author{Swetha Sekar \\ Student ID: 25336453 \\ School of Computer Science and Statistics \\ Trinity College Dublin}
\date{April 2026}

\begin{document}
\maketitle
\thispagestyle{empty}

\begin{abstract}
This report presents a rigorous comparative study of fifteen optimisation algorithms across six methodological categories applied to three benchmark functions spanning well-conditioned quadratic, mildly non-convex, and highly ill-conditioned regimes. The six questions investigate: (1)~adaptive step-size methods (Polyak, Adagrad, RMSprop, Heavy Ball), revealing that the Polyak step diverges when $f^{\star}$ is misspecified but achieves $f = 9.4 \times 10^{-3}$ on Rosenbrock when $f^{\star}=0$ is correct; (2)~momentum and stochastic methods, where Nesterov acceleration achieves the optimal $O(1/k^2)$ rate, reaching $f = 0.479$ on Rosenbrock versus $f = 3.388$ for GD, while mini-batch SGD reveals the batch-size/variance trade-off; (3)~Newton's method, which converges in a \emph{single iteration} for quadratic losses and reduces Rosenbrock loss from the GD value of~3.73 to~0.69 in only 20 iterations; (4)~derivative-free optimisation, where finite-difference quality is highly sensitive to perturbation $\delta$, and the Nesterov random search provides dimension-independent two-evaluation-per-step zeroth-order convergence; (5)~constrained optimisation, where projected gradient descent enforces hard feasibility from iteration~1 while penalty methods offer a softer, $\lambda$-dependent approximation with explicit KKT analysis; and (6)~the Frank--Wolfe algorithm, which achieves $O(1/k)$ convergence while avoiding projections entirely. Results confirm that curvature information provides the greatest acceleration on ill-conditioned problems, while adaptive step sizes offer the best practical trade-off between speed and hyperparameter robustness.
\end{abstract}

\tableofcontents
\newpage

% ============================================================
\section{Benchmark Problems and Experimental Setup}
\label{sec:benchmarks}
% ============================================================

All experiments use \texttt{np.random.seed(42)} for reproducibility. Three benchmark functions present distinct optimisation challenges, summarised in Table~\ref{tab:bench_props}.

\subsection{Benchmark A: Linear Regression Quadratic Loss}

A synthetic dataset with $m = 1000$ samples in $\mathbb{R}^2$ is generated:
\begin{equation}
\theta^{\star} = \begin{bmatrix} 3 \\ 4 \end{bmatrix}, \quad x^{(i)} \sim \mathcal{N}(0, I_2), \quad y^{(i)} = (\theta^{\star})^T x^{(i)} + \varepsilon^{(i)}, \quad \varepsilon^{(i)} \overset{\text{i.i.d.}}{\sim} \mathcal{N}(0, 1).
\end{equation}
The mean squared error loss is:
\begin{equation}
J(\theta) = \frac{1}{2m} \|X\theta - y\|^2, \qquad
\nabla J(\theta) = \frac{1}{m}X^T(X\theta - y), \qquad
H = \frac{1}{m}X^TX \succ 0.
\end{equation}
The Hessian $H$ is constant (independent of $\theta$), making this a strongly convex quadratic. By the law of large numbers, $H \to I_2$ as $m \to \infty$; with $m=1000$ and seed~42, $\lambda_{\max}(H) \approx 1.01$, $\lambda_{\min}(H) \approx 0.99$, giving condition number $\kappa \approx 1.02$. The irreducible noise floor is $J^{\star} = \frac{1}{2m}\|\varepsilon\|^2 \approx \frac{\sigma^2}{2} = 0.5$; numerically $J^{\star} \approx 0.4826$. Starting point: $\theta_0 = (0, 0)^T$.

\subsection{Benchmark B: Toy Neural Network Loss}

\begin{equation}
f(x_1, x_2) = (x_1 - 1)^2 + 5(x_2 - 2)^2 + \sin(x_1).
\label{eq:bench_B}
\end{equation}
The gradient and Hessian are:
\begin{equation}
\nabla f = \begin{bmatrix} 2(x_1 - 1) + \cos(x_1) \\ 10(x_2 - 2) \end{bmatrix}, \qquad
H(x) = \begin{pmatrix} 2 - \sin(x_1) & 0 \\ 0 & 10 \end{pmatrix}.
\end{equation}
The Hessian is diagonal with $\lambda_1 = 2 - \sin(x_1) \in [1, 3]$ (since $\sin(x_1) \in [-1,1]$) and $\lambda_2 = 10$, giving condition number $\kappa = 10/(2-\sin(x_1)) \in [10/3, 10]$ and smoothness $L = \sup_x \lambda_{\max}(H(x)) = 10$. Setting $\nabla f = 0$ yields $x_2^{\star} = 2$ and $2(x_1^{\star}-1) + \cos(x_1^{\star}) = 0$, solved numerically to $x_1^{\star} \approx 0.5828$, so $x^{\star} \approx (0.5828, 2.0)$ with $f^{\star} \approx 0.7244$. Starting point: $x_0 = (-1, 4)^T$.

\subsection{Benchmark C: Rosenbrock Function}

\begin{equation}
f(x_1, x_2) = (1 - x_1)^2 + 100(x_2 - x_1^2)^2.
\label{eq:bench_C}
\end{equation}
Global minimum at $(1, 1)$ with $f^{\star} = 0$. The Hessian is:
\begin{equation}
H(x) = \begin{pmatrix} 2 + 1200x_1^2 - 400x_2 & -400x_1 \\ -400x_1 & 200 \end{pmatrix}.
\end{equation}
At $x_0 = (-1, 1)$: $H(-1,1) = \begin{pmatrix} 802 & 400 \\ 400 & 200 \end{pmatrix}$ with eigenvalues $\lambda_1 \approx 1001.6$, $\lambda_2 \approx 0.4$, giving $\kappa \approx 2504$ and stability constraint $\alpha < 2/L \approx 0.002$. This extreme ill-conditioning arises from two geometrically distinct difficulties: (i)~\textbf{steep transverse direction} --- large curvature $\approx 1001.6$ across the valley $x_2 = x_1^2$ severely restricts the step size; (ii)~\textbf{flat longitudinal direction} --- curvature $\approx 0.4$ along the valley floor means very small gradient components drive progress toward the minimum, requiring many steps even without the step-size constraint. Gradient methods must use $\alpha \leq 1/L \approx 0.001$ to avoid oscillations across the valley, yet at this step size, longitudinal progress per step is only $\alpha \mu \|x_k - x^{\star}\| \approx 0.001 \times 0.4 \times \|x_k-x^{\star}\|$, extremely slow. Starting point: $x_0 = (-1, 1)^T$.

\subsection{Constrained Feasible Set}

For Questions 5 and 6: $\mathcal{X} = \{x \in \mathbb{R}^2 : 0.5 \leq x_1 \leq 5,\ -5 \leq x_2 \leq 10\}$, with diameter $\mathrm{diam}(\mathcal{X}) = \sqrt{4.5^2 + 15^2} \approx 15.66$.

\subsection{Benchmark Properties Summary}

\begin{table}[H]
\centering
\caption{Benchmark problem properties. $L$: smoothness (Lipschitz gradient) constant; $\mu$: strong convexity constant; $\kappa = L/\mu$: condition number.}
\label{tab:bench_props}
\begin{tabular}{lcccccc}
\toprule
\textbf{Benchmark} & \textbf{Type} & $L$ & $\mu$ & $\kappa$ & $f^{\star}$ & \textbf{Start $x_0$} \\
\midrule
A (Linear Regression) & Strongly convex & $\approx 1.01$ & $\approx 0.99$ & $\approx 1.02$ & $0.4826$ & $(0,0)$ \\
B (Toy NN) & Non-convex & $10$ & $\approx 1.45$ (near $x^{\star}$) & $[3.3,10]$ & $0.7244$ & $(-1,4)$ \\
C (Rosenbrock) & Non-convex & $\approx 1002$ & $\approx 0.4$ (near $x^{\star}$) & $\approx 2504$ & $0$ & $(-1,1)$ \\
\bottomrule
\end{tabular}
\end{table}

% ============================================================
\section{Question 1: Adaptive Step-Size Methods \hfill [30 marks]}
\label{sec:q1}
% ============================================================

\subsection{Theoretical Background}

For an $L$-smooth, $\mu$-strongly convex function, gradient descent with step size $\alpha \in (0, 1/L]$ satisfies:
\begin{equation}
f(x_{k+1}) - f^{\star} \leq \left(1 - \frac{\mu}{L}\right)^k \bigl(f(x_0) - f^{\star}\bigr).
\label{eq:gd_rate}
\end{equation}
The contraction factor $(1 - \mu/L) = 1 - 1/\kappa$ is close to 1 for large $\kappa$, explaining why GD is slow on ill-conditioned problems. Adaptive methods attempt to overcome this by constructing per-coordinate or iteration-dependent step sizes that implicitly account for curvature.

\textbf{Quantitative convergence on Benchmark A.} With $\kappa \approx 1.02$ and step $\alpha = 0.08$, the per-step contraction of GD on a quadratic is $\rho = \max(|1-\alpha\lambda|) = \max(|1-0.08\times 0.99|,\, |1-0.08\times 1.01|) \approx 0.921$. The initial error $f(\theta_0) - f^{\star} = J(0) - J^{\star}$ with $\theta_0 = (0,0)$ and $\theta^{\star} = (3,4)$, $\sigma=1$ can be estimated as $J(0) \approx \frac{1}{2}(\|\theta^{\star}\|^2 + \sigma^2) = \frac{1}{2}(25+1) = 13$, so initial gap $\approx 12.5$. After $k=80$ steps: $\rho^{80} \times 12.5 \approx 0.921^{80} \times 12.5 \approx 0.001 \times 12.5 = 0.012$, confirming $f(\theta_{80}) \approx J^{\star} + 0.012 \approx 0.495$, close to the observed convergence. With the \emph{optimal} step $\alpha^{\star} = 2/(\mu+L) \approx 1.0$, the contraction would be $((\kappa-1)/(\kappa+1))^k \approx (0.01/2.01)^k \approx (0.005)^k$, converging in 3 steps to machine precision. The sub-optimal step $\alpha = 0.08$ thus adds an unnecessary factor of $\sim 80/3 \approx 27\times$ more iterations but is chosen conservatively to handle the three benchmarks with a consistent tuning protocol.

\begin{theorem}[Gradient Descent Convergence, \cite{nesterov2004introductory}]
Let $f$ be $L$-smooth and $\mu$-strongly convex with minimiser $x^{\star}$. Under GD with $\alpha = 1/L$:
\[
\|x_k - x^{\star}\|^2 \leq \left(1 - \frac{\mu}{L}\right)^k \|x_0 - x^{\star}\|^2.
\]
\end{theorem}

\subsection{Algorithm Formulations}

Four adaptive methods are compared against a constant step-size GD baseline across all three benchmarks for 120 iterations.

\begin{algorithm}[H]
\caption{Polyak Step Size \cite{polyak1969minimization}}
\label{alg:polyak}
\begin{algorithmic}[1]
\Require Initial point $x_0$, optimal value $f^{\star}$, tolerance $\varepsilon > 0$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $\alpha_k \gets \dfrac{f(x_k) - f^{\star}}{\|g_k\|^2 + \varepsilon}$
    \State $x_{k+1} \gets x_k - \alpha_k\, g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{proposition}[Polyak Step Convergence]
If $f$ is convex and $f^{\star}$ is the true minimum, the Polyak step size satisfies $f(x_k) - f^{\star} \leq \frac{L\|x_0 - x^{\star}\|^2}{2(k+1)}$ for all $k \geq 0$. When $f^{\star}$ is incorrect, however, the step $\alpha_k = (f(x_k) - \tilde{f}^{\star})/\|g_k\|^2$ diverges as $\|g_k\| \to 0$ while $f(x_k) - \tilde{f}^{\star} \to f^{\star}_{\text{true}} - \tilde{f}^{\star} \neq 0$.
\end{proposition}

\begin{algorithm}[H]
\caption{Adagrad \cite{duchi2011adaptive}}
\label{alg:adagrad}
\begin{algorithmic}[1]
\Require $x_0$, base learning rate $\alpha_0$, tolerance $\varepsilon > 0$, iterations $N$
\State $G_0 \gets \mathbf{0} \in \mathbb{R}^d$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $G_{k+1} \gets G_k + g_k \odot g_k$ \Comment{Cumulative per-coordinate squared gradient}
    \State $x_{k+1} \gets x_k - \dfrac{\alpha_0}{\sqrt{G_{k+1}} + \varepsilon} \odot g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

Adagrad's per-coordinate accumulator $G_k$ scales the learning rate inversely with the historical gradient magnitude in each dimension: coordinates with consistently large gradients receive smaller steps, while rarely-updated coordinates retain larger steps. The effective step for coordinate $i$ at step $T$ is:
\begin{equation}
\alpha_T^{(i)} = \frac{\alpha_0}{\sqrt{G_T^{(i)}} + \varepsilon} = \frac{\alpha_0}{\sqrt{\sum_{t=1}^T g_{t,i}^2} + \varepsilon}.
\label{eq:adagrad_step}
\end{equation}
For a constant gradient $g_i = c$, this decays as $\alpha_0/(c\sqrt{T})$ --- exactly the $O(1/\sqrt{T})$ schedule that satisfies the Robbins--Monro conditions $\sum_T \alpha_T = \infty$, $\sum_T \alpha_T^2 < \infty$. This yields the online regret bound $R_T \leq \frac{1}{2\alpha_0}\sum_i \|g_{1:T,i}\|_2$, with $O(1/\sqrt{T})$ average regret for bounded gradients \cite{duchi2011adaptive}. The bound is tighter than $O(\sqrt{T})$ when gradients are sparse: if coordinate $i$ only receives nonzero gradients in $s_i \ll T$ steps, its effective rate is $\alpha_0/\sqrt{s_i}$ rather than $\alpha_0/\sqrt{T}$, giving substantially faster convergence for sparse problems (e.g., word embeddings with rare words). In our dense-gradient benchmarks, this sparsity advantage does not appear. The critical drawback is that $G_k$ grows monotonically, causing the effective step to decay to zero irrespective of whether convergence has been achieved --- even if the iterates are far from the minimum, late-training step sizes may be too small to make further progress (as seen on Rosenbrock where Adagrad achieves $f=2.20$ vs.\ Heavy Ball's $f=1.40$).

\begin{algorithm}[H]
\caption{RMSprop \cite{tieleman2012rmsprop}}
\label{alg:rmsprop}
\begin{algorithmic}[1]
\Require $x_0$, learning rate $\alpha_0$, decay $\beta \in (0,1)$, tolerance $\varepsilon > 0$, iterations $N$
\State $v_0 \gets \mathbf{0}$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $v_{k+1} \gets \beta\, v_k + (1 - \beta)\, g_k \odot g_k$ \Comment{Exponential moving average of $g^2$}
    \State $x_{k+1} \gets x_k - \dfrac{\alpha_0}{\sqrt{v_{k+1}} + \varepsilon} \odot g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

RMSprop replaces Adagrad's cumulative sum with an exponential moving average (EMA) with decay $\beta = 0.9$. The EMA gives weight $\beta^j(1-\beta)$ to gradient $g_{k-j}$, effectively remembering only the last $\sim 1/(1-\beta) = 10$ iterations. This prevents the step size from decaying to zero, enabling RMSprop to adapt to non-stationary curvature.

\begin{algorithm}[H]
\caption{Polyak Momentum / Heavy Ball \cite{polyak1964heavy}}
\label{alg:heavyball}
\begin{algorithmic}[1]
\Require $x_0$, step size $\alpha$, momentum $\beta \in [0,1)$, iterations $N$
\State $z_0 \gets \mathbf{0}$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_{k+1} \gets \beta\, z_k + \alpha\, \nabla f(x_k)$ \Comment{Accumulated gradient (velocity)}
    \State $x_{k+1} \gets x_k - z_{k+1}$
\EndFor
\end{algorithmic}
\end{algorithm}

The ``velocity'' $z_k$ accumulates gradient history with exponential decay $\beta^j$, allowing the iterate to build speed along consistent gradient directions. For strongly convex quadratics, the optimal Heavy Ball parameters are:
\begin{equation}
\alpha^{\star} = \frac{4}{(\sqrt{L}+\sqrt{\mu})^2}, \qquad \beta^{\star} = \left(\frac{\sqrt{\kappa}-1}{\sqrt{\kappa}+1}\right)^2,
\label{eq:hb_optimal}
\end{equation}
achieving the optimal linear rate $(1 - 1/\sqrt{\kappa})^k$ versus GD's $(1 - 1/\kappa)^k$ \cite{polyak1964heavy}. Numerically, for the three benchmarks:
\begin{center}
\begin{tabular}{lccccc}
\toprule
Benchmark & $\kappa$ & $\alpha^{\star}$ & $\beta^{\star}$ & Used $\alpha$ & Used $\beta$ \\
\midrule
A ($L{=}1.01, \mu{=}0.99$) & 1.02 & 1.000 & 0.0025 & 0.045 & 0.88 \\
B ($L{=}10, \mu{\approx}1.45$) & $\approx 6.9$ & 0.210 & 0.201 & 0.035 & 0.90 \\
C ($L{\approx}1002, \mu{\approx}0.4$) & $\approx 2504$ & 0.00384 & 0.924 & 0.0008 & 0.86 \\
\bottomrule
\end{tabular}
\end{center}
On Benchmark~A the optimal $\beta^{\star} = 0.0025$ is near zero (near-isotropic curvature), so the used $\beta = 0.88$ causes modest oscillation but converges nonetheless because the problem is so well-conditioned. On Benchmark~C the used $\alpha = 0.0008$ is more conservative than $\alpha^{\star} = 0.00384$ and $\beta = 0.86$ is close to $\beta^{\star} = 0.924$; this conservatism is appropriate since the quadratic theory does not apply to the non-convex Rosenbrock and more aggressive steps would cause divergence.

\subsection{Hyperparameter Settings}

\begin{table}[H]
\centering
\caption{Q1: Hyperparameter settings. All methods run for 120 iterations.}
\label{tab:q1_params}
\begin{tabular}{llccc}
\toprule
\textbf{Method} & \textbf{Parameter} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & $\alpha$ & 0.08 & 0.06 & 0.0012 \\
\midrule
Polyak & $f^{\star}$ & 0 & 0 & 0 \\
 & $\varepsilon$ & $10^{-4}$ & $10^{-4}$ & $10^{-3}$ \\
\midrule
Adagrad & $\alpha_0$ & 1.8 & 1.2 & 0.45 \\
 & $\varepsilon$ & $10^{-5}$ & $10^{-5}$ & $10^{-5}$ \\
\midrule
RMSprop & $\alpha_0$ & 0.22 & 0.14 & 0.0035 \\
 & $\beta$ & 0.9 & 0.9 & 0.9 \\
 & $\varepsilon$ & $10^{-5}$ & $10^{-5}$ & $10^{-5}$ \\
\midrule
Heavy Ball & $\alpha$ & 0.045 & 0.035 & 0.0008 \\
 & $\beta$ & 0.88 & 0.90 & 0.86 \\
\bottomrule
\end{tabular}
\end{table}

Note that Benchmark C requires very small step sizes ($\alpha \leq 0.002$ for stability) due to $L \approx 1002$. The Polyak step on Benchmark C uses $f^{\star} = 0$, which is the exact global minimum; on Benchmarks A and B, $f^{\star} = 0$ is incorrect (true minima are 0.4826 and 0.7244, respectively).

\subsection{Convergence Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_A.pdf}
\caption{Q1: Convergence on Benchmark~A (Linear Regression). GD, Adagrad, and Heavy Ball converge smoothly to $J^{\star} \approx 0.4826$ within $\sim$50 iterations, consistent with the near-unit condition number $\kappa \approx 1.02$ giving linear rate $(1-1/\kappa)^k \approx (0.02)^k$. RMSprop converges to a slightly higher value ($\approx 0.503$) because the EMA in $v_k$ is non-zero when the iterates stagnate near the noise floor, keeping the effective step size marginally too large. Polyak \textbf{diverges} (final $f = 2.72$): as $\theta_k \to \theta_{\text{OLS}}$, $\|\nabla J(\theta_k)\| \to 0$ while $J(\theta_k) - f^{\star}_{\text{specified}} = J(\theta_k) - 0 \to 0.4826 \neq 0$, driving $\alpha_k = 0.4826/\|\nabla J\|^2 \to \infty$.}
\label{fig:q1_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_B.pdf}
\caption{Q1: Convergence on Benchmark~B (Toy Neural Network). Polyak diverges again ($f = 24.1$) due to $f^{\star}$ misspecification: the true minimum $0.7244$ is much greater than the specified $0$, so $\alpha_k \to \infty$ as the iterate approaches the true minimum. Adagrad, GD, and Heavy Ball all reach $f^{\star} \approx 0.7244$. The moderate anisotropy ($\kappa \leq 10$) does not severely hinder constant-step methods, explaining why GD performs well here. Heavy Ball with $\beta = 0.90$ provides slight acceleration due to momentum accumulation in the consistent $x_2$ direction.}
\label{fig:q1_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_C.pdf}
\caption{Q1: Convergence on Benchmark~C (Rosenbrock). Here $f^{\star} = 0$ is the exact global minimum, so Polyak is well-posed and achieves the best result ($f = 9.4 \times 10^{-3}$): the step $\alpha_k = f(x_k)/\|\nabla f(x_k)\|^2$ naturally decays to zero as $f(x_k) \to 0$. Heavy Ball ($f = 1.40$) outperforms GD ($f = 3.51$) by accumulating momentum along the valley floor. Adagrad ($f = 2.20$) partially compensates for ill-conditioning through per-coordinate scaling. RMSprop ($f = 3.12$) is limited by the very small base rate $\alpha_0 = 0.0035$ needed for stability with its EMA.}
\label{fig:q1_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q1: Final objective values after 120 iterations. \textbf{Bold} indicates best per benchmark.}
\label{tab:q1_final}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & 0.4826 & 0.7244 & 3.5142 \\
Polyak & 2.7189 & 24.0659 & \textbf{0.0094} \\
Adagrad & \textbf{0.4826} & \textbf{0.7244} & 2.2034 \\
RMSprop & 0.5027 & 0.7271 & 3.1204 \\
Heavy Ball & 0.4826 & 0.7244 & 1.4003 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Contour Plots with Optimisation Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q1_contour_B.pdf}
\caption{Q1: Trajectories on Benchmark~B. The elliptical contours reflect the curvature anisotropy ($\lambda_2/\lambda_1 \approx 5$--$10$): contour lines are elongated in the $x_1$ direction (gentler slope) and compressed in $x_2$ (steeper slope). GD and Adagrad follow nearly direct paths toward the minimum. Heavy Ball overshoots slightly due to momentum, then corrects; this overshoot is characteristic and bounded for stable hyperparameter choices. The Polyak trajectory departs rapidly from the feasible region due to the step-size explosion.}
\label{fig:q1_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q1_contour_C.pdf}
\caption{Q1: Trajectories on Benchmark~C (Rosenbrock). The banana-shaped valley $x_2 \approx x_1^2$ is evident. All methods first descend rapidly in the steep direction to reach the valley floor (few iterations), then progress slowly along it toward $(1,1)$. With the correct $f^{\star}=0$, the Polyak step adaptively grows when far from the minimum and shrinks near it, achieving the most valley-floor progress. GD with $\alpha = 0.0012$ barely advances along the valley because each step must be tiny to remain stable in the steep direction.}
\label{fig:q1_contour_C}
\end{figure}

\subsection{Adaptive Step-Size Evolution}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_A.pdf}
\caption{Q1: Step-size evolution on Benchmark~A. The Polyak step grows pathologically: as $\|\nabla J\| \to 0$ near $\theta_{\text{OLS}}$, the denominator $\|\nabla J\|^2 + \varepsilon \to \varepsilon$ while the numerator $J(\theta_k) - 0 \approx 0.4826$ is bounded away from zero, so $\alpha_k \to 0.4826/\varepsilon = 0.4826/10^{-4} = 4826$. Adagrad decays monotonically because $G_k = \sum_{j=0}^k g_j^2$ grows without bound. RMSprop stabilises around a moderate value because the EMA forgets old gradients.}
\label{fig:q1_stepsize_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_B.pdf}
\caption{Q1: Step-size evolution on Benchmark~B. The same pathological growth occurs for Polyak due to misspecified $f^{\star}$. Adagrad's effective step decays rapidly in early iterations when large gradients near $x_0 = (-1,4)$ accumulate, then settles to a small value. RMSprop maintains a moderate, slowly-varying rate due to exponential forgetting of past large gradients.}
\label{fig:q1_stepsize_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_C.pdf}
\caption{Q1: Step-size evolution on Benchmark~C. With correct $f^{\star}=0$, the Polyak step behaves as intended: it decreases smoothly as $f(x_k) \to 0$, providing the well-calibrated step at each iteration. This well-behaved decay is precisely why Polyak achieves the best Rosenbrock convergence of all Q1 methods. Adagrad and RMSprop decay more slowly due to smaller base rates required for stability at $\kappa \approx 2504$.}
\label{fig:q1_stepsize_C}
\end{figure}

\subsection{Comparative Analysis and Key Findings}

\begin{itemize}[nosep]
\item \textbf{Polyak step size} is theoretically optimal (achieving $f(x_k) - f^{\star} = O(1/k)$) when $f^{\star}$ is known exactly. In practice, the requirement of knowing $f^{\star}$ is a severe limitation; misspecification causes catastrophic divergence as shown on Benchmarks A and B.

\item \textbf{Adagrad} provides the most reliable performance across all benchmarks when $f^{\star}$ is unknown, matching the GD noise floor on the well-conditioned benchmarks. Its monotonically decreasing step size is a double-edged sword: it guarantees stability but prevents it from maintaining sufficient step sizes late in training on ill-conditioned problems.

\item \textbf{RMSprop} introduces forgetting ($\beta = 0.9$), which prevents step-size collapse but makes it sensitive to the choice of $\alpha_0$. On Rosenbrock, the required $\alpha_0 = 0.0035$ is so small that little progress is made, whereas a larger $\alpha_0$ would cause instability.

\item \textbf{Heavy Ball} achieves the best constant-step-size performance on the ill-conditioned Rosenbrock (Table~\ref{tab:q1_final}), due to momentum accumulation in the valley-floor direction. Its convergence is theoretically guaranteed for convex quadratics with optimal parameters, but not for non-convex functions.

\item \textbf{Practical implication:} In machine learning where $f^{\star}$ is unknown and the loss landscape is non-convex, Adagrad or RMSprop (or their combination in Adam, Question~2) are more robust choices than Polyak step size.
\end{itemize}

% ============================================================
\section{Question 2: Momentum and Stochastic Methods \hfill [30 marks]}
\label{sec:q2}
% ============================================================

\subsection{Theoretical Background}

Nesterov (1983) proved that for $L$-smooth convex functions, no first-order method can achieve a convergence rate better than $O(1/k^2)$, and his accelerated gradient method attains this lower bound exactly.

\begin{theorem}[Nesterov Optimal Rates, \cite{nesterov1983method}]
\begin{enumerate}[nosep,label=(\roman*)]
\item \textbf{Convex case:} Let $f$ be $L$-smooth and convex with minimiser $x^{\star}$. Nesterov's accelerated gradient method with appropriately scheduled momentum satisfies:
\[
f(x_k) - f^{\star} \leq \frac{2L\|x_0 - x^{\star}\|^2}{(k+1)^2}.
\]
This rate $O(1/k^2)$ is optimal among all first-order algorithms for $L$-smooth convex functions.
\item \textbf{Strongly convex case:} If additionally $f$ is $\mu$-strongly convex, the accelerated method achieves:
\[
f(x_k) - f^{\star} \leq \left(1 - \frac{1}{\sqrt{\kappa}}\right)^k (f(x_0) - f^{\star}),
\]
matching the Heavy Ball rate $O((1-1/\sqrt{\kappa})^k)$. This is a $\sqrt{\kappa}$-factor improvement over GD's $O((1-1/\kappa)^k)$. The following table quantifies the speedup across the three benchmark condition numbers:
\begin{center}
\begin{tabular}{lcccc}
\toprule
Problem & $\kappa$ & GD iters ($\varepsilon=0.01$) & Nesterov iters & Speedup \\
\midrule
Bench.\ A & 1.02 & $O(1.02 \times \ln 100) \approx 5$ & $\approx 5$ & $1\times$ \\
Bench.\ B & $\approx 7$ & $O(7 \times 4.6) \approx 32$ & $O(\sqrt{7} \times 4.6) \approx 12$ & $2.6\times$ \\
Bench.\ C & 2504 & $O(2504 \times 4.6) \approx 11{,}500$ & $O(50 \times 4.6) \approx 230$ & $50\times$ \\
\bottomrule
\end{tabular}
\end{center}
For $\kappa \approx 2504$ (Rosenbrock): the empirical $7\times$ improvement with only 150 iterations is consistent with the asymptotic $50\times$ speedup --- at 150 iterations both methods are still far from the asymptotic regime where the full speedup is realised, confirming the theoretical prediction direction even if not the magnitude.
\end{enumerate}
\end{theorem}

The key idea is the \emph{lookahead}: evaluating the gradient at a predicted future point $\tilde{x}_k = x_k + \beta_k z_k$ rather than at $x_k$ itself. This allows the gradient to ``look ahead'' along the momentum direction and correct for overshoot before it occurs.

\subsection{Algorithm Formulations}

\begin{algorithm}[H]
\caption{Nesterov Momentum / Acceleration \cite{nesterov1983method}}
\label{alg:nesterov}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, max momentum $\beta_{\max}$, iterations $N$
\State $z_0 \gets \mathbf{0}$
\For{$k = 1, 2, \ldots, N$}
    \State $\beta_k \gets \min\!\left(\frac{k-1}{k+2},\ \beta_{\max}\right)$ \Comment{Adaptive momentum schedule}
    \State $\tilde{x}_k \gets x_k + \beta_k\, z_k$ \Comment{Lookahead point}
    \State $z_{k+1} \gets \beta_k\, z_k - \alpha\, \nabla f(\tilde{x}_k)$ \Comment{Update velocity}
    \State $x_{k+1} \gets x_k + z_{k+1}$
\EndFor
\end{algorithmic}
\end{algorithm}

The schedule $\beta_k = \min\!\left((k-1)/(k+2),\ \beta_{\max}\right)$ starts at $\beta_1 = 0$ (pure gradient step) and increases toward $\beta_{\max}$, gradually building momentum as the iterate approaches the minimum. The capped schedule ensures stability: without $\beta_{\max}$, momentum could grow too quickly and cause oscillation on non-convex problems.

Crucially, Nesterov momentum differs from Heavy Ball in the \emph{point of gradient evaluation}:
\begin{center}
\begin{tabular}{ll}
\textbf{Heavy Ball:} & $x_{k+1} = x_k - \alpha\, \nabla f(x_k) + \beta(x_k - x_{k-1})$ \\
\textbf{Nesterov:} & $x_{k+1} = x_k - \alpha\, \nabla f(x_k + \beta_k(x_k - x_{k-1})) + \beta_k(x_k - x_{k-1})$
\end{tabular}
\end{center}
The lookahead gradient makes Nesterov provably optimal for convex functions, whereas Heavy Ball can oscillate on non-quadratic problems. Intuitively, the Heavy Ball update ``commits'' to the momentum direction before computing the gradient, so if momentum is too large, there is no mechanism to correct overshoot within the same step. Nesterov's lookahead evaluates the gradient \emph{after} taking the momentum step, allowing immediate correction. This distinction is critical on non-quadratic functions like Benchmark~B (where $\sin(x_1)$ creates local curvature variation) and Rosenbrock (where the valley curves sharply).

\begin{algorithm}[H]
\caption{Adam Optimiser \cite{kingma2014adam}}
\label{alg:adam}
\begin{algorithmic}[1]
\Require $x_0$, learning rate $\alpha$, moments $\beta_1, \beta_2 \in (0,1)$, tolerance $\varepsilon > 0$, iterations $N$
\State $m_0 \gets \mathbf{0},\ v_0 \gets \mathbf{0}$
\For{$t = 1, 2, \ldots, N$}
    \State $g_t \gets \nabla f(x_t)$
    \State $m_t \gets \beta_1\, m_{t-1} + (1 - \beta_1)\, g_t$ \Comment{Biased first moment (EMA of gradient)}
    \State $v_t \gets \beta_2\, v_{t-1} + (1 - \beta_2)\, g_t \odot g_t$ \Comment{Biased second moment}
    \State $\hat{m}_t \gets m_t / (1 - \beta_1^t)$ \Comment{Bias-corrected first moment}
    \State $\hat{v}_t \gets v_t / (1 - \beta_2^t)$ \Comment{Bias-corrected second moment}
    \State $x_{t+1} \gets x_t - \alpha\, \hat{m}_t / (\sqrt{\hat{v}_t} + \varepsilon)$
\EndFor
\end{algorithmic}
\end{algorithm}

Adam combines momentum (via $m_t$, inheriting from Heavy Ball/Nesterov) with per-coordinate adaptive scaling (via $v_t$, inheriting from RMSprop), making it a synthesis of both approaches. The bias correction terms $1 - \beta_1^t$ and $1 - \beta_2^t$ are critical in early iterations. \textbf{Concrete first-step analysis:} With Benchmark B values $\beta_1=0.80$, $\beta_2=0.999$, $\alpha=0.08$, and $g_1 \approx (-3.460, 20)$ at $x_0=(-1,4)$:
\begin{itemize}[nosep]
\item \textit{Without} bias correction: $m_1 = 0.20\, g_1 = (-0.692, 4.0)$; $v_1 = 0.001\, g_1^2 = (0.012, 0.40)$; effective step $= \alpha m_1/\sqrt{v_1} \approx 0.08\times(-6.32, 6.32)$ --- \textbf{magnitude $\approx 6.32\alpha$, too large}.
\item \textit{With} bias correction: $\hat{m}_1 = g_1$; $\hat{v}_1 = g_1^2$; effective step $= \alpha g_1/|g_1| = 0.08\,\text{sign}(g_1)$ --- \textbf{magnitude exactly $\alpha$, as intended}.
\end{itemize}
The unintuitive direction of the bias: the second moment $v_t$ is biased toward zero more severely than $m_t$ (since $\beta_2 = 0.999 \gg \beta_1 = 0.80$), so $\sqrt{v_1}$ is much too small, making the uncorrected step \emph{too large} ($\times 6.32$), not too small. The bias correction normalises both EMAs to give an effective learning rate of exactly $\alpha$ at every step $t$, regardless of the EMA parameters. The EMA time constants are $\tau_1 = 1/(1-\beta_1)$ and $\tau_2 = 1/(1-\beta_2)$: with typical values $\beta_1 = 0.9$ and $\beta_2 = 0.999$, the first moment ``remembers'' the last $\sim 10$ gradients while the second moment ``remembers'' the last $\sim 1000$, giving the gradient direction a short memory (allowing rapid directional changes) while the curvature estimate is stable over long horizons (avoiding oscillation in the effective learning rate). Note that Adam lacks general convergence guarantees in non-convex settings; the AMSGrad variant \cite{reddi2019convergence} fixes this by using a non-decreasing running maximum $\hat{v}_t = \max(\hat{v}_{t-1}, v_t/({1-\beta_2^t}))$, restoring the diminishing step size property needed for convergence.

\subsection{Hyperparameter Settings}

\begin{table}[H]
\centering
\caption{Q2: Hyperparameter settings. Nesterov and Adam run for 150 iterations; GD baseline uses 150 iterations at the same step sizes as in Q1.}
\label{tab:q2_params}
\begin{tabular}{llccc}
\toprule
\textbf{Method} & \textbf{Parameter} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & $\alpha$ & 0.08 & 0.06 & 0.0012 \\
\midrule
Nesterov & $\alpha$ & 0.06 & 0.035 & 0.0007 \\
 & $\beta_{\max}$ & 0.90 & 0.92 & 0.90 \\
\midrule
Adam & $\alpha$ & 0.12 & 0.08 & 0.006 \\
 & $\beta_1$ & 0.82 & 0.80 & 0.80 \\
 & $\beta_2$ & 0.999 & 0.999 & 0.999 \\
 & $\varepsilon$ & $10^{-8}$ & $10^{-8}$ & $10^{-8}$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Convergence Comparison}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_A.pdf}
\caption{Q2: Convergence on Benchmark~A. All three methods converge to $J^{\star} \approx 0.4826$. On this well-conditioned problem ($\kappa \approx 1.02$), the advantage of Nesterov acceleration is minimal since GD already converges near-optimally. Adam's first-moment bias correction has negligible effect here because gradients are large throughout most of the trajectory.}
\label{fig:q2_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_B.pdf}
\caption{Q2: Convergence on Benchmark~B. Both Nesterov and Adam converge to $f^{\star} \approx 0.7244$ faster than GD. Adam's per-coordinate scaling handles the anisotropic curvature ($\kappa \in [3.3, 10]$) by assigning a larger effective step to the $x_1$ coordinate (smaller gradient history) and smaller step to $x_2$ (larger gradient history), effectively pre-conditioning the system. Nesterov's lookahead prevents overshoot across the narrow $x_2$ direction.}
\label{fig:q2_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_C.pdf}
\caption{Q2: Convergence on Benchmark~C (Rosenbrock). The most significant differences emerge here. Nesterov achieves $f = 0.479$ --- a $7\times$ improvement over GD's $f = 3.388$ in the same 150 iterations --- by accumulating momentum along the consistent valley-floor direction. Adam reaches $f = 1.578$; while per-coordinate scaling helps, the rapid Hessian variation along the Rosenbrock valley limits the accuracy of Adam's second-moment estimate as a curvature proxy.}
\label{fig:q2_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q2: Final objective values after 150 iterations. \textbf{Bold} indicates best per benchmark.}
\label{tab:q2_final}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & 0.4826 & 0.7244 & 3.3880 \\
Nesterov & \textbf{0.4826} & \textbf{0.7244} & \textbf{0.4788} \\
Adam & 0.4826 & 0.7244 & 1.5785 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Contour Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q2_contour_B.pdf}
\caption{Q2: Trajectories on Benchmark~B. Nesterov's trajectory shows a smooth, direct arc toward the minimum, with the lookahead mechanism preventing the oscillation that would arise from heavy momentum on the narrow $x_2$ direction. Adam takes more direct, nearly-straight steps by independently normalising each coordinate's gradient.}
\label{fig:q2_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q2_contour_C.pdf}
\caption{Q2: Trajectories on Benchmark~C. All methods descend quickly to the valley floor. Nesterov's momentum accumulates effectively along the valley's gentle floor, producing the fastest advance toward $(1,1)$. Adam's steps are individually scaled but the extremely high curvature ratio ($\kappa \approx 2504$) still constrains progress.}
\label{fig:q2_contour_C}
\end{figure}

\subsection{Mini-Batch Stochastic Gradient Descent}
\label{sec:q2_sgd}

Mini-batch SGD on Benchmark A uses a fixed learning rate $\alpha = 0.06$, batch sizes $b \in \{5, 40\}$, 50 epochs with data reshuffled each epoch (Algorithm~\ref{alg:sgd}).

\begin{algorithm}[H]
\caption{Mini-Batch SGD}
\label{alg:sgd}
\begin{algorithmic}[1]
\Require Data $(X, y)$, initial $\theta_0$, learning rate $\alpha$, batch size $b$, epochs $E$
\For{epoch $e = 1, 2, \ldots, E$}
    \State Randomly permute all $m$ data indices: $\pi \gets \text{Shuffle}(\{1,\ldots,m\})$
    \For{each mini-batch $\mathcal{B} = \{\pi_{ib+1}, \ldots, \pi_{(i+1)b}\}$ for $i = 0, 1, \ldots$}
        \State $\hat{g} \gets \dfrac{1}{b} X_{\mathcal{B}}^T(X_{\mathcal{B}}\theta - y_{\mathcal{B}})$ \Comment{Stochastic gradient}
        \State $\theta \gets \theta - \alpha\, \hat{g}$
    \EndFor
\EndFor
\end{algorithmic}
\end{algorithm}

The stochastic gradient $\hat{g}$ is an unbiased estimate of $\nabla J(\theta)$ ($\mathbb{E}[\hat{g}] = \nabla J$) with variance $\text{Var}(\hat{g}) = \sigma^2_g / b$, where $\sigma^2_g$ is the per-sample gradient variance and $b$ is the batch size. With $b = 5$: each epoch performs $m/b = 200$ updates; with $b = 40$: only 25 updates per epoch. For $\mu$-strongly convex $J$ with SGD run at fixed step size $\alpha$, the convergence floor satisfies:
\begin{equation}
\lim_{k\to\infty}\mathbb{E}[J(\theta_k) - J^{\star}] = \frac{\alpha\, \sigma^2_g}{2\mu\, b},
\label{eq:sgd_floor}
\end{equation}
so the noise floor is proportional to $\alpha/b$. To halve the floor, one must either halve $\alpha$ (slowing convergence by factor $\approx \sqrt{2}$) or double $b$ (doubling the per-update cost). For Benchmark A, $\mu \approx 0.99$, $\alpha = 0.06$: with $b=5$ the floor is $\approx 0.006\sigma^2_g/0.99 \approx 0.006\sigma^2_g$; with $b=40$ it is $\approx 0.00075\sigma^2_g$, a factor-of-8 reduction. The trade-off is: smaller batches make more frequent updates (faster convergence per epoch) but each update is noisier (higher floor around the minimum).

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_sgd_batch.pdf}
\caption{Q2: Mini-batch SGD with different batch sizes on Benchmark~A. The smaller batch ($b=5$) converges faster per epoch due to $4\times$ more parameter updates, but exhibits sustained oscillation near the noise floor because each mini-batch gradient deviates significantly from the true gradient. The larger batch ($b=40$) converges more smoothly but achieves comparable accuracy slower in epoch-count terms. In wall-clock-time terms, smaller batches are typically faster until the irreducible stochastic noise floor is reached.}
\label{fig:q2_sgd_batch}
\end{figure}

\subsection{Effect of Increased Observation Noise}
\label{sec:q2_noisy_sgd}

The experiment is repeated with output noise scaled by 6 ($\sigma = 6$): $y_{\text{noisy}} = X\theta^{\star} + 6\varepsilon$.

\textbf{SGD and non-convex optimisation.} An often-cited practical advantage of SGD is that gradient noise can help escape sharp local minima and saddle points, which deterministic GD would stagnate at (since GD has no mechanism to escape a point where $\nabla f = 0$ or the gradient is very small). Near a saddle point $x_s$ with $\nabla f(x_s) \approx 0$ and $H(x_s)$ indefinite, the SGD stochastic gradient $\hat{g} = \nabla f_{i_k}(x_s)$ has $\mathbb{E}[\hat{g}] \approx 0$ but substantial variance; the noise perturbations allow the iterate to drift off the saddle. This property is particularly important in deep learning where the loss landscape has many saddle points; see \cite{dauphin2014identifying} for empirical evidence. In our Benchmark A (convex quadratic), there are no saddle points, so this advantage does not apply, but it motivates the use of SGD in neural network training even beyond pure noise-floor analysis.

\textbf{Irreducible noise floor analysis.} For the MSE loss with observation noise $\varepsilon \sim \mathcal{N}(0, \sigma^2)$, the best achievable expected loss (over all estimators) is:
\begin{equation}
J^{\star} = \frac{\sigma^2}{2},
\label{eq:noise_floor}
\end{equation}
because $\mathbb{E}[J(\hat\theta)] = \frac{1}{2}\mathbb{E}\|\varepsilon\|^2/m = \frac{\sigma^2}{2}$ at $\hat\theta = \theta^{\star}$. Scaling $\sigma$ by 6 multiplies $J^{\star}$ by $6^2 = 36$: from $\approx 0.5$ to $\approx 18$. Since mini-batch SGD converges to within $O(\alpha\sigma^2_g/b)$ of $J^{\star}$ (where $\sigma^2_g$ is per-sample gradient variance), and $\sigma^2_g \propto \sigma^2$, the convergence floor for both batch sizes rises proportionally to $\sigma^2$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q2_sgd_noisy.pdf}
\caption{Q2: SGD convergence under original ($\sigma=1$, left) and high ($\sigma=6$, right) observation noise. Scaling $\sigma$ by 6 raises the irreducible loss floor by factor $\sigma^2 = 36$ (since $J^{\star} = \sigma^2/2$): the floor moves from $\approx 0.5$ to $\approx 18$. Under high noise, the gradient variance dominates the mini-batch sampling variance, so both batch sizes ($b=5$ and $b=40$) converge to approximately the same elevated floor. The batch-size advantage seen in the left panel ($b=5$ converges faster per epoch) is washed out by the observation noise: reducing mini-batch noise through larger $b$ brings diminishing returns when the observational noise dominates.}
\label{fig:q2_sgd_noisy}
\end{figure}

% ============================================================
\section{Question 3: Newton's Method and Local Approximation \hfill [30 marks]}
\label{sec:q3}
% ============================================================

\subsection{Local Approximations of $g(x) = x^4$}

Given $g(x) = x^4$ and expansion point $x_0 = 0.25$: $g(x_0) = 0.25^4 = 3.906 \times 10^{-3}$, $g'(x_0) = 4 \times 0.25^3 = 0.0625$, $g''(x_0) = 12 \times 0.25^2 = 0.75$.

\textbf{First-order approximation:}
\begin{equation}
\hat{g}_1(x) = g(x_0) + g'(x_0)(x - x_0) = 3.906 \times 10^{-3} + 0.0625(x - 0.25).
\end{equation}

\textbf{Second-order approximation:}
\begin{equation}
\hat{g}_2(x) = g(x_0) + g'(x_0)(x - x_0) + \tfrac{1}{2}g''(x_0)(x - x_0)^2 = 3.906 \times 10^{-3} + 0.0625(x - 0.25) + 0.375(x - 0.25)^2.
\end{equation}

The Newton step minimises $\hat{g}_2$: setting $\hat{g}_2'(x) = 0$ gives $x_{k+1} = x_k - g'(x_k)/g''(x_k)$. This is the fundamental motivation for Newton's method: each iteration moves to the minimiser of the local quadratic model.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_approximation.pdf}
\caption{Q3: Local approximations of $g(x) = x^4$ at $x_0 = 0.25$. The first-order approximation (tangent line) captures only local slope and diverges rapidly from $g(x)$ away from $x_0$. The second-order approximation (parabola) captures curvature and fits well in a neighbourhood of $x_0$. The minimum of the parabola at $x_{\min} = x_0 - g'(x_0)/g''(x_0) = 0.25 - 0.0625/0.75 \approx 0.167$ is the Newton update, providing a much better estimate of the true minimum (at $x^{\star} = 0$) than the gradient-step would.}
\label{fig:q3_approx}
\end{figure}

\subsection{Newton's Method with Hessian Regularisation}

\begin{algorithm}[H]
\caption{Newton's Method with Damping}
\label{alg:newton}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha \in (0, 1]$, damping $\lambda > 0$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $H_k \gets \nabla^2 f(x_k) + \lambda I$ \Comment{Regularised Hessian (guarantees PD)}
    \State Solve $H_k\, p_k = g_k$ for $p_k$ \Comment{Newton direction: $p_k = H_k^{-1} g_k$}
    \State $x_{k+1} \gets x_k - \alpha\, p_k$ \Comment{$\alpha = 1$: pure Newton; $\alpha < 1$: damped Newton}
\EndFor
\end{algorithmic}
\end{algorithm}

The damping term $\lambda I$ with $\lambda = 10^{-8}$ ensures positive definiteness of $H_k$ when the Hessian has near-zero eigenvalues (as occurs on Rosenbrock away from the optimum). The damped Newton step ($\alpha < 1$) trades the quadratic convergence rate for stability when the quadratic model is inaccurate far from the optimum.

\textbf{Linear solve via Cholesky factorisation.} For the regularised (positive definite) Hessian $H_k + \lambda I$, the Newton direction $p_k = (H_k+\lambda I)^{-1}g_k$ is computed by Cholesky factorisation: $(H_k + \lambda I) = LL^T$ with $L$ lower triangular, then solving two triangular systems $Lw = g_k$ and $L^T p_k = w$. This costs $O(d^3/3)$ flops for the factorisation and $O(d^2)$ for the two solves. For $d=2$ this is trivial; for large $n$ (e.g., deep learning with $d \sim 10^8$), this $O(d^3)$ cost is prohibitive, motivating quasi-Newton and conjugate gradient approaches. In our $d=2$ setting, \texttt{np.linalg.solve} performs an LU factorisation equivalent to Cholesky in $O(d^3) = O(8)$ flops.

\textbf{Saddle-point issue.} On non-convex functions, the Newton direction $p_k = H_k^{-1}g_k$ is guaranteed to be a descent direction only when $H_k \succ 0$ (positive definite). At a saddle point $x_s$ where $\nabla f(x_s) = 0$ but $H(x_s)$ has negative eigenvalues, the unregularised Newton step $H^{-1}g_k$ is undefined (or zero), while the Hessian regularisation $H_k + \lambda I$ with $\lambda > |\lambda_{\min}(H_k)|$ makes the system positive definite but may direct the step towards a saddle-adjacent region rather than the minimum. A principled alternative: when a negative eigenvalue $\lambda_{\min}(H_k) < 0$ is detected, move in the direction of the corresponding eigenvector (the curvature ``escape'' direction) using a safeguarded step. The current implementation's $\lambda = 10^{-8}$ damping is sufficient for near-PD Hessians (as in Benchmarks A and B) but the effective regularisation on Rosenbrock may be insufficient at certain trajectory points where $\lambda_{\min}(H) \approx 0.4$ and $\lambda = 10^{-8} \ll 0.4$; there, $H$ is already PD and the damping is essentially inactive.

\begin{theorem}[Newton Convergence Phases, \cite{nocedal2006numerical}]
Let $f$ be twice continuously differentiable with $L_H$-Lipschitz Hessian and $\nabla^2 f(x^{\star}) \succ 0$. Newton's method exhibits two convergence phases:
\begin{enumerate}[nosep,label=(\roman*)]
\item \textbf{Damped (global) phase}: Far from $x^{\star}$, each backtracking line search reduces $f$ by at least a constant $\gamma > 0$: $f(x_k) - f(x_{k+1}) \geq \gamma$, giving linear convergence to a neighbourhood of $x^{\star}$.
\item \textbf{Quadratic (local) phase}: For $x_0$ sufficiently close to $x^{\star}$ (specifically $\|x_0 - x^{\star}\| \leq \mu_{\min}(\nabla^2 f(x^{\star}))/L_H$), the full Newton step ($\alpha=1$) satisfies:
\[
\|x_{k+1} - x^{\star}\| \leq \frac{L_H}{2\mu_{\min}(\nabla^2 f(x^{\star}))}\, \|x_k - x^{\star}\|^2.
\]
\end{enumerate}
This \emph{quadratic convergence} means the number of correct decimal places doubles at every iteration. The transition between phases is monitored by the Newton decrement $\lambda_N(x_k)$: the quadratic phase begins when $\lambda_N(x_k)$ falls below a problem-dependent threshold (typically $\lambda_N^2/2 < \delta$ for $\delta$ depending on the Hessian Lipschitz constant), after which the convergence rate becomes superlinear \cite{boyd2004convex}.
\end{theorem}

\begin{proposition}[Exact Convergence on Quadratics]
For a strictly convex quadratic $f(\theta) = \frac{1}{2}\theta^T H \theta - b^T \theta$, Newton's method with $\alpha = 1$ converges in \emph{exactly one iteration}: $\theta_1 = \theta_0 - H^{-1}(H\theta_0 - b) = H^{-1}b = \theta^{\star}$.
\end{proposition}

This exact convergence in one step is observed on Benchmark A (Figure~\ref{fig:q3_fval_A}).

\textbf{Gauss--Newton approximation.} For least-squares problems $f(x) = \frac{1}{2}\|r(x)\|^2$ where $r : \mathbb{R}^d \to \mathbb{R}^m$ is a residual vector, the Hessian is $H = J^T J + \sum_i r_i(x) H_i$ (where $J$ is the Jacobian of $r$ and $H_i = \nabla^2 r_i$). The Gauss--Newton approximation drops the second-order term: $H_{GN} = J^T J \succeq 0$, which is always positive semidefinite and avoids computing the second-order correction. The Newton update $H_{GN}^{-1}\nabla f = (J^T J)^{-1} J^T r$ is the Gauss--Newton step, equivalent to the least-squares normal equations update. For Benchmark A, $r(\theta) = (X\theta - y)/\sqrt{m}$ is linear in $\theta$, so $H_i = 0$ and the Gauss--Newton Hessian equals the true Hessian: $J^T J = X^T X/m = H$. This confirms that Benchmark A's one-step Newton convergence also holds for the Gauss--Newton approximation. For non-linear least squares (e.g., neural network training with squared loss), the Gauss--Newton approximation avoids the expensive full Hessian while still providing faster convergence than GD.

\textbf{Affine invariance.} A fundamental property distinguishing Newton's method from gradient descent is \emph{affine invariance}: if $\tilde{f}(y) = f(Ay)$ for any invertible $A$, then Newton's method on $\tilde{f}$ produces iterates $\tilde{x}_k = A^{-1}x_k$ that are the exact linear image of the iterates on $f$. This is because the Newton direction $H_{\tilde{f}}^{-1}\nabla\tilde{f} = (A^T H_f A)^{-1} A^T g_f = A^{-1} H_f^{-1} g_f$, so the update is scale-invariant. In contrast, GD's direction $-\alpha g$ has magnitude $\alpha\|\nabla f\|$ that scales with the coordinate system, so GD performance degrades under poor scaling (large $\kappa$) while Newton is unaffected. On Benchmark C, this is illustrated by the fact that Newton with $\alpha=0.22$ still makes substantial progress despite $\kappa \approx 2504$, whereas GD with $\alpha=0.001$ barely moves.

\textbf{Newton decrement.} A key quantity for monitoring Newton's progress is the \emph{Newton decrement}:
\begin{equation}
\lambda_N(x_k) = \sqrt{g_k^T H_k^{-1} g_k} = \|p_k\|_{H_k},
\label{eq:newton_decrement}
\end{equation}
which satisfies $f(x_k) - f^{\star} \leq \frac{\lambda_N(x_k)^2}{2}$ for strongly convex functions. The Newton decrement is therefore a computable, scale-invariant upper bound on the suboptimality, providing a principled stopping criterion: terminate when $\lambda_N(x_k) \leq \varepsilon$ (which guarantees $f(x_k) - f^{\star} \leq \varepsilon^2/2$). It is also affine-invariant: $\lambda_N$ does not change if $f$ is reparametrised by a linear transformation, unlike $\|\nabla f\|$ which scales with the problem.

\textbf{Worked example (Benchmark B at convergence).} At $x^{\star} \approx (0.5828, 2.0)$ for Benchmark B, $g = \nabla f(x^{\star}) \approx (0, 0)$ and $H = \text{diag}(2-\sin(0.5828), 10) \approx \text{diag}(1.453, 10)$. Starting from $x_0 = (-1, 4)$:
$g_0 \approx (2(-1-1)+\cos(-1),\; 10(4-2)) = (-3.460,\; 20)$, $H_0 \approx \text{diag}(2-\sin(-1), 10) = \text{diag}(2.841,\; 10)$.
Newton direction: $p_0 = H_0^{-1}g_0 \approx (-3.460/2.841,\; 20/10) = (-1.218,\; 2.0)$.
Newton decrement: $\lambda_N^2 = g_0^T p_0 = (-3.460)(-1.218) + 20 \times 2 = 4.214 + 40 = 44.21$, so $\lambda_N = 6.65$.
The upper bound $\lambda_N^2/2 = 22.1$ on $f(x_0) - f^{\star}$. At $x_0$: $f(-1,4) = 4 + 20 + \sin(-1) = 4 + 20 - 0.841 = 23.16 - 0.7244 \approx 22.4$, matching the bound.
After one damped Newton step ($\alpha = 0.85$): $x_1 = x_0 - 0.85 p_0 \approx (-1 + 1.035,\; 4 - 1.7) = (0.035,\; 2.3)$. By the second step, $\lambda_N \ll 1$, triggering the stopping criterion.

The update norm shown in Figure~\ref{fig:q3_update} is $\alpha\|p_k\| = \alpha\|H_k^{-1}g_k\|$ (unscaled), which approximates the Newton decrement up to a factor of $\alpha\sqrt{\lambda_{\min}(H_k)}$.

\subsection{Hyperparameter Settings}

\begin{table}[H]
\centering
\caption{Q3: Hyperparameters. GD runs for 80 iterations; Newton runs for 20 iterations with damping $\lambda = 10^{-8}$.}
\label{tab:q3_params}
\begin{tabular}{lcccccc}
\toprule
 & \multicolumn{2}{c}{\textbf{Bench.\ A}} & \multicolumn{2}{c}{\textbf{Bench.\ B}} & \multicolumn{2}{c}{\textbf{Bench.\ C}} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5} \cmidrule(lr){6-7}
 & GD & Newton & GD & Newton & GD & Newton \\
\midrule
$\alpha$ & 0.08 & 1.0 & 0.06 & 0.85 & 0.001 & 0.22 \\
Iterations & 80 & 20 & 80 & 20 & 80 & 20 \\
\bottomrule
\end{tabular}
\end{table}

The Newton step sizes reflect the local nonlinearity: $\alpha = 1.0$ for the exactly quadratic Benchmark~A, $\alpha = 0.85$ for the nearly-quadratic Benchmark~B (where $\sin(x_1)$ introduces mild nonlinearity), and $\alpha = 0.22$ for Rosenbrock (where the Hessian varies rapidly along the trajectory, making the quadratic model inaccurate beyond a small neighbourhood).

\textbf{Trust-region methods.} The damped Newton step ($\alpha < 1$) is one strategy for controlling step length; trust-region methods are more principled. A trust-region method solves:
\begin{equation}
p_k = \arg\min_{\|p\| \leq \Delta_k} m_k(p), \quad m_k(p) = f(x_k) + g_k^T p + \tfrac{1}{2}p^T H_k p,
\label{eq:trust_region}
\end{equation}
where $\Delta_k$ is the trust-region radius, updated by comparing the actual reduction $f(x_k) - f(x_k+p_k)$ to the predicted reduction $m_k(0) - m_k(p_k)$. If the ratio exceeds a threshold (the model was accurate), $\Delta_k$ is increased; otherwise it is decreased. The Levenberg--Marquardt method corresponds to a trust-region approach with a quadratic penalty on $\|p\|$, equivalent to solving $(H_k + \mu_k I)p = -g_k$ with $\mu_k$ chosen so that $\|p_k\| = \Delta_k$. Trust-region methods provide global convergence guarantees without requiring the step size to be manually tuned, at the cost of solving the constrained quadratic subproblem (which can itself be solved iteratively via Lanczos or the Moré--Sorensen algorithm).

\subsection{Convergence Comparison}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_A.pdf}
\caption{Q3: Newton vs GD on Benchmark~A. Newton converges to $J^{\star} = 0.4826$ in \textbf{one iteration}, as predicted by Proposition~4.2: $\theta_1 = \theta_0 - H^{-1}\nabla J(\theta_0) = H^{-1}b = \theta_{\text{OLS}}$, the exact OLS solution. GD requires 80 iterations to achieve comparable accuracy. This single-step convergence occurs because the loss is exactly quadratic and the Hessian is constant.}
\label{fig:q3_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_B.pdf}
\caption{Q3: Newton vs GD on Benchmark~B. Newton converges in approximately 5 iterations to $f^{\star} \approx 0.7244$, exploiting the near-quadratic structure (the $\sin(x_1)$ term is small relative to the quadratic terms for $x_1$ near 0.58). The Hessian $H(x) = \text{diag}(2 - \sin(x_1), 10)$ varies slowly along the trajectory, so each Newton step is highly accurate.}
\label{fig:q3_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_C.pdf}
\caption{Q3: Newton vs GD on Benchmark~C. Newton with $\alpha = 0.22$ achieves $f = 0.686$ in 20 iterations versus GD's $f = 3.732$ in 80 iterations. The damped step is necessary because the Rosenbrock Hessian changes rapidly: near $x_0 = (-1,1)$, $H$ has condition number $\approx 2504$, but the Newton direction $H^{-1}g$ changes significantly over even a small step. Despite damping reducing the rate from quadratic to linear, Newton still makes dramatically more progress per iteration than GD by accounting for the anisotropic curvature.}
\label{fig:q3_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q3: Final objective values and approximate converged positions. GD runs 80 iterations; Newton runs 20 iterations with $\lambda = 10^{-8}$ damping.}
\label{tab:q3_final}
\begin{tabular}{lccccc}
\toprule
\textbf{Bench.} & \textbf{GD $f$ (80 it.)} & \textbf{Newton $f$ (20 it.)} & \textbf{Newton iter.\ to converge} & \textbf{Gain factor} & \textbf{Newton final $x$} \\
\midrule
A & 0.4827 & 0.4826 & 1 & $80\times$ fewer & $(3.028,\ 4.017)$ \\
B & 0.7244 & 0.7244 & $\approx 5$ & $16\times$ fewer & $(0.582,\ 2.000)$ \\
C & 3.7319 & 0.6864 & 20 (damped) & $5.4\times$ lower $f$ & $(0.183,\ 0.020)$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Contour Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q3_contour_B.pdf}
\caption{Q3: Newton vs GD trajectories on Benchmark~B. Newton takes large, curvature-informed steps that simultaneously correct both coordinates, reaching the minimum in $\sim 5$ steps. GD's small constant step size and the anisotropic curvature ($\lambda_2/\lambda_1 \approx 5$--$10$) cause it to take many steps with the $x_2$ component converging first (steeper direction).}
\label{fig:q3_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q3_contour_C.pdf}
\caption{Q3: Newton vs GD trajectories on Rosenbrock. Newton's trajectory follows the valley floor by using the Hessian to identify that the long axis of the quadratic model points along the valley. GD must use $\alpha = 0.001$ (far below the threshold $2/L \approx 0.002$) and barely advances in 80 steps. Newton reaches $(0.183, 0.020)$ from $(-1, 1)$ in 20 iterations, clearly traversing the valley floor.}
\label{fig:q3_contour_C}
\end{figure}

\subsection{Update Magnitude Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q3_update_magnitude.pdf}
\caption{Q3: Update magnitude $\|x_{k+1} - x_k\|$ vs.\ iteration across all three benchmarks. Newton's update magnitude exhibits the expected rapid decrease: on Benchmark~A it drops to near-zero after 1 step (quadratic convergence in finite time for exactly quadratic functions). On Benchmarks B and C, the quadratic convergence phase begins once Newton enters the region of attraction, characterised by the step shrinking by a square of the previous step. GD's updates decrease slowly and linearly throughout, reflecting the linear convergence rate $(1-\mu/L)^k$.}
\label{fig:q3_update}
\end{figure}

% ============================================================
\section{Question 4: Derivative Approximation and Derivative-Free Optimisation \hfill [30 marks]}
\label{sec:q4}
% ============================================================

\subsection{Part A: Finite Difference and Nesterov Random Search (Benchmark B)}

\subsubsection{Finite Difference Gradient Approximation: Error Analysis}

The forward finite-difference approximation of partial derivatives is:
\begin{equation}
\frac{\partial f}{\partial x_i}(x) \approx \frac{f(x + \delta e_i) - f(x)}{\delta}.
\label{eq:fd}
\end{equation}
By Taylor expansion: $f(x + \delta e_i) = f(x) + \delta \frac{\partial f}{\partial x_i}(x) + \frac{\delta^2}{2}\frac{\partial^2 f}{\partial x_i^2}(x) + O(\delta^3)$, so the approximation error is:
\begin{equation}
\left|\frac{f(x+\delta e_i) - f(x)}{\delta} - \frac{\partial f}{\partial x_i}(x)\right| \leq \frac{L_H \delta}{2} + \frac{\epsilon_{\text{mach}}}{\delta},
\label{eq:fd_error}
\end{equation}
where $L_H$ bounds the second derivative and $\epsilon_{\text{mach}} \approx 10^{-16}$ is machine epsilon. The first term (truncation error) grows with $\delta$; the second term (floating-point error) grows as $\delta \to 0$. The optimal $\delta$ minimises the total error:
\begin{equation}
\delta^{\star} = \sqrt{\frac{2\epsilon_{\text{mach}}}{L_H}} \approx \sqrt{2 \times 10^{-16}} \approx 10^{-8}.
\end{equation}
In the experiments, $\delta = 0.05$ (``good'') is close to the regime where truncation error dominates but is small enough for accurate approximation on Benchmark B. $\delta = 0.8$ (``poor'') is far too large: the truncation error $L_H \times 0.8/2 \approx 0.4 L_H$ dominates and the approximated gradient points in a significantly wrong direction.

\textbf{Central finite differences.} An alternative with lower truncation error is:
\begin{equation}
\frac{\partial f}{\partial x_i}(x) \approx \frac{f(x + \delta e_i) - f(x - \delta e_i)}{2\delta}.
\label{eq:cfd}
\end{equation}
By Taylor expansion the odd terms cancel, giving $O(\delta^2)$ truncation error (vs.\ $O(\delta)$ for forward differences):
\begin{equation}
\left|\frac{f(x+\delta e_i) - f(x-\delta e_i)}{2\delta} - \frac{\partial f}{\partial x_i}(x)\right| \leq \frac{L_3 \delta^2}{6} + \frac{\epsilon_{\text{mach}}}{\delta},
\label{eq:cfd_error}
\end{equation}
where $L_3$ bounds the third derivative. The optimal central-difference step is $\delta^{\star}_c = (3\epsilon_{\text{mach}}/L_3)^{1/3} \approx 10^{-5}$, achieving error $O(\epsilon_{\text{mach}}^{2/3})$ versus $O(\epsilon_{\text{mach}}^{1/2})$ for forward differences. The cost of this improvement is $2d$ function evaluations per gradient (versus $d+1$ for forward differences), a factor-of-two increase in evaluation cost:
\begin{center}
\begin{tabular}{lccc}
\toprule
Scheme & Trunc.\ error & FE per gradient & Optimal $\delta^{\star}$ \\
\midrule
Forward FD & $O(\delta)$ & $d+1$ & $\approx 10^{-8}$ \\
Central FD & $O(\delta^2)$ & $2d$ & $\approx 10^{-5}$ \\
\bottomrule
\end{tabular}
\end{center}
For problems where gradient accuracy matters more than oracle budget (e.g., Benchmark C with $\kappa \approx 2504$ where gradient direction quality is critical), central differences are preferred. For high-dimensional problems ($d \gg 1$) where function evaluations are expensive, the $2d$ vs.\ $d+1$ cost difference is significant. \textbf{Automatic differentiation (AD)} provides an exact alternative: AD computes the exact gradient at $O(c \cdot d)$ cost (where $c$ is the cost of one forward pass, typically $c \approx 5$) with \emph{no} discretisation error, by applying the chain rule symbolically through the computation graph in reverse mode (backpropagation). AD is the standard approach in modern deep learning frameworks (PyTorch, JAX, TensorFlow) and eliminates the need to choose $\delta$. The relative merits are:
\begin{itemize}[nosep]
\item \textbf{FD:} Requires only function evaluation (black-box oracle); robust when the function is only available as a black box or simulator.
\item \textbf{AD:} Requires differentiable code but provides exact gradients at $O(d)$ cost with no $\delta$-tuning; preferred when the function is implemented as explicit code.
\item \textbf{NRS:} Requires only black-box evaluation; dimension-independent cost $O(1)$ per step at the expense of high gradient variance.
\end{itemize}

\subsubsection{Nesterov Random Search}

\begin{algorithm}[H]
\caption{Nesterov Random Search (Zeroth-Order) \cite{nesterov2017random}}
\label{alg:nrs}
\begin{algorithmic}[1]
\Require $x_0$, step size $\alpha$, perturbation $\delta > 0$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State Sample $u_k \sim \mathcal{N}(0, I)$, normalise: $u_k \gets u_k / \|u_k\|$ (uniform on unit sphere)
    \State Compute stochastic directional derivative: $\hat{d}_k \gets \dfrac{f(x_k + \delta u_k) - f(x_k)}{\delta}\, u_k$
    \State $x_{k+1} \gets x_k - \alpha\, \hat{d}_k$
\EndFor
\end{algorithmic}
\end{algorithm}

This zeroth-order method requires only 2 function evaluations per iteration regardless of dimension $d$, in contrast to FD which requires $d+1$ evaluations. The estimator satisfies $\mathbb{E}[\hat{d}_k] = \nabla f_\delta(x_k)$ where $f_\delta$ is the Gaussian-smoothed function; for small $\delta$, $f_\delta \approx f$. The main limitation is high variance: a single random direction is sampled per step, requiring many more iterations than FD or exact GD.

\textbf{Connection to directional derivatives and AD.} The NRS estimator $(f(x+\delta u) - f(x))/\delta \cdot u$ is a scalar multiple of $u$, estimating the directional derivative $\nabla f(x)^T u \cdot u/\|u\|^2 = u$. The exact directional derivative $\nabla f(x)^T v$ for a unit vector $v$ can be computed without full gradient knowledge using only two function evaluations (same cost as NRS), or computed exactly via forward-mode automatic differentiation (one forward pass through the computation graph with dual numbers, evaluating $f$ and $\nabla f^T v$ simultaneously). This forward-mode AD directional derivative at cost $O(d)$ per direction forms the basis of randomised algorithms like NRS but without discretisation error.

\subsubsection{Results on Benchmark B}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q4_fval_B.pdf}
\caption{Q4A: Convergence on Benchmark~B. Exact GD converges fastest, FD GD with $\delta = 0.05$ nearly matches it (gradient approximation error $\leq \delta L_H / 2 \approx 0.05 \times 10/2 = 0.25$ per coordinate, small relative to gradient magnitude). FD GD with $\delta = 0.8$ converges slowly and to a higher value: the truncation error $\approx 0.8 \times 10/2 = 4$ per coordinate exceeds the gradient magnitude near the minimum, so steps point in the wrong direction. Nesterov random search ($\alpha = 0.025$, $\delta = 0.08$, 220 iterations) makes steady but slow progress, consistent with the $O(d/\sqrt{k})$ convergence rate of zeroth-order methods in $d=2$ dimensions \cite{nesterov2017random}.}
\label{fig:q4_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q4_contour_B.pdf}
\caption{Q4A: Trajectories on Benchmark~B. Exact GD and good FD ($\delta=0.05$) follow nearly identical paths, confirming gradient approximation fidelity. Poor FD ($\delta=0.8$) takes a deviated path due to inaccurate gradient direction. Nesterov random search follows a noisy, wandering path reflecting the stochastic directional sampling --- yet the expected direction is correct, so the centroid of many random steps points toward the minimum.}
\label{fig:q4_contour_B}
\end{figure}

\subsection{Part B: Derivative-Free Methods (Benchmark C)}

\subsubsection{Nelder--Mead Simplex Method}

The Nelder--Mead algorithm maintains a simplex of $n+1 = 3$ vertices in $\mathbb{R}^2$, iteratively transforming it through four operations:
\begin{enumerate}[nosep]
\item \textbf{Reflection} ($\alpha_r = 1$): Move the worst vertex through the centroid of the remaining vertices.
\item \textbf{Expansion} ($\gamma = 2$): If reflection improved substantially, try moving further in the same direction.
\item \textbf{Contraction} ($\rho = 0.5$): If reflection did not improve, contract toward the centroid.
\item \textbf{Shrink} ($\sigma = 0.5$): If all else fails, contract the entire simplex toward the best vertex.
\end{enumerate}
No gradient information is required. The simplex adapts its shape to the local landscape: elongating along promising directions and contracting when near the minimum. Settings: initial step $= 0.35$, iterations $= 160$.

\textbf{Dimensionality limitation.} In $d$ dimensions, the simplex has $d+1$ vertices. In high dimensions, the ratio of the simplex's ``volume'' to the search space grows exponentially small, making meaningful exploration infeasible. Moreover, convergence proofs exist only for $d=1$ (where Nelder--Mead reduces to golden section search); for $d \geq 2$, the method can stagnate on degenerate simplices or cycle without convergence \cite{nelder1965simplex}. This contrasts with FD gradient descent and Nesterov random search, which both retain meaningful convergence guarantees in arbitrary $d$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q4_nelder_mead_C.pdf}
\caption{Q4B: Nelder--Mead on Rosenbrock. Left: the simplex centroid trajectory navigates the curved valley without any gradient information. The algorithm first descends to the valley floor, then slowly progresses along it as the simplex adapts its shape to the narrow, elongated valley. Right: convergence is non-monotone (visible steps up in the log-scale plot) when expansion or reflection temporarily worsens the best value, but the overall trend is downward. Nelder--Mead reaches lower objective values than GD with the same number of function evaluations, though it is outperformed by gradient-based methods overall.}
\label{fig:q4_nm}
\end{figure}

\subsubsection{Grid Search}

Grid search evaluates $f$ on a uniform $55 \times 55 = 3025$-point grid over $x_1 \in [-2, 2]$, $x_2 \in [-1, 3]$, giving resolution $\Delta x_1 = 4/54 \approx 0.074$ and $\Delta x_2 = 4/54 \approx 0.074$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q4_grid_search_C.pdf}
\caption{Q4B: Grid search on Rosenbrock. Left: all 3025 points are evaluated, covering the search domain uniformly. The closest grid point to the true optimum $(1,1)$ is found at roughly $(0.96, 0.93)$ (within one grid spacing $\approx 0.074$). Right: the best-so-far value decreases in steps as better grid points are discovered during the row-by-row scan. Grid search is guaranteed to find the global optimum to within $\pm\Delta x$ in each coordinate, but requires $O(N^d)$ evaluations: with the same $N=55$ points per dimension, the evaluation count grows exponentially:
\begin{center}
\begin{tabular}{ccr}
\toprule
$d$ & Evaluations $55^d$ & Comparison \\
\midrule
2 & 3,025 & This experiment (seconds) \\
3 & 166,375 & Seconds on modern CPU \\
5 & $5.0 \times 10^8$ & Hours on cluster \\
10 & $2.5 \times 10^{17}$ & $>$ age of universe \\
20 & $6.3 \times 10^{34}$ & Physically impossible \\
\bottomrule
\end{tabular}
\end{center}
This exponential scaling (``curse of dimensionality'') fundamentally rules out grid search for any $d \geq 5$ problem. In contrast, gradient-based methods need $O(\kappa\log(1/\varepsilon))$ iterations regardless of $d$, and Nesterov random search needs $O(d/\sqrt{k})$ iterations, both polynomial in $d$.}
\label{fig:q4_grid}
\end{figure}

\subsection{Computational Comparison}

\begin{table}[H]
\centering
\caption{Q4: Comparison of derivative-approximation and derivative-free methods. ``FE'' = function evaluations (calls to $f$); ``GE'' = gradient evaluations; $d$ = problem dimension. Exact GD uses one GE (computing all $d$ components jointly); FD requires $d+1$ separate FEs to approximate the gradient.}
\label{tab:q4_comparison}
\begin{tabular}{lccccl}
\toprule
\textbf{Method} & \textbf{Oracle} & \textbf{Cost/iter} & \textbf{Scalability} & \textbf{Accuracy} & \textbf{Global?} \\
\midrule
Exact GD & Exact gradient & 1 GE & $O(d)$ GE & High & Local \\
FD GD (forward, $\delta$ small) & Approx gradient & $d+1$ FE & $O(d)$ FE & High ($O(\delta)$ err) & Local \\
FD GD (central, $\delta$ small) & Approx gradient & $2d$ FE & $O(d)$ FE & Higher ($O(\delta^2)$ err) & Local \\
FD GD ($\delta$ large) & Poor gradient & $d+1$ FE & $O(d)$ FE & Low & Local \\
Nesterov Random & Zero-order & 2 FE & $O(1)$ FE ($d$-free) & Low & Local \\
Nelder--Mead & Zero-order & ${\leq}3$ FE & Low-dim only ($d\leq 5$) & Moderate & Local \\
Grid Search & Zero-order & $N^d$ FE (total) & Exponential in $d$ & Grid-limited & \textbf{Yes} \\
\bottomrule
\end{tabular}
\end{table}

The key finding from Q4 is the \textbf{curse of dimensionality}: grid search and Nelder--Mead become infeasible as $d$ grows, FD approximation maintains $O(d)$ evaluation cost (same as exact GD), while Nesterov random search achieves $O(1)$ evaluations per step independent of $d$. In high-dimensional machine learning (millions of parameters), only methods with $O(1)$ or $O(d)$ evaluation cost per iteration are practical.

% ============================================================
\section{Question 5: Constrained Optimisation \hfill [30 marks]}
\label{sec:q5}
% ============================================================

\subsection{Problem Setup and KKT Analysis}

The constrained problem is:
\begin{equation}
\min_{x \in \mathbb{R}^2}\; f(x) \quad \text{subject to}\quad x_1 \geq 0.5,
\label{eq:constrained_problem}
\end{equation}
where $f$ is Benchmark~B (\ref{eq:bench_B}). The starting point $x_0 = (0.2, 4.0)$ is infeasible. Writing the constraint as $g(x) = 0.5 - x_1 \leq 0$, the KKT necessary conditions for a local minimiser $x^{\star}$ are:
\begin{align}
\nabla f(x^{\star}) - \mu^{\star} e_1 &= 0, \label{eq:kkt_stationarity} \\
\mu^{\star} &\geq 0, \label{eq:kkt_dual_feasibility} \\
0.5 - x_1^{\star} &\leq 0, \label{eq:kkt_primal_feasibility} \\
\mu^{\star}(0.5 - x_1^{\star}) &= 0, \label{eq:kkt_complementary_slackness}
\end{align}
where $e_1 = (1, 0)^T$. Since the unconstrained minimum of $f$ is at $x^{\star}_{\text{unc}} \approx (0.5828, 2.0)$ with $0.5828 > 0.5$, the constraint is \emph{inactive} at the constrained solution. By complementary slackness (\ref{eq:kkt_complementary_slackness}), $\mu^{\star} = 0$, and stationarity (\ref{eq:kkt_stationarity}) reduces to $\nabla f(x^{\star}) = 0$, i.e., the constrained and unconstrained minima coincide. All constrained methods should therefore converge to $(0.5828, 2.0)$ with $f^{\star} = 0.7244$, provided they maintain or achieve feasibility.

\textbf{Slater condition (strong duality verification):} A strictly feasible point $\bar{x}$ satisfying $0.5 - \bar{x}_1 < 0$ (i.e., $\bar{x}_1 > 0.5$ strictly) exists: for example, $\bar{x} = (1, 2)$ satisfies $1 > 0.5$. By Slater's theorem for convex programs, this strict feasibility guarantees strong duality: $\max_{\mu \geq 0} \min_x L(x,\mu) = f^{\star}$, i.e., the duality gap is zero. (Note: Benchmark B is non-convex, so Slater's theorem does not strictly apply; however, since the constraint is inactive and the KKT conditions have a solution with $\mu^{\star}=0$, strong duality still holds at this specific local minimum by the regularity of the constraint qualification.)

\subsection{Algorithm Formulations}

\begin{algorithm}[H]
\caption{Projected Gradient Descent (PGD)}
\label{alg:pgd}
\begin{algorithmic}[1]
\Require $x_0$, step size $\alpha$, projection $\Pi_{\mathcal{X}}$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_{k+1} \gets x_k - \alpha\, \nabla f(x_k)$ \Comment{Unconstrained gradient step}
    \State $x_{k+1} \gets \Pi_{\mathcal{X}}(z_{k+1}) = ({\max}(0.5, z_{k+1,1}),\; z_{k+1,2})$ \Comment{Project onto feasible set}
\EndFor
\end{algorithmic}
\end{algorithm}

For the half-space $\{x : x_1 \geq 0.5\}$, the projection is simply $x_1 \leftarrow \max(0.5, x_1)$, a $O(1)$ operation. This closed form follows from the projection KKT conditions: $\Pi_{\mathcal{X}}(z) = \arg\min_{x: x_1 \geq 0.5} \|x-z\|^2$ has first-order conditions $2(x^* - z) = \mu e_1$ (stationarity), $x_1^* \geq 0.5$ (primal feasibility), $\mu \geq 0$ (dual feasibility), $\mu(x_1^* - 0.5) = 0$ (complementary slackness). If $z_1 \geq 0.5$: setting $\mu=0$ gives $x^* = z$ (unconstrained min is feasible). If $z_1 < 0.5$: the constraint is active, $x_1^* = 0.5$, $\mu = 2(z_1-0.5)/(-1) = 2(0.5-z_1) > 0$. Both cases give $x_1^* = \max(0.5, z_1)$.

Projected GD satisfies: for any $x \in \mathcal{X}$, $\|x_{k+1} - x\|^2 \leq \|x_k - x\|^2 - 2\alpha(f(x_k) - f(x)) + \alpha^2 L^2 \|g_k\|^2$, giving linear convergence rate for smooth, strongly convex $f$ restricted to $\mathcal{X}$.

\begin{algorithm}[H]
\caption{Linear Penalty Method}
\label{alg:penalty}
\begin{algorithmic}[1]
\Require $x_0$, step size $\alpha$, penalty weight $\lambda > 0$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $F_{\lambda}(x) \gets f(x) + \lambda \max(0,\, 0.5 - x_1)$ \Comment{Penalised objective (linear $\ell_1$ penalty)}
    \State $\nabla F_{\lambda}(x_k) \gets \nabla f(x_k) - \lambda \mathbf{1}[x_{k,1} < 0.5] \cdot e_1$ \Comment{Subgradient}
    \State $x_{k+1} \gets x_k - \alpha\, \nabla F_{\lambda}(x_k)$
\EndFor
\end{algorithmic}
\end{algorithm}

The penalty $\lambda\max(0, 0.5 - x_1)$ adds a linear cost proportional to the constraint violation. As $\lambda \to \infty$, the penalised minimiser approaches the constrained minimiser. For finite $\lambda$, the penalised problem has solution $x^{\star}(\lambda)$ with $x^{\star}(\lambda) \to x^{\star}$ as $\lambda \to \infty$.

\textbf{Connection to Lagrangian duality.} The Lagrangian of the constrained problem is $L(x, \mu) = f(x) + \mu(0.5 - x_1)$ for $\mu \geq 0$. The dual function is $d(\mu) = \min_x L(x, \mu)$, which provides a lower bound $d(\mu) \leq f^{\star}$ for all $\mu \geq 0$. Strong duality (Slater's condition holds since $x_0 = (0.2, 4)$ is interior infeasible but a strictly feasible point e.g.\ $(1, 2)$ exists) gives $d(\mu^{\star}) = f^{\star}$ at the optimal multiplier $\mu^{\star} = 0$ (since constraint is inactive). The penalty method implicitly solves the Lagrangian with $\mu = \lambda \mathbf{1}[x_1 < 0.5]$: for the infeasible iterates, the effective multiplier estimate is $\lambda$, which is too large (overestimates $\mu^{\star}=0$). This overestimation creates an asymmetric penalty that pushes iterates toward the feasible region, but the true optimal multiplier being zero means any $\lambda > 0$ eventually yields convergence to the correct point.

\begin{proposition}[Augmented Lagrangian Method]
Define the augmented Lagrangian:
\begin{equation}
\mathcal{L}_\rho(x, \mu) = f(x) + \mu (0.5 - x_1) + \frac{\rho}{2}\max(0,\, 0.5 - x_1)^2.
\label{eq:alm}
\end{equation}
The augmented Lagrangian method (ALM) alternates between:
\begin{enumerate}[nosep]
\item Minimise $x_{k+1} \gets \arg\min_x \mathcal{L}_\rho(x, \mu_k)$, and
\item Update the dual: $\mu_{k+1} \gets \mu_k + \rho\,\max(0,\, 0.5 - x_{k+1,1})$.
\end{enumerate}
For fixed $\rho > 0$, this converges to $\mu^{\star}$ and achieves primal feasibility $x_1 \to 0.5$ without requiring $\rho \to \infty$, thereby avoiding the ill-conditioning that makes pure penalty methods ($\rho = \lambda \to \infty$) numerically unstable.
\end{proposition}

Since the true KKT multiplier here is $\mu^{\star} = 0$ (constraint inactive), the dual update satisfies $\mu_k \to 0$ and the ALM reduces to minimising $f$ directly, confirming the primal/dual consistency of the problem setup.

\textbf{Interior point / barrier methods.} A complementary approach replaces the hard constraint $x_1 \geq 0.5$ with the log-barrier term:
\begin{equation}
f_t(x) = f(x) - \frac{1}{t}\log(x_1 - 0.5),
\label{eq:barrier}
\end{equation}
where $t > 0$ is the barrier parameter. As $t \to \infty$, the minimiser $x^{\star}(t)$ of $f_t$ approaches the constrained minimiser $x^{\star}$ along the \emph{central path}. Interior point methods follow this path by solving a sequence of unconstrained problems (using Newton's method on $f_t$) with increasing $t$. The key advantage over penalty methods is that the barrier ensures strict interior feasibility ($x_1 > 0.5$) throughout, while achieving polynomial-time complexity for convex problems. For this problem, since $x^{\star} = (0.5828, 2.0)$ is strictly interior to $\mathcal{X}$ (and the barrier is only at the lower bound $x_1 = 0.5$), the log-barrier gradient at $x^{\star}$ contributes $-e_1/(t \cdot (0.5828-0.5)) = -e_1/(0.0828t) \to 0$ as $t \to \infty$, consistent with the KKT multiplier $\mu^{\star} = 0$.

Settings: Projected GD ($\alpha = 0.08$, 100 iterations); Penalty with $\lambda \in \{0.15, 1.8, 4.5\}$ ($\alpha = 0.05$ for $\lambda \leq 1.8$, $\alpha = 0.03$ for $\lambda = 4.5$, as larger penalties increase the effective Lipschitz constant and require smaller steps); Unconstrained GD ($\alpha = 0.07$, 100 iterations).

\subsection{Objective Value Results}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q5_fval.pdf}
\caption{Q5: Objective value $f(x)$ (not penalised) vs.\ iteration. Projected GD and Penalty with $\lambda = 4.5$ converge to the constrained (= unconstrained) minimum $f \approx 0.7244$. Penalty with $\lambda = 1.8$ achieves comparable final accuracy; $\lambda = 0.15$ settles at a slightly higher value, indicating insufficient penalisation to overcome the gradient from the infeasible starting point. Unconstrained GD converges to the same value but takes an infeasible path throughout.}
\label{fig:q5_fval}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q5_contour.pdf}
\caption{Q5: Contour plot with feasible boundary $x_1 = 0.5$ (dashed red) and infeasible region (shaded). Starting from infeasible $(0.2, 4.0)$: Projected GD immediately clips to $x_1 = 0.5$ and thereafter optimises within $\mathcal{X}$, following the boundary briefly before reaching the interior minimum at $(0.5828, 2.0)$. Penalty methods are steered away from the infeasible region by the gradient of the penalty term; larger $\lambda$ creates a steeper ``wall'' at $x_1 = 0.5$, bringing the trajectory closer to the feasible region sooner.}
\label{fig:q5_contour}
\end{figure}

\subsection{Constraint Violation Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q5_violation.pdf}
\caption{Q5: Constraint violation $\max(0, 0.5 - x_1)$ on logarithmic scale. Left: full 100 iterations. Right: first 30 iterations (zoomed). Projected GD achieves \textbf{zero violation from iteration~1}: the projection $\max(0.5, x_{k+1,1})$ is applied at every step, enforcing $x_1 \geq 0.5$ as a hard constraint regardless of the gradient step. Penalty with $\lambda = 4.5$ reduces violation to near-zero within $\sim 10$ iterations. With $\lambda = 0.15$, the penalty gradient $-0.15 e_1$ is weak relative to the objective gradient ($|\partial f/\partial x_1| \approx 2.4$ at $(0.2, 4)$), so the iterate drifts slowly toward feasibility. The unconstrained GD never achieves feasibility because the unconstrained gradient drives it toward $x_1 \approx 0.58 > 0.5$, but from the infeasible starting point it crosses back to the feasible region only incidentally.}
\label{fig:q5_violation}
\end{figure}

\subsection{Complete Method Comparison}

\begin{table}[H]
\centering
\caption{Q5: Final positions and objective/violation values after 100 iterations. All methods converge to approximately the same constrained optimum $(0.582, 2.0)$ with sufficient $\lambda$, as the KKT analysis predicts.}
\label{tab:q5_final}
\begin{tabular}{lccccc}
\toprule
\textbf{Method} & \textbf{Final $x_1$} & \textbf{Final $x_2$} & \textbf{$f(x)$} & \textbf{Violation} & \textbf{Always Feasible?} \\
\midrule
Unconstrained GD & 0.582 & 2.000 & 0.7244 & 0.000 & No \\
Projected GD & 0.582 & 2.000 & 0.7244 & 0.000 & \textbf{Yes} \\
Penalty $\lambda = 0.15$ & $\approx 0.58$ & $\approx 2.00$ & $\approx 0.73$ & $\approx 0$ & No \\
Penalty $\lambda = 1.8$ & 0.582 & 2.000 & 0.7244 & 0.000 & No \\
Penalty $\lambda = 4.5$ & 0.583 & 2.000 & 0.7244 & 0.000 & No \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Key comparison:}
\begin{itemize}[nosep]
\item \textbf{Projected GD} guarantees feasibility at every iterate through the projection operator. This is the correct choice when constraint violations are inadmissible (safety-critical systems, physical constraints). The cost is requiring a tractable projection oracle. The computational complexity of projection varies dramatically by constraint set:
\begin{center}
\begin{tabular}{lcc}
\toprule
\textbf{Constraint set $\mathcal{X}$} & \textbf{Projection cost} & \textbf{Algorithm} \\
\midrule
Half-space $\{x : a^Tx \leq b\}$ & $O(d)$ & Closed form \\
Box $\{x : l \leq x \leq u\}$ & $O(d)$ & Coordinate clipping \\
Probability simplex $\Delta_d$ & $O(d\log d)$ & Sort + threshold \\
$\ell_2$ ball $\{\|x\| \leq r\}$ & $O(d)$ & Scale if needed \\
Nuclear norm ball $\{\|X\|_* \leq \tau\}$ & $O(\min(m,n)^2 \max(m,n))$ & Full SVD \\
Positive semidefinite cone & $O(d^3)$ & Eigendecomposition \\
\bottomrule
\end{tabular}
\end{center}
When projection is expensive (e.g., nuclear norm, SDP constraints), the LP oracle in Frank--Wolfe may be substantially cheaper, making FW the preferred method despite its slower convergence rate.
\item \textbf{Penalty methods} only require a modified gradient, making them easy to implement for any constraint type. The penalty parameter $\lambda$ must be tuned: too small fails to enforce feasibility; too large requires smaller step sizes for stability, increasing iteration count. The linear ($\ell_1$) penalty can cause chattering near the constraint boundary due to the non-smooth subgradient. A smoother quadratic penalty $\lambda(0.5-x_1)^2 \mathbf{1}[x_1 < 0.5]$ would avoid this at the cost of more iterations needed to enforce feasibility at a given $\lambda$. From a Lagrangian duality perspective, the penalty parameter $\lambda$ approximates the KKT multiplier $\mu^{\star}$: the minimiser of $F_{\lambda}(x)$ satisfies $\mu^{\star}(\lambda) \approx \lambda \cdot \mathbf{1}[x_1 < 0.5]$. As $\lambda \to \infty$, $\mu^{\star}(\lambda) \to \mu^{\star}_{\text{true}} = 0$ (since the constraint is inactive). The \emph{augmented Lagrangian method} (ALM) combines penalty with an explicit dual update $\mu_{k+1} = \mu_k + \lambda(0.5 - x_{k+1,1})$, achieving primal feasibility without requiring $\lambda \to \infty$ and thus avoiding the associated step-size restriction.
\end{itemize}

% ============================================================
\section{Question 6: Linear Programmes and Frank--Wolfe Algorithm \hfill [30 marks]}
\label{sec:q6}
% ============================================================

\subsection{Linear Programme over $\mathcal{X}$}

The feasible set is $\mathcal{X} = \{x \in \mathbb{R}^2 : 0.5 \leq x_1 \leq 5,\ -5 \leq x_2 \leq 10\}$.

\textbf{LP formulation:} Minimise $f(x) = x_1 + 2x_2$ over $\mathcal{X}$.

Since $f$ is linear, the minimum over a bounded polytope is attained at a vertex. With both coefficients positive ($a = [1, 2]^T > 0$), the minimum is at the vertex minimising $a^T x$, which for a box constraint is the lower bound of each coordinate:
\begin{equation}
x^{\star} = (0.5,\ -5), \qquad f^{\star} = 0.5 + 2(-5) = -9.5.
\end{equation}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q6_lp.pdf}
\caption{Q6-I: Linear programme over the box constraint $\mathcal{X}$ (blue). The parallel contour lines of $f(x) = x_1 + 2x_2$ are visible; the minimum is at the lower-left corner $(0.5, -5)$ where the contour achieves the smallest value within $\mathcal{X}$. This illustrates the fundamental theorem of linear programming: the optimum of a linear function over a polytope is always attained at an extreme point (vertex).}
\label{fig:q6_lp}
\end{figure}

\subsection{Frank--Wolfe Algorithm}

\begin{algorithm}[H]
\caption{Frank--Wolfe (Conditional Gradient Method, Fixed-Step Variant) \cite{frank1956algorithm}}
\label{alg:fw}
\begin{algorithmic}[1]
\Require $x_0 \in \mathcal{X}$, mixing parameter $\beta \in (0,1)$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State Solve LP: $z_k \gets \arg\min_{z \in \mathcal{X}}\, \nabla f(x_k)^T z$ \Comment{Linear minimisation oracle}
    \State $x_{k+1} \gets \beta\, x_k + (1 - \beta)\, z_k$ \Comment{Convex combination (fixed step)}
\EndFor
\end{algorithmic}
\end{algorithm}

For box constraints, the LP subproblem decomposes per coordinate in $O(d)$ operations:
\begin{equation}
(z_k)_i = \begin{cases} l_i & \text{if } (\nabla f(x_k))_i > 0 \\ u_i & \text{if } (\nabla f(x_k))_i \leq 0 \end{cases}
\label{eq:fw_lp}
\end{equation}
Each $x_{k+1}$ is a convex combination of $x_k$ and a vertex of $\mathcal{X}$, so $x_{k+1} \in \mathcal{X}$ by convexity --- no projection is needed. The standard Frank--Wolfe convergence theorem uses the open-loop step size $\gamma_k = 2/(k+2)$:

\begin{theorem}[Frank--Wolfe Convergence, \cite{frank1956algorithm}]
For $L$-smooth convex $f$ and compact convex $\mathcal{X}$ with diameter $D$, the standard Frank--Wolfe algorithm with $\gamma_k = 2/(k+2)$ satisfies:
\[
f(x_k) - f^{\star} \leq \frac{2LD^2}{k+2}.
\]
\end{theorem}

\begin{remark}
The implementation uses a \emph{fixed} mixing parameter $\beta$ (constant step $(1-\beta)$ toward $z_k$), a variant of standard FW. The standard open-loop schedule $\gamma_k = 2/(k+2)$ achieves the $O(1/k)$ guarantee; the fixed-$\beta$ variant converges geometrically for interior optima but does not carry a tight $O(1/k)$ guarantee. A practical certificate of optimality is the \textbf{Frank--Wolfe gap}:
\begin{equation}
g_k^{\text{FW}} \;=\; \nabla f(x_k)^T(x_k - z_k) \;\geq\; f(x_k) - f^{\star} \;\geq\; 0,
\label{eq:fw_gap}
\end{equation}
where the first inequality follows from $f(z_k) \geq f^{\star}$ and the linear lower bound $f(x_k) + \nabla f(x_k)^T(z_k - x_k) \leq f(z_k)$ when $f$ is convex. This gap is computable at no additional cost since $z_k$ is already known, and serves as a stopping criterion. For the standard FW with $\gamma_k = 2/(k+2)$, $g_k^{\text{FW}} \leq 2LD^2/(k+2)$, matching the $O(1/k)$ rate.
\end{remark}

\subsection{Interior Optimum: $f(x) = (x_1 - 1)^2 + (x_2 - 5)^2$}

The unconstrained minimum $(1, 5)$ lies inside $\mathcal{X}$ (since $0.5 \leq 1 \leq 5$ and $-5 \leq 5 \leq 10$). Starting from $x_0 = (1, 1)$, two mixing parameters are tested: $\beta = 0.90$ and $\beta = 0.985$.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q6_fw_convergence.pdf}
\caption{Q6-II: FW convergence for the interior optimum. Despite $\beta = 0.985$ taking smaller steps toward $z_k$ (only 1.5\% per iteration), it achieves a lower final objective ($f = 0.0046$) than $\beta = 0.90$ ($f = 0.0089$). This seemingly counterintuitive result occurs because large steps ($1-\beta = 0.10$) cause the iterates to oscillate: once $x_k$ is near $(1,5)$, $z_k$ jumps between the four vertices of $\mathcal{X}$ depending on the sign of the gradient, dragging $x_{k+1}$ away from the optimum. Smaller steps ($1-\beta = 0.015$) allow the convex combination to smooth out these oscillations, maintaining proximity to the optimum.}
\label{fig:q6_fw_conv}
\end{figure}

\subsection{Boundary Optimum: $f(x) = x_1^2 + x_2^2$}

The unconstrained minimum $(0, 0)$ lies outside $\mathcal{X}$ ($x_1 \geq 0.5$ prevents reaching $x_1 = 0$). The constrained minimum is $(0.5, 0)$ on the boundary, with $f^{\star} = 0.25$. Starting from $x_0 = (3, 3)$ with $\beta = 0.93$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q6_fw_contour.pdf}
\caption{Q6: FW trajectories. Left (interior optimum): trajectories converge toward $(1,5)$ from $(1,1)$. Both $\beta$ values reach the neighbourhood of the optimum, with $\beta=0.985$ showing smoother final convergence. Right (boundary optimum): the trajectory converges toward $(0.5, 0)$ on the constraint boundary. Frank--Wolfe handles boundary optima naturally: near $(0.5, 0)$, the gradient $\nabla f = (2x_1, 2x_2) \approx (1, 0)$ is positive in $x_1$ and small in $x_2$, so the LP oracle selects $z = (0.5, \cdot)$ (lower bound), pulling the iterate toward the boundary.}
\label{fig:q6_fw_contour}
\end{figure}

\subsection{Evolution of $x_k$ and $z_k$}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q6_evolution.pdf}
\caption{Q6: Evolution of iterates $x_k$ and LP vertices $z_k$. Top row (interior optimum): $z_k$ oscillates discontinuously between vertices of $\mathcal{X}$ as the gradient direction changes sign near $(1,5)$, while $x_k$ converges smoothly via the weighted average. The oscillation in $z_k$ is more pronounced for $\beta = 0.90$ (top-left) than $\beta = 0.985$ (top-right). Bottom-left (boundary optimum): $x_{1,k}$ converges to 0.5 (the constraint boundary) and $x_{2,k} \to 0$, while $z_k$ consistently selects the lower bound for $x_1$ and the vertex closest to $(0, 0)$ for $x_2$. Bottom-right: convergence comparison --- the boundary case converges to $f = 0.364$ at iteration~140, still above $f^{\star} = 0.25$, indicating that more iterations are needed to close the remaining gap.}
\label{fig:q6_evolution}
\end{figure}

\begin{table}[H]
\centering
\caption{Q6: Final positions and objective values for all Frank--Wolfe experiments.}
\label{tab:q6_final}
\begin{tabular}{lcccc}
\toprule
\textbf{Case} & $\beta$ & \textbf{Final $x$} & \textbf{$f(x)$} & \textbf{$f^{\star}$} \\
\midrule
Interior & 0.90 & $(1.066,\ 4.932)$ & 0.0089 & 0.0 \\
Interior & 0.985 & $(0.997,\ 5.067)$ & 0.0046 & 0.0 \\
Boundary & 0.93 & $(0.500,\ {-0.337})$ & 0.3637 & 0.25 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Interior vs.\ Boundary Behaviour}

\textbf{Interior optimum:} When the constrained optimum lies strictly inside $\mathcal{X}$, FW converges but exhibits the ``zig-zag'' pattern from the LP vertex oscillation. The iterate $x_k$ is always a convex combination of past LP vertices, so it can only approach the optimum asymptotically. The fixed-$\beta$ schedule provides linear convergence but with a constant offset due to the vertex oscillation; in contrast, the standard schedule $\gamma_k \to 0$ damps out the oscillation and achieves $O(1/k)$ guaranteed convergence. The \textbf{FW gap} can be computed at the final iterate to certify progress. For the interior case with $\beta = 0.985$ and final $x \approx (0.997, 5.067)$: $\nabla f(x) = 2(x-(1,5)) \approx (-0.006,\; 0.134)$. The LP oracle sets $z_1 = u_1 = 5$ (since $\nabla_1 < 0$, take upper bound) and $z_2 = l_2 = -5$ (since $\nabla_2 > 0$, take lower bound), giving $z = (5, -5)$. The FW gap is:
\[
g^{\text{FW}} = \nabla f(x)^T(x - z) = (-0.006)(0.997-5) + (0.134)(5.067+5) \approx 0.024 + 1.349 \approx 1.37.
\]
Since $g^{\text{FW}} = 1.37 \geq f(x) - f^{\star} = 0.0046$, the certificate is valid but loose: the iterate is very close to $f^{\star}$ yet the gap remains large because the LP vertex $(5,-5)$ is far from the optimum $(1,5)$. This looseness is characteristic of FW for \emph{interior} optima and motivates away-step variants.

\textbf{Boundary optimum:} FW is particularly natural for boundary optima since the LP oracle consistently selects the vertex nearest the true optimum, allowing the iterate to approach the boundary directly. Unlike projected gradient descent, no explicit projection is needed: feasibility is maintained automatically by taking convex combinations. However, convergence is slower ($O(1/k)$ vs.\ linear for PGD) because the FW step is fundamentally ``open-loop'' in the standard formulation.

\textbf{Improving FW convergence: away-step and pairwise variants.} A known limitation of standard FW is that iterates are convex combinations of vertices, which can prevent them from moving \emph{away} from vertices once they are in the convex hull interior. The \textbf{away-step FW} \cite{lacoste2015global} augments each step with an ``away step'' that removes weight from the vertex in the current support that most opposes descent, achieving \emph{linear convergence} $O((1-\rho)^k)$ on $\mu$-strongly convex objectives (where $\rho$ depends on the curvature and constraint geometry), matching PGD's linear rate while remaining projection-free. The \textbf{pairwise FW} variant simultaneously moves weight from the worst vertex to the best LP vertex in one step. Both variants maintain feasibility by construction and are particularly effective for simplex-constrained problems where the active set of vertices is small.

\textbf{Nuclear norm ball application.} For the matrix completion problem (low-rank matrix recovery), the constraint set is the nuclear norm ball $\mathcal{X} = \{X \in \mathbb{R}^{m \times n} : \|X\|_* \leq \tau\}$ (sum of singular values $\leq \tau$). The LP oracle $\min_{X \in \mathcal{X}} \nabla f(Y)^T X$ selects $X = -\tau u_1 v_1^T$ where $u_1, v_1$ are the leading singular vectors of $-\nabla f(Y)$, computed by a single power iteration (much cheaper than full SVD = $O(\min(m,n)^2\max(m,n))$). Frank--Wolfe iterates are automatically low-rank (each step adds one rank-1 term), ideal for large-scale problems where full-rank iterates would be prohibitive to store.

\textbf{Advantages of Frank--Wolfe over projection-based methods:}
\begin{itemize}[nosep]
\item No projection oracle required --- only an LP oracle over $\mathcal{X}$
\item Iterates are always feasible by construction
\item The LP oracle is computationally cheap for many structured constraint sets (box, simplex, nuclear norm ball)
\item Sparsity-promoting: LP vertex solutions tend to be sparse (e.g., simplex vertices are one-hot vectors), useful in matrix completion and nuclear norm problems
\end{itemize}

\textbf{Application to the probability simplex.} The most computationally elegant FW application is over the probability simplex $\Delta_d = \{x \in \mathbb{R}^d : x \geq 0,\; \mathbf{1}^T x = 1\}$, where the LP oracle reduces to selecting the argmin coordinate:
\begin{equation}
z_k = \arg\min_{z \in \Delta_d} \nabla f(x_k)^T z = e_{i^{\star}}, \qquad i^{\star} = \arg\min_i (\nabla f(x_k))_i.
\label{eq:fw_simplex}
\end{equation}
This $O(d)$ LP is far cheaper than the $O(d\log d)$ projection onto $\Delta_d$ (which requires sorting). Since each $z_k$ is a one-hot vector, the iterate $x_{k+1} = \beta x_k + (1-\beta) e_{i^{\star}}$ has at most $k+1$ nonzero coordinates, automatically yielding sparse solutions --- crucial in boosting (weights on training examples), matrix completion (low-rank matrices as sums of rank-1 outer products), and sparse signal recovery.

% ============================================================
\section{Conclusion}
\label{sec:conclusion}
% ============================================================

\subsection{Key Findings Per Question}

\begin{table}[H]
\centering
\caption{One-sentence summary of the key finding from each question, together with the most important formula.}
\label{tab:key_findings}
\begin{tabular}{clp{6cm}p{4.5cm}}
\toprule
\textbf{Q} & \textbf{Topic} & \textbf{Key finding} & \textbf{Central formula} \\
\midrule
1 & Adaptive $\alpha$ & Polyak diverges when $f^{\star}$ is misspecified; Adagrad is the most robust black-box choice. & $\alpha_k^{\text{Polyak}} = (f(x_k)-f^{\star})/\|g_k\|^2$ \\
2 & Momentum/SGD & Nesterov achieves $O(1/k^2)$ optimal rate and $7\times$ improvement over GD on Rosenbrock; SGD has irreducible noise floor $\propto \alpha\sigma^2/b$. & $f(x_k)-f^{\star} \leq 2L\|x_0-x^{\star}\|^2/(k+1)^2$ \\
3 & Newton & Newton is affine-invariant and converges in one step on quadratics; quadratic convergence near the optimum breaks the condition-number barrier. & $\|x_{k+1}-x^{\star}\| \leq C\|x_k-x^{\star}\|^2$ \\
4 & Deriv.-free & FD approximation error is minimised at $\delta^{\star} \approx 10^{-8}$; NRS achieves $O(d/\sqrt{k})$ at only 2 FE/step; grid search scales as $N^d$ (infeasible for $d \geq 5$). & $\delta^{\star} = \sqrt{2\varepsilon_{\text{mach}}/L_H}$ \\
5 & Constrained & KKT analysis shows constraint is inactive ($\mu^{\star}=0$); PGD guarantees hard feasibility from step 1; penalty requires $\lambda \to \infty$ for exact feasibility. & $\nabla f(x^{\star}) - \mu^{\star}e_1 = 0$, $\mu^{\star}(0.5-x_1^{\star})=0$ \\
6 & Frank--Wolfe & FW achieves $O(1/k)$ rate projection-free; the FW gap certifies optimality; away-step FW achieves linear convergence on strongly convex objectives. & $g_k^{\text{FW}} = \nabla f(x_k)^T(x_k-z_k) \geq f(x_k)-f^{\star}$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Comprehensive Method Comparison}

Table~\ref{tab:conclusion_compare} summarises all fifteen methods studied across six questions, their theoretical convergence guarantees, and practical performance characteristics.

\begin{table}[H]
\centering
\caption{Comprehensive comparison of all methods studied. ``Oracle cost'' = evaluations needed per iteration: GE = gradient evaluation, FE = function evaluation, HE = Hessian evaluation. ``---'' = method not applied to Bench.\ C or no formal guarantee exists.}
\label{tab:conclusion_compare}
\begin{tabular}{llccc}
\toprule
\textbf{Method} & \textbf{Category} & \textbf{Convergence Rate} & \textbf{Oracle cost} & \textbf{Bench.\ C result} \\
\midrule
GD (constant $\alpha$) & Q1 baseline & $(1-1/\kappa)^k$ linear & 1 GE & $f=3.51$ (120 it.) \\
Polyak step & Q1 adaptive & $O(1/k)$ if $f^{\star}$ exact & 1 GE $+$ 1 FE & $f=0.009$ \\
Adagrad & Q1 adaptive & $O(1/\sqrt{k})$ regret & 1 GE & $f=2.20$ \\
RMSprop & Q1 adaptive & No formal guarantee & 1 GE & $f=3.12$ \\
Heavy Ball & Q1 momentum & $(1-1/\sqrt{\kappa})^k$ (quadratic) & 1 GE & $f=1.40$ \\
Nesterov & Q2 momentum & $O(1/k^2)$ optimal & 1 GE & $f=0.479$ (150 it.) \\
Adam & Q2 adaptive+mom & Regret bound (AMSGrad) & 1 GE & $f=1.58$ \\
Newton & Q3 second-order & $O(\|x_k-x^{\star}\|^2)$ quadratic & 1 HE $+ O(d^3)$ & $f=0.686$ (20 it.) \\
FD GD ($\delta$ good) & Q4 zero-order & Same as GD & $d+1$ FE & High accuracy \\
FD GD ($\delta$ poor) & Q4 zero-order & Biased by truncation err.\ & $d+1$ FE & Low accuracy \\
Nesterov RS & Q4 zero-order & $O(d/\sqrt{k})$ & 2 FE & --- \\
Nelder--Mead & Q4 zero-order & No guarantee & ${\leq}3$ FE & Moderate \\
Grid Search & Q4 global & --- & $N^d$ FE total & Guaranteed (grid res.) \\
Projected GD & Q5 constrained & Linear (strongly convex) & 1 GE $+$ proj & Hard feasible \\
Penalty GD & Q5 constrained & Depends on $\lambda$ & 1 GE & Soft feasible \\
Frank--Wolfe & Q6 FW & $O(1/k)$ standard & 1 GE $+$ LP & Projection-free \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Key Lessons and Theoretical Insights}

\textbf{The role of condition number.} All gradient-based methods are fundamentally limited by the condition number $\kappa = L/\mu$. Table~\ref{tab:convergence_rates} summarises the iteration complexity of first-order methods to reach $\varepsilon$ accuracy.

\begin{table}[H]
\centering
\caption{Iteration complexity to achieve $f(x_k) - f^{\star} \leq \varepsilon$ for $L$-smooth, $\mu$-strongly convex $f$. $D = \|x_0 - x^{\star}\|$. Applicable only in the convex setting; rates are asymptotic.}
\label{tab:convergence_rates}
\begin{tabular}{lcc}
\toprule
\textbf{Method} & \textbf{Rate} & \textbf{Complexity} \\
\midrule
GD (constant $\alpha=1/L$) & $(1-1/\kappa)^k$ & $O(\kappa \log(1/\varepsilon))$ \\
Heavy Ball (optimal params) & $(1-1/\sqrt{\kappa})^k$ & $O(\sqrt{\kappa} \log(1/\varepsilon))$ \\
Nesterov Acceleration & $O(1/k^2)$ & $O(\sqrt{\kappa} \log(1/\varepsilon))$ \\
Newton (near optimum) & $O(C^{2^k})$ quadratic & $O(\log\log(1/\varepsilon))$ \\
\bottomrule
\end{tabular}
\end{table}

On Benchmark~C with $\kappa \approx 2504$, GD requires $O(\kappa \log(1/\varepsilon)) \approx 2504 \log(1/\varepsilon)$ iterations, while Nesterov requires $O(\sqrt{\kappa} \log(1/\varepsilon)) \approx 50 \log(1/\varepsilon)$ iterations --- a $50\times$ speedup. Yet both remain slow for high accuracy; Newton achieves the same accuracy in $O(\log\log(1/\varepsilon))$ iterations, showing that no amount of first-order optimisation can substitute for curvature information on severely ill-conditioned problems. The Rosenbrock results confirm this hierarchy: excluding the oracle-assisted Polyak ($f=0.009$, $f^{\star}=0$ known), the ranking by final objective value (lower is better) is Nesterov ($f=0.479$, 150 it.) $\prec$ Newton ($f=0.686$, 20 it.) $\prec$ Heavy Ball ($f=1.40$, 120 it.) $\prec$ Adam ($f=1.578$, 150 it.) $\prec$ GD ($f=3.51$, 120 it.). Note that Heavy Ball outperforms Adam here despite both being $O(1/k)$ in the worst case: the consistent gradient direction along the Rosenbrock valley allows Heavy Ball's momentum to accumulate effectively, whereas Adam's per-coordinate scaling is less suited to the tightly coupled anisotropy of the Rosenbrock Hessian (off-diagonal coupling between $x_1$ and $x_2$).

\textbf{The value of second-order information.} Newton's method breaks the condition-number barrier by using Hessian information: the Newton direction $p_k = H_k^{-1}g_k$ effectively pre-conditions the gradient, rescaling each direction by the inverse curvature. This yields quadratic convergence near the optimum ($O(\|x_k - x^{\star}\|^2)$ per step) and exact one-step convergence for quadratics. The cost --- computing and factoring an $n \times n$ Hessian ($O(n^3)$ per iteration) --- makes Newton impractical for large-scale machine learning ($n \sim 10^8$). \textbf{Quasi-Newton methods: connection to finite differences.} The BFGS update uses gradient-difference pairs $(s_k, y_k) = (x_{k+1}-x_k,\, g_{k+1}-g_k)$ to build a Hessian approximation. The secant condition $B_{k+1}s_k = y_k$ enforces that the Hessian model agrees with the actual gradient change: $\nabla^2 f(x_k) \approx B_{k+1} \approx y_k/s_k^T$ (one-dimensional view). This is a \emph{gradient finite difference}: $y_k = \nabla f(x_k + s_k) - \nabla f(x_k) \approx H(x_k) s_k$, so $B_{k+1}$ approximates the Hessian as if a finite difference of the gradient were taken along the step direction $s_k$. This connects quasi-Newton methods directly to finite-difference Hessian approximation: L-BFGS implicitly builds a rank-$m$ approximation of the Hessian from $m$ directional finite differences of the gradient.

\textbf{Quasi-Newton BFGS update:} the full update rule is:
\begin{equation}
B_{k+1}^{-1} = \left(I - \frac{s_k y_k^T}{y_k^T s_k}\right) B_k^{-1} \left(I - \frac{y_k s_k^T}{y_k^T s_k}\right) + \frac{s_k s_k^T}{y_k^T s_k},
\label{eq:bfgs}
\end{equation}
which satisfies the secant condition $B_{k+1} s_k = y_k$ and maintains positive definiteness when $y_k^T s_k > 0$. L-BFGS stores only the $m$ most recent $(s_j, y_j)$ pairs (discarding older ones) and applies the implicit inverse via the two-loop recursion in $O(md)$ operations per iteration. With $m \approx 20$, L-BFGS achieves superlinear convergence with $O(md)$ memory --- the standard choice for large-scale smooth optimisation (e.g., logistic regression, neural networks with smooth activations).

\textbf{Adaptive step sizes improve robustness, not rate.} Adagrad, RMSprop, and Adam do not improve the asymptotic convergence rate for smooth convex functions (they remain $O(1/k)$ sublinear or $O(\rho^k)$ linear), but they improve practical performance by: (i) automatically adapting to heterogeneous curvature through per-coordinate scaling; and (ii) reducing the need for manual step-size tuning. The bias-variance trade-off between Adagrad (cumulative, decaying) and RMSprop (EMA, stable) offers a practical dial for non-stationary problems.

\textbf{Stochastic gradients introduce an irreducible noise floor.} Mini-batch SGD does not converge to the exact minimiser with a fixed step size; it converges to a neighbourhood of size $O(\alpha \sigma^2/b)$ where $\sigma^2$ is the per-sample gradient variance. Reducing batch size increases the noise floor but reduces per-iteration cost; the optimal batch size balances these effects. Under high observation noise, the irreducible floor $J^{\star} = \sigma^2/2$ dominates, making the convergence floor independent of batch size.

\textbf{Variance reduction methods.} The SGD noise floor can be eliminated while retaining cheap per-iteration cost through \emph{variance reduction} methods. The key idea is to periodically compute the full gradient $g_{\text{full}} = \nabla f(x)$ (at cost $O(m)$) and use it to correct the stochastic gradient. SVRG (Stochastic Variance-Reduced Gradient, \cite{johnson2013accelerating}) uses the update:
\begin{equation}
\hat{g}_k = \nabla f_{i_k}(x_k) - \nabla f_{i_k}(\tilde{x}) + g_{\text{full}},
\label{eq:svrg}
\end{equation}
where $\tilde{x}$ is a periodic snapshot and $g_{\text{full}} = \nabla f(\tilde{x})$ is the full gradient at the snapshot. The correction term ensures $\mathbb{E}[\hat{g}_k] = \nabla f(x_k)$ and reduces variance to $O(\|x_k - \tilde{x}\|^2)$, which goes to zero as $x_k \to x^{\star}$. This allows SVRG to achieve \emph{linear} convergence $O(\rho^k)$ for strongly convex functions with only $O(1)$ gradient evaluations per iteration (plus an occasional full gradient), whereas standard SGD is limited to $O(1/k)$ with fixed $\alpha$. SVRG, SAG, and SARAH represent the state-of-the-art for large-scale convex optimisation in machine learning.

\textbf{Derivative-free methods require exponentially many evaluations for global optimality.} Finite-difference approximation maintains $O(d)$ evaluation cost (same order as exact GD) and achieves comparable accuracy when $\delta$ is chosen well ($\delta \approx 10^{-4}$ to $10^{-2}$ for typical functions). Nesterov random search provides $O(1)$ evaluations per step at the cost of much higher variance. Grid search is the only method that guarantees global optimality, but at $O(N^d)$ cost, it is impractical beyond $d \approx 3$.

\textbf{Constrained optimisation: projection vs.\ penalty.} Projected GD provides hard feasibility guarantees at the cost of a projection oracle; penalty methods offer simplicity but only soft feasibility. For problems where the unconstrained optimum is feasible (as in this assignment), both methods converge to the same point, but Projected GD does so while always remaining in $\mathcal{X}$. For safety-critical applications, hard feasibility is essential.

\textbf{Frank--Wolfe exploits constraint structure.} By reducing each iteration to an LP over $\mathcal{X}$, Frank--Wolfe avoids projection entirely. For constraint sets where the LP is cheap (box: $O(d)$; simplex: $O(d\log d)$) but projection is not, FW is the method of choice. The $O(1/k)$ rate is slower than projected GD ($O(\rho^k)$ linear) but may be acceptable in applications requiring sparse iterates (e.g., compressed sensing, matrix completion) since LP vertices tend to be sparse.

\subsection{Practical Recommendations}

Based on the experimental results across all three benchmarks, the following guidelines emerge:

\begin{enumerate}[nosep]
\item \textbf{Well-conditioned problems} ($\kappa < 10$, Benchmark~A): GD with $\alpha = 0.5/L$ is sufficient. Adaptive methods provide negligible benefit.
\item \textbf{Moderately ill-conditioned problems} ($\kappa \in [10, 100]$, Benchmark~B): Adam with defaults ($\beta_1=0.9, \beta_2=0.999, \varepsilon=10^{-8}, \alpha=10^{-3}$) is a robust first choice. Nesterov with $\beta_{\max} \approx 1-1/\sqrt{\kappa}$ provides theoretically optimal first-order convergence.
\item \textbf{Severely ill-conditioned problems} ($\kappa > 100$, Benchmark~C): L-BFGS with $m=20$ history pairs is the practical choice. For first-order: Nesterov with $\beta_{\max} \approx (\sqrt{\kappa}-1)/(\sqrt{\kappa}+1)$ and $\alpha \leq 1/L$; Polyak step (if $f^{\star}$ is known) achieves near-oracle performance.
\item \textbf{Gradient unavailable}: For $d \leq 10$: central FD ($\delta \approx 10^{-5}$) with any gradient-based method. For $d > 100$: Nesterov random search. Nelder--Mead is effective for $d \leq 5$ with moderate function evaluation budget.
\item \textbf{Constrained problems}: Projected GD if the projection is cheap ($O(d)$ for box/half-space); Frank--Wolfe if the LP is cheap or sparsity is desired; ALM for equality constraints or when exact Lagrange multipliers are needed.
\item \textbf{Stochastic settings}: Adam ($\beta_1=0.9, \beta_2=0.999, \varepsilon=10^{-8}$) with linear warmup ($T_{\text{warm}} \approx T/10$) and cosine decay is the state-of-the-art default. For strongly convex problems (logistic regression, linear SVM), SVRG achieves linear convergence. Learning rate schedules further improve practical performance:
\begin{itemize}[nosep]
\item \textbf{Linear warmup}: $\alpha_t = \alpha_{\max} \cdot t/T_{\text{warmup}}$ for $t \leq T_{\text{warmup}}$, avoiding the large initial step from cold-start second moments.
\item \textbf{Cosine decay}: $\alpha_t = \alpha_{\min} + \frac{1}{2}(\alpha_{\max}-\alpha_{\min})(1 + \cos(\pi t/T))$ for $t \in [T_{\text{warmup}}, T]$, smoothly reducing the step size toward zero.
\item \textbf{Linear decay with warmup} (BERT-style): Widely used in transformer training; $\alpha_t = \alpha_{\max}\min(t^{-0.5}, t \cdot T_{\text{warmup}}^{-1.5})$ satisfies the Robbins--Monro conditions for SGD convergence ($\sum \alpha_t = \infty$, $\sum \alpha_t^2 < \infty$).
\end{itemize}
These schedules allow the algorithm to explore with large steps early (fast progress from high-loss starting points) and converge precisely with small steps late, combining the benefits of large and small step sizes.
\end{enumerate}

% ============================================================
\newpage
\appendix
\section{Python Code}
\label{app:code}
% ============================================================

The complete implementation is in \texttt{CS7DS2\_Final\_Code\_25336453.py}. Key algorithm implementations are reproduced below.

\begin{lstlisting}[caption={Gradient Descent}]
def gradient_descent(grad_fn, loss_fn, x0, alpha, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for _ in range(n_iters):
        x = x - alpha * grad_fn(x)
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Polyak Step Size}]
def polyak_step(grad_fn, loss_fn, x0, f_star, eps, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        alpha_k = (loss_fn(x) - f_star) / (np.dot(g, g) + eps)
        alpha_hist.append(alpha_k)
        x = x - alpha_k * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Adagrad}]
def adagrad(grad_fn, loss_fn, x0, alpha0, eps, n_iters):
    x = x0.copy().astype(float)
    G = np.zeros_like(x)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        G += g**2
        eff_alpha = alpha0 / (np.sqrt(G) + eps)
        alpha_hist.append(np.mean(eff_alpha))
        x = x - eff_alpha * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={RMSprop}]
def rmsprop(grad_fn, loss_fn, x0, alpha0, beta, eps, n_iters):
    x = x0.copy().astype(float)
    v = np.zeros_like(x)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        v = beta * v + (1 - beta) * g**2
        eff_alpha = alpha0 / (np.sqrt(v) + eps)
        alpha_hist.append(np.mean(eff_alpha))
        x = x - eff_alpha * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Heavy Ball / Polyak Momentum}]
def heavy_ball(grad_fn, loss_fn, x0, alpha, beta, n_iters):
    x = x0.copy().astype(float)
    z = np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for _ in range(n_iters):
        g = grad_fn(x)
        z = beta * z + alpha * g
        x = x - z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Nesterov Momentum}]
def nesterov_momentum(grad_fn, loss_fn, x0, alpha, beta_max, n_iters):
    x = x0.copy().astype(float)
    z = np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for k in range(1, n_iters + 1):
        beta_k = min((k - 1) / (k + 2), beta_max)
        lookahead = x + beta_k * z
        g = grad_fn(lookahead)
        z = beta_k * z - alpha * g
        x = x + z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Adam Optimiser}]
def adam_optimiser(grad_fn, loss_fn, x0, alpha, beta1, beta2, eps, n_iters):
    x = x0.copy().astype(float)
    m_vec, v_vec = np.zeros_like(x), np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for t in range(1, n_iters + 1):
        g = grad_fn(x)
        m_vec = beta1 * m_vec + (1 - beta1) * g
        v_vec = beta2 * v_vec + (1 - beta2) * g**2
        m_hat = m_vec / (1 - beta1**t)
        v_hat = v_vec / (1 - beta2**t)
        x = x - alpha * m_hat / (np.sqrt(v_hat) + eps)
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Newton's Method with Regularisation}]
def newtons_method(grad_fn, hess_fn, loss_fn, x0, alpha, n_iters, damping=1e-8):
    x = x0.copy().astype(float)
    x_hist, f_hist, update_norms = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        H = hess_fn(x) if callable(hess_fn) else hess_fn
        H_reg = H + damping * np.eye(len(x))
        try:
            p = np.linalg.solve(H_reg, g)
        except np.linalg.LinAlgError:
            p = g
        update_norms.append(np.linalg.norm(alpha * p))
        x = x - alpha * p
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(update_norms)
\end{lstlisting}

\begin{lstlisting}[caption={Frank--Wolfe Algorithm}]
def frank_wolfe(grad_fn, loss_fn, x0, bounds, beta, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist, z_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        z = np.array([bounds[i][0] if g[i] > 0 else bounds[i][1]
                       for i in range(len(g))])
        z_hist.append(z.copy())
        x = beta * x + (1 - beta) * z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(z_hist)
\end{lstlisting}

% ============================================================
\begin{thebibliography}{99}

\bibitem{polyak1969minimization}
Polyak, B.T.\ (1969).
Minimization of unsmooth functionals.
\textit{USSR Computational Mathematics and Mathematical Physics}, 9(3), 14--29.

\bibitem{duchi2011adaptive}
Duchi, J., Hazan, E., \& Singer, Y.\ (2011).
Adaptive subgradient methods for online learning and stochastic optimization.
\textit{Journal of Machine Learning Research}, 12, 2121--2159.

\bibitem{tieleman2012rmsprop}
Tieleman, T.\ \& Hinton, G.\ (2012).
Lecture 6.5 --- RMSprop: Divide the gradient by a running average of its recent magnitude.
\textit{COURSERA: Neural Networks for Machine Learning}, 4(2), 26--31.

\bibitem{polyak1964heavy}
Polyak, B.T.\ (1964).
Some methods of speeding up the convergence of iteration methods.
\textit{USSR Computational Mathematics and Mathematical Physics}, 4(5), 1--17.

\bibitem{nesterov1983method}
Nesterov, Y.\ (1983).
A method for unconstrained convex minimization problem with the rate of convergence $O(1/k^2)$.
\textit{Soviet Mathematics Doklady}, 27(2), 372--376.

\bibitem{kingma2014adam}
Kingma, D.P.\ \& Ba, J.\ (2015).
Adam: A method for stochastic optimization.
\textit{Proceedings of the International Conference on Learning Representations (ICLR)}.

\bibitem{reddi2019convergence}
Reddi, S.J., Kale, S., \& Kumar, S.\ (2018).
On the convergence of Adam and beyond.
\textit{Proceedings of the International Conference on Learning Representations (ICLR)}.

\bibitem{nesterov2004introductory}
Nesterov, Y.\ (2004).
\textit{Introductory Lectures on Convex Optimization: A Basic Course}.
Kluwer Academic Publishers, Boston.

\bibitem{nocedal2006numerical}
Nocedal, J.\ \& Wright, S.J.\ (2006).
\textit{Numerical Optimization}, 2nd ed.
Springer, New York.

\bibitem{frank1956algorithm}
Frank, M.\ \& Wolfe, P.\ (1956).
An algorithm for quadratic programming.
\textit{Naval Research Logistics Quarterly}, 3(1--2), 95--110.

\bibitem{nesterov2017random}
Nesterov, Y.\ \& Spokoiny, V.\ (2017).
Random gradient-free minimization of convex functions.
\textit{Foundations of Computational Mathematics}, 17(2), 527--566.

\bibitem{nelder1965simplex}
Nelder, J.A.\ \& Mead, R.\ (1965).
A simplex method for function minimization.
\textit{The Computer Journal}, 7(4), 308--313.

\bibitem{boyd2004convex}
Boyd, S.\ \& Vandenberghe, L.\ (2004).
\textit{Convex Optimization}.
Cambridge University Press.

\bibitem{johnson2013accelerating}
Johnson, R.\ \& Zhang, T.\ (2013).
Accelerating stochastic gradient descent using predictive variance reduction.
\textit{Advances in Neural Information Processing Systems (NeurIPS)}, 26, 315--323.

\bibitem{dauphin2014identifying}
Dauphin, Y.N., Ganguli, S., Ganguli, S., Yuille, A., \& Bengio, Y.\ (2014).
Identifying and attacking the saddle point problem in high-dimensional non-convex optimization.
\textit{Advances in Neural Information Processing Systems (NeurIPS)}, 27.

\bibitem{lacoste2015global}
Lacoste-Julien, S.\ \& Jaggi, M.\ (2015).
On the global linear convergence of Frank--Wolfe optimization variants.
\textit{Advances in Neural Information Processing Systems (NeurIPS)}, 28.

\end{thebibliography}

\end{document}
