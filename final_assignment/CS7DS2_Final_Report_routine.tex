\documentclass[11pt,a4paper]{article}
\usepackage[margin=2.2cm]{geometry}
\usepackage{graphicx}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{float}
\usepackage{hyperref}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{listings}
\usepackage{fancyhdr}
\usepackage{microtype}

\hypersetup{colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue}

% Code listing style
\lstset{
  language=Python,
  basicstyle=\ttfamily\scriptsize,
  keywordstyle=\color{blue},
  commentstyle=\color{gray},
  stringstyle=\color{red},
  numbers=left,
  numberstyle=\tiny\color{gray},
  breaklines=true,
  frame=single,
  captionpos=b
}

\theoremstyle{definition}
\newtheorem{theorem}{Theorem}
\newtheorem{definition}{Definition}
\newtheorem{proposition}{Proposition}
\newtheorem{remark}{Remark}

\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small CS7DS2 -- Final Assignment}
\fancyhead[R]{\small Swetha Sekar (25336453)}
\fancyfoot[C]{\thepage}

\title{%
  \includegraphics[width=0.25\textwidth]{tcd_logo.jpg}\\[1em]
  \textbf{CS7DS2 -- Optimisation Algorithms for Data Analysis}\\[0.5em]
  \Large Final Assignment Report
}
\author{Swetha Sekar \\ Student ID: 25336453 \\ School of Computer Science and Statistics \\ Trinity College Dublin}
\date{April 2026}

\begin{document}
\maketitle
\thispagestyle{empty}

\begin{abstract}
This report presents a systematic comparative study of twelve optimisation algorithms across
three canonical benchmark functions of increasing difficulty: a linear regression quadratic loss
($\kappa \approx 1.12$, Benchmark A), a toy neural network loss with sinusoidal perturbation
($\kappa \leq 10$, Benchmark B), and the Rosenbrock function ($\kappa \approx 2508$,
Benchmark C).  Six algorithm families are investigated: (Q1) adaptive step-size methods
(Polyak, Adagrad, RMSprop, Heavy Ball); (Q2) momentum and stochastic methods (Nesterov
acceleration, Adam, mini-batch SGD); (Q3) Newton's method and second-order local
approximation; (Q4) derivative-free approaches (finite-difference gradients, Nesterov random
search, Nelder--Mead, grid search); (Q5) constrained optimisation (projected gradient descent,
penalty methods); and (Q6) the Frank--Wolfe conditional-gradient algorithm.

Key findings include: (i) the Polyak step achieves the best first-order convergence on
Rosenbrock when $f^{\star}$ is known, but diverges catastrophically under misspecification
--- confirmed both theoretically and numerically; (ii) Nesterov's $O(1/k^2)$ acceleration
yields a $7\times$ improvement over GD on Rosenbrock despite the function's non-convexity;
(iii) Newton's method converges in \emph{one step} on the quadratic benchmark and requires
only 20 damped steps to outperform 80 GD steps on Rosenbrock, confirming its
condition-number-independent quadratic convergence; (iv) finite differences with $\delta = 0.05$
approximate the exact gradient to within $1.2\%$ error, while $\delta = 0.8$ introduces $20\%$
error and substantially degrades convergence; (v) projected gradient descent enforces
feasibility from the first iteration as a hard constraint, while penalty methods require
$\lambda \geq 1.8$ for adequate feasibility enforcement; and (vi) Frank--Wolfe with fixed
$\beta = 0.985$ converges more smoothly than $\beta = 0.90$ for an interior optimum, and
naturally handles boundary optima without explicit projection.
\end{abstract}

\tableofcontents
\newpage

% ============================================================
\section{Benchmark Problems and Experimental Setup}
\label{sec:benchmarks}
% ============================================================

All experiments are conducted in Python 3 using a fixed random seed
(\texttt{np.random.seed(42)}) for reproducibility.  The three benchmark problems are chosen
to represent a spectrum of optimisation difficulty: a well-conditioned convex quadratic (A), a
moderately nonlinear convex function (B), and a highly nonlinear, ill-conditioned non-convex
function (C).

\subsection{Mathematical Preliminaries}
\label{sec:prelims}

We recall the standard regularity conditions used throughout:

\begin{definition}[$L$-smooth gradient]
A differentiable function $f:\mathbb{R}^d \to \mathbb{R}$ has an $L$-Lipschitz gradient
(is $L$-smooth) if for all $x, y$:
\[
\|\nabla f(x) - \nabla f(y)\| \leq L\,\|x - y\|,
\]
equivalently $f(y) \leq f(x) + \nabla f(x)^T(y-x) + \frac{L}{2}\|y-x\|^2$.
\end{definition}

\begin{definition}[$\mu$-strongly convex]
A differentiable function $f$ is $\mu$-strongly convex if for all $x, y$:
\[
f(y) \geq f(x) + \nabla f(x)^T(y-x) + \frac{\mu}{2}\|y-x\|^2.
\]
\end{definition}

For a twice-differentiable function, $L$ equals the maximum eigenvalue of $\nabla^2 f$ and
$\mu$ equals the minimum eigenvalue (when $\nabla^2 f \succ 0$). The condition number
$\kappa = L/\mu$ governs the convergence of gradient-based methods: GD requires
$O(\kappa \log(1/\varepsilon))$ iterations while Newton's method is $\kappa$-independent.

\subsection{Benchmark A: Linear Regression Quadratic Loss}

A synthetic dataset with $m = 1000$ samples and two parameters is generated:
\begin{equation}
\theta^{\star} = \begin{bmatrix} 3 \\ 4 \end{bmatrix}, \quad
x^{(i)} \stackrel{\mathrm{iid}}{\sim} \mathcal{N}(0, I_2),\quad
y^{(i)} = \theta^{\star T} x^{(i)} + \varepsilon^{(i)}, \quad
\varepsilon^{(i)} \stackrel{\mathrm{iid}}{\sim} \mathcal{N}(0, 1).
\end{equation}
The mean squared error loss is:
\begin{equation}
J(\theta) = \frac{1}{2m} \sum_{i=1}^{m} \left(\theta^T x^{(i)} - y^{(i)}\right)^2
= \frac{1}{2m}\|X\theta - y\|^2.
\end{equation}
The gradient and Hessian are constant:
\begin{equation}
\nabla J(\theta) = \frac{1}{m}X^T(X\theta - y) = H\theta - b, \qquad
H = \frac{1}{m}X^T X, \quad b = \frac{1}{m}X^T y.
\end{equation}
For this dataset (seed 42), the Hessian has eigenvalues $\lambda_1 \approx 0.925$ and
$\lambda_2 \approx 1.033$, yielding condition number $\kappa \approx 1.12$ --- an extremely
well-conditioned problem.  The smoothness constant is $L = \lambda_{\max} \approx 1.033$ and
the strong convexity constant is $\mu = \lambda_{\min} \approx 0.925$.  The OLS minimiser is
$\theta^{\star}_{\mathrm{OLS}} = H^{-1}b \approx (3.028,\,4.017)^T$ (close to the true
$\theta^{\star} = (3, 4)^T$, differing due to observation noise), with minimum loss
$J^{\star} = J(\theta^{\star}_{\mathrm{OLS}}) \approx 0.4826$.  Starting point:
$\theta_0 = [0, 0]^T$.

\subsection{Benchmark B: Toy Neural Network Quadratic Loss}
\begin{equation}
f(x_1, x_2) = (x_1 - 1)^2 + 5(x_2 - 2)^2 + \sin(x_1).
\end{equation}
The gradient and Hessian are:
\begin{equation}
\nabla f = \begin{bmatrix} 2(x_1 - 1) + \cos(x_1) \\ 10(x_2 - 2) \end{bmatrix},
\qquad
\nabla^2 f = \begin{bmatrix} 2 - \sin(x_1) & 0 \\ 0 & 10 \end{bmatrix}.
\end{equation}
The Hessian is diagonal; the $(1,1)$ entry $2 - \sin(x_1)$ ranges in $[1, 3]$, so the
condition number ranges between $10/3 \approx 3.3$ and $10/1 = 10$.  The global minimum
satisfies $x_2^{\star} = 2$ exactly, and $x_1^{\star}$ is the solution of:
\[
2(x_1 - 1) + \cos(x_1) = 0 \implies x_1^{\star} \approx 0.5824,
\]
giving $f^{\star} \approx 0.7244$.  Starting point: $x_0 = [-1, 4]^T$.

\subsection{Benchmark C: Rosenbrock Function}
\begin{equation}
f(x_1, x_2) = (1 - x_1)^2 + 100(x_2 - x_1^2)^2.
\end{equation}
The Rosenbrock function is not convex. Its unique global minimum is at $(1, 1)$ with
$f^{\star} = 0$.  The Hessian at the optimum is:
\begin{equation}
\nabla^2 f(1,1) = \begin{bmatrix} 802 & -400 \\ -400 & 200 \end{bmatrix},
\end{equation}
with eigenvalues $\lambda_{\min} \approx 0.399$ and $\lambda_{\max} \approx 1001.6$, giving a
condition number $\kappa \approx 2508$.  This severe ill-conditioning, combined with the narrow
curved valley along $x_2 \approx x_1^2$, makes Rosenbrock one of the most challenging standard
test functions.  Starting point: $x_0 = [-1, 1]^T$.

\subsection{Benchmark D: Constrained Feasible Set}
For Questions 5 and 6: $\mathcal{X} = \{x \in \mathbb{R}^2 : 0.5 \leq x_1 \leq 5,\
-5 \leq x_2 \leq 10\}$.  This is a compact, convex box polytope.

% ============================================================
\section{Question 1: Adaptive Step-Size Methods}
\label{sec:q1}
% ============================================================

\subsection{Theoretical Background}

For a quadratic function with Hessian $H$ (eigenvalues $\mu \leq L$), standard GD with
fixed step $\alpha \leq 1/L$ satisfies:
\begin{equation}
\label{eq:gd_rate}
f(x_k) - f^{\star} \leq \left(1 - \frac{\mu}{L}\right)^k \bigl[f(x_0) - f^{\star}\bigr]
= \left(\frac{\kappa - 1}{\kappa}\right)^k \bigl[f(x_0) - f^{\star}\bigr].
\end{equation}
This linear convergence rate degrades as $\kappa \to \infty$.  Adaptive step-size methods
attempt to overcome this limitation by automatically adjusting the effective learning rate.

\subsection{Algorithm Formulations}

Four adaptive methods are compared against a constant step-size gradient descent baseline
across all three benchmarks for 120 iterations.

\begin{algorithm}[H]
\caption{Polyak Step Size}
\label{alg:polyak}
\begin{algorithmic}[1]
\Require Initial point $x_0$, optimal value $f^{\star}$, regularisation $\varepsilon > 0$,
         iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $\alpha_k \gets \dfrac{f(x_k) - f^{\star}}{\|g_k\|^2 + \varepsilon}$
    \State $x_{k+1} \gets x_k - \alpha_k\, g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

The Polyak step size is motivated by the inequality: for any $L$-smooth function with
minimum $f^{\star}$,
\[
f(x_k) - f^{\star} \geq \frac{1}{2L}\|\nabla f(x_k)\|^2,
\]
so $\alpha_k = (f(x_k) - f^{\star})/\|\nabla f(x_k)\|^2$ is the maximal step that minimises
an upper bound on the next iterate.  When $f^{\star}$ is known exactly and $f$ is convex,
the Polyak step satisfies the contraction:
\[
\|x_{k+1} - x^{\star}\|^2 \leq \|x_k - x^{\star}\|^2 - \frac{(f(x_k) - f^{\star})^2}{\|\nabla f(x_k)\|^2},
\]
guaranteeing strict decrease of the distance to $x^{\star}$ at every step.  For strongly
convex $f$ with constant $\mu$, this implies $f(x_k) - f^{\star} = O(e^{-k\mu/L})$ --- the
same linear rate as GD with optimal fixed step, but without requiring knowledge of $L$.

When $f^{\star}$ is unknown, one uses an estimate $\tilde{f}^{\star}$; misspecification
causes $\alpha_k$ to grow unboundedly (see Section~\ref{sec:q1_convergence}).

\begin{algorithm}[H]
\caption{Adagrad}
\label{alg:adagrad}
\begin{algorithmic}[1]
\Require Initial point $x_0$, base learning rate $\alpha_0$, regularisation $\varepsilon > 0$,
         iterations $N$
\State $G_0 \gets \mathbf{0} \in \mathbb{R}^d$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $G_{k+1} \gets G_k + g_k \odot g_k$
    \Comment{Per-coordinate squared gradient accumulation}
    \State $x_{k+1} \gets x_k - \dfrac{\alpha_0}{\sqrt{G_{k+1}} + \varepsilon} \odot g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

Adagrad's per-coordinate update normalises gradients by the square root of cumulative squared
gradients.  This allows larger steps in directions with historically small gradients (rarely
updated coordinates) and smaller steps where gradients are large.  Its theoretical guarantee
in the online convex optimisation setting is the regret bound:
\[
\sum_{k=1}^{T} f_k(x_k) - \sum_{k=1}^{T} f_k(x^{\star})
\leq \frac{\|x_0 - x^{\star}\|^2}{2\alpha_0}\sum_{i=1}^{d}\sqrt{\sum_{k=1}^{T} g_{k,i}^2},
\]
which is $O(\sqrt{T}\,G_{1:T})$ where $G_{1:T}$ is the total gradient magnitude.  This adapts
to the geometry of the loss: if many coordinates have small gradients (as in sparse problems),
Adagrad achieves much better bounds than standard SGD.  The monotonically decreasing effective
step $\alpha_0 / \sqrt{G_k}$ is a feature (guaranteed convergence) but also a drawback: the
step size can become negligibly small on long runs, causing premature stagnation.

\begin{algorithm}[H]
\caption{RMSprop}
\label{alg:rmsprop}
\begin{algorithmic}[1]
\Require Initial point $x_0$, learning rate $\alpha_0$, decay $\beta \in (0,1)$,
         regularisation $\varepsilon > 0$, iterations $N$
\State $v_0 \gets \mathbf{0}$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $v_{k+1} \gets \beta\, v_k + (1 - \beta)\, g_k \odot g_k$
    \Comment{Exponential moving average of $g^2$}
    \State $x_{k+1} \gets x_k - \dfrac{\alpha_0}{\sqrt{v_{k+1}} + \varepsilon} \odot g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

RMSprop addresses Adagrad's diminishing-step problem by replacing the cumulative sum with an
exponential moving average controlled by $\beta = 0.9$ (standard setting).  The effective
denominator $\sqrt{v_k}$ tracks recent gradient magnitudes, preventing over-decay and
maintaining non-negligible steps throughout training.

\begin{algorithm}[H]
\caption{Polyak Momentum / Heavy Ball}
\label{alg:heavyball}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, momentum coefficient $\beta \in [0,1)$,
         iterations $N$
\State $z_0 \gets \mathbf{0}$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_{k+1} \gets \beta\, z_k + \alpha\, \nabla f(x_k)$
    \State $x_{k+1} \gets x_k - z_{k+1}$
\EndFor
\end{algorithmic}
\end{algorithm}

The Heavy Ball update can be written as:
$x_{k+1} = x_k - \alpha \nabla f(x_k) + \beta(x_k - x_{k-1})$,
adding a fraction of the previous displacement.  For a quadratic $f$ with eigenvalues
$[\mu, L]$, the optimal parameters are $\alpha^{\star} = 4/(\sqrt{L}+\sqrt{\mu})^2$ and
$\beta^{\star} = ((\sqrt{L}-\sqrt{\mu})/(\sqrt{L}+\sqrt{\mu}))^2$, achieving the
$O((\sqrt{\kappa}-1)/(\sqrt{\kappa}+1))^k$ convergence rate --- the same asymptotic rate as
Nesterov acceleration for strongly convex quadratics.

\subsection{Hyperparameter Settings}

\begin{table}[H]
\centering
\caption{Q1: Hyperparameter settings for each method and benchmark (all 120 iterations).}
\label{tab:q1_params}
\begin{tabular}{llccc}
\toprule
\textbf{Method} & \textbf{Parameter} & \textbf{Bench.~A} & \textbf{Bench.~B} &
\textbf{Bench.~C} \\
\midrule
GD (baseline) & $\alpha$ & 0.08 & 0.06 & 0.0012 \\
\midrule
Polyak & $f^{\star}$ & 0 & 0 & 0 \\
 & $\varepsilon$ & $10^{-4}$ & $10^{-4}$ & $10^{-3}$ \\
\midrule
Adagrad & $\alpha_0$ & 1.8 & 1.2 & 0.45 \\
 & $\varepsilon$ & $10^{-5}$ & $10^{-5}$ & $10^{-5}$ \\
\midrule
RMSprop & $\alpha_0$ & 0.22 & 0.14 & 0.0035 \\
 & $\beta$ & 0.9 & 0.9 & 0.9 \\
 & $\varepsilon$ & $10^{-5}$ & $10^{-5}$ & $10^{-5}$ \\
\midrule
Heavy Ball & $\alpha$ & 0.045 & 0.035 & 0.0008 \\
 & $\beta$ & 0.88 & 0.90 & 0.86 \\
\bottomrule
\end{tabular}
\end{table}

The GD step sizes are chosen below the theoretical stability limit $2/(L + \mu)$.  For
Benchmark A, $2/(L+\mu) \approx 2/(1.033 + 0.925) \approx 1.02$, so $\alpha = 0.08$ is very
conservative.  For Benchmark C, the ill-conditioning ($\kappa \approx 2508$) forces a very
small step to maintain stability: $1/L \approx 1/1001.6 \approx 0.001$, matching the chosen
$\alpha = 0.0012$.

\subsection{Convergence Analysis}
\label{sec:q1_convergence}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_A.pdf}
\caption{Q1: Convergence on Benchmark~A (Linear Regression).  GD, Adagrad, and Heavy Ball all
converge to the noise floor $J^{\star} \approx 0.4826$.  RMSprop settles at $\approx 0.5027$
due to its adaptive step oscillating near the minimum.  Polyak diverges ($f \to 2.72$) because
the misspecified $f^{\star} = 0 < J^{\star} = 0.4826$: as $\|\nabla f\| \to 0$ but $f(x_k)
\to 0.4826$, the step $\alpha_k = (f - 0)/\|\nabla f\|^2 \to \infty$, inducing unbounded
oscillations.}
\label{fig:q1_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_B.pdf}
\caption{Q1: Convergence on Benchmark~B (Toy Neural Network).  The same Polyak misspecification
mechanism causes divergence ($f \to 24.07$ vs true minimum $0.7244$).  Adagrad matches GD's
final accuracy.  Heavy Ball with $\beta = 0.90$ converges smoothly, benefiting from momentum
across the anisotropic curvature (condition number up to 10).  RMSprop reaches $\approx 0.7271$
with stable effective step.}
\label{fig:q1_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_C.pdf}
\caption{Q1: Convergence on Benchmark~C (Rosenbrock).  Here $f^{\star} = 0$ is correctly
specified, so Polyak converges well to $f = 0.0094$ --- the best result among all five methods.
Heavy Ball ($f = 1.40$) outperforms GD ($f = 3.51$) through momentum accumulation along the
valley floor.  Adagrad ($f = 2.20$) benefits from its monotonically decaying step that is
better-suited to the ill-conditioned landscape than a fixed rate.  RMSprop ($f = 3.12$) lags
because the base rate $\alpha_0 = 0.0035$ is too conservative.}
\label{fig:q1_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q1: Final objective values after 120 iterations.  Bold indicates best per benchmark.}
\label{tab:q1_final}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Bench.~A} & \textbf{Bench.~B} & \textbf{Bench.~C} \\
\midrule
GD (baseline) & 0.4826 & 0.7244 & 3.5142 \\
Polyak & 2.7189 & 24.0659 & \textbf{0.0094} \\
Adagrad & \textbf{0.4826} & \textbf{0.7244} & 2.2034 \\
RMSprop & 0.5027 & 0.7271 & 3.1204 \\
Heavy Ball & 0.4826 & 0.7244 & 1.4003 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Key finding: Polyak sensitivity.}
The Polyak step size is theoretically optimal when $f^{\star}$ is known exactly, but
catastrophically fails when $f^{\star}$ is misspecified.  Let $\delta = f^{\star}_{\mathrm{spec}} - f^{\star}_{\mathrm{true}}$.  Then the Polyak step becomes:
\[
\alpha_k = \frac{f(x_k) - f^{\star}_{\mathrm{spec}}}{\|\nabla f(x_k)\|^2 + \varepsilon}
= \frac{[f(x_k) - f^{\star}_{\mathrm{true}}] - \delta}{\|\nabla f(x_k)\|^2 + \varepsilon}.
\]
For Benchmarks A and B, $f^{\star}_{\mathrm{spec}} = 0 < f^{\star}_{\mathrm{true}}$, so
$\delta < 0$.  As $x_k \to x^{\star}$, the numerator approaches $-\delta > 0$ while
$\|\nabla f\|^2 \to 0$, making $\alpha_k \to \infty$.

\paragraph{Adagrad vs RMSprop.}
On Benchmarks A and B (low condition number), both methods perform similarly.  On Benchmark C
(high condition number), Adagrad's accumulated history enforces progressively smaller steps,
which --- counterintuitively --- helps on this problem because the gradient magnitudes along
the valley decrease over time.  RMSprop's exponential forgetting loses this history, causing
its effective step to occasionally increase and destabilise.

\subsection{Contour Plots with Optimisation Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q1_contour_B.pdf}
\caption{Q1: Trajectories on Benchmark~B.  The elliptical contours reflect the 5:1 curvature
ratio ($\nabla^2 f = \mathrm{diag}(\approx1.45, 10)$ at the minimum).  GD and Adagrad follow
near-direct paths.  Heavy Ball exhibits slight overshoot near the minimum before settling, a
characteristic of momentum-based methods.  The Polyak trajectory diverges as
$\alpha_k \to \infty$, leaving the displayed region.}
\label{fig:q1_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q1_contour_C.pdf}
\caption{Q1: Trajectories on Benchmark~C (Rosenbrock).  The banana-shaped valley along
$x_2 \approx x_1^2$ is visible.  All methods descend rapidly to the valley floor, then
progress slowly along it.  Polyak's large adaptive steps drive the most rapid progress once in
the valley.  GD barely advances along the valley due to the tiny step ($\alpha = 0.0012$)
required for stability in the direction perpendicular to the valley.}
\label{fig:q1_contour_C}
\end{figure}

\subsection{Adaptive Step-Size Evolution}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_A.pdf}
\caption{Q1: Step-size evolution on Benchmark~A.  The Polyak step grows rapidly as
$\|\nabla J\| \to 0$ while the numerator $J(x_k) - 0 \approx 0.4826$ remains large.
Adagrad decays monotonically as the cumulative denominator $G_k$ grows.  RMSprop stabilises
near a non-negligible effective rate due to its exponential forgetting factor ($\beta = 0.9$).}
\label{fig:q1_stepsize_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_B.pdf}
\caption{Q1: Step-size evolution on Benchmark~B.  The same pathological growth appears for
Polyak.  Adagrad's initial rapid decay reflects large early gradients (far from minimum);
it then settles to a small effective rate.  RMSprop maintains a moderate, slowly-varying
step throughout.}
\label{fig:q1_stepsize_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_C.pdf}
\caption{Q1: Step-size evolution on Benchmark~C.  With correct $f^{\star} = 0$, the Polyak
step decreases monotonically as $f(x_k) \to 0$ (numerator shrinks) while $\|\nabla f\|$
remains bounded away from zero until convergence.  This well-behaved decay confirms why
Polyak achieves the best Rosenbrock result.}
\label{fig:q1_stepsize_C}
\end{figure}

\subsection{Theoretical Rate Verification}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_gd_rate_theory.pdf}
\caption{Q1: Empirical GD convergence vs theoretical rate on Benchmark~A.  The theoretical
prediction $(1 - \mu/L)^k = (1 - 0.896)^k$ bound (using exact eigenvalues $\mu \approx 0.925$,
$L \approx 1.033$) closely matches the empirical excess loss $f(x_k) - f^{\star}$.  The
condition number $\kappa \approx 1.12$ is so close to 1 that the contraction factor
$(1 - 1/\kappa) \approx 0.107$ is large --- explaining why GD converges very rapidly on this
well-conditioned benchmark.  This validates both the implementation and the theoretical
convergence rate formula.}
\label{fig:q1_gd_theory}
\end{figure}

\subsection{Comparative Analysis}

\begin{itemize}[nosep]
\item \textbf{Convergence speed:} When $f^{\star}$ is known exactly, Polyak converges fastest
  (Rosenbrock: $f = 0.0094$, Heavy Ball second at $f = 1.40$, GD worst at $f = 3.51$).
  Otherwise Adagrad and Heavy Ball match or exceed GD.
\item \textbf{Smoothness:} RMSprop produces the smoothest convergence curves due to its
  exponential averaging, damping per-coordinate oscillations.  Heavy Ball exhibits
  characteristic overshoot followed by damped oscillation, especially on anisotropic surfaces.
\item \textbf{Stability:} GD baseline is the most stable but slowest.  Polyak is
  \emph{catastrophically unstable} when $f^{\star}$ is misspecified.  Adagrad is uniformly
  stable due to its monotonically non-increasing step size.
\item \textbf{Practical recommendation:} In practice, exact $f^{\star}$ is rarely available,
  limiting Polyak's applicability to settings where a good lower bound is known (e.g.,
  non-negative loss functions with a natural zero floor).  RMSprop and Adagrad are more robust
  choices for general problems; Heavy Ball is effective when the landscape is approximately
  quadratic and step/momentum parameters can be tuned.
\end{itemize}

% ============================================================
\section{Question 2: Momentum and Stochastic Methods}
\label{sec:q2}
% ============================================================

\subsection{Theoretical Background}

\begin{theorem}[Nesterov optimal rate, 1983]
For a convex $L$-smooth function $f$ and Nesterov's accelerated gradient method with
step $\alpha \leq 1/L$ and the momentum schedule $\beta_k = (k-1)/(k+2)$:
\[
f(x_k) - f^{\star} \leq \frac{2L\,\|x_0 - x^{\star}\|^2}{(k+1)^2}.
\]
\end{theorem}
This $O(1/k^2)$ rate is \emph{optimal} for first-order methods on smooth convex functions,
compared to GD's $O(1/k)$ rate.  For strongly convex functions, a fixed momentum
$\beta = (\sqrt{\kappa}-1)/(\sqrt{\kappa}+1)$ achieves the linear rate
$O(e^{-k/\sqrt{\kappa}})$, which can be dramatically faster than GD's $O(e^{-k/\kappa})$ for
large $\kappa$.

\subsection{Algorithm Formulations}

\begin{algorithm}[H]
\caption{Nesterov Momentum / Acceleration}
\label{alg:nesterov}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, max momentum $\beta_{\max}$, iterations $N$
\State $z_0 \gets \mathbf{0}$
\For{$k = 1, 2, \ldots, N$}
    \State $\beta_k \gets \min\!\left(\frac{k-1}{k+2},\ \beta_{\max}\right)$
    \State $\tilde{x}_k \gets x_k + \beta_k\, z_k$
    \Comment{Lookahead (extrapolated) point}
    \State $z_{k+1} \gets \beta_k\, z_k - \alpha\, \nabla f(\tilde{x}_k)$
    \State $x_{k+1} \gets x_k + z_{k+1}$
\EndFor
\end{algorithmic}
\end{algorithm}

The critical distinction from Heavy Ball is the \emph{lookahead}: the gradient is evaluated at
$\tilde{x}_k = x_k + \beta_k z_k$ rather than $x_k$.  By evaluating at the extrapolated
point, Nesterov can ``anticipate'' its next position and correct overshooting before it occurs.
The adaptive schedule $\beta_k = (k-1)/(k+2)$ gradually increases momentum, preventing the
instability that arises from immediately using high momentum.

\begin{algorithm}[H]
\caption{Adam Optimiser}
\label{alg:adam}
\begin{algorithmic}[1]
\Require Initial point $x_0$, learning rate $\alpha$, first-moment decay $\beta_1$,
         second-moment decay $\beta_2$, regularisation $\varepsilon$, iterations $N$
\State $m_0 \gets \mathbf{0},\ v_0 \gets \mathbf{0}$
\For{$t = 1, 2, \ldots, N$}
    \State $g_t \gets \nabla f(x_t)$
    \State $m_t \gets \beta_1\, m_{t-1} + (1 - \beta_1)\, g_t$
    \Comment{First moment (gradient EMA)}
    \State $v_t \gets \beta_2\, v_{t-1} + (1 - \beta_2)\, g_t \odot g_t$
    \Comment{Second moment (squared gradient EMA)}
    \State $\hat{m}_t \gets m_t\, /\, (1 - \beta_1^t)$
    \Comment{Bias-corrected first moment}
    \State $\hat{v}_t \gets v_t\, /\, (1 - \beta_2^t)$
    \Comment{Bias-corrected second moment}
    \State $x_{t+1} \gets x_t - \alpha\, \hat{m}_t\, /\, (\sqrt{\hat{v}_t} + \varepsilon)$
\EndFor
\end{algorithmic}
\end{algorithm}

Adam synthesises RMSprop (per-coordinate adaptive scaling via $v_t$) and momentum (via $m_t$).
The bias-correction terms $(1-\beta_1^t)$ and $(1-\beta_2^t)$ are essential in early iterations
when $m_t$ and $v_t$ are initialised at zero and would otherwise be biased towards zero.
As $t \to \infty$, the corrections approach 1.  Adam's effective step in coordinate $i$ is
approximately $\alpha$ regardless of gradient magnitude, since $|\hat{m}_{t,i}| \leq |\hat{v}_{t,i}|^{1/2}$, bounding the update.

\subsection{Hyperparameter Settings}

\begin{table}[H]
\centering
\caption{Q2: Hyperparameter settings (all methods run for 150 iterations).}
\label{tab:q2_params}
\begin{tabular}{llccc}
\toprule
\textbf{Method} & \textbf{Parameter} & \textbf{Bench.~A} & \textbf{Bench.~B} &
\textbf{Bench.~C} \\
\midrule
GD (baseline) & $\alpha$ & 0.08 & 0.06 & 0.0012 \\
\midrule
Nesterov & $\alpha$ & 0.06 & 0.035 & 0.0007 \\
 & $\beta_{\max}$ & 0.90 & 0.92 & 0.90 \\
\midrule
Adam & $\alpha$ & 0.12 & 0.08 & 0.006 \\
 & $\beta_1$ & 0.82 & 0.80 & 0.80 \\
 & $\beta_2$ & 0.999 & 0.999 & 0.999 \\
 & $\varepsilon$ & $10^{-8}$ & $10^{-8}$ & $10^{-8}$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Convergence Comparison}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_A.pdf}
\caption{Q2: Convergence on Benchmark~A.  All three methods converge to
$J^{\star} \approx 0.4826$.  The well-conditioned surface ($\kappa \approx 1.12$) means even
GD converges rapidly, so the advantage of momentum is modest but observable in the initial
transient.}
\label{fig:q2_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_B.pdf}
\caption{Q2: Convergence on Benchmark~B.  Nesterov and Adam converge to $f^{\star} \approx
0.7244$.  Adam's per-coordinate scaling effectively handles the anisotropic curvature
(condition number up to 10).  Nesterov's lookahead avoids overshoot across the narrow $x_1$
direction.}
\label{fig:q2_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_C.pdf}
\caption{Q2: Convergence on Benchmark~C (Rosenbrock).  Nesterov achieves $f = 0.479$, a
substantial improvement over GD ($f = 3.388$), by accumulating momentum along the valley
floor.  Adam reaches $f = 1.578$: its per-coordinate scaling helps but cannot fully compensate
for the severe curvature mismatch ($\kappa \approx 2508$) in the Rosenbrock valley.}
\label{fig:q2_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q2: Final objective values after 150 iterations.  Bold indicates best per benchmark.}
\label{tab:q2_final}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Bench.~A} & \textbf{Bench.~B} & \textbf{Bench.~C} \\
\midrule
GD (baseline) & 0.4826 & 0.7244 & 3.3880 \\
Nesterov & \textbf{0.4826} & \textbf{0.7244} & \textbf{0.4788} \\
Adam & \textbf{0.4826} & \textbf{0.7244} & 1.5785 \\
\bottomrule
\end{tabular}
\end{table}

On Benchmarks~A and B both Nesterov and Adam reach the minimum, while on the challenging
Rosenbrock, Nesterov's acceleration is clearly superior.

\begin{remark}[Rosenbrock is non-convex]
Rosenbrock is \emph{not} a convex function, so the $O(1/k^2)$ theorem does not directly apply.
Nevertheless, Nesterov acceleration empirically outperforms GD on non-convex problems in
practice, particularly when the trajectory remains in a locally convex region for many
iterations.  Near the valley floor of Rosenbrock the function is locally well-approximated by
a convex quadratic, and Nesterov's momentum effectively exploits this structure.
\end{remark}

\paragraph{Why Nesterov beats Adam on Rosenbrock.}
Adam's per-coordinate scaling $1/\sqrt{\hat{v}_{t,i}}$ is effective when the ill-conditioning
is axis-aligned --- i.e., when different coordinates have very different gradient magnitudes.
However, Rosenbrock's ill-conditioning arises from a \emph{coupled} (off-diagonal) Hessian:
$H_{12} = -400x_1$ at the starting point, so the eigenvalues are not aligned with the
coordinate axes.  Adam's diagonal approximation cannot correct this off-diagonal curvature.
Nesterov's momentum, by contrast, accumulates velocity along the valley floor direction
(which is consistent across many iterations), effectively exploiting the local curvature
without needing to approximate the full Hessian.

\subsection{Contour Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q2_contour_B.pdf}
\caption{Q2: Trajectories on Benchmark~B.  Nesterov's trajectory shows the characteristic
smooth arc of accelerated methods, avoiding the zig-zag pattern of GD on anisotropic surfaces.
Adam takes more direct, coordinate-wise-scaled steps, reaching the minimum quickly.}
\label{fig:q2_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q2_contour_C.pdf}
\caption{Q2: Trajectories on Benchmark~C.  All methods descend quickly to the valley floor but
differ in traversal speed along the valley.  Nesterov's momentum builds up in the consistent
valley-floor direction, enabling significantly more progress than GD's cautious constant step.}
\label{fig:q2_contour_C}
\end{figure}

\subsection{Mini-Batch Stochastic Gradient Descent}

Mini-batch SGD partitions the $m = 1000$ training samples into batches of size $b$ and
performs one parameter update per batch.  The mini-batch gradient is an unbiased estimate of
the full gradient:
\[
\hat{g}(\theta; \mathcal{B}) = \frac{1}{b}\sum_{i \in \mathcal{B}}
\nabla_\theta \ell(\theta; x^{(i)}, y^{(i)}), \quad
\mathbb{E}[\hat{g}] = \nabla J(\theta).
\]
The variance of the mini-batch gradient decreases with $b$:
$\mathrm{Var}[\hat{g}] = \sigma^2 / b$, where $\sigma^2$ is the per-sample gradient variance.

\begin{algorithm}[H]
\caption{Mini-Batch SGD}
\label{alg:sgd}
\begin{algorithmic}[1]
\Require Data $(X, y)$, initial $\theta_0$, learning rate $\alpha$, batch size $b$, epochs $E$
\For{$e = 1, 2, \ldots, E$}
    \State Randomly permute data indices $\{1, \ldots, m\}$
    \For{each mini-batch $\mathcal{B}$ of size $b$}
        \State $g \gets \frac{1}{b} X_{\mathcal{B}}^T(X_{\mathcal{B}}\theta - y_{\mathcal{B}})$
        \State $\theta \gets \theta - \alpha\, g$
    \EndFor
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_sgd_batch.pdf}
\caption{Q2: Mini-batch SGD with batch sizes $b = 5$ and $b = 40$ on Benchmark~A
($\alpha = 0.06$, 50 epochs).  With $b = 5$: $1000/5 = 200$ updates per epoch, rapid
per-epoch progress but higher gradient variance.  With $b = 40$: $1000/40 = 25$ updates per
epoch, smoother convergence but slower per-epoch progress.  The smaller batch reaches lower
loss faster in epochs owing to the greater frequency of updates, but oscillates near the
minimum because individual mini-batch gradients deviate from the true gradient.}
\label{fig:q2_sgd_batch}
\end{figure}

\subsection{Batch Size Sweep}

Figure~\ref{fig:q2_sgd_sweep} shows the effect of varying batch size across a wider range
$b \in \{1, 5, 20, 40, 100, 200, 1000\}$ (where $b = 1000 = m$ is equivalent to full-batch GD).

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q2_sgd_batchsize_sweep.pdf}
\caption{Q2: Mini-batch SGD convergence for seven batch sizes on Benchmark~A
($\alpha = 0.06$, 50 epochs).  As $b$ decreases from 1000 (full batch) to 1 (online), the
per-epoch update count increases from 1 to 1000.  Small batches converge more quickly per
epoch due to more gradient steps, but plateau at higher loss due to gradient noise.  Full-batch
GD is the smoothest but slowest per epoch.  This sweep illustrates the classical
\emph{bias-variance trade-off} in stochastic optimisation: small $b$ reduces bias towards the
current point but increases gradient variance $\propto 1/b$.}
\label{fig:q2_sgd_sweep}
\end{figure}

\subsection{SGD under Increased Observation Noise}

The experiment is repeated with output noise scaled by a factor of 6 ($\sigma = 6$).
Since $J^{\star} = \frac{1}{2}\mathbb{E}[(y - \theta^{\star T}x)^2] = \frac{\sigma^2}{2}$,
scaling $\sigma \mapsto 6\sigma$ raises the irreducible loss floor by factor $36$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q2_sgd_noisy.pdf}
\caption{Q2: Effect of observation noise on SGD convergence.  Left: $\sigma = 1$, minimum
$J^{\star} \approx 0.483$.  Right: $\sigma = 6$, minimum $J^{\star} \approx 17.3$ (scaled by
$36$).  Under high noise, both batch sizes converge to the same elevated floor; the relative
advantage of smaller batches (more updates per epoch) diminishes because the irreducible
gradient noise from the data model dominates the mini-batch sampling variance.}
\label{fig:q2_sgd_noisy}
\end{figure}

\paragraph{Discussion.}
The key insight is that SGD has two sources of gradient noise: (1) \emph{mini-batch sampling
noise} $\propto 1/b$, which can be reduced by increasing batch size; and (2) \emph{irreducible
observation noise} from $\sigma^2$, which cannot be eliminated by changing $b$.  When the
observation noise is small ($\sigma = 1$), small batches provide a meaningful speed advantage.
When observation noise dominates ($\sigma = 6$), there is little to be gained from small
batches, and the smoother convergence of larger batches becomes preferable.

For the linear model, the mini-batch gradient $\hat{g}(\theta; \mathcal{B})$ has variance:
\[
\mathrm{Var}[\hat{g}] = \frac{\sigma^2}{b}\,\frac{1}{m}\sum_{i=1}^{m}\|x^{(i)}\|^2,
\]
so increasing $b$ linearly decreases gradient variance.  However, the irreducible floor
$J^{\star} = \sigma^2/2$ cannot be reduced regardless of $b$.

% ============================================================
\section{Question 3: Newton's Method and Local Approximation}
\label{sec:q3}
% ============================================================

\subsection{Local Approximations of $g(x) = x^4$}

Newton's method is motivated by the second-order Taylor approximation.  Given a function
$g : \mathbb{R} \to \mathbb{R}$, expanded around $x_0 = 0.25$:

\textbf{First-order (linear) approximation:}
\begin{equation}
g(x) \approx g(x_0) + g'(x_0)(x - x_0)
= (0.25)^4 + 4(0.25)^3(x - 0.25)
= 0.0039 + 0.0625(x - 0.25).
\label{eq:first_order}
\end{equation}

\textbf{Second-order (quadratic) approximation:}
\begin{equation}
g(x) \approx g(x_0) + g'(x_0)(x - x_0) + \tfrac{1}{2}g''(x_0)(x - x_0)^2
= 0.0039 + 0.0625(x - 0.25) + 0.375(x - 0.25)^2.
\label{eq:second_order}
\end{equation}

Setting the derivative of \eqref{eq:second_order} to zero:
\[
g'(x_0) + g''(x_0)(x - x_0) = 0 \implies x_{\mathrm{Newton}} = x_0 - \frac{g'(x_0)}{g''(x_0)}
= 0.25 - \frac{0.0625}{0.75} \approx 0.167.
\]
Comparing: the exact minimum of $g(x) = x^4$ is $x^{\star} = 0$.  The Newton step brings us
to 0.167, which is substantially closer than the gradient step of size $\alpha \cdot 0.0625$
for any moderate $\alpha$.  This illustrates the curvature-awareness of Newton's method.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_approximation.pdf}
\caption{Q3: Local approximations of $g(x) = x^4$ at $x_0 = 0.25$.  The first-order
approximation (tangent line) captures only the local slope and diverges from the true function
rapidly.  The second-order approximation (parabola) captures local curvature and remains
accurate over a larger neighbourhood.  Minimising the parabola yields the Newton update,
which uses curvature to take a much more informed step than gradient descent.}
\label{fig:q3_approx}
\end{figure}

\subsection{Newton's Method}

In higher dimensions, Newton's method solves the linear system $H_k\, p_k = \nabla f(x_k)$
at each step, where $H_k = \nabla^2 f(x_k) + \lambda I$ is the (regularised) Hessian.

\begin{algorithm}[H]
\caption{Damped Newton's Method}
\label{alg:newton}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, Hessian damping $\lambda > 0$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $H_k \gets \nabla^2 f(x_k) + \lambda I$
    \Comment{Tikhonov regularisation for positive definiteness}
    \State $p_k \gets H_k^{-1}\, g_k$
    \Comment{Newton direction via linear solve}
    \State $x_{k+1} \gets x_k - \alpha\, p_k$
\EndFor
\end{algorithmic}
\end{algorithm}

The Newton direction $p_k = H_k^{-1}g_k$ is the gradient in the metric defined by $H_k^{-1}$
(the \emph{natural gradient}).  It is invariant to affine transformations of the domain ---
Newton's method is \emph{affine-invariant}, meaning its iterates are identical under any
invertible linear change of variables.  Gradient descent does not share this property, which
explains why GD struggles on ill-conditioned problems while Newton does not.

\paragraph{Newton decrement.}
The \emph{Newton decrement} at $x_k$ is defined as:
\[
\lambda(x_k) = \left(\nabla f(x_k)^T H_k^{-1} \nabla f(x_k)\right)^{1/2} = \|p_k\|_{H_k},
\]
which satisfies $f(x_k) - \inf_{x} q_k(x) = \tfrac{1}{2}\lambda(x_k)^2$ where $q_k$ is the
local quadratic model.  Thus $\tfrac{1}{2}\lambda(x_k)^2$ is a tight lower bound on how
much $f$ can decrease in one Newton step, making it a useful stopping criterion:
$\lambda(x_k)^2 / 2 < \varepsilon$ implies $f(x_k) - f^{\star} < \varepsilon$ (for convex $f$).

\begin{theorem}[Local quadratic convergence]
Let $f$ be twice continuously differentiable near $x^{\star}$ with $\nabla^2 f(x^{\star})$
positive definite, and $\nabla^2 f$ Lipschitz continuous.  For the full Newton step
($\alpha = 1$) and $x_0$ sufficiently close to $x^{\star}$:
\[
\|x_{k+1} - x^{\star}\| \leq C\,\|x_k - x^{\star}\|^2.
\]
\end{theorem}
This \emph{quadratic convergence} means the number of correct digits doubles at each iteration
--- dramatically faster than GD's linear convergence.

\begin{table}[H]
\centering
\caption{Q3: Hyperparameters for GD (80 iterations) and Newton (20 iterations, damping
$\lambda = 10^{-8}$).}
\label{tab:q3_params}
\begin{tabular}{lcccccc}
\toprule
 & \multicolumn{2}{c}{\textbf{Bench.~A}} & \multicolumn{2}{c}{\textbf{Bench.~B}} &
\multicolumn{2}{c}{\textbf{Bench.~C}} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}\cmidrule(lr){6-7}
 & GD & Newton & GD & Newton & GD & Newton \\
\midrule
$\alpha$ & 0.08 & 1.0 & 0.06 & 0.85 & 0.001 & 0.22 \\
Iterations & 80 & 20 & 80 & 20 & 80 & 20 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Convergence Comparison}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_A.pdf}
\caption{Q3: Newton vs GD on Benchmark~A.  Newton with $\alpha = 1.0$ converges in
\textbf{one iteration} to $J = 0.4826$.  For a strictly quadratic loss
$J(\theta) = \frac{1}{2}\theta^T H \theta - b^T\theta + c$, the Newton step is
$\theta_1 = \theta_0 - H^{-1}\nabla J(\theta_0) = \theta_0 - H^{-1}(H\theta_0 - b) = H^{-1}b
= \theta^{\star}_{\mathrm{OLS}}$, the exact minimiser.  Thus one Newton step always suffices
for a quadratic.  GD requires 80 iterations to reach comparable accuracy.}
\label{fig:q3_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_B.pdf}
\caption{Q3: Newton vs GD on Benchmark~B.  Newton converges in a few iterations because the
function is close to quadratic (the Hessian $H = \mathrm{diag}(2-\sin(x_1), 10)$ varies
slowly near the minimum).  The damped step $\alpha = 0.85$ is used because the nonlinear
$-\sin(x_1)$ term makes the full Newton step ($\alpha = 1$) slightly overshoot.}
\label{fig:q3_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_C.pdf}
\caption{Q3: Newton vs GD on Benchmark~C.  Newton with $\alpha = 0.22$ reaches $f = 0.686$ in
20 iterations --- a dramatic improvement over GD's $f = 3.732$ in 80 iterations.  The heavily
damped step ($\alpha = 0.22$) is necessary because the Rosenbrock Hessian changes rapidly
throughout the trajectory (eigenvalue ratio varies from $\sim 4$ at the start to $2508$ at the
optimum), so the full Newton step would overshoot.  Even so, Newton's curvature-scaled steps
advance along the valley far more efficiently than GD.}
\label{fig:q3_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q3: Final objective values and converged positions.}
\label{tab:q3_final}
\begin{tabular}{lccc}
\toprule
\textbf{Benchmark} & \textbf{GD $f$ (80~it.)} & \textbf{Newton $f$ (20~it.)} &
\textbf{Newton final $x$} \\
\midrule
A & 0.4827 & \textbf{0.4826} & $(3.028,\ 4.017)$ \\
B & 0.7244 & \textbf{0.7244} & $(0.582,\ 2.000)$ \\
C & 3.7319 & \textbf{0.6864} & $(0.183,\ 0.020)$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Contour Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q3_contour_B.pdf}
\caption{Q3: Newton vs GD trajectories on Benchmark~B.  Newton takes 3--4 large,
well-directed steps that account for the curvature difference between coordinates, reaching the
minimum efficiently.  GD takes many small steps; the $x_2$ component converges first (steeper
curvature $\lambda = 10$), then $x_1$ follows (shallower curvature $\lambda \approx 1.45$).}
\label{fig:q3_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q3_contour_C.pdf}
\caption{Q3: Newton vs GD trajectories on Rosenbrock.  Newton's trajectory follows the valley
floor more closely, using local curvature to step in the most productive direction.  GD with
$\alpha = 0.001$ is constrained to tiny steps to maintain stability, making negligible progress
in 80 iterations.  This stark difference illustrates the practical impact of $\kappa \approx
2508$ on gradient-based methods.}
\label{fig:q3_contour_C}
\end{figure}

\subsection{Update Magnitude Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q3_update_magnitude.pdf}
\caption{Q3: Update magnitude $\|x_{k+1} - x_k\|$ vs iteration for GD and Newton.  Newton's
magnitude drops rapidly, reflecting quadratic convergence: once near the solution, each step
reduces the error quadratically.  For Benchmark~A, Newton's update drops to near machine
precision after a single iteration (solving the quadratic exactly).  GD's magnitude decreases
at a linear rate $\rho = 1 - \mu/L$ per iteration.  On Rosenbrock, Newton maintains larger
updates than GD throughout, indicating more aggressive and productive steps despite only 20
total iterations.}
\label{fig:q3_update}
\end{figure}

\paragraph{Why Newton outperforms GD on ill-conditioned problems.}
Gradient descent is sensitive to the condition number $\kappa = L/\mu$ because it applies the
same step in all directions.  Newton's method rescales the gradient by the inverse Hessian,
effectively normalising the curvature: a step of $H^{-1}\nabla f$ has the same magnitude in
every eigendirection of $H$, regardless of the corresponding eigenvalue.  This
\emph{affine invariance} explains why Newton's convergence rate (in terms of iterations) is
independent of $\kappa$, making it far superior on Rosenbrock where $\kappa \approx 2508$.

\paragraph{Cost comparison.}
Each Newton iteration requires computing and inverting (or factoring) the $d \times d$ Hessian
at cost $O(d^3)$, in addition to one gradient evaluation.  For large $d$, this is prohibitive,
motivating \emph{quasi-Newton} methods (e.g., L-BFGS) that approximate $H^{-1}$ from gradient
differences without ever forming the full Hessian.

\subsection{Newton Convergence Phases}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q3_newton_phases.pdf}
\caption{Q3: Newton's method on Rosenbrock for 60 damped iterations ($\alpha = 0.22$).
Left: objective value decreases rapidly in the first 20 iterations, then transitions to a
slower phase as the iterate approaches the valley floor.  Right: Newton step magnitude
$\|p_k\|$ decreases, reflecting the \emph{Newton decrement} shrinking.  The clear phase
transition around iteration 20--30 illustrates the distinction between the global
(descent) phase and the local (quadratic convergence) phase.  In the local phase near the
optimum, the decrement would decrease quadratically; the damping $\alpha = 0.22$ slows this
but ensures global stability.}
\label{fig:q3_phases}
\end{figure}

\subsection{Comparison of All Methods on Rosenbrock}

Figure~\ref{fig:all_methods_C} places Newton alongside the first-order methods from Q1 and Q2
on Benchmark~C for 200 iterations (Newton shown for 20 iterations).

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q_all_methods_C.pdf}
\caption{All methods on Benchmark~C (Rosenbrock) over 200 iterations.  Newton (20~damped
iterations, $\alpha = 0.22$) substantially outperforms all first-order methods.  Nesterov
(first-order) is the best among gradient-based methods.  GD with constant step barely makes
progress.  This figure illustrates the practical impact of the $\kappa \approx 2508$ condition
number: first-order methods require $O(\kappa)$ or $O(\sqrt{\kappa})$ iterations, while
Newton's quadratic convergence is condition-number independent.}
\label{fig:all_methods_C}
\end{figure}

% ============================================================
\section{Question 4: Derivative Approximation and Derivative-Free Optimisation}
\label{sec:q4}
% ============================================================

\subsection{Part A: Finite Difference and Nesterov Random Search (Benchmark B)}

\subsubsection{Finite Difference Gradient Approximation}

The forward finite-difference approximation replaces each partial derivative:
\begin{equation}
\frac{\partial f}{\partial x_i}(x) \approx \frac{f(x + \delta e_i) - f(x)}{\delta},
\label{eq:fd}
\end{equation}
where $e_i$ is the $i$-th coordinate vector.  The total error has two components:
\begin{itemize}
\item \textbf{Truncation error:} $O(\delta)$ from Taylor expansion (the higher-order terms
  omitted in \eqref{eq:fd}).
\item \textbf{Round-off error:} $O(\varepsilon_{\mathrm{machine}} / \delta)$ from floating
  point arithmetic.
\end{itemize}
These balance at $\delta^{\star} \sim \sqrt{\varepsilon_{\mathrm{machine}}} \approx 10^{-8}$,
giving a minimum total error $\approx \sqrt{\varepsilon_{\mathrm{machine}}} \approx 10^{-8}$.
In practice, the choice $\delta \approx 0.01$--$0.1$ (truncation-error dominated) is common.

A centred difference $[f(x + \delta e_i) - f(x - \delta e_i)]/(2\delta)$ achieves $O(\delta^2)$
truncation error at the cost of $2d$ function evaluations instead of $d+1$.

Figure~\ref{fig:q4_fd_error} quantifies the gradient approximation error as a function of
$\delta$ at the starting point $x_0 = (-1, 4)$ for Benchmark~B.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q4_fd_error.pdf}
\caption{Q4: Forward finite-difference gradient error $\|\hat{g}_\delta - g\|_2$ vs step
size $\delta$ at $x_0 = (-1, 4)$ on Benchmark~B.  The error increases linearly with $\delta$
(truncation dominated), confirming $O(\delta)$ theory.  At $\delta = 0.05$ (good choice in
experiments), the error is $\approx 0.012$ --- small but non-negligible.  At $\delta = 0.8$
(poor choice), the error is $\approx 0.2$, 17$\times$ larger, explaining the degraded
convergence observed.  The optimal $\delta$ balancing truncation and round-off is near
$10^{-8}$, but practically any $\delta \in [0.001, 0.1]$ yields acceptable accuracy.}
\label{fig:q4_fd_error}
\end{figure}

\subsubsection{Nesterov Random Search}

\begin{algorithm}[H]
\caption{Nesterov Random Search (Zeroth-Order)}
\label{alg:nrs}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, perturbation $\delta$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State Sample $u_k \sim \mathcal{N}(0, I_d)$; normalise $u_k \gets u_k / \|u_k\|$
    \State $\hat{g}_k \gets \dfrac{f(x_k + \delta u_k) - f(x_k)}{\delta}\, u_k$
    \Comment{Stochastic directional derivative estimate}
    \State $x_{k+1} \gets x_k - \alpha\, \hat{g}_k$
\EndFor
\end{algorithmic}
\end{algorithm}

Nesterov random search estimates a directional derivative along a random unit vector $u_k$.
Crucially, $\mathbb{E}[\hat{g}_k] = \frac{1}{\delta}\mathbb{E}[f(x + \delta u) - f(x)]\,u
\approx \frac{1}{d}\nabla f(x)$ (the expected update is proportional to the gradient).
Only \emph{two} function evaluations are needed per iteration regardless of dimension $d$,
making per-step cost $O(1)$ compared to $O(d)$ for full gradient or finite-difference methods.
However, the effective signal-to-noise ratio decreases with $d$, since the random direction
captures only a $1/d$ fraction of the gradient.

\subsubsection{Results}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q4_fval_B.pdf}
\caption{Q4A: Convergence comparison on Benchmark~B.  Exact GD converges fastest.  FD-GD
with $\delta = 0.05$ closely tracks exact GD, confirming the gradient quality.  FD-GD with
$\delta = 0.8$ converges poorly: the step spans a region where curvature changes significantly,
introducing large truncation error.  Nesterov random search converges slowest due to high
gradient variance from projecting onto a single random direction per step.}
\label{fig:q4_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q4_contour_B.pdf}
\caption{Q4A: Trajectories on Benchmark~B.  Exact GD and good FD ($\delta = 0.05$) follow
nearly identical paths, confirming gradient fidelity.  Poor FD ($\delta = 0.8$) deviates
noticeably due to inaccurate gradient directions.  Nesterov random search follows a noisy,
wandering path; the overall drift is still towards the minimum because the expected update is
in the gradient direction.}
\label{fig:q4_contour_B}
\end{figure}

\subsection{Part B: Derivative-Free Methods on Benchmark C}

\subsubsection{Nelder--Mead Simplex Method}

The Nelder--Mead algorithm maintains a simplex of $n + 1 = 3$ vertices (for $n = 2$
dimensions) and iteratively transforms it through four operations:
\begin{enumerate}[noitemsep]
\item \textbf{Reflection} ($\alpha_r = 1$): move the worst vertex $x_w$ through the centroid
  $\bar{x}$ of the remaining vertices: $x_r = \bar{x} + \alpha_r(\bar{x} - x_w)$.
\item \textbf{Expansion} ($\gamma = 2$): if reflection improved, try farther:
  $x_e = \bar{x} + \gamma(\bar{x} - x_w)$.
\item \textbf{Contraction} ($\rho = 0.5$): if reflection did not improve, contract towards
  centroid.
\item \textbf{Shrink} ($\sigma = 0.5$): if contraction fails, contract all vertices towards
  the best vertex.
\end{enumerate}
Settings: initial simplex step $= 0.35$, iterations $= 160$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q4_nelder_mead_C.pdf}
\caption{Q4B: Nelder--Mead on Rosenbrock.  Left: the centroid trajectory navigates the curved
valley without any gradient information, adapting the simplex geometry to the local landscape.
Right: the best objective value decreases steadily over 160 iterations, with occasional
plateaus during shrink operations where all vertices collapse towards the current best point.
Final value $f \approx 10^{-3}$--$10^{-2}$, substantially closer to the optimum than GD
achieved in a comparable number of function evaluations.}
\label{fig:q4_nm}
\end{figure}

\paragraph{How Nelder--Mead adapts to Rosenbrock.}
The key strength of Nelder--Mead on Rosenbrock is its ability to \emph{orient} the simplex
along the valley.  As the algorithm progresses, the simplex elongates in the direction of
easiest descent (the valley floor) and contracts perpendicular to it.  This implicit
adaptation to the local geometry --- without any gradient information --- makes Nelder--Mead
remarkably effective on smooth, low-dimensional problems.  However, this advantage is
empirical: Nelder--Mead has no general convergence guarantee and can stagnate or cycle on
non-smooth or high-dimensional functions.  Theoretical analysis by McKinnon (1998) shows it
can converge to non-stationary points even for smooth functions in $d \geq 2$.

\paragraph{Function evaluation budget.}
With 160 iterations, the total function evaluations are at most $4 \times 160 = 640$ (one
reflection/expansion + check per iteration), plus the initial $d + 1 = 3$ evaluations.
In contrast, FD-GD requires $(d+1) \times T = 3 \times 120 = 360$ evaluations for 120
iterations.  Nelder--Mead uses somewhat more evaluations per unit of progress but is gradient-free.

\subsubsection{Grid Search}

Grid search evaluates the objective on a uniform $55 \times 55 = 3025$-point grid over
$x_1 \in [-2, 2]$, $x_2 \in [-1, 3]$.  The grid resolution is
$\Delta x_1 = 4/54 \approx 0.074$ and $\Delta x_2 = 4/54 \approx 0.074$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q4_grid_search_C.pdf}
\caption{Q4B: Grid search on Rosenbrock.  Left: the $3025$ grid points cover the search
region.  The best point found is close to the true optimum $(1, 1)$, constrained by grid
resolution $\Delta x \approx 0.074$.  Right: the best-so-far curve decreases step-wise as
better points are discovered; many evaluations find no improvement due to the highly peaked
objective.}
\label{fig:q4_grid}
\end{figure}

The grid search is guaranteed to find the global optimum to within $O(\Delta x)$ but requires
$N^d$ evaluations for an $N$-point grid in $d$ dimensions --- the \emph{curse of
dimensionality}.  For $d = 10$ dimensions at grid resolution $0.074$, the grid would need
$\approx 55^{10} \approx 2.5 \times 10^{17}$ evaluations, completely impractical.

\subsection{Computational Comparison}

\begin{table}[H]
\centering
\caption{Q4: Comparison of derivative approximation and derivative-free methods.}
\label{tab:q4_comparison}
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{Gradient} & \textbf{FE/iter} & \textbf{Scalability} &
\textbf{Accuracy} \\
\midrule
Exact GD & Full & $1$ & $O(d)$ gradient & High \\
FD-GD ($\delta$ small) & Approximated & $d + 1$ & $O(d)$ per iter & High \\
FD-GD ($\delta$ large) & Poor approx. & $d + 1$ & $O(d)$ per iter & Low \\
Nesterov random search & None & 2 & $O(1)$ per iter & Low \\
Nelder--Mead & None & $\leq 4$ & Low-$d$ only & Moderate \\
Grid search & None & $N^d$ total & Very poor ($O(N^d)$) & Resolution-limited \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Discussion.}
The methods represent a trade-off between gradient information and scalability.  When gradient
access is available and cheap (as on Benchmark B), exact GD with proper step size is
unambiguously best.  Finite differences are a practical substitute when the gradient is
unavailable but function evaluations are cheap, provided $\delta$ is chosen carefully.
Zeroth-order methods (Nesterov random search, Nelder--Mead) sacrifice per-step efficiency for
gradient-free applicability; they are valuable for black-box, simulation-based, or
non-differentiable objectives.  Grid search is only viable in very low dimensions ($d \leq 3$).

% ============================================================
\section{Question 5: Constrained Optimisation}
\label{sec:q5}
% ============================================================

\subsection{Problem Formulation}

The constrained problem is:
\begin{equation}
\min_{x \in \mathbb{R}^2}\; f_B(x) \quad \text{subject to}\quad x_1 \geq 0.5,
\label{eq:q5_problem}
\end{equation}
starting from the infeasible point $x_0 = (0.2, 4.0)$.

The feasible set $\mathcal{C} = \{x : x_1 \geq 0.5\}$ is a half-plane (a convex set).
The starting point $x_0 = (0.2, 4.0)$ is \emph{infeasible} ($0.2 < 0.5$), meaning
initialisation violates the constraint.  This tests whether the algorithms can recover
feasibility while also minimising the objective.

The unconstrained minimum $x^{\star}_{\mathrm{unc}} \approx (0.582, 2.0)$ lies \emph{inside}
the feasible set ($0.582 > 0.5$), so the constraint is \emph{inactive} at the solution: the
constrained minimiser equals the unconstrained minimiser.  This means both projected GD and
penalty methods should converge to the same point --- the practical interest lies in
\emph{how} they handle the infeasible starting point.

\paragraph{KKT conditions.}
The constrained problem~\eqref{eq:q5_problem} satisfies the KKT conditions at
$x^{\star} = (0.582, 2.0)$:
\begin{align}
\nabla f_B(x^{\star}) + \mu \nabla c(x^{\star}) &= 0, \quad c(x) = 0.5 - x_1 \leq 0, \\
\mu &\geq 0, \quad \mu c(x^{\star}) = 0.
\end{align}
Since $c(x^{\star}) = 0.5 - 0.582 < 0$ (strict feasibility), the complementary slackness
condition forces $\mu = 0$, and the KKT condition reduces to $\nabla f_B(x^{\star}) = 0$ ---
the standard unconstrained stationarity.

\subsection{Algorithms}

\begin{algorithm}[H]
\caption{Projected Gradient Descent}
\label{alg:pgd}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, projection $\Pi_{\mathcal{C}}$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_{k+1} \gets x_k - \alpha\, \nabla f(x_k)$
    \Comment{Unconstrained gradient step}
    \State $x_{k+1} \gets \Pi_{\mathcal{C}}(z_{k+1}) = (\max(0.5, z_{k+1,1}),\, z_{k+1,2})$
    \Comment{Project onto $\mathcal{C}$}
\EndFor
\end{algorithmic}
\end{algorithm}

The projection $\Pi_{\mathcal{C}}(z) = (\max(0.5, z_1), z_2)$ is closed-form for this
half-plane constraint.  Each step maintains feasibility as a \emph{hard constraint}: the
iterate is always in $\mathcal{C}$ regardless of step size.  For smooth convex $f$ and convex
$\mathcal{C}$, projected GD with $\alpha \leq 1/L$ satisfies:
\[
f(x_k) - f(x^{\star}) \leq \frac{\|x_0 - x^{\star}\|^2}{2\alpha k},
\]
the standard $O(1/k)$ rate.

\begin{algorithm}[H]
\caption{Penalty Method (Gradient Descent on Penalised Objective)}
\label{alg:penalty}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, penalty weight $\lambda > 0$, iterations $N$
\State Define $F_\lambda(x) = f(x) + \lambda\,\max(0, -x_1 + 0.5)$
\For{$k = 0, 1, \ldots, N-1$}
    \State $x_{k+1} \gets x_k - \alpha\, \nabla F_\lambda(x_k)$
    \Comment{GD on penalised objective}
\EndFor
\end{algorithmic}
\end{algorithm}

The penalty method replaces the constrained problem with an unconstrained problem by adding a
term $\lambda \max(0, -x_1 + 0.5)$ that penalises violations.  As $\lambda \to \infty$, the
solution of the penalised problem approaches the constrained solution.  However, very large
$\lambda$ increases the effective Lipschitz constant of $F_\lambda$, forcing a smaller step
size to maintain stability.

Settings: Projected GD ($\alpha = 0.08$, 100 iterations); Penalty with $\lambda \in \{0.15,
1.8, 4.5\}$ ($\alpha = 0.05$ for $\lambda \leq 1.8$, $\alpha = 0.03$ for $\lambda = 4.5$);
unconstrained GD ($\alpha = 0.07$, 100 iterations).

\subsection{Results}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q5_fval.pdf}
\caption{Q5: Objective value $f_B(x)$ vs iteration (penalties plotted without the penalty
term to show progress on the original objective).  Projected GD converges directly to the
constrained minimum $f^{\star} \approx 0.7244$.  Larger $\lambda$ drives the penalty solution
towards the same constrained minimum.  With $\lambda = 0.15$, the penalty gradient is too weak
to enforce the constraint, and the trajectory approaches an infeasible point with lower
(penalised) cost.}
\label{fig:q5_fval}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q5_contour.pdf}
\caption{Q5: Contour plot with constraint boundary $x_1 = 0.5$ (red dashed) and infeasible
region (shaded).  Starting from $(0.2, 4.0)$ (infeasible), projected GD immediately clips
$x_1$ to 0.5 and optimises from there.  Unconstrained GD crosses into the infeasible region.
Penalty trajectories are steered progressively towards the feasible side as $\lambda$ increases.}
\label{fig:q5_contour}
\end{figure}

\subsection{Constraint Violation Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q5_violation.pdf}
\caption{Q5: Constraint violation $v(x_k) = \max(0, 0.5 - x_{k,1})$ on log scale.  Left:
full 100 iterations.  Right: first 30 iterations (zoomed).  Projected GD achieves
\textbf{zero violation from iteration 1}: the projection $\max(0.5, x_1)$ enforces $x_1 \geq
0.5$ as a hard constraint at every step.  Penalty with $\lambda = 4.5$ achieves feasibility
within $\approx 10$ iterations.  With $\lambda = 0.15$, the penalty is too weak and the
trajectory remains infeasible throughout.}
\label{fig:q5_violation}
\end{figure}

\begin{table}[H]
\centering
\caption{Q5: Final positions and objective values after 100 iterations.}
\label{tab:q5_final}
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{Final $x$} & \textbf{$f_B(x)$} & \textbf{Violation} \\
\midrule
Unconstrained GD & $(0.582,\ 2.000)$ & 0.7244 & 0.0000 (inactive) \\
Projected GD & $(0.582,\ 2.000)$ & 0.7244 & \textbf{0.0000} \\
Penalty $\lambda = 0.15$ & (infeasible) & $< 0.7244$ & $> 0$ \\
Penalty $\lambda = 1.8$ & $({\approx}0.55,\ 2.000)$ & ${\approx}0.7244$ & ${\approx}0$ \\
Penalty $\lambda = 4.5$ & $(0.583,\ 2.000)$ & 0.7244 & \textbf{0.0000} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Comparative Discussion}

\textbf{Projected GD} enforces feasibility as a hard constraint at every step, with guaranteed
convergence for convex $f$ and convex $\mathcal{C}$.  The cost is requiring a projection oracle
--- trivial for half-planes and boxes ($O(d)$ per step), but expensive for general convex sets
(solving a QP, $O(d^3)$).

\textbf{Penalty methods} require only gradient computations of the augmented objective, with
no projection.  The weight $\lambda$ controls the trade-off between constraint satisfaction
and step-size stability.  A key limitation is that the penalty method can never achieve
\emph{exact} feasibility with finite $\lambda$ (since the gradient of $\max(0, c(x))$ is zero
in the feasible region); it can only approach feasibility asymptotically.

\paragraph{Augmented Lagrangian connection.}
The \emph{augmented Lagrangian} (method of multipliers) overcomes this limitation by iterating:
\begin{align}
x_{k+1} &= \arg\min_x\, f(x) + \mu_k c^+(x) + \tfrac{\lambda}{2}\|c^+(x)\|^2, \\
\mu_{k+1} &= \mu_k + \lambda\, c^+(x_{k+1}),
\end{align}
where $c^+(x) = \max(0, c(x))$.  The dual variable $\mu_k$ is updated to keep track of the
accumulated constraint violation.  Unlike the pure penalty method, the augmented Lagrangian
achieves exact feasibility even for fixed finite $\lambda$, and the conditioning of the
augmented objective does not deteriorate with $\lambda$.

In this problem, since the unconstrained minimum is inside the feasible set, the constraint
is inactive and both methods converge to the same point.  The projected GD is more
direct and achieves zero violation from the first iteration, making it the preferred method
for this problem.  For problems where the optimum lies on the boundary, projected GD may
be restricted to the boundary (the projection is ``active'' at every step) and converges
more slowly than when the optimum is in the interior.

% ============================================================
\section{Question 6: Linear Programmes and Frank--Wolfe Algorithm}
\label{sec:q6}
% ============================================================

\subsection{Feasible Set and Linear Programme}

Feasible set: $\mathcal{X} = \{x \in \mathbb{R}^2 : 0.5 \leq x_1 \leq 5,\
-5 \leq x_2 \leq 10\}$.  This is a closed convex box (polytope with four vertices).

\textbf{Linear programme:} Minimise $f(x) = a^T x = x_1 + 2x_2$ ($a = [1, 2]^T$) over
$\mathcal{X}$.  Since $a$ has strictly positive components, the minimum is attained at the
coordinate-wise lower bounds:
\begin{equation}
x^{\star} = (0.5,\ -5), \qquad f^{\star} = 0.5 + 2 \cdot (-5) = -9.5.
\end{equation}

\begin{proposition}
The minimum of a linear function $f(x) = a^T x$ over a bounded polytope is attained at a
vertex.
\end{proposition}
\begin{proof}
A linear function has no interior critical points ($\nabla f = a \neq 0$).  By the extreme
value theorem the minimum exists on the compact set $\mathcal{X}$, and since any boundary
point of a polytope that is not a vertex can be written as a convex combination of two other
boundary points, a non-vertex boundary minimiser could be improved by moving to one of those
points.
\end{proof}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q6_lp.pdf}
\caption{Q6-I: Feasible region $\mathcal{X}$ (blue box) and linear objective contours.  The
level curves of $f(x) = x_1 + 2x_2$ are parallel lines with slope $-1/2$.  The minimum is
at the lower-left vertex $(0.5, -5)$ where these lines are lowest, confirming the theoretical
prediction.}
\label{fig:q6_lp}
\end{figure}

\subsection{Frank--Wolfe (Conditional Gradient) Algorithm}

\begin{algorithm}[H]
\caption{Frank--Wolfe / Conditional Gradient}
\label{alg:fw}
\begin{algorithmic}[1]
\Require Initial feasible $x_0 \in \mathcal{X}$, mixing parameter $\beta \in (0,1)$,
         iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_k \gets \arg\min_{z \in \mathcal{X}}\; \nabla f(x_k)^T z$
    \Comment{Linear minimisation oracle (LMO)}
    \State $x_{k+1} \gets \beta\, x_k + (1 - \beta)\, z_k$
    \Comment{Convex combination: maintains feasibility}
\EndFor
\end{algorithmic}
\end{algorithm}

For box constraints, the LMO decomposes per coordinate:
\begin{equation}
(z_k)_i = \begin{cases} l_i & \text{if } (\nabla f(x_k))_i > 0 \\ u_i & \text{if }
(\nabla f(x_k))_i \leq 0 \end{cases}
\end{equation}
requiring only $O(d)$ work per iteration, \emph{no projection}.  The key property is that
since both $x_k \in \mathcal{X}$ and $z_k \in \mathcal{X}$, and $\mathcal{X}$ is convex, the
convex combination $x_{k+1} = \beta x_k + (1-\beta) z_k \in \mathcal{X}$ automatically.

\begin{theorem}[Frank--Wolfe convergence, O(1/k)]
For a convex $L$-smooth $f$ over a compact convex set $\mathcal{X}$ with diameter $D$, and
the step-size schedule $\gamma_k = 2/(k+2)$:
\[
f(x_k) - f^{\star} \leq \frac{2 L D^2}{k + 2}.
\]
\end{theorem}
This $O(1/k)$ rate is slower than projected GD's $O(1/k)$ rate in iterations, but the
per-iteration cost is only $O(d)$ vs $O(d)$ for projection onto a box --- the advantage of
Frank--Wolfe lies in problems where the LMO is much cheaper than the projection oracle (e.g.,
nuclear norm constraints, simplex constraints).

Note: in the implementation, a fixed $\beta \in (0,1)$ is used as the mixing parameter,
corresponding to $(1-\beta)$ fraction moved towards $z_k$ at each step.  The standard
decreasing schedule $\gamma_k = 2/(k+2)$ (corresponding to $(1-\beta_k)$ in this notation)
guarantees the $O(1/k)$ rate; fixed $\beta$ does not.  Fixed $\beta$ close to 1 (small steps)
leads to smoother convergence but slower initial progress and may not converge to exact optimum.

\paragraph{Frank--Wolfe gap.}
A useful primal convergence certificate is the \emph{Frank--Wolfe gap} at iteration $k$:
\[
g(x_k) = \nabla f(x_k)^T(x_k - z_k) = \max_{z \in \mathcal{X}} \nabla f(x_k)^T(x_k - z),
\]
which satisfies $g(x_k) \geq f(x_k) - f^{\star}$ for convex $f$.  Thus when $g(x_k)$ is
small, the current iterate is provably near-optimal.  Computing $g(x_k)$ requires only one
additional LMO call (which returns $z_k$ anyway), making it a practical convergence criterion.

\subsection{Interior Optimum: $f(x) = (x_1 - 1)^2 + (x_2 - 5)^2$}

The unconstrained minimum $(1, 5)$ lies strictly inside $\mathcal{X}$ (since
$0.5 < 1 < 5$ and $-5 < 5 < 10$).  Two mixing parameters are tested: $\beta = 0.90$
(10\% of each step moves towards the LP vertex $z_k$) and $\beta = 0.985$ (only 1.5\% moves).

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q6_fw_convergence.pdf}
\caption{Q6-II: Frank--Wolfe convergence for the interior optimum.  With $\beta = 0.985$
(smaller steps towards $z_k$), the final objective ($f = 0.0046$) is lower than with
$\beta = 0.90$ ($f = 0.0089$) after 180 iterations.  Smaller $(1-\beta)$ reduces the magnitude
of the oscillations caused by $z_k$ jumping between opposite vertices of $\mathcal{X}$ as the
gradient direction fluctuates.}
\label{fig:q6_fw_conv}
\end{figure}

\subsection{Boundary Optimum: $f(x) = x_1^2 + x_2^2$}

The unconstrained minimum $(0, 0)$ lies outside $\mathcal{X}$ (since $x_1 \geq 0.5$).
The constrained minimum is at $(0.5, 0)$, a boundary point, with $f^{\star} = 0.25$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q6_fw_contour.pdf}
\caption{Q6: Frank--Wolfe trajectories.  Left (interior optimum): trajectories from $(1,1)$
converge towards $(1, 5)$.  The zig-zag pattern reflects $z_k$ alternating between different
vertices.  Right (boundary optimum): trajectory from $(3,3)$ converges towards $(0.5, 0)$,
approaching the boundary naturally without explicit projection.}
\label{fig:q6_fw_contour}
\end{figure}

\subsection{Evolution of $x_k$ and $z_k$}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q6_evolution.pdf}
\caption{Q6: Evolution of iterates $x_k$ and LP vertices $z_k$.  Top-left ($\beta = 0.90$,
interior): $x_k$ converges rapidly but $z_k$ oscillates between extreme vertices as the
gradient direction fluctuates near the interior minimum.  Top-right ($\beta = 0.985$): slower,
smoother convergence.  Bottom-left (boundary, $\beta = 0.93$): $x_{k,1} \to 0.5$ and
$x_{k,2} \to 0$ as the constraint is approached.  Bottom-right: boundary case converges
to $f = 0.364$ after 140 iterations (above $f^{\star} = 0.25$); more iterations are needed.}
\label{fig:q6_evolution}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q6_fw_gap.pdf}
\caption{Q6-II: Frank--Wolfe convergence and duality gap for the interior optimum.  Left:
objective $f(x_k)$ vs iteration for $\beta = 0.90$ and $\beta = 0.985$.  Right: the
Frank--Wolfe duality gap $g(x_k) = \nabla f(x_k)^T(x_k - z_k)$, which provides a
\emph{certificate} of sub-optimality.  At any iteration, $f(x_k) - f^{\star} \leq g(x_k)$.
Both $\beta$ values show gaps decreasing but oscillating, consistent with the $O(1/k)$ rate.
With $\beta = 0.90$ the gap exhibits more pronounced oscillations because larger steps cause
more movement of the LP vertex $z_k$ between boundary vertices.}
\label{fig:q6_fw_gap}
\end{figure}

\begin{table}[H]
\centering
\caption{Q6: Final Frank--Wolfe positions and objective values after the specified iterations.}
\label{tab:q6_final}
\begin{tabular}{lcccc}
\toprule
\textbf{Case} & \textbf{Iterations} & \textbf{Final $x$} & \textbf{$f(x)$} &
\textbf{$f^{\star}$} \\
\midrule
Interior, $\beta = 0.90$ & 180 & $(1.066,\ 4.932)$ & 0.0089 & 0.0 \\
Interior, $\beta = 0.985$ & 180 & $(0.997,\ 5.067)$ & 0.0046 & 0.0 \\
Boundary, $\beta = 0.93$ & 140 & $(0.500,\ -0.337)$ & 0.3637 & 0.25 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Interior vs Boundary Behaviour}

\paragraph{Interior optimum.}
When the optimum lies strictly inside $\mathcal{X}$, the gradient near the optimum is small
and the LP vertex $z_k$ jumps erratically between opposite vertices of $\mathcal{X}$ (a
vertex-chasing phenomenon).  The iterates average out these jumps through the convex
combination.  The $O(1/k)$ Frank--Wolfe convergence is slower than projected GD's $O(1/k)$
by a factor, but both achieve the same asymptotic rate.

\paragraph{Boundary optimum.}
When the optimum lies on the boundary, Frank--Wolfe is naturally suited: the LP oracle
$z_k$ will repeatedly select the boundary vertex nearest the optimum once the gradient points
towards the infeasible region.  The iterate converges to the constrained optimum without
explicit projection.  However, the fixed $\beta = 0.93$ schedule does not decrease sufficiently
fast, leaving residual error ($f = 0.364$ vs $f^{\star} = 0.25$).  Using the standard decaying
schedule $\gamma_k = 2/(k+2)$ would correct this.

\paragraph{Comparison with projected gradient descent.}
Both methods maintain feasibility and achieve $O(1/k)$ convergence for smooth convex $f$.
The advantage of Frank--Wolfe is its \emph{projection-free} nature: it requires only a linear
minimisation oracle (solvable in $O(d)$ for boxes), while projection can require $O(d^3)$ for
complex convex sets.  Frank--Wolfe also produces \emph{sparse iterates} --- since $x_{k+1}$
is a convex combination of a small number of vertices, the iterate has at most $k+1$ nonzero
``vertex weights'' in its history.  This is a key practical advantage in:
\begin{itemize}[noitemsep]
\item \textbf{Low-rank matrix optimisation}: the nuclear norm ball's LMO is a rank-1 SVD,
  while projection requires a full truncated SVD.
\item \textbf{Sparse optimisation} (Lasso): the $\ell_1$ ball's LMO is $O(d)$ (argmax of
  absolute values), while projection is $O(d \log d)$ (sorting for water-filling).
\item \textbf{Large-scale ML}: when the constraint set is a probability simplex (e.g.,
  boosting, attention mechanisms), the LMO is $O(d)$ (argmin), while projection
  requires $O(d \log d)$.
\end{itemize}

\paragraph{Limitation: linear convergence rate.}
A key weakness of Frank--Wolfe is that it cannot achieve a linear (geometric) convergence
rate even for strongly convex $f$ over a polytope, due to the ``zigzag'' phenomenon near the
optimum (the LP vertex $z_k$ alternates between two corners).  Projected gradient descent
and quasi-Newton methods achieve linear rates $O(e^{-\mu k / L})$ for strongly convex
problems.  This makes Frank--Wolfe primarily attractive when projection is expensive, not
when fast convergence to high accuracy is required.

% ============================================================
\section{Summary and Conclusions}
\label{sec:conclusions}
% ============================================================

This report has systematically investigated six families of optimisation algorithms across
three benchmark functions of increasing difficulty.  The key conclusions are as follows.

\subsection{Per-Question Summary}

\paragraph{Q1: Adaptive Step-Size Methods.}
The Polyak step size achieves the best convergence when $f^{\star}$ is known exactly
(Rosenbrock: $f = 0.0094$ in 120 iterations) but diverges catastrophically when $f^{\star}$ is
misspecified.  Adagrad and Heavy Ball are robust alternatives that consistently match or
outperform constant-step GD.  RMSprop's exponential forgetting provides smoother convergence
but can cause it to lose the accumulated gradient history that helps Adagrad on
ill-conditioned problems.

\paragraph{Q2: Momentum and Stochastic Methods.}
Nesterov acceleration's theoretical $O(1/k^2)$ advantage over GD's $O(1/k)$ translates to
substantial empirical gains on the ill-conditioned Rosenbrock function ($f = 0.479$ vs
$f = 3.388$ for GD).  Adam, which combines momentum with adaptive scaling, performs well on
all benchmarks but falls behind Nesterov on Rosenbrock.  Mini-batch SGD reveals the
bias-variance trade-off inherent in batch size selection: small batches provide more updates
per epoch but higher gradient variance, and the optimal batch size depends on the signal-to-noise
ratio of the data.

\paragraph{Q3: Newton's Method.}
Newton's method demonstrates its quadratic convergence: one step suffices for the quadratic
benchmark, and only 20 heavily-damped steps are needed to dramatically outperform 80 steps of
GD on Rosenbrock ($f = 0.686$ vs $f = 3.732$).  The affine invariance of Newton's method
explains its condition-number independence, in stark contrast to GD which degrades as
$O(\kappa^{-k})$.

\paragraph{Q4: Derivative-Free Methods.}
Finite differences with appropriate step size $\delta$ closely approximate exact GD gradients.
Poor $\delta$ choice introduces significant truncation error.  Zeroth-order methods (Nesterov
random search, Nelder--Mead) converge more slowly but are applicable to black-box functions.
Grid search is only feasible in very low dimensions due to the curse of dimensionality.

\paragraph{Q5: Constrained Optimisation.}
Projected gradient descent enforces feasibility as a hard constraint from the first iteration,
making it the gold standard when a projection oracle is available.  Penalty methods offer a
simpler implementation but require careful tuning of the penalty weight $\lambda$: too small
leaves constraints violated, too large degrades step-size stability.

\paragraph{Q6: Frank--Wolfe.}
Frank--Wolfe's projection-free nature and $O(1/k)$ convergence make it attractive for
problems with computationally expensive projections.  Its characteristic zig-zag behaviour
near interior optima is a consequence of the LP oracle selecting extreme vertices; smaller
step sizes mitigate this.  A decaying step-size schedule $\gamma_k = 2/(k+2)$ is recommended
for better practical convergence guarantees.

\subsection{Theoretical Convergence Rates Summary}

Table~\ref{tab:rates} summarises the convergence guarantees for the methods studied.

\begin{table}[H]
\centering
\caption{Convergence rates and per-iteration costs for all methods studied.
$L$: smoothness constant, $\mu$: strong convexity, $\kappa = L/\mu$, $d$: dimension,
$D$: constraint set diameter, $b$: batch size.}
\label{tab:rates}
\begin{tabular}{lllc}
\toprule
\textbf{Method} & \textbf{Rate (convex)} & \textbf{Rate (strongly convex)} &
\textbf{Cost/iter} \\
\midrule
GD (fixed $\alpha$) & $O(L\|x_0-x^*\|^2/k)$ & $O(e^{-\mu k/L})$ & $O(d)$ \\
Polyak step ($f^*$ known) & --- & $O(e^{-\mu k/L})$ & $O(d)$ \\
Adagrad & $O(\sqrt{T})$ regret & (geometry-adaptive) & $O(d)$ \\
Heavy Ball & $O(e^{-k/\sqrt{\kappa}})$ & $O(e^{-k/\sqrt{\kappa}})$ & $O(d)$ \\
Nesterov & $O(L\|x_0-x^*\|^2/k^2)$ & $O(e^{-k/\sqrt{\kappa}})$ & $O(d)$ \\
Adam & $O(1/\sqrt{T})$ regret & (heuristic, no proof) & $O(d)$ \\
Newton (damped) & --- & $O(e^{-ck^2})$ locally & $O(d^3)$ \\
FD-GD ($\delta$ small) & $O(L\|x_0-x^*\|^2/k)$ & $O(e^{-\mu k/L})$ & $O(d^2)$ \\
Nesterov rand.\ search & $O(d/\sqrt{k})$ & --- & $O(1)$ \\
Nelder--Mead & (no general guarantee) & --- & $O(1)$ \\
Projected GD & $O(1/k)$ & $O(e^{-\mu k/L})$ & $O(d)$ + proj \\
Frank--Wolfe & $O(LD^2/k)$ & (no improvement) & $O(d)$ LMO \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Overall Observations}

Table~\ref{tab:overall} summarises the best-performing method on each benchmark.

\begin{table}[H]
\centering
\caption{Best method per benchmark across all questions tested.}
\label{tab:overall}
\begin{tabular}{lll}
\toprule
\textbf{Benchmark} & \textbf{Best first-order method} & \textbf{Best overall} \\
\midrule
A (quadratic, $\kappa \approx 1.12$) & Any (well-conditioned) & Newton (1 iteration) \\
B (near-quadratic, $\kappa \leq 10$) & Nesterov / Adam & Newton ($\sim$3 iterations) \\
C (Rosenbrock, $\kappa \approx 2508$) & Nesterov (Polyak with known $f^{\star}$) &
Newton (damped) \\
\bottomrule
\end{tabular}
\end{table}

The overarching principle is: \emph{the harder the problem (higher $\kappa$, more nonlinearity,
more active constraints), the more it rewards methods that exploit additional problem
structure} --- curvature (Newton), momentum (Nesterov), or constraint geometry (Frank--Wolfe).
When such structure is unavailable or too expensive to exploit, robust adaptive methods
(Adagrad, RMSprop, Adam) provide practical, reliable performance with minimal tuning.

% ============================================================
\newpage
\appendix
\section{Python Code}
\label{app:code}
% ============================================================

The complete implementation is in \texttt{CS7DS2\_Final\_Code\_25336453.py}.
Key algorithm implementations are reproduced below.

\begin{lstlisting}[caption={Gradient Descent}]
def gradient_descent(grad_fn, loss_fn, x0, alpha, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for _ in range(n_iters):
        x = x - alpha * grad_fn(x)
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Polyak Step Size}]
def polyak_step(grad_fn, loss_fn, x0, f_star, eps, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        alpha_k = (loss_fn(x) - f_star) / (np.dot(g, g) + eps)
        alpha_hist.append(alpha_k)
        x = x - alpha_k * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Adagrad}]
def adagrad(grad_fn, loss_fn, x0, alpha0, eps, n_iters):
    x = x0.copy().astype(float)
    G = np.zeros_like(x)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        G += g**2
        eff_alpha = alpha0 / (np.sqrt(G) + eps)
        alpha_hist.append(np.mean(eff_alpha))
        x = x - eff_alpha * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={RMSprop}]
def rmsprop(grad_fn, loss_fn, x0, alpha0, beta, eps, n_iters):
    x = x0.copy().astype(float)
    v = np.zeros_like(x)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        v = beta * v + (1 - beta) * g**2
        eff_alpha = alpha0 / (np.sqrt(v) + eps)
        alpha_hist.append(np.mean(eff_alpha))
        x = x - eff_alpha * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Heavy Ball / Polyak Momentum}]
def heavy_ball(grad_fn, loss_fn, x0, alpha, beta, n_iters):
    x = x0.copy().astype(float)
    z = np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for _ in range(n_iters):
        g = grad_fn(x)
        z = beta * z + alpha * g
        x = x - z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Nesterov Momentum}]
def nesterov_momentum(grad_fn, loss_fn, x0, alpha, beta_max, n_iters):
    x = x0.copy().astype(float)
    z = np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for k in range(1, n_iters + 1):
        beta_k = min((k - 1) / (k + 2), beta_max)
        lookahead = x + beta_k * z
        g = grad_fn(lookahead)
        z = beta_k * z - alpha * g
        x = x + z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Adam Optimiser}]
def adam_optimiser(grad_fn, loss_fn, x0, alpha, beta1, beta2, eps, n_iters):
    x = x0.copy().astype(float)
    m_vec, v_vec = np.zeros_like(x), np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for t in range(1, n_iters + 1):
        g = grad_fn(x)
        m_vec = beta1 * m_vec + (1 - beta1) * g
        v_vec = beta2 * v_vec + (1 - beta2) * g**2
        m_hat = m_vec / (1 - beta1**t)
        v_hat = v_vec / (1 - beta2**t)
        x = x - alpha * m_hat / (np.sqrt(v_hat) + eps)
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Newton's Method with Tikhonov Damping}]
def newtons_method(grad_fn, hess_fn, loss_fn, x0, alpha, n_iters, damping=1e-8):
    x = x0.copy().astype(float)
    x_hist, f_hist, update_norms = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        H = hess_fn(x) if callable(hess_fn) else hess_fn
        H_reg = H + damping * np.eye(len(x))
        try:
            p = np.linalg.solve(H_reg, g)
        except np.linalg.LinAlgError:
            p = g
        update_norms.append(np.linalg.norm(alpha * p))
        x = x - alpha * p
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(update_norms)
\end{lstlisting}

\begin{lstlisting}[caption={Frank--Wolfe (Conditional Gradient)}]
def frank_wolfe(grad_fn, loss_fn, x0, bounds, beta, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist, z_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        z = np.array([bounds[i][0] if g[i] > 0 else bounds[i][1]
                       for i in range(len(g))])
        z_hist.append(z.copy())
        x = beta * x + (1 - beta) * z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(z_hist)
\end{lstlisting}

\end{document}
