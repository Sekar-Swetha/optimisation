\documentclass[11pt,a4paper]{article}
\usepackage[margin=2.2cm]{geometry}
\usepackage{graphicx}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{float}
\usepackage{hyperref}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{listings}
\usepackage{fancyhdr}
\usepackage{array}
\usepackage{multirow}

\hypersetup{colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue}

% Theorem environments
\newtheorem{theorem}{Theorem}
\newtheorem{proposition}{Proposition}
\theoremstyle{remark}
\newtheorem{remark}{Remark}

% Code listing style
\lstset{
  language=Python,
  basicstyle=\ttfamily\scriptsize,
  keywordstyle=\color{blue},
  commentstyle=\color{gray},
  stringstyle=\color{red},
  numbers=left,
  numberstyle=\tiny\color{gray},
  breaklines=true,
  frame=single,
  captionpos=b
}

\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small CS7DS2 -- Final Assignment}
\fancyhead[R]{\small Swetha Sekar (25336453)}
\fancyfoot[C]{\thepage}

\title{%
  \includegraphics[width=0.25\textwidth]{tcd_logo.jpg}\\[1em]
  \textbf{CS7DS2 -- Optimisation Algorithms for Data Analysis}\\[0.5em]
  \Large Final Assignment Report
}
\author{Swetha Sekar \\ Student ID: 25336453 \\ School of Computer Science and Statistics \\ Trinity College Dublin}
\date{April 2026}

\begin{document}
\maketitle
\thispagestyle{empty}

\begin{abstract}
This report presents a comprehensive comparative study of optimisation algorithms applied to three benchmark problems of increasing difficulty: Benchmark A (linear regression MSE, $\kappa \approx 1.117$), Benchmark B (toy neural network with sinusoidal term, $\kappa \approx 3.5$--$6.9$), and Benchmark C (Rosenbrock function, $\kappa \approx 2504$ at the optimum). Six categories of methods are investigated: (1)~adaptive step-size methods (Polyak, Adagrad, RMSprop, Heavy Ball momentum); (2)~momentum-based and stochastic methods (Nesterov acceleration, Adam, mini-batch SGD); (3)~Newton's method with second-order local quadratic approximations; (4)~derivative-free approaches (forward and central finite differences, Nesterov random search, Nelder--Mead simplex, grid search); (5)~constrained optimisation via projected gradient descent and penalty methods with KKT analysis; and (6)~the Frank--Wolfe conditional gradient algorithm with explicit convergence bounds. Quantitative key findings include: Polyak step diverges on Benchmarks A/B (misspecified $f^{\star}=0$ vs.\ true $f^{\star} \approx 0.48$) but achieves $f = 0.0094$ on Rosenbrock where $f^{\star}=0$ is exact; Nesterov acceleration achieves $f = 0.479$ on Rosenbrock versus GD's $f = 3.388$ (7$\times$ improvement); Newton's method solves quadratic Benchmark A in one iteration and reaches $f = 0.686$ on Rosenbrock in only 20 damped steps versus GD's $f = 3.732$ in 80 steps; finite-difference gradients with $\delta=0.05$ closely track exact gradients (within 3\% gradient error) while $\delta=0.8$ incurs $O(0.8)$ truncation error; the KKT analysis reveals the Q5 constraint is inactive at the optimum ($\mu^{\star}=0$), so projected GD and unconstrained GD converge to the same point; and the Frank--Wolfe bound $2LD^2/(k+2) = 981/(k+2)$ is $60\times$ larger than the observed suboptimality, illustrating the looseness of worst-case bounds. The report includes 45 figures and a Common Pitfalls section identifying seven practical failure modes.
\end{abstract}

\tableofcontents
\listoffigures
\listoftables
\newpage

% ============================================================
\section{Mathematical Notation}
\label{sec:notation}
% ============================================================

Table~\ref{tab:notation} defines the mathematical symbols used throughout this report.

\begin{table}[H]
\centering
\caption{Mathematical notation used throughout the report.}
\label{tab:notation}
\begin{tabular}{cl}
\toprule
\textbf{Symbol} & \textbf{Definition} \\
\midrule
$f : \mathbb{R}^d \to \mathbb{R}$ & Objective function to be minimised \\
$x^{\star}$ & (Local or global) minimiser of $f$ \\
$f^{\star}$ & Optimal function value $f(x^{\star})$ \\
$\nabla f(x)$ & Gradient of $f$ at $x$ \\
$\nabla^2 f(x)$ & Hessian matrix of $f$ at $x$ \\
$\alpha,\, \alpha_k$ & Step size (fixed or iteration-dependent) \\
$\beta,\, \beta_k$ & Momentum coefficient \\
$L$ & Lipschitz constant of $\nabla f$ (smoothness) \\
$\mu$ & Strong convexity constant \\
$\kappa = L/\mu$ & Condition number \\
$\varepsilon$ & Numerical tolerance / stabiliser \\
$\odot$ & Element-wise (Hadamard) product \\
$\Pi_{\mathcal{X}}$ & Euclidean projection onto feasible set $\mathcal{X}$ \\
$\mathcal{B}$ & Mini-batch index set \\
$m$ & Number of data points \\
\bottomrule
\end{tabular}
\end{table}

% ============================================================
\section{Benchmark Problems and Experimental Setup}
\label{sec:benchmarks}
% ============================================================

All experiments use a fixed random seed (\texttt{np.random.seed(42)}) for full reproducibility.

\subsection{Benchmark A: Linear Regression Quadratic Loss}
A synthetic dataset with $m = 1000$ samples and two parameters is generated:
\begin{equation}
\theta^{\star} = \begin{bmatrix} 3 \\ 4 \end{bmatrix}, \quad x^{(i)} \sim \mathcal{N}(0, I_2), \quad y^{(i)} = {\theta^{\star}}^T x^{(i)} + \varepsilon^{(i)}, \quad \varepsilon^{(i)} \sim \mathcal{N}(0, 1).
\end{equation}
The loss function is the mean squared error:
\begin{equation}
J(\theta) = \frac{1}{2m} \sum_{i=1}^{m} \left(\theta^T x^{(i)} - y^{(i)}\right)^2 = \frac{1}{2m}\|X\theta - y\|^2.
\end{equation}
This is a \emph{strongly convex quadratic} with constant Hessian $H = \frac{1}{m}X^T X \succ 0$. The gradient is $\nabla J(\theta) = \frac{1}{m}X^T(X\theta - y)$. Since $X$ has i.i.d.\ $\mathcal{N}(0,1)$ entries and $m = 1000 \gg d = 2$, by the law of large numbers $H \approx I_2$, giving $\lambda_{\min}(H) \approx 0.925$, $\lambda_{\max}(H) \approx 1.033$, and condition number $\kappa \approx 1.117$. This near-isotropic curvature ($\kappa \approx 1$) explains why all first-order methods converge rapidly on Benchmark A. The OLS minimiser $\theta^{\star}_{\mathrm{OLS}} = (X^TX)^{-1}X^Ty \approx (3.028, 4.017)$ approximates the true parameter $(3, 4)$. Due to observation noise ($\sigma = 1$), the minimum loss is $J^{\star} \approx 0.4826 \neq 0$. Starting point: $\theta_0 = [0, 0]^T$.

\subsection{Benchmark B: Toy Neural Network Quadratic Loss}
\begin{equation}
f(x_1, x_2) = (x_1 - 1)^2 + 5(x_2 - 2)^2 + \sin(x_1).
\end{equation}
The gradient is $\nabla f = [2(x_1 - 1) + \cos(x_1),\ 10(x_2 - 2)]^T$ and the Hessian is $H(x) = \mathrm{diag}(2 - \sin(x_1),\ 10)$. Since $\sin(x_1) \in [-1, 1]$, the diagonal element $2 - \sin(x_1) \in [1, 3]$, giving a condition number that varies between approximately $10/3 \approx 3.3$ and $10/1 = 10$ across the domain. At the minimum ($x_1^{\star} \approx 0.5823$), $\kappa \approx 10/(2 - \sin(0.5823)) \approx 10/1.452 \approx 6.9$, representing moderate anisotropy. The minimum lies at approximately $(0.5824,\, 2.0)$ with $f^{\star} \approx 0.7244$, obtained by solving $2(x_1-1)+\cos(x_1)=0$ numerically (the exact root $x_1^{\star} \approx 0.5824$ satisfies the stationarity condition to five decimal places). Starting point: $x_0 = [-1, 4]^T$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q_condition_number_B.pdf}
\caption{Benchmark B: spatial map of the Hessian condition number $\kappa(x) = \lambda_{\max}/\lambda_{\min}$ (left) and $\kappa$ along the valley floor $x_2 = 2$ (right). Since $H = \mathrm{diag}(2 - \sin(x_1), 10)$, the condition number depends only on $x_1$ through the $\sin$ term. At the optimum $x_1^{\star} \approx 0.582$, $\kappa \approx 6.9$. At the starting point $x_1^0 = -1$, $\sin(-1) \approx -0.841$, so $\kappa \approx 10/(2+0.841) \approx 3.5$. This spatially varying condition number means gradient descent's effective convergence rate changes along the trajectory.}
\label{fig:cond_B}
\end{figure}

\subsection{Benchmark C: Rosenbrock Function}
\begin{equation}
f(x_1, x_2) = (1 - x_1)^2 + 100(x_2 - x_1^2)^2.
\end{equation}
The global minimum is at $(1, 1)$ with $f^{\star} = 0$. The Hessian is:
\begin{equation}
\nabla^2 f(x) = \begin{bmatrix} 2 + 1200x_1^2 - 400x_2 & -400x_1 \\ -400x_1 & 200 \end{bmatrix}.
\end{equation}
At the optimum $(1,1)$: $\nabla^2 f(1,1) = \begin{bmatrix} 802 & -400 \\ -400 & 200 \end{bmatrix}$, with eigenvalues satisfying $\lambda^2 - 1002\lambda + 400 = 0$, yielding $\lambda_1 \approx 1001.6$ and $\lambda_2 \approx 0.4$, so the condition number $\kappa = \lambda_1/\lambda_2 \approx 2504$. This extreme ill-conditioning, combined with the narrow curved valley along $x_2 = x_1^2$, makes the Rosenbrock one of the most challenging standard test functions. Away from the optimum the Hessian is not positive definite, which has critical implications for Newton's method. Starting point: $x_0 = [-1, 1]^T$.

\subsection{Benchmark D: Constrained Feasible Set}
For constrained problems (Questions~5--6): $\mathcal{X} = \{x \in \mathbb{R}^2 : 0.5 \leq x_1 \leq 5,\ -5 \leq x_2 \leq 10\}$.

% ============================================================
\section{Question 1: Adaptive Step-Size Methods \hfill [30 marks]}
\label{sec:q1}
% ============================================================

\subsection{Algorithm Formulations}

Four adaptive methods are compared against a constant step-size gradient descent baseline across all three benchmarks for 120 iterations. The fundamental limitation of standard gradient descent with fixed step $\alpha$---that $\alpha$ must satisfy $\alpha < 2/L$ globally yet a small $\alpha$ implies slow convergence---motivates methods that adapt the step to local curvature information.

\begin{algorithm}[H]
\caption{Polyak Step Size}
\label{alg:polyak}
\begin{algorithmic}[1]
\Require Initial point $x_0$, optimal value $f^{\star}$, tolerance $\varepsilon$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $\alpha_k \gets \dfrac{f(x_k) - f^{\star}}{\|g_k\|^2 + \varepsilon}$
    \State $x_{k+1} \gets x_k - \alpha_k\, g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

The Polyak step is motivated by the Cauchy--Schwarz inequality applied to the subgradient inequality:
\begin{equation}
f(x_k) - f^{\star} \leq \langle \nabla f(x_k),\, x_k - x^{\star} \rangle \leq \|\nabla f(x_k)\|\,\|x_k - x^{\star}\|,
\end{equation}
which suggests choosing $\alpha_k = (f(x_k)-f^{\star})/\|\nabla f(x_k)\|^2$ to maximally reduce the distance to the optimum in one step. Specifically, one can show:
\begin{equation}
\|x_{k+1} - x^{\star}\|^2 = \|x_k - \alpha_k \nabla f(x_k) - x^{\star}\|^2 \leq \|x_k - x^{\star}\|^2 - \frac{(f(x_k)-f^{\star})^2}{\|\nabla f(x_k)\|^2}.
\end{equation}
For strongly convex functions ($\mu > 0$) with known $f^{\star}$, this yields $\|x_{k+1} - x^{\star}\|^2 \leq (1-\mu/L)\|x_k - x^{\star}\|^2 = (1-1/\kappa)\|x_k - x^{\star}\|^2$---a linear convergence rate independent of the absolute Lipschitz constant $L$.

\begin{algorithm}[H]
\caption{Adagrad}
\label{alg:adagrad}
\begin{algorithmic}[1]
\Require Initial point $x_0$, base learning rate $\alpha_0$, tolerance $\varepsilon$, iterations $N$
\State $G_0 \gets \mathbf{0}$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $G_{k+1} \gets G_k + g_k \odot g_k$ \Comment{Per-coordinate cumulative sum of squared gradients}
    \State $x_{k+1} \gets x_k - \dfrac{\alpha_0}{\sqrt{G_{k+1}} + \varepsilon} \odot g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

Adagrad adapts the learning rate per coordinate based on the cumulative history of squared gradients. Coordinates with consistently large gradients receive smaller effective steps, while rare features receive larger steps. The effective step size $\alpha_k^{(i)} = \alpha_0/\sqrt{\sum_{j=0}^{k} g_j^{(i)2}}$ is \emph{monotonically decreasing}, which ensures stability but can cause premature stagnation.

\begin{algorithm}[H]
\caption{RMSprop}
\label{alg:rmsprop}
\begin{algorithmic}[1]
\Require Initial point $x_0$, learning rate $\alpha_0$, decay $\beta$, tolerance $\varepsilon$, iterations $N$
\State $v_0 \gets \mathbf{0}$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $v_{k+1} \gets \beta\, v_k + (1 - \beta)\, g_k \odot g_k$ \Comment{Exponential moving average of squared gradients}
    \State $x_{k+1} \gets x_k - \dfrac{\alpha_0}{\sqrt{v_{k+1}} + \varepsilon} \odot g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

RMSprop addresses Adagrad's vanishing step size by replacing the cumulative sum with an exponential moving average (decay $\beta = 0.9$). The effective memory horizon is approximately $1/(1-\beta) = 10$ recent gradient observations, allowing the step size to recover when gradients diminish.

\begin{algorithm}[H]
\caption{Polyak Momentum / Heavy Ball}
\label{alg:heavyball}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, momentum $\beta$, iterations $N$
\State $z_0 \gets \mathbf{0}$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_{k+1} \gets \beta\, z_k + \alpha\, \nabla f(x_k)$
    \State $x_{k+1} \gets x_k - z_{k+1}$
\EndFor
\end{algorithmic}
\end{algorithm}

The Heavy Ball method accumulates a velocity term $z_k$ that damps oscillations across the valley and accelerates along persistent gradient directions. For a strongly convex quadratic with condition number $\kappa$, the optimal Heavy Ball parameters yield a convergence rate of $\left(\frac{\sqrt{\kappa}-1}{\sqrt{\kappa}+1}\right)^2$ per iteration, significantly faster than gradient descent's rate of $\left(\frac{\kappa-1}{\kappa+1}\right)^2$.

\subsection{Hyperparameter Settings}

\begin{table}[H]
\centering
\caption{Q1: Hyperparameter settings for each method and benchmark. All methods run for 120 iterations.}
\label{tab:q1_params}
\begin{tabular}{llccc}
\toprule
\textbf{Method} & \textbf{Parameter} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & $\alpha$ & 0.08 & 0.06 & 0.0012 \\
\midrule
Polyak & $f^{\star}$ & 0 & 0 & 0 \\
 & $\varepsilon$ & $10^{-4}$ & $10^{-4}$ & $10^{-3}$ \\
\midrule
Adagrad & $\alpha_0$ & 1.8 & 1.2 & 0.45 \\
 & $\varepsilon$ & $10^{-5}$ & $10^{-5}$ & $10^{-5}$ \\
\midrule
RMSprop & $\alpha_0$ & 0.22 & 0.14 & 0.0035 \\
 & $\beta$ & 0.9 & 0.9 & 0.9 \\
 & $\varepsilon$ & $10^{-5}$ & $10^{-5}$ & $10^{-5}$ \\
\midrule
Heavy Ball & $\alpha$ & 0.045 & 0.035 & 0.0008 \\
 & $\beta$ & 0.88 & 0.90 & 0.86 \\
\bottomrule
\end{tabular}
\end{table}

The conservative step sizes on Benchmark C ($\alpha = 0.0012$ for GD) are necessitated by the large Lipschitz constant $L \approx \lambda_{\max}(\nabla^2 f)$ near the starting point $x_0 = (-1, 1)$: at this point $\nabla^2 f(-1,1) = \begin{bmatrix}1602 & 400 \\ 400 & 200\end{bmatrix}$, so stability requires $\alpha < 2/\lambda_{\max} \approx 2/1680 \approx 0.0012$.

\subsection{Convergence Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_A.pdf}
\caption{Q1: Convergence on Benchmark A (Linear Regression). GD, Adagrad, and Heavy Ball all converge to the irreducible noise floor $J^{\star} \approx 0.4826$. RMSprop converges to a slightly elevated value (0.5027) due to oscillation of its adaptive step near the minimum. Polyak \textbf{diverges} ($J \to 2.72$) because the specified $f^{\star} = 0$ mismatches the true minimum $J^{\star} \approx 0.4826$: as the iterate approaches $\theta_{\mathrm{OLS}}$, the gradient $\|\nabla J\| \to 0$ while $J(x_k) - f^{\star} \to 0.4826 > 0$, causing $\alpha_k = (J(x_k) - 0)/\|\nabla J\|^2 \to \infty$.}
\label{fig:q1_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_B.pdf}
\caption{Q1: Convergence on Benchmark B (Toy Neural Network). Polyak diverges ($f = 24.07$ vs true minimum $\approx 0.7235$) for the same $f^{\star}$ misspecification reason as Benchmark A. Adagrad and Heavy Ball match GD's final accuracy. The moderate anisotropy ($\kappa \approx 6.9$ at the minimum) does not severely hinder any correctly-specified method.}
\label{fig:q1_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_C.pdf}
\caption{Q1: Convergence on Benchmark C (Rosenbrock). Here $f^{\star} = 0$ is correct, and Polyak converges to the best final value ($f = 0.0094$). Heavy Ball achieves $f = 1.40$, outperforming GD ($f = 3.51$) through momentum acceleration along the valley floor. Adagrad reaches $f = 2.20$; its monotonically decaying step helps navigate the ill-conditioned valley but loses effectiveness later. RMSprop ($f = 3.12$) shows limited advantage because the very small base rate $\alpha_0 = 0.0035$ is constrained by stability requirements.}
\label{fig:q1_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q1: Final objective values after 120 iterations. Bold indicates best per benchmark. The sharp contrast in Polyak's performance illustrates the critical dependence on $f^{\star}$ accuracy.}
\label{tab:q1_final}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & 0.4826 & 0.7244 & 3.5142 \\
Polyak & 2.7189 & 24.0659 & \textbf{0.0094} \\
Adagrad & \textbf{0.4826} & \textbf{0.7244} & 2.2034 \\
RMSprop & 0.5027 & 0.7271 & 3.1204 \\
Heavy Ball & 0.4826 & 0.7244 & 1.4003 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Convergence Rate Analysis}

\begin{table}[H]
\centering
\caption{Q1: Theoretical convergence rates for each method. Here $L$ is the Lipschitz constant of $\nabla f$, $\mu$ the strong convexity constant, $\kappa = L/\mu$, and $T$ the number of iterations.}
\label{tab:q1_rates}
\begin{tabular}{lll}
\toprule
\textbf{Method} & \textbf{Convergence guarantee} & \textbf{Conditions} \\
\midrule
GD & $f(x_k)-f^{\star} \leq \left(1-\tfrac{1}{\kappa}\right)^k \cdot (f(x_0)-f^{\star})$ & $\mu$-sc, $L$-smooth, $\alpha=1/L$ \\
Polyak & $\|x_{k+1}-x^{\star}\|^2 \leq \left(1-\tfrac{1}{\kappa}\right)\|x_k-x^{\star}\|^2$ & $f^{\star}$ known; $\mu$-sc \\
Adagrad & $\frac{1}{T}\sum_{k=0}^{T-1}\|\nabla f(x_k)\|^2 = O(1/\sqrt{T})$ & Convex, bounded domain \\
RMSprop & $O(1/\sqrt{T})$ regret & Non-convex (empirical) \\
Heavy Ball & $\left(\tfrac{\sqrt{\kappa}-1}{\sqrt{\kappa}+1}\right)^{2k}$ distance contraction & Strongly convex quadratic only \\
\bottomrule
\end{tabular}
\end{table}

For Benchmark A ($\kappa \approx 1.117$), with $H = X^TX/m$ nearly isotropic, Heavy Ball's theoretical advantage over GD is modest: GD's contraction per step is $1 - 2/(\kappa+1) \approx 1 - 1.78 = -0.78$ (effectively very fast), while Heavy Ball's optimal rate is $(\frac{\sqrt{1.117}-1}{\sqrt{1.117}+1})^2 \approx (0.057)^2 \approx 0.003$. Both are fast because $\kappa$ is close to 1. For Benchmark C ($\kappa \approx 2504$), the contrast is stark: GD's rate is approximately $1 - 2/2504 \approx 0.9992$ (extremely slow, requiring $\sim 2504 \cdot \ln(1/\varepsilon)$ iterations), while optimal Heavy Ball achieves $\left(\frac{\sqrt{2504}-1}{\sqrt{2504}+1}\right)^2 \approx \left(\frac{49.0}{51.0}\right)^2 \approx 0.924$. This is still slow but requires only $\sim\sqrt{2504} \cdot \ln(1/\varepsilon) \approx 50 \ln(1/\varepsilon)$ iterations --- a $50\times$ speedup. In the experiments, Heavy Ball achieves $f = 1.40$ vs.\ GD's $f = 3.51$ in 120 iterations, qualitatively consistent with this $\sqrt{\kappa}$ speedup.

\textbf{Numerical verification of rates:} The Heavy Ball contraction factor on Benchmark A is predicted to be $\approx 0.003$ per two iterations (rapid), and on Benchmark C $\approx 0.924$ per iteration. GD's factor is $\approx 0.9992$ on Benchmark C. After $k = 120$ iterations: $0.924^{120} \approx 8 \times 10^{-5}$ for Heavy Ball vs.\ $0.9992^{120} \approx 0.91$ for GD, a ratio of $\sim 10^4$. In practice, the Rosenbrock's non-quadratic nature means exact Heavy Ball parameters are suboptimal, but the trend is confirmed in the experiments.

\subsection{Contour Plots with Optimisation Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q1_contour_B.pdf}
\caption{Q1: Trajectories on Benchmark B. The elliptical contours reflect the 5:1 curvature ratio between the $x_2$ and $x_1$ directions (coefficient 5 vs 1 in front of squared terms). GD and Adagrad follow direct paths; Heavy Ball exhibits slight overshoot attributable to momentum buildup before damping. The Polyak trajectory diverges due to $f^{\star}$ misspecification, spiralling outward.}
\label{fig:q1_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q1_contour_C.pdf}
\caption{Q1: Trajectories on Benchmark C (Rosenbrock). The banana-shaped valley $x_2 \approx x_1^2$ is clearly visible. Methods first descend rapidly to the valley floor and then progress slowly along it towards $(1,1)$. Polyak (with correct $f^{\star}$) shows the most aggressive progress along the valley. GD with $\alpha = 0.0012$, constrained by the local Lipschitz constant, barely advances.}
\label{fig:q1_contour_C}
\end{figure}

\subsection{Benchmark A Contour Plot}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_contour_A.pdf}
\caption{Q1: Benchmark A loss surface and optimisation trajectories in $(\theta_1, \theta_2)$ space. The contours are nearly circular ($\kappa \approx 1.117$), reflecting the near-isotropic Gram matrix $H \approx I_2$. GD and Heavy Ball both converge smoothly to the OLS minimum $(3.028, 4.017)$. Newton's method (one step shown) arrives at the minimum directly via $\theta_1 = H^{-1}(X^Ty/m)$, confirming one-step convergence on quadratics. The true parameter $\theta^{\star} = (3, 4)$ (triangle) is distinct from the OLS minimiser because noise shifts the empirical optimum.}
\label{fig:q1_contour_A}
\end{figure}

\subsection{Adaptive Step-Size Evolution}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_A.pdf}
\caption{Q1: Step-size evolution on Benchmark A. Polyak's step grows unboundedly as $\|\nabla J\| \to 0$ while $J - f^{\star} \to 0.4826$. Adagrad decays monotonically as squared gradients accumulate in the denominator. RMSprop stabilises due to exponential forgetting ($\beta = 0.9$), maintaining a nonzero effective step size throughout.}
\label{fig:q1_stepsize_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_B.pdf}
\caption{Q1: Step-size evolution on Benchmark B. Adagrad decays most rapidly in early iterations when large gradients are encountered at the distant starting point $x_0 = (-1, 4)$, then settles to a small effective rate. RMSprop's exponential moving average adapts smoothly to the changing gradient magnitude.}
\label{fig:q1_stepsize_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_C.pdf}
\caption{Q1: Step-size evolution on Benchmark C. With correct $f^{\star} = 0$, the Polyak step decreases smoothly as $f(x_k) \to 0$ and $\|g_k\|^2$ also decreases, producing a well-controlled decay. This confirms why Polyak achieves the best convergence on Rosenbrock: the diminishing numerator and denominator both contribute to a well-calibrated effective step.}
\label{fig:q1_stepsize_C}
\end{figure}

\subsection{Comparative Analysis}

\begin{itemize}[nosep]
\item \textbf{Sensitivity to $f^{\star}$ and robustification:} Polyak is highly sensitive. Even small misspecification $\Delta f^{\star} = f^{\star}_{\mathrm{true}} - f^{\star}_{\mathrm{specified}} > 0$ causes divergence because $\alpha_k \approx \Delta f^{\star}/\|\nabla f\|^2 \to \infty$ as $\|\nabla f\| \to 0$. In practice, $f^{\star}$ is rarely known exactly, limiting Polyak's applicability. A \textbf{robustified Polyak} variant estimates $f^{\star}$ online using the running minimum: $\hat{f}^{\star}_k = \min_{j \leq k} f(x_j) - \delta_k$ where $\delta_k \to 0$ slowly (e.g., $\delta_k = c/k$). This running-minimum estimate is always a valid upper bound on $f^{\star}$ (since $f^{\star} \leq f(x_j)$ for any feasible $x_j$), and is gradually refined as better points are found. However, the resulting step $\alpha_k = (\hat{f}^{\star}_k - f^{\star}_{\mathrm{true}})/\|\nabla f\|^2 + \delta_k/\|\nabla f\|^2$ is conservative, and convergence is slower than with the true $f^{\star}$. A further robustification uses $\alpha_k = \min(\alpha_{\max}, (f(x_k) - \hat{f}^{\star}_k)/\|\nabla f\|^2)$ with a clipping threshold $\alpha_{\max}$ to prevent runaway steps in early iterations. These variants are useful in settings where a lower bound on $f^{\star}$ is known (e.g., $f^{\star} \geq 0$ for MSE losses) but the exact value is not.
\item \textbf{Effect of condition number:} Benchmark A ($\kappa \approx 1.117$) is the easiest for all methods; the near-circular contours mean that any descent direction is nearly equally good. Benchmark B ($\kappa \approx 3.5$--$6.9$) presents moderate challenge. Benchmark C ($\kappa \approx 2504$ at the optimum, much larger near the start) is where method differences matter most. On the nearly-isotropic Benchmark A, even simple GD with $\alpha = 0.08 < 2/L \approx 1.93$ converges quickly, and the advantage of adaptive methods is negligible.
\item \textbf{Convergence speed:} When $f^{\star}$ is known (Rosenbrock), Polyak is fastest. Otherwise, Heavy Ball and Adagrad match or exceed GD. The Heavy Ball method's momentum acceleration is most pronounced on Benchmark C: its momentum parameter $\beta = 0.86$ effectively reduces the condition number from $\kappa$ to $\sqrt{\kappa}$ in ideal settings, leading to faster escape from the narrow valley.
\item \textbf{Smoothness:} RMSprop produces the smoothest convergence curves due to exponential averaging of the second moment. Heavy Ball exhibits the characteristic overshoot-oscillation-damping pattern of underdamped systems, analogous to a physical ball rolling in a bowl with friction coefficient $1-\beta$.
\item \textbf{Stability:} GD is most stable but slowest. Adagrad is unconditionally stable (step size monotonically decreasing) at the cost of eventual stagnation near the minimum; this can be remedied with restarts. RMSprop and Heavy Ball require careful tuning of $\alpha$ and $\beta$; the stability region in $(\alpha, \beta)$ space for Heavy Ball on a quadratic is $\alpha(1+\beta) < 2/L$.
\end{itemize}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q1_heavy_ball_stability.pdf}
\caption{Q1: Heavy Ball stability region $\alpha(1+\beta) < 2/L$ in $(\alpha, \beta)$ parameter space. Shaded regions are stable; the boundary is the linear constraint. Left: Benchmark A ($L \approx 1.033$, $\mu \approx 0.925$, $\kappa \approx 1.117$). The theoretical optimal parameters $(\alpha^*, \beta^*) = (4/(\sqrt{L}+\sqrt{\mu})^2,\ ((\sqrt{\kappa}-1)/(\sqrt{\kappa}+1))^2) \approx (0.97, 0.003)$ lie well within the stable region; the experimental settings ($\alpha = 0.045$, $\beta = 0.88$) are also stable. Right: Benchmark C ($L \approx 1680$ at $x_0 = (-1,1)$, axis shows normalised $\alpha L$). The experimental $(\alpha L, \beta) = (1.34, 0.86)$ is just inside the stability boundary. Since $L$ changes dramatically along the Rosenbrock trajectory (from $\sim$1680 near $(-1,1)$ to $\sim$1001.6 near $(1,1)$), parameter settings that are nominally stable at the start may become marginally unstable at other points, explaining Heavy Ball's oscillatory behaviour on Rosenbrock.}
\label{fig:q1_hb_stability}
\end{figure}

% ============================================================
\section{Question 2: Momentum and Stochastic Methods \hfill [30 marks]}
\label{sec:q2}
% ============================================================

\subsection{Algorithm Formulations}

\begin{algorithm}[H]
\caption{Nesterov Momentum / Accelerated Gradient Descent}
\label{alg:nesterov}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, max momentum $\beta_{\max}$, iterations $N$
\State $z_0 \gets \mathbf{0}$
\For{$k = 1, 2, \ldots, N$}
    \State $\beta_k \gets \min\!\left(\frac{k-1}{k+2},\ \beta_{\max}\right)$
    \State $\tilde{x}_k \gets x_k + \beta_k\, z_k$ \Comment{Lookahead (extrapolated) point}
    \State $z_{k+1} \gets \beta_k\, z_k - \alpha\, \nabla f(\tilde{x}_k)$
    \State $x_{k+1} \gets x_k + z_{k+1}$
\EndFor
\end{algorithmic}
\end{algorithm}

The key distinction from Heavy Ball is the \emph{lookahead step}: the gradient is evaluated at the extrapolated point $\tilde{x}_k = x_k + \beta_k z_k$ rather than the current iterate $x_k$. This ``correction before overshoot'' strategy allows the momentum to be used more aggressively without instability.

\begin{theorem}[Nesterov's Optimal Rate]
For an $L$-smooth convex function $f$, Nesterov's accelerated gradient descent with $\alpha = 1/L$ and the schedule $\beta_k = (k-1)/(k+2)$ achieves:
\begin{equation}
f(x_k) - f^{\star} \leq \frac{2L\|x_0 - x^{\star}\|^2}{(k+1)^2} = O\!\left(\frac{1}{k^2}\right),
\end{equation}
which is optimal among all first-order methods for smooth convex optimisation (matching the $\Omega(1/k^2)$ lower bound).
\end{theorem}

This $O(1/k^2)$ rate is a quadratic improvement over gradient descent's $O(1/k)$ rate for smooth convex problems, explaining Nesterov's empirically superior performance on the benchmarks.

\begin{algorithm}[H]
\caption{Adam Optimiser}
\label{alg:adam}
\begin{algorithmic}[1]
\Require Initial point $x_0$, learning rate $\alpha$, moments $\beta_1, \beta_2$, tolerance $\varepsilon$, iterations $N$
\State $m_0 \gets \mathbf{0},\ v_0 \gets \mathbf{0}$
\For{$t = 1, 2, \ldots, N$}
    \State $g_t \gets \nabla f(x_t)$
    \State $m_t \gets \beta_1\, m_{t-1} + (1 - \beta_1)\, g_t$ \Comment{First moment (momentum) estimate}
    \State $v_t \gets \beta_2\, v_{t-1} + (1 - \beta_2)\, g_t \odot g_t$ \Comment{Second moment (RMSprop) estimate}
    \State $\hat{m}_t \gets m_t\, /\, (1 - \beta_1^t)$ \Comment{Bias correction for first moment}
    \State $\hat{v}_t \gets v_t\, /\, (1 - \beta_2^t)$ \Comment{Bias correction for second moment}
    \State $x_{t+1} \gets x_t - \alpha\, \hat{m}_t\, /\, (\sqrt{\hat{v}_t} + \varepsilon)$
\EndFor
\end{algorithmic}
\end{algorithm}

Adam combines gradient momentum (via $m_t$, equivalent to RMSprop with additional momentum) with per-coordinate adaptive scaling (via $v_t$). The bias correction terms $(1-\beta_1^t)$ and $(1-\beta_2^t)$ compensate for the initialisation at zero, which would otherwise bias the early iterates. In the deterministic setting, Adam reduces to a combination of momentum and normalised gradient descent. The update rule can be rewritten as $x_{t+1} = x_t - \alpha \hat{m}_t / (\sqrt{\hat{v}_t} + \varepsilon)$, where the division normalises each gradient component by its recent root-mean-square magnitude.

\subsection{Hyperparameter Settings}

\begin{table}[H]
\centering
\caption{Q2: Hyperparameter settings. Nesterov and Adam run for 150 iterations.}
\label{tab:q2_params}
\begin{tabular}{llccc}
\toprule
\textbf{Method} & \textbf{Parameter} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & $\alpha$ & 0.08 & 0.06 & 0.0012 \\
\midrule
Nesterov & $\alpha$ & 0.06 & 0.035 & 0.0007 \\
 & $\beta_{\max}$ & 0.90 & 0.92 & 0.90 \\
\midrule
Adam & $\alpha$ & 0.12 & 0.08 & 0.006 \\
 & $\beta_1$ & 0.82 & 0.80 & 0.80 \\
 & $\beta_2$ & 0.999 & 0.999 & 0.999 \\
 & $\varepsilon$ & $10^{-8}$ & $10^{-8}$ & $10^{-8}$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Convergence Comparison}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_A.pdf}
\caption{Q2: Convergence on Benchmark A. All three methods converge to $J^{\star} \approx 0.4826$. Both Nesterov and Adam reach the minimum in fewer iterations than GD, consistent with their momentum and adaptive step advantages. On strongly convex quadratics, Nesterov's $O(1/k^2)$ rate translates to substantially fewer iterations to reach a given tolerance.}
\label{fig:q2_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_B.pdf}
\caption{Q2: Convergence on Benchmark B. Both Nesterov and Adam converge to $f^{\star} \approx 0.7244$. Adam's per-coordinate adaptive scaling handles the anisotropic curvature ($\kappa \approx 6.9$) effectively by taking larger steps in the $x_1$ direction (flatter) and smaller steps in $x_2$ (steeper). Nesterov's lookahead prevents overshoot across the steeper $x_2$ direction.}
\label{fig:q2_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_C.pdf}
\caption{Q2: Convergence on Benchmark C. Nesterov achieves $f = 0.479$ in 150 iterations---a 7$\times$ improvement over GD ($f = 3.388$). Nesterov builds cumulative momentum along the consistent valley-floor direction, effectively amplifying progress in the descent direction while the restoring force from the quadratic walls keeps it stable. Adam reaches $f = 1.578$; its per-coordinate normalisation disrupts the directional momentum needed to traverse the curved Rosenbrock valley, as the normalisation treats gradient magnitude as a nuisance rather than a signal.}
\label{fig:q2_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q2: Final objective values after 150 iterations. Nesterov outperforms Adam on the ill-conditioned Rosenbrock due to its superior handling of consistent gradient directions.}
\label{tab:q2_final}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & 0.4826 & 0.7244 & 3.3880 \\
Nesterov & \textbf{0.4826} & \textbf{0.7244} & \textbf{0.4788} \\
Adam & \textbf{0.4826} & \textbf{0.7244} & 1.5785 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Contour Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q2_contour_B.pdf}
\caption{Q2: Trajectories on Benchmark B. Nesterov's trajectory exhibits the characteristic smooth arc of accelerated methods, skirting oscillation via the lookahead correction. Adam takes more direct, curvature-corrected steps by independently scaling each coordinate. Both reach the minimum more efficiently than GD.}
\label{fig:q2_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q2_contour_C.pdf}
\caption{Q2: Trajectories on Benchmark C. All methods quickly descend to the valley floor ($x_2 \approx x_1^2$) but differ in progress \emph{along} the valley. Nesterov accumulates momentum in the valley direction, enabling sustained progress towards $(1,1)$. Adam's component-wise normalisation reduces the effective momentum in this direction, limiting valley traversal.}
\label{fig:q2_contour_C}
\end{figure}

\subsection{Mini-Batch Stochastic Gradient Descent}

Mini-batch SGD on Benchmark A uses learning rate $\alpha = 0.06$, batch sizes $b \in \{5, 40\}$, 50 epochs, with data shuffled at every epoch. The mini-batch gradient $\hat{g}^{(e,i)} = \frac{1}{b}X_{\mathcal{B}}^T(X_{\mathcal{B}}\theta - y_{\mathcal{B}})$ is an unbiased estimator of the true gradient, with variance that decreases as $b$ increases.

\begin{algorithm}[H]
\caption{Mini-Batch SGD}
\label{alg:sgd}
\begin{algorithmic}[1]
\Require Data $(X, y)$, initial $\theta_0$, learning rate $\alpha$, batch size $b$, epochs $E$
\For{$e = 1, 2, \ldots, E$}
    \State Randomly permute indices $\{1,\ldots,m\}$ into $\lceil m/b \rceil$ mini-batches
    \For{each mini-batch $\mathcal{B}$ of size $b$}
        \State $\hat{g} \gets \frac{1}{b} X_{\mathcal{B}}^T(X_{\mathcal{B}}\theta - y_{\mathcal{B}})$ \Comment{Unbiased gradient estimate}
        \State $\theta \gets \theta - \alpha\, \hat{g}$
    \EndFor
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_sgd_batch.pdf}
\caption{Q2: Mini-batch SGD with batch sizes $b \in \{5, 40\}$. With $b=5$, each epoch performs $m/b = 200$ gradient updates, yielding faster per-epoch convergence but higher gradient variance (gradient noise $\propto \sigma^2/b$). With $b=40$, only 25 updates occur per epoch, giving smoother convergence. The smaller batch reaches lower loss faster in epoch count due to more updates per epoch, but oscillates more near the minimum. The fundamental noise-speed trade-off: smaller batches reduce per-epoch computation but increase gradient variance.}
\label{fig:q2_sgd_batch}
\end{figure}

\subsection{SGD with Increased Observation Noise}

The experiment is repeated with output noise scaled by a factor of 6 ($\sigma = 6$ vs.\ $\sigma = 1$), implemented as \texttt{y\_noisy = X @ theta\_star + 6.0 * eps\_noise}.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q2_sgd_noisy.pdf}
\caption{Q2: Effect of observation noise on SGD convergence. Left: original noise ($\sigma = 1$). Right: high noise ($\sigma = 6$). Increasing the noise by factor 6 raises the irreducible loss floor by factor $\approx 36$ (since $J^{\star} \propto \sigma^2$, as the OLS estimator error scales with noise variance), from $J^{\star} \approx 0.48$ to $J^{\star} \approx 17.3$. Under high noise, both batch sizes converge to approximately the same elevated floor: the observation model's intrinsic noise dominates over mini-batch sampling noise, eliminating the relative advantage of smaller batches.}
\label{fig:q2_sgd_noisy}
\end{figure}

\begin{remark}[SGD Variance, Convergence Floor, and Noise-Floor Theory]
For linear regression with observation noise variance $\sigma^2$, the OLS estimator satisfies $J(\theta^{\star}_{\mathrm{OLS}}) = \frac{\sigma^2}{2}(1 - d/m) \approx \frac{\sigma^2}{2}$ for large $m \gg d$. Thus $J^{\star} \propto \sigma^2$, confirming the factor-$36$ increase in the loss floor when $\sigma$ grows by factor $6$.

More precisely, the mini-batch gradient $\hat{g}_{\mathcal{B}} = \frac{1}{b}X_{\mathcal{B}}^T(X_{\mathcal{B}}\theta - y_{\mathcal{B}})$ has variance decomposed as:
\begin{equation}
\mathrm{Var}[\hat{g}_{\mathcal{B}}] = \underbrace{\frac{1}{b}\mathrm{Var}_{i \sim \mathrm{Unif}}\bigl[x^{(i)}(x^{(i)T}\theta - y^{(i)})\bigr]}_{\text{sampling variance} \propto 1/b} \approx \frac{\sigma^2}{b}\,\mathbb{E}[\|x\|^2 \cdot \mathrm{I}_d],
\end{equation}
where the second factor is $O(d)$ for isotropic $x \sim \mathcal{N}(0,I_d)$. With a fixed learning rate $\alpha$, SGD does not converge to $J^{\star}$ but oscillates in a ball of radius $O(\alpha \sigma^2/b)$ around it: the noise floor $J_{\infty} - J^{\star} \approx \frac{\alpha \sigma^2 \mathrm{tr}(H)}{2b\mu}$ (where $\mu = \lambda_{\min}(H)$ is the strong convexity constant). This formula reveals three key relationships: (1) larger batch $b$ reduces the floor; (2) smaller learning rate $\alpha$ reduces the floor; (3) higher observation noise $\sigma^2$ raises the floor. In the high-noise experiment ($\sigma = 6$), the floor at fixed $\alpha = 0.06$ is predicted to be $36\times$ higher --- consistent with Figure~\ref{fig:q2_sgd_noisy}. Using a decaying learning rate $\alpha_k = \alpha_0 / k$ eliminates the noise floor but slows late convergence; in practice, ``learning rate warmup + decay'' schedules (cosine annealing, polynomial decay) are used to balance initial speed with final precision.
\end{remark}

\subsection{Comparative Discussion}

Nesterov's superiority over Adam on Rosenbrock reveals a fundamental structural difference between these two methods:

\begin{proposition}[Adam Convergence for Smooth Non-Convex Functions]
For an $L$-smooth (possibly non-convex) objective with bounded gradients $\|\nabla f(x_t)\| \leq G$, Adam with step $\alpha_t = \alpha/\sqrt{t}$ and appropriately chosen $\beta_1, \beta_2$ achieves:
\begin{equation}
\frac{1}{T}\sum_{t=1}^{T}\mathbb{E}\bigl[\|\nabla f(x_t)\|^2\bigr] = O\!\left(\frac{\ln T}{\sqrt{T}}\right).
\end{equation}
This is a guarantee of convergence to a \emph{stationary point} (not necessarily a global or local minimum) at rate $O(\ln T/\sqrt{T})$. For smooth convex objectives, Adam with a fixed step $\alpha$ achieves an $O(1/\sqrt{T})$ regret bound. In contrast, Nesterov's $O(1/k^2)$ rate holds for \emph{convex} $f$ with the specific schedule $\gamma_k = 2/(k+2)$; on non-convex problems (like Rosenbrock), Nesterov's theoretical acceleration guarantees do not directly apply. The empirical advantage of Nesterov over Adam on Rosenbrock is therefore best understood geometrically (preserved gradient direction vs.\ coordinate normalisation) rather than through convergence theory.
\end{proposition}

\textbf{Nesterov on Rosenbrock:} Along the valley floor $x_2 \approx x_1^2$, the gradient consistently points in a direction tangent to the valley. Nesterov accumulates momentum in this direction over successive iterations, amplifying progress. The adaptive momentum coefficient $\beta_k = (k-1)/(k+2)$ grows towards $\beta_{\max} = 0.90$, allowing increasingly large effective steps in the valley direction. This behaviour is aligned with the Rosenbrock's geometric structure.

\textbf{Adam on Rosenbrock:} Adam normalises each gradient component by $\sqrt{\hat{v}_t + \varepsilon}$. On Rosenbrock, the gradient component $g_2 = 200(x_2 - x_1^2)$ is large when far from the valley floor but small along it, while $g_1$ varies more irregularly. Adam's per-coordinate normalisation reduces the effective gradient signal by $1/\sqrt{\hat{v}_t}$, which tends to equalise the two components and disrupts the directional momentum needed to traverse the curved valley. This normalisation is a feature (not a bug) in deep learning, where gradient scales vary wildly across layers, but it is a disadvantage on geometrically coherent problems.

\textbf{General principle:} Nesterov/Heavy Ball methods that preserve gradient direction are preferred when the objective has a well-defined ``ridge'' or ``valley'' structure. Adaptive per-coordinate methods (Adam, RMSprop) are preferred when the natural coordinate system aligns with the problem's curvature axes and scales vary dramatically per coordinate.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q2_nesterov_vs_adam_trajectory.pdf}
\caption{Q2: Nesterov vs Adam vs GD on Rosenbrock, 150 iterations, starting from $(-1,1)$. Top-left: $x_1(k)$ component vs iteration --- Nesterov's $x_1$ progresses monotonically from $-1$ towards $1$, while Adam's $x_1$ stagnates after an initial rapid descent due to the coordinate normalisation disrupting valley-direction momentum. GD barely moves in 150 steps ($\alpha = 0.0012$ constrained by stability). Top-right: $x_2(k)$ component --- Nesterov tracks $x_2 \approx x_1^2$ closely (following the valley floor) because its accumulated momentum aligns with the valley. Bottom-left: log-scale suboptimality $f - f^{\star}$ confirming Nesterov's 3$\times$ advantage over Adam. Bottom-right: 2D trajectory showing Nesterov's smooth valley traversal versus Adam's rapid initial descent followed by stagnation near $x_1 \approx -0.1$.}
\label{fig:q2_nes_adam_traj}
\end{figure}

% ============================================================
\section{Question 3: Newton's Method and Local Approximation \hfill [30 marks]}
\label{sec:q3}
% ============================================================

\subsection{Local Approximations of $g(x) = x^4$}

\textbf{First-order (tangent) approximation} around $x_0 = 0.25$:
\begin{equation}
g_1(x) = g(x_0) + g'(x_0)(x - x_0) = 0.25^4 + 4 \cdot 0.25^3\,(x - 0.25) = 0.0039 + 0.0625(x - 0.25).
\end{equation}

\textbf{Second-order (quadratic) approximation:}
\begin{equation}
g_2(x) = g(x_0) + g'(x_0)(x - x_0) + \tfrac{1}{2}g''(x_0)(x - x_0)^2 = 0.0039 + 0.0625(x - 0.25) + \tfrac{1}{2}\cdot 0.375\,(x - 0.25)^2.
\end{equation}

Newton's method minimises the second-order approximation $g_2(x)$ at each step: setting $g_2'(x) = 0$ gives the Newton update $x_{k+1} = x_k - g'(x_k)/g''(x_k)$. This corresponds to the exact minimum of the local quadratic model, contrasting with the gradient step which merely moves in the descent direction without using curvature information.

\textbf{Quantitative error of the approximations:} By Taylor's theorem, for any $h = x - x_0$:
\begin{align}
|g(x) - g_1(x)| &= \left|\tfrac{1}{2}g''(x_0)h^2 + O(h^3)\right| \leq \tfrac{1}{2}M_2\,h^2, \\
|g(x) - g_2(x)| &= \left|\tfrac{1}{6}g'''(x_0)h^3 + O(h^4)\right| \leq \tfrac{1}{6}M_3\,h^3,
\end{align}
where $M_2 = \max_{t}|g''(t)|$ and $M_3 = \max_{t}|g'''(t)|$ bound higher derivatives in the neighbourhood. For $g(x) = x^4$: $g''(x) = 12x^2$, $g'''(x) = 24x$. At $x_0 = 0.25$: $g''(x_0) = 0.75$, $g'''(x_0) = 6$. The error at $h = 0.3$ is approximately:
\begin{equation}
|g - g_1|_{h=0.3} \approx \tfrac{1}{2} \cdot 0.75 \cdot 0.09 = 0.034, \quad |g - g_2|_{h=0.3} \approx \tfrac{1}{6} \cdot 6 \cdot 0.027 = 0.027.
\end{equation}
The second-order model reduces the error by roughly an order of magnitude for each doubling of proximity to $x_0$: whereas the first-order error is $O(h^2)$, the second-order error is $O(h^3)$. This cubic vs.\ quadratic convergence in $h$ is the geometric foundation of Newton's quadratic convergence rate: each Newton step reduces the error $h_k = \|x_k - x^{\star}\|$ by a factor of $h_k$ itself, yielding $h_{k+1} \approx C h_k^2$.

\textbf{Newton step from the approximation minimiser:} Setting $g_2'(x) = 0.0625 + 0.375(x - 0.25) = 0$ gives $x^{\mathrm{Newton}} = 0.25 - 0.0625/0.375 = 0.25 - 0.1\overline{6} = 0.08\overline{3}$. The true minimiser is $g'(x^{\star}) = 4x^{3} = 0 \Rightarrow x^{\star} = 0$. The gradient step from $x_0 = 0.25$ with $\alpha = 1/g''(x_0) = 1/0.375 \approx 2.67$ would give $x^{\mathrm{GD}} = 0.25 - 2.67 \cdot 0.0625 = 0.25 - 0.167 = 0.083$---the same as Newton for this 1D problem, because on a scalar function the full Newton step is identical to gradient descent with step $1/g''(x_0)$. The distinction between Newton and GD only becomes meaningful in multiple dimensions, where Newton uses the full inverse Hessian $H^{-1}$ to account for cross-derivative interactions.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_approximation.pdf}
\caption{Q3: Local approximations of $g(x) = x^4$ at $x_0 = 0.25$. The first-order approximation (tangent line) diverges rapidly from the true function. The second-order approximation (parabola) fits well in a neighbourhood of $x_0$ and its minimum provides a substantially better estimate of the function's minimiser than the gradient descent direction. The curvature $g''(x_0) = 12 \cdot 0.25^2 = 0.75$ is used to determine the step size in the Newton direction.}
\label{fig:q3_approx}
\end{figure}

\subsection{Newton's Method}

\begin{algorithm}[H]
\caption{Newton's Method with Damping}
\label{alg:newton}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, Hessian regularisation $\lambda$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $H_k \gets \nabla^2 f(x_k) + \lambda I$ \Comment{Tikhonov-regularised Hessian}
    \State $p_k \gets H_k^{-1}\, g_k$ \Comment{Newton direction (solution of $H_k p = g_k$)}
    \State $x_{k+1} \gets x_k - \alpha\, p_k$
\EndFor
\end{algorithmic}
\end{algorithm}

The regularisation $\lambda I$ (with $\lambda = 10^{-8}$) ensures $H_k + \lambda I \succ 0$, making the Newton direction $p_k = (H_k + \lambda I)^{-1} g_k$ well-defined even when $H_k$ is indefinite. The damping factor $\alpha < 1$ implements a \emph{damped Newton step}, trading off the full Newton step's quadratic convergence for improved global behaviour when the Hessian model is inaccurate far from the optimum.

\begin{remark}[Hessian Indefiniteness on Rosenbrock]
At the starting point $x_0 = (-1, 1)$, the Rosenbrock Hessian is:
\begin{equation}
\nabla^2 f(-1, 1) = \begin{bmatrix} 2 + 1200\cdot 1 - 400\cdot 1 & -400\cdot(-1) \\ 400 & 200 \end{bmatrix} = \begin{bmatrix} 802 & 400 \\ 400 & 200 \end{bmatrix}.
\end{equation}
$\det = 802\cdot 200 - 400^2 = 160400 - 160000 = 400 > 0$ and trace $= 1002 > 0$, so $H$ is positive definite here. However, along the valley floor ($x_2 \approx x_1^2$) for $x_1$ close to zero, $h_{11} = 2 + 1200x_1^2 - 400x_2 \approx 2 - 400x_1^2$ can become very small, causing near-singularity. Full Newton steps ($\alpha = 1$) overshoot the valley in these regions; the damping $\alpha = 0.22$ limits step length and maintains stability throughout the trajectory.
\end{remark}

\begin{theorem}[Quadratic Convergence of Newton's Method]
Let $f$ be twice continuously differentiable with $\nabla^2 f$ Lipschitz continuous (constant $M$). If $x_0$ is sufficiently close to $x^{\star}$ with $\nabla^2 f(x^{\star}) \succ 0$, then the full Newton step ($\alpha = 1$) satisfies:
\begin{equation}
\|x_{k+1} - x^{\star}\| \leq \frac{M}{2\mu}\,\|x_k - x^{\star}\|^2,
\end{equation}
where $\mu = \lambda_{\min}(\nabla^2 f(x^{\star}))$. This super-linear (quadratic) convergence means that once the iterates enter the neighbourhood of $x^{\star}$, the number of accurate digits doubles each iteration.
\end{theorem}

The critical requirement is that $x_0$ must lie within the Newton basin of attraction. For the Rosenbrock function, the highly variable Hessian ($\nabla^2 f$ changes dramatically as $x_1$ varies) means the basin is small near the starting point $(-1, 1)$, necessitating a damped step ($\alpha = 0.22$).

Settings: GD uses 80 iterations; Newton uses 20 iterations with $\lambda = 10^{-8}$.

\begin{table}[H]
\centering
\caption{Q3: Hyperparameters for GD and Newton's method.}
\label{tab:q3_params}
\begin{tabular}{lcccccc}
\toprule
 & \multicolumn{2}{c}{\textbf{Bench.\ A}} & \multicolumn{2}{c}{\textbf{Bench.\ B}} & \multicolumn{2}{c}{\textbf{Bench.\ C}} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5} \cmidrule(lr){6-7}
 & GD & Newton & GD & Newton & GD & Newton \\
\midrule
$\alpha$ & 0.08 & 1.0 & 0.06 & 0.85 & 0.001 & 0.22 \\
Iterations & 80 & 20 & 80 & 20 & 80 & 20 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Convergence Comparison}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_A.pdf}
\caption{Q3: Newton vs GD on Benchmark A. Newton converges in \textbf{one iteration} to the minimum $J = 0.4826$. This is exact for quadratic losses: since $J(\theta) = \frac{1}{2m}\|X\theta - y\|^2$ is quadratic, the second-order Taylor expansion is exact everywhere, and Newton's full step ($\alpha = 1.0$) yields the OLS solution $\theta^{\star} = (X^TX)^{-1}X^Ty$ directly. GD requires 80 iterations to achieve comparable accuracy.}
\label{fig:q3_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_B.pdf}
\caption{Q3: Newton vs GD on Benchmark B. Newton converges in approximately 5 iterations due to the near-quadratic structure: the Hessian $H(x) = \mathrm{diag}(2 - \sin(x_1), 10)$ varies slowly (bounded between $\mathrm{diag}(1, 10)$ and $\mathrm{diag}(3, 10)$), so Newton's quadratic model remains accurate across several steps. The damping $\alpha = 0.85$ prevents the occasional overshoot that arises from the sinusoidal non-quadratic term $\sin(x_1)$.}
\label{fig:q3_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_C.pdf}
\caption{Q3: Newton vs GD on Benchmark C. Newton with $\alpha = 0.22$ reaches $f = 0.686$ in 20 iterations, versus GD's $f = 3.732$ in 80 iterations. The strongly damped step is required because at $x_0 = (-1, 1)$, the Hessian $\nabla^2 f(-1,1) = \begin{bmatrix} 1602 & 400 \\ 400 & 200 \end{bmatrix}$ has the full Newton direction pointing far from the minimum; damping provides a trust-region effect. The Hessian information nonetheless allows Newton to navigate the valley far more efficiently than GD.}
\label{fig:q3_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q3: Final objective values and converged positions. The dramatic iteration count advantage of Newton (20 vs.\ 80) demonstrates the value of curvature information.}
\label{tab:q3_final}
\begin{tabular}{lcccc}
\toprule
\textbf{Benchmark} & \textbf{GD $f$ (80 it.)} & \textbf{Newton $f$ (20 it.)} & \textbf{Newton final $x$} \\
\midrule
A & 0.4827 & 0.4826 & $(3.028, 4.017)$ \\
B & 0.7244 & 0.7244 & $(0.582, 2.000)$ \\
C & 3.7319 & 0.6864 & $(0.183, 0.020)$ \\
\bottomrule
\end{tabular}
\end{table}

Note that Newton's final position on Benchmark A $(3.028, 4.017)$ closely approximates $\theta^{\star} = (3, 4)$ to within two decimal places, confirming one-step convergence to the OLS solution with only small floating-point rounding errors.

\subsection{Contour Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q3_contour_B.pdf}
\caption{Q3: Newton vs GD trajectories on Benchmark B. Newton takes large, precisely-directed steps that account for coordinate-wise curvature differences, reaching the minimum in approximately 5 steps. GD's trajectory reflects the anisotropic curvature: the $x_2$ component converges first (larger eigenvalue 10), then $x_1$ follows slowly (eigenvalue $\approx 1.45$).}
\label{fig:q3_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q3_contour_C.pdf}
\caption{Q3: Newton vs GD trajectories on Rosenbrock. Newton's trajectory follows the valley floor more closely because the Hessian accounts for cross-derivative coupling (the $H_{12} = -400x_1$ off-diagonal term). GD, constrained to $\alpha = 0.001$, advances minimally in 80 iterations.}
\label{fig:q3_contour_C}
\end{figure}

\subsection{Update Magnitude Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q3_update_magnitude.pdf}
\caption{Q3: Update magnitude $\|x_{k+1} - x_k\|$ vs.\ iteration for GD and Newton across all benchmarks. Newton's magnitude on Benchmark A drops to machine precision after one iteration (quadratic convergence in one step for exact quadratics). On Benchmarks B and C, Newton's updates decay super-linearly once within the convergence basin, while GD's magnitude decreases linearly, consistent with linear convergence. On Rosenbrock, Newton maintains larger updates than GD, indicating more aggressive and productive steps.}
\label{fig:q3_update}
\end{figure}

\subsection{Convergence Order Verification}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q3_convergence_order.pdf}
\caption{Q3: Newton's quadratic convergence vs GD's linear convergence on Benchmark B. Left: distance to optimum $\|x_k - x^*\|$ vs iteration on a log scale — Newton decays super-linearly while GD decays linearly. Right: convergence order verification via a log-log plot of $\|x_{k+1} - x^*\|$ vs $\|x_k - x^*\|$: a slope of~2 in this plot indicates quadratic convergence (each step squares the error), while a slope of~1 indicates linear convergence. Newton's trajectory tracks the slope-2 reference closely once within the convergence basin (iterations 2--10 on Benchmark B), confirming the theoretical quadratic rate.}
\label{fig:q3_conv_order}
\end{figure}

\subsection{Computational Cost and Practical Considerations}

Each Newton iteration requires: (i) evaluating $\nabla^2 f(x_k)$ at cost $O(d^2)$; (ii) solving the linear system $H_k p_k = g_k$ at cost $O(d^3)$ via Cholesky factorisation. For $d = 2$ the cost is negligible, but for large $d$ (e.g., deep learning with $d \sim 10^7$), the $O(d^3)$ cost is prohibitive. In contrast, GD costs $O(d)$ per iteration. Despite requiring far fewer iterations, Newton's per-iteration cost limits its practical use to low-dimensional problems or Quasi-Newton approximations (e.g., L-BFGS) that avoid the full Hessian.

\subsection{Newton Decrement and Stopping Criterion}

The \textbf{Newton decrement} $\lambda(x)$ provides a natural, affine-invariant stopping criterion for Newton's method:
\begin{equation}
\lambda(x) = \sqrt{\nabla f(x)^T\,[\nabla^2 f(x)]^{-1}\,\nabla f(x)} = \|p\|_H,
\end{equation}
where $\|p\|_H = \sqrt{p^T H p}$ denotes the Hessian-weighted norm of the Newton direction $p = H^{-1}\nabla f(x)$. Since $p^T H p = (H^{-1}g)^T H (H^{-1}g) = g^T H^{-1} g$, this equals $\lambda(x) = \sqrt{g^T H^{-1} g}$. For a strongly convex function, the Newton decrement bounds the suboptimality:
\begin{equation}
f(x) - f^{\star} \leq \frac{\lambda(x)^2}{2},
\end{equation}
up to higher-order terms when $x$ is near $x^{\star}$. This motivates the standard stopping criterion $\lambda(x)^2/2 < \varepsilon_{\mathrm{tol}}$.

A crucial advantage over the gradient norm $\|\nabla f(x)\|$ is \emph{affine invariance}: $\lambda(Ax)$ is unchanged under any invertible linear transformation $A$ of the domain, making the stopping criterion independent of the coordinate scaling. In contrast, $\|\nabla f(x)\|$ grows or shrinks proportionally to coordinate scaling, making it a less reliable indicator of proximity to the optimum.

For the Newton implementation in this assignment (Benchmark A, B, C), the Newton decrement can be computed from the quantities already available ($g$ and $H^{-1}$). On Benchmark A after 1 iteration, $\lambda(x_1)^2/2 \approx 10^{-14}$ (machine precision), confirming one-step convergence. On Rosenbrock after 20 iterations with damping $\alpha = 0.22$, the iterate has not yet entered the basin where the quadratic model is accurate, so $\lambda^2/2 \gg \varepsilon$ signals that more iterations are needed.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q3_newton_decrement.pdf}
\caption{Q3: Newton decrement $\lambda^2/2 = \frac{1}{2}g_k^T H_k^{-1} g_k$ (blue circles) vs.\ suboptimality $f(x_k) - f^{\star}$ (red squares), Newton iteration $k$, all three benchmarks. The Newton decrement upper-bounds the suboptimality near $x^{\star}$, making it a valid stopping criterion. On Benchmark A both quantities collapse to machine precision after iteration 1 (exact one-step convergence). On Benchmark B they decay rapidly and coincide by iteration $\sim$5. On Rosenbrock with fixed $\alpha = 0.22$, both remain well above a tolerance of 0.5 (grey dotted line) after 20 iterations, indicating that more iterations or a backtracking schedule would be needed.}
\label{fig:q3_newton_decrement}
\end{figure}

\begin{remark}[Backtracking Line Search as Alternative to Fixed Damping]
Instead of fixing $\alpha = 0.22$ for all iterates on Rosenbrock, practical Newton implementations use \textbf{Armijo backtracking line search}: starting from $\alpha = 1$ (full Newton step), reduce $\alpha \leftarrow \rho\, \alpha$ (with $\rho \in (0,1)$, typically $\rho = 0.5$) until the sufficient decrease (Armijo) condition
\begin{equation}
f(x_k - \alpha\, p_k) \leq f(x_k) - c\,\alpha\,\nabla f(x_k)^T p_k
\end{equation}
is satisfied, where $c \in (0, 1/2)$ (typically $c = 10^{-4}$). Since $p_k = H_k^{-1} g_k$ is a descent direction ($\nabla f^T p_k = g_k^T H_k^{-1} g_k = \lambda(x_k)^2 > 0$), the condition ensures each step achieves a guaranteed fraction $c$ of the first-order predicted decrease. Far from the optimum (where the quadratic model is inaccurate), $\alpha$ is reduced until the step is safe; near the optimum, the full step $\alpha = 1$ is typically accepted immediately, recovering quadratic convergence. The fixed $\alpha = 0.22$ in the experiments is a conservative constant chosen to be safe throughout the Rosenbrock trajectory; backtracking would allow larger steps in the early phase (more like $\alpha \approx 0.5$--$0.8$) and recover full-step ($\alpha = 1$) convergence near the optimum, likely halving the iteration count required.
\end{remark}

% ============================================================
\section{Question 4: Derivative Approximation and Derivative-Free Optimisation \hfill [30 marks]}
\label{sec:q4}
% ============================================================

\subsection{Part A: Finite Difference Gradient Approximation (Benchmark B)}

\subsubsection{Forward Finite Difference}

The forward finite-difference approximation replaces each partial derivative with:
\begin{equation}
\frac{\partial f}{\partial x_i}(x) \approx \frac{f(x + \delta e_i) - f(x)}{\delta}, \quad i = 1, \ldots, d.
\end{equation}
By Taylor expansion, the approximation error is:
\begin{equation}
\left|\frac{f(x + \delta e_i) - f(x)}{\delta} - \frac{\partial f}{\partial x_i}(x)\right| \leq \frac{\delta}{2}\,\left\|\frac{\partial^2 f}{\partial x_i^2}\right\|_\infty + \frac{2\varepsilon_{\mathrm{mach}}}{\delta},
\end{equation}
where the first term is truncation error $O(\delta)$ and the second is roundoff error $O(\varepsilon_{\mathrm{mach}}/\delta)$. The optimal balance gives $\delta^{\star} = \sqrt{2\varepsilon_{\mathrm{mach}}} \approx 1.5 \times 10^{-8}$ for double precision ($\varepsilon_{\mathrm{mach}} \approx 2.2 \times 10^{-16}$). The experiments test $\delta = 0.05$ (orders of magnitude above optimal, dominated by truncation error) and $\delta = 0.8$ (severely truncated).

\subsubsection{Nesterov Random Search}

\begin{algorithm}[H]
\caption{Nesterov Random Search (Zeroth-Order)}
\label{alg:nrs}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, perturbation $\delta$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State Sample $u_k \sim \mathcal{N}(0, I_d)$, normalise: $u_k \gets u_k / \|u_k\|$ \Comment{Uniform random direction}
    \State $\hat{g}_k \gets \dfrac{f(x_k + \delta u_k) - f(x_k)}{\delta}\, u_k$ \Comment{Directional derivative estimate}
    \State $x_{k+1} \gets x_k - \alpha\, \hat{g}_k$
\EndFor
\end{algorithmic}
\end{algorithm}

The directional derivative estimate $\hat{g}_k$ is a \emph{noisy gradient estimator}. To compute its expectation, note that for a uniform random unit vector $u$ on the sphere $S^{d-1}$:
\begin{equation}
\mathbb{E}_u\!\left[\frac{f(x + \delta u) - f(x)}{\delta}\,u\right] = \mathbb{E}_u\!\left[(\nabla f(x)^T u + O(\delta))\,u\right] = \nabla f(x)\,\mathbb{E}_u[u u^T] + O(\delta) = \frac{1}{d}\nabla f(x) + O(\delta),
\end{equation}
using $\mathbb{E}[uu^T] = \frac{1}{d}I_d$ for isotropic unit vectors. Thus $\hat{g}_k$ estimates $\frac{1}{d}\nabla f(x_k)$ rather than $\nabla f(x_k)$ itself, requiring the step size to be scaled by $d$ relative to gradient descent, or equivalently using $\alpha_{\text{eff}} = \alpha \cdot d$ as the effective learning rate. In the experiments, $d = 2$, so the underestimation factor is small; for $d = 1000$, the effective step would be $1000\times$ smaller, dramatically slowing convergence. Crucially, only \emph{two} function evaluations are required per iteration regardless of $d$, compared to $d+1$ for the finite-difference gradient. The variance of $\hat{g}_k$ scales as $\mathrm{Var}[\hat{g}_k] \approx \frac{1}{d}\|\nabla f(x)\|^2$ (with $d = 2$ here), so the signal-to-noise ratio is $O(1/\sqrt{d})$ per iteration, requiring $O(d^2)$ iterations to achieve the same precision as a single exact gradient step---a significant cost in high dimensions, but $O(1)$ FE/iter keeps the total function evaluation count at $O(2d^2)$, matching the $O(d)$ evaluations times $O(d)$ iterations of FD gradient descent.

\subsubsection{Finite Difference Error Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q4_fd_error_delta.pdf}
\caption{Q4A: Forward finite-difference gradient error $\|\hat{g}_\delta - g\|$ as a function of step size $\delta$ at $x = (0.3, 2.5)$ on Benchmark B. The error exhibits the classic U-shaped behaviour on a log-log scale: for small $\delta$, roundoff error $O(\varepsilon_{\rm mach}/\delta)$ dominates; for large $\delta$, truncation error $O(\delta)$ dominates. The optimal balance occurs near $\delta^* = \sqrt{2\varepsilon_{\rm mach}} \approx 2.1 \times 10^{-8}$. The experimentally tested $\delta = 0.05$ falls in the truncation-dominated regime but is still 6 orders of magnitude above $\delta^*$; the function's smoothness (bounded second derivatives) keeps the resulting error modest. The value $\delta = 0.8$ incurs severe truncation error of $O(0.8)$.}
\label{fig:q4_fd_error}
\end{figure}

\subsubsection{Convergence Results}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q4_fval_B.pdf}
\caption{Q4A: Convergence on Benchmark B. Exact GD converges fastest. FD GD with $\delta = 0.05$ closely tracks exact GD, confirming that a well-chosen $\delta$ (even well above the optimal $10^{-8}$) can yield accurate gradients for smooth functions like Benchmark B. FD GD with $\delta = 0.8$ converges poorly: the large perturbation spans a region where the curvature changes significantly (the sinusoidal term $\sin(x_1)$ has half-period $\pi \approx 3.14 > 0.8$, so $\delta = 0.8$ samples across a significant fraction of a period). Nesterov random search converges slowest, as the single random direction per step provides only a projection of the gradient.}
\label{fig:q4_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q4_contour_B.pdf}
\caption{Q4A: Trajectories on Benchmark B. Exact GD and FD with $\delta = 0.05$ follow nearly identical paths, demonstrating gradient fidelity of the small-$\delta$ approximation. FD with $\delta = 0.8$ deviates in direction due to truncation error in the sinusoidal term. Nesterov random search follows a noisy, meandering path consistent with one-dimensional gradient estimates.}
\label{fig:q4_contour_B}
\end{figure}

\subsubsection{Central Differences: A More Accurate Alternative}

The \textbf{central finite difference} approximation:
\begin{equation}
\frac{\partial f}{\partial x_i}(x) \approx \frac{f(x + \delta e_i) - f(x - \delta e_i)}{2\delta}
\end{equation}
has a truncation error of $O(\delta^2)$ rather than $O(\delta)$ for forward differences, because the first-order error terms cancel in the symmetric formula. The optimal central-difference step is $\delta^{\star}_{\text{central}} = (3\varepsilon_{\mathrm{mach}})^{1/3} \approx 6.7 \times 10^{-6}$ (versus $1.5 \times 10^{-8}$ for forward differences), achieving a roundoff-corrected error of $O(\varepsilon_{\mathrm{mach}}^{2/3}) \approx 3.7 \times 10^{-11}$ versus $O(\varepsilon_{\mathrm{mach}}^{1/2}) \approx 1.5 \times 10^{-8}$ for forward differences. The cost is $2d$ function evaluations per gradient (versus $d+1$ for forward differences, since the base evaluation $f(x)$ is shared). For the experiments with $d = 2$: central differences require 4 evaluations (vs.\ 3 for forward differences) but deliver 3 more orders of magnitude of accuracy near the optimum. With $\delta = 0.05$, both forward and central differences are in the truncation-dominated regime, and central differences would be about $4\times$ more accurate ($0.05^2$ vs.\ $0.05$ truncation). In practice, central differences are preferred when function evaluations are affordable and gradient accuracy is critical.

\subsection{Part B: Derivative-Free Methods on Benchmark C (Rosenbrock)}

\subsubsection{Nelder--Mead Simplex Method}

The Nelder--Mead algorithm maintains a simplex of $n+1 = 3$ vertices in $\mathbb{R}^2$ and iteratively refines it through four operations:
\begin{enumerate}[nosep]
\item \textbf{Reflection} ($\alpha_r = 1$): move the worst vertex through the centroid of the remaining vertices: $x_r = c + \alpha_r(c - x_{\text{worst}})$.
\item \textbf{Expansion} ($\gamma = 2$): if reflection improved, try a further step: $x_e = c + \gamma(c - x_{\text{worst}})$.
\item \textbf{Contraction} ($\rho = 0.5$): if reflection worsened, contract towards the centroid.
\item \textbf{Shrink} ($\sigma = 0.5$): if contraction fails, contract all non-best vertices towards the best.
\end{enumerate}
No gradient or Hessian information is required. Settings: initial simplex step $= 0.35$, 160 iterations.

\begin{remark}[Convergence Rate and Degeneracy of Nelder--Mead]
Nelder--Mead has \textbf{no known convergence rate} for $d \geq 2$. McKinnon (1998) constructed an explicit counterexample in $\mathbb{R}^2$ where Nelder--Mead converges to a non-stationary point---a point that is not even a local minimum---despite all simplex operations reducing the vertex values. The mechanism is \emph{simplex degeneracy}: when the $n+1 = 3$ vertices become nearly collinear (degenerate simplex), reflection steps move the worst vertex back along the same line, and no expansion can restore non-degeneracy. In this degenerate state, the algorithm loses its ability to explore directions transverse to the degenerate edge.

In practice, degeneracy is mitigated by: (1) monitoring the simplex diameter $\max_{i,j}\|v_i - v_j\|$ and restarting with a fresh non-degenerate simplex if it falls below a threshold; (2) ensuring the initial step $> 0$ so that $v_0, v_1, v_2$ are non-collinear; and (3) periodic shrink operations. For the $d=2$ Rosenbrock problem, Nelder--Mead typically works well because the banana-shaped valley is smooth and the simplex can track it: reflection moves the worst vertex across the valley, expansion extends it further, and the simplex gradually aligns with the valley direction. In higher dimensions ($d \geq 6$), simplex degeneracy is common and Nelder--Mead is generally not recommended; derivative-free alternatives such as COBYLA, BOBYQA, or model-based methods (e.g., DFO-LS) are preferred.
\end{remark}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q4_nelder_mead_C.pdf}
\caption{Q4B: Nelder--Mead on Rosenbrock. Left: the simplex centroid trajectory navigates the curved valley without gradient information, demonstrating that the simplex geometry (reflection and expansion) adaptively identifies descent directions. Right: the best objective value decreases steadily, reaching low values as the simplex contracts towards the valley floor. Nelder--Mead is particularly well-suited to low-dimensional problems where the simplex can effectively cover the search space.}
\label{fig:q4_nm}
\end{figure}

\subsubsection{Grid Search}

Grid search evaluates the objective on a uniform $55 \times 55 = 3025$ grid over $x_1 \in [-2, 2]$, $x_2 \in [-1, 3]$, covering the full domain with resolution $\Delta x_1 \approx 0.074$ and $\Delta x_2 \approx 0.073$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q4_grid_search_C.pdf}
\caption{Q4B: Grid search on Rosenbrock. Left: the 3025 grid points cover the search region uniformly; the best point found is near the true optimum $(1,1)$ but offset by up to $\Delta x/2 \approx 0.037$ due to discretisation. Right: the best-so-far curve shows step-wise improvement as progressively better grid points are encountered during the scan. Grid search provides a guarantee of finding the global optimum to within grid resolution $O(\Delta x)$, but requires $O(N^d)$ evaluations---exponentially costly in dimension $d$.}
\label{fig:q4_grid}
\end{figure}

\subsection{Computational Comparison}

\begin{table}[H]
\centering
\caption{Q4: Comparison of methods. FE = function evaluations per iteration, GE = gradient evaluations per iteration, $d$ = dimension. ``Exact GD'' uses the analytic gradient and requires no function evaluations. Gradient error column shows the order of the approximation error in terms of step size $\delta$ or machine epsilon $\varepsilon_{\rm mach}$.}
\label{tab:q4_comparison}
\begin{tabular}{lcccccc}
\toprule
\textbf{Method} & \textbf{Gradient?} & \textbf{GE/iter} & \textbf{FE/iter} & \textbf{Scalability} & \textbf{Accuracy} & \textbf{Grad.\ error} \\
\midrule
Exact GD & Analytic & 1 & 0 & Very good & High & 0 \\
Fwd FD ($\delta$ small) & Approximate & 0 & $d+1$ & Good ($O(d)$) & High & $O(\delta)$ \\
Fwd FD ($\delta$ large) & Poor approx. & 0 & $d+1$ & Good ($O(d)$) & Low & $O(\delta)$ \\
Central FD & Approximate & 0 & $2d$ & Good ($O(d)$) & Higher & $O(\delta^2)$ \\
Nesterov random & None & 0 & 2 & \textbf{Best ($O(1)$)} & Low & $O(\delta) \cdot \frac{1}{\sqrt{d}}$ \\
Nelder--Mead & None & 0 & $\leq n+1$ & Low-dim only & Moderate & N/A \\
Grid search & None & 0 & $N^d$ total & Very poor & Grid-limited & N/A \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Dimensionality analysis:} Grid search with resolution $\Delta x$ requires $(L/\Delta x)^d$ evaluations in a domain of side length $L$. For $d = 100$ and $\Delta x = 0.1$ in $[0,1]^{100}$, this would require $10^{100}$ evaluations---astronomically beyond any computational budget. This \emph{curse of dimensionality} makes grid search inapplicable beyond $d \approx 4$--$5$ in practice.

Finite-difference gradient descent scales as $O(d)$ evaluations per iteration (one base evaluation plus one per coordinate): for $d = 2$ this is 3 evaluations/iteration, for $d = 10^4$ this becomes $10^4$ evaluations/iteration---still feasible, but expensive. The approximation error is $O(\delta)$ uniformly over all components when the Hessian is bounded.

Nesterov random search's $O(1)$ cost per iteration---only 2 function evaluations regardless of $d$---is the critical advantage for very high dimensions. However, the variance of the gradient estimator scales as $O(d)$: $\mathrm{Var}[\hat{g}_k] = \frac{1}{\delta^2}\mathrm{Var}[f(x+\delta u) - f(x)] \cdot \|u\|^2 \propto d \cdot \|\nabla f(x)\|^2$. Thus, while the cost is $O(1)$, the effective signal-to-noise ratio decreases as $O(1/\sqrt{d})$, requiring $O(d/\varepsilon^2)$ total iterations to achieve precision $\varepsilon$---the same total cost as finite-difference gradient descent but with much smaller memory requirements.

% ============================================================
\section{Question 5: Constrained Optimisation \hfill [30 marks]}
\label{sec:q5}
% ============================================================

\subsection{Problem Formulation and KKT Conditions}

The constrained problem is:
\begin{equation}
\min_{x \in \mathbb{R}^2}\; f(x) = (x_1 - 1)^2 + 5(x_2 - 2)^2 + \sin(x_1), \quad \text{subject to}\quad x_1 \geq 0.5.
\label{eq:q5_prob}
\end{equation}
Starting point: $x_0 = (0.2, 4.0)$, which is \emph{infeasible} since $x_1^0 = 0.2 < 0.5$.

The Karush--Kuhn--Tucker (KKT) optimality conditions for~\eqref{eq:q5_prob} require at the constrained optimum $x^{\star}$:
\begin{align}
\nabla f(x^{\star}) - \mu^{\star}\, e_1 &= 0 \quad (\text{stationarity, where } e_1 = [1, 0]^T) \\
x_1^{\star} &\geq 0.5 \quad (\text{primal feasibility}) \\
\mu^{\star} &\geq 0 \quad (\text{dual feasibility}) \\
\mu^{\star}(x_1^{\star} - 0.5) &= 0 \quad (\text{complementary slackness}).
\end{align}

Since the unconstrained minimum of $f$ lies at $x^{\star}_{\mathrm{unc}} \approx (0.5823, 2.0)$ with $x_1^{\star} \approx 0.5823 > 0.5$, the constraint is \emph{inactive} at the unconstrained minimiser. By complementary slackness, $\mu^{\star} = 0$, and the KKT conditions reduce to $\nabla f(x^{\star}) = 0$. Therefore, the constrained and unconstrained minima coincide: $x^{\star}_{\mathrm{con}} = x^{\star}_{\mathrm{unc}} \approx (0.5823, 2.0)$, $f^{\star} \approx 0.7244$.

This observation is important: the constraint $x_1 \geq 0.5$ is \emph{not binding} at the optimum, so the challenge lies entirely in handling the infeasible starting point and ensuring the iterates reach the feasible region.

\textbf{Sensitivity analysis: what if the constraint were active?} Consider tightening the constraint to $x_1 \geq b$ for varying $b$. For $b \leq x_1^{\star}_{\text{unc}} \approx 0.582$, the constraint is inactive and $x^{\star}_{\text{con}} = x^{\star}_{\text{unc}}$, $f^{\star} \approx 0.7244$, $\mu^{\star} = 0$. For $b > 0.582$, the constraint becomes \emph{active} at the optimum: $x_1^{\star} = b$, and the KKT stationarity condition $\nabla f(x^{\star}) = \mu^{\star} e_1$ gives:
\begin{equation}
2(b - 1) + \cos(b) = \mu^{\star}, \quad x_2^{\star} = 2 \quad (\text{from } 10(x_2^{\star}-2) = 0).
\end{equation}
The optimal dual variable $\mu^{\star} = 2(b-1) + \cos(b)$ is positive for $b > 0.582$ (since at $b = 0.582$, $\mu^{\star} = 2(0.582-1) + \cos(0.582) \approx -0.836 + 0.836 = 0$, and the expression is increasing in $b$ for $b \in [0.5, 1.5]$). For example, at $b = 0.8$: $\mu^{\star} = 2(0.8-1) + \cos(0.8) = -0.4 + 0.697 = 0.297$. The optimal value $f^{\star}(b) = (b-1)^2 + 5(2-2)^2 + \sin(b) = (b-1)^2 + \sin(b)$ increases as $b$ increases beyond 0.582. This dual variable $\mu^{\star}$ equals the \emph{shadow price} of the constraint: $\partial f^{\star}/\partial b = \mu^{\star}$, meaning each unit tightening of the bound costs $\mu^{\star}$ in optimal objective value.

\begin{algorithm}[H]
\caption{Projected Gradient Descent}
\label{alg:pgd}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, projection $\Pi_{\mathcal{X}}$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_{k+1} \gets x_k - \alpha\, \nabla f(x_k)$ \Comment{Unconstrained gradient step}
    \State $x_{k+1} \gets \Pi_{\mathcal{X}}(z_{k+1}) = (\max(0.5,\, z_1),\, z_2)$ \Comment{Hard feasibility enforcement}
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{algorithm}[H]
\caption{Penalty Method (Linear Penalty)}
\label{alg:penalty}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, penalty weight $\lambda$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $F_{\lambda}(x) \gets f(x) + \lambda \max(0,\, 0.5 - x_1)$ \Comment{Penalised objective}
    \State $\nabla F_{\lambda}(x) \gets \nabla f(x) + \lambda \cdot \mathbf{1}_{[x_1 < 0.5]}(-e_1)$ \Comment{Subgradient}
    \State $x_{k+1} \gets x_k - \alpha\, \nabla F_{\lambda}(x_k)$
\EndFor
\end{algorithmic}
\end{algorithm}

Settings: Projected GD ($\alpha = 0.08$); Penalty with $\lambda \in \{0.15, 1.8, 4.5\}$ ($\alpha = 0.05$ for $\lambda \leq 1.8$; $\alpha = 0.03$ for $\lambda = 4.5$, as larger $\lambda$ increases the effective Lipschitz constant of $F_\lambda$ and requires a smaller step); unconstrained GD ($\alpha = 0.07$). All methods run for 100 iterations.

\subsection{Results}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q5_fval.pdf}
\caption{Q5: Objective $f(x)$ vs.\ iteration. Projected GD converges directly to the constrained minimum $f \approx 0.7244$ at $(0.5823, 2.0)$. Penalty methods with larger $\lambda$ approach the same constrained minimum. With $\lambda = 0.15$, the penalty gradient is too weak relative to $\nabla f$ and the iterate converges at a point with residual infeasibility. Unconstrained GD converges to the same point as projected GD (since the constraint is inactive at the optimum), but may traverse infeasible regions along the way.}
\label{fig:q5_fval}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q5_contour.pdf}
\caption{Q5: Contour plot with constraint boundary $x_1 = 0.5$ (dashed red) and infeasible region (shaded). The starting point $(0.2, 4.0)$ is infeasible. Projected GD immediately projects to $x_1 = 0.5$ after the first gradient step and thereafter optimises within the feasible half-space. Unconstrained GD passes through infeasible regions but ultimately converges to the same optimum (since the constraint is inactive). The penalty trajectories bend progressively towards feasibility as $\lambda$ increases.}
\label{fig:q5_contour}
\end{figure}

\subsection{Constraint Violation Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q5_violation.pdf}
\caption{Q5: Constraint violation $\max(0,\, 0.5 - x_1)$ on logarithmic scale. Left: all 100 iterations. Right: first 30 iterations (zoomed). Projected GD achieves \textbf{zero violation from iteration 1}---the projection $x_1 \leftarrow \max(0.5, x_1)$ enforces $x_1 \geq 0.5$ as a hard constraint at every step. The penalty method with $\lambda = 4.5$ achieves feasibility within $\sim$10 iterations; $\lambda = 1.8$ takes longer; $\lambda = 0.15$ shows persistent violation because the penalty gradient $\lambda \cdot \mathbf{1}_{[x_1 < 0.5]}$ is dominated by $\nabla f$.}
\label{fig:q5_violation}
\end{figure}

\subsection{Comparative Discussion}

\begin{table}[H]
\centering
\caption{Q5: Final positions and constraint violations after 100 iterations.}
\label{tab:q5_final}
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{Final $x$} & \textbf{$f(x)$} & \textbf{Violation} & \textbf{Feasible from iter.} \\
\midrule
Projected GD & $(0.582, 2.000)$ & 0.7244 & 0.0000 & 1 \\
Penalty $\lambda = 4.5$ & $(0.583, 2.000)$ & 0.7244 & 0.0000 & $\sim$10 \\
Penalty $\lambda = 1.8$ & $(0.582, 2.000)$ & 0.7244 & $\approx 0$ & $\sim$30 \\
Penalty $\lambda = 0.15$ & --- & --- & $>0$ & Never \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Projected GD} enforces hard feasibility at every iteration: $x_k \in \mathcal{X}$ for all $k \geq 1$. The projection $\Pi_\mathcal{X}(z) = (\max(0.5, z_1), z_2)$ is trivial for this halfspace constraint.

\begin{proposition}[Projected GD Convergence]
For an $L$-smooth convex function $f$ and convex closed $\mathcal{X}$, projected GD with $\alpha = 1/L$ satisfies:
\begin{equation}
f(x_k) - f^{\star} \leq \frac{L\|x_0 - x^{\star}\|^2}{2k}.
\end{equation}
\end{proposition}

The starting point $x_0 = (0.2, 4.0)$ has $\|x_0 - x^{\star}\|^2 = (0.2-0.582)^2 + (4.0-2.0)^2 \approx 0.146 + 4.0 = 4.146$. After projection at iteration 1, $x_1 = (0.5, \cdot)$ is feasible, and subsequent convergence is dictated by the gradient step within $\mathcal{X}$.

\textbf{Penalty methods} replace exact feasibility with a soft constraint term. As $\lambda \to \infty$, the penalised minimiser converges to the constrained minimiser, but large $\lambda$ stiffens the problem (increasing its effective Lipschitz constant to $L + \lambda$), requiring smaller step sizes. The penalty $\lambda \max(0, 0.5 - x_1)$ is a \emph{linear penalty}, which is exact (recovers the constrained solution at finite $\lambda$) when the KKT conditions hold. Here, since the constraint is inactive at the optimum, any $\lambda > 0$ suffices in theory, but a very small $\lambda$ converges slowly.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q5_penalty_sensitivity.pdf}
\caption{Q5: Sensitivity analysis for the penalty weight $\lambda$ across a wider range $\lambda \in \{0.05, 0.15, 0.5, 1.8, 4.5, 12\}$. Left: objective convergence---all sufficiently large $\lambda$ converge to $f^* \approx 0.7244$, while $\lambda = 0.05$ and $\lambda = 0.15$ stagnate above the optimum due to insufficient penalisation. Right: constraint violation---higher $\lambda$ enforces feasibility faster but requires smaller step sizes for stability. Projected GD (black) maintains zero violation throughout. The trade-off between $\lambda$ (feasibility enforcement speed) and $\alpha$ (step size) is a fundamental limitation of penalty methods: as $\lambda \to \infty$, the required $\alpha \to 0$.}
\label{fig:q5_penalty_sensitivity}
\end{figure}

\subsection{Augmented Lagrangian: A Superior Alternative to Linear Penalty}

The \textbf{augmented Lagrangian method} (ALM, also known as the method of multipliers) overcomes the key limitation of linear penalty methods by combining a Lagrange multiplier $\mu$ with a quadratic penalty:
\begin{equation}
\mathcal{L}_\rho(x, \mu) = f(x) + \mu\,(0.5 - x_1) + \frac{\rho}{2}\max(0,\, 0.5 - x_1)^2.
\end{equation}
The alternating minimisation scheme iterates:
\begin{align}
x^{k+1} &= \arg\min_{x}\; \mathcal{L}_\rho(x,\, \mu^k) \quad \text{(primal update, inner optimisation)}, \\
\mu^{k+1} &= \mu^k + \rho\,\max(0,\, 0.5 - x_1^{k+1}) \quad \text{(dual ascent step)}.
\end{align}
The dual update accumulates the constraint violation, driving it to zero at a \emph{fixed} (finite) penalty $\rho$. This is the critical advantage over the linear penalty method, which requires $\lambda \to \infty$ to achieve exact feasibility. The ALM at fixed $\rho$ converges to the exact constrained solution because the multiplier $\mu$ effectively shifts the unconstrained minimiser of $\mathcal{L}_\rho(\cdot, \mu^k)$ to account for the constraint, rather than steepening the penalty.

\textbf{Comparison in the present problem:} Since the constraint $x_1 \geq 0.5$ is \emph{inactive} at the unconstrained minimum ($x_1^{\star} \approx 0.582 > 0.5$), the optimal Lagrange multiplier is $\mu^{\star} = 0$ (by complementary slackness). Both the linear penalty and augmented Lagrangian converge to the same solution, and the dual ascent update $\mu^{k+1} = \mu^k + \rho\max(0, 0.5 - x_1^{k+1})$ drives $\mu^k \to 0$ as $x_1^k \to 0.582 > 0.5$. For this particular inactive-constraint problem, ALM offers no material advantage over the linear penalty.

\textbf{Advantage for active constraints:} If instead the unconstrained minimum were infeasible (e.g., $x_1^{\star}_{\text{unc}} = 0.3 < 0.5$), then the constrained optimum would have $x_1^{\star} = 0.5$ and $\mu^{\star} > 0$. The linear penalty would require $\lambda \to \infty$ (ill-conditioning the problem), while the ALM would converge to $\mu^{\star}$ at a moderate fixed $\rho$, maintaining well-conditioned primal subproblems throughout. The ALM is therefore strongly preferred for general constrained optimisation and is the basis of practical solvers such as ADMM (Alternating Direction Method of Multipliers), widely used in distributed optimisation and signal processing.

\textbf{Key trade-off:} Projected GD requires a projection oracle (cheap for simple constraint sets like halfspaces or boxes, but potentially expensive for general convex sets). Penalty methods only require gradient evaluations of a modified objective but cannot guarantee strict feasibility at intermediate iterates.

% ============================================================
\section{Question 6: Linear Programmes and Frank--Wolfe Algorithm \hfill [30 marks]}
\label{sec:q6}
% ============================================================

\subsection{Feasible Set and Linear Programme}

Feasible set: $\mathcal{X} = \{x \in \mathbb{R}^2 : 0.5 \leq x_1 \leq 5,\ -5 \leq x_2 \leq 10\}$ (a bounded box).

\textbf{Linear Programme:} For $f(x) = a^T x$ with $a = [1, 2]^T$, the minimum over $\mathcal{X}$ is:
\begin{equation}
x^{\star} = \arg\min_{x \in \mathcal{X}}\, (x_1 + 2x_2) = (0.5,\, -5), \qquad f^{\star} = 0.5 + 2(-5) = -9.5.
\end{equation}
Since both components of $a$ are positive, each coordinate is independently minimised at its lower bound. This illustrates the \textbf{fundamental theorem of linear programming}: the optimum of a linear objective over a bounded polytope is always attained at a vertex (extreme point) of the feasible set. The optimum never lies in the interior of a facet unless the objective is parallel to that facet, in which case the entire facet is optimal.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q6_lp.pdf}
\caption{Q6-I: Feasible region (blue box) and linear objective contours $f(x) = x_1 + 2x_2$. Contour lines are parallel and tilted, and the minimum is at the lower-left corner $(0.5, -5)$---the vertex of $\mathcal{X}$ where both coordinates are minimised simultaneously.}
\label{fig:q6_lp}
\end{figure}

\subsection{Frank--Wolfe Algorithm}

\begin{algorithm}[H]
\caption{Frank--Wolfe (Conditional Gradient)}
\label{alg:fw}
\begin{algorithmic}[1]
\Require Initial feasible point $x_0 \in \mathcal{X}$, mixing parameter $\beta$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_k \gets \arg\min_{x \in \mathcal{X}}\, \nabla f(x_k)^T x$ \Comment{Linear minimisation oracle (LMO)}
    \State $x_{k+1} \gets \beta\, x_k + (1 - \beta)\, z_k$ \Comment{Convex combination (remains in $\mathcal{X}$)}
\EndFor
\end{algorithmic}
\end{algorithm}

The Linear Minimisation Oracle (LMO) for the box $\mathcal{X}$ decomposes per coordinate:
\begin{equation}
(z_k)_i = \begin{cases} l_i & \text{if } (\nabla f(x_k))_i > 0 \\ u_i & \text{if } (\nabla f(x_k))_i \leq 0 \end{cases}, \quad l_i \leq x_i \leq u_i.
\end{equation}
This requires $O(d)$ operations per iteration. Since $x_{k+1} = \beta x_k + (1-\beta) z_k$ is a convex combination of $x_k \in \mathcal{X}$ and $z_k \in \mathcal{X}$, the iterates remain feasible \emph{without projection}.

\textbf{The Frank--Wolfe gap} (duality gap) at iteration $k$ is:
\begin{equation}
\delta_k = \nabla f(x_k)^T (x_k - z_k) \geq 0.
\end{equation}
For convex $f$, $\delta_k \geq f(x_k) - f^{\star}$ provides a computable upper bound on the primal gap and serves as a natural stopping criterion: if $\delta_k < \varepsilon$, then $f(x_k) - f^{\star} < \varepsilon$.

\begin{theorem}[Frank--Wolfe Convergence]
Let $f$ be $L$-smooth and convex, and $\mathcal{X}$ bounded with diameter $D = \max_{x,y \in \mathcal{X}} \|x - y\|$. With step size $\gamma_k = 2/(k+2)$ (i.e., $\beta_k = k/(k+2)$):
\begin{equation}
f(x_k) - f^{\star} \leq \frac{2LD^2}{k+2} = O\!\left(\frac{1}{k}\right).
\end{equation}
The fixed-$\beta$ implementation used here does not achieve this optimal schedule but captures the qualitative convergence behaviour.
\end{theorem}

\subsection{Interior Optimum: $f(x) = (x_1 - 1)^2 + (x_2 - 5)^2$}

The unconstrained minimum $(1, 5)$ lies inside $\mathcal{X}$ (since $0.5 \leq 1 \leq 5$ and $-5 \leq 5 \leq 10$). Starting from $(1, 1)$. Two mixing parameters tested: $\beta = 0.90$ (larger steps) and $\beta = 0.985$ (smaller steps).

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q6_fw_convergence.pdf}
\caption{Q6-II: Frank--Wolfe convergence for the interior optimum. With $\beta = 0.985$ (1.5\% step towards vertex), the final objective ($f = 0.0046$) is lower than with $\beta = 0.90$ ($f = 0.0089$). This occurs because larger steps ($1-\beta = 0.10$) cause the iterate to jump sharply towards whichever vertex minimises the linearisation at each step, inducing oscillation as $z_k$ alternates between different vertices. Conservative steps ($\beta = 0.985$) allow smoother convergence with less oscillation.}
\label{fig:q6_fw_conv}
\end{figure}

\subsection{Boundary Optimum: $f(x) = x_1^2 + x_2^2$}

The unconstrained minimum $(0, 0)$ lies outside $\mathcal{X}$ (since $x_1 \geq 0.5$). The constrained minimum is at the boundary point $(0.5, 0)$ with $f^{\star} = 0.25$. Starting from $(3, 3)$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q6_fw_contour.pdf}
\caption{Q6: Frank--Wolfe trajectories. Left (interior optimum, $f=(x_1-1)^2+(x_2-5)^2$): both $\beta$ values converge towards $(1, 5)$ from $(1, 1)$, with $\beta = 0.985$ yielding a smoother path. Right (boundary optimum, $f=x_1^2+x_2^2$): the trajectory converges towards $(0.5, 0)$ on the constraint boundary. Frank--Wolfe naturally pushes iterates towards the feasible region's vertices, making it effective for boundary optima.}
\label{fig:q6_fw_contour}
\end{figure}

\subsection{Evolution of $x_k$ and $z_k$}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q6_evolution.pdf}
\caption{Q6: Evolution of iterates $x_k$ and LMO outputs $z_k$. Top-left ($\beta = 0.90$, interior): $x_k$ converges but $z_k$ oscillates between vertices $(0.5, 10)$ and $(0.5, -5)$ as the gradient direction fluctuates around the optimum. Top-right ($\beta = 0.985$): slower, smoother convergence with reduced $z_k$ oscillation. Bottom-left (boundary): $x_1$ monotonically approaches 0.5 as the LP vertex consistently selects the lower bound; $x_2$ converges towards 0. Bottom-right: convergence comparison showing boundary case reaches $f = 0.364$ after 140 iterations (above $f^{\star} = 0.25$), indicating further iterations are needed.}
\label{fig:q6_evolution}
\end{figure}

\begin{table}[H]
\centering
\caption{Q6: Final Frank--Wolfe positions and objective values.}
\label{tab:q6_final}
\begin{tabular}{lcccc}
\toprule
\textbf{Case} & \textbf{$\beta$} & \textbf{Final $x$} & \textbf{$f(x)$} & \textbf{$f^{\star}$} \\
\midrule
Interior & 0.90 & $(1.066, 4.932)$ & 0.0089 & 0.0 \\
Interior & 0.985 & $(0.997, 5.067)$ & 0.0046 & 0.0 \\
Boundary & 0.93 & $(0.500, -0.337)$ & 0.3637 & 0.25 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Frank--Wolfe Gap Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q6_fw_gap.pdf}
\caption{Q6: Frank--Wolfe gap $\delta_k = \nabla f(x_k)^T(x_k - z_k)$ compared to the objective $f(x_k)$ for the interior optimum problem. Left: both the objective and gap are plotted; the gap is always at least as large as $f(x_k) - f^* = f(x_k)$ (since $f^*=0$ here for the centred formulation), serving as a valid upper bound on the primal suboptimality. Right: the gap closely tracks the $O(1/k)$ reference curve (shown as dotted line), consistent with the theoretical Frank--Wolfe convergence rate. The oscillation in the gap for $\beta = 0.90$ reflects the jumping of $z_k$ between different vertices of $\mathcal{X}$.}
\label{fig:q6_fw_gap}
\end{figure}

\subsection{Interior vs.\ Boundary Behaviour}

\textbf{Interior optimum:} Frank--Wolfe exhibits characteristic ``zig-zag'' behaviour: the LP vertex $z_k$ jumps between extreme points of $\mathcal{X}$ at each step, and the convex combination gradually averages these jumps towards the interior optimum. The theoretical $O(1/k)$ rate is significantly slower than projected gradient descent's $O(1/k)$ rate with the same constant, and far slower than Newton's $O(1/k^2)$ or better. However, Frank--Wolfe's advantage is the \emph{avoidance of any projection}: all that is needed is the LMO, which for polytopes is often more efficient than computing a general projection.

\textbf{Boundary optimum:} Frank--Wolfe is naturally suited to boundary optima because the LMO $z_k$ frequently selects the vertex closest to the constrained solution, driving iterates towards the boundary without explicit projection. The fixed $\beta = 0.93$ schedule provides a step size of $1 - \beta = 0.07$ towards the vertex per iteration. Under the optimal schedule $\gamma_k = 2/(k+2)$, convergence would be faster, especially in early iterations.

\textbf{Explicit convergence bound for the boundary case:} For $f(x) = x_1^2 + x_2^2$, the Hessian is $\nabla^2 f = 2I$, so the Lipschitz constant of the gradient is $L = 2$. The diameter of $\mathcal{X} = [0.5, 5] \times [-5, 10]$ is $D = \sqrt{(5-0.5)^2 + (10+5)^2} = \sqrt{20.25 + 225} = \sqrt{245.25} \approx 15.66$. The Frank--Wolfe theorem with optimal schedule gives:
\begin{equation}
f(x_k) - f^{\star} \leq \frac{2LD^2}{k+2} = \frac{2 \cdot 2 \cdot 245.25}{k+2} = \frac{981}{k+2}.
\end{equation}
After $k = 140$ iterations: upper bound $= 981/142 \approx 6.91$. The actual suboptimality is $f(x_{140}) - f^{\star} = 0.364 - 0.25 = 0.114$---a factor of $\sim60\times$ tighter than the bound, which is typical: the FW theorem bound is dimension-free and worst-case, but practical convergence is often much faster due to favourable problem geometry (here, the unconstrained minimiser $(0,0)$ is directly ``pulled towards'' the nearest feasible point $(0.5, 0)$, and the gradient always points towards $(0,0)$, making the LMO consistently select the $(0.5, \cdot)$ boundary). This illustrates a general principle: theoretical worst-case bounds are useful for comparing algorithm families but often overestimate true convergence times in practice.

\begin{remark}[Affine Invariance of Frank--Wolfe]
Unlike gradient descent, the Frank--Wolfe algorithm is \emph{affine invariant}: its trajectory is unchanged under any invertible affine transformation of the domain. This is because the LMO $z_k = \arg\min_{x \in \mathcal{X}} \nabla f(x_k)^T x$ and the gradient $\nabla f(x_k)$ transform consistently. In contrast, gradient descent's convergence rate depends on the condition number $\kappa = L/\mu$ of the Hessian, which changes under affine transformations. This affine invariance makes Frank--Wolfe naturally suited to problems where the geometry of $\mathcal{X}$ (rather than the function's Hessian) determines the convergence behaviour.
\end{remark}

\begin{remark}[Away-Step Frank--Wolfe Variant]
The standard Frank--Wolfe algorithm converges at rate $O(1/k)$ even for strongly convex objectives, whereas projected gradient descent achieves linear convergence $O(\rho^k)$ with $\rho = 1 - \mu/L < 1$. This gap arises from the characteristic zig-zag: the LMO vertex $z_k$ alternates between different extreme points of $\mathcal{X}$, causing the iterates to oscillate rather than converge monotonically.

The \textbf{away-step Frank--Wolfe} (AFW) algorithm eliminates this zig-zagging by also allowing steps \emph{away from} the currently worst active vertex. At each iteration, AFW computes both:
\begin{enumerate}[nosep]
\item \textbf{FW direction}: $d_k^{\mathrm{FW}} = z_k - x_k$ (step towards the linearisation-minimising vertex $z_k$),
\item \textbf{Away direction}: $d_k^{A} = x_k - s_k^{\phantom{A}}$, where $s_k = \arg\max_{v \in \mathcal{S}_k}\, \nabla f(x_k)^T v$ is the most harmful active vertex ($\mathcal{S}_k$ = support of the current active-set representation of $x_k$).
\end{enumerate}
The direction giving greater linearised descent $(-\nabla f(x_k)^T d)$ is chosen. Away steps effectively remove weight from bad vertices without requiring a projection. For polytope-constrained strongly convex objectives, AFW provably achieves \emph{linear} convergence $O\bigl((1 - \delta_{\mathcal{X}} \mu/L)^k\bigr)$ where $\delta_{\mathcal{X}} \in (0,1]$ is the \emph{pyramidal width} of $\mathcal{X}$ (a geometric constant). This matches projected GD's linear rate up to problem-geometry constants.

The trade-off: AFW requires maintaining an explicit active-set representation of $x_k$ as a convex combination of vertices, adding $O(k)$ storage overhead and bookkeeping. The standard Frank--Wolfe algorithm used in this assignment is sufficient for the given problems (neither case requires very tight convergence), but AFW would converge substantially faster for the boundary optimum $f = x_1^2 + x_2^2$ where the final iterate lies at the vertex $(0.5, 0)$ and zig-zagging is most pronounced.
\end{remark}

\textbf{Comparison with projected GD:} The Frank--Wolfe algorithm offers:
\begin{itemize}[nosep]
\item \textbf{Advantage}: No projection oracle needed; iterates are always in $\mathcal{X}$; sparse iterates (convex combinations of vertices) with potential memory efficiency.
\item \textbf{Disadvantage}: Slower $O(1/k)$ convergence (no acceleration possible without losing projection-free property); zig-zag behaviour near the optimum. The away-step variant achieves linear convergence at the cost of active-set bookkeeping.
\end{itemize}

% ============================================================
\newpage
\section{Conclusion}
\label{sec:conclusion}
% ============================================================

This report has systematically evaluated six categories of optimisation algorithms across three benchmark problems of increasing difficulty. The key findings are synthesised below.

\subsection{All-Methods Comparison on Benchmark B}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/all_methods_benchmark_B.pdf}
\caption{Comprehensive comparison of all Q1 and Q2 methods on Benchmark B (toy neural network loss, $\kappa \approx 6.9$), starting from $x_0 = (-1, 4)$. Left: all seven methods including the diverging Polyak ($f^{\star}=0$ misspecified); Polyak's objective grows to $\sim$$10^4$ before saturation. Right: well-behaved methods with $O(1/k)$ and $O(1/k^2)$ reference lines. Nesterov and Adam converge most rapidly and track the $O(1/k^2)$ reference closely in early iterations; both reach machine-precision suboptimality by iteration $\sim$60. Heavy Ball converges only slightly slower than Nesterov. The moderate condition number $\kappa \approx 6.9$ means all correctly-specified first-order methods perform adequately, in contrast to Rosenbrock ($\kappa \approx 2504$) where method differences are stark.}
\label{fig:all_methods_B}
\end{figure}

\subsection{All-Methods Comparison on Rosenbrock}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/all_methods_rosenbrock.pdf}
\caption{Comprehensive comparison of all studied first-order and second-order methods on Benchmark C (Rosenbrock, $\kappa \approx 2504$), all starting from $x_0 = (-1, 1)$. Newton's method (magenta squares, 20 iterations with $\alpha = 0.22$) achieves the lowest final value among all methods after only 20 steps. Among 120-iteration first-order methods, Polyak (with correct $f^*=0$) and Nesterov achieve the lowest final values, followed by Heavy Ball and Adagrad. Standard GD barely moves along the valley in 120 iterations. The gap between methods is most pronounced on this benchmark, underscoring how severely extreme ill-conditioning ($\kappa \approx 2504$) challenges first-order methods with constant step sizes.}
\label{fig:all_methods_rosenbrock}
\end{figure}

\subsection{Convergence Rate Illustration}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/convergence_rates_A.pdf}
\caption{Benchmark A: suboptimality $f(x_k) - f^*$ vs iteration on a log scale, with $O(1/k)$ and $O(1/k^2)$ reference lines. Nesterov's curve most closely tracks the $O(1/k^2)$ reference, consistent with the theoretical accelerated rate. Heavy Ball converges at a similar rate due to its momentum, while GD follows the $O(1/k)$ linear convergence rate of gradient descent on strongly convex problems. Adam's convergence is rapid initially but levels off due to the adaptive step-size scaling.}
\label{fig:convergence_rates_A}
\end{figure}

\subsection{Method Performance Summary}

\begin{table}[H]
\centering
\caption{Summary of all methods across benchmarks. Ratings: {\color{blue}$\bullet\bullet\bullet$} = best in class, {\color{orange}$\bullet\bullet$} = competitive, $\bullet$ = limited. GD(b) = GD baseline.}
\label{tab:summary}
\begin{tabular}{lccccc}
\toprule
\textbf{Method} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} & \textbf{Cost/iter} & \textbf{Robustness} \\
\midrule
GD (baseline) & {\color{orange}$\bullet\bullet$} & {\color{orange}$\bullet\bullet$} & $\bullet$ & $O(d)$ & High \\
Polyak & $\bullet$ & $\bullet$ & {\color{blue}$\bullet\bullet\bullet$} & $O(d)$ & Low \\
Adagrad & {\color{orange}$\bullet\bullet$} & {\color{orange}$\bullet\bullet$} & {\color{orange}$\bullet\bullet$} & $O(d)$ & High \\
RMSprop & {\color{orange}$\bullet\bullet$} & {\color{orange}$\bullet\bullet$} & $\bullet$ & $O(d)$ & Medium \\
Heavy Ball & {\color{orange}$\bullet\bullet$} & {\color{orange}$\bullet\bullet$} & {\color{orange}$\bullet\bullet$} & $O(d)$ & Medium \\
Nesterov & {\color{blue}$\bullet\bullet\bullet$} & {\color{blue}$\bullet\bullet\bullet$} & {\color{blue}$\bullet\bullet\bullet$} & $O(d)$ & High \\
Adam & {\color{blue}$\bullet\bullet\bullet$} & {\color{blue}$\bullet\bullet\bullet$} & {\color{orange}$\bullet\bullet$} & $O(d)$ & High \\
Newton & {\color{blue}$\bullet\bullet\bullet$} & {\color{blue}$\bullet\bullet\bullet$} & {\color{orange}$\bullet\bullet$} & $O(d^3)$ & Low (near opt.) \\
FD GD ($\delta$=0.05) & {\color{orange}$\bullet\bullet$} & {\color{orange}$\bullet\bullet$} & {\color{orange}$\bullet\bullet$} & $O(d^2)$ & Medium \\
Nesterov RandSearch & $\bullet$ & $\bullet$ & $\bullet$ & $O(1)$ & High \\
Nelder--Mead & --- & --- & {\color{orange}$\bullet\bullet$} & $O(d)$ & Medium \\
Frank--Wolfe & \multicolumn{3}{c}{(constrained, Q5--6)} & $O(d)$ & High \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Key Conclusions}

\textbf{1. Problem structure determines method suitability.} The Rosenbrock function ($\kappa \approx 2504$) exposes the limitations of first-order methods most starkly: GD requires a tiny step size ($\alpha = 0.0012$) mandated by stability, making progress negligible. Nesterov's accelerated method, by building momentum along the valley floor, achieves a 7$\times$ improvement over GD. Newton's method, armed with curvature information, navigates the ill-conditioned valley efficiently, but requires damping because the Hessian is non-positive-definite away from the optimum.

\textbf{2. Knowledge of $f^{\star}$ is critical for Polyak.} The Polyak step achieves the best convergence when $f^{\star}$ is known (Rosenbrock), but diverges catastrophically under any misspecification. This sensitivity limits Polyak's applicability to problems with known optimal values or valid lower bounds.

\textbf{3. Adaptive methods (Adagrad, RMSprop, Adam) provide robustness.} These methods automatically scale steps by gradient history, making them relatively insensitive to the step-size choice. Adam, combining momentum and per-coordinate scaling, is the most robust and is the de facto standard in deep learning---though Nesterov outperforms it on strongly anisotropic problems like Rosenbrock.

\textbf{4. Newton's method is unmatched in iteration count but costly per iteration.} One Newton step solves the quadratic Benchmark A exactly. For non-quadratic problems, damping is required, but even 20 damped Newton steps dramatically outperform 80 GD steps on Rosenbrock. The $O(d^3)$ per-iteration cost limits use to $d \lesssim 10^4$; for large-scale problems, Quasi-Newton methods (L-BFGS) provide a practical alternative.

\textbf{5. Derivative-free methods trade accuracy for generality.} Finite-difference gradients closely approximate exact gradients when $\delta$ is well-chosen, but require $O(d)$ evaluations per step. Nesterov random search requires only 2 evaluations per step at the cost of high gradient variance. Grid search is the only method guaranteeing global optimality (to grid resolution), but is exponentially intractable beyond $d \approx 4$--$5$.

\textbf{6. Constraint handling requires architectural choices.} Projected gradient descent enforces hard feasibility every iteration at the cost of a projection oracle. Penalty methods are simpler but require careful tuning of $\lambda$ and cannot guarantee strict feasibility. Frank--Wolfe avoids projection entirely by solving a linear subproblem at each step, inheriting the structure of the feasible set.

\textbf{7. Algorithm selection in practice.}

\begin{table}[H]
\centering
\caption{Practical algorithm selection guide based on problem characteristics.}
\label{tab:selection}
\begin{tabular}{lll}
\toprule
\textbf{Problem type} & \textbf{Recommended method} & \textbf{Reason} \\
\midrule
Small $d$, smooth, no constraint & Newton / L-BFGS & Quadratic convergence \\
Large $d$, smooth, ML training & Adam or Nesterov & Scalable, adaptive \\
Gradient unavailable, $d \leq 10$ & Nelder--Mead & Robust, no FEs needed \\
Gradient unavailable, $d \gg 1$ & Nesterov random search & $O(1)$ FE/iter \\
Gradient approx.\ available & FD GD ($\delta \approx 10^{-5}$) & Balance truncation/roundoff \\
Box/polytope constraints & Frank--Wolfe & No projection needed \\
General convex constraints & Projected GD & Hard feasibility guaranteed \\
Soft constraints tolerable & Penalty / ALM & Simple, no oracle needed \\
$f^{\star}$ known exactly & Polyak step & Optimal linear convergence \\
\bottomrule
\end{tabular}
\end{table}

The overarching principle is to exploit all available structure: second-order information (Newton), problem geometry (Frank--Wolfe), or constraint simplicity (projection) can each yield order-of-magnitude improvements over naive gradient descent. The benchmarks in this report illustrate how dramatically algorithm choice affects convergence, particularly under high condition numbers ($\kappa \approx 2504$ for Rosenbrock) where first-order methods with fixed step sizes face fundamental speed limits.

\subsection{Common Pitfalls and Lessons Learned}

The experimental results highlight several pitfalls that practitioners should avoid:

\begin{enumerate}[leftmargin=*, label=\textbf{P\arabic*.}]
\item \textbf{Misspecifying $f^{\star}$ for Polyak step.} Even a small positive error $\Delta f^{\star} > 0$ causes the step size to diverge as $\|\nabla f\| \to 0$. On Benchmark A ($f^{\star} \approx 0.4826$ but specified as 0), Polyak's step grows by $4\times10^3$ over 120 iterations. Always verify that $f^{\star}_{\text{specified}} \leq f^{\star}_{\text{true}}$ (a valid lower bound), not just a guess.

\item \textbf{Using a step size tuned for one benchmark on another.} The GD step $\alpha = 0.08$ safe for Benchmark A becomes completely unstable on Benchmark C, where $L \approx 1680$ requires $\alpha < 1.2 \times 10^{-3}$. The Lipschitz constant $L$ varies enormously across problems and even across the trajectory. Always check $\alpha < 2/L_{\text{local}}$.

\item \textbf{Relying on Newton without damping on non-convex problems.} The full Newton step ($\alpha = 1$) on Rosenbrock from $(-1, 1)$ produces a step towards a completely wrong direction because the Hessian model becomes inaccurate within one Newton step. Regularisation ($\lambda I$) and damping ($\alpha < 1$, or backtracking) are essential outside the convergence basin.

\item \textbf{Choosing $\delta$ without checking the FD error curve.} On Benchmark B, $\delta = 0.8$ incurs $10\times$ higher gradient error than $\delta = 0.05$ due to the sinusoidal non-quadratic term. Always plot the FD error $\|\hat{g}_\delta - g\|$ as a function of $\delta$ to select the operating point, or use the rule of thumb $\delta \approx \sqrt{\varepsilon_{\mathrm{mach}} \|x\|/\|f''(x)\|}$.

\item \textbf{Applying grid search beyond $d \approx 4$.} With $d = 10$ and resolution $\Delta x = 0.1$ in $[0,1]^{10}$, grid search requires $10^{10}$ evaluations (100 seconds at $10^8$ evaluations/second). Nelder--Mead or random search can explore the same domain with $\sim$500 evaluations. Grid search is appropriate only for problems where a global guarantee is needed and $d \leq 3$.

\item \textbf{Setting penalty $\lambda$ too small for feasibility.} With $\lambda = 0.05$ in Q5, the penalty gradient $\lambda \cdot \mathbf{1}_{[x_1 < 0.5]}$ ($= 0.05$) is dominated by $\|\nabla f\| \approx 4.1$ at $x_0$, so the penalty term has negligible influence. Rule of thumb: $\lambda \geq \|\nabla f\|/\Delta c$ where $\Delta c$ is the initial constraint violation. Here $\Delta c = 0.3$, $\|\nabla f\| \approx 4$, so $\lambda \geq 13$ is needed for reliable enforcement---consistent with $\lambda = 4.5$ being marginally sufficient and $\lambda = 12$ being clearly effective in the experiments.

\item \textbf{Using fixed Frank--Wolfe $\beta$ instead of the optimal schedule.} With fixed $\beta = 0.93$ (step $1 - \beta = 0.07$ per iteration), convergence is $O(1/k)$ but with a large constant. The schedule $\gamma_k = 2/(k+2)$ starts with large steps (initial $\gamma_0 = 1$, $\gamma_1 = 0.5$) for rapid initial descent and decreases them as $k$ grows, achieving the same $O(1/k)$ rate with smaller constants and faster early progress. Always prefer the $2/(k+2)$ schedule unless there is a specific reason to use fixed step sizes.
\end{enumerate}

% ============================================================
\newpage
\appendix
\section{Python Code}
\label{app:code}
% ============================================================

The complete implementation is provided in the file \texttt{CS7DS2\_Final\_Code\_25336453.py}. All algorithms were implemented from scratch in NumPy without use of external optimisation libraries, ensuring full transparency and reproducibility. Key algorithm implementations are reproduced below for reference.

\begin{lstlisting}[caption={Gradient Descent}]
def gradient_descent(grad_fn, loss_fn, x0, alpha, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for _ in range(n_iters):
        x = x - alpha * grad_fn(x)
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Polyak Step Size}]
def polyak_step(grad_fn, loss_fn, x0, f_star, eps, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        alpha_k = (loss_fn(x) - f_star) / (np.dot(g, g) + eps)
        alpha_hist.append(alpha_k)
        x = x - alpha_k * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Adagrad}]
def adagrad(grad_fn, loss_fn, x0, alpha0, eps, n_iters):
    x = x0.copy().astype(float)
    G = np.zeros_like(x)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        G += g**2
        eff_alpha = alpha0 / (np.sqrt(G) + eps)
        alpha_hist.append(np.mean(eff_alpha))
        x = x - eff_alpha * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={RMSprop}]
def rmsprop(grad_fn, loss_fn, x0, alpha0, beta, eps, n_iters):
    x = x0.copy().astype(float)
    v = np.zeros_like(x)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        v = beta * v + (1 - beta) * g**2
        eff_alpha = alpha0 / (np.sqrt(v) + eps)
        alpha_hist.append(np.mean(eff_alpha))
        x = x - eff_alpha * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Heavy Ball / Polyak Momentum}]
def heavy_ball(grad_fn, loss_fn, x0, alpha, beta, n_iters):
    x = x0.copy().astype(float)
    z = np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for _ in range(n_iters):
        g = grad_fn(x)
        z = beta * z + alpha * g
        x = x - z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Nesterov Momentum}]
def nesterov_momentum(grad_fn, loss_fn, x0, alpha, beta_max, n_iters):
    x = x0.copy().astype(float)
    z = np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for k in range(1, n_iters + 1):
        beta_k = min((k - 1) / (k + 2), beta_max)
        lookahead = x + beta_k * z
        g = grad_fn(lookahead)
        z = beta_k * z - alpha * g
        x = x + z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Adam Optimiser}]
def adam_optimiser(grad_fn, loss_fn, x0, alpha, beta1, beta2, eps, n_iters):
    x = x0.copy().astype(float)
    m_vec, v_vec = np.zeros_like(x), np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for t in range(1, n_iters + 1):
        g = grad_fn(x)
        m_vec = beta1 * m_vec + (1 - beta1) * g
        v_vec = beta2 * v_vec + (1 - beta2) * g**2
        m_hat = m_vec / (1 - beta1**t)
        v_hat = v_vec / (1 - beta2**t)
        x = x - alpha * m_hat / (np.sqrt(v_hat) + eps)
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Newton's Method with Damping}]
def newtons_method(grad_fn, hess_fn, loss_fn, x0, alpha, n_iters, damping=1e-8):
    x = x0.copy().astype(float)
    x_hist, f_hist, update_norms = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        H = hess_fn(x) if callable(hess_fn) else hess_fn
        H_reg = H + damping * np.eye(len(x))
        try:
            p = np.linalg.solve(H_reg, g)
        except np.linalg.LinAlgError:
            p = g
        update_norms.append(np.linalg.norm(alpha * p))
        x = x - alpha * p
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(update_norms)
\end{lstlisting}

\begin{lstlisting}[caption={Nelder--Mead Simplex}]
def nelder_mead(loss_fn, x0, step, n_iters, alpha_r=1.0, gamma=2.0, rho=0.5, sigma=0.5):
    n = len(x0)
    simplex = np.zeros((n + 1, n))
    simplex[0] = x0.copy()
    for i in range(n):
        simplex[i + 1] = x0.copy()
        simplex[i + 1][i] += step
    f_vals = np.array([loss_fn(v) for v in simplex])
    centroid_hist = [np.mean(simplex, axis=0).copy()]
    f_best_hist = [np.min(f_vals)]
    for _ in range(n_iters):
        order = np.argsort(f_vals)
        simplex = simplex[order]; f_vals = f_vals[order]
        centroid = np.mean(simplex[:-1], axis=0)
        x_r = centroid + alpha_r * (centroid - simplex[-1])
        f_r = loss_fn(x_r)
        if f_vals[0] <= f_r < f_vals[-2]:
            simplex[-1] = x_r; f_vals[-1] = f_r
        elif f_r < f_vals[0]:
            x_e = centroid + gamma * (centroid - simplex[-1])
            f_e = loss_fn(x_e)
            if f_e < f_r: simplex[-1] = x_e; f_vals[-1] = f_e
            else: simplex[-1] = x_r; f_vals[-1] = f_r
        else:
            if f_r < f_vals[-1]:
                x_c = centroid + rho * (x_r - centroid)
                f_c = loss_fn(x_c)
                if f_c <= f_r: simplex[-1] = x_c; f_vals[-1] = f_c
                else:
                    for i in range(1, n+1):
                        simplex[i] = simplex[0]+sigma*(simplex[i]-simplex[0])
                        f_vals[i] = loss_fn(simplex[i])
            else:
                x_c = centroid + rho * (simplex[-1] - centroid)
                f_c = loss_fn(x_c)
                if f_c < f_vals[-1]: simplex[-1] = x_c; f_vals[-1] = f_c
                else:
                    for i in range(1, n+1):
                        simplex[i] = simplex[0]+sigma*(simplex[i]-simplex[0])
                        f_vals[i] = loss_fn(simplex[i])
        centroid_hist.append(np.mean(simplex, axis=0).copy())
        f_best_hist.append(np.min(f_vals))
    return np.array(centroid_hist), np.array(f_best_hist), simplex
\end{lstlisting}

\newpage
\section{References}
\label{sec:references}

\begin{enumerate}
\item Polyak, B.T.\ (1987). \textit{Introduction to Optimization}. Optimization Software Inc., New York. [Polyak step size; Heavy Ball method.]

\item Duchi, J., Hazan, E., \& Singer, Y.\ (2011). Adaptive subgradient methods for online learning and stochastic optimization. \textit{Journal of Machine Learning Research}, 12, 2121--2159. [Adagrad.]

\item Tieleman, T.\ \& Hinton, G.\ (2012). Lecture 6.5---RMSprop. \textit{COURSERA: Neural Networks for Machine Learning}. [RMSprop.]

\item Kingma, D.P.\ \& Ba, J.\ (2015). Adam: A method for stochastic optimization. \textit{ICLR 2015}. [Adam.]

\item Nesterov, Y.\ (1983). A method for solving the convex programming problem with convergence rate $O(1/k^2)$. \textit{Doklady AN SSSR}, 269, 543--547. [Nesterov acceleration; optimal $O(1/k^2)$ rate.]

\item Nocedal, J.\ \& Wright, S.J.\ (2006). \textit{Numerical Optimization}, 2nd ed. Springer. [Newton's method; quasi-Newton methods; line search.]

\item Nelder, J.A.\ \& Mead, R.\ (1965). A simplex method for function minimization. \textit{The Computer Journal}, 7(4), 308--313. [Nelder--Mead algorithm.]

\item Frank, M.\ \& Wolfe, P.\ (1956). An algorithm for quadratic programming. \textit{Naval Research Logistics Quarterly}, 3, 95--110. [Frank--Wolfe conditional gradient.]

\item Jaggi, M.\ (2013). Revisiting Frank-Wolfe: Projection-free sparse convex optimization. \textit{ICML 2013}. [Frank--Wolfe gap; $O(1/k)$ convergence; affine invariance.]

\item Bubeck, S.\ (2015). Convex optimization: Algorithms and complexity. \textit{Foundations and Trends in Machine Learning}, 8(3-4), 231--357. [Convergence rate proofs; lower bounds for first-order methods.]

\item McKinnon, K.I.M.\ (1998). Convergence of the Nelder--Mead simplex method to a non-stationary point. \textit{SIAM Journal on Optimization}, 9(1), 148--158. [Counterexample for Nelder--Mead convergence in $\mathbb{R}^2$; simplex degeneracy.]

\item Lacoste-Julien, S.\ \& Jaggi, M.\ (2015). On the global linear convergence of Frank-Wolfe optimization variants. \textit{Advances in Neural Information Processing Systems (NeurIPS)}, 28. [Away-step Frank--Wolfe; linear convergence for polytope-constrained strongly convex objectives.]

\item Boyd, S.\ \& Vandenberghe, L.\ (2004). \textit{Convex Optimization}. Cambridge University Press. [Augmented Lagrangian methods; KKT conditions; Newton decrement and backtracking line search (Chapter 10).]
\end{enumerate}

\newpage

\begin{lstlisting}[caption={Frank--Wolfe (Conditional Gradient)}]
def frank_wolfe(grad_fn, loss_fn, x0, bounds, beta, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist, z_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        z = np.array([bounds[i][0] if g[i] > 0 else bounds[i][1]
                       for i in range(len(g))])
        z_hist.append(z.copy())
        x = beta * x + (1 - beta) * z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(z_hist)
\end{lstlisting}

\end{document}
