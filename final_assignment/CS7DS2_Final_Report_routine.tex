\documentclass[11pt,a4paper]{article}
\usepackage[margin=2.2cm]{geometry}
\usepackage{graphicx}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{float}
\usepackage{hyperref}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{listings}
\usepackage{fancyhdr}
\usepackage{microtype}
\usepackage{multirow}

\hypersetup{colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue}

% Code listing style
\lstset{
  language=Python,
  basicstyle=\ttfamily\scriptsize,
  keywordstyle=\color{blue},
  commentstyle=\color{gray},
  stringstyle=\color{red},
  numbers=left,
  numberstyle=\tiny\color{gray},
  breaklines=true,
  frame=single,
  captionpos=b
}

% Theorem environments
\theoremstyle{plain}
\newtheorem{theorem}{Theorem}[section]
\newtheorem{proposition}{Proposition}[section]
\theoremstyle{definition}
\newtheorem{definition}{Definition}[section]
\theoremstyle{remark}
\newtheorem{remark}{Remark}[section]

\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small CS7DS2 -- Final Assignment}
\fancyhead[R]{\small Swetha Sekar (25336453)}
\fancyfoot[C]{\thepage}

\title{%
  \includegraphics[width=0.25\textwidth]{tcd_logo.jpg}\\[1em]
  \textbf{CS7DS2 -- Optimisation Algorithms for Data Analysis}\\[0.5em]
  \Large Final Assignment Report
}
\author{Swetha Sekar \\ Student ID: 25336453 \\ School of Computer Science and Statistics \\ Trinity College Dublin}
\date{April 2026}

\begin{document}
\maketitle
\thispagestyle{empty}

\begin{abstract}
This report presents a comprehensive comparative study of optimisation algorithms across six categories applied to three benchmark problems: a convex quadratic linear regression loss (Benchmark~A, condition number $\kappa \approx 6.4$), a non-convex toy neural network loss with sinusoidal perturbation (Benchmark~B, $\kappa \approx 5$--$10$), and the highly ill-conditioned Rosenbrock function (Benchmark~C, $\kappa \approx 2000$). The six categories investigated are: (1)~adaptive step-size methods (Polyak, Adagrad, RMSprop, Heavy Ball), demonstrating that Polyak achieves the best Rosenbrock convergence ($f = 0.0094$) when the optimal value $f^{\star}$ is correctly specified, but diverges catastrophically otherwise; (2)~momentum and stochastic methods (Nesterov, Adam), with Nesterov's $O(1/k^2)$ rate achieving $f = 0.479$ on Rosenbrock --- a $7\times$ improvement over gradient descent's $O(1/k)$ rate; (3)~Newton's method, converging in a single iteration on the quadratic loss by exactly inverting the constant Hessian; (4)~derivative-free methods (finite differences, Nesterov random search, Nelder--Mead, grid search), revealing that finite-difference quality depends critically on the perturbation magnitude $\delta$; (5)~constrained optimisation via projected gradient descent and penalty methods, establishing that for this problem the constraint $x_1 \geq 0.5$ is \emph{inactive} at the unconstrained minimum $x_1^{\star} \approx 0.582$, making all methods converge to the same feasible optimum; and (6)~the Frank--Wolfe algorithm, exploiting the $O(d)$ LP oracle and maintaining feasibility by construction. Across all problems, the strong connection between algorithmic convergence properties and the local curvature (condition number) of the objective is demonstrated.
\end{abstract}

\tableofcontents
\newpage

% ============================================================
\section{Introduction}
\label{sec:intro}
% ============================================================

Optimisation is the mathematical backbone of modern machine learning: training neural networks, fitting regression models, and solving constrained resource allocation problems all reduce to minimising or maximising an objective function. The choice of algorithm significantly impacts convergence speed, computational cost, and whether the method finds a good solution at all.

The fundamental challenge in optimisation is balancing three competing demands: \emph{per-iteration cost} (how much computation is required per step), \emph{convergence rate} (how quickly the error decreases), and \emph{robustness} (whether convergence holds under realistic conditions). Gradient descent, the simplest first-order method, is cheap per iteration but may converge slowly on ill-conditioned problems. Second-order methods (Newton) converge quadratically but require computing and inverting the $d \times d$ Hessian matrix. Adaptive methods (Adagrad, Adam) lie between these extremes, using gradient history to approximate curvature at $O(d)$ cost.

This report systematically evaluates algorithms across the major categories studied in CS7DS2. The three benchmark functions are chosen to span a range of difficulty: Benchmark~A (linear regression, $\kappa \approx 6.4$) is the canonical convex quadratic on which many algorithms achieve their theoretical best rates; Benchmark~B (toy neural network, $\kappa \approx 5$--$10$) adds non-convexity through a sinusoidal term, creating a mildly anisotropic landscape; and Benchmark~C (Rosenbrock, $\kappa \approx 2000$) is a classic challenge --- a non-convex function whose narrow curved valley defeats algorithms that do not exploit curvature information effectively.

All experiments are implemented in Python using NumPy and visualised with Matplotlib. The complete source code is provided in \texttt{CS7DS2\_Final\_Code\_25336453.py}.

Table~\ref{tab:theory_rates} summarises the theoretical convergence rates of the methods studied, providing a reference framework for interpreting the experimental results in subsequent sections.

\begin{table}[H]
\centering
\caption{Theoretical convergence rates and key properties of the studied algorithms.}
\label{tab:theory_rates}
\begin{tabular}{lllcc}
\toprule
\textbf{Algorithm} & \textbf{Rate (convex)} & \textbf{Rate (str. convex)} & \textbf{Hessian?} & \textbf{Grad?} \\
\midrule
Gradient Descent & $O(1/k)$ & $O((1{-}\mu/L)^k)$ & No & Yes \\
Polyak Step & $O(1/k)$ & Optimal & No & Yes \\
Adagrad & $O(\log k/\sqrt{k})$ regret & Per-coord linear & No & Yes \\
RMSprop & Empirical & Empirical & No & Yes \\
Heavy Ball & ---$^\dagger$ & Linear (quadratic $f$) & No & Yes \\
Nesterov & $O(1/k^2)$ & $O((1{-}\sqrt{\mu/L})^k)$ & No & Yes \\
Adam & Empirical & Empirical & No & Yes \\
Newton & Quadratic (local) & Quadratic (local) & Yes & Yes \\
Nesterov Random & $O(d/\sqrt{k})$ & $O(d(1{-}\mu/L)^k)$ & No & No \\
Frank--Wolfe & $O(1/k)$ & $O(1/k)$ & No & Yes \\
\bottomrule
\multicolumn{5}{l}{$^\dagger$Heavy Ball has no general convergence guarantee for non-quadratic functions.}\\
\multicolumn{5}{l}{str.\ convex = strongly convex with parameter $\mu$, Lipschitz gradient constant $L$.}
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/convergence_rates.pdf}
\caption{Illustration of theoretical convergence rate profiles. Newton's quadratic rate ($c^{2^k}$) achieves machine precision in $\sim$50 iterations from moderate initialisation; Nesterov's $O(1/k^2)$ rate substantially outpaces GD's $O(1/k)$ rate; and strongly convex GD achieves linear (geometric) decrease depending on condition number $\kappa = L/\mu$.}
\label{fig:conv_rates}
\end{figure}

% ============================================================
\section{Benchmark Problems and Experimental Setup}
\label{sec:benchmarks}
% ============================================================

All experiments use a fixed random seed (\texttt{np.random.seed(42)}) for reproducibility. The three benchmark functions and a constrained feasible set are defined as follows.

\subsection{Benchmark A: Linear Regression Quadratic Loss}
A synthetic dataset with $m = 1000$ samples and two parameters is generated:
\begin{equation}
\theta^{\star} = \begin{bmatrix} 3 \\ 4 \end{bmatrix}, \quad x^{(i)} \sim \mathcal{N}(0, I_2), \quad y^{(i)} = \theta^{\star T} x^{(i)} + \varepsilon^{(i)}, \quad \varepsilon^{(i)} \sim \mathcal{N}(0, 1).
\end{equation}
The loss function is the mean squared error:
\begin{equation}
J(\theta) = \frac{1}{2m} \sum_{i=1}^{m} \left(\theta^T x^{(i)} - y^{(i)}\right)^2 = \frac{1}{2m}\|X\theta - y\|^2.
\end{equation}
This is a strongly convex quadratic with constant Hessian $H = \frac{1}{m}X^T X \succ 0$ and gradient $\nabla J(\theta) = \frac{1}{m}X^T(X\theta - y)$. The OLS solution found numerically is $\theta^{\star}_{\text{OLS}} \approx (3.028, 4.017)^T$, deviating slightly from $\theta^{\star}$ due to the finite sample and noise. The condition number of $H$ is $\kappa(H) \approx 6.4$, making this the best-conditioned benchmark. The irreducible noise floor is $J^{\star} \approx 0.4826$. Starting point: $\theta_0 = [0, 0]^T$.

\subsection{Benchmark B: Toy Neural Network Loss}
\begin{equation}
\label{eq:bench_b}
f(x_1, x_2) = (x_1 - 1)^2 + 5(x_2 - 2)^2 + \sin(x_1).
\end{equation}
The gradient is $\nabla f = [2(x_1 - 1) + \cos(x_1),\ 10(x_2 - 2)]^T$ and the Hessian is $H(x) = \operatorname{diag}(2 - \sin(x_1),\ 10)$. The sinusoidal term makes this non-convex: $H_{11} = 2 - \sin(x_1) \in [1, 3]$, so $H$ is positive definite but curvature varies. The minimiser in $x_2$ is $x_2^{\star} = 2$, and $x_1^{\star}$ solves the transcendental equation $2(x_1 - 1) + \cos(x_1) = 0$, yielding $x_1^{\star} \approx 0.582$. The minimum value is $f^{\star} \approx 0.7244$. The condition number $\kappa \approx 10/(2 - \sin(0.582)) \approx 5.3$ produces moderate anisotropy. Starting point: $x_0 = [-1, 4]^T$.

\subsection{Benchmark C: Rosenbrock Function}
\begin{equation}
\label{eq:rosenbrock}
f(x_1, x_2) = (1 - x_1)^2 + 100(x_2 - x_1^2)^2.
\end{equation}
The global minimum is at $(1, 1)$ with $f^{\star} = 0$. The Hessian at the optimum is
\begin{equation}
H(1,1) = \begin{bmatrix} 802 & -400 \\ -400 & 200 \end{bmatrix},
\end{equation}
with eigenvalues $\approx 1001$ and $\approx 0.5$, giving $\kappa \approx 2002$ --- extreme ill-conditioning. This creates a narrow curved valley along $x_2 \approx x_1^2$. Methods that do not account for curvature must use tiny step sizes to maintain stability, making progress along the valley extremely slow. Starting point: $x_0 = [-1, 1]^T$.

\subsection{Conditioning and Algorithm Selection}

Table~\ref{tab:bench_summary} summarises the key properties of each benchmark that determine which algorithms are most effective.

\begin{table}[H]
\centering
\caption{Benchmark properties and their implications for algorithm selection.}
\label{tab:bench_summary}
\begin{tabular}{lllll}
\toprule
\textbf{Benchmark} & \textbf{$\kappa$ at $x_0$} & \textbf{Convex?} & \textbf{$f^{\star}$} & \textbf{Difficulty} \\
\midrule
A (Linear Regression) & $\approx 6.4$ (constant) & Strongly & $0.4826$ & Low \\
B (Toy Neural Net) & $\approx 5$--$10$ & Non-convex & $0.7244$ & Moderate \\
C (Rosenbrock) & $\approx 2508$ at $x_0$ & Non-convex & $0$ & Very high \\
\bottomrule
\end{tabular}
\end{table}

The Rosenbrock function is particularly treacherous: not only is the initial condition number $\kappa \approx 2508$, but $\kappa$ \emph{grows dramatically} along gradient descent's trajectory, reaching values exceeding $10^{12}$ in high-anisotropy regions (Figure~\ref{fig:rosenbrock_cond}). This explains why first-order methods require extremely small step sizes and make negligible per-step progress along the low-curvature valley floor.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/rosenbrock_conditioning.pdf}
\caption{Rosenbrock Hessian condition number $\kappa(H)$ (left) and eigenvalues $\lambda_{\min},\lambda_{\max}$ (right) along the GD trajectory ($\alpha = 0.0012$, 80 iterations). The condition number grows from $\kappa_0 \approx 2508$ to over $10^{12}$: while $\lambda_{\max} \approx 800$ remains bounded, $\lambda_{\min}$ shrinks toward zero, reflecting near-zero curvature along the valley floor direction. The required stability step size $\alpha \leq 2/(\lambda_{\max} + \lambda_{\min}) \approx 1/800$ provides essentially no progress in the low-curvature direction. This is the fundamental reason first-order methods fail on Rosenbrock.}
\label{fig:rosenbrock_cond}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/theory_condition_number.pdf}
\caption{Theoretical analysis of condition number as the central determinant of algorithm performance. \textbf{Top-left}: GD convergence rate $((\kappa-1)/(\kappa+1))^k$ for various $\kappa$: at $\kappa = 2000$ (Benchmark~C), the rate $0.9990^k$ barely decreases in 300 iterations; at $\kappa = 6.4$ (Benchmark~A), the rate $0.732^k$ drops to $10^{-6}$ in $\sim$40 iterations. \textbf{Top-right}: iterations required for $\varepsilon = 10^{-6}$ accuracy: GD requires $O(\kappa)$ iterations ($\sim$27,624 at $\kappa=2000$), Nesterov $O(\sqrt{\kappa})$ ($\sim$618 at $\kappa=2000$, a $44.7\times$ speedup), and Newton $O(\log\log 1/\varepsilon)$ essentially independent of $\kappa$. Vertical dashed lines mark Benchmarks~A ($\kappa=6.4$), B ($\kappa\approx7$), and C ($\kappa\approx2000$). \textbf{Bottom-left}: normalised convergence bounds for $\kappa=2000$. \textbf{Bottom-right}: final objective on Rosenbrock across all methods; the two-order-of-magnitude spread from GD to Nelder--Mead illustrates the condition-number-driven hierarchy.}
\label{fig:theory_cond}
\end{figure}

\subsection{Benchmark D: Constrained Feasible Set}
For Questions~5 and 6: $\mathcal{X} = \{x \in \mathbb{R}^2 : 0.5 \leq x_1 \leq 5,\ -5 \leq x_2 \leq 10\}$ --- a box-constrained polytope with 4 vertices.

% ============================================================
\section{Question 1: Adaptive Step-Size Methods \hfill [30 marks]}
\label{sec:q1}
% ============================================================

\subsection{Background and Motivation}

Gradient descent with a fixed step size $\alpha$ converges at rate $O(L\|x_0 - x^{\star}\|^2 / k)$ for $L$-smooth convex functions, where $\alpha \leq 1/L$ is required for stability. For strongly convex functions, the rate is geometric:

\begin{theorem}[Gradient Descent Linear Rate]
\label{thm:gd_linear}
For an $L$-smooth, $\mu$-strongly convex function $f$, gradient descent with $\alpha = 2/(L + \mu)$ satisfies
\begin{equation}
\label{eq:gd_rate}
f(x_k) - f^{\star} \leq \left(\frac{\kappa - 1}{\kappa + 1}\right)^{2k} (f(x_0) - f^{\star}), \quad \kappa = L/\mu.
\end{equation}
The optimal (oracle) rate $\left(\frac{\sqrt{\kappa}-1}{\sqrt{\kappa}+1}\right)^{2k}$ from Nesterov's accelerated method improves the exponent from $O(\kappa)$ to $O(\sqrt{\kappa})$ iterations to $\varepsilon$ accuracy.
\end{theorem}

For $\kappa = 2000$, Eq.~\eqref{eq:gd_rate} gives rate $0.9990^{2k}$: to reach $10^{-6}$ accuracy from $f(x_0) - f^{\star} = 1$, requires $k \approx 27,624$ iterations, versus $k \approx 618$ for Nesterov --- a $44.7\times$ speedup (Figure~\ref{fig:theory_cond}, top-right). However, choosing $\alpha$ requires knowledge of the Lipschitz constant $L$, and a single global step size is suboptimal when coordinates have very different curvatures. Adaptive methods address this by adjusting the effective step size based on observed gradient information.

\subsection{Algorithm Formulations}

Four adaptive methods are compared against a constant step-size gradient descent baseline across all three benchmarks for 120 iterations.

\begin{definition}[Polyak Step Size]
Given a lower bound $f^{\star}$ on the optimal value, the Polyak step size is
\begin{equation}
\label{eq:polyak}
\alpha_k = \frac{f(x_k) - f^{\star}}{\|\nabla f(x_k)\|^2 + \varepsilon},
\end{equation}
where $\varepsilon > 0$ is a small regularisation constant preventing division by zero.
\end{definition}

\begin{theorem}[Polyak Convergence]
\label{thm:polyak}
For an $L$-smooth convex function with $f^{\star}$ known exactly, the Polyak step satisfies
\begin{equation}
\min_{k \leq K} \|\nabla f(x_k)\|^2 \leq \frac{2L(f(x_0) - f^{\star})}{K+1}.
\end{equation}
When $f^{\star}$ is misspecified (e.g., $f^{\star} = 0$ while the true minimum is positive), $\alpha_k \to \infty$ as $\|\nabla f(x_k)\| \to 0$ and $f(x_k) \to f^{\star}_{\text{true}} > 0$, causing divergence.
\end{theorem}

\begin{algorithm}[H]
\caption{Polyak Step Size}
\label{alg:polyak}
\begin{algorithmic}[1]
\Require Initial point $x_0$, optimal value $f^{\star}$, tolerance $\varepsilon$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $\alpha_k \gets (f(x_k) - f^{\star}) / (\|g_k\|^2 + \varepsilon)$
    \State $x_{k+1} \gets x_k - \alpha_k\, g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{algorithm}[H]
\caption{Adagrad}
\label{alg:adagrad}
\begin{algorithmic}[1]
\Require Initial point $x_0$, base learning rate $\alpha_0$, tolerance $\varepsilon$, iterations $N$
\State $G_0 \gets \mathbf{0}$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $G_{k+1} \gets G_k + g_k \odot g_k$ \Comment{Cumulative squared gradient sum}
    \State $x_{k+1} \gets x_k - \dfrac{\alpha_0}{\sqrt{G_{k+1}} + \varepsilon} \odot g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

Adagrad assigns a smaller effective step size to frequently-updated coordinates (large $G_i$) and a larger step to rarely-updated ones, automatically adapting to the local curvature. The effective step for coordinate $i$ is $\alpha_0/\sqrt{\sum_{j=0}^{k} g_{j,i}^2}$, which decays monotonically. This guarantees stability but limits long-term learning --- a limitation that RMSprop addresses.

\begin{algorithm}[H]
\caption{RMSprop}
\label{alg:rmsprop}
\begin{algorithmic}[1]
\Require Initial point $x_0$, learning rate $\alpha_0$, decay $\beta$, tolerance $\varepsilon$, iterations $N$
\State $v_0 \gets \mathbf{0}$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $v_{k+1} \gets \beta\, v_k + (1 - \beta)\, g_k \odot g_k$ \Comment{Exponentially-weighted moving average}
    \State $x_{k+1} \gets x_k - \dfrac{\alpha_0}{\sqrt{v_{k+1}} + \varepsilon} \odot g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

RMSprop replaces Adagrad's cumulative sum with an exponential moving average (decay $\beta = 0.9$), so old squared gradients are ``forgotten'' at rate $(1-\beta)$ per iteration. This allows the effective step size to recover if gradients become small, preventing Adagrad's premature learning rate decay.

\begin{algorithm}[H]
\caption{Heavy Ball (Polyak Momentum)}
\label{alg:heavyball}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, momentum $\beta$, iterations $N$
\State $z_0 \gets \mathbf{0}$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_{k+1} \gets \beta\, z_k + \alpha\, \nabla f(x_k)$
    \State $x_{k+1} \gets x_k - z_{k+1}$
\EndFor
\end{algorithmic}
\end{algorithm}

Heavy Ball maintains a velocity $z_k$ that accumulates gradient directions, allowing the iterate to build momentum in persistent descent directions. For strongly convex quadratics, the optimal parameters $\alpha^{\star} = 4/(\sqrt{L}+\sqrt{\mu})^2$ and $\beta^{\star} = ((\sqrt{L}-\sqrt{\mu})/(\sqrt{L}+\sqrt{\mu}))^2$ achieve the optimal linear rate $O((1-\sqrt{\mu/L})^k)$, matching Nesterov. However, convergence is not guaranteed for general non-convex functions.

\subsection{Hyperparameter Settings}

\begin{table}[H]
\centering
\caption{Q1: Hyperparameter settings for each method and benchmark. All methods run for 120 iterations.}
\label{tab:q1_params}
\begin{tabular}{llccc}
\toprule
\textbf{Method} & \textbf{Parameter} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & $\alpha$ & 0.08 & 0.06 & 0.0012 \\
\midrule
Polyak & $f^{\star}$ & 0 & 0 & 0 \\
 & $\varepsilon$ & $10^{-4}$ & $10^{-4}$ & $10^{-3}$ \\
\midrule
Adagrad & $\alpha_0$ & 1.8 & 1.2 & 0.45 \\
 & $\varepsilon$ & $10^{-5}$ & $10^{-5}$ & $10^{-5}$ \\
\midrule
RMSprop & $\alpha_0$ & 0.22 & 0.14 & 0.0035 \\
 & $\beta$ & 0.9 & 0.9 & 0.9 \\
 & $\varepsilon$ & $10^{-5}$ & $10^{-5}$ & $10^{-5}$ \\
\midrule
Heavy Ball & $\alpha$ & 0.045 & 0.035 & 0.0008 \\
 & $\beta$ & 0.88 & 0.90 & 0.86 \\
\bottomrule
\end{tabular}
\end{table}

For Benchmarks~A and B, $f^{\star} = 0$ is a \emph{misspecification}: the true minimum is $J^{\star} \approx 0.4826$ for A and $f^{\star} \approx 0.7244$ for B. For Benchmark~C, $f^{\star} = 0$ is correct. The GD step size $\alpha = 0.0012$ for Rosenbrock reflects the much larger Lipschitz constant $L \approx 1002$.

\subsection{Convergence Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_A.pdf}
\caption{Q1: Convergence on Benchmark~A (Linear Regression). GD, Adagrad, and Heavy Ball converge to the noise floor $J^{\star} \approx 0.4826$. RMSprop converges to $\approx 0.5027$ due to oscillation near the minimum from adaptive step fluctuations. \textbf{Polyak diverges} ($f \to 2.72$) because $f^{\star} = 0$ misspecifies the true minimum: as $\|\nabla J(\theta_k)\| \to 0$, the step size $\alpha_k = (J(\theta_k) - 0)/\|\nabla J\|^2 \to (0.4826)/0^+ = \infty$, inducing unstable oscillations. This confirms Theorem~\ref{thm:polyak}.}
\label{fig:q1_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_B.pdf}
\caption{Q1: Convergence on Benchmark~B (Toy Neural Network). The Polyak divergence mechanism is identical to Benchmark~A: $f^{\star} = 0$ misspecifies the true $f^{\star} \approx 0.7244$, resulting in $f \to 24.07$. Adagrad and Heavy Ball converge to the true minimum; the moderate anisotropy ($\kappa \approx 5$) does not impede gradient-based methods.}
\label{fig:q1_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_C.pdf}
\caption{Q1: Convergence on Benchmark~C (Rosenbrock). With $f^{\star} = 0$ correct, Polyak achieves the best convergence ($f = 0.0094$), adaptively enlarging steps when far from the minimum and shrinking them as $f(x_k) \to 0$. Heavy Ball reaches $f = 1.40$, demonstrating momentum's benefit in navigating the curved valley. Adagrad's monotonically decaying steps help on the ill-conditioned surface ($f = 2.20$). GD ($f = 3.51$) and RMSprop ($f = 3.12$) show the least improvement, as fixed or poorly-tuned rates are inadequate for $\kappa \approx 2000$.}
\label{fig:q1_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q1: Final objective values after 120 iterations. Bold indicates best per benchmark.}
\label{tab:q1_final}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Bench.\ A ($J^{\star}=0.4826$)} & \textbf{Bench.\ B ($f^{\star}\approx 0.7244$)} & \textbf{Bench.\ C ($f^{\star}=0$)} \\
\midrule
GD (baseline) & 0.4826 & 0.7244 & 3.5142 \\
Polyak & 2.7189 (diverges) & 24.0659 (diverges) & \textbf{0.0094} \\
Adagrad & \textbf{0.4826} & \textbf{0.7244} & 2.2034 \\
RMSprop & 0.5027 & 0.7271 & 3.1204 \\
Heavy Ball & 0.4826 & 0.7244 & 1.4003 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Contour Plots with Optimisation Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_contour_A.pdf}
\caption{Q1: GD trajectory in parameter space for Benchmark~A. The contours form an elliptical bowl (the quadratic loss). GD descends directly from $(0,0)$ towards the OLS minimum $(\hat\theta_1, \hat\theta_2) \approx (3.03, 4.02)$, with the path reflecting the elliptical geometry of $H = X^TX/m$.}
\label{fig:q1_contour_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q1_contour_B.pdf}
\caption{Q1: Trajectories on Benchmark~B. The elliptical contours reflect the $5{:}1$ curvature ratio between $x_2$ and $x_1$ directions. GD and Adagrad follow direct paths; Heavy Ball overshoots slightly due to momentum. The Polyak trajectory diverges widely due to $f^{\star}$ misspecification. Note: the ``optimum'' marker in the figure is plotted at $(0.61, 2.0)$ by the generating code; the true minimum is at $x_1^{\star} \approx 0.582$.}
\label{fig:q1_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q1_contour_C.pdf}
\caption{Q1: Trajectories on Benchmark~C (Rosenbrock). The banana-shaped valley $x_2 \approx x_1^2$ is visible. All methods first descend rapidly into the valley, then progress slowly along it. Polyak (with correct $f^{\star} = 0$) makes the most progress along the valley floor. Conservative GD ($\alpha = 0.0012$) barely advances in 120 iterations, as the stability requirement forces tiny steps.}
\label{fig:q1_contour_C}
\end{figure}

\subsection{Adaptive Step-Size Evolution}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_A.pdf}
\caption{Q1: Step-size evolution on Benchmark~A. The Polyak step grows unboundedly near the noise floor ($f(x_k) \to 0.4826$, $\|\nabla J\| \to 0$), confirming the divergence mechanism. Adagrad decays monotonically as $G_k = \sum_{j=0}^k g_j^2$ grows. RMSprop stabilises due to exponential forgetting ($\beta = 0.9$), maintaining a finite steady-state step size proportional to $\alpha_0 / \sqrt{v_\infty}$.}
\label{fig:q1_stepsize_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_B.pdf}
\caption{Q1: Step-size evolution on Benchmark~B. Polyak diverges as on Benchmark~A. Adagrad rapidly decays after the large initial gradients near $x_0 = (-1, 4)$. RMSprop's step size reflects recent gradient magnitudes, adapting smoothly to the changing landscape.}
\label{fig:q1_stepsize_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_C.pdf}
\caption{Q1: Step-size evolution on Benchmark~C. With $f^{\star} = 0$ correct, the Polyak step decays smoothly as $f(x_k) \to 0$ --- the numerator vanishes while the denominator $\|\nabla f\|^2$ remains bounded, giving a well-behaved decreasing sequence. This well-behaved step-size decay is precisely why Polyak achieves the best Rosenbrock convergence among all Q1 methods.}
\label{fig:q1_stepsize_C}
\end{figure}

\subsection{Comparative Analysis}

\begin{table}[H]
\centering
\caption{Q1: Method comparison across key properties.}
\label{tab:q1_compare}
\begin{tabular}{lllll}
\toprule
\textbf{Method} & \textbf{Convergence} & \textbf{Step adaptivity} & \textbf{Key requirement} & \textbf{Stability} \\
\midrule
GD & Linear ($\kappa$-dep.) & None & $L$ known & High \\
Polyak & Near-optimal & Global (from $f^{\star}$) & Exact $f^{\star}$ & Fragile \\
Adagrad & $O(\log k / \sqrt{k})$ & Per-coord (cumulative) & $\alpha_0$ & High \\
RMSprop & Empirical & Per-coord (EMA) & $\alpha_0, \beta$ & Moderate \\
Heavy Ball & Linear (quadratic) & None & $\alpha, \beta$ tuning & Moderate \\
\bottomrule
\end{tabular}
\end{table}

The condition number $\kappa$ of the problem is the primary determinant of method performance. On Benchmark~A ($\kappa \approx 6.4$), most methods converge rapidly since even a suboptimal step size reaches the minimum quickly --- GD's linear rate $(1 - 1/6.4)^k = 0.844^k$ halves the error every $\sim 4$ iterations. On Benchmark~C ($\kappa \approx 2000$), GD's rate $(1 - 1/2000)^k = 0.9995^k$ requires $\sim 1386$ iterations just to halve the error, and the stability requirement forces $\alpha \leq 2/(L+\mu) \approx 2/802 \approx 0.002$, leaving essentially no room to exploit the low-curvature valley direction. Per-coordinate adaptation (Adagrad, RMSprop) and momentum (Heavy Ball) provide meaningful improvement, but as Figure~\ref{fig:rosenbrock_cond} shows, the condition number worsens along the trajectory, compounding the difficulty.

\begin{remark}[Why Adagrad Helps on Rosenbrock]
On Rosenbrock, the two gradient coordinates have very different magnitudes: the $x_1$-gradient is $\sim 400\times$ larger than the $x_2$-gradient along the valley. Adagrad's per-coordinate accumulation $G_{k,i} = \sum_{j=0}^k g_{j,i}^2$ automatically assigns a smaller effective rate to $x_1$ (large gradient, large $G_{k,1}$) and a larger rate to $x_2$ (small gradient, small $G_{k,2}$), partially compensating for the curvature imbalance. This is the same mechanism that makes Adagrad effective for sparse gradients in NLP.
\end{remark}

% ============================================================
\section{Question 2: Momentum and Stochastic Methods \hfill [30 marks]}
\label{sec:q2}
% ============================================================

\subsection{Background}

Nesterov's accelerated gradient method achieves the optimal $O(1/k^2)$ convergence rate for smooth convex minimisation, provably faster than the $O(1/k)$ rate of gradient descent. Adam combines momentum with per-coordinate adaptive scaling, combining the advantages of both Nesterov-style acceleration and RMSprop.

\subsection{Algorithm Formulations}

\begin{algorithm}[H]
\caption{Nesterov Momentum / Acceleration}
\label{alg:nesterov}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, max momentum $\beta_{\max}$, iterations $N$
\State $z_0 \gets \mathbf{0}$
\For{$k = 1, 2, \ldots, N$}
    \State $\beta_k \gets \min\!\left(\frac{k-1}{k+2},\ \beta_{\max}\right)$ \Comment{Adaptive momentum schedule}
    \State $\tilde{x}_k \gets x_k + \beta_k\, z_k$ \Comment{Lookahead point}
    \State $z_{k+1} \gets \beta_k\, z_k - \alpha\, \nabla f(\tilde{x}_k)$
    \State $x_{k+1} \gets x_k + z_{k+1}$
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{theorem}[Nesterov Acceleration]
\label{thm:nesterov}
For an $L$-smooth convex function $f$ with $f^{\star} = \min f$, the Nesterov method with step $\alpha = 1/L$ and $\beta_k = (k-1)/(k+2)$ satisfies
\begin{equation}
f(x_k) - f^{\star} \leq \frac{2L\|x_0 - x^{\star}\|^2}{(k+1)^2} = O(1/k^2),
\end{equation}
which is optimal (no first-order method can achieve a better worst-case rate for this problem class).
\end{theorem}

The crucial difference from Heavy Ball is that Nesterov evaluates the gradient at the \emph{lookahead point} $\tilde{x}_k = x_k + \beta_k z_k$ rather than the current iterate $x_k$. This look-ahead allows the method to ``correct'' its trajectory before overshooting, providing the $O(1/k^2)$ guarantee absent from Heavy Ball.

\begin{algorithm}[H]
\caption{Adam Optimiser}
\label{alg:adam}
\begin{algorithmic}[1]
\Require Initial point $x_0$, learning rate $\alpha$, moments $\beta_1, \beta_2$, tolerance $\varepsilon$, iterations $N$
\State $m_0 \gets \mathbf{0},\ v_0 \gets \mathbf{0}$
\For{$t = 1, 2, \ldots, N$}
    \State $g_t \gets \nabla f(x_t)$
    \State $m_t \gets \beta_1\, m_{t-1} + (1 - \beta_1)\, g_t$ \Comment{First moment (EMA of gradients)}
    \State $v_t \gets \beta_2\, v_{t-1} + (1 - \beta_2)\, g_t \odot g_t$ \Comment{Second moment (EMA of sq.\ grads)}
    \State $\hat{m}_t \gets m_t / (1 - \beta_1^t)$ \Comment{Bias correction (critical for early iters)}
    \State $\hat{v}_t \gets v_t / (1 - \beta_2^t)$
    \State $x_{t+1} \gets x_t - \alpha\, \hat{m}_t / (\sqrt{\hat{v}_t} + \varepsilon)$
\EndFor
\end{algorithmic}
\end{algorithm}

Adam uses $\beta_1 = 0.8$--$0.82$ (momentum) and $\beta_2 = 0.999$ (second-moment decay). The bias corrections in steps 6--7 are critical in early iterations: without correction, $m_1 = (1-\beta_1)g_1$ is biased towards zero, leading to artificially small first steps. The effective per-coordinate step size is $\alpha_i^{\text{eff}}(t) = \alpha / \sqrt{\hat{v}_{t,i} + \varepsilon^2} \cdot \sqrt{(1-\beta_2^t)/(1-\beta_1^t)^2}$, which adapts per-coordinate like RMSprop while also incorporating momentum. Figure~\ref{fig:adaptive_lr} analyses how Adagrad, RMSprop, and Adam adapt their effective step sizes over time on Benchmark~B.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/adaptive_lr_analysis.pdf}
\caption{Effective per-coordinate step sizes for Adagrad, RMSprop, and Adam on Benchmark~B over 120 iterations. Top row: effective step sizes for coordinates $x_1$ (top-left) and $x_2$ (top-right). Adagrad decays monotonically for both. RMSprop stabilises at steady-state values reflecting recent gradient magnitudes. Adam adapts more aggressively early via bias correction. Bottom-left: ratio $\alpha_{x_2}^{\text{eff}} / \alpha_{x_1}^{\text{eff}}$ converges toward $\approx 5$ (the true curvature ratio), demonstrating automatic curvature compensation. Bottom-right: corresponding convergence, showing all three methods reach $f \approx f^{\star} = 0.7244$.}
\label{fig:adaptive_lr}
\end{figure}

\subsection{Hyperparameter Settings}

\begin{table}[H]
\centering
\caption{Q2: Hyperparameter settings. Nesterov and Adam run for 150 iterations.}
\label{tab:q2_params}
\begin{tabular}{llccc}
\toprule
\textbf{Method} & \textbf{Parameter} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & $\alpha$ & 0.08 & 0.06 & 0.0012 \\
\midrule
Nesterov & $\alpha$ & 0.06 & 0.035 & 0.0007 \\
 & $\beta_{\max}$ & 0.90 & 0.92 & 0.90 \\
\midrule
Adam & $\alpha$ & 0.12 & 0.08 & 0.006 \\
 & $\beta_1$ & 0.82 & 0.80 & 0.80 \\
 & $\beta_2$ & 0.999 & 0.999 & 0.999 \\
 & $\varepsilon$ & $10^{-8}$ & $10^{-8}$ & $10^{-8}$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Convergence Comparison}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_A.pdf}
\caption{Q2: Convergence on Benchmark~A. All three methods converge to $J^{\star} \approx 0.4826$. The well-conditioned quadratic surface ($\kappa \approx 6.4$) means even GD converges quickly, leaving little room for Nesterov or Adam to demonstrate superior rates.}
\label{fig:q2_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_B.pdf}
\caption{Q2: Convergence on Benchmark~B. All three methods converge to $f^{\star} \approx 0.7244$. Adam's per-coordinate scaling efficiently handles the $5{:}1$ curvature anisotropy, while Nesterov's lookahead step avoids the overshoot common on anisotropic surfaces.}
\label{fig:q2_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_C.pdf}
\caption{Q2: Convergence on Benchmark~C. Nesterov achieves $f = 0.479$ in 150 iterations --- a $7\times$ improvement over GD ($f = 3.39$) and $3\times$ improvement over Adam ($f = 1.58$). The $O(1/k^2)$ rate allows Nesterov to build progressively more momentum along the flat valley direction, efficiently traversing the Rosenbrock channel. Adam's per-coordinate scaling helps but is limited by the nonlinear valley curvature.}
\label{fig:q2_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q2: Final objective values after 150 iterations. Nesterov dominates on Rosenbrock.}
\label{tab:q2_final}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & 0.4826 & 0.7244 & 3.3880 \\
Nesterov & \textbf{0.4826} & \textbf{0.7244} & \textbf{0.4788} \\
Adam & \textbf{0.4826} & \textbf{0.7244} & 1.5785 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Nesterov Momentum Schedule}

Figure~\ref{fig:nest_schedule} illustrates the momentum schedule $\beta_k = \min((k-1)/(k+2), \beta_{\max})$ and compares the theoretical rate of Nesterov versus GD for the Rosenbrock condition number $\kappa = 2000$.

For $\kappa = 2000$, the quantitative rate comparison is striking. GD's linear rate $(1 - 1/\kappa)^k = 0.9995^k$ satisfies:
\begin{equation}
(0.9995)^{150} = e^{150 \ln(0.9995)} \approx e^{-0.075} \approx 0.928,
\end{equation}
meaning 150 GD iterations reduce the error by only $7.2\%$ --- a tiny improvement. By contrast, Nesterov's $O(1/k^2)$ bound gives:
\begin{equation}
f(x_k) - f^{\star} \leq \frac{2L\|x_0 - x^{\star}\|^2}{(k+1)^2}.
\end{equation}
With $\|x_0 - x^{\star}\|^2 = \|(-1,1)-(1,1)\|^2 = 4$ and $L \approx 1002$ (Rosenbrock Lipschitz constant at $x_0$), after $k = 150$ iterations: $\frac{2 \times 1002 \times 4}{151^2} \approx 0.177$. This upper bound predicts final error $\approx 0.177$, consistent with the observed $f(x_{150}) - f^{\star} = 0.479$. The $\sim$7$\times$ improvement of Nesterov over GD on Rosenbrock is thus a direct consequence of the quadratic ($1/k^2$) versus logarithmic ($O(1-1/\kappa)^k$) rate difference at high condition numbers.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/nesterov_schedule.pdf}
\caption{Left: Nesterov momentum schedule $\beta_k$ for different caps $\beta_{\max}$. The schedule increases from 0 (at $k=1$) toward $\beta_{\max}$, building momentum gradually to avoid early oscillation. Right: theoretical convergence rate comparison for $\kappa = 2000$. GD's linear rate $(1 - 1/\kappa)^k$ makes negligible progress (after 100 iterations, the error barely halves), while Nesterov's $O(1/k^2)$ rate reduces the error by $\sim 10^4\times$ in 100 iterations. This quantitative gap explains why Nesterov's $f = 0.479$ far outperforms GD's $f = 3.39$ on Rosenbrock.}
\label{fig:nest_schedule}
\end{figure}

\subsection{Contour Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q2_contour_B.pdf}
\caption{Q2: Trajectories on Benchmark~B. Nesterov's trajectory shows the characteristic smooth arc of accelerated methods: the lookahead prevents zig-zagging across the narrow $x_2$ direction. Adam takes more direct coordinate-wise steps due to independent scaling, converging efficiently despite the anisotropy.}
\label{fig:q2_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q2_contour_C.pdf}
\caption{Q2: Trajectories on Benchmark~C. All methods enter the Rosenbrock valley quickly but differ dramatically in their along-valley progress. Nesterov builds momentum along the valley floor direction; GD ($\alpha = 0.0012$) barely advances in 150 iterations. The $O(1/k^2)$ rate advantage quantified in Figure~\ref{fig:nest_schedule} (right) manifests as $7\times$ better final objective.}
\label{fig:q2_contour_C}
\end{figure}

\subsection{Mini-Batch Stochastic Gradient Descent}

Mini-batch SGD on Benchmark~A uses $\alpha = 0.06$, batch sizes $b \in \{5, 40\}$, $E = 50$ epochs, with data shuffled each epoch.

\begin{algorithm}[H]
\caption{Mini-Batch SGD}
\label{alg:sgd}
\begin{algorithmic}[1]
\Require Data $(X, y)$, initial $\theta_0$, learning rate $\alpha$, batch size $b$, epochs $E$
\For{$e = 1, 2, \ldots, E$}
    \State Randomly permute data indices $\{1, \ldots, m\}$
    \For{each mini-batch $\mathcal{B}$ of size $b$}
        \State $g_\mathcal{B} \gets \frac{1}{b} X_{\mathcal{B}}^T(X_{\mathcal{B}}\theta - y_{\mathcal{B}})$
        \State $\theta \gets \theta - \alpha\, g_\mathcal{B}$
    \EndFor
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{remark}[Bias--Variance Tradeoff in SGD]
Each mini-batch gradient $g_\mathcal{B}$ is an unbiased estimator of the full gradient $\nabla J(\theta)$, with variance $\operatorname{Var}(g_\mathcal{B}) = \operatorname{Var}(g_i)/b$ where $g_i$ is a single sample gradient. Smaller batches provide noisier but cheaper gradient estimates: $b=5$ executes $m/b = 200$ updates per epoch, while $b=40$ executes only 25. The benefit of smaller batches (more updates per epoch) is offset by higher gradient variance, which creates persistent oscillation near the minimum.
\end{remark}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_sgd_batch.pdf}
\caption{Q2: Mini-batch SGD with different batch sizes (50 epochs, $\alpha = 0.06$). Batch $b=5$ descends faster per epoch due to $200/25 = 8\times$ more updates, but oscillates more near the minimum. Batch $b=40$ descends more smoothly, reflecting lower gradient variance. Both eventually reach the same noise floor $J^{\star} \approx 0.4826$.}
\label{fig:q2_sgd_batch}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/sgd_iter_analysis.pdf}
\caption{SGD iteration analysis on Benchmark~A ($m=1000$ samples). \textbf{Top row}: per-epoch training loss for $b=5$ (left) and $b=40$ (right). Both converge to the same noise floor $J^{\star} \approx 0.4826$. \textbf{Bottom-left}: cumulative gradient evaluations vs.\ loss, showing $b=5$ performs $8\times$ more total updates (10\,000 vs.\ 1\,250 per 50 epochs), explaining its faster per-epoch descent. \textbf{Bottom-right}: per-update (per gradient evaluation) loss decay. When measured per gradient evaluation rather than per epoch, $b=40$ is more sample-efficient early on, while $b=5$ exhibits higher variance but reaches the noise floor faster. This illustrates the fundamental SGD tradeoff: smaller batches maximise updates per epoch but sacrifice per-update quality; the optimal batch size depends on whether throughput (updates/epoch) or sample efficiency (loss per gradient call) is prioritised.}
\label{fig:sgd_iter}
\end{figure}

\subsection{SGD with Increased Noise}

The output noise is scaled by factor 6 ($\sigma = 6$ vs $\sigma = 1$). For the linear regression model $y = X\theta + \varepsilon$ with $\varepsilon \sim \mathcal{N}(0, \sigma^2 I)$, the Bayes-optimal loss is
\begin{equation}
J^{\star}(\sigma) = \frac{\sigma^2}{2} \cdot \frac{m - d}{m} \approx \frac{\sigma^2}{2} \quad \text{for } m \gg d,
\end{equation}
so scaling $\sigma$ by 6 raises the irreducible floor by $6^2 = 36$: $J^{\star}(\sigma{=}6) \approx 36 \times 0.4826 \approx 17.4$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q2_sgd_noisy.pdf}
\caption{Q2: Effect of noise scaling on SGD convergence. Left: original noise ($\sigma = 1$). Right: high noise ($\sigma = 6$). The irreducible loss floor scales as $J^{\star} \propto \sigma^2$: $J^{\star}_{(\sigma=6)} \approx 36 \times J^{\star}_{(\sigma=1)} \approx 17.4$ (observed $\approx 17.4$, confirming the theory). Under high noise, both batch sizes converge to the same elevated floor, and the relative advantage of smaller batches diminishes because the observation noise (fixed, $\sigma^2/m$ per sample) dominates the mini-batch variance ($\sigma^2/b$). This illustrates that SGD with \emph{fixed} $\alpha$ cannot converge below the irreducible noise floor; a decaying schedule $\alpha_k \to 0$ (satisfying $\sum \alpha_k = \infty$, $\sum \alpha_k^2 < \infty$) is required to converge to the true minimum, at the cost of slower later-stage convergence.}
\label{fig:q2_sgd_noisy}
\end{figure}

% ============================================================
\section{Question 3: Newton's Method and Local Approximation \hfill [30 marks]}
\label{sec:q3}
% ============================================================

\subsection{Local Approximations of $g(x) = x^4$}

Newton's method is motivated by minimising the local second-order Taylor approximation of $f$ at each iterate.

\textbf{First-order approximation} around $x_0 = 0.25$:
\begin{equation}
g(x) \approx g(x_0) + g'(x_0)(x - x_0) = 0.0039 + 0.0625(x - 0.25).
\end{equation}

\textbf{Second-order approximation:}
\begin{equation}
g(x) \approx g(x_0) + g'(x_0)(x - x_0) + \tfrac{1}{2}g''(x_0)(x - x_0)^2 = 0.0039 + 0.0625(x - 0.25) + 0.375(x - 0.25)^2.
\end{equation}

The Newton step minimises the second-order approximation, yielding the update
\begin{equation}
\label{eq:newton_update_1d}
x_{k+1} = x_k - \frac{g'(x_k)}{g''(x_k)}.
\end{equation}
For $g(x) = x^4$: $g'(x) = 4x^3$ and $g''(x) = 12x^2$, so the Newton step is $x_{k+1} = x_k - (4x_k^3)/(12x_k^2) = x_k - x_k/3 = (2/3)x_k$, contracting linearly towards zero from any $x_0 \neq 0$. (Quadratic convergence requires the function to be locally quadratic, which $x^4$ is not at $x=0$ since $g''(0)=0$.)

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_approximation.pdf}
\caption{Q3: Local approximations of $g(x) = x^4$ at $x_0 = 0.25$. The first-order approximation (tangent line) captures the local slope but deviates rapidly. The second-order approximation (parabola) captures curvature and provides a much better fit near $x_0$. The minimum of the parabola is a better candidate for the next iterate than taking a step in the negative gradient direction --- this geometric insight drives Newton's method.}
\label{fig:q3_approx}
\end{figure}

\subsection{Newton's Method}

\begin{algorithm}[H]
\caption{Newton's Method with Hessian Damping}
\label{alg:newton}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, damping $\lambda$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $H_k \gets \nabla^2 f(x_k) + \lambda I$ \Comment{Regularised Hessian ensures positive definiteness}
    \State Solve $H_k p_k = g_k$ \Comment{Newton direction}
    \State $x_{k+1} \gets x_k - \alpha\, p_k$
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{theorem}[Quadratic Convergence of Newton's Method]
\label{thm:newton_quad}
Suppose $f$ is twice continuously differentiable, $\nabla^2 f$ is Lipschitz continuous with constant $M$, and $\nabla^2 f(x^{\star}) \succ 0$ at the minimiser $x^{\star}$. Then for $x_0$ sufficiently close to $x^{\star}$, Newton's method with $\alpha = 1$ satisfies
\begin{equation}
\|x_{k+1} - x^{\star}\| \leq \frac{M}{2\mu} \|x_k - x^{\star}\|^2,
\end{equation}
where $\mu = \lambda_{\min}(\nabla^2 f(x^{\star}))$. This quadratic rate means the number of correct digits doubles at each iteration.
\end{theorem}

\begin{remark}
Damping ($\alpha < 1$ or $\lambda > 0$) is necessary when $x_k$ is far from $x^{\star}$: the quadratic approximation may be inaccurate, and the full Newton step can overshoot. Here $\lambda = 10^{-8}$ ensures the regularised Hessian $H_k + \lambda I$ is positive definite (avoiding negative-curvature directions), and $\alpha < 1$ on non-quadratic functions prevents overshooting.
\end{remark}

Settings: GD uses 80 iterations; Newton uses 20 iterations with Hessian damping $\lambda = 10^{-8}$.

\begin{table}[H]
\centering
\caption{Q3: Hyperparameters for GD and Newton.}
\label{tab:q3_params}
\begin{tabular}{lcccccc}
\toprule
 & \multicolumn{2}{c}{\textbf{Bench.\ A}} & \multicolumn{2}{c}{\textbf{Bench.\ B}} & \multicolumn{2}{c}{\textbf{Bench.\ C}} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5} \cmidrule(lr){6-7}
 & GD & Newton & GD & Newton & GD & Newton \\
\midrule
$\alpha$ & 0.08 & 1.0 & 0.06 & 0.85 & 0.001 & 0.22 \\
Iterations & 80 & 20 & 80 & 20 & 80 & 20 \\
\bottomrule
\end{tabular}
\end{table}

The comparison uses different iteration counts to highlight Newton's efficiency: 20 Newton steps vs.\ 80 GD steps. Newton with $\alpha = 1$ on the quadratic loss (Bench.\ A) minimises in a single step, as the constant Hessian $H = X^TX/m$ provides exact curvature information globally.

\begin{proposition}[Newton's Method on Quadratic Functions]
\label{prop:newton_quadratic}
For the quadratic objective $J(\theta) = \frac{1}{2}\theta^T H \theta - b^T \theta + c$ with $H \succ 0$, Newton's method with $\alpha = 1$ converges in \textbf{exactly one iteration} from any starting point $\theta_0$:
\begin{equation}
\theta_1 = \theta_0 - H^{-1}(H\theta_0 - b) = H^{-1}b = \theta^{\star}.
\end{equation}
This follows because the Newton direction $p_0 = H^{-1}\nabla J(\theta_0) = H^{-1}(H\theta_0 - b) = \theta_0 - \theta^{\star}$ points exactly from $\theta_0$ to the minimiser, and a full step $\alpha = 1$ reaches $\theta^{\star}$ exactly.
\end{proposition}

\subsection{Convergence Comparison}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_A.pdf}
\caption{Q3: Newton vs GD on Benchmark~A. Newton converges in \textbf{one iteration} to $J = 0.4826$ with $\alpha = 1.0$. For quadratic $J(\theta) = \frac{1}{2}\theta^T H \theta - b^T\theta + c$, the Newton step with $\alpha=1$ gives $\theta_{k+1} = \theta_k - H^{-1}\nabla J = \theta_k - H^{-1}(H\theta_k - b) = H^{-1}b = \theta^{\star}$, the exact minimiser regardless of starting point. GD requires 80 iterations for comparable accuracy.}
\label{fig:q3_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_B.pdf}
\caption{Q3: Newton vs GD on Benchmark~B. Newton converges rapidly ($\approx 5$ iterations) owing to the near-quadratic structure near $x^{\star}$. The Hessian $H = \operatorname{diag}(2 - \sin(x_1),\, 10)$ varies slowly along the trajectory, so the quadratic approximation remains accurate across several steps. Final value: Newton $f = 0.7244$ vs GD $f = 0.7244$, but Newton requires far fewer iterations.}
\label{fig:q3_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_C.pdf}
\caption{Q3: Newton vs GD on Benchmark~C. Newton with $\alpha = 0.22$ reaches $f = 0.686$ in 20 iterations, versus GD's $f = 3.73$ in 80 iterations. The damped step $\alpha = 0.22 \ll 1$ is essential: near the starting point $(-1, 1)$, the Rosenbrock Hessian has condition number $\kappa \approx 500$, and the full Newton step overshoots. The damped Newton still dramatically outperforms GD because the Hessian information permits much larger effective steps towards the valley.}
\label{fig:q3_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q3: Final objective values and converged positions (GD: 80 iters; Newton: 20 iters).}
\label{tab:q3_final}
\begin{tabular}{lccc}
\toprule
\textbf{Benchmark} & \textbf{GD $f$ (80 iters)} & \textbf{Newton $f$ (20 iters)} & \textbf{Newton final $x$} \\
\midrule
A & 0.4827 & 0.4826 & $(3.028, 4.017)$ \\
B & 0.7244 & 0.7244 & $(0.582, 2.000)$ \\
C & 3.7319 & 0.6864 & $(0.183, 0.020)$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{remark}[Global vs Local Convergence of Newton's Method]
On Rosenbrock (Bench.\ C), Newton converges to $(0.183, 0.020)$ in 20 iterations, far from the global minimum $(1,1)$. This reflects Newton's fundamental limitation: Theorem~\ref{thm:newton_quad} guarantees quadratic convergence only locally (when $\|x_0 - x^{\star}\|$ is sufficiently small). Far from the optimum, the quadratic approximation is inaccurate and the damped Newton step $\alpha = 0.22$ compensates, but global convergence requires more iterations. With 50 iterations, Newton reaches $f = 0.0005$ (Figure~\ref{fig:rosenbrock_full}), demonstrating that once within the local quadratic convergence basin, progress is extremely rapid.

For high-dimensional problems where computing $H^{-1}$ is prohibitive ($O(d^3)$), quasi-Newton methods such as L-BFGS approximate the inverse Hessian using a rank-$m$ update from the $m$ most recent gradient differences, achieving superlinear convergence at $O(md)$ cost per iteration. This makes them the standard choice in large-scale machine learning.
\end{remark}

\subsection{Contour Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q3_contour_B.pdf}
\caption{Q3: Newton vs GD trajectories on Benchmark~B. Newton reaches the minimum in $\sim$5 large, well-directed steps that account for the $10{:}(2 - \sin x_1)$ curvature ratio. GD takes many small steps, exhibiting the characteristic zig-zag pattern caused by the anisotropic Hessian: it oscillates between the $x_2$ and $x_1$ directions as each update overshoots the narrow curvature.}
\label{fig:q3_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q3_contour_C.pdf}
\caption{Q3: Newton vs GD trajectories on Rosenbrock. Newton descends steeply into the valley and follows the curved floor using Hessian information. GD ($\alpha = 0.001$) barely advances in 80 iterations due to the extreme conditioning --- the stability-required step size is $\alpha \leq 2/(L+\mu) \approx 1/1001$, providing negligible per-step progress.}
\label{fig:q3_contour_C}
\end{figure}

\subsection{Update Magnitude}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q3_update_magnitude.pdf}
\caption{Q3: Update magnitude $\|x_{k+1} - x_k\|$ vs iteration. Newton's magnitude decreases rapidly (characteristic of quadratic convergence --- the steps halve in length at each iteration near the minimum). For Benchmark~A, Newton's update drops to machine precision after a single step. GD's magnitude decreases linearly, reflecting its linear convergence rate $O((1 - \mu/L)^k)$. On Rosenbrock, Newton maintains larger steps than GD, indicating substantially more effective progress along the valley.}
\label{fig:q3_update}
\end{figure}

\subsection{Quadratic Convergence Verification}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/newton_quadratic_convergence.pdf}
\caption{Verification of Newton quadratic convergence via the error sequence $\|x_k - x^{\star}\|$. \textbf{Left (Benchmark~A):} Newton's error drops from $\|x_0 - x^{\star}\| \approx 5.03$ to machine precision ($\approx 10^{-9}$) in a single iteration, confirming Proposition~\ref{prop:newton_quadratic}: for a quadratic loss with constant Hessian, the Newton step with $\alpha = 1$ is exact. GD requires $\sim$15 iterations to reach comparable accuracy with $\alpha = 0.08$. \textbf{Right (Benchmark~B):} Newton requires $\sim$5--6 iterations to enter the local convergence basin, after which the super-linear rate dominates. The log-error ratios $\log\|e_{k+1}\|/\log\|e_k\|$ approach 2 near the minimum, consistent with quadratic convergence (Theorem~\ref{thm:newton_quad}). GD exhibits a constant linear rate throughout.}
\label{fig:q3_newton_quad}
\end{figure}

% ============================================================
\section{Question 4: Derivative Approximation and Derivative-Free Optimisation \hfill [30 marks]}
\label{sec:q4}
% ============================================================

\subsection{Part A: Finite Difference and Nesterov Random Search (Benchmark B)}

\subsubsection{Finite Difference Gradient Approximation}

When the gradient is unavailable, it can be approximated using function values alone. The forward finite-difference approximation for the $i$-th coordinate:
\begin{equation}
\label{eq:fd}
\frac{\partial f}{\partial x_i}(x) \approx \frac{f(x + \delta e_i) - f(x)}{\delta}.
\end{equation}
The approximation has a mixed error:
\begin{equation}
\left|\frac{\partial f}{\partial x_i}(x) - \frac{f(x + \delta e_i) - f(x)}{\delta}\right| = \underbrace{\frac{\delta}{2}\left|\frac{\partial^2 f}{\partial x_i^2}(x)\right|}_{\text{truncation error, } O(\delta)} + \underbrace{\frac{2\varepsilon_{\text{mach}}}{\delta}}_{\text{round-off error, } O(1/\delta)}.
\end{equation}
The optimal $\delta$ minimises the total error: $\delta^{\star} = (4\varepsilon_{\text{mach}} / |f''|)^{1/2} \approx \sqrt{\varepsilon_{\text{mach}}} \approx 10^{-8}$. In practice, for smooth functions with moderate $|f''|$, values in the range $\delta \in [10^{-4}, 10^{-1}]$ all give low error (Figure~\ref{fig:fd_error}). The values $\delta = 0.05$ (good) and $\delta = 0.8$ (poor) used in this experiment span a large portion of the error curve.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/fd_error_analysis.pdf}
\caption{Q4A: Finite difference gradient error $\|\hat{g}_{\delta} - g\|$ vs step size $\delta$ on Benchmark~B at test point $x = (0, 3)$. The characteristic U-shaped curve reflects the trade-off between truncation error ($O(\delta)$, dominant for large $\delta$) and round-off error ($O(\varepsilon_{\text{mach}}/\delta)$, dominant for small $\delta$). The optimal $\delta^{\star} \approx 10^{-4}$ achieves minimum error $\approx 10^{-8}$. The experimentally used $\delta = 0.05$ gives error $\approx 0.017$ (good) while $\delta = 0.8$ gives error $\approx 0.27$ (poor) --- the figure confirms these are on opposite sides of the optimal. Actual gradient magnitude $\|g\| \approx 2.5$, so relative errors are $\approx 0.7\%$ vs $\approx 11\%$ respectively.}
\label{fig:fd_error}
\end{figure}

\subsubsection{Nesterov Random Search}

\begin{algorithm}[H]
\caption{Nesterov Random Search (Zeroth-Order)}
\label{alg:nrs}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, perturbation $\delta$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State Sample $u_k \sim \mathcal{N}(0, I_d)$, normalise: $\hat{u}_k \gets u_k / \|u_k\|$
    \State $\hat{g}_k \gets \dfrac{f(x_k + \delta \hat{u}_k) - f(x_k)}{\delta}\, \hat{u}_k$ \Comment{Directional derivative estimate}
    \State $x_{k+1} \gets x_k - \alpha\, \hat{g}_k$
\EndFor
\end{algorithmic}
\end{algorithm}

This zeroth-order method uses only 2 function evaluations per iteration, regardless of dimension $d$. The estimator $\hat{g}_k$ provides an unbiased estimate of a smoothed gradient: $\mathbb{E}_u[\hat{g}_k] = \mathbb{E}_u[\frac{f(x + \delta u) - f(x)}{\delta} u] \approx \nabla f_\delta(x)$ where $f_\delta$ is a Gaussian-smoothed version of $f$. The drawback is high variance: only one direction is sampled per step, requiring $O(d/\varepsilon)$ steps to achieve $\varepsilon$-accuracy, versus $O(1/\varepsilon)$ for exact gradient methods.

\subsubsection{Results: Benchmark B}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q4_fval_B.pdf}
\caption{Q4A: Convergence comparison on Benchmark~B. Exact GD converges fastest. FD GD with $\delta = 0.05$ closely tracks exact GD, confirming the gradient approximation quality (truncation error $\approx 0.05 \times |f''|/2 \approx 0.025$ per coordinate, small relative to gradient magnitude). FD GD with $\delta = 0.8$ converges poorly: truncation error $\approx 0.4 \times |f''|$ is comparable to the true gradient, corrupting the descent direction. Nesterov random search converges slowest due to single-direction sampling variance.}
\label{fig:q4_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q4_contour_B.pdf}
\caption{Q4A: Trajectories on Benchmark~B. Exact GD and good FD ($\delta = 0.05$) follow nearly identical paths, confirming gradient fidelity. Poor FD ($\delta = 0.8$) deviates substantially --- the gradient direction is corrupted by truncation error spanning the anisotropic curvature. Nesterov random search follows a noisy, wandering path whose variance decreases slowly as $O(1/k)$.}
\label{fig:q4_contour_B}
\end{figure}

\subsection{Part B: Derivative-Free Methods (Benchmark C)}

\subsubsection{Nelder--Mead Simplex Method}

The Nelder--Mead algorithm maintains a simplex of $n+1 = 3$ vertices in $\mathbb{R}^2$ and iteratively transforms it through four operations: \textbf{reflection} ($\alpha_r = 1$, move worst vertex through centroid), \textbf{expansion} ($\gamma = 2$, extend if reflection improved), \textbf{contraction} ($\rho = 0.5$, pull toward centroid if reflection worsened), and \textbf{shrink} ($\sigma = 0.5$, contract all vertices toward best). These operations are selected by comparing function values at the simplex vertices, requiring no gradient computation.

\begin{remark}[Why Nelder--Mead Excels on Rosenbrock]
The key insight is that the Nelder--Mead simplex \emph{adapts its shape} to the local landscape. On Rosenbrock's narrow curved valley, the simplex naturally elongates along the valley direction: reflection repeatedly moves the worst vertex (off the valley) to the opposite side of the centroid (along the valley), while contraction shrinks towards the best vertex (deepest in the valley). This implicit curvature adaptation is why Nelder--Mead reaches $f \approx 10^{-9}$ (machine precision with 200 iterations) --- a performance that gradient methods cannot match without Hessian information. Gradient methods are \emph{trapped} by the condition number: the step size required for stability along the high-curvature direction ($\lambda_{\max} \approx 800$) provides almost no progress along the low-curvature direction ($\lambda_{\min} \approx 10^{-12}$). The simplex has no such constraint.
\end{remark}

Settings: initial simplex step $= 0.35$, 160 iterations. Standard parameters ($\alpha_r = 1$, $\gamma = 2$, $\rho = 0.5$, $\sigma = 0.5$) are the default Nelder--Mead configuration.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q4_nelder_mead_C.pdf}
\caption{Q4B: Nelder--Mead on Rosenbrock (160 iterations, initial step $= 0.35$). Left: the centroid trajectory navigates the curved valley via repeated reflection and contraction --- the simplex elongates along the low-curvature valley direction, providing implicit curvature adaptation that gradient methods cannot achieve without the Hessian. Right: best objective decreases steadily, reaching $f \approx 0.002$ in 160 iterations (and $f \approx 10^{-9}$ in 200 iterations), dramatically outperforming all gradient-based Q1--Q2 methods.}
\label{fig:q4_nm}
\end{figure}

\subsubsection{Grid Search}

Grid search evaluates the objective at all $55 \times 55 = 3025$ points of a uniform grid over $x_1 \in [-2, 2]$, $x_2 \in [-1, 3]$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q4_grid_search_C.pdf}
\caption{Q4B: Grid search on Rosenbrock. Left: the 3025 grid points cover the search region uniformly; the best point found approximates the true optimum $(1,1)$. Right: the best-so-far curve shows stepwise improvement as better grid points are discovered during the raster scan. Grid search guarantees finding the global optimum to within grid resolution $\Delta x \approx 4/54 \approx 0.074$, but requires $O(N^d)$ evaluations and is impractical for $d \geq 5$.}
\label{fig:q4_grid}
\end{figure}

\subsection{Budget-Equated Comparison on Rosenbrock}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q4_full_comparison.pdf}
\caption{Budget-equated comparison of derivative-based and derivative-free methods on Rosenbrock (Benchmark~C) from $x_0 = (-1, 1)$. \textbf{Left:} convergence vs.\ iteration. Nelder--Mead reaches machine precision ($f \approx 0$) in 200 iterations. Nesterov ($f = 0.196$) substantially outperforms GD ($f = 3.17$) and FD-GD ($f = 5.76$) --- the FD gradient corruption with $\delta = 0.05$ is magnified on the ill-conditioned Rosenbrock landscape. \textbf{Right:} convergence vs.\ total function evaluations (FE). Nelder--Mead uses $\approx 449$ FEs across 200 iterations ($\approx 2.2$ FEs/iter), while FD-GD with $d = 2$ uses $d + 1 = 3$ FEs/iter ($= 601$ total). On an equal FE budget, Nelder--Mead dominates all gradient methods due to its implicit curvature adaptation: the simplex elongates along the valley, bypassing the condition-number bottleneck that cripples all gradient-based methods (both exact and approximate).}
\label{fig:q4_full_cmp}
\end{figure}

\subsection{Computational Comparison}

\begin{table}[H]
\centering
\caption{Q4: Comparison of derivative approximation and derivative-free methods.}
\label{tab:q4_comparison}
\begin{tabular}{lccccl}
\toprule
\textbf{Method} & \textbf{Grad.\ info} & \textbf{FE/iter} & \textbf{Scalability} & \textbf{Accuracy} & \textbf{When to use} \\
\midrule
Exact GD & Full & $d+1$\textsuperscript{a} & $O(md)$ & Optimal & Always, if available \\
FD GD ($\delta$ small) & Approx.\ & $d+1$ & $O(d)$ & Near-optimal & Grad.\ unavail., smooth $f$ \\
FD GD ($\delta$ large) & Corrupted & $d+1$ & $O(d)$ & Poor & Not recommended \\
Nesterov Random & None & 2 & $O(1)$ & $O(1/\sqrt{k})$ & High-$d$, FE cheap \\
Nelder--Mead & None & $\leq d+1$ & Low-$d$ only & Good (small $d$) & $d \leq 10$, non-smooth $f$ \\
Grid Search & None & $N^d$ total & Very poor & Grid-limited & $d \leq 3$, global search \\
\bottomrule
\multicolumn{6}{l}{\textsuperscript{a}FE = function evaluations; $d$ = dimension; $m$ = data size.}
\end{tabular}
\end{table}

The key trade-off is between gradient information quality and computational cost. For $d$-dimensional problems, finite-difference approximation costs $d+1$ evaluations per step (same as exact GD in evaluation count, but with approximation error). Nesterov random search is dimension-free in cost per step but requires $O(d)$ more steps to achieve the same accuracy. For $d = 2$, the cost difference is negligible; for $d = 10^6$ (deep networks), random search becomes the only tractable zeroth-order option.

\begin{remark}[Practical Guidelines for Method Selection]
\begin{itemize}[nosep]
\item \textbf{Use exact gradient methods} when the gradient is available (e.g., via automatic differentiation in PyTorch/JAX). The cost of implementing automatic differentiation is far outweighed by the convergence benefits.
\item \textbf{Use finite differences} when the gradient is not available but the function is cheap to evaluate and $d$ is moderate ($d \leq 1000$). Choose $\delta$ in the range $[10^{-5}, 10^{-2}]$ based on the function's smoothness.
\item \textbf{Use Nesterov random search or evolution strategies} when $d$ is large, the gradient is unavailable, and function evaluations are expensive (e.g., physical simulations, black-box APIs).
\item \textbf{Use Nelder--Mead} when $d \leq 10$, the function may be non-smooth, and high precision is required (it reaches machine precision on Rosenbrock).
\item \textbf{Use grid search} only for global search with $d \leq 3$ and when function evaluations are cheap.
\end{itemize}
\end{remark}

% ============================================================
\section{Question 5: Constrained Optimisation \hfill [30 marks]}
\label{sec:q5}
% ============================================================

\subsection{Problem Setup}

The problem is to minimise Benchmark~B (Equation~\eqref{eq:bench_b}) subject to the constraint $x_1 \geq 0.5$, starting from the infeasible point $x_0 = (0.2, 4.0)$.

\begin{remark}[Constraint Activity at the Optimum]
\label{rem:inactive}
The unconstrained minimiser of Benchmark~B is $x^{\star} = (x_1^{\star}, 2) \approx (0.582, 2)$ with $x_1^{\star} \approx 0.582 > 0.5$. Since the unconstrained minimum already satisfies the constraint, the constraint is \textbf{inactive} at the optimum. Consequently, the constrained and unconstrained problems share the same solution: all methods that converge to the unconstrained minimum will automatically satisfy the constraint. The interesting comparison is therefore about \emph{trajectory behaviour} --- how quickly and reliably methods reach the feasible optimal point starting from the infeasible $x_0$.
\end{remark}

\subsubsection{KKT Conditions}

The Karush--Kuhn--Tucker (KKT) conditions for this problem state that at the constrained optimum $x^{\star}$:
\begin{align}
\nabla f(x^{\star}) + \mu^{\star} \nabla (-x_1 + 0.5) &= 0, \\
\mu^{\star}(-x_1^{\star} + 0.5) &= 0 \quad \text{(complementary slackness)}, \\
\mu^{\star} &\geq 0, \quad x_1^{\star} \geq 0.5.
\end{align}
Since $x_1^{\star} \approx 0.582 > 0.5$, complementary slackness gives $\mu^{\star} = 0$, and the KKT conditions reduce to $\nabla f(x^{\star}) = 0$ --- the unconstrained optimality condition. This confirms the constraint is inactive.

\begin{algorithm}[H]
\caption{Projected Gradient Descent}
\label{alg:pgd}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, projection $\Pi_{\mathcal{X}}$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_{k+1} \gets x_k - \alpha\, \nabla f(x_k)$ \Comment{Unconstrained gradient step}
    \State $x_{k+1} \gets \Pi_{\mathcal{X}}(z_{k+1})$ \Comment{Projection: $x_1 \leftarrow \max(0.5,\, x_1)$}
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{algorithm}[H]
\caption{Penalty Method}
\label{alg:penalty}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, penalty weight $\lambda$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $F_\lambda(x) \gets f(x) + \lambda \max(0,\, 0.5 - x_1)$ \Comment{Penalised objective}
    \State $x_{k+1} \gets x_k - \alpha\, \nabla F_\lambda(x_k)$
\EndFor
\end{algorithmic}
\end{algorithm}

Settings: Projected GD ($\alpha = 0.08$), penalty with $\lambda \in \{0.15, 1.8, 4.5\}$ ($\alpha = 0.05$ for $\lambda \leq 1.8$; $\alpha = 0.03$ for $\lambda = 4.5$), unconstrained GD ($\alpha = 0.07$). All run for 100 iterations.

\subsection{Results}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q5_fval.pdf}
\caption{Q5: Objective value $f(x)$ vs iteration (using the original loss $f$, not penalised loss). All methods converge to $f \approx 0.7244$, the common constrained/unconstrained minimum. The differences lie in the path taken: projected GD projects immediately and descends within the feasible set; penalty methods are initially guided by the penalty gradient away from the infeasible region.}
\label{fig:q5_fval}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q5_contour.pdf}
\caption{Q5: Contour plot with constraint boundary $x_1 = 0.5$ (dashed red) and infeasible region (shaded). Starting point $(0.2, 4.0)$ is infeasible. Projected GD immediately clips to $x_1 = 0.5$ and descends within the feasible half-space. Unconstrained GD's trajectory passes through the infeasible region but converges to the naturally feasible minimum $(0.582, 2.0)$. Penalty methods with larger $\lambda$ are increasingly steered toward the boundary before descending to the minimum.}
\label{fig:q5_contour}
\end{figure}

\subsection{Constraint Violation Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q5_violation.pdf}
\caption{Q5: Constraint violation $v(x) = \max(0,\, 0.5 - x_1)$ on logarithmic scale. Left: full 100 iterations. Right: first 30 iterations (zoomed). Projected GD achieves zero violation from iteration 1 by design. The penalty methods and unconstrained GD all pass through infeasible territory early but converge to the feasible optimum. With small $\lambda = 0.15$, the penalty gradient is weak and infeasibility persists longer; large $\lambda = 4.5$ enforces feasibility rapidly (within $\sim$10 iterations) at the cost of a smaller stable step size.}
\label{fig:q5_violation}
\end{figure}

\subsection{Complete Comparative Results}

\begin{table}[H]
\centering
\caption{Q5: Final positions, objective values, and constraint violations after 100 iterations. All methods converge to the same optimum because the constraint is inactive there (see Remark~\ref{rem:inactive}).}
\label{tab:q5_final}
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{Final $x_1$} & \textbf{Final $x_2$} & \textbf{$f(x)$} & \textbf{Violation $v(x)$} \\
\midrule
Unconstrained GD ($\alpha=0.07$) & 0.5824 & 2.0000 & 0.7244 & 0.0000 \\
Projected GD ($\alpha=0.08$) & 0.5824 & 2.0000 & 0.7244 & 0.0000 \\
Penalty $\lambda=0.15$ ($\alpha=0.05$) & 0.5823 & 2.0000 & 0.7244 & 0.0000 \\
Penalty $\lambda=1.8$ ($\alpha=0.05$) & 0.5824 & 2.0000 & 0.7244 & 0.0000 \\
Penalty $\lambda=4.5$ ($\alpha=0.03$) & 0.5832 & 2.0000 & 0.7244 & 0.0000 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Comparative Discussion}

\textbf{Projected Gradient Descent:} Enforces feasibility as a hard constraint at every iteration via the projection $\Pi_\mathcal{X}(x) = (\max(0.5, x_1), x_2)^T$. When the feasible set $\mathcal{X}$ is a simple half-space, projection is a single $\max$ operation. For general convex sets, the projection may require solving a quadratic programme.

\textbf{Penalty Methods:} Replace the constrained problem with an unconstrained surrogate $F_\lambda(x) = f(x) + \lambda P(x)$ where $P(x) = \max(0, 0.5-x_1)$ is the constraint violation. As $\lambda \to \infty$, the solution of the penalised problem converges to the constrained solution, but larger $\lambda$ requires smaller step sizes for stability. For this specific problem, since the constraint is inactive at the solution, the penalty term vanishes at convergence for any $\lambda > 0$.

\textbf{Limitation of Penalty Methods:} The $L^1$ penalty $\lambda \max(0, 0.5-x_1)$ is non-smooth at $x_1 = 0.5$. Its (sub)gradient is $-\lambda \mathbf{1}[x_1 < 0.5]$ in the $x_1$ direction, causing a discrete jump at the boundary. This can cause gradient oscillation for iterates near $x_1 = 0.5$. A smooth alternative is the $L^2$ penalty $\lambda \max(0, 0.5-x_1)^2$, with gradient $-2\lambda\max(0, 0.5-x_1)$ --- continuous at the boundary. The \emph{augmented Lagrangian} method combines a Lagrange multiplier estimate $\hat\mu$ with a quadratic penalty:
\begin{equation}
\mathcal{L}_\lambda(x, \hat\mu) = f(x) + \hat\mu (-x_1 + 0.5) + \frac{\lambda}{2}\max(0,\, 0.5-x_1)^2,
\end{equation}
updating $\hat\mu$ via a dual ascent step. This achieves exact feasibility at finite $\lambda$ (unlike pure penalty methods which need $\lambda \to \infty$) while maintaining smooth gradient structure.

% ============================================================
\section{Question 6: Linear Programmes and Frank--Wolfe Algorithm \hfill [30 marks]}
\label{sec:q6}
% ============================================================

\subsection{Feasible Set and Linear Programme}

Feasible set: $\mathcal{X} = \{x \in \mathbb{R}^2 : 0.5 \leq x_1 \leq 5,\, -5 \leq x_2 \leq 10\}$ (box-constrained polytope with 4 extreme points / vertices).

\textbf{Linear Programme:} For $f(x) = a^T x$ with $a = [1, 2]^T$, minimise over $\mathcal{X}$:
\begin{equation}
\min_{x \in \mathcal{X}} x_1 + 2x_2.
\end{equation}
Since both components of $a$ are positive, the minimum is attained by taking each coordinate at its lower bound:
\begin{equation}
x^{\star} = (0.5,\, -5), \qquad f^{\star} = 0.5 + 2(-5) = -9.5.
\end{equation}
This is an instance of the \emph{fundamental theorem of linear programming}: the optimum of a linear objective over a bounded polytope is attained at a vertex (extreme point).

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q6_lp.pdf}
\caption{Q6-I: Feasible region $\mathcal{X}$ (blue box) and linear objective contours $f(x) = x_1 + 2x_2$. The contour lines are parallel (linear objective), and the minimum lies at the lower-left vertex $(0.5, -5)$ where $f^{\star} = -9.5$.}
\label{fig:q6_lp}
\end{figure}

\subsection{Frank--Wolfe Algorithm}

\begin{algorithm}[H]
\caption{Frank--Wolfe (Conditional Gradient)}
\label{alg:fw}
\begin{algorithmic}[1]
\Require Initial point $x_0 \in \mathcal{X}$, mixing parameter $\beta$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_k \gets \arg\min_{x \in \mathcal{X}}\, \nabla f(x_k)^T x$ \Comment{LP oracle: linear minimisation over $\mathcal{X}$}
    \State $x_{k+1} \gets \beta\, x_k + (1 - \beta)\, z_k$ \Comment{Convex combination, stays in $\mathcal{X}$}
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{theorem}[Frank--Wolfe Convergence]
\label{thm:fw}
For an $L$-smooth convex function $f$ over a convex set $\mathcal{X}$ with diameter $D = \sup_{x,y \in \mathcal{X}} \|x - y\|$, the Frank--Wolfe algorithm with step size $\gamma_k = 2/(k+2)$ satisfies
\begin{equation}
f(x_k) - f^{\star} \leq \frac{2LD^2}{k+2} = O(1/k).
\end{equation}
\end{theorem}

\begin{remark}
In this implementation, a fixed mixing parameter $\beta$ replaces the standard decaying step $\gamma_k = 1 - \beta = 2/(k+2)$. With fixed $\beta$, the iteration $x_{k+1} = \beta x_k + (1-\beta) z_k$ does not allow the step to decay, so convergence may stall. The Frank--Wolfe gap $g_k = \nabla f(x_k)^T (x_k - z_k)$ measures the optimality gap: $f(x_k) - f^{\star} \leq g_k$ for convex $f$, providing a stopping criterion without knowledge of $f^{\star}$.
\end{remark}

For box constraints, the LP oracle decomposes per coordinate in $O(d)$ time:
\begin{equation}
(z_k)_i = \begin{cases} l_i & \text{if } (\nabla f(x_k))_i > 0 \\ u_i & \text{if } (\nabla f(x_k))_i \leq 0 \end{cases}
\end{equation}
This requires no projection oracle (unlike projected gradient) and costs $O(d)$ per iteration.

\subsection{Interior Optimum: $f(x) = (x_1 - 1)^2 + (x_2 - 5)^2$}

The unconstrained minimum $(1, 5)$ lies inside $\mathcal{X}$ (since $0.5 \leq 1 \leq 5$ and $-5 \leq 5 \leq 10$). Two mixing parameters are tested: $\beta = 0.90$ (larger step, $1 - \beta = 0.10$) and $\beta = 0.985$ (smaller step, $1 - \beta = 0.015$).

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q6_fw_convergence.pdf}
\caption{Q6-II: Frank--Wolfe convergence for interior optimum. With $\beta = 0.985$ (smaller steps), the final objective ($f = 0.0046$) is lower than with $\beta = 0.90$ ($f = 0.0089$). Although larger steps are faster initially, they cause greater oscillation as $z_k$ alternates between boundary vertices of $\mathcal{X}$ (the LP oracle always returns a vertex), while the conservative $\beta = 0.985$ allows smoother incremental progress.}
\label{fig:q6_fw_conv}
\end{figure}

\subsection{Boundary Optimum: $f(x) = x_1^2 + x_2^2$}

The unconstrained minimum $(0, 0)$ lies outside $\mathcal{X}$ (since $x_1 \geq 0.5$). The constrained minimum is at $x^{\star}_c = (0.5, 0)$ on the constraint boundary, with $f^{\star}_c = 0.25$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q6_fw_contour.pdf}
\caption{Q6: Frank--Wolfe trajectories. Left: interior optimum --- trajectories from $(1,1)$ converge towards $(1,5)$ with both $\beta$ values. Right: boundary optimum --- trajectory from $(3,3)$ converges towards $(0.5, 0)$ on the constraint boundary. The key property of Frank--Wolfe is that all iterates are \emph{automatically} in $\mathcal{X}$ (as convex combinations of points in $\mathcal{X}$), without explicit projection at each step.}
\label{fig:q6_fw_contour}
\end{figure}

\subsection{Evolution of $x_k$ and $z_k$}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q6_evolution.pdf}
\caption{Q6: Evolution of iterates $x_k$ and LP vertices $z_k$ over iterations. Top-left ($\beta = 0.90$): $x_k$ converges quickly but $z_k$ oscillates between vertices of $\mathcal{X}$ as the gradient direction alternates near the interior optimum. Top-right ($\beta = 0.985$): convergence is slower but smoother. Bottom-left (boundary optimum): $x_1$ converges toward 0.5, $x_2$ toward 0. Bottom-right: convergence comparison --- the boundary case converges to $f = 0.364$ (above $f^{\star}_c = 0.25$) after 140 iterations; more iterations or a decaying step schedule would be needed to close the gap.}
\label{fig:q6_evolution}
\end{figure}

\begin{table}[H]
\centering
\caption{Q6: Final Frank--Wolfe positions and objective values after specified iterations.}
\label{tab:q6_final}
\begin{tabular}{lcccc}
\toprule
\textbf{Case} & $\beta$ & \textbf{Iters} & \textbf{Final $x$} & \textbf{$f(x)$ / $f^{\star}$} \\
\midrule
Interior opt.\ $(1,5)$ & 0.90 & 180 & $(1.066, 4.932)$ & $0.0089$ / $0.0$ \\
Interior opt.\ $(1,5)$ & 0.985 & 180 & $(0.997, 5.067)$ & $0.0046$ / $0.0$ \\
Boundary opt.\ $(0.5,0)$ & 0.93 & 140 & $(0.500, -0.337)$ & $0.3637$ / $0.25$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Interior vs Boundary Behaviour}

\textbf{Interior optimum:} Frank--Wolfe exhibits a characteristic zig-zag pattern because $z_k$ always selects a vertex of $\mathcal{X}$, and the convex combination $x_{k+1} = \beta x_k + (1-\beta) z_k$ approximates the optimum by averaging many vertex selections. The Theorem~\ref{thm:fw} bound with $L = 2$ (for $f(x) = \|x - x^{\star}\|^2$) and $D = \sqrt{(5-0.5)^2 + 15^2} \approx 15.7$ gives $f(x_k) - f^{\star} \leq 2 \cdot 2 \cdot 15.7^2 / (k+2) = 987/(k+2)$, which predicts $f(x_{180}) - f^{\star} \leq 5.4$. This is a loose bound; the actual convergence is much faster due to the interior location.

The \textbf{Frank--Wolfe gap} at iteration $k$ is defined as
\begin{equation}
g_k^{\text{FW}} = \nabla f(x_k)^T (x_k - z_k),
\end{equation}
and satisfies $f(x_k) - f^{\star} \leq g_k^{\text{FW}}$ for convex $f$. This provides a computable certificate of optimality without knowing $f^{\star}$: when $g_k^{\text{FW}} \leq \varepsilon$, the current iterate is $\varepsilon$-suboptimal. For the interior case with $\beta = 0.985$, the empirically measured final FW gap is $g_{180}^{\text{FW}} \approx 0.082$ (computed numerically), while the actual suboptimality is $f(x_{180}) - f^{\star} = 0.00455 \ll g_{180}^{\text{FW}}$, confirming that the gap bound $f(x_k) - f^{\star} \leq g_k^{\text{FW}}$ holds and is not too loose once the iterate is near the interior optimum.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/fw_gap_analysis.pdf}
\caption{Frank--Wolfe gap analysis for the interior optimum $f(x) = (x_1-1)^2 + (x_2-5)^2$. \textbf{Left:} objective convergence --- $\beta=0.985$ achieves $f = 0.0046$ vs $\beta=0.90$ achieves $f = 0.0089$ after 180 iterations, both converging $O(1/k)$. \textbf{Centre:} FW gap $g_k^{\text{FW}} = \nabla f(x_k)^T(x_k - z_k)$ vs iteration. For $\beta = 0.985$, the gap decreases smoothly to $\approx 0.082$; for $\beta = 0.90$, the gap oscillates ($\approx 7.26$) because the iterates alternate across the feasible region. The theoretical $O(1/k)$ upper bound (dashed, $2LD^2/(k+2) \approx 5.5$ at $k=180$) is looser than the actual gap for $\beta=0.985$. \textbf{Right:} gap as optimality certificate --- $f(x_k) - f^{\star}$ (solid) is always below $g_k^{\text{FW}}$ (dashed), confirming $f(x_k) - f^{\star} \leq g_k^{\text{FW}}$ for both choices of $\beta$.}
\label{fig:fw_gap}
\end{figure}

\textbf{Boundary optimum:} Frank--Wolfe is well-suited to boundary optima: the LP oracle frequently selects the vertex $(0.5, 0)$ closest to the constrained minimum. The iterate naturally approaches the boundary without projection. The residual gap ($f = 0.364$ vs $f^{\star}_c = 0.25$ after 140 iterations) reflects the $O(1/k)$ rate; the standard decaying schedule $\gamma_k = 2/(k+2)$ would guarantee a tighter bound.

\textbf{Comparison with Projected Gradient Descent:} Projected GD achieves a linear rate $O((1-\mu/L)^k)$ for strongly convex $f$, much faster than Frank--Wolfe's $O(1/k)$. However, Frank--Wolfe replaces the projection oracle with the LP oracle: for box constraints, both are $O(d)$, making Frank--Wolfe competitive in cost per iteration. For complex constraint sets (nuclear norm balls, probability simplices, flow polytopes), the LP oracle can be orders of magnitude cheaper than the projection oracle, making Frank--Wolfe the method of choice.

\begin{remark}[Frank--Wolfe in Machine Learning Practice]
The Frank--Wolfe algorithm achieves its most significant advantages over projected gradient methods in three settings common in machine learning:
\begin{itemize}[nosep]
\item \textbf{Low-rank matrix completion} (nuclear norm constraint $\|W\|_* \leq \tau$): projecting onto the nuclear norm ball requires a full SVD ($O(\min(m,n)^2 \max(m,n))$), while the LP oracle requires only the top singular vector ($O(mn)$ via power iteration) --- a speedup of $O(\min(m,n))$.
\item \textbf{Sparse optimisation} (simplex constraint $\|w\|_1 \leq \tau$ or probability simplex $\Delta_d$): the LP oracle is a single \texttt{argmax}, costing $O(d)$, while projection onto $\Delta_d$ requires sorting ($O(d \log d)$).
\item \textbf{Structured prediction} with combinatorial constraint sets (e.g., doubly stochastic matrices, flow polytopes): the LP oracle corresponds to a combinatorial algorithm (optimal assignment, max-flow), while projection is intractable.
\end{itemize}
In these settings, the slower $O(1/k)$ convergence rate is more than compensated by the per-iteration cost saving.
\end{remark}

% ============================================================
\section{Summary Comparison Across All Questions}
\label{sec:summary}
% ============================================================

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/summary_comparison.pdf}
\caption{Summary of final objective values across Q1 (adaptive step-size), Q2 (momentum), and Q3 (Newton's method) methods. The log scale reveals the dramatic differences: on Benchmarks~A and B, most methods converge to the true minimum, while on Benchmark~C (Rosenbrock, $\kappa \approx 2000$), the hierarchy between methods becomes clear. Newton uses only 20 iterations but substantially outperforms all first-order methods with 80--150 iterations on the ill-conditioned Rosenbrock.}
\label{fig:summary}
\end{figure}

Figure~\ref{fig:rosenbrock_full} provides the most informative comparison: all algorithms run for 200 iterations on the most challenging benchmark.

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{figures/rosenbrock_full_comparison.pdf}
\caption{Rosenbrock function (Benchmark~C): comprehensive convergence comparison over 200 iterations (Newton: 50 iterations). All algorithms start from $x_0 = (-1, 1)$. Nelder--Mead achieves essentially machine precision ($f \approx 10^{-9}$), exploiting the valley geometry via simplex adaptation without gradient information. Newton reaches $f = 0.0005$ in 50 iterations via quadratic convergence near the solution. Nesterov reaches $f = 0.196$, Adam $f = 0.598$, while GD barely improves ($f = 3.17$). The hierarchy is: Nelder--Mead $\gg$ Newton $>$ Nesterov $>$ Adam $\gg$ GD.}
\label{fig:rosenbrock_full}
\end{figure}

Table~\ref{tab:full_comparison} consolidates the best results achieved across all methods and benchmarks in the assignment (at the iteration counts used in each question).

\begin{table}[H]
\centering
\caption{Best final objective values across all questions for each benchmark. Rosenbrock ($\kappa\approx2000$) is the most discriminating. `---' = not applied to that benchmark.}
\label{tab:full_comparison}
\begin{tabular}{lcccc}
\toprule
\textbf{Method (Question)} & \textbf{Iters} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD baseline & 80--200 & 0.4826 & 0.7244 & 3.17--3.73 \\
Polyak (Q1, correct $f^{\star}$) & 120 & diverges & diverges & 0.0094 \\
Adagrad (Q1) & 120 & 0.4826 & 0.7244 & 2.20 \\
Heavy Ball (Q1) & 120 & 0.4826 & 0.7244 & 1.40 \\
Nesterov (Q2) & 150--200 & 0.4826 & 0.7244 & 0.196 \\
Adam (Q2) & 150--200 & 0.4826 & 0.7244 & 0.598 \\
Newton (Q3, 20 iters) & 20 & 0.4826 & 0.7244 & 0.686 \\
Newton (Q3+, 50 iters) & 50 & 0.4826 & 0.7244 & 0.0005 \\
Nelder--Mead (Q4, 200 iters) & 200 & --- & --- & $\approx 10^{-9}$ \\
FD-GD $\delta=0.05$ (Q4) & 100 & --- & 0.7244 & --- \\
\midrule
$f^{\star}$ (true minimum) & --- & 0.4826 & 0.7244 & 0.000 \\
\bottomrule
\end{tabular}
\end{table}

A striking result is that Nelder--Mead (derivative-free, 160 iterations) substantially outperforms all gradient-based first-order methods on Rosenbrock. With 200 iterations, it converges to essentially machine precision ($f \approx 10^{-9}$). The simplex naturally elongates along the valley direction, providing implicit curvature adaptation that bypasses the condition-number bottleneck. For gradient methods to match this, either second-order information (Newton) or far more iterations are required.

% ============================================================
\section{Conclusion}
\label{sec:conclusion}
% ============================================================

This report has provided a systematic evaluation of optimisation algorithms across six major categories, revealing several consistent themes.

\textbf{Condition number governs everything.} The condition number $\kappa$ of the problem's Hessian is the single most important determinant of convergence behaviour (Figure~\ref{fig:theory_cond}). On Benchmark~A ($\kappa \approx 6.4$), nearly all methods converge rapidly and the differences are minor. On Benchmark~C ($\kappa \approx 2000$), the same methods diverge dramatically in performance, with differences of two orders of magnitude in final objective value. Theorem~\ref{thm:gd_linear} quantifies this: GD requires $O(\kappa) \approx 27,624$ iterations for $10^{-6}$ accuracy at $\kappa = 2000$, versus $O(\sqrt{\kappa}) \approx 618$ for Nesterov, a $44.7\times$ theoretical speedup.

\textbf{Second-order information is decisive on ill-conditioned problems.} Newton's method reaches $f = 0.686$ on Rosenbrock in 20 iterations (and $f = 0.0005$ in 50 iterations) --- far better than first-order methods with 80--150 iterations, as verified in Figure~\ref{fig:q3_newton_quad}. Proposition~\ref{prop:newton_quadratic} explains the extreme case: on the quadratic Benchmark~A, Newton converges to machine precision in a single step. The cost (solving a $d \times d$ system) scales as $O(d^3)$, prohibitive for large $d$ where quasi-Newton methods (L-BFGS) or conjugate gradient provide practical alternatives with $O(d)$ memory.

\textbf{Nesterov acceleration provides the best first-order option.} With $O(1/k^2)$ rate, Nesterov achieves $f = 0.479$ on Rosenbrock --- $7\times$ better than GD's $f = 3.39$ --- confirming the theoretical superiority of accelerated methods for smooth convex (and near-convex) problems. The explicit quantitative comparison in Section~\ref{sec:q2} shows that GD's linear rate reduces error by only $7.2\%$ in 150 iterations at $\kappa=2000$, while Nesterov's $O(1/k^2)$ bound predicts error $\leq 0.177$, consistent with the observed $0.479$.

\textbf{Polyak step size requires exact $f^{\star}$.} While achieving the best Q1 result when $f^{\star}$ is correctly specified, Polyak diverges catastrophically when $f^{\star}$ is misspecified. This practical limitation makes it less suitable than Adagrad or RMSprop for general use.

\textbf{Derivative-free methods are surprisingly competitive on low-dimensional problems.} Nelder--Mead achieves $f \approx 10^{-9}$ on Rosenbrock with 200 iterations --- machine precision --- surpassing every gradient-based method tested. The simplex's geometric adaptation to the curved valley implicitly captures second-order curvature information. However, this comes without convergence guarantees and the method scales poorly: for $d > 10$ the simplex cannot adequately cover the search space.

\textbf{Constraints were inactive for the Q5 setup.} The unconstrained Benchmark~B minimum $x_1^{\star} \approx 0.582$ already satisfies $x_1 \geq 0.5$, meaning all constrained methods converge to the same point. The comparison highlights trajectory differences: projected GD enforces feasibility immediately, while penalty methods and unconstrained GD pass through the infeasible region before converging to the naturally feasible minimum.

\textbf{Frank--Wolfe exploits structure at the cost of rate.} The $O(1/k)$ rate is slower than projected gradient descent's linear rate, but the LP oracle is $O(d)$ versus the $O(d^2)$ (or more) cost of general projection. For large-scale problems with structured constraint sets (matrix factorisation with nuclear norm constraints, sparse learning with simplex constraints), Frank--Wolfe is the practical choice.

\medskip
\textbf{Overall lesson: match the algorithm to the problem structure.} The hierarchy of algorithm effectiveness on the three benchmarks tells a clear story:

\begin{itemize}[nosep]
\item On \textbf{well-conditioned convex} problems (Benchmark~A): virtually all methods converge; the choice of algorithm is a secondary concern compared to hyperparameter tuning.
\item On \textbf{mildly non-convex, moderate conditioning} problems (Benchmark~B): momentum and adaptive methods provide modest benefits over GD; all gradient methods work reliably.
\item On \textbf{highly ill-conditioned non-convex} problems (Benchmark~C): the choice of algorithm is decisive. Newton's method and Nelder--Mead (for $d = 2$) are orders of magnitude better than first-order gradient methods. For large $d$ where these are intractable, Nesterov is the best first-order option.
\end{itemize}

This analysis directly confirms the central insight from the CS7DS2 course: the \emph{condition number} $\kappa = \lambda_{\max}/\lambda_{\min}$ is the fundamental measure of optimisation difficulty, and algorithm selection should be guided by $\kappa$ and the dimension $d$ of the problem.

% ============================================================
\newpage
\appendix
\section{Python Code}
\label{app:code}
% ============================================================

The complete implementation is provided in the file \texttt{CS7DS2\_Final\_Code\_25336453.py}. Key algorithm implementations are reproduced below for reference.

\begin{lstlisting}[caption={Gradient Descent}]
def gradient_descent(grad_fn, loss_fn, x0, alpha, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for _ in range(n_iters):
        x = x - alpha * grad_fn(x)
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Polyak Step Size}]
def polyak_step(grad_fn, loss_fn, x0, f_star, eps, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        alpha_k = (loss_fn(x) - f_star) / (np.dot(g, g) + eps)
        alpha_hist.append(alpha_k)
        x = x - alpha_k * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Adagrad}]
def adagrad(grad_fn, loss_fn, x0, alpha0, eps, n_iters):
    x = x0.copy().astype(float)
    G = np.zeros_like(x)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        G += g**2
        eff_alpha = alpha0 / (np.sqrt(G) + eps)
        alpha_hist.append(np.mean(eff_alpha))
        x = x - eff_alpha * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={RMSprop}]
def rmsprop(grad_fn, loss_fn, x0, alpha0, beta, eps, n_iters):
    x = x0.copy().astype(float)
    v = np.zeros_like(x)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        v = beta * v + (1 - beta) * g**2
        eff_alpha = alpha0 / (np.sqrt(v) + eps)
        alpha_hist.append(np.mean(eff_alpha))
        x = x - eff_alpha * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Heavy Ball / Polyak Momentum}]
def heavy_ball(grad_fn, loss_fn, x0, alpha, beta, n_iters):
    x = x0.copy().astype(float)
    z = np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for _ in range(n_iters):
        g = grad_fn(x)
        z = beta * z + alpha * g
        x = x - z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Nesterov Momentum}]
def nesterov_momentum(grad_fn, loss_fn, x0, alpha, beta_max, n_iters):
    x = x0.copy().astype(float)
    z = np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for k in range(1, n_iters + 1):
        beta_k = min((k - 1) / (k + 2), beta_max)
        lookahead = x + beta_k * z
        g = grad_fn(lookahead)
        z = beta_k * z - alpha * g
        x = x + z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Adam Optimiser}]
def adam_optimiser(grad_fn, loss_fn, x0, alpha, beta1, beta2, eps, n_iters):
    x = x0.copy().astype(float)
    m_vec, v_vec = np.zeros_like(x), np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for t in range(1, n_iters + 1):
        g = grad_fn(x)
        m_vec = beta1 * m_vec + (1 - beta1) * g
        v_vec = beta2 * v_vec + (1 - beta2) * g**2
        m_hat = m_vec / (1 - beta1**t)
        v_hat = v_vec / (1 - beta2**t)
        x = x - alpha * m_hat / (np.sqrt(v_hat) + eps)
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Newton's Method with Hessian Damping}]
def newtons_method(grad_fn, hess_fn, loss_fn, x0, alpha, n_iters, damping=1e-8):
    x = x0.copy().astype(float)
    x_hist, f_hist, update_norms = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        H = hess_fn(x) if callable(hess_fn) else hess_fn
        H_reg = H + damping * np.eye(len(x))
        try:
            p = np.linalg.solve(H_reg, g)
        except np.linalg.LinAlgError:
            p = g
        update_norms.append(np.linalg.norm(alpha * p))
        x = x - alpha * p
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(update_norms)
\end{lstlisting}

\begin{lstlisting}[caption={Frank--Wolfe (Conditional Gradient)}]
def frank_wolfe(grad_fn, loss_fn, x0, bounds, beta, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist, z_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        z = np.array([bounds[i][0] if g[i] > 0 else bounds[i][1]
                       for i in range(len(g))])
        z_hist.append(z.copy())
        x = beta * x + (1 - beta) * z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(z_hist)
\end{lstlisting}

% ============================================================
\newpage
\section*{References}
\addcontentsline{toc}{section}{References}
% ============================================================

\begin{enumerate}[label={[\arabic*]}]
\item Polyak, B.T. (1969). \textit{Minimization of unsmooth functionals}. USSR Computational Mathematics and Mathematical Physics, 9(3), 14--29.

\item Duchi, J., Hazan, E., \& Singer, Y. (2011). \textit{Adaptive subgradient methods for online learning and stochastic optimization}. Journal of Machine Learning Research, 12, 2121--2159. [Adagrad]

\item Tieleman, T., \& Hinton, G. (2012). \textit{Lecture 6.5 -- RMSprop: Divide the gradient by a running average of its recent magnitude}. COURSERA: Neural Networks for Machine Learning, 4(2).

\item Polyak, B.T. (1964). \textit{Some methods of speeding up the convergence of iteration methods}. USSR Computational Mathematics and Mathematical Physics, 4(5), 1--17. [Heavy Ball]

\item Nesterov, Y. (1983). \textit{A method for solving the convex programming problem with convergence rate $O(1/k^2)$}. Doklady Akademii Nauk SSSR, 269, 543--547.

\item Kingma, D.P., \& Ba, J. (2014). \textit{Adam: A method for stochastic optimization}. arXiv preprint arXiv:1412.6980.

\item Nocedal, J., \& Wright, S.J. (2006). \textit{Numerical Optimization} (2nd ed.). Springer. [Newton's method, quasi-Newton, L-BFGS]

\item Nelder, J.A., \& Mead, R. (1965). \textit{A simplex method for function minimization}. The Computer Journal, 7(4), 308--313.

\item Frank, M., \& Wolfe, P. (1956). \textit{An algorithm for quadratic programming}. Naval Research Logistics Quarterly, 3(1--2), 95--110.

\item Jaggi, M. (2013). \textit{Revisiting Frank-Wolfe: Projection-free sparse convex optimization}. Proceedings of the 30th International Conference on Machine Learning (ICML), 28, 427--435.

\item Boyd, S., \& Vandenberghe, L. (2004). \textit{Convex Optimization}. Cambridge University Press. [KKT conditions, penalty methods, projected gradient]

\item Rosenbrock, H.H. (1960). \textit{An automatic method for finding the greatest or least value of a function}. The Computer Journal, 3(3), 175--184.
\end{enumerate}

\end{document}
