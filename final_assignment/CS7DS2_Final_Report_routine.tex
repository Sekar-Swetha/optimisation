\documentclass[11pt,a4paper]{article}
\usepackage[margin=2.2cm]{geometry}
\usepackage{graphicx}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{float}
\usepackage{hyperref}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{listings}
\usepackage{fancyhdr}
\usepackage{microtype}
\usepackage{array}
\usepackage{multirow}

\hypersetup{colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue}

\lstset{
  language=Python,
  basicstyle=\ttfamily\scriptsize,
  keywordstyle=\color{blue},
  commentstyle=\color{gray},
  stringstyle=\color{red},
  numbers=left,
  numberstyle=\tiny\color{gray},
  breaklines=true,
  frame=single,
  captionpos=b
}

\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small CS7DS2 -- Final Assignment}
\fancyhead[R]{\small Swetha Sekar (25336453)}
\fancyfoot[C]{\thepage}

\newtheorem{remark}{Remark}
\newtheorem{theorem}{Theorem}

\title{%
  \includegraphics[width=0.25\textwidth]{tcd_logo.jpg}\\[1em]
  \textbf{CS7DS2 -- Optimisation Algorithms for Data Analysis}\\[0.5em]
  \Large Final Assignment Report
}
\author{Swetha Sekar \\ Student ID: 25336453 \\ School of Computer Science and Statistics \\ Trinity College Dublin}
\date{April 2026}

\begin{document}
\maketitle
\thispagestyle{empty}

\begin{abstract}
This report presents a comprehensive comparative study of optimisation algorithms applied to three benchmark problems: a linear regression quadratic loss (Benchmark~A), a nonlinear toy neural network loss with sinusoidal perturbation (Benchmark~B), and the highly ill-conditioned Rosenbrock function (Benchmark~C). Six categories of methods are investigated across six questions: (1)~adaptive step-size methods including Polyak, Adagrad, RMSprop, and Heavy Ball; (2)~momentum-based methods including Nesterov acceleration and Adam, with mini-batch stochastic gradient descent; (3)~Newton's method with first- and second-order local approximations; (4)~derivative-free methods including finite differences, Nesterov random search, Nelder--Mead simplex, and grid search; (5)~constrained optimisation via projected gradient descent and penalty methods; and (6)~the Frank--Wolfe algorithm for linear-constrained problems. Key findings include: the Polyak step size diverges when $f^\star$ is misspecified (observed on Benchmarks~A and~B) but achieves superior convergence on Rosenbrock where $f^\star=0$ is exact; Nesterov acceleration achieves $f = 0.479$ on Rosenbrock versus $f = 3.39$ for baseline gradient descent; Newton's method converges to the linear regression minimum in a single step; the penalty method constraint violation decreases with penalty weight $\lambda$ but requires reduced step sizes for stability; and the Frank--Wolfe algorithm naturally maintains feasibility without projection, achieving $O(1/k)$ convergence for general convex objectives. Each method is evaluated in terms of convergence speed, trajectory behaviour, stability, and computational trade-offs, with connections to theoretical guarantees from the course.
\end{abstract}

\tableofcontents
\newpage

% ============================================================
\section{Benchmark Problems and Experimental Setup}
\label{sec:benchmarks}
% ============================================================

All experiments use a fixed random seed (\texttt{np.random.seed(42)}) for reproducibility.

\subsection{Benchmark A: Linear Regression Quadratic Loss}

A synthetic dataset with $m = 1000$ samples and two parameters is generated:
\begin{equation}
\theta^{\star} = \begin{bmatrix} 3 \\ 4 \end{bmatrix}, \quad x^{(i)} \in \mathbb{R}^2, \quad y^{(i)} = {\theta^{\star}}^T x^{(i)} + \varepsilon^{(i)}, \quad \varepsilon^{(i)} \sim \mathcal{N}(0, 1).
\end{equation}
The loss function is the mean squared error:
\begin{equation}
J(\theta) = \frac{1}{2m} \sum_{i=1}^{m} \left(\theta^T x^{(i)} - y^{(i)}\right)^2 = \frac{1}{2}\theta^T H \theta - b^T \theta + c,
\end{equation}
where $H = \frac{1}{m}X^TX$ is the Gram matrix, $b = \frac{1}{m}X^T y$, and $c = \frac{1}{2m}\|y\|^2$. This is a strongly convex quadratic with constant Hessian $H$. The gradient is $\nabla J(\theta) = H\theta - b = \frac{1}{m}X^T(X\theta - y)$. Due to observation noise, the ordinary least-squares minimiser is $\hat{\theta}_{\text{OLS}} = H^{-1}b = [3.028,\; 4.017]^T$ (close to $\theta^\star$, as expected by statistical consistency), with minimum loss $J^\star = J(\hat{\theta}_{\text{OLS}}) = 0.4826$. The Hessian eigenvalues are $\lambda_{\min} = 0.925$ and $\lambda_{\max} = 1.033$, giving condition number $\kappa(H) = 1.117$ --- a near-perfectly conditioned problem. For gradient descent to be stable, the step size must satisfy $\alpha < 2/\lambda_{\max} = 1.936$; the chosen $\alpha = 0.08$ is well within this range. Starting point: $\theta_0 = [0, 0]^T$.

\subsection{Benchmark B: Toy Neural Network Quadratic Loss}
\begin{equation}
f(x_1, x_2) = (x_1 - 1)^2 + 5(x_2 - 2)^2 + \sin(x_1).
\end{equation}
The gradient is $\nabla f = [2(x_1 - 1) + \cos(x_1),\ 10(x_2 - 2)]^T$ and the Hessian is $H(x) = \text{diag}(2 - \sin(x_1),\ 10)$. The $x_2$ direction is 10 times stiffer than the $x_1$ direction, creating anisotropy. The unconstrained minimum is found numerically at $x^\star \approx (0.5824, 2.0)$ with $f^\star \approx 0.7244$; at this point the Hessian has eigenvalues $1.450$ and $10.0$, giving local condition number $\kappa \approx 6.90$. Starting point: $x_0 = [-1, 4]^T$.

\subsection{Benchmark C: Rosenbrock Function}
\begin{equation}
f(x_1, x_2) = (1 - x_1)^2 + 100(x_2 - x_1^2)^2.
\end{equation}
The global minimum is at $(1, 1)$ with $f^\star = 0$. The Hessian at the optimum $(1,1)$ is:
\begin{equation}
\nabla^2 f(1,1) = \begin{bmatrix} 2 + 1200(1)^2 - 400(1) & -400(1) \\ -400(1) & 200 \end{bmatrix} = \begin{bmatrix} 802 & -400 \\ -400 & 200 \end{bmatrix},
\end{equation}
with eigenvalues $0.40$ and $1001.6$, giving condition number $\kappa \approx 2508$. For gradient descent stability at the optimum, the step size must satisfy $\alpha < 2/\lambda_{\max} \approx 2/1001.6 \approx 0.002$, which is why the baseline uses $\alpha = 0.0012$. This extreme ill-conditioning, combined with the narrow parabolic valley $x_2 \approx x_1^2$, makes Rosenbrock the canonical challenge for gradient-based optimisers: the gradient is nearly perpendicular to the valley floor, so gradient steps mostly oscillate across the valley rather than advancing along it. Starting point: $x_0 = [-1, 1]^T$.

\subsection{Benchmark D: Constrained Feasible Set}
For constrained problems (Questions~5 and~6): $\mathcal{X} = \{x \in \mathbb{R}^2 : 0.5 \leq x_1 \leq 5,\ -5 \leq x_2 \leq 10\}$.

% ============================================================
\section{Question 1: Adaptive Step-Size Methods \hfill [30 marks]}
\label{sec:q1}
% ============================================================

\subsection{Algorithm Formulations}

Four adaptive step-size methods are compared against a constant step-size gradient descent (GD) baseline across all three benchmarks for 120 iterations. The baseline GD update is $x_{k+1} = x_k - \alpha \nabla f(x_k)$, where $\alpha$ is fixed.

\begin{algorithm}[H]
\caption{Polyak Step Size}
\label{alg:polyak}
\begin{algorithmic}[1]
\Require Initial point $x_0$, optimal value $f^{\star}$, tolerance $\varepsilon > 0$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $\alpha_k \gets \dfrac{f(x_k) - f^{\star}}{\|g_k\|^2 + \varepsilon}$
    \State $x_{k+1} \gets x_k - \alpha_k\, g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{remark}
The Polyak step size is theoretically motivated: for a convex function, minimising the bound $\|x_{k+1} - x^\star\|^2 \leq \|x_k - x^\star\|^2 - \frac{(f(x_k) - f^\star)^2}{\|\nabla f(x_k)\|^2}$ yields exactly $\alpha_k = (f(x_k)-f^\star)/\|\nabla f(x_k)\|^2$. When $f^\star$ is exact and $f$ is strongly convex with parameter $\mu$, the Polyak step achieves linear convergence at the optimal rate.
\end{remark}

\begin{algorithm}[H]
\caption{Adagrad}
\label{alg:adagrad}
\begin{algorithmic}[1]
\Require Initial point $x_0$, base learning rate $\alpha_0$, tolerance $\varepsilon$, iterations $N$
\State $G_0 \gets \mathbf{0} \in \mathbb{R}^d$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $G_{k+1} \gets G_k + g_k \odot g_k$ \Comment{Per-coordinate accumulation}
    \State $x_{k+1} \gets x_k - \dfrac{\alpha_0}{\sqrt{G_{k+1}} + \varepsilon} \odot g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

Adagrad accumulates squared gradients per coordinate, reducing the effective learning rate more aggressively in directions with large historical gradients. This makes it well-suited to sparse gradients (common in NLP tasks) but causes premature slowing on dense gradient problems, since $G_k \to \infty$ and the effective step $\alpha_0/\sqrt{G_k} \to 0$ monotonically.

\begin{algorithm}[H]
\caption{RMSprop}
\label{alg:rmsprop}
\begin{algorithmic}[1]
\Require Initial point $x_0$, learning rate $\alpha_0$, decay $\beta \in (0,1)$, tolerance $\varepsilon$, iterations $N$
\State $v_0 \gets \mathbf{0}$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $v_{k+1} \gets \beta\, v_k + (1 - \beta)\, g_k \odot g_k$ \Comment{Exponential moving average}
    \State $x_{k+1} \gets x_k - \dfrac{\alpha_0}{\sqrt{v_{k+1}} + \varepsilon} \odot g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

RMSprop replaces Adagrad's cumulative sum with an exponential moving average (decay $\beta = 0.9$), preventing the step size from vanishing. It can be seen as a heuristic approximation to the diagonal of the Fisher information matrix. The effective step size $\alpha_0/\sqrt{v_{k+1}}$ adapts to recent gradient magnitudes rather than all historical gradients.

\begin{algorithm}[H]
\caption{Polyak Momentum / Heavy Ball}
\label{alg:heavyball}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, momentum $\beta \in (0,1)$, iterations $N$
\State $z_0 \gets \mathbf{0}$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_{k+1} \gets \beta\, z_k + \alpha\, \nabla f(x_k)$
    \State $x_{k+1} \gets x_k - z_{k+1}$
\EndFor
\end{algorithmic}
\end{algorithm}

The Heavy Ball method accumulates a velocity $z_k$, allowing it to build up speed in consistent gradient directions and damp oscillations across them. For strongly convex quadratics with condition number $\kappa$, the optimal parameters are $\alpha^\star = \frac{4}{(\sqrt{\lambda_{\max}} + \sqrt{\lambda_{\min}})^2}$ and $\beta^\star = \left(\frac{\sqrt{\kappa}-1}{\sqrt{\kappa}+1}\right)^2$, achieving a convergence rate of $\left(\frac{\sqrt{\kappa}-1}{\sqrt{\kappa}+1}\right)^2 < 1-1/\sqrt{\kappa}$, which is significantly faster than gradient descent's rate of $1 - 1/\kappa$.

\subsection{Hyperparameter Settings}

\begin{table}[H]
\centering
\caption{Q1: Hyperparameter settings for each method and benchmark. All methods run for 120 iterations.}
\label{tab:q1_params}
\begin{tabular}{llccc}
\toprule
\textbf{Method} & \textbf{Parameter} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & $\alpha$ & 0.08 & 0.06 & 0.0012 \\
\midrule
Polyak & $f^{\star}$ & 0 & 0 & 0 \\
 & $\varepsilon$ & $10^{-4}$ & $10^{-4}$ & $10^{-3}$ \\
\midrule
Adagrad & $\alpha_0$ & 1.8 & 1.2 & 0.45 \\
 & $\varepsilon$ & $10^{-5}$ & $10^{-5}$ & $10^{-5}$ \\
\midrule
RMSprop & $\alpha_0$ & 0.22 & 0.14 & 0.0035 \\
 & $\beta$ & 0.9 & 0.9 & 0.9 \\
 & $\varepsilon$ & $10^{-5}$ & $10^{-5}$ & $10^{-5}$ \\
\midrule
Heavy Ball & $\alpha$ & 0.045 & 0.035 & 0.0008 \\
 & $\beta$ & 0.88 & 0.90 & 0.86 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Convergence Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_A.pdf}
\caption{Q1: Convergence on Benchmark~A (Linear Regression, $J^\star = 0.4826$). GD, Adagrad, and Heavy Ball all converge to the noise floor $J^\star = 0.4826$, confirming successful minimisation. RMSprop converges to a slightly higher value ($J = 0.5064$) due to oscillation of its adaptive step near the minimum. Polyak \textbf{diverges} (final $f = 4.767$) because $f^\star = 0$ is misspecified: the true minimum is $J^\star = 0.4826 > 0$, so as the iterate approaches $\hat{\theta}_{\text{OLS}}$ and $\|\nabla J\| \to 0$, the step size $\alpha_k = (J(x_k) - 0)/\|\nabla J\|^2 \to \infty$, causing oscillation rather than convergence.}
\label{fig:q1_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_B.pdf}
\caption{Q1: Convergence on Benchmark~B (Toy Neural Network, $f^\star = 0.7244$). The $f^\star = 0$ misspecification again affects Polyak (final $f = 0.982$), which stalls near the true minimum but fails to converge. Adagrad and Heavy Ball both reach the true minimum $f^\star = 0.7244$. The moderate anisotropy ($\kappa \approx 6.9$) does not pose severe difficulties for momentum or adaptive methods, but GD with step $\alpha = 0.06$ also converges given the relatively small condition number.}
\label{fig:q1_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_C.pdf}
\caption{Q1: Convergence on Benchmark~C (Rosenbrock, $f^\star = 0$). Here $f^\star = 0$ is exact, and Polyak converges to $f = 0.024$, achieving the best performance. The well-specified $f^\star$ yields $\alpha_k = f(x_k)/\|\nabla f(x_k)\|^2$, which decreases as $f \to 0$ and prevents divergence. Heavy Ball achieves $f = 1.400$, outperforming GD ($f = 3.514$) through momentum accumulation along the valley. Adagrad ($f = 2.203$) provides moderate improvement, while RMSprop ($f = 3.120$) performs similarly to GD due to the very small base rate $\alpha_0 = 0.0035$.}
\label{fig:q1_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q1: Final objective values after 120 iterations. Bold indicates best per benchmark.}
\label{tab:q1_final}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Bench.\ A} ($J^\star=0.4826$) & \textbf{Bench.\ B} ($f^\star=0.7244$) & \textbf{Bench.\ C} ($f^\star=0$) \\
\midrule
GD (baseline) & 0.4826 & 0.7244 & 3.5142 \\
Polyak & 4.7670 & 0.9819 & \textbf{0.0239} \\
Adagrad & \textbf{0.4826} & \textbf{0.7244} & 2.2034 \\
RMSprop & 0.5064 & 0.7276 & 3.1204 \\
Heavy Ball & \textbf{0.4826} & \textbf{0.7244} & 1.4003 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Key insight regarding Polyak divergence:} The step size formula $\alpha_k = (f(x_k) - f^\star)/(\|\nabla f(x_k)\|^2 + \varepsilon)$ requires an accurate lower bound $f^\star \leq f(x^\star)$. When $f^\star = 0$ is used but the true minimum is $J^\star = 0.4826$ (Benchmark~A) or $f^\star = 0.7244$ (Benchmark~B), the numerator $(f(x_k) - 0)$ remains large even when the iterate is near the optimum while the gradient norm $\|\nabla f(x_k)\|^2$ becomes small. This causes $\alpha_k \to \infty$, inducing oscillations and divergence. On Rosenbrock, $f^\star = 0$ is exact, so the numerator correctly vanishes as $f(x_k) \to 0$, yielding a decreasing, well-behaved step sequence.

\subsection{Contour Plots with Optimisation Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q1_contour_B.pdf}
\caption{Q1: Trajectories on Benchmark~B. The elliptical contours reflect the anisotropy ($5\times$ stiffer in $x_2$). GD and Adagrad follow near-direct paths towards the minimum. Heavy Ball with $\beta = 0.90$ shows slight overshoot in the flat $x_1$-direction before settling. Polyak diverges widely, with iterates oscillating as its step size grows unbounded near the misspecified minimum.}
\label{fig:q1_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q1_contour_C.pdf}
\caption{Q1: Trajectories on Benchmark~C (Rosenbrock). The banana-shaped narrow valley $x_2 \approx x_1^2$ is visible. All methods first descend rapidly towards the valley floor, then progress slowly along it towards $(1,1)$. The Polyak trajectory (correct $f^\star$) shows the most aggressive valley-floor progress. GD with $\alpha = 0.0012$ barely advances due to the small step size required for stability in the ill-conditioned ($\kappa \approx 2508$) landscape.}
\label{fig:q1_contour_C}
\end{figure}

\subsection{Adaptive Step-Size Evolution}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_A.pdf}
\caption{Q1: Step-size evolution on Benchmark~A. Polyak's step size grows unboundedly as $\|\nabla J\| \to 0$ near the true minimum while the misspecified gap $(J - 0)$ remains nonzero — confirming the divergence mechanism. Adagrad decays monotonically as $G_k = \sum_j g_j^2 \to \infty$ in an unrecoverable manner. RMSprop stabilises via its exponential forgetting ($\beta = 0.9$), maintaining a moderate effective step size throughout.}
\label{fig:q1_stepsize_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_B.pdf}
\caption{Q1: Step-size evolution on Benchmark~B. The same Polyak divergence pattern appears. Adagrad decays sharply in early iterations when large gradients from the starting point $x_0 = [-1, 4]^T$ (far from minimum) accumulate. RMSprop maintains a moderate, slowly-varying effective step as it adapts to recent gradient magnitudes.}
\label{fig:q1_stepsize_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_C.pdf}
\caption{Q1: Step-size evolution on Benchmark~C (Rosenbrock). With the exact $f^\star = 0$, the Polyak step decreases smoothly: $\alpha_k = f(x_k)/\|\nabla f(x_k)\|^2$ decreases as $f(x_k) \to 0$, confirming well-behaved convergence. Adagrad and RMSprop show their characteristic adaptive patterns.}
\label{fig:q1_stepsize_C}
\end{figure}

\subsection{Comparative Analysis}

\textbf{Convergence speed:} When $f^\star$ is known exactly (Rosenbrock), Polyak converges fastest ($f = 0.024$), exploiting the adaptive step formula $\alpha_k = (f(x_k) - f^\star)/\|\nabla f(x_k)\|^2$ which is optimal for subgradient methods. For well-conditioned problems (Benchmark~A, $\kappa = 1.117$), the GD linear convergence rate $(1 - \mu/L)^k = (1 - 0.925/1.033)^k \approx 0.895^k$ is already fast, and adaptive methods offer little additional benefit. For moderately ill-conditioned problems (Benchmark~B, $\kappa = 6.9$), Heavy Ball's convergence rate $(\frac{\sqrt{\kappa}-1}{\sqrt{\kappa}+1})^2 \approx (\frac{1.63}{3.63})^2 \approx 0.20^2 = 0.04$ per iteration represents a significant improvement over GD's rate $1 - 1/\kappa \approx 0.855$.

\textbf{Smoothness:} RMSprop produces the smoothest convergence curves due to exponential averaging of squared gradients with $\beta = 0.9$, which acts as a low-pass filter on the effective step size. Heavy Ball shows characteristic overshoot followed by damped oscillation --- the momentum accumulates in the wrong direction briefly before the gradient corrects it. Adagrad curves are monotonically non-increasing (by construction, since the accumulated sum $G_k$ only grows).

\textbf{Stability:} GD baseline is always stable for step sizes well below the stability threshold $\alpha < 2/\lambda_{\max}$. Polyak is unstable when $f^\star$ is misspecified. Adagrad is uniformly stable (monotonically shrinking step), but this is also its weakness: on Benchmark~C, the step decays so aggressively that later iterations make negligible progress. RMSprop and Heavy Ball are stable with proper hyperparameter tuning.

\textbf{Connection to course theory:} As discussed in the lectures, the Polyak step is the solution to $\min_\alpha \|x_k - \alpha g_k - x^\star\|^2$ (the step that minimises the distance to $x^\star$), yielding $\alpha_k = \langle x_k - x^\star, g_k \rangle / \|g_k\|^2 \leq (f(x_k) - f^\star)/\|g_k\|^2$ for convex $f$ (by the subgradient inequality). The formula used here adds $\varepsilon$ to the denominator for numerical stability. Adagrad's theoretical motivation comes from online convex optimisation: it achieves a regret bound $R_T \leq \alpha_0 \sum_i \sqrt{T\sum_t g_{t,i}^2}$ which is optimal for adversarial settings with sparse gradients.

% ============================================================
\section{Question 2: Momentum and Stochastic Methods \hfill [30 marks]}
\label{sec:q2}
% ============================================================

\subsection{Algorithm Formulations}

\begin{algorithm}[H]
\caption{Nesterov Momentum / Acceleration}
\label{alg:nesterov}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, max momentum $\beta_{\max}$, iterations $N$
\State $z_0 \gets \mathbf{0}$
\For{$k = 1, 2, \ldots, N$}
    \State $\beta_k \gets \min\!\left(\dfrac{k-1}{k+2},\ \beta_{\max}\right)$
    \State $\tilde{x}_k \gets x_k + \beta_k\, z_k$ \Comment{Lookahead / extrapolated point}
    \State $z_{k+1} \gets \beta_k\, z_k - \alpha\, \nabla f(\tilde{x}_k)$
    \State $x_{k+1} \gets x_k + z_{k+1}$
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{remark}
The crucial distinction from Heavy Ball is the \emph{lookahead}: the gradient is evaluated at the extrapolated point $\tilde{x}_k = x_k + \beta_k z_k$ rather than at $x_k$. This ``corrective'' evaluation anticipates the next position, allowing Nesterov momentum to damp oscillations that Heavy Ball would amplify. For smooth convex functions with Lipschitz constant $L$, Nesterov's method achieves $f(x_k) - f^\star \leq O(L\|x_0 - x^\star\|^2/k^2)$, the optimal first-order convergence rate, compared to $O(L/k)$ for gradient descent.
\end{remark}

\begin{algorithm}[H]
\caption{Adam Optimiser}
\label{alg:adam}
\begin{algorithmic}[1]
\Require Initial point $x_0$, learning rate $\alpha$, moments $\beta_1, \beta_2 \in (0,1)$, tolerance $\varepsilon$, iterations $N$
\State $m_0 \gets \mathbf{0},\ v_0 \gets \mathbf{0}$
\For{$t = 1, 2, \ldots, N$}
    \State $g_t \gets \nabla f(x_t)$
    \State $m_t \gets \beta_1\, m_{t-1} + (1 - \beta_1)\, g_t$ \Comment{First moment (momentum)}
    \State $v_t \gets \beta_2\, v_{t-1} + (1 - \beta_2)\, g_t \odot g_t$ \Comment{Second moment (adaptive scaling)}
    \State $\hat{m}_t \gets m_t\, /\, (1 - \beta_1^t)$ \Comment{Bias correction (prevents cold start)}
    \State $\hat{v}_t \gets v_t\, /\, (1 - \beta_2^t)$
    \State $x_{t+1} \gets x_t - \alpha\, \hat{m}_t\, /\, (\sqrt{\hat{v}_t} + \varepsilon)$
\EndFor
\end{algorithmic}
\end{algorithm}

Adam combines heavy-ball momentum (via $m_t$) with per-coordinate RMSprop-style adaptive scaling (via $v_t$). The bias correction terms $(1 - \beta_j^t)$ prevent the initialisation at zero from biasing early iterates: without them, early estimates $m_t \approx (1-\beta_1)g_1$ and $v_t \approx (1-\beta_2)g_1^2$ would be far below their true scale. In practice, Adam is the default optimiser for training deep neural networks due to its robustness to hyperparameter choices. However, theoretical convergence guarantees for non-convex problems are limited, and counter-examples showing Adam can fail to converge exist.

\subsection{Hyperparameter Settings}

\begin{table}[H]
\centering
\caption{Q2: Hyperparameter settings. Nesterov and Adam run for 150 iterations.}
\label{tab:q2_params}
\begin{tabular}{llccc}
\toprule
\textbf{Method} & \textbf{Parameter} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & $\alpha$ & 0.08 & 0.06 & 0.0012 \\
\midrule
Nesterov & $\alpha$ & 0.06 & 0.035 & 0.0007 \\
 & $\beta_{\max}$ & 0.90 & 0.92 & 0.90 \\
\midrule
Adam & $\alpha$ & 0.12 & 0.08 & 0.006 \\
 & $\beta_1$ & 0.82 & 0.80 & 0.80 \\
 & $\beta_2$ & 0.999 & 0.999 & 0.999 \\
 & $\varepsilon$ & $10^{-8}$ & $10^{-8}$ & $10^{-8}$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Convergence Comparison}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_A.pdf}
\caption{Q2: Convergence on Benchmark~A (Linear Regression). All three methods converge to $J^\star = 0.4826$. Nesterov and Adam converge in fewer iterations than GD owing to momentum and adaptive scaling, though on this near-isotropic problem ($\kappa = 1.12$) all methods converge quickly and differences are minor. The quadratic loss surface is ideally suited for both momentum and adaptive methods.}
\label{fig:q2_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_B.pdf}
\caption{Q2: Convergence on Benchmark~B (Toy Neural Network, $\kappa \approx 6.9$). Both Nesterov and Adam converge to $f^\star = 0.7244$. Adam's per-coordinate adaptive scaling effectively compensates for the 6.9:1 curvature ratio between directions, taking larger steps in the flat $x_1$-direction and smaller steps in the steep $x_2$-direction. Nesterov's lookahead prevents overshoot across the narrow dimension.}
\label{fig:q2_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_C.pdf}
\caption{Q2: Convergence on Benchmark~C (Rosenbrock, $\kappa \approx 2508$). The ill-conditioned curved valley reveals the most significant differences. Nesterov achieves $f = 0.479$ — a $7\times$ improvement over GD ($f = 3.388$) — by accumulating velocity along the consistent valley-floor direction. Adam reaches $f = 1.578$; while per-coordinate scaling helps navigate the anisotropy, the rapidly changing curvature of the nonlinear valley limits Adam's effectiveness relative to Nesterov's pure momentum accumulation.}
\label{fig:q2_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q2: Final objective values after 150 iterations. Note GD baseline runs 150 iterations here vs 120 in Q1.}
\label{tab:q2_final}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Bench.\ A} ($J^\star=0.4826$) & \textbf{Bench.\ B} ($f^\star=0.7244$) & \textbf{Bench.\ C} ($f^\star=0$) \\
\midrule
GD (baseline) & 0.4826 & 0.7244 & 3.3880 \\
Nesterov & 0.4826 & 0.7244 & \textbf{0.4788} \\
Adam & 0.4826 & 0.7244 & 1.5785 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Contour Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q2_contour_B.pdf}
\caption{Q2: Trajectories on Benchmark~B. Nesterov's trajectory shows the characteristic smooth arc of accelerated methods: the lookahead step predicts and corrects potential overshoots, resulting in a cleaner path to the minimum. Adam takes more direct per-step progress by independently scaling the two coordinates, exploiting the known anisotropy. GD follows a straight-line descent but takes more steps.}
\label{fig:q2_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q2_contour_C.pdf}
\caption{Q2: Trajectories on Benchmark~C. All methods descend to the parabolic valley floor ($x_2 \approx x_1^2$) quickly from $x_0 = (-1, 1)$, but differ significantly in valley-floor progress. Nesterov builds momentum in the consistent rightward direction along the valley, achieving greater displacement per iteration. GD must take tiny steps ($\alpha = 0.0012$) to avoid crossing the steep valley walls, severely limiting valley traversal speed. Adam improves upon GD but cannot match Nesterov's directional momentum in this geometry.}
\label{fig:q2_contour_C}
\end{figure}

\subsection{Mini-Batch Stochastic Gradient Descent}

Mini-batch SGD on Benchmark~A uses learning rate $\alpha = 0.06$, batch sizes $b \in \{5, 40\}$, 50 epochs, with data shuffled at every epoch. The mini-batch gradient is:
\begin{equation}
g_{\mathcal{B}} = \frac{1}{|\mathcal{B}|} X_{\mathcal{B}}^T (X_{\mathcal{B}} \theta - y_{\mathcal{B}}),
\end{equation}
which is an unbiased estimator of the full gradient $\nabla J(\theta)$, since $\mathbb{E}[g_{\mathcal{B}}] = \nabla J(\theta)$. The variance satisfies $\mathbb{V}[g_{\mathcal{B}}] = \frac{1}{b} \mathbb{V}[g_i]$ where $g_i$ is a single-sample gradient, so variance decreases linearly with batch size. For a fixed learning rate, the expected suboptimality after $T$ iterations converges to a noise floor proportional to $\alpha \mathbb{V}[g_{\mathcal{B}}] \propto \alpha/b$, not zero. This is the fundamental limitation of constant step-size SGD: it cannot converge to the exact minimiser, only to a ball around it of radius $O(\alpha/b)$.

\begin{algorithm}[H]
\caption{Mini-Batch SGD}
\label{alg:sgd}
\begin{algorithmic}[1]
\Require Data $(X, y)$, initial $\theta_0$, learning rate $\alpha$, batch size $b$, epochs $E$
\For{$e = 1, 2, \ldots, E$}
    \State Randomly permute data indices
    \For{each mini-batch $\mathcal{B}$ of size $b$}
        \State $g \gets \frac{1}{|\mathcal{B}|} X_{\mathcal{B}}^T(X_{\mathcal{B}}\theta - y_{\mathcal{B}})$
        \State $\theta \gets \theta - \alpha\, g$
    \EndFor
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_sgd_batch.pdf}
\caption{Q2: Mini-batch SGD with different batch sizes on Benchmark~A (50 epochs). With $b = 5$, each epoch performs $1000/5 = 200$ gradient updates, providing rapid per-epoch convergence but with higher variance noise (each mini-batch gradient deviates more from the true gradient). With $b = 40$, each epoch performs $1000/40 = 25$ updates; the gradient estimates are more accurate (lower variance), but progress is slower per epoch due to fewer updates. Smaller batches converge faster in epochs but produce more oscillation near the minimum — a fundamental bias-variance trade-off of stochastic optimisation.}
\label{fig:q2_sgd_batch}
\end{figure}

\subsection{SGD with Increased Noise}

The experiment is repeated with output noise scaled by a factor of $6$ (i.e., $\varepsilon^{(i)} \sim \mathcal{N}(0, 36)$ instead of $\mathcal{N}(0,1)$), increasing the irreducible noise floor.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q2_sgd_noisy.pdf}
\caption{Q2: Effect of noise on SGD convergence. Left: original noise ($\sigma = 1$). Right: high noise ($\sigma = 6$). Increasing the observation noise by a factor of 6 raises the minimum achievable loss by a factor of $\sim 36$ (since $J^\star \propto \sigma^2$), pushing the noise floor from $\approx 0.48$ to $\approx 17.4$. Under high noise, both batch sizes converge to approximately the same elevated floor --- the irreducible noise from the data-generating process dominates the mini-batch sampling variance. Crucially, large batch size $b = 40$ does not offer a meaningful advantage under high label noise, because the dominant source of gradient error is the noise in $y$ rather than the sampling of the mini-batch.}
\label{fig:q2_sgd_noisy}
\end{figure}

\subsection{Comparative Discussion: Nesterov vs Adam}

On well-conditioned problems (Benchmarks~A and~B), Nesterov and Adam both achieve convergence to the true minimum, with little qualitative difference. The key distinction emerges on the ill-conditioned Rosenbrock function, where convergence to a low $f$ value requires efficiently traversing a narrow, curved valley:

\begin{itemize}[nosep]
  \item \textbf{Nesterov} achieves $f = 0.479$ on Rosenbrock by accumulating momentum in the consistent valley-floor direction. The gradient along the Rosenbrock valley floor points roughly in the increasing-$x_1$ direction, so a momentum accumulator $z_k$ that persists across iterations efficiently exploits this directional consistency. The lookahead step $\tilde{x}_k = x_k + \beta_k z_k$ pre-positions the gradient evaluation in the expected next location, correcting potential overshoots before they occur. This is why Nesterov achieves the theoretically optimal $O(1/k^2)$ rate on convex functions, compared to $O(1/k)$ for GD.
  \item \textbf{Adam} achieves $f = 1.578$. While Adam's per-coordinate scaling (via $v_t$) compensates for diagonal curvature differences between $x_1$ and $x_2$, the Rosenbrock valley's curvature is highly non-diagonal (the off-diagonal Hessian entry $-400x_1$ is large). Adam cannot exploit off-diagonal curvature structure, limiting its effectiveness. Furthermore, the second-moment estimate $v_t$ reacts to recent gradient magnitudes, which vary significantly along the curved valley, causing the effective step size to fluctuate rather than accumulate speed.
  \item \textbf{Key principle:} Pure momentum methods (Nesterov) outperform adaptive step-size methods (Adam) on problems with consistent gradient directions, while adaptive methods outperform momentum on problems with highly variable gradient scales and sparse updates (such as NLP tasks).
\end{itemize}

% ============================================================
\section{Question 3: Newton's Method and Local Approximation \hfill [30 marks]}
\label{sec:q3}
% ============================================================

\subsection{Local Approximations of $g(x) = x^4$}

\textbf{First-order (Taylor) approximation} at $x_0 = 0.25$:
\begin{equation}
g(x) \approx g(x_0) + g'(x_0)(x - x_0) = (0.25)^4 + 4(0.25)^3 (x - 0.25) = 0.0039 + 0.0625(x - 0.25).
\end{equation}
This is a linear approximation (tangent line) that captures only the local slope.

\textbf{Second-order approximation:}
\begin{equation}
g(x) \approx g(x_0) + g'(x_0)(x - x_0) + \tfrac{1}{2}g''(x_0)(x - x_0)^2 = 0.0039 + 0.0625(x - 0.25) + 0.375(x - 0.25)^2.
\end{equation}
This parabolic model captures local curvature. Setting its derivative to zero yields the Newton update:
\begin{equation}
0 = g'(x_0) + g''(x_0)(x - x_0) \implies x_1 = x_0 - \frac{g'(x_0)}{g''(x_0)} = 0.25 - \frac{0.0625}{0.75} = 0.25 - 0.0833 = 0.167.
\end{equation}
This is the Newton step: move to the minimiser of the local quadratic approximation.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_approximation.pdf}
\caption{Q3: Local approximations of $g(x) = x^4$ at $x_0 = 0.25$. The first-order approximation (blue dashed line) captures only the local slope and diverges rapidly from $g(x)$ away from $x_0$. The second-order approximation (red dot-dash parabola) captures local curvature and fits the function well in a neighbourhood. The minimum of the parabola at $x_1 = 0.167$ is a better estimate of the global minimum at $x^\star = 0$ than the first-order gradient step, motivating Newton's method. Note that $g(x) = x^4$ is flatter than quadratic near $x=0$, so quadratic approximation overestimates the curvature, causing Newton to undershoot the true minimum.}
\label{fig:q3_approx}
\end{figure}

\subsection{Newton's Method Applied to All Benchmarks}

\begin{algorithm}[H]
\caption{Newton's Method with Hessian Damping}
\label{alg:newton}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha \in (0,1]$, damping $\lambda > 0$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $H_k \gets \nabla^2 f(x_k) + \lambda I$ \Comment{Tikhonov regularisation for stability}
    \State $p_k \gets H_k^{-1}\, g_k$ \Comment{Newton direction: solves $H_k p_k = g_k$}
    \State $x_{k+1} \gets x_k - \alpha\, p_k$
\EndFor
\end{algorithmic}
\end{algorithm}

The damping term $\lambda I$ (with $\lambda = 10^{-8}$ here) ensures $H_k + \lambda I$ is positive definite even when the Hessian is singular or nearly so, preventing ill-conditioned linear system solves. The step size $\alpha$ is chosen less than 1 for Benchmarks~B and~C where the Hessian changes significantly between iterations.

\begin{remark}[Convergence theory]
Near the optimum $x^\star$ of a strongly convex, twice-differentiable function, Newton's method achieves \emph{quadratic} local convergence:
\begin{equation}
\|x_{k+1} - x^\star\| \leq C \|x_k - x^\star\|^2
\end{equation}
for some constant $C > 0$, meaning the number of correct digits doubles at each iteration. This is dramatically faster than gradient descent's linear convergence $\|x_{k+1} - x^\star\| \leq (1 - \mu/L)\|x_k - x^\star\|$. The trade-off is the $O(d^3)$ cost of solving the linear system $H_k p_k = g_k$ (or $O(d^2)$ with iterative methods), making Newton impractical for large-scale problems without approximation.
\end{remark}

\begin{table}[H]
\centering
\caption{Q3: Hyperparameters for GD (80 iterations) and Newton (20 iterations, damping $\lambda = 10^{-8}$).}
\label{tab:q3_params}
\begin{tabular}{lcccccc}
\toprule
 & \multicolumn{2}{c}{\textbf{Bench.\ A}} & \multicolumn{2}{c}{\textbf{Bench.\ B}} & \multicolumn{2}{c}{\textbf{Bench.\ C}} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5} \cmidrule(lr){6-7}
 & GD & Newton & GD & Newton & GD & Newton \\
\midrule
$\alpha$ & 0.08 & 1.0 & 0.06 & 0.85 & 0.001 & 0.22 \\
Iterations & 80 & 20 & 80 & 20 & 80 & 20 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Convergence Comparison}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_A.pdf}
\caption{Q3: Newton vs GD on Benchmark~A. Newton's method with $\alpha = 1.0$ reaches $J^\star = 0.4826$ in \textbf{one iteration} (up to floating-point precision). This is mathematically expected: for a quadratic $J(\theta) = \frac{1}{2}\theta^T H \theta - b^T\theta + c$, the Newton direction is $p = H^{-1}\nabla J(\theta_0) = H^{-1}(H\theta_0 - b)$, and one step gives $\theta_1 = \theta_0 - H^{-1}(H\theta_0 - b) = H^{-1}b = \theta^\star$ exactly. GD requires 80 iterations to reach comparable accuracy. Note: after iteration 1, Newton's curve is flat at the minimum.}
\label{fig:q3_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_B.pdf}
\caption{Q3: Newton vs GD on Benchmark~B. Newton converges within a few iterations. The near-quadratic structure (the Hessian $H = \text{diag}(2-\sin(x_1), 10)$ varies slowly near the optimum) means the second-order Taylor model is accurate, and Newton steps are near-optimal. Final value: Newton $f = 0.7244$ in 20 iterations vs GD $f = 0.7244$ in 80 iterations --- Newton achieves the same accuracy in $4\times$ fewer iterations.}
\label{fig:q3_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_C.pdf}
\caption{Q3: Newton vs GD on Benchmark~C. Newton with damped step $\alpha = 0.22$ reaches $f = 0.686$ in 20 iterations, dramatically outperforming GD's $f = 3.732$ in 80 iterations. The reduced step size ($\alpha = 0.22 \ll 1$) is essential: the Rosenbrock Hessian $H(x) = \begin{bsmallmatrix}2 + 1200x_1^2 - 400x_2 & -400x_1 \\ -400x_1 & 200\end{bsmallmatrix}$ changes rapidly between iterates, so the quadratic model becomes inaccurate away from the expansion point, requiring a conservative step. Even with damping, Newton navigates the curved valley far more effectively than GD by exploiting curvature information.}
\label{fig:q3_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q3: Final objective values and converged positions (Newton uses 20 iterations, GD uses 80).}
\label{tab:q3_final}
\begin{tabular}{lccc}
\toprule
\textbf{Benchmark} & \textbf{GD $f$ (80 iter.)} & \textbf{Newton $f$ (20 iter.)} & \textbf{Newton final $x$} \\
\midrule
A & 0.4826 & 0.4826 & $(3.028, 4.017)$ \\
B & 0.7244 & 0.7244 & $(0.582, 2.000)$ \\
C & 3.7319 & 0.6864 & $(0.183, 0.020)$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{remark}[Newton on Rosenbrock]
For Benchmark~C, Newton reaches $(0.183, 0.020)$ after 20 iterations, still far from the optimum $(1,1)$. This reflects two challenges: (1) far from the optimum, the quadratic Taylor model is a poor approximation to the highly nonlinear Rosenbrock, causing Newton to take steps that are not aligned with the true descent direction; (2) the damped step $\alpha = 0.22$ further limits per-iteration progress. In practice, Newton's method on Rosenbrock typically requires a line search (Wolfe conditions or backtracking) and either more iterations or a warm start near the optimum to exhibit its quadratic convergence. For large-scale problems, \emph{quasi-Newton} methods such as L-BFGS approximate the inverse Hessian using a rank-2 update (BFGS formula) from gradient differences, achieving superlinear convergence without the $O(d^3)$ cost of exact Hessian inversion.
\end{remark}

\subsection{Convergence Order Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q3_convergence_order.pdf}
\caption{Q3: Convergence analysis on Benchmark~B. Left: distance to optimum $\|x_k - x^\star\|$ vs iteration on log scale. GD decreases linearly (constant slope), while Newton's curve bends down more steeply in later iterations. Right: convergence order plot ($\log \|x_{k+1}-x^\star\|$ vs $\log \|x_k - x^\star\|$) --- GD data points cluster around slope~1 (linear convergence), while Newton's data points follow slope $\approx 2$ once close to the optimum, confirming quadratic convergence $\|x_{k+1}-x^\star\| \approx C\|x_k-x^\star\|^2$.}
\label{fig:q3_conv_order}
\end{figure}

\subsection{Contour Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q3_contour_B.pdf}
\caption{Q3: Newton vs GD trajectories on Benchmark~B. Newton takes large, curvature-corrected steps directly towards the minimum, reaching it in approximately 5 iterations. The Hessian at the starting point $x_0 = (-1, 4)$ has eigenvalues $[2-\sin(-1), 10] \approx [2.841, 10]$, allowing accurate step scaling. GD's trajectory follows the anisotropic gradient direction, with many small steps required to reach the minimum.}
\label{fig:q3_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q3_contour_C.pdf}
\caption{Q3: Newton vs GD trajectories on Rosenbrock. Newton's path closely follows the valley floor by using local curvature to step in the direction of decreasing curvature mismatch. GD ($\alpha = 0.001$) barely moves in 80 iterations, constrained to tiny steps to avoid instability in the direction of large curvature.}
\label{fig:q3_contour_C}
\end{figure}

\subsection{Update Magnitude vs Iteration}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q3_update_magnitude.pdf}
\caption{Q3: Update magnitude $\|x_{k+1} - x_k\| = \|\alpha p_k\|$ for GD and Newton across all benchmarks. \textbf{Benchmark~A}: Newton's update drops to machine precision after one step --- a direct consequence of the quadratic loss where one Newton step achieves exact minimisation. \textbf{Benchmark~B}: Newton's update magnitude decreases rapidly (approximately quadratically on log scale), while GD's magnitude decreases at a constant linear rate. The ratio $\|x_{k+1}-x^\star\| / \|x_k - x^\star\|^2$ remains approximately constant for Newton, confirming quadratic convergence. \textbf{Rosenbrock}: Newton maintains larger update magnitudes than GD because Newton's curvature-corrected direction allows larger productive steps, while GD ($\alpha = 0.001$) is constrained to tiny steps for stability. The slowly-decreasing Newton magnitudes on Rosenbrock reflect the fact that the Hessian approximation degrades rapidly away from the optimum.}
\label{fig:q3_update}
\end{figure}

% ============================================================
\section{Question 4: Derivative Approximation and Derivative-Free Optimisation \hfill [30 marks]}
\label{sec:q4}
% ============================================================

\subsection{Part A: Finite Difference and Nesterov Random Search (Benchmark~B)}

\subsubsection{Finite Difference Gradient Approximation}

The forward finite-difference approximation replaces each partial derivative with:
\begin{equation}
\frac{\partial f}{\partial x_i}(x) \approx \frac{f(x + \delta e_i) - f(x)}{\delta}.
\end{equation}
The truncation error is $O(\delta)$ (from the Taylor expansion remainder), while numerical round-off error is $O(\varepsilon_{\text{machine}}/\delta)$. Balancing these gives an optimal step $\delta^\star = \sqrt{\varepsilon_{\text{machine}}} \approx 10^{-8}$ for double precision. The assignment tests $\delta = 0.05$ (moderate, close to optimal region) and $\delta = 0.8$ (large, dominated by truncation error).

The centred finite-difference formula $\frac{f(x + \delta e_i) - f(x - \delta e_i)}{2\delta}$ gives $O(\delta^2)$ truncation error at double the function evaluation cost; however, the forward difference is used here as specified. Figure~\ref{fig:q4_fd_error} illustrates this trade-off empirically.

\subsubsection{Nesterov Random Search}

\begin{algorithm}[H]
\caption{Nesterov Random Search (Zeroth-Order)}
\label{alg:nrs}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, perturbation $\delta$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State Sample $u_k \sim \mathcal{N}(0, I_d)$, normalise: $u_k \gets u_k / \|u_k\|$ (uniform on sphere)
    \State $\hat{g}_k \gets \dfrac{f(x_k + \delta u_k) - f(x_k)}{\delta}\, u_k$ \Comment{Directional derivative estimate}
    \State $x_{k+1} \gets x_k - \alpha\, \hat{g}_k$
\EndFor
\end{algorithmic}
\end{algorithm}

This zeroth-order method estimates a one-dimensional directional derivative along a random unit vector $u_k$, requiring only \textbf{two function evaluations} per iteration regardless of dimension $d$. The gradient estimator satisfies $\mathbb{E}[\hat{g}_k] = \frac{1}{d}\nabla f_\delta(x_k)$ where $f_\delta$ is a smoothed version of $f$, so Nesterov random search is an asymptotically correct but high-variance gradient estimator in high dimensions. In $d=2$ dimensions (as here), the single random direction samples half the information of a full gradient at 50\% the cost of a forward-difference gradient estimate.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q4_fd_error.pdf}
\caption{Q4A: Finite-difference gradient error $\|\hat{g} - \nabla f\|$ as a function of step size $\delta$ at a test point $x = (0, 3)$. The U-shaped curve reflects the two competing error sources: $O(\delta)$ truncation error (dominant for large $\delta$, shown by the $\delta=0.8$ marker) and $O(\varepsilon_{\rm machine}/\delta)$ round-off error (dominant for tiny $\delta$, shown by the $\delta = \sqrt{\varepsilon_{\rm machine}} \approx 10^{-8}$ marker). The optimal $\delta \approx 10^{-4}$ (minimum of the U-curve) minimises total error. The assignment's $\delta = 0.05$ is in the moderately good range, while $\delta = 0.8$ is dominated by large truncation error.}
\label{fig:q4_fd_error}
\end{figure}

\subsubsection{Results}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q4_fval_B.pdf}
\caption{Q4A: Convergence comparison on Benchmark~B. Exact GD converges fastest to $f = 0.7244$. FD GD with $\delta = 0.05$ closely tracks exact GD (final $f = 0.7280$), demonstrating high gradient fidelity at this step size. FD GD with $\delta = 0.8$ stalls at $f = 1.640$: the large $\delta$ introduces $O(\delta) = O(0.8)$ truncation error, causing the gradient estimate to point in the wrong direction as the curvature varies significantly over a step of that size. Nesterov random search (final $f = 0.726$) converges slowly due to its high-variance, one-dimensional gradient estimate, but does reach a solution close to the minimum given enough iterations.}
\label{fig:q4_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q4_contour_B.pdf}
\caption{Q4A: Trajectories on Benchmark~B. Exact GD and good FD ($\delta = 0.05$) follow nearly identical paths towards the minimum, confirming high gradient fidelity. Poor FD ($\delta = 0.8$) deviates into a suboptimal region due to inaccurate gradient directions caused by truncation error spanning a region of significant curvature change. Nesterov random search follows a noisy, wandering path due to random directional sampling, but gradually converges towards the basin of attraction.}
\label{fig:q4_contour_B}
\end{figure}

\subsection{Part B: Derivative-Free Methods (Benchmark~C)}

\subsubsection{Nelder--Mead Simplex Method}

The Nelder--Mead algorithm maintains a simplex of $n + 1 = 3$ vertices in $\mathbb{R}^2$ and iteratively transforms it using four operations:

\begin{enumerate}[nosep]
  \item \textbf{Reflection} ($\alpha_r = 1.0$): Move the worst vertex $x_{\text{worst}}$ through the centroid $\bar{x}$ of the remaining vertices: $x_r = \bar{x} + \alpha_r(\bar{x} - x_{\text{worst}})$.
  \item \textbf{Expansion} ($\gamma = 2.0$): If reflection improves beyond the current best, attempt a further expansion $x_e = \bar{x} + \gamma(\bar{x} - x_{\text{worst}})$.
  \item \textbf{Contraction} ($\rho = 0.5$): If reflection does not improve sufficiently, contract the simplex.
  \item \textbf{Shrink} ($\sigma = 0.5$): Contract all vertices toward the best vertex when contraction also fails.
\end{enumerate}

No gradient information is required at any step — only function evaluations at the simplex vertices.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q4_nelder_mead_C.pdf}
\caption{Q4B: Nelder--Mead on the Rosenbrock function (160 iterations, step $= 0.35$). Left: the simplex centroid trajectory navigates the curved valley, adapting its shape to the local landscape through reflection, expansion, and contraction. The method successfully locates the narrow valley and progresses along it without any gradient information. Right: the best objective value decreases from the initial $f \approx 4.0$ to a final $f_{\text{best}} \approx 6.8 \times 10^{-3}$, demonstrating effective convergence despite using only function values. The staircase pattern reflects the four operation types: smooth decreases during reflections/expansions, plateaus during contractions/shrinks.}
\label{fig:q4_nm}
\end{figure}

\subsubsection{Grid Search}

Grid search evaluates the objective on a uniform $55 \times 55 = 3025$ grid over $x_1 \in [-2, 2]$, $x_2 \in [-1, 3]$. The grid spacing is $\Delta x_1 = \Delta x_2 = 4/54 \approx 0.0741$. The best grid point found is $(1.037, 1.074)$ with $f = 1.56 \times 10^{-3}$, versus the true optimum $(1, 1)$ with $f^\star = 0$. The approximation error is bounded by the grid resolution: $\|x_{\text{grid}} - x^\star\| \lesssim \sqrt{2} \cdot 0.074 \approx 0.105$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q4_grid_search_C.pdf}
\caption{Q4B: Grid search on Rosenbrock. Left: the 3025 grid points uniformly cover the search region. The best point found (red star) is close to the true optimum (green star) at $(1, 1)$. Right: the best-so-far curve shows step-wise improvement as grid points are scanned in raster order. Grid search guarantees finding the global optimum to within the grid resolution $\approx 0.074$, but requires $O(N^d)$ evaluations, making it computationally prohibitive for $d \geq 4$ or fine resolutions.}
\label{fig:q4_grid}
\end{figure}

\subsection{Computational Comparison}

\begin{table}[H]
\centering
\caption{Q4: Comparison of methods on Benchmark~B and~C. FE/iter = function evaluations per iteration; $d$ = problem dimension.}
\label{tab:q4_comparison}
\begin{tabular}{lp{2.2cm}ccc}
\toprule
\textbf{Method} & \textbf{Gradient Info} & \textbf{FE/iter} & \textbf{Scalability} & \textbf{Best $f$ reached} \\
\midrule
Exact GD & Full gradient & $1 + d$ & Good & 0.7244 (B) \\
FD GD ($\delta = 0.05$) & Forward-diff approx. & $d + 1$ & Moderate & 0.7280 (B) \\
FD GD ($\delta = 0.8$) & Poor approx. & $d + 1$ & Moderate & 1.6402 (B) \\
Nesterov random & None & 2 & \textbf{Best} & 0.7262 (B) \\
Nelder--Mead & None & $\leq 3$ & Low-dim & $0.0068$ (C) \\
Grid search & None & $N^d$ total & Very poor & $0.0016$ (C) \\
\bottomrule
\end{tabular}
\end{table}

The fundamental trade-off between gradient-based and derivative-free methods is: gradient methods require gradient information (which may be unavailable or expensive for black-box functions) but converge with $O(1/k)$ or better rates; derivative-free methods are more general but converge at $O(1/\sqrt{k})$ or slower. Finite-difference approximation bridges this gap, providing gradient-like convergence at moderate additional cost ($d$ extra function evaluations per step), provided $\delta$ is well-chosen.

% ============================================================
\section{Question 5: Constrained Optimisation \hfill [30 marks]}
\label{sec:q5}
% ============================================================

\subsection{Problem Setup}

Minimise Benchmark~B subject to the inequality constraint $x_1 \geq 0.5$, starting from the infeasible initial point $x_0 = (0.2, 4.0)$. The feasible set is the half-space $\mathcal{C} = \{x \in \mathbb{R}^2 : x_1 \geq 0.5\}$.

\begin{remark}[Constraint Activity and KKT Conditions]
An important observation: the unconstrained minimum of Benchmark~B is at $x^\star \approx (0.5824, 2.0)$, which satisfies $x_1^\star \approx 0.582 > 0.5$. Therefore, the constraint $x_1 \geq 0.5$ is \emph{inactive} at the optimum (also called a non-binding constraint). This can be verified via the Karush--Kuhn--Tucker (KKT) conditions for the constrained problem: $\min f(x)$ s.t.\ $g(x) = 0.5 - x_1 \leq 0$. The KKT conditions require:
\begin{align}
\nabla f(x^\star) + \mu \nabla g(x^\star) &= 0, \quad \mu \geq 0, \quad \mu g(x^\star) = 0.
\end{align}
Since $\nabla f(x^\star) = [0, 0]^T$ at the unconstrained minimum and $\nabla g = [-1, 0]^T$, the first condition gives $\mu \cdot (-1) = 0$, so $\mu = 0$. The complementary slackness condition $\mu g(x^\star) = 0$ is satisfied with $\mu = 0$ regardless of $g(x^\star)$. Thus the constraint is inactive and the unconstrained and constrained optima coincide. The key practical difference between methods lies in \emph{how they handle the infeasible starting point} $(0.2, 4.0)$ and \emph{how quickly they enforce feasibility along the trajectory}.
\end{remark}

\begin{algorithm}[H]
\caption{Projected Gradient Descent (PGD)}
\label{alg:pgd}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, projection $\Pi_{\mathcal{C}}$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z \gets x_k - \alpha\, \nabla f(x_k)$ \Comment{Unconstrained gradient step}
    \State $x_{k+1} \gets \Pi_{\mathcal{C}}(z)$ \Comment{Project: $(x_1, x_2) \gets (\max(0.5,\, x_1),\, x_2)$}
\EndFor
\end{algorithmic}
\end{algorithm}

The projection onto a half-space $\{x : x_1 \geq 0.5\}$ is trivial: $\Pi_{\mathcal{C}}(z) = (\max(0.5, z_1), z_2)^T$. For more complex constraints (e.g., norm balls or polytopes with many inequalities), projection can require solving a separate optimisation problem.

\begin{algorithm}[H]
\caption{Penalty Method}
\label{alg:penalty}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, penalty weight $\lambda > 0$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $F_\lambda(x) \gets f(x) + \lambda \max(0,\, -x_1 + 0.5)$ \Comment{Penalised objective}
    \State $\nabla F_\lambda(x) \gets \nabla f(x) - \lambda\, \mathbf{1}[x_1 < 0.5]\, e_1$
    \State $x_{k+1} \gets x_k - \alpha\, \nabla F_\lambda(x_k)$
\EndFor
\end{algorithmic}
\end{algorithm}

The penalty approach replaces the hard constraint with a soft penalty in the objective. As $\lambda \to \infty$, the penalty term dominates, forcing the solution to satisfy the constraint. However, very large $\lambda$ makes the penalised objective ill-conditioned (its Hessian acquires large eigenvalues near the constraint boundary), requiring smaller step sizes for stability.

Settings: Projected GD ($\alpha = 0.08$), Penalty with $\lambda \in \{0.15, 1.8, 4.5\}$ ($\alpha = 0.05$ for $\lambda \leq 1.8$; $\alpha = 0.03$ for $\lambda = 4.5$), unconstrained GD ($\alpha = 0.07$). All run for 100 iterations.

\subsection{Results}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q5_fval.pdf}
\caption{Q5: Original objective $f(x)$ vs iteration (note: penalty methods optimise the penalised objective $F_\lambda$, but we plot the raw $f$ for comparison). All methods converge to $f = 0.7244$ at the constrained optimum $(0.582, 2.0)$, which coincides with the unconstrained optimum since the constraint is inactive at the optimum. The main difference is the convergence trajectory: projected GD moves more directly, while penalty methods with small $\lambda$ allow constraint violations early on.}
\label{fig:q5_fval}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q5_contour.pdf}
\caption{Q5: Contour plot with constraint boundary $x_1 = 0.5$ (red dashed line) and infeasible region (shaded). The starting point $(0.2, 4.0)$ is infeasible. Projected GD immediately clips to $x_1 = 0.5$ at the first iteration, then optimises entirely within the feasible region $x_1 \geq 0.5$. Unconstrained GD crosses through the infeasible region (since the gradient at $x_0$ points towards the constrained minimum which lies in the feasible region, GD nonetheless moves through $x_1 < 0.5$ transiently). Penalty methods are progressively steered towards feasibility as $\lambda$ increases.}
\label{fig:q5_contour}
\end{figure}

\subsection{Constraint Violation Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q5_violation.pdf}
\caption{Q5: Constraint violation $\max(0, 0.5 - x_1)$ on logarithmic scale. Left: full 100 iterations. Right: zoomed first 30 iterations. Projected GD achieves \textbf{zero violation from iteration 1} --- the projection $x_1 \leftarrow \max(0.5, x_1)$ is applied after every step, enforcing the constraint as a hard guarantee at every iterate. The penalty method with $\lambda = 4.5$ achieves feasibility within approximately 10 iterations: the strong penalty gradient $(- 4.5 \cdot \mathbf{1}[x_1 < 0.5])$ aggressively pushes $x_1$ above 0.5. With $\lambda = 0.15$, the penalty gradient is too weak and feasibility is enforced slowly.}
\label{fig:q5_violation}
\end{figure}

\subsection{Comparative Discussion}

\begin{table}[H]
\centering
\caption{Q5: Final positions and constraint violations after 100 iterations. All methods reach the same optimal point since the constraint is inactive at the optimum.}
\label{tab:q5_final}
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{Final $x$} & \textbf{$f(x)$} & \textbf{Feasibility enforced by} & \textbf{Violation} \\
\midrule
Unconstrained GD & $(0.582, 2.000)$ & 0.7244 & Never (accidental) & 0.0000 \\
Projected GD & $(0.582, 2.000)$ & 0.7244 & Every iteration & 0.0000 \\
Penalty $\lambda = 0.15$ & $(0.582, 2.000)$ & 0.7244 & Asymptotically & 0.0000 \\
Penalty $\lambda = 1.8$ & $(0.582, 2.000)$ & 0.7244 & $\sim$15 iterations & 0.0000 \\
Penalty $\lambda = 4.5$ & $(0.582, 2.000)$ & 0.7244 & $\sim$10 iterations & 0.0000 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Projected GD} enforces feasibility as a \emph{hard constraint} at every iteration via the projection operator. The cost is requiring a projection oracle, which is trivial for simple constraint sets (box, ball) but can be expensive for polyhedral or non-convex constraints. Its convergence on convex problems is $O(1/k)$ for the suboptimality gap.

\textbf{Penalty methods} convert the constrained problem into an unconstrained one, requiring only gradient computation on the modified objective. The weight $\lambda$ controls the trade-off: small $\lambda$ enforces feasibility slowly, large $\lambda$ enforces it quickly but introduces numerical ill-conditioning requiring smaller step sizes. In the limit $\lambda \to \infty$, penalty methods recover the constrained solution.

\textbf{Unconstrained GD} happens to converge to a feasible point in this problem because the unconstrained minimum $(0.582, 2.0)$ is already in the feasible region. This coincidence should not be expected in general --- when the unconstrained minimum violates constraints (as in Question~6 Boundary case), unconstrained GD diverges from the constrained optimum.

\textbf{When to use which:} For simple constraint sets, projected GD is preferred (hard feasibility, well-understood convergence). For complex constraint sets where projection is expensive, penalty or augmented Lagrangian methods are preferred. The augmented Lagrangian method $F_{\lambda, \mu}(x) = f(x) + \lambda \cdot c(x) + \frac{\mu}{2}c(x)^2$ (not implemented here) combines penalty with a Lagrange multiplier update, achieving feasibility with less ill-conditioning than pure penalty.

% ============================================================
\section{Question 6: Linear Programmes and Frank--Wolfe Algorithm \hfill [30 marks]}
\label{sec:q6}
% ============================================================

\subsection{Feasible Set and Linear Programme}

Feasible set: $\mathcal{X} = \{x \in \mathbb{R}^2 : 0.5 \leq x_1 \leq 5,\ -5 \leq x_2 \leq 10\}$ (a box / bounded polytope).

\textbf{Linear Programme:} For $f(x) = a^T x$ with $a = [1, 2]^T$, the minimum over $\mathcal{X}$ is found by minimising each coordinate independently. Since both components of $a$ are positive, the minimum of $a_i x_i$ over $[l_i, u_i]$ is at the lower bound $x_i = l_i$:
\begin{equation}
x^{\star} = (0.5,\, -5), \qquad f^{\star} = 1 \cdot 0.5 + 2 \cdot (-5) = -9.5.
\end{equation}
This illustrates the \emph{fundamental theorem of linear programming}: the minimum of a linear objective over a bounded convex polytope is always attained at a vertex (extreme point). Since there are finitely many vertices (here: 4 corners of the box), the LP can be solved by vertex enumeration, or more efficiently by the Simplex algorithm or interior-point methods.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q6_lp.pdf}
\caption{Q6-I: Feasible region $\mathcal{X}$ (blue box) and contours of the linear objective $f(x) = x_1 + 2x_2$. The contour lines are parallel with direction $a = [1, 2]^T$; the minimum is attained where the lowest contour line touches the feasible region, which is the lower-left vertex $(0.5, -5)$ with $f^\star = -9.5$.}
\label{fig:q6_lp}
\end{figure}

\subsection{Frank--Wolfe Algorithm}

\begin{algorithm}[H]
\caption{Frank--Wolfe (Conditional Gradient)}
\label{alg:fw}
\begin{algorithmic}[1]
\Require Initial point $x_0 \in \mathcal{X}$, mixing parameter $\beta \in (0,1)$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_k \gets \arg\min_{x \in \mathcal{X}}\, \nabla f(x_k)^T x$ \Comment{Linear minimisation oracle (LMO)}
    \State $x_{k+1} \gets \beta\, x_k + (1 - \beta)\, z_k$ \Comment{Convex combination: $x_{k+1} \in \mathcal{X}$ always}
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{remark}[Key properties of Frank--Wolfe]
  \begin{enumerate}[nosep]
    \item \textbf{Projection-free}: Since $x_{k+1}$ is a convex combination of $x_k \in \mathcal{X}$ and $z_k \in \mathcal{X}$, feasibility is maintained automatically by convexity of $\mathcal{X}$. No projection oracle is needed.
    \item \textbf{Sparse iterates}: Each iterate is a convex combination of previous LMO solutions (vertices of $\mathcal{X}$), naturally generating sparse solutions in applications such as matrix completion or lasso.
    \item \textbf{Convergence}: For smooth convex $f$ with Lipschitz gradient constant $L$ and the standard step-size rule $\gamma_k = 2/(k+2)$ (corresponding to $\beta = k/(k+2)$), $f(x_k) - f^\star \leq 2LC/(k+2)$ where $C = \text{diam}(\mathcal{X})^2$. This is an $O(1/k)$ convergence rate.
    \item \textbf{Fixed $\beta$ variant}: The implementation here uses a fixed $\beta$ (not the optimal decaying schedule), which can cause oscillation near the optimum but may converge faster in early iterations for well-chosen $\beta$.
  \end{enumerate}
\end{remark}

For box constraints, the LP subproblem decomposes per coordinate in $O(d)$ operations:
\begin{equation}
(z_k)_i = \begin{cases} l_i & \text{if } (\nabla f(x_k))_i > 0 \\ u_i & \text{if } (\nabla f(x_k))_i \leq 0 \end{cases}
\end{equation}
This is vastly cheaper than projection onto a general polytope (which requires solving an LP), making Frank--Wolfe particularly attractive for constrained problems over polytopes.

\subsection{Interior Optimum: $f(x) = (x_1 - 1)^2 + (x_2 - 5)^2$}

The unconstrained minimum $(1, 5)$ lies inside $\mathcal{X}$ (since $0.5 \leq 1 \leq 5$ and $-5 \leq 5 \leq 10$). Two fixed mixing parameters are tested: $\beta = 0.90$ (10\% step towards LP vertex per iteration) and $\beta = 0.985$ (1.5\% step).

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q6_fw_convergence.pdf}
\caption{Q6-II: Frank--Wolfe convergence for the interior optimum ($f^\star = 0$ at $(1,5)$). With $\beta = 0.985$ (smaller steps), the final objective ($f = 0.00455$) is lower than with $\beta = 0.90$ ($f = 0.00891$). At first this seems counterintuitive --- larger steps should converge faster. However, with $\beta = 0.90$ the iterates make larger jumps towards LP vertices, causing more oscillation near the optimum as $z_k$ switches between $\mathcal{X}$ vertices due to gradient direction changes. The smaller $\beta = 0.985$ makes gentler, smoother progress.}
\label{fig:q6_fw_conv}
\end{figure}

\subsection{Boundary Optimum: $f(x) = x_1^2 + x_2^2$}

The unconstrained minimum $(0, 0)$ lies outside $\mathcal{X}$ (since $x_1 \geq 0.5$ required). The constrained minimum is at $(0.5, 0)$ on the $x_1$ boundary, with $f^\star = 0.25$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q6_fw_contour.pdf}
\caption{Q6: Frank--Wolfe trajectories. Left: interior optimum --- both $\beta = 0.90$ and $\beta = 0.985$ trajectories converge towards $(1, 5)$ from $x_0 = (1, 1)$. The trajectory exhibits characteristic FW ``zig-zag'' as $z_k$ alternates between vertices. Right: boundary optimum ($f = x_1^2 + x_2^2$, min at $(0.5, 0)$) --- trajectory converges towards the boundary of $\mathcal{X}$, with the iterate naturally approaching $(0.5, 0)$ as the gradient $\nabla f = 2x$ points away from the feasible boundary's lower-left vertex.}
\label{fig:q6_fw_contour}
\end{figure}

\subsection{Evolution of $x_k$ and $z_k$}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q6_evolution.pdf}
\caption{Q6: Evolution of iterates $x_k$ and LP solutions $z_k$ vs iteration. Top-left ($\beta = 0.90$, interior): $x_k$ converges towards $(1, 5)$ quickly in both coordinates, while $z_k$ oscillates between boundary vertices of $\mathcal{X}$ (switching between lower-left and upper-right corners as the gradient direction changes). Top-right ($\beta = 0.985$, interior): similar but slower, smoother convergence. Bottom-left (boundary, $\beta = 0.93$): $x_{1,k}$ converges to $0.5$ and $x_{2,k}$ converges towards $0$; $z_k$ is consistently at the lower-left vertex $(0.5, -5)$ since the gradient always points in that LP direction. Bottom-right: convergence comparison --- the boundary case converges slowly (final $f = 0.364 > f^\star = 0.25$), indicating the fixed $\beta$ schedule slows convergence near the boundary.}
\label{fig:q6_evolution}
\end{figure}

\begin{table}[H]
\centering
\caption{Q6: Final Frank--Wolfe positions and objective values after the specified iterations.}
\label{tab:q6_final}
\begin{tabular}{lcccc}
\toprule
\textbf{Case} & \textbf{Iterations} & \textbf{Final $x$} & \textbf{$f(x)$} & \textbf{$f^{\star}$} \\
\midrule
Interior, $\beta = 0.90$ & 180 & $(1.066, 4.932)$ & 0.00891 & 0.0 \\
Interior, $\beta = 0.985$ & 180 & $(0.997, 5.067)$ & 0.00455 & 0.0 \\
Boundary, $\beta = 0.93$ & 140 & $(0.500, -0.337)$ & 0.3637 & 0.25 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Interior vs Boundary Behaviour}

\textbf{Interior optimum:} Frank--Wolfe converges to the unconstrained minimum as the iterates remain interior to $\mathcal{X}$. The gradient at the optimum $\nabla f(1, 5) = (0, 0)$ vanishes, so $z_k$ can point to any vertex; however, since the iterate is already at the minimum, the convex combination $x_{k+1} = \beta x_k + (1-\beta) z_k$ moves slightly away from $(1,5)$, causing small oscillations bounded by $(1-\beta) \cdot \text{diam}(\mathcal{X})$. This explains why convergence is approximate rather than exact with a fixed $\beta$ schedule.

\textbf{Boundary optimum:} When the unconstrained minimum lies outside $\mathcal{X}$, the gradient at the constrained optimum $(0.5, 0)$ is $\nabla f = (1, 0)$, which is non-zero and points towards decreasing $x_1$. The LMO consistently selects $z_k = (0.5, -5)$ (lower-left vertex), which is the vertex of $\mathcal{X}$ minimising the linear objective $\nabla f^T z$. The iterate is attracted to this vertex but, with fixed $\beta = 0.93$, converges only approximately. The final value $f = 0.364 > f^\star = 0.25$ indicates that the optimal decaying schedule $\gamma_k = 2/(k+2)$ (i.e., $\beta_k = k/(k+2) \to 1$) is needed for exact convergence.

\textbf{Frank--Wolfe (linearisation) gap:} A useful stopping criterion for Frank--Wolfe is the \emph{FW gap} (also called the duality gap):
\begin{equation}
g_k = \nabla f(x_k)^T (x_k - z_k),
\end{equation}
where $z_k$ is the LMO solution. The gap satisfies $g_k \geq f(x_k) - f^\star \geq 0$ for convex $f$, providing a computable upper bound on the suboptimality without knowing $f^\star$. When $g_k = 0$, the iterate $x_k$ already satisfies the first-order optimality condition (no descent direction within $\mathcal{X}$ is available), indicating convergence to the constrained optimum. For the boundary case, the FW gap would remain positive as long as the iterate has not reached $(0.5, 0)$.

\textbf{Advantage of FW over projection:} For the box constraint $\mathcal{X}$, projection is also trivial ($O(d)$). However, for more complex polytopes --- such as the spectrahedron $\{X : X \succeq 0, \text{tr}(X) = 1\}$ in semidefinite programming, or the nuclear norm ball $\{W : \|W\|_* \leq \tau\}$ in low-rank matrix optimisation --- the LMO requires only a leading singular vector computation ($O(d^2)$) versus a full eigendecomposition for projection ($O(d^3)$). This computational advantage is the primary motivation for Frank--Wolfe in large-scale structured optimisation problems.

% ============================================================
\section{Conclusion}
\label{sec:conclusion}
% ============================================================

\subsection{Summary of Findings}

This study systematically applied and analysed optimisation algorithms spanning six major categories. Figure~\ref{fig:all_rosenbrock} provides a unified view of all methods on the most challenging benchmark, Rosenbrock, highlighting the dramatic performance differences. Table~\ref{tab:summary} summarises the key performance metrics across all benchmarks.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{figures/all_methods_rosenbrock.pdf}
\caption{Unified comparison of all methods tested on the Rosenbrock function (all using 120 iterations, except Newton which uses only 20). The Rosenbrock function is the most discriminating benchmark: well-adapted methods (Polyak, Nesterov) reduce $f$ by two to three orders of magnitude, while gradient descent and RMSprop with constrained step sizes barely improve from the initial $f \approx 5.0$. Newton achieves the lowest final value in just 20 iterations. This figure summarises the central theme: the choice of optimisation algorithm matters enormously when the problem geometry is challenging.}
\label{fig:all_rosenbrock}
\end{figure}

\begin{table}[H]
\centering
\caption{Summary of algorithm performance across benchmarks. Convergence rate refers to theoretical guarantees for convex objectives; practical performance on non-convex benchmarks may differ.}
\label{tab:summary}
\begin{tabular}{lp{3.2cm}p{2.5cm}p{4cm}}
\toprule
\textbf{Algorithm} & \textbf{Convergence Rate} & \textbf{Per-Iter Cost} & \textbf{Key Strengths / Limitations} \\
\midrule
Gradient Descent & $O(1/k)$ convex; $O((1-\mu/L)^k)$ str. cvx & 1 gradient & Simple, reliable; slow for ill-cond. \\
Polyak Step & Linear when $f^\star$ known & 1 gradient & Fast, but requires exact $f^\star$ \\
Adagrad & $O(1/\sqrt{k})$ regret bound & 1 gradient & Stable; step vanishes, premature stopping \\
RMSprop & Heuristic, $O(1/\sqrt{k})$ & 1 gradient & Stable adaptive step; heuristic \\
Heavy Ball & $O((1-1/\sqrt{\kappa})^k)$ quadratic & 1 gradient & Fast for quadratics; can oscillate \\
Nesterov & $O(1/k^2)$ convex & 1 gradient & Optimal first-order; excels on valleys \\
Adam & Heuristic; $O(\sqrt{T})$ regret & 1 gradient & Default in ML; robust hyper-param \\
Newton & Quadratic local; $O(d^3)$ step & 1 Hessian solve & Fastest near opt.; impractical large-$d$ \\
FD GD & Same as GD (degraded quality) & $d+1$ eval & Gradient-free input; accuracy depends on $\delta$ \\
NRS & $O(1/\sqrt{k})$ d-dim case & 2 eval & 2 FE/iter; scales to large $d$ \\
Nelder--Mead & Not guaranteed cvx & $\leq 3$ eval & Practical; no convergence guarantees \\
Grid Search & N/A (exhaustive) & $N^d$ total & Global opt; exponential cost in $d$ \\
Proj.\ GD & $O(1/k)$ cvx constrained & 1 grad + proj & Hard feasibility; needs projection \\
Penalty & $O(1/k)$ (approx) & 1 gradient & No projection; ill-cond. for large $\lambda$ \\
Frank--Wolfe & $O(1/k)$ general cvx & 1 grad + LMO & Projection-free; natural sparsity \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Benchmark-Specific Conclusions}

\textbf{Benchmark~A (well-conditioned quadratic, $\kappa = 1.12$):} Nearly all methods converge effectively due to the near-perfect conditioning. Newton's method achieves exact minimisation in one step, as predicted by theory. The problem is too easy to differentiate methods meaningfully beyond convergence speed. Practical lesson: conditioning is the dominant factor in determining which algorithm is superior.

\textbf{Benchmark~B (moderately ill-conditioned, $\kappa = 6.9$):} The sinusoidal term introduces nonlinearity but does not fundamentally change the landscape near the minimum. Momentum methods (Nesterov, Heavy Ball) and adaptive methods (Adam, Adagrad) all achieve the minimum. The key observation is that all convergent methods reach the same minimum ($f^\star = 0.7244$), so the meaningful comparison is speed and robustness.

\textbf{Benchmark~C (severely ill-conditioned, $\kappa = 2508$):} This benchmark is the most informative discriminator. Gradient descent barely progresses due to the stability constraint on step size. Nesterov's $O(1/k^2)$ rate and momentum accumulation along the valley give a dramatic improvement. Newton's method navigates curvature directly but requires a conservative step size due to the rapidly changing Hessian. Derivative-free Nelder--Mead reaches very good accuracy ($f = 0.0068$) without any gradient information.

\subsection{General Recommendations}

Based on the experimental and theoretical analysis:
\begin{enumerate}[nosep]
  \item Use \textbf{Adam or RMSprop} as the default for deep learning and general non-convex problems with unknown structure.
  \item Use \textbf{Nesterov momentum} when the problem has a known convex or near-convex structure and convergence rate matters.
  \item Use \textbf{Newton's method} (or quasi-Newton variants like L-BFGS) when $d$ is small (up to thousands) and second-order information is affordable.
  \item Use \textbf{finite differences} only when gradients are unavailable and $d$ is small; prefer Nesterov random search for large $d$.
  \item Use \textbf{Nelder--Mead} for low-dimensional, smooth, derivative-free problems where some inaccuracy is tolerable.
  \item Use \textbf{projected gradient descent} for constrained problems with simple, cheap projections; prefer \textbf{Frank--Wolfe} when projection is expensive but the LMO is cheap.

\end{enumerate}

% ============================================================
\newpage
\appendix
\section{Python Code}
\label{app:code}
% ============================================================

The complete implementation is provided in \texttt{CS7DS2\_Final\_Code\_25336453.py}. Key algorithm implementations are reproduced below for reference.

\begin{lstlisting}[caption={Gradient Descent}]
def gradient_descent(grad_fn, loss_fn, x0, alpha, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for _ in range(n_iters):
        x = x - alpha * grad_fn(x)
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Polyak Step Size}]
def polyak_step(grad_fn, loss_fn, x0, f_star, eps, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        alpha_k = (loss_fn(x) - f_star) / (np.dot(g, g) + eps)
        alpha_hist.append(alpha_k)
        x = x - alpha_k * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Adagrad}]
def adagrad(grad_fn, loss_fn, x0, alpha0, eps, n_iters):
    x = x0.copy().astype(float)
    G = np.zeros_like(x)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        G += g**2
        eff_alpha = alpha0 / (np.sqrt(G) + eps)
        alpha_hist.append(np.mean(eff_alpha))
        x = x - eff_alpha * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={RMSprop}]
def rmsprop(grad_fn, loss_fn, x0, alpha0, beta, eps, n_iters):
    x = x0.copy().astype(float)
    v = np.zeros_like(x)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        v = beta * v + (1 - beta) * g**2
        eff_alpha = alpha0 / (np.sqrt(v) + eps)
        alpha_hist.append(np.mean(eff_alpha))
        x = x - eff_alpha * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Heavy Ball / Polyak Momentum}]
def heavy_ball(grad_fn, loss_fn, x0, alpha, beta, n_iters):
    x = x0.copy().astype(float)
    z = np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for _ in range(n_iters):
        g = grad_fn(x)
        z = beta * z + alpha * g
        x = x - z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Nesterov Momentum}]
def nesterov_momentum(grad_fn, loss_fn, x0, alpha, beta_max, n_iters):
    x = x0.copy().astype(float)
    z = np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for k in range(1, n_iters + 1):
        beta_k = min((k - 1) / (k + 2), beta_max)
        lookahead = x + beta_k * z
        g = grad_fn(lookahead)
        z = beta_k * z - alpha * g
        x = x + z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Adam Optimiser}]
def adam_optimiser(grad_fn, loss_fn, x0, alpha, beta1, beta2, eps, n_iters):
    x = x0.copy().astype(float)
    m_vec, v_vec = np.zeros_like(x), np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for t in range(1, n_iters + 1):
        g = grad_fn(x)
        m_vec = beta1 * m_vec + (1 - beta1) * g
        v_vec = beta2 * v_vec + (1 - beta2) * g**2
        m_hat = m_vec / (1 - beta1**t)
        v_hat = v_vec / (1 - beta2**t)
        x = x - alpha * m_hat / (np.sqrt(v_hat) + eps)
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Newton's Method with Hessian Damping}]
def newtons_method(grad_fn, hess_fn, loss_fn, x0, alpha, n_iters, damping=1e-8):
    x = x0.copy().astype(float)
    x_hist, f_hist, update_norms = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        H = hess_fn(x) if callable(hess_fn) else hess_fn
        H_reg = H + damping * np.eye(len(x))
        try:
            p = np.linalg.solve(H_reg, g)
        except np.linalg.LinAlgError:
            p = g
        update_norms.append(np.linalg.norm(alpha * p))
        x = x - alpha * p
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(update_norms)
\end{lstlisting}

\begin{lstlisting}[caption={Frank--Wolfe Algorithm}]
def frank_wolfe(grad_fn, loss_fn, x0, bounds, beta, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist, z_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        z = np.array([bounds[i][0] if g[i] > 0 else bounds[i][1]
                       for i in range(len(g))])
        z_hist.append(z.copy())
        x = beta * x + (1 - beta) * z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(z_hist)
\end{lstlisting}

\end{document}
