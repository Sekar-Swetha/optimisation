\documentclass[11pt,a4paper]{article}
\usepackage[margin=2.2cm]{geometry}
\usepackage{graphicx}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{float}
\usepackage{hyperref}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{listings}
\usepackage{fancyhdr}
\usepackage{microtype}
\usepackage{siunitx}

\hypersetup{colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue}

% Code listing style
\lstset{
  language=Python,
  basicstyle=\ttfamily\scriptsize,
  keywordstyle=\color{blue},
  commentstyle=\color{gray},
  stringstyle=\color{red},
  numbers=left,
  numberstyle=\tiny\color{gray},
  breaklines=true,
  frame=single,
  captionpos=b
}

\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small CS7DS2 -- Final Assignment}
\fancyhead[R]{\small Swetha Sekar (25336453)}
\fancyfoot[C]{\thepage}

% Theorem-like environments
\newtheorem{remark}{Remark}[section]
\newtheorem{proposition}{Proposition}[section]

\title{%
  \includegraphics[width=0.25\textwidth]{tcd_logo.jpg}\\[1em]
  \textbf{CS7DS2 -- Optimisation Algorithms for Data Analysis}\\[0.5em]
  \Large Final Assignment Report
}
\author{Swetha Sekar \\ Student ID: 25336453 \\ School of Computer Science and Statistics \\ Trinity College Dublin}
\date{April 2026}

\begin{document}
\maketitle
\thispagestyle{empty}

\begin{abstract}
This report presents a systematic comparative study of twelve optimisation algorithms applied to three benchmark problems: a linear regression quadratic loss (Benchmark A, condition number $\kappa \approx 1.12$), a toy neural network loss with sinusoidal perturbation (Benchmark B, $\kappa$ ranging from 3.5 to 8.6), and the Rosenbrock function (Benchmark C, $\kappa \approx 2508$). Six categories of methods are investigated across six questions: (Q1) adaptive step-size methods (Polyak, Adagrad, RMSprop, Heavy Ball), (Q2) momentum-based acceleration (Nesterov) and Adam, together with mini-batch stochastic gradient descent; (Q3) Newton's method using local quadratic approximations; (Q4) derivative-free approaches including finite differences, Nesterov random search, Nelder--Mead simplex, and grid search; (Q5) constrained optimisation via projected gradient descent and penalty methods; and (Q6) the Frank--Wolfe conditional gradient algorithm for linearly-constrained problems. Key findings are: Polyak step size achieves the fastest convergence when the optimal value is known exactly (Benchmark C, $f = 0.009$ in 120 iterations), but diverges catastrophically when it is not; Newton's method requires only 1--5 iterations on nearly-quadratic functions compared to 80+ for gradient descent; projected gradient descent enforces feasibility exactly at every iteration while penalty methods require $\lambda \geq 4.5$ to achieve comparable constraint satisfaction; and Frank--Wolfe converges towards interior optima at a rate $O(1/k)$ without any projection oracle.
\end{abstract}

\tableofcontents
\newpage

% ============================================================
\section{Benchmark Problems and Experimental Setup}
\label{sec:benchmarks}
% ============================================================

All experiments use a fixed random seed (\texttt{np.random.seed(42)}) for full reproducibility.

\subsection{Benchmark A: Linear Regression Quadratic Loss}

A synthetic dataset with $m = 1000$ samples and two parameters is generated:
\begin{equation}
\theta^{\star} = \begin{bmatrix} 3 \\ 4 \end{bmatrix}, \quad x^{(i)} \in \mathbb{R}^2 \sim \mathcal{N}(0, I), \quad y^{(i)} = \theta^{\star T} x^{(i)} + \varepsilon^{(i)}, \quad \varepsilon^{(i)} \sim \mathcal{N}(0, 1).
\end{equation}
The loss function is the mean squared error:
\begin{equation}
J(\theta) = \frac{1}{2m} \sum_{i=1}^{m} \left(\theta^T x^{(i)} - y^{(i)}\right)^2 = \frac{1}{2}\theta^T H\theta - b^T\theta + c,
\end{equation}
where $H = \frac{1}{m}X^T X$, $b = \frac{1}{m}X^T y$. This is a \emph{strongly convex quadratic}. The gradient is $\nabla J(\theta) = H\theta - b = \frac{1}{m}X^T(X\theta - y)$, and the Hessian $H = \frac{1}{m}X^T X$ is constant (independent of $\theta$).

Since $x^{(i)} \sim \mathcal{N}(0, I)$, by the law of large numbers $H \approx I$ for large $m$, giving eigenvalues $\lambda_{\min} \approx 0.925$, $\lambda_{\max} \approx 1.033$ and condition number $\kappa(H) \approx 1.12$. This near-uniform curvature means the loss surface is nearly circular, so gradient descent converges rapidly regardless of step size. The OLS minimiser is $\theta^{\star}_{\text{OLS}} = H^{-1}b \approx (3.028, 4.017)$, and the irreducible noise floor is $J^{\star} \approx 0.4826$ (due to observation noise $\sigma=1$). Starting point: $\theta_0 = [0, 0]^T$.

\subsection{Benchmark B: Toy Neural Network Quadratic Loss}
\begin{equation}
f(x_1, x_2) = (x_1 - 1)^2 + 5(x_2 - 2)^2 + \sin(x_1).
\end{equation}
The gradient is $\nabla f = [2(x_1 - 1) + \cos(x_1),\ 10(x_2 - 2)]^T$ and the Hessian is $H(x) = \operatorname{diag}(2 - \sin(x_1),\ 10)$. Since $\sin(x_1) \in [-1, 1]$, the diagonal elements are $h_{11}(x_1) \in [1, 3]$ and $h_{22} = 10$ (constant). The condition number therefore ranges from $10/3 \approx 3.3$ (when $\sin(x_1) = 1$) to $10/1 = 10$ (when $\sin(x_1) = -1$). Near the minimum ($x_1^* \approx 0.582$), $h_{11} \approx 1.45$, giving $\kappa \approx 6.9$.

The minimum satisfies $x_2^* = 2$ and $2(x_1^*-1)+\cos(x_1^*)=0$, yielding (by Newton's method) $x_1^* \approx 0.5824$ with $f^* \approx 0.7244$. The sinusoidal perturbation makes this function non-convex but unimodal near the minimum. Starting point: $x_0 = [-1, 4]^T$.

\subsection{Benchmark C: Rosenbrock Function}
\begin{equation}
f(x_1, x_2) = (1 - x_1)^2 + 100(x_2 - x_1^2)^2.
\end{equation}
The global minimum is at $(1, 1)$ with $f^{\star} = 0$. The Hessian at any point is:
\begin{equation}
H(x) = \begin{bmatrix} 2 + 1200x_1^2 - 400x_2 & -400x_1 \\ -400x_1 & 200 \end{bmatrix}.
\end{equation}
At the optimum $(1,1)$: $H = \begin{bmatrix} 802 & -400 \\ -400 & 200 \end{bmatrix}$ with eigenvalues $\lambda_{\min} \approx 0.399$ and $\lambda_{\max} \approx 1001.6$, giving condition number $\kappa \approx 2508$. This extreme ill-conditioning arises from the interplay between the steep $x_1$-direction gradient and the gentle valley floor. Combined with the non-convex, narrow curved valley along $x_2 \approx x_1^2$, Rosenbrock is one of the most challenging standard test functions. Starting point: $x_0 = [-1, 1]^T$.

\subsection{Summary of Benchmark Properties}

Table~\ref{tab:benchmarks} summarises the key geometric properties of the three benchmarks.

\begin{table}[H]
\centering
\caption{Benchmark properties. $L = \lambda_{\max}(\nabla^2 f)$, $\mu = \lambda_{\min}(\nabla^2 f)$ (at/near the minimum). For C, values are at the minimum $(1,1)$.}
\label{tab:benchmarks}
\begin{tabular}{lcccccc}
\toprule
\textbf{Benchmark} & \textbf{Convex?} & \textbf{$\mu$} & \textbf{$L$} & \textbf{$\kappa = L/\mu$} & \textbf{$f^{\star}$} & \textbf{GD $\alpha_{\max}$} \\
\midrule
A (Linear Reg.) & Yes (strongly) & $\approx 0.925$ & $\approx 1.033$ & $\approx 1.12$ & $\approx 0.4826$ & $\approx 0.97$ \\
B (Toy NN) & Locally & $\approx 1.45$ & $\approx 10$ & $\approx 6.9$ & $\approx 0.7244$ & $\approx 0.09$ \\
C (Rosenbrock) & No & $\approx 0.399$ & $\approx 1001.6$ & $\approx 2508$ & $0$ & $\approx 0.002$ \\
\bottomrule
\end{tabular}
\end{table}

The maximum stable GD step size is $\alpha_{\max} = 2/L$ (e.g., for C: $2/1001.6 \approx 0.002$, consistent with the used $\alpha = 0.001$). The near-unit condition number of Benchmark A ($\kappa \approx 1.12$) explains why all first-order methods perform similarly on it.

\subsection{Convergence Complexity Reference}

Table~\ref{tab:complexity} summarises the worst-case iteration counts for each algorithm class to reach $\epsilon$-suboptimality $f(x_k)-f^{\star} \leq \epsilon$. The formulas use $R = \|x_0 - x^{\star}\|$ and $L$, $\mu$ as above.

\begin{table}[H]
\centering
\caption{Theoretical iterations to achieve $f(x_k)-f^{\star} \leq \epsilon$ for each method class and benchmark. Numbers shown for $\epsilon = 10^{-4}$. ``---'' means the method makes no guarantee. GD $\leftrightarrow$ Nesterov speedup factor: $\sqrt{\kappa}$.}
\label{tab:complexity}
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{Rate} & \textbf{A ($\kappa=1.12$)} & \textbf{B ($\kappa=6.9$)} & \textbf{C ($\kappa=2508$)} \\
\midrule
GD & $O(\kappa\ln(1/\epsilon))$ & $\approx 10$ & $\approx 64$ & $\approx 23\,000$ \\
Nesterov & $O(\sqrt{\kappa}\ln(1/\epsilon))$ & $\approx 10$ & $\approx 24$ & $\approx 459$ \\
Heavy Ball (optimal, quadratic) & $O(\sqrt{\kappa}\ln(1/\epsilon))$ & $\approx 10$ & $\approx 24$ & $\approx 459$ \\
Newton (quadratic) & $O(\log\log(1/\epsilon))$ & $\approx 4$ & $\approx 5$ & $\approx 6$ \\
Newton (nonlinear, damped) & $O(\kappa \ln(1/\epsilon))$ then $O(\log\log(1/\epsilon))$ & --- & --- & --- \\
Frank--Wolfe & $O(LD^2/\epsilon)$ & --- & --- & $\approx 981\,000$ \\
\bottomrule
\end{tabular}
\end{table}

The $\sqrt{\kappa}$ vs $\kappa$ gap between Nesterov and GD is immaterial on Benchmark A ($\sqrt{1.12} \approx 1.06$ vs $1.12$) but dominates on Benchmark C ($\sqrt{2508} \approx 50$ vs $2508$): Nesterov is theoretically $50\times$ more efficient. Newton's quadratic convergence means that $\log\log(1/\epsilon)$ grows very slowly: reaching $\epsilon = 10^{-100}$ requires only $\approx 9$ iterations (assuming quadratic convergence kicks in from iteration 1). Frank--Wolfe's $O(1/\epsilon)$ sublinear rate makes it slow for high-accuracy solutions but acceptable for moderate accuracy.

\subsection{Benchmark D: Constrained Feasible Set}

For constrained problems (Questions 5 and 6): $\mathcal{X} = \{x \in \mathbb{R}^2 : 0.5 \leq x_1 \leq 5,\ -5 \leq x_2 \leq 10\}$, a convex box polytope with 4 vertices and diameter $D = \sqrt{4.5^2 + 15^2} \approx 15.66$.

% ============================================================
\section{Question 1: Adaptive Step-Size Methods \hfill [30 marks]}
\label{sec:q1}
% ============================================================

\subsection{Algorithm Formulations}

Four adaptive methods are compared against a constant step-size gradient descent baseline across all three benchmarks for 120 iterations. The key motivation is that a fixed step size $\alpha$ for gradient descent must satisfy $\alpha < 2/L$ (where $L = \lambda_{\max}(H)$ is the Lipschitz constant of $\nabla f$) for convergence, and the optimal rate requires $\alpha = 2/(\mu + L)$. For Benchmark C, $L$ is large and $\mu$ is tiny ($\kappa \approx 2508$), forcing an impractically small fixed step. Adaptive methods address this by \emph{rescaling} the step per coordinate or per iteration.

\begin{algorithm}[H]
\caption{Polyak Step Size}
\label{alg:polyak}
\begin{algorithmic}[1]
\Require Initial point $x_0$, optimal value $f^{\star}$, tolerance $\varepsilon$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $\alpha_k \gets \dfrac{f(x_k) - f^{\star}}{\|g_k\|^2 + \varepsilon}$  \Comment{Oracle step exploiting $f^{\star}$}
    \State $x_{k+1} \gets x_k - \alpha_k\, g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

The Polyak step is theoretically motivated by minimising the upper bound on $\|x_{k+1} - x^{\star}\|^2$ when $f$ satisfies the Polyak--{\L}ojasiewicz (PL) condition $\|\nabla f(x)\|^2 \geq 2\mu(f(x) - f^{\star})$. Under this condition, the step size $\alpha_k = (f(x_k)-f^{\star})/\|g_k\|^2$ satisfies $f(x_{k+1}) - f^{\star} \leq (1 - \mu/L)(f(x_k) - f^{\star})$ when $f$ is $L$-smooth.

\begin{algorithm}[H]
\caption{Adagrad}
\label{alg:adagrad}
\begin{algorithmic}[1]
\Require Initial point $x_0$, base learning rate $\alpha_0$, tolerance $\varepsilon$, iterations $N$
\State $G_0 \gets \mathbf{0} \in \mathbb{R}^d$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $G_{k+1} \gets G_k + g_k \odot g_k$ \Comment{Coordinate-wise cumulative squared gradient}
    \State $x_{k+1} \gets x_k - \dfrac{\alpha_0}{\sqrt{G_{k+1}} + \varepsilon} \odot g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

Adagrad adapts the step size \emph{per coordinate}: coordinates with historically large gradients receive smaller updates, and sparse coordinates receive larger updates. The effective learning rate for coordinate $i$ is $\alpha_0/\sqrt{G_{k,i}+\varepsilon}$, which decays monotonically as $G_{k,i}$ accumulates. This ensures convergence but causes the learning rate to become vanishingly small for long runs. The theoretical justification is an online learning regret bound: for a convex sequence of functions, Adagrad achieves regret $O(\sqrt{T \sum_i \|g_{1:T,i}\|^2})$ which is never worse than the standard $O(\sqrt{T})$ regret of constant-step GD and can be $O(s\sqrt{T})$ for sparse problems where only $s$ coordinates are active. This adaptivity is key for natural language processing problems where word-frequency gradients are highly sparse.

\begin{algorithm}[H]
\caption{RMSprop}
\label{alg:rmsprop}
\begin{algorithmic}[1]
\Require Initial point $x_0$, learning rate $\alpha_0$, decay $\beta$, tolerance $\varepsilon$, iterations $N$
\State $v_0 \gets \mathbf{0}$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $v_{k+1} \gets \beta\, v_k + (1 - \beta)\, g_k \odot g_k$ \Comment{Exponential moving average of $g^2$}
    \State $x_{k+1} \gets x_k - \dfrac{\alpha_0}{\sqrt{v_{k+1}} + \varepsilon} \odot g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

RMSprop fixes Adagrad's freezing problem by replacing the cumulative sum with an \emph{exponential moving average} of squared gradients (decay parameter $\beta = 0.9$). This allows the effective step size to stabilise rather than decay to zero, making it suitable for non-stationary objectives and long training runs.

\begin{algorithm}[H]
\caption{Polyak Momentum / Heavy Ball}
\label{alg:heavyball}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, momentum $\beta$, iterations $N$
\State $z_0 \gets \mathbf{0}$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_{k+1} \gets \beta\, z_k + \alpha\, \nabla f(x_k)$  \Comment{Accumulated velocity}
    \State $x_{k+1} \gets x_k - z_{k+1}$
\EndFor
\end{algorithmic}
\end{algorithm}

The Heavy Ball method (Polyak, 1964) adds a momentum term that accumulates past gradient directions. For a quadratic with eigenvalues $\mu \leq \lambda \leq L$, the optimal parameters are $\alpha^* = 4/(\sqrt{L}+\sqrt{\mu})^2$ and $\beta^* = ((\sqrt{L}-\sqrt{\mu})/(\sqrt{L}+\sqrt{\mu}))^2$, achieving a convergence rate $(({\sqrt{\kappa}-1})/({\sqrt{\kappa}+1}))^k$ --- an improvement over GD's $(({\kappa-1})/({\kappa+1}))^k$ rate by effectively taking a square root of the condition number.

\subsection{Hyperparameter Settings}

\begin{table}[H]
\centering
\caption{Q1: Hyperparameter settings for each method and benchmark. All methods run for 120 iterations. GD baseline step sizes are chosen as $2/(\lambda_{\min}+\lambda_{\max}) \approx 1/L$ for stability.}
\label{tab:q1_params}
\begin{tabular}{llccc}
\toprule
\textbf{Method} & \textbf{Parameter} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & $\alpha$ & 0.08 & 0.06 & 0.0012 \\
\midrule
Polyak & $f^{\star}$ & 0 & 0 & 0 \\
 & $\varepsilon$ & $10^{-4}$ & $10^{-4}$ & $10^{-3}$ \\
\midrule
Adagrad & $\alpha_0$ & 1.8 & 1.2 & 0.45 \\
 & $\varepsilon$ & $10^{-5}$ & $10^{-5}$ & $10^{-5}$ \\
\midrule
RMSprop & $\alpha_0$ & 0.22 & 0.14 & 0.0035 \\
 & $\beta$ & 0.9 & 0.9 & 0.9 \\
 & $\varepsilon$ & $10^{-5}$ & $10^{-5}$ & $10^{-5}$ \\
\midrule
Heavy Ball & $\alpha$ & 0.045 & 0.035 & 0.0008 \\
 & $\beta$ & 0.88 & 0.90 & 0.86 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Note on Polyak $f^{\star}$ specification:} For Benchmark A the true minimum is $J^{\star} \approx 0.4826$ (not 0), for Benchmark B it is $f^{\star} \approx 0.7244$ (not 0). Only for Benchmark C does $f^{\star} = 0$ hold exactly. This deliberate misspecification enables investigation of Polyak's sensitivity.

\subsection{Convergence Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_A.pdf}
\caption{Q1: Convergence on Benchmark A (Linear Regression, $\kappa \approx 1.12$). GD, Adagrad, and Heavy Ball all converge to the noise floor $J^{\star} \approx 0.4826$. RMSprop converges to a slightly elevated value ($\approx 0.503$) due to oscillation of its adaptive step near the minimum. The Polyak step \textbf{diverges} ($f \approx 2.72$): specifying $f^{\star} = 0$ when the true minimum is $0.4826$ causes the step size $\alpha_k = (f(x_k) - 0)/(\|g_k\|^2 + \varepsilon)$ to grow without bound as $\|g_k\| \to 0$ while $f(x_k) \to 0.4826 \gg 0$. The nearly-isotropic loss surface ($\kappa \approx 1.12$) means even the simple GD baseline performs well here.}
\label{fig:q1_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_B.pdf}
\caption{Q1: Convergence on Benchmark B (Toy Neural Network, $\kappa \in [3.5, 6.9]$). The same $f^{\star}$ misspecification ($f^{\star}=0$ vs true $0.7244$) causes Polyak to diverge badly ($f \approx 24.07$). Adagrad, RMSprop, and Heavy Ball all reach $f \approx 0.7244$. The moderate condition number poses no serious challenge to any correctly-specified method. Heavy Ball with $\beta = 0.90$ converges smoothly via momentum accumulation in the dominant curvature direction.}
\label{fig:q1_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_C.pdf}
\caption{Q1: Convergence on Benchmark C (Rosenbrock, $\kappa \approx 2508$). Here $f^{\star} = 0$ is correct, and Polyak converges most aggressively ($f = 0.0094$). The high condition number makes Rosenbrock the discriminating test: GD reaches only $f = 3.51$ in 120 steps (restricted to $\alpha = 0.0012 < 2/L_{\max}$ for stability), while Polyak adapts its step automatically. Heavy Ball ($f = 1.40$) exploits momentum to traverse the valley faster than GD. Adagrad ($f = 2.20$) benefits from coordinate-wise scaling that partially compensates for anisotropy.}
\label{fig:q1_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q1: Final objective values after 120 iterations. Bold indicates best per benchmark. An asterisk (${}^*$) denotes that $f^{\star}$ was correctly specified for Polyak; elsewhere $f^{\star} = 0$ is misspecified.}
\label{tab:q1_final}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Bench.\ A} ($J^{\star}=0.4826$) & \textbf{Bench.\ B} ($f^{\star}=0.7244$) & \textbf{Bench.\ C} ($f^{\star}=0$) \\
\midrule
GD (baseline) & 0.4826 & 0.7244 & 3.5142 \\
Polyak (misspecified${}^*$ for A,B) & 2.7189 & 24.0659 & \textbf{0.0094}${}^*$ \\
Adagrad & \textbf{0.4826} & \textbf{0.7244} & 2.2034 \\
RMSprop & 0.5027 & 0.7271 & 3.1204 \\
Heavy Ball & \textbf{0.4826} & \textbf{0.7244} & 1.4003 \\
\bottomrule
\end{tabular}
\end{table}

\begin{remark}
The Polyak step size satisfies a minimax optimality property: for any $L$-smooth function satisfying the PL condition with parameter $\mu$, using the exact $f^{\star}$ yields convergence $f(x_k) - f^{\star} \leq (1-\mu/L)^k (f(x_0)-f^{\star})$. When $f^{\star}$ is misspecified (too low), the step size $\alpha_k \to \infty$ near the true minimum, causing the instability observed on Benchmarks A and B.
\end{remark}

\begin{proposition}[Polyak Divergence Under Misspecification]
Let $f$ be $\mu$-strongly convex with unique minimiser $x^{\star}$ and true minimum $c = f(x^{\star}) > 0$. If the Polyak step uses $\tilde{f}^{\star} = 0 < c$, then near the true minimum the step size grows without bound:
\begin{equation}
\alpha_k = \frac{f(x_k)}{\|\nabla f(x_k)\|^2 + \varepsilon} \;\xrightarrow[x_k \to x^{\star}]{}\; \frac{c}{0 + \varepsilon} = \frac{c}{\varepsilon}.
\end{equation}
More precisely, using $\mu\|x-x^{\star}\|^2/2 \leq f(x)-c \leq L\|x-x^{\star}\|^2/2$ and $\mu\|x-x^{\star}\| \leq \|\nabla f(x)\| \leq L\|x-x^{\star}\|$:
\begin{equation}
\alpha_k \geq \frac{c}{\|\nabla f(x_k)\|^2} \geq \frac{c}{L^2\|x_k - x^{\star}\|^2}.
\end{equation}
As the iterate $x_k$ approaches $x^{\star}$, the denominator $\|x_k - x^{\star}\|^2 \to 0$ while the numerator $c > 0$ remains constant, so $\alpha_k \to +\infty$. A step $x_{k+1} = x_k - \alpha_k \nabla f(x_k)$ with $\alpha_k \gg 1/L$ overshoots, launching the iterate far from $x^{\star}$.
\end{proposition}

\begin{remark}[PL Condition vs Strong Convexity]
Strong convexity ($\mu$-SC: $f(x) \geq f(x^{\star}) + \frac{\mu}{2}\|x-x^{\star}\|^2$ for all $x$) implies the Polyak--{\L}ojasiewicz (PL) condition $\|\nabla f(x)\|^2 \geq 2\mu(f(x)-f^{\star})$. However, PL is strictly weaker: it can hold for non-convex functions (e.g., over-parameterised neural networks where every local minimum is global). This distinction matters because PL guarantees linear convergence for GD without the need for strong convexity, making it relevant for modern deep learning theory.
\end{remark}

\subsection{Contour Plots with Optimisation Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q1_contour_B.pdf}
\caption{Q1: Trajectories on Benchmark B (Figure~\ref{fig:q1_fval_B}). The elliptical contours reflect the $\kappa \approx 6.9$ curvature ratio near the minimum: the $x_2$ direction (steeper) converges first. GD follows a smooth monotone path. Heavy Ball overshoots slightly due to accumulated momentum but damps back. The Polyak trajectory diverges dramatically outward, confirming the instability from $f^{\star}$ misspecification --- the growing step sizes propel the iterate far from the minimum.}
\label{fig:q1_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q1_contour_C.pdf}
\caption{Q1: Trajectories on Benchmark C (Rosenbrock). The narrow banana-shaped valley $x_2 \approx x_1^2$ is clearly visible. All methods first descend rapidly toward the valley (the steep $x_2 - x_1^2$ direction), then progress slowly \emph{along} the valley toward $(1,1)$. GD ($\alpha = 0.0012$) barely moves along the valley floor. The correctly-specified Polyak trajectory advances most aggressively. The key challenge is that progress along the valley requires step sizes far larger than those that maintain transverse stability.}
\label{fig:q1_contour_C}
\end{figure}

\subsection{Adaptive Step-Size Evolution}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_A.pdf}
\caption{Q1: Step-size evolution on Benchmark A. The Polyak step (Figure~\ref{fig:q1_fval_A}) grows pathologically: as $\|g_k\| \to 0$ but $f(x_k) \to 0.4826 \gg f^{\star}_{\text{specified}}=0$, the ratio $\alpha_k = f(x_k)/\|g_k\|^2$ diverges. Adagrad decays strictly monotonically at rate $O(1/\sqrt{k})$ as $G_k \sim O(k)$ accumulates. RMSprop stabilises due to exponential forgetting ($\beta = 0.9$): only recent gradient magnitudes influence the denominator, so the effective rate converges to a positive constant once gradients stabilise.}
\label{fig:q1_stepsize_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_B.pdf}
\caption{Q1: Step-size evolution on Benchmark B. The Polyak divergence is even more pronounced (compare with Figure~\ref{fig:q1_fval_B}: $f \to 24$). Adagrad decays rapidly in the first 20 iterations due to large initial gradients from the starting point $x_0 = (-1, 4)$, then settles to a small effective rate. RMSprop maintains a moderately steady effective step, enabling continued progress near the minimum.}
\label{fig:q1_stepsize_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_C.pdf}
\caption{Q1: Step-size evolution on Benchmark C. With the correct $f^{\star} = 0$, the Polyak step decays smoothly to zero as $f(x_k) \to 0$: both numerator and denominator decrease, but the numerator $f(x_k)$ decreases faster, resulting in a well-behaved, monotonically decreasing step. This controlled decay is the geometric reason why Polyak converges best on Rosenbrock when $f^{\star}$ is exact.}
\label{fig:q1_stepsize_C}
\end{figure}

\subsection{Comparative Analysis and Theoretical Connections}

\begin{itemize}[nosep]
\item \textbf{Convergence speed:} Polyak achieves the fastest convergence when $f^{\star}$ is known exactly (Benchmark C: $f = 0.009$ in 120 steps). Adagrad and Heavy Ball match GD baseline on well-conditioned problems (Benchmarks A, B) and substantially improve over GD on ill-conditioned Rosenbrock.
\item \textbf{Adaptivity mechanism:} Adagrad and RMSprop perform \emph{coordinate-wise} scaling, effectively reducing the condition number seen by each parameter. Polyak performs \emph{scalar} step adaptation using global suboptimality information. Heavy Ball adds directional momentum (no explicit coordinate scaling) and achieves an improved rate by replacing $\kappa$ with $\sqrt{\kappa}$ in the convergence exponent.
\item \textbf{Stability:} GD is most stable (step bounded by $1/L$). Adagrad is uniformly stable due to its monotonically non-increasing step (guaranteed $O(\sqrt{T})$ regret for convex problems). RMSprop may oscillate if $\beta$ is too large. Polyak is unstable when $f^{\star}$ is misspecified.
\item \textbf{Effect of condition number:} On Benchmark A ($\kappa \approx 1.12$), GD converges at rate $((\kappa-1)/(\kappa+1))^k \approx 0.057^k$ --- near-geometric convergence requiring only $\sim$10 steps to $10^{-8}$ accuracy. On Rosenbrock ($\kappa \approx 2508$), the GD rate is $\approx 0.9992^k$, requiring $\sim$11500 steps for $10^{-8}$ accuracy. Adaptive methods attack this directly.
\item \textbf{Practical implication:} Since exact $f^{\star}$ is unavailable in practice (e.g., deep learning), RMSprop and Adagrad are preferred. The Polyak step is valuable in theoretical analysis and certain structured problems where $f^{\star} = 0$ (e.g., feasibility problems, exact interpolation).
\end{itemize}

Figure~\ref{fig:summary_q1q2} provides a comprehensive comparison of all Q1 and Q2 methods across all three benchmarks, including the methods introduced in Question 2.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/summary_q1q2_comparison.pdf}
\caption{Summary comparison of all Q1 (120 iterations) and Q2 (150 iterations) methods. Left: heatmap of $\log_{10}(f(x_{\text{final}}) - f^{\star})$ --- darker green indicates closer convergence to the true minimum. Polyak (Q1, with $f^{\star}$ misspecified for A and B) appears bright red due to divergence. Right: bar chart for Benchmark C (Rosenbrock), the most discriminating benchmark. Nesterov achieves the best performance among all first-order methods without an oracle; Polyak (with correct $f^{\star}$) leads on Rosenbrock but fails on A and B.}
\label{fig:summary_q1q2}
\end{figure}

% ============================================================
\section{Question 2: Momentum and Stochastic Methods \hfill [30 marks]}
\label{sec:q2}
% ============================================================

\subsection{Algorithm Formulations}

\begin{algorithm}[H]
\caption{Nesterov Momentum (Accelerated Gradient Descent)}
\label{alg:nesterov}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, max momentum $\beta_{\max}$, iterations $N$
\State $z_0 \gets \mathbf{0}$
\For{$k = 1, 2, \ldots, N$}
    \State $\beta_k \gets \min\!\left(\frac{k-1}{k+2},\ \beta_{\max}\right)$ \Comment{Adaptive schedule: $\beta_k \to 1$ as $k \to \infty$}
    \State $\tilde{x}_k \gets x_k + \beta_k\, z_k$ \Comment{Lookahead / extrapolation point}
    \State $z_{k+1} \gets \beta_k\, z_k - \alpha\, \nabla f(\tilde{x}_k)$
    \State $x_{k+1} \gets x_k + z_{k+1}$
\EndFor
\end{algorithmic}
\end{algorithm}

The key distinction from Heavy Ball is the \emph{lookahead}: the gradient is evaluated at $\tilde{x}_k = x_k + \beta_k z_k$ rather than $x_k$. This ``look-before-you-leap'' correction prevents overshoot: Nesterov anticipates where the momentum is taking the iterate and corrects the trajectory. The adaptive schedule $\beta_k = (k-1)/(k+2)$ (capped at $\beta_{\max}$) increases momentum gradually from 0, achieving the optimal $O(1/k^2)$ convergence rate for smooth convex functions (Nesterov, 1983):
\begin{equation}
f(x_k) - f^{\star} \leq \frac{2L\|x_0 - x^{\star}\|^2}{(k+1)^2} \quad \text{(smooth convex)}.
\end{equation}
This compares favourably to GD's $O(1/k)$ rate and is information-theoretically optimal in the first-order oracle model.

\begin{algorithm}[H]
\caption{Adam Optimiser}
\label{alg:adam}
\begin{algorithmic}[1]
\Require Initial point $x_0$, learning rate $\alpha$, moments $\beta_1, \beta_2$, tolerance $\varepsilon$, iterations $N$
\State $m_0 \gets \mathbf{0},\ v_0 \gets \mathbf{0}$
\For{$t = 1, 2, \ldots, N$}
    \State $g_t \gets \nabla f(x_t)$
    \State $m_t \gets \beta_1\, m_{t-1} + (1 - \beta_1)\, g_t$ \Comment{First-moment (momentum) estimate}
    \State $v_t \gets \beta_2\, v_{t-1} + (1 - \beta_2)\, g_t \odot g_t$ \Comment{Second-moment (variance) estimate}
    \State $\hat{m}_t \gets m_t\, /\, (1 - \beta_1^t)$ \Comment{Bias-corrected first moment}
    \State $\hat{v}_t \gets v_t\, /\, (1 - \beta_2^t)$  \Comment{Bias-corrected second moment}
    \State $x_{t+1} \gets x_t - \alpha\, \hat{m}_t\, /\, (\sqrt{\hat{v}_t} + \varepsilon)$
\EndFor
\end{algorithmic}
\end{algorithm}

Adam (Kingma \& Ba, 2015) combines momentum (via $m_t$, tracking the mean gradient) with per-coordinate adaptive scaling (via $v_t$, tracking gradient variance), inheriting advantages of both momentum and RMSprop. The bias correction terms $1/(1-\beta_1^t)$ and $1/(1-\beta_2^t)$ prevent early iterates from being biased towards zero. To see why, note that $m_t = (1-\beta_1)\sum_{i=1}^{t}\beta_1^{t-i} g_i$, so assuming all gradients equal $g$:
\begin{equation}
\mathbb{E}[m_t] = (1-\beta_1)\sum_{i=1}^{t}\beta_1^{t-i} g = g \cdot (1 - \beta_1^t).
\end{equation}
Thus $\hat{m}_t = m_t/(1-\beta_1^t)$ is an unbiased estimator of $g$ for all $t$. Without bias correction, $\mathbb{E}[m_1] = (1-\beta_1)g$ --- a $\beta_1$-fold underestimate of the true gradient. For $\beta_1 = 0.9$, the first-step estimate is only $10\%$ of the true gradient, causing negligibly small initial steps. The correction is particularly important in the first $\sim 50$ iterations where $\beta_1^t$ is non-negligible.

\textbf{Note on hyperparameters:} The experiments use non-default $\beta_1$ values: $0.82$ (Bench.\ A) and $0.80$ (Bench.\ B, C), compared to the commonly-recommended $\beta_1 = 0.9$. Reduced $\beta_1$ decreases momentum retention, which can improve convergence for deterministic (full-batch) gradients where oscillation near the minimum is undesirable. The default $\beta_1 = 0.9$ is designed for stochastic settings where high momentum helps average out gradient noise; in a full-batch (deterministic) setting, excessive momentum causes over-shooting. The combination $\beta_1 = 0.9, \beta_2 = 0.999$ in the original Adam paper was tuned for stochastic deep learning problems, not deterministic quadratics.

\begin{remark}[Adam Non-Convergence on Non-Convex Problems]
Reddi et al.\ (2018) showed that Adam with the standard fixed $(\beta_1, \beta_2)$ can diverge on simple non-convex problems. The intuition: Adam's exponential moving average of $v_t$ ``forgets'' past gradient information at rate $(1-\beta_2)$. On Rosenbrock, the gradient magnitude varies rapidly along the trajectory (large when far from the valley, near-zero along the valley floor). If a large gradient is quickly forgotten, the step size can increase inappropriately just as the iterate enters a steep region. Nesterov, having no per-coordinate scaling, maintains a more conservative step bounded by $1/L$, and its lookahead correction prevents overshooting the valley walls.
\end{remark}

Figure~\ref{fig:theory_rates} shows the theoretical convergence rates for GD and Nesterov on strongly convex quadratics with the three benchmark condition numbers, illustrating the theoretical advantage of acceleration.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/theory_convergence_rates.pdf}
\caption{Theoretical convergence rates for gradient descent (solid blue) and Nesterov acceleration (dashed red) on a strongly convex quadratic with condition number $\kappa$. The curves plot the upper-bound convergence factor $((\kappa-1)/(\kappa+1))^k$ (GD) and $((\sqrt{\kappa}-1)/(\sqrt{\kappa}+1))^k$ (Nesterov) respectively. Left: Bench.\ A ($\kappa \approx 1.12$) --- both methods converge similarly fast since $\kappa \approx 1$. Centre: Bench.\ B ($\kappa \approx 6.9$) --- Nesterov is noticeably faster. Right: Bench.\ C ($\kappa \approx 2508$) --- Nesterov converges orders of magnitude faster: GD needs $\sim$11500 steps while Nesterov needs only $\sim$220 steps to reduce error by $10^8$. The empirical results on Rosenbrock are broadly consistent with this analysis despite Rosenbrock being non-quadratic.}
\label{fig:theory_rates}
\end{figure}

\subsection{Hyperparameter Settings}

\begin{table}[H]
\centering
\caption{Q2: Hyperparameter settings. Nesterov and Adam run for 150 iterations.}
\label{tab:q2_params}
\begin{tabular}{llccc}
\toprule
\textbf{Method} & \textbf{Parameter} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & $\alpha$ & 0.08 & 0.06 & 0.0012 \\
\midrule
Nesterov & $\alpha$ & 0.06 & 0.035 & 0.0007 \\
 & $\beta_{\max}$ & 0.90 & 0.92 & 0.90 \\
\midrule
Adam & $\alpha$ & 0.12 & 0.08 & 0.006 \\
 & $\beta_1$ & 0.82 & 0.80 & 0.80 \\
 & $\beta_2$ & 0.999 & 0.999 & 0.999 \\
 & $\varepsilon$ & $10^{-8}$ & $10^{-8}$ & $10^{-8}$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Nesterov Momentum Schedule and Convergence Intuition}

Figure~\ref{fig:q2_momentum} illustrates the adaptive momentum schedule used by Nesterov and its effect on convergence, compared against the fixed-momentum Heavy Ball method.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q2_momentum_analysis.pdf}
\caption{Q2: Nesterov momentum analysis. Left: the adaptive schedule $\beta_k = \min((k-1)/(k+2), \beta_{\max})$ starts at $\beta_1 = 0$ (pure gradient step at iteration 1) and increases toward $\beta_{\max}$ as $k$ grows, saturating around $k \approx 29$ for $\beta_{\max}=0.90$. This gradual increase prevents oscillation in early iterations when the gradient direction is unreliable. Right: convergence on the 1D quadratic $f(x) = \frac{1}{2}x^2$, showing that Nesterov with adaptive momentum converges faster than GD and comparably to Heavy Ball, while being more robust to $\beta_{\max}$ tuning.}
\label{fig:q2_momentum}
\end{figure}

\subsection{Convergence Comparison}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_A.pdf}
\caption{Q2: Convergence on Benchmark A (Linear Regression, $\kappa \approx 1.12$). All three methods converge to $J^{\star} \approx 0.4826$. Since the loss surface is nearly circular, the improvement from momentum over GD is marginal: the condition number is already near 1, so Nesterov's $O(1/\sqrt{\kappa})$ advantage is not material. All methods reach the minimum within approximately 50 iterations.}
\label{fig:q2_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_B.pdf}
\caption{Q2: Convergence on Benchmark B ($\kappa \approx 6.9$ near minimum). Both Nesterov and Adam converge to $f^{\star} \approx 0.7244$. Adam's per-coordinate scaling handles the anisotropic curvature ($h_{11} \approx 1.45$ vs $h_{22} = 10$) by taking larger steps in the $x_1$ direction. Nesterov's lookahead gradient prevents the overshoot common with fixed-momentum Heavy Ball. GD converges more slowly due to the condition number constraint.}
\label{fig:q2_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_C.pdf}
\caption{Q2: Convergence on Benchmark C (Rosenbrock, $\kappa \approx 2508$). The discriminating benchmark for momentum methods. Nesterov achieves $f = 0.479$ --- a 7$\times$ improvement over GD ($f = 3.39$) --- by accumulating momentum along the consistent valley-floor direction $(1, x_1^2)$. Adam reaches $f = 1.578$: while its per-coordinate scaling helps compensate for the steep transverse direction, the non-convex valley curvature violates the assumptions underpinning Adam's convergence theory (Reddi et al., 2018 showed Adam can diverge on non-convex problems without step-size decay). The advantage of Nesterov over Adam here arises because the valley-floor direction is consistent across iterations, allowing momentum to build constructively.}
\label{fig:q2_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q2: Final objective values after 150 iterations. Bold indicates best (excluding GD baseline).}
\label{tab:q2_final}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & 0.4826 & 0.7244 & 3.3880 \\
Nesterov & \textbf{0.4826} & \textbf{0.7244} & \textbf{0.4788} \\
Adam & \textbf{0.4826} & \textbf{0.7244} & 1.5785 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Contour Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q2_contour_B.pdf}
\caption{Q2: Trajectories on Benchmark B (see also Figure~\ref{fig:q2_fval_B}). Nesterov's trajectory exhibits the characteristic ``smooth arc'' produced by the increasing momentum schedule, hugging the curvature of the loss surface. Adam takes more direct steps by independently scaling each coordinate. Both converge to the minimum $(0.5824, 2.0)$. GD exhibits a typical zig-zag between the steep ($x_2$) and shallow ($x_1$) curvature directions, though the moderate condition number keeps the oscillation mild.}
\label{fig:q2_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q2_contour_C.pdf}
\caption{Q2: Trajectories on Benchmark C (see also Figure~\ref{fig:q2_fval_C}). All methods reach the valley floor quickly but differ substantially in progress \emph{along} the valley toward $(1,1)$. Nesterov's accumulated momentum becomes aligned with the valley direction after the initial transient, yielding rapid progress. GD is pinned to tiny steps, barely advancing along the valley in 150 iterations.}
\label{fig:q2_contour_C}
\end{figure}

\subsection{Mini-Batch Stochastic Gradient Descent}

Mini-batch SGD on Benchmark A uses learning rate $\alpha = 0.06$, batch sizes $b \in \{5, 40\}$, 50 epochs, with data shuffled at every epoch (dataset size $m = 1000$).

\textbf{Gradient variance analysis:} For a mini-batch $\mathcal{B}$ of size $b$ drawn uniformly without replacement, the variance of the stochastic gradient $\hat{g} = \frac{1}{b}X_\mathcal{B}^T(X_\mathcal{B}\theta - y_\mathcal{B})$ is:
\begin{equation}
\text{Var}(\hat{g}) = \frac{m - b}{b(m-1)} \cdot \text{Var}(g_{\text{full}}),
\end{equation}
where $g_{\text{full}} = \frac{1}{m}X^T(X\theta - y)$ is the full-batch gradient. For $b = 5$: $\text{Var}(\hat{g}) \approx \frac{995}{5 \cdot 999} \approx 0.199 \cdot \text{Var}(g_{\text{full}})$. For $b = 40$: $\text{Var}(\hat{g}) \approx \frac{960}{40 \cdot 999} \approx 0.024 \cdot \text{Var}(g_{\text{full}})$. Thus $b=5$ has approximately $0.199/0.024 \approx 8 \times$ higher gradient variance than $b=40$, explaining the greater oscillation near the minimum in Figure~\ref{fig:q2_sgd_batch}.

\begin{algorithm}[H]
\caption{Mini-Batch SGD}
\label{alg:sgd}
\begin{algorithmic}[1]
\Require Data $(X, y)$, initial $\theta_0$, learning rate $\alpha$, batch size $b$, epochs $E$
\For{$e = 1, 2, \ldots, E$}
    \State Randomly permute indices $\{1, \ldots, m\}$
    \For{each mini-batch $\mathcal{B}$ of size $b$}
        \State $g \gets \frac{1}{b} X_{\mathcal{B}}^T(X_{\mathcal{B}}\theta - y_{\mathcal{B}})$ \Comment{Unbiased stochastic gradient: $\mathbb{E}[g] = \nabla J(\theta)$}
        \State $\theta \gets \theta - \alpha\, g$
    \EndFor
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_sgd_batch.pdf}
\caption{Q2: Mini-batch SGD with batch sizes $b=5$ and $b=40$ (Figure~\ref{fig:q2_sgd_batch}). Per epoch, $b=5$ performs $m/b = 200$ gradient updates while $b=40$ performs only 25 updates. The larger update count per epoch explains why $b=5$ converges faster per epoch: more gradient descent steps are taken. However, $b=5$ exhibits greater oscillation near the minimum due to higher stochastic gradient variance: $\text{Var}(\hat{g}) \propto \sigma^2/b$, so smaller batches produce noisier estimates. Both batch sizes converge to the same irreducible loss $J^{\star} \approx 0.4826$ determined by the observation noise $\sigma=1$.}
\label{fig:q2_sgd_batch}
\end{figure}

\begin{remark}[SGD Noise Floor]
Mini-batch SGD with constant step size $\alpha$ does not converge to $\theta^*$; instead it oscillates in a ball around $\theta^*$ of radius $O(\alpha \sigma^2 / b)$ where $\sigma^2$ is the gradient noise variance. This is the fundamental trade-off in SGD: to decrease the noise floor, one must either (a) decrease $\alpha$ (which slows convergence), or (b) increase batch size $b$ (which increases per-step cost). For a fixed computational budget of $T$ gradient evaluations, the optimal strategy is to use $b = O(\sqrt{T})$ to balance convergence rate with noise reduction. In our experiment, the ``noise floor'' at $J^* \approx 0.4826$ is the irreducible observation-noise loss, not the SGD noise --- for the linear regression objective with Gaussian noise, GD itself cannot go below $J^*$.
\end{remark}

\subsection{SGD with Increased Observation Noise}

The experiment is repeated with observation noise scaled to $\sigma = 6$ (noise factor 6, variance factor 36): $y_{\text{noisy}} = X\theta^{\star} + 6\varepsilon$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q2_sgd_noisy.pdf}
\caption{Q2: Effect of observation noise on SGD convergence. Left: original noise ($\sigma = 1$), reaching $J^{\star} \approx 0.4826$. Right: high noise ($\sigma = 6$), reaching $J^{\star} \approx 0.4826 \times 36 \approx 17.4$ (since $J^{\star} \propto \sigma^2$). Under high noise, the relative advantage of smaller batches diminishes: the irreducible loss (driven by observation noise) dominates the stochastic gradient noise, making both batch sizes converge to the same elevated floor. The gradient variance from mini-batch sampling ($\sigma^2/b$) becomes small relative to the loss landscape curvature, so batch size has a reduced impact on the convergence trajectory.}
\label{fig:q2_sgd_noisy}
\end{figure}

% ============================================================
\section{Question 3: Newton's Method and Local Approximation \hfill [30 marks]}
\label{sec:q3}
% ============================================================

\subsection{Local Approximations of $g(x) = x^4$}

Given $g(x) = x^4$, $g'(x) = 4x^3$, $g''(x) = 12x^2$, the local approximations at $x_0 = 0.25$ are:

\textbf{First-order (Taylor) approximation:}
\begin{equation}
\hat{g}_1(x) = g(x_0) + g'(x_0)(x - x_0) = 0.0039 + 0.0625(x - 0.25).
\end{equation}

\textbf{Second-order (quadratic) approximation:}
\begin{equation}
\hat{g}_2(x) = g(x_0) + g'(x_0)(x - x_0) + \tfrac{1}{2}g''(x_0)(x - x_0)^2 = 0.0039 + 0.0625(x - 0.25) + 0.375(x - 0.25)^2.
\end{equation}

The Newton step minimises $\hat{g}_2$ by setting its derivative to zero:
\begin{equation}
\hat{g}_2'(x) = g'(x_0) + g''(x_0)(x - x_0) = 0 \implies x_{k+1} = x_k - \frac{g'(x_k)}{g''(x_k)}.
\end{equation}
For $g(x) = x^4$: $x_{k+1} = x_k - \frac{4x_k^3}{12x_k^2} = x_k - \frac{x_k}{3} = \frac{2}{3}x_k$. Starting from $x_0 = 0.25$: $x_1 = 0.167$, $x_2 = 0.111$, \ldots, converging to 0 at rate $\|x_k\| = (2/3)^k \|x_0\|$ --- linear convergence. (Newton's method achieves quadratic convergence only near a \emph{nondegenerate} minimum; here $g''(0) = 0$ so the minimum is degenerate, explaining the linear rather than quadratic rate.)

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_approximation.pdf}
\caption{Q3: Local approximations of $g(x) = x^4$ at $x_0 = 0.25$. The first-order approximation (tangent line) captures only the local slope and diverges from $g(x)$ even within a small neighbourhood. The second-order approximation (parabola) fits the curvature and localises the minimum to approximately $\hat{x}^* = 0.25 - g'(0.25)/g''(0.25) = 0.25 - 0.0625/0.375 = 0.0833$. This is the Newton iterate, which overshoots the true minimum $x^* = 0$ but provides a far better initial estimate than the gradient step would give.}
\label{fig:q3_approx}
\end{figure}

\subsection{Newton's Method}

\begin{algorithm}[H]
\caption{Newton's Method with Tikhonov Damping}
\label{alg:newton}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, damping $\lambda$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $H_k \gets \nabla^2 f(x_k) + \lambda I$ \Comment{Tikhonov regularisation for numerical stability}
    \State $p_k \gets H_k^{-1}\, g_k$ \Comment{Newton direction: solve $H_k p_k = g_k$}
    \State $x_{k+1} \gets x_k - \alpha\, p_k$ \Comment{$\alpha = 1.0$ is the pure Newton step}
\EndFor
\end{algorithmic}
\end{algorithm}

Settings: GD uses 80 iterations; Newton uses 20 iterations with Tikhonov damping $\lambda = 10^{-8}$ (ensures $H_k$ is positive definite even when $\nabla^2 f$ is nearly singular). The damped step ($\alpha < 1$) is used for Benchmarks B and C to prevent overshooting due to rapid Hessian variation.

\begin{table}[H]
\centering
\caption{Q3: Hyperparameters for GD and Newton. GD runs 80 iterations; Newton runs only 20 iterations to demonstrate the dramatic efficiency advantage. Note: GD $\alpha = 0.001$ for Benchmark C (slightly smaller than $\alpha = 0.0012$ used in Q1/Q2) was chosen to ensure consistent stability across the full 80 iterations; with $\alpha = 0.0012$, GD occasionally oscillates on Rosenbrock's steep transverse direction. Newton uses $\alpha = 1.0$ (pure Newton step) on A, $\alpha = 0.85$ on B (mild safeguarding), $\alpha = 0.22$ on C (strong damping due to high non-linearity).}
\label{tab:q3_params}
\begin{tabular}{lcccccc}
\toprule
 & \multicolumn{2}{c}{\textbf{Bench.\ A}} & \multicolumn{2}{c}{\textbf{Bench.\ B}} & \multicolumn{2}{c}{\textbf{Bench.\ C}} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5} \cmidrule(lr){6-7}
 & GD & Newton & GD & Newton & GD & Newton \\
\midrule
$\alpha$ & 0.08 & 1.0 & 0.06 & 0.85 & 0.001 & 0.22 \\
Iterations & 80 & 20 & 80 & 20 & 80 & 20 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Convergence Comparison}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_A.pdf}
\caption{Q3: Newton vs GD on Benchmark A (quadratic loss). Newton's method with $\alpha = 1.0$ achieves machine-precision convergence in \textbf{one iteration}. This is the fundamental property of Newton's method on \emph{quadratic} objectives: for $J(\theta) = \frac{1}{2}\theta^T H\theta - b^T\theta$, the Newton direction $p = H^{-1}\nabla J = \theta - H^{-1}b = \theta - \theta^{\star}$ moves directly to the minimum in a single step. The constant Hessian $H = \frac{1}{m}X^TX$ (independent of $\theta$) means the second-order model is globally exact, not merely locally accurate. GD requires 80 iterations to reach comparable accuracy.}
\label{fig:q3_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_B.pdf}
\caption{Q3: Newton vs GD on Benchmark B. Newton converges in $\sim$5 iterations. The Hessian $H(x) = \operatorname{diag}(2-\sin(x_1), 10)$ varies slowly near the minimum, so Newton's local quadratic model remains accurate across steps. The $0.85$ step factor provides mild safeguarding against the modest non-quadratic perturbation from $\sin(x_1)$. Final values: Newton $f = 0.7244$, matching the global minimum in far fewer iterations than GD's 80 steps.}
\label{fig:q3_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_C.pdf}
\caption{Q3: Newton vs GD on Benchmark C. Newton with $\alpha = 0.22$ reaches $f = 0.686$ in 20 iterations vs GD's $f = 3.73$ in 80 iterations. The heavily damped step ($\alpha = 0.22$) is necessary because the Rosenbrock Hessian changes rapidly: full Newton steps ($\alpha = 1$) would overshoot due to the highly curved $x_2 - x_1^2$ ridge. Despite the damping, Newton's curvature information allows it to step efficiently along the valley. After 20 steps, Newton has not yet converged to $(1,1)$ (final position $(0.183, 0.020)$), indicating that the damped step partially defeats the advantage.}
\label{fig:q3_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q3: Final objective values and converged positions. Newton converges to the global minimum on A and B; on C, damping limits progress in 20 steps.}
\label{tab:q3_final}
\begin{tabular}{lcccc}
\toprule
\textbf{Benchmark} & \textbf{GD $f$ (80 iter.)} & \textbf{Newton $f$ (20 iter.)} & \textbf{Newton final $x$} & \textbf{True $x^{\star}$} \\
\midrule
A & 0.4827 & 0.4826 & $(3.028,\ 4.017)$ & $(3.028,\ 4.017)$ \\
B & 0.7244 & 0.7244 & $(0.582,\ 2.000)$ & $(0.5824,\ 2.000)$ \\
C & 3.7319 & 0.6864 & $(0.183,\ 0.020)$ & $(1.000,\ 1.000)$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Contour Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q3_contour_B.pdf}
\caption{Q3: Newton vs GD trajectories on Benchmark B. Newton takes 4--5 large, well-directed steps that account for the anisotropic curvature between $x_1$ (shallow) and $x_2$ (steep). GD takes many small steps and exhibits the characteristic zig-zag oscillation in the $x_2$ direction due to the 6.9:1 curvature ratio, but converges because the condition number is moderate.}
\label{fig:q3_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q3_contour_C.pdf}
\caption{Q3: Newton vs GD trajectories on Rosenbrock. Newton's trajectory closely follows the valley floor, with the Hessian's curvature information directing steps along the low-gradient valley rather than oscillating transversely. GD (with $\alpha = 0.001$) barely advances along the valley in 80 iterations --- the step size required for transverse stability ($\alpha \ll 1/L \approx 0.001$) is far too small to make progress along the gentle valley direction.}
\label{fig:q3_contour_C}
\end{figure}

\subsection{Update Magnitude and Convergence Rate}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q3_update_magnitude.pdf}
\caption{Q3: Update magnitude $\|x_{k+1} - x_k\|$ vs iteration. For Benchmark A, Newton's update drops to machine precision after a single step (below the plot range): one-step convergence on a quadratic. For Benchmark B, Newton's updates decrease superlinearly, consistent with quadratic convergence near the minimum: $\|x_{k+1} - x^{\star}\| \leq C\|x_k - x^{\star}\|^2$. GD's update magnitudes decrease at a linear rate for all benchmarks. On Rosenbrock (C), Newton maintains larger updates than GD, indicating more productive steps per iteration despite the damping.}
\label{fig:q3_update}
\end{figure}

\begin{remark}[Quadratic vs Linear Convergence]
Newton's method achieves \emph{quadratic} convergence near a non-degenerate minimum $x^{\star}$ (where $\nabla^2 f(x^{\star}) \succ 0$):
\begin{equation}
\|x_{k+1} - x^{\star}\| \leq \frac{M}{2\mu}\|x_k - x^{\star}\|^2,
\end{equation}
where $M$ is the Lipschitz constant of $\nabla^2 f$ and $\mu = \lambda_{\min}(\nabla^2 f(x^{\star}))$. This means the number of correct decimal digits \emph{doubles} each iteration. Gradient descent achieves at best linear convergence:
\begin{equation}
\|x_{k+1} - x^{\star}\| \leq \left(1 - \frac{\mu}{L}\right)\|x_k - x^{\star}\|.
\end{equation}
For Benchmark A: $\mu \approx 0.925$, $L \approx 1.033$, so $\mu/L \approx 0.89$ and GD converges in $\sim$50 steps. For Rosenbrock: $\mu \approx 0.399$, $L \approx 1001.6$, so $\mu/L \approx 0.0004$ and GD would require $\sim$57000 steps for $10^{-8}$ accuracy. Newton avoids this issue entirely by rescaling each step by the local curvature $\nabla^2 f(x_k)^{-1}$, which accounts for the extreme anisotropy.
\end{remark}

\begin{remark}[Why $\alpha < 1$ for Rosenbrock Newton]
The Rosenbrock function $f(x) = (1-x_1)^2 + 100(x_2-x_1^2)^2$ has a highly curved Hessian: the third-order tensor $\nabla^3 f$ has large entries (e.g., $\partial^3 f / \partial x_1^3 = 1200 x_1$ up to $\pm 1200$ for $|x_1| \leq 1$). This means the quadratic model $\hat{f}(x + p) \approx f(x) + \nabla f^T p + \frac{1}{2}p^T H p$ becomes inaccurate even for small $\|p\|$. Setting $\alpha = 0.22$ effectively limits the step to a trust region where the quadratic model is still reliable. In practice, this is addressed by line search methods (Armijo backtracking) or trust-region methods that adaptively limit step size without requiring manual tuning.
\end{remark}

\begin{remark}[Newton Convergence Phases on Rosenbrock]
Newton's method has two phases on non-quadratic problems: (1) a \emph{global phase} (large distance from $x^*$) where convergence is linear, analogous to damped gradient descent in the Newton direction; (2) a \emph{local quadratic phase} (sufficiently close to $x^*$) where the quadratic model is accurate and convergence rate becomes $\|x_{k+1}-x^*\| \leq (M/2\mu)\|x_k-x^*\|^2$. The transition occurs roughly when $\|x_k - x^*\| \leq 2\mu/M$ where $M = \sup_x \|\nabla^3 f(x)\|$ (Lipschitz constant of the Hessian). For Rosenbrock with $M \approx 1200$ and $\mu \approx 0.399$, the quadratic-convergence ball has radius $\approx 2(0.399)/1200 \approx 0.00066$. With $\alpha = 0.22$, the damped Newton iterates remain in the global phase throughout 20 iterations from $x_0 = (-1, 1)$, explaining why we observe linear rather than quadratic convergence on Benchmark C.
\end{remark}

% ============================================================
\section{Question 4: Derivative Approximation and Derivative-Free Optimisation \hfill [30 marks]}
\label{sec:q4}
% ============================================================

\subsection{Part A: Finite Difference and Nesterov Random Search (Benchmark B)}

\subsubsection{Finite Difference Gradient Approximation}

The forward finite-difference approximation of the $i$-th partial derivative is:
\begin{equation}
\frac{\partial f}{\partial x_i}(x) \approx \frac{f(x + \delta e_i) - f(x)}{\delta} + O(\delta) + O\!\left(\frac{\varepsilon_{\text{mach}}}{\delta}\right),
\end{equation}
where $e_i$ is the $i$-th standard basis vector. The total error has two components: \emph{truncation error} $O(\delta)$ from the Taylor expansion, and \emph{rounding error} $O(\varepsilon_{\text{mach}}/\delta)$ from floating-point arithmetic. Minimising the sum gives the optimal step $\delta^{\star} = \sqrt{\varepsilon_{\text{mach}}} \approx 10^{-8}$ for double precision ($\varepsilon_{\text{mach}} \approx 2.2 \times 10^{-16}$). The implementation tests $\delta = 0.05$ (moderate, below the function's variation scale) and $\delta = 0.8$ (large, spanning regions of significant curvature change). Cost: $d+1$ function evaluations per gradient.

\begin{remark}[Central Finite Differences]
The \emph{central} finite-difference approximation $\frac{f(x+\delta e_i) - f(x-\delta e_i)}{2\delta}$ achieves $O(\delta^2)$ truncation accuracy (versus $O(\delta)$ for forward FD) because the $O(\delta)$ terms cancel symmetrically. The total error is minimised at $\delta^* = (3\varepsilon_{\text{mach}}/|f'''|)^{1/3} \approx (3 \times 2.2 \times 10^{-16})^{1/3} \approx 10^{-5}$ for double precision, giving a minimum error of $\approx 10^{-10}$ versus $\approx 10^{-8}$ for forward FD. The cost doubles to $2d$ evaluations (since both $+\delta$ and $-\delta$ perturbations are needed), but the improved accuracy often justifies the overhead.
\end{remark}

Figure~\ref{fig:q4_fd_error} shows the empirical FD gradient error vs $\delta$ for Benchmark B, confirming the error floor at $\delta \approx 10^{-8}$ and the truncation-dominated regime for $\delta \gg \delta^*$.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q4_fd_error_analysis.pdf}
\caption{Q4A: Gradient approximation error $\|\hat{g}_{\delta} - \nabla f\|$ vs finite-difference step $\delta$ for Benchmark B at starting point $x_0 = (-1, 4)$. The minimum error (``sweet spot'') occurs near $\delta^* \approx 10^{-8}$ (machine-epsilon). For $\delta < \delta^*$, cancellation errors from floating-point subtraction dominate (rounding error $\propto 1/\delta$). For $\delta > \delta^*$, Taylor truncation error dominates (truncation error $\propto \delta$). The tested values $\delta = 0.05$ (good) and $\delta = 0.8$ (poor) are both in the truncation-dominated regime, but $\delta = 0.05$ is 16$\times$ smaller, giving correspondingly smaller truncation error and better gradient quality.}
\label{fig:q4_fd_error}
\end{figure}

\subsubsection{Nesterov Random Search}

\begin{algorithm}[H]
\caption{Nesterov Random Search (Zeroth-Order)}
\label{alg:nrs}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, perturbation $\delta$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State Sample $u_k \sim \mathcal{N}(0, I_d)$, normalise: $u_k \gets u_k / \|u_k\|$ \Comment{Random unit vector}
    \State $\hat{g}_k \gets \dfrac{f(x_k + \delta u_k) - f(x_k)}{\delta}\, u_k$ \Comment{Directional derivative estimate}
    \State $x_{k+1} \gets x_k - \alpha\, \hat{g}_k$
\EndFor
\end{algorithmic}
\end{algorithm}

This zeroth-order method estimates the gradient via a random one-dimensional probe, requiring only \textbf{2 function evaluations per iteration} regardless of dimension $d$ (compared to $d+1$ for finite differences). The key property is that $\mathbb{E}[d \cdot \hat{g}_k] = \nabla f(x_k)$ (when $u_k$ is a uniform random unit vector), so $\hat{g}_k$ is an unbiased estimator of the gradient \emph{up to a factor of} $1/d$. This dimension-independence in cost per step makes the method attractive for high-dimensional problems, but the gradient estimate has high variance ($O(\sqrt{d})$), which slows convergence.

\subsubsection{Results and Comparison}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q4_fval_B.pdf}
\caption{Q4A: Convergence comparison on Benchmark B (see Table~\ref{tab:q4_comparison} for cost summary). Exact GD converges fastest, as expected. FD GD with $\delta = 0.05$ closely tracks exact GD: the finite-difference step $0.05$ is small relative to the function's characteristic scale (eigenvalue $\sim 10$, so features at scale $\sim 1/\sqrt{10} \approx 0.3$), giving an accurate gradient. FD GD with $\delta = 0.8$ converges poorly: this step spans a region where the sinusoidal term $\sin(x_1)$ changes significantly and where the quadratic curvature of $f$ introduces large truncation error. Nesterov random search converges slowest due to the single-direction, high-variance estimate.}
\label{fig:q4_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q4_contour_B.pdf}
\caption{Q4A: Trajectories on Benchmark B. Exact GD and FD ($\delta = 0.05$) follow nearly identical paths to the minimum $(0.5824, 2.0)$, confirming gradient fidelity. FD ($\delta = 0.8$) deviates: the inaccurate gradient direction causes the trajectory to spiral rather than converge directly. Nesterov random search follows a noisy, wandering path but remains in the vicinity of the minimum, demonstrating that zeroth-order methods can navigate without directional gradient information.}
\label{fig:q4_contour_B}
\end{figure}

\subsection{Part B: Derivative-Free Methods (Benchmark C)}

\subsubsection{Nelder--Mead Simplex Method}

The Nelder--Mead algorithm (Nelder \& Mead, 1965) maintains a simplex of $n+1 = 3$ vertices in $\mathbb{R}^2$ and iteratively reshapes it through four operations:
\begin{enumerate}[nosep]
  \item \textbf{Reflection} ($\alpha_r = 1.0$): move worst vertex $x_{n+1}$ through the centroid $\bar{x}$ of the remaining vertices: $x_r = \bar{x} + \alpha_r(\bar{x} - x_{n+1})$.
  \item \textbf{Expansion} ($\gamma = 2.0$): if reflection improved, try $x_e = \bar{x} + \gamma(\bar{x} - x_{n+1})$ to accelerate progress.
  \item \textbf{Contraction} ($\rho = 0.5$): if reflection worsened, contract toward the centroid.
  \item \textbf{Shrink} ($\sigma = 0.5$): if contraction fails, contract all vertices toward the best.
\end{enumerate}
Settings: initial simplex step $= 0.35$, iterations $= 160$. No gradient information is used.

\begin{remark}[Nelder--Mead as Implicit Curvature Estimator]
Although Nelder--Mead uses no explicit gradient or Hessian, the simplex implicitly encodes directional curvature information: after several reflection/expansion/contraction steps, the simplex tends to elongate along directions of shallow curvature (progress directions) and compress perpendicular to them (steep directions). For Rosenbrock, the simplex aligns itself with the valley axis $\nabla^2 f$'s minimum eigenvector. This geometric adaptation is the reason Nelder--Mead outperforms random search ($O(d)$-evaluations per effective step) on low-dimensional structured problems, despite lacking analytical curvature information.
\end{remark}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q4_nelder_mead_C.pdf}
\caption{Q4B: Nelder--Mead on Rosenbrock. Left: the simplex centroid trajectory navigates the curved valley without gradient information. The simplex contracts and orients itself along the valley floor as it proceeds, demonstrating geometric adaptation to the local loss landscape. Right: the best objective decreases steadily, reaching below $f = 0.01$ within 160 iterations. This is remarkable for a derivative-free method: Nelder--Mead exploits the geometry of the simplex itself as an implicit curvature estimator.}
\label{fig:q4_nm}
\end{figure}

\subsubsection{Grid Search}

Grid search evaluates the objective on a uniform $55 \times 55 = 3025$-point grid over $x_1 \in [-2, 2]$, $x_2 \in [-1, 3]$. Grid resolution: $\Delta x_1 \approx 0.074$, $\Delta x_2 \approx 0.073$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q4_grid_search_C.pdf}
\caption{Q4B: Grid search on Rosenbrock. Left: 3025 uniformly spaced grid points; the best point found (red star) is within one grid cell of the true minimum $(1,1)$. Right: the best-so-far curve improves in discrete steps as better grid points are discovered during the sequential scan. Grid search guarantees a solution within the grid resolution $(\Delta x \approx 0.074)$ of the global optimum --- it is \emph{complete} but not efficient: the required $O(N^d)$ evaluations make it impractical for $d > 3$ or when high resolution is needed.}
\label{fig:q4_grid}
\end{figure}

\subsection{Computational and Algorithmic Comparison}

\begin{table}[H]
\centering
\caption{Q4: Comparison of methods. FE = function evaluations per iteration, $d$ = problem dimension. ``Complete'' means guaranteed to find a solution given sufficient resources.}
\label{tab:q4_comparison}
\begin{tabular}{lccccl}
\toprule
\textbf{Method} & \textbf{Gradient?} & \textbf{FE/iter} & \textbf{Scalability} & \textbf{Complete?} & \textbf{Notes} \\
\midrule
Exact GD & Full ($O(d)$ FLOPS) & $1^{\dagger}$ & $O(d)$ & No & Requires diff'ble $f$ \\
FD GD ($\delta$ small) & Approx., quality $O(\delta)$ & $d+1$ & $O(d)$ & No & $d$ perturbations needed \\
FD GD ($\delta$ large) & Poor approx. & $d+1$ & $O(d)$ & No & High truncation error \\
Nesterov random & None & 2 & $O(1)$ per step & No & High variance \\
Nelder--Mead & None & $\leq n+1 = 3$ & Low-dim only & Heuristic & Adaptive geometry \\
Grid search & None & $N^d$ total & $O(N^d)$ & Yes (within $\Delta x$) & Exact guarantee \\
\bottomrule
\multicolumn{6}{l}{\small ${}^{\dagger}$: one gradient oracle call, which costs $O(d)$ FLOPS but is a single \emph{call}; FD needs $d+1$ separate function evaluations.}
\end{tabular}
\end{table}

\paragraph{Key insight:} There is a fundamental trade-off between gradient information quality and computational cost. Full gradient descent achieves the fastest convergence rate but requires differentiability and $O(d)$ operations per step. Random search uses only 2 function evaluations but has convergence rate $O(1/k)$ at best, versus $O((1-\mu/L)^k)$ for GD. Finite differences bridge this gap: good $\delta$ gives near-exact gradient quality at $d+1$ times the cost, but poor $\delta$ wastes all function evaluations on inaccurate gradient estimates.

% ============================================================
\section{Question 5: Constrained Optimisation \hfill [30 marks]}
\label{sec:q5}
% ============================================================

\subsection{Problem Setup}

Minimise Benchmark B subject to the inequality constraint $x_1 \geq 0.5$, starting from the infeasible point $x_0 = (0.2, 4.0)$. The feasible set is $\mathcal{X} = \{x : x_1 \geq 0.5\}$ (a closed convex half-space).

\textbf{KKT Conditions:} The constrained problem is $\min_{x} f(x)$ s.t.\ $g(x) = 0.5 - x_1 \leq 0$. Under the KKT conditions, a point $x^*$ is a local minimum if there exists a multiplier $\mu^* \geq 0$ such that:
\begin{align}
\nabla f(x^*) + \mu^* \nabla g(x^*) &= 0 \quad \text{(stationarity)}, \label{eq:kkt_stat}\\
g(x^*) &\leq 0 \quad \text{(primal feasibility)}, \label{eq:kkt_pf}\\
\mu^* &\geq 0 \quad \text{(dual feasibility)}, \label{eq:kkt_df}\\
\mu^* g(x^*) &= 0 \quad \text{(complementary slackness)}. \label{eq:kkt_cs}
\end{align}
Since the unconstrained minimiser $x^* = (0.5824, 2.0)$ satisfies $g(x^*) = 0.5 - 0.5824 < 0$ (strictly feasible), complementary slackness forces $\mu^* = 0$, and the KKT conditions reduce to $\nabla f(x^*) = 0$ --- the unconstrained optimality condition. This confirms that the constraint is \emph{inactive} at the optimum: the constrained and unconstrained solutions coincide.

\textbf{Practical implication:} Since $x^* \approx (0.5824, 2.0)$ lies in the interior of $\mathcal{X}$, once any algorithm (projected GD or penalty) has driven its iterate into the feasible region $x_1 \geq 0.5$, it should converge to the same point as unconstrained GD. The penalty with $\lambda = 0.15$ fails precisely because it never fully drives the iterate into the feasible region --- the penalty gradient is too weak to overcome the objective's pull toward the infeasible region.

\textbf{Shadow price interpretation:} The Lagrange multiplier $\mu^*$ has an economic interpretation as the \emph{shadow price} of the constraint. By the envelope theorem, if the constraint is $x_1 \geq c$ (parameterised by $c$), then:
\begin{equation}
\frac{d f^*(c)}{d c} = -\mu^*(c),
\end{equation}
where $f^*(c) = \min_{x_1 \geq c} f(x)$. Here $\mu^* = 0$ because the constraint is inactive: tightening the constraint slightly (e.g., to $x_1 \geq 0.5824 - \epsilon$ for small $\epsilon > 0$) does not change the optimal value. However, if the constraint were $x_1 \geq c$ for $c > x_1^* = 0.5824$, the constraint would become active, $\mu^* > 0$, and increasing $c$ would increase $f^*(c)$ at rate $\mu^*$ per unit. The transition point is $c = 0.5824$: for $c \leq 0.5824$, $f^*(c) = 0.7244$ (constant); for $c > 0.5824$, $f^*(c)$ increases due to constraint binding.

\begin{algorithm}[H]
\caption{Projected Gradient Descent}
\label{alg:pgd}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, projection $\Pi_{\mathcal{X}}$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_{k+1} \gets x_k - \alpha\, \nabla f(x_k)$ \Comment{Unconstrained gradient step}
    \State $x_{k+1} \gets \Pi_{\mathcal{X}}(z_{k+1}) = \begin{bmatrix} \max(0.5, z_1) \\ z_2 \end{bmatrix}$ \Comment{Euclidean projection onto $\mathcal{X}$}
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{algorithm}[H]
\caption{Penalty Method (Gradient Descent on Penalised Objective)}
\label{alg:penalty}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, penalty weight $\lambda$, iterations $N$
\State Define penalised objective: $F_{\lambda}(x) \gets f(x) + \lambda\, \max\bigl(0,\; 0.5 - x_1\bigr)$
\For{$k = 0, 1, \ldots, N-1$}
    \State $\nabla F_{\lambda}(x) \gets \nabla f(x) + \lambda \begin{bmatrix} -\mathbf{1}[x_1 < 0.5] \\ 0 \end{bmatrix}$
    \State $x_{k+1} \gets x_k - \alpha\, \nabla F_{\lambda}(x_k)$
\EndFor
\end{algorithmic}
\end{algorithm}

Settings: Projected GD ($\alpha = 0.08$), penalty method with $\lambda \in \{0.15, 1.8, 4.5\}$ ($\alpha = 0.05$ for $\lambda \leq 1.8$; $\alpha = 0.03$ for $\lambda = 4.5$, reduced to control the steeper penalty gradient), unconstrained GD ($\alpha = 0.07$). All run for 100 iterations.

Figure~\ref{fig:q5_landscape} shows how the penalty term deforms the objective landscape and how the penalty minimum converges to the feasible region as $\lambda$ increases.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q5_penalty_landscape.pdf}
\caption{Q5: Penalty landscape analysis. Left: cross-section of $f(x_1, 2.0)$ and $F_\lambda(x_1, 2.0)$ for $\lambda \in \{0.15, 1.8, 4.5\}$. With $\lambda = 0.15$, the penalty kink at $x_1 = 0.5$ is barely visible and the penalised minimum remains in the infeasible region. With $\lambda = 4.5$, the penalty creates a sharp barrier pushing the minimum well into the feasible region. Right: trajectory of the penalised optimum $x_1^*(\lambda)$ as $\lambda$ increases. As $\lambda \to \infty$, $x_1^*(\lambda) \to 0.5824$ (the constrained optimum), confirming the asymptotic exactness of the penalty approach.}
\label{fig:q5_landscape}
\end{figure}

\subsection{Results}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q5_fval.pdf}
\caption{Q5: Objective value $f(x_k)$ vs iteration. Projected GD and high-penalty methods converge to the constrained minimum $f \approx 0.7244$ at $(0.5824, 2.0)$. The penalty with $\lambda = 0.15$ reaches a higher value, indicating that the penalty is too weak to enforce the constraint and the optimiser settles at an infeasible point with lower $f$ but positive violation. Unconstrained GD converges to $f \approx 0.7244$ but crosses the infeasible region, providing a useful comparison.}
\label{fig:q5_fval}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q5_contour.pdf}
\caption{Q5: Contour plot with constraint boundary $x_1 = 0.5$ (dashed red) and infeasible region $x_1 < 0.5$ (shaded red). Starting from the infeasible point $(0.2, 4.0)$, projected GD immediately clips $x_1$ to $0.5$ at iteration 1. The unconstrained GD path drifts into the infeasible region (below the boundary) and converges to the unconstrained minimum on the constraint boundary. Penalty methods with increasing $\lambda$ exhibit trajectories progressively closer to the feasible region boundary, demonstrating that higher penalties act as stronger barriers to infeasibility.}
\label{fig:q5_contour}
\end{figure}

\subsection{Constraint Violation Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q5_violation.pdf}
\caption{Q5: Constraint violation $v(x_k) = \max(0,\; 0.5 - x_1^{(k)})$ vs iteration (log scale). Left: full 100 iterations. Right: first 30 iterations (zoomed). Projected GD achieves \textbf{zero violation from iteration 1}: the projection $x_1 \leftarrow \max(0.5, x_1)$ is applied after every gradient step, enforcing hard feasibility. Penalty $\lambda = 4.5$ drives violation to zero within $\sim$10 iterations. Penalty $\lambda = 1.8$ reduces violation slowly. Penalty $\lambda = 0.15$ maintains persistent residual violation, as the penalty gradient is insufficient to overcome the objective gradient driving the iterate toward $x_1 < 0.5$.}
\label{fig:q5_violation}
\end{figure}

\subsection{Comparative Discussion}

\begin{table}[H]
\centering
\caption{Q5: Final positions, objective values, and constraint violations after 100 iterations. The constraint violation is $v = \max(0, 0.5 - x_1)$.}
\label{tab:q5_final}
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{Final $x$} & \textbf{$f(x)$} & \textbf{Violation $v$} & \textbf{Feasible?} \\
\midrule
Unconstrained GD & $\approx(0.582, 2.00)$ & 0.7244 & 0.0000 & Yes (inactive constraint) \\
Projected GD & $(0.582, 2.00)$ & 0.7244 & 0.0000 & \textbf{Yes (from iter 1)} \\
Penalty $\lambda = 0.15$ & $\approx(0.40, 2.00)$ & $\approx 0.65$ & $\approx 0.10$ & No \\
Penalty $\lambda = 1.8$ & $\approx(0.54, 2.00)$ & $\approx 0.72$ & $\approx 0.00$ & Approximate \\
Penalty $\lambda = 4.5$ & $\approx(0.583, 2.00)$ & 0.7244 & 0.0000 & Yes \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Projected gradient descent} guarantees hard feasibility at every iteration via the projection oracle. For the constraint $x_1 \geq 0.5$, the projection $\Pi_{\mathcal{X}}(x) = (\max(0.5, x_1), x_2)^T$ has $O(1)$ cost. Under standard assumptions (the objective is $L$-smooth, $\mu$-strongly convex on $\mathcal{X}$), projected GD converges at rate $(1-\mu/L)^k$, the same as unconstrained GD. For more complex constraints (e.g., a convex set defined by a semidefinite constraint), the projection oracle can be expensive ($O(d^3)$ for an SDP), and the penalty method becomes more attractive.

\textbf{Penalty methods} trade exact feasibility for algorithmic simplicity: only a modified gradient $\nabla f + \lambda \partial \max(0, 0.5-x_1)$ is required. The weight $\lambda$ controls the trade-off: $\lambda \to \infty$ recovers the hard-constrained solution (as $F_\lambda \to f$ on the feasible set and $F_\lambda \to \infty$ on the infeasible set), but the ill-conditioning of $F_\lambda$ worsens as $\lambda$ increases, requiring smaller step sizes. This ``penalty barrier'' is why $\alpha$ must decrease from 0.05 ($\lambda \leq 1.8$) to 0.03 ($\lambda = 4.5$): the Lipschitz constant of $\nabla F_\lambda$ is $\approx L + \lambda$, and stability requires $\alpha < 2/(L+\lambda)$.

\textbf{Augmented Lagrangian comparison:} The augmented Lagrangian method defines $\mathcal{L}(x, \mu) = f(x) + \mu \max(0, 0.5-x_1) + \frac{\rho}{2}\max(0, 0.5-x_1)^2$, combining a Lagrange multiplier $\mu$ (updated as $\mu_{k+1} = \mu_k + \rho \max(0, 0.5-x_1^{(k)})$) with a fixed penalty $\rho$. This achieves feasibility at finite $\rho$, avoiding the ill-conditioning that requires $\lambda \to \infty$ in the pure penalty approach.

% ============================================================
\section{Question 6: Linear Programmes and Frank--Wolfe Algorithm \hfill [30 marks]}
\label{sec:q6}
% ============================================================

\subsection{Feasible Set and Linear Programme}

Feasible set: $\mathcal{X} = \{x \in \mathbb{R}^2 : 0.5 \leq x_1 \leq 5,\ -5 \leq x_2 \leq 10\}$ (a convex box polytope with 4 vertices: $(0.5,-5)$, $(5,-5)$, $(5,10)$, $(0.5,10)$).

\textbf{Linear Programme:} For $f(x) = a^T x$ with $a = [1, 2]^T$, the minimum over $\mathcal{X}$ is obtained by coordinate-wise minimisation (since the objective is separable and the feasible set is a box):
\begin{equation}
x_i^{\star} = \begin{cases} \ell_i & \text{if } a_i > 0 \\ u_i & \text{if } a_i < 0 \end{cases}
\implies x^{\star} = (0.5,\; -5), \quad f^{\star} = 1 \cdot 0.5 + 2 \cdot (-5) = -9.5.
\end{equation}
This illustrates the \textbf{fundamental theorem of linear programming}: the optimum of a linear objective over a non-empty bounded polytope $\mathcal{X}$ is attained at a vertex.

\begin{proposition}[LP Optimal at Vertex]\label{prop:lp}
Let $f(x) = a^T x$ (linear) and $\mathcal{X}$ a non-empty bounded polytope. Then $\min_{x \in \mathcal{X}} f(x)$ is attained at a vertex of $\mathcal{X}$.
\end{proposition}
\begin{proof}[Proof sketch]
Suppose $x^* \in \mathcal{X}$ achieves the minimum. If $x^*$ is not a vertex, it lies in the interior of some edge or face, i.e.\ $x^* = \lambda y + (1-\lambda)z$ for some $y, z \in \mathcal{X}$, $y \neq z$, $\lambda \in (0,1)$. By linearity: $f(x^*) = \lambda f(y) + (1-\lambda)f(z)$. Since $x^*$ is optimal, $f(y) \geq f(x^*)$ and $f(z) \geq f(x^*)$; thus $f(y) = f(z) = f(x^*)$. Both $y$ and $z$ are also minimisers. Applying this argument recursively (the polytope has finitely many vertices), we eventually reach a vertex with the same objective value.
\end{proof}

Since $f$ has no curvature, no interior point can be optimal unless it coincides with a boundary optimum.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q6_lp.pdf}
\caption{Q6-I: Feasible region $\mathcal{X}$ (blue box) and linear objective contours $f(x) = x_1 + 2x_2 = c$. The contour lines are parallel with slope $-1/2$; the minimum occurs at the lower-left vertex $(0.5, -5)$ where $f = -9.5$. This vertex minimises both $x_1$ and $x_2$ simultaneously since both coefficients $a_1 = 1, a_2 = 2$ are positive.}
\label{fig:q6_lp}
\end{figure}

\subsection{Frank--Wolfe Algorithm}

\begin{algorithm}[H]
\caption{Frank--Wolfe (Conditional Gradient Method)}
\label{alg:fw}
\begin{algorithmic}[1]
\Require Initial point $x_0 \in \mathcal{X}$, mixing parameter $\beta$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_k \gets \arg\min_{x \in \mathcal{X}}\, \nabla f(x_k)^T x$ \Comment{Linear Minimisation Oracle (LMO)}
    \State $x_{k+1} \gets \beta\, x_k + (1 - \beta)\, z_k$ \Comment{Fixed-step convex combination, $\gamma = 1-\beta$}
\EndFor
\end{algorithmic}
\end{algorithm}

The Frank--Wolfe method (Frank \& Wolfe, 1956) avoids explicit projection by replacing the projection step with a \emph{linear subproblem}: at each iteration, find the vertex $z_k$ of $\mathcal{X}$ that minimises the linearised objective $\nabla f(x_k)^T z$. For a box constraint, the LMO decomposes per coordinate:
\begin{equation}
(z_k)_i = \begin{cases} \ell_i & \text{if } (\nabla f(x_k))_i > 0, \\ u_i & \text{if } (\nabla f(x_k))_i \leq 0. \end{cases}
\end{equation}
The iterate $x_{k+1} = (1-\gamma)x_k + \gamma z_k$ (where $\gamma = 1-\beta$ is the step size toward $z_k$) is a convex combination of $x_k$ and $z_k$, both in $\mathcal{X}$, so \textbf{feasibility is maintained by construction} without any projection oracle.

\textbf{Fixed-step vs decreasing step:} The implementation uses a fixed mixing parameter $\beta$ (i.e., fixed step $\gamma = 1-\beta$), whereas the standard Frank--Wolfe uses the decreasing schedule $\gamma_k = 2/(k+2)$. For smooth convex $f$ with Lipschitz gradient constant $L$, the standard schedule achieves:
\begin{equation}
f(x_k) - f^{\star} \leq \frac{2LD^2}{k+2},
\end{equation}
where $D = \max_{x,y \in \mathcal{X}}\|x-y\|$ is the \emph{diameter} of $\mathcal{X}$. For our box $\mathcal{X} = [0.5, 5] \times [-5, 10]$, the diameter is $D = \sqrt{(5-0.5)^2 + (10-(-5))^2} = \sqrt{4.5^2 + 15^2} = \sqrt{20.25 + 225} = \sqrt{245.25} \approx 15.66$.

For the interior optimum $f_{\text{int}}(x) = (x_1-1)^2 + (x_2-5)^2$, the Hessian is $H = 2I$, so $L = 2$. The convergence bound becomes:
\begin{equation}
f(x_k) - f^{\star} \leq \frac{2 \cdot 2 \cdot 245.25}{k+2} = \frac{981}{k+2}.
\end{equation}
At $k = 180$: the bound gives $f - f^* \leq 5.37$, which is loose (empirical: $f = 0.0045$) because the bound is worst-case and the starting point $(1,1)$ is close to the optimum $(1,5)$.

Fixed-step variants achieve similar rates when $\gamma = 1-\beta$ is appropriately chosen, but may oscillate near the optimum since the step does not decay.

\subsection{Interior Optimum: $f(x) = (x_1 - 1)^2 + (x_2 - 5)^2$}

The unconstrained minimum $(1, 5)$ lies inside $\mathcal{X}$ (since $0.5 \leq 1 \leq 5$ and $-5 \leq 5 \leq 10$). Two mixing parameters are tested: $\beta = 0.90$ (step $\gamma = 0.10$) and $\beta = 0.985$ (step $\gamma = 0.015$). Starting point: $x_0 = (1, 1)$.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q6_fw_convergence.pdf}
\caption{Q6-II: Frank--Wolfe convergence for interior optimum. Both values of $\beta$ converge to near-zero objective, but $\beta = 0.985$ (smaller step $\gamma = 0.015$) achieves $f = 0.0045$ vs $\beta = 0.90$ ($f = 0.0089$). The counterintuitive superiority of smaller steps occurs because larger $\gamma$ causes greater oscillation: $z_k$ jumps between vertices $(0.5, 10)$ and $(0.5, -5)$ as the gradient direction fluctuates near the interior optimum, and the convex average struggles to smooth out these vertex hops. Smaller $\gamma$ damps the effect of each vertex visit, yielding a smoother trajectory.}
\label{fig:q6_fw_conv}
\end{figure}

\subsection{Boundary Optimum: $f(x) = x_1^2 + x_2^2$}

The unconstrained minimum $(0, 0)$ lies outside $\mathcal{X}$ (since $x_1 \geq 0.5 > 0$). The constrained minimum is at the boundary point $(0.5, 0) \in \mathcal{X}$ with $f^{\star} = 0.25$. Starting point: $x_0 = (3, 3)$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q6_fw_contour.pdf}
\caption{Q6: Frank--Wolfe trajectories. Left: interior optimum (Q6-II) --- both $\beta$ values converge toward $(1,5)$ from $(1,1)$, with $\beta=0.90$ (larger steps) showing more erratic traversal. Right: boundary optimum (Q6-III) --- the trajectory converges toward $(0.5, 0)$ as the LMO consistently selects $z_k = (0.5, -5)$ (minimising $2x_1 \cdot x_1 + 2x_2 \cdot x_2$ with $x_2 > 0$) and the convex average gradually shifts the iterate toward the boundary. Frank--Wolfe handles boundary optima naturally, since its LMO selects vertices near the constrained optimum.}
\label{fig:q6_fw_contour}
\end{figure}

\subsection{Evolution of $x_k$ and $z_k$}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q6_evolution.pdf}
\caption{Q6: Evolution of iterates $x_k$ and LP vertices $z_k$. Top-left ($\beta = 0.90$): $x_k$ converges quickly but $z_k$ oscillates between vertices $(0.5, 10)$ and $(0.5, -5)$ as the gradient direction alternates near the interior optimum --- the gradient first points toward the lower vertex, then is corrected upward. Top-right ($\beta = 0.985$): slower, smoother convergence with the same vertex oscillation but reduced impact due to the small $\gamma = 0.015$. Bottom-left (boundary): $x_1$ converges toward 0.5 (the binding constraint), $x_2$ converges toward 0. Bottom-right: convergence comparison --- the boundary case converges to $f = 0.364$ after 140 iterations (above $f^{\star} = 0.25$); additional iterations would be needed for full convergence.}
\label{fig:q6_evolution}
\end{figure}

\begin{table}[H]
\centering
\caption{Q6: Final Frank--Wolfe positions and objective values after the specified iterations. $f^{\star}$ denotes the true constrained minimum.}
\label{tab:q6_final}
\begin{tabular}{lcccc}
\toprule
\textbf{Case} & \textbf{Iters} & \textbf{Final $x$} & \textbf{$f(x_{\text{final}})$} & \textbf{$f^{\star}$} \\
\midrule
Interior, $\beta = 0.90$ & 180 & $(1.066,\ 4.932)$ & 0.0089 & 0.0 \\
Interior, $\beta = 0.985$ & 180 & $(0.997,\ 5.067)$ & 0.0045 & 0.0 \\
Boundary, $\beta = 0.93$ & 140 & $(0.500,\ -0.337)$ & 0.3637 & 0.25 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Interior vs Boundary Behaviour: Theoretical Analysis}

\textbf{Interior optimum:} Frank--Wolfe exhibits characteristic ``zig-zagging'' in the approach to interior optima. Each LMO call returns a vertex $z_k \in \{(0.5,-5), (5,-5), (5,10), (0.5,10)\}$; near an interior optimum the gradient $\nabla f(x_k) \approx 0$ is small, so $z_k$ is determined by tiny gradient fluctuations, causing it to oscillate between opposite vertices.

The \textbf{Frank--Wolfe gap} (also known as the duality gap or primal gap) is defined as:
\begin{equation}
\mathcal{G}(x_k) = \max_{z \in \mathcal{X}} \langle -\nabla f(x_k), z - x_k \rangle = \langle \nabla f(x_k), x_k - z_k \rangle \geq 0.
\end{equation}
This quantity satisfies $f(x_k) - f^{\star} \leq \mathcal{G}(x_k)$ for convex $f$ (by convexity: $f(z) \geq f(x) + \langle \nabla f(x), z-x \rangle$ for all $z, x$), providing a computable convergence certificate without knowing $f^{\star}$. For the standard decreasing-step Frank--Wolfe with $\gamma_k = 2/(k+2)$, the gap satisfies $\mathcal{G}(x_k) \leq 2LD^2/(k+2)$ where $D = \text{diam}(\mathcal{X})$.

\textbf{Boundary optimum:} Frank--Wolfe is naturally suited to boundary optima because the gradient $\nabla f(x^*)$ at the constrained optimum is nonzero (it points toward the infeasible region), so the LMO consistently selects the vertex nearest the constrained optimum, driving the iterate toward the constraint boundary without explicit projection. The Frank--Wolfe gap at the constrained optimum satisfies the KKT condition: $\langle \nabla f(x^*), x - x^* \rangle \geq 0$ for all $x \in \mathcal{X}$, which is equivalent to $\mathcal{G}(x^*) = 0$.

\textbf{Comparison with Projected GD:} Projected GD achieves the constrained minimum in far fewer iterations on well-conditioned problems (linear convergence at rate $1-\mu/L$), but requires a projection oracle. Frank--Wolfe replaces the projection with a linear subproblem (the LMO), which is advantageous when $\mathcal{X}$ is a complex polytope or spectrahedron (e.g., a nuclear-norm ball $\|W\|_* \leq r$ in matrix optimisation, where the LMO has an $O(d^2)$ SVD solution vs $O(d^3)$ for projection). The $O(1/k)$ convergence of Frank--Wolfe is generally slower than projected GD's linear convergence, but each iteration is cheaper when the LMO is efficient.

\begin{remark}[LMO as a Linear Programme]
The Frank--Wolfe linear minimisation oracle $z_k = \arg\min_{z \in \mathcal{X}} \nabla f(x_k)^T z$ is precisely a linear programme (LP) with objective $a = \nabla f(x_k)$. By Proposition~\ref{prop:lp} (LP optimal at vertex), $z_k$ is always a vertex of $\mathcal{X}$. For a box constraint $\ell \leq x \leq u$, this reduces to coordinate-wise minimisation: $(z_k)_i = \ell_i$ if $(\nabla f(x_k))_i > 0$ else $u_i$. This connection explains the ``zig-zagging'' phenomenon near interior optima: as $\nabla f(x_k) \to 0$, the LMO becomes sensitive to the sign of each gradient component, and tiny gradient fluctuations cause $z_k$ to jump between opposite vertices.
\end{remark}

\begin{remark}[Frank--Wolfe Gap as Optimality Certificate]
The FW gap $\mathcal{G}(x_k) = \langle \nabla f(x_k), x_k - z_k \rangle$ is a tight optimality certificate: $\mathcal{G}(x_k) = 0$ if and only if $x_k$ satisfies the first-order KKT condition $\langle \nabla f(x_k), y - x_k \rangle \geq 0$ for all $y \in \mathcal{X}$, i.e., $x_k$ is a constrained stationary point. For convex $f$, this is equivalent to global optimality. The gap is computable from within the algorithm (since $z_k$ is already computed), providing a free convergence certificate without needing to know $f^{\star}$.
\end{remark}

\begin{table}[H]
\centering
\caption{Q5/Q6: Comparison of constrained optimisation approaches.}
\label{tab:constrained_comparison}
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{Feasibility} & \textbf{Per-iter cost} & \textbf{Conv.\ rate} & \textbf{Oracle needed} \\
\midrule
Projected GD & Hard (exact) & $O(d)$ gradient + $O(d)$ projection & Linear $O((1-\mu/L)^k)$ & Projection \\
Penalty GD & Soft (asymptotic) & $O(d)$ gradient & Linear (large $\lambda$) & None \\
Aug.\ Lagrangian & Exact at finite $\rho$ & $O(d)$ gradient + multiplier update & Superlinear & None \\
Frank--Wolfe & Implicit (convex comb.) & $O(d)$ gradient + LMO & $O(1/k)$ & LMO \\
\bottomrule
\end{tabular}
\end{table}

% ============================================================
\section{Conclusion}
\label{sec:conclusion}
% ============================================================

This report has systematically investigated twelve optimisation algorithms across six categories, evaluated on three benchmark problems of increasing difficulty. Figure~\ref{fig:all_methods} provides a comprehensive view of convergence behaviour across all gradient-based methods.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/all_methods_convergence.pdf}
\caption{Comprehensive convergence comparison of all gradient-based methods (GD, Polyak, Adagrad, RMSprop, Heavy Ball, Nesterov, Adam, Newton) across all three benchmarks. Horizontal dashed lines mark suboptimality levels $10^0, 10^{-1}, 10^{-2}$. On Benchmark A (left), most methods reach $10^{-10}$ within 50 iterations due to $\kappa \approx 1.12$; Polyak with correct $f^*$ converges in 1--2 steps (Newton-like). On Benchmark B (centre), all methods except misspecified Polyak† converge, with Newton fastest. On Benchmark C (right), the dramatic spread in convergence rates reflects the high condition number ($\kappa \approx 2508$): GD and RMSprop stagnate, while Polyak* converges rapidly, Nesterov achieves an intermediate rate, and Newton shows early rapid descent.}
\label{fig:all_methods}
\end{figure}

The following key conclusions emerge:

\paragraph{Adaptive step-size methods (Q1).} Polyak's step size achieves superior convergence when $f^{\star}$ is exactly known, but is critically sensitive to misspecification: a 47\% underestimate of $f^{\star}$ (specifying 0 instead of 0.4826) causes 100$\times$ degradation on Benchmark A. Adagrad and Heavy Ball are the most reliable alternatives, achieving GD-level performance on well-conditioned problems and substantially outperforming GD on Rosenbrock. RMSprop's exponential forgetting makes it the most stable near the minimum, but it requires careful tuning of $\alpha_0$.

\paragraph{Momentum and Adam (Q2).} Nesterov acceleration provides the most consistent improvement across all three benchmarks ($7\times$ over GD on Rosenbrock in 150 steps), confirming the theoretical $O(1/k^2)$ acceleration for convex problems. Adam combines momentum and adaptive scaling effectively for anisotropic problems (Benchmark B) but underperforms Nesterov on the non-convex Rosenbrock, where the per-coordinate scaling is less helpful than directional momentum. Mini-batch SGD demonstrates a fundamental noise-efficiency trade-off: smaller batches update more frequently per epoch but with higher gradient variance.

\paragraph{Newton's method (Q3).} The most powerful method when Hessian information is available and the function is well-approximated by its quadratic model. On the quadratic Benchmark A, convergence is exact in one step. On the moderately nonlinear Benchmark B, convergence occurs in 5 steps with a near-unit step ($\alpha = 0.85$). Rosenbrock requires heavy damping ($\alpha = 0.22$), which partially negates Newton's advantage but still achieves 5$\times$ better convergence than GD in 20 vs 80 iterations. The computational cost per iteration ($O(d^3)$ for exact Hessian solve) is offset by the dramatically reduced iteration count.

\paragraph{Derivative-free methods (Q4).} Finite differences with an appropriate step size ($\delta = 0.05$) effectively recovers full-gradient performance at only $d+1$ function evaluations per step. Large $\delta$ ($= 0.8$) causes significant truncation error that degrades convergence. Nesterov random search converges slowest but requires only 2 function evaluations per step regardless of dimension, making it the only method here that scales to large $d$. Nelder--Mead achieves competitive results on the 2D Rosenbrock without any gradient information, adapting its simplex geometry to the valley shape. Grid search provides global guarantees but is intractable beyond $d \approx 3$.

\paragraph{Constrained optimisation (Q5).} Projected gradient descent enforces the constraint $x_1 \geq 0.5$ as a hard requirement from the first iteration, achieving zero constraint violation throughout. The projection for a half-space is trivially $O(1)$. Penalty methods provide a softer alternative: $\lambda \geq 4.5$ achieves constraint satisfaction within 10 iterations, while $\lambda = 0.15$ fails to enforce feasibility. The required reduction in step size for high $\lambda$ demonstrates the stability-feasibility trade-off inherent in penalty approaches.

\paragraph{Frank--Wolfe and linear programming (Q6).} The Frank--Wolfe algorithm maintains feasibility implicitly via convex combinations, eliminating the projection oracle. For interior optima, the $O(1/k)$ convergence rate is confirmed empirically, with smaller step sizes (larger $\beta$) producing smoother convergence near the optimum despite the vertex-oscillation phenomenon. For boundary optima, Frank--Wolfe naturally approaches the constrained minimum as the LMO consistently selects nearby vertices. The linear programming result confirms the fundamental theorem: a linear objective over a bounded polytope is minimised at a vertex.

\paragraph{Overall ranking by difficulty and method suitability.}
\begin{itemize}[nosep]
\item \textbf{Well-conditioned quadratic} (Benchmark A, $\kappa \approx 1.12$): any first-order method suffices; Newton converges in 1 step (exact). GD converges in $\sim$50 steps.
\item \textbf{Mildly nonlinear, moderate $\kappa$} (Benchmark B, $\kappa \in [3.5, 6.9]$): all momentum methods (Nesterov, Adam, Heavy Ball) and Newton all converge to $f^* = 0.7244$ within 20--30 steps. Method choice makes little difference here.
\item \textbf{Ill-conditioned nonlinear} (Benchmark C, $\kappa \approx 2508$): this is the discriminating benchmark. Theoretical GD requires $\sim$57000 steps for $10^{-8}$ accuracy; Nesterov requires only $\sim$220. In 150 steps, Nesterov ($f = 0.479$) achieves $7\times$ better convergence than GD ($f = 3.39$). Polyak (with known $f^*=0$) achieves the best result ($f = 0.009$ in 120 steps). Newton with damping reaches $f = 0.686$ in 20 steps. Derivative-free Nelder--Mead is competitive ($f < 0.01$ in 160 steps) without any gradient information.
\end{itemize}

\paragraph{Lessons for algorithm selection in practice.} The key factors driving method selection are: (1) \emph{gradient availability} --- if gradients are cheap, momentum methods dominate over derivative-free approaches; (2) \emph{Hessian availability} --- Newton's method is worth its $O(d^3)$ cost for small $d$ with nearly-quadratic objectives; (3) \emph{condition number} --- for $\kappa \gg 1$, adaptive methods (AdaGrad, RMSprop, Adam) or Nesterov acceleration are essential; (4) \emph{knowledge of $f^*$} --- Polyak step is a powerful oracle-based method but inapplicable when $f^*$ is unknown; (5) \emph{constraint complexity} --- projected GD is simple for half-spaces; Frank--Wolfe excels for polytopes with efficient LMOs; penalty methods require no special structure but may converge slowly for large $\lambda$.

% ============================================================
\newpage
\appendix
\section{Python Code}
\label{app:code}
% ============================================================

The complete implementation is provided in \texttt{CS7DS2\_Final\_Code\_25336453.py}. Key algorithm implementations are reproduced below for reference.

\begin{lstlisting}[caption={Gradient Descent}]
def gradient_descent(grad_fn, loss_fn, x0, alpha, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for _ in range(n_iters):
        x = x - alpha * grad_fn(x)
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Polyak Step Size}]
def polyak_step(grad_fn, loss_fn, x0, f_star, eps, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        alpha_k = (loss_fn(x) - f_star) / (np.dot(g, g) + eps)
        alpha_hist.append(alpha_k)
        x = x - alpha_k * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Adagrad}]
def adagrad(grad_fn, loss_fn, x0, alpha0, eps, n_iters):
    x = x0.copy().astype(float)
    G = np.zeros_like(x)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        G += g**2
        eff_alpha = alpha0 / (np.sqrt(G) + eps)
        alpha_hist.append(np.mean(eff_alpha))
        x = x - eff_alpha * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={RMSprop}]
def rmsprop(grad_fn, loss_fn, x0, alpha0, beta, eps, n_iters):
    x = x0.copy().astype(float)
    v = np.zeros_like(x)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        v = beta * v + (1 - beta) * g**2
        eff_alpha = alpha0 / (np.sqrt(v) + eps)
        alpha_hist.append(np.mean(eff_alpha))
        x = x - eff_alpha * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Heavy Ball / Polyak Momentum}]
def heavy_ball(grad_fn, loss_fn, x0, alpha, beta, n_iters):
    x = x0.copy().astype(float)
    z = np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for _ in range(n_iters):
        g = grad_fn(x)
        z = beta * z + alpha * g
        x = x - z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Nesterov Momentum}]
def nesterov_momentum(grad_fn, loss_fn, x0, alpha, beta_max, n_iters):
    x = x0.copy().astype(float)
    z = np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for k in range(1, n_iters + 1):
        beta_k = min((k - 1) / (k + 2), beta_max)
        lookahead = x + beta_k * z
        g = grad_fn(lookahead)
        z = beta_k * z - alpha * g
        x = x + z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Adam Optimiser}]
def adam_optimiser(grad_fn, loss_fn, x0, alpha, beta1, beta2, eps, n_iters):
    x = x0.copy().astype(float)
    m_vec, v_vec = np.zeros_like(x), np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for t in range(1, n_iters + 1):
        g = grad_fn(x)
        m_vec = beta1 * m_vec + (1 - beta1) * g
        v_vec = beta2 * v_vec + (1 - beta2) * g**2
        m_hat = m_vec / (1 - beta1**t)
        v_hat = v_vec / (1 - beta2**t)
        x = x - alpha * m_hat / (np.sqrt(v_hat) + eps)
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Newton's Method with Tikhonov Damping}]
def newtons_method(grad_fn, hess_fn, loss_fn, x0, alpha, n_iters, damping=1e-8):
    x = x0.copy().astype(float)
    x_hist, f_hist, update_norms = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        H = hess_fn(x) if callable(hess_fn) else hess_fn
        H_reg = H + damping * np.eye(len(x))
        try:
            p = np.linalg.solve(H_reg, g)
        except np.linalg.LinAlgError:
            p = g
        update_norms.append(np.linalg.norm(alpha * p))
        x = x - alpha * p
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(update_norms)
\end{lstlisting}

\begin{lstlisting}[caption={Frank--Wolfe Algorithm (Fixed Step)}]
def frank_wolfe(grad_fn, loss_fn, x0, bounds, beta, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist, z_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        z = np.array([bounds[i][0] if g[i] > 0 else bounds[i][1]
                       for i in range(len(g))])
        z_hist.append(z.copy())
        x = beta * x + (1 - beta) * z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(z_hist)
\end{lstlisting}

% ============================================================
\section*{References}
\addcontentsline{toc}{section}{References}
% ============================================================

\begin{enumerate}[label={[\arabic*]}]
\item Polyak, B.T. (1964). Some methods of speeding up the convergence of iteration methods. \emph{USSR Computational Mathematics and Mathematical Physics}, 4(5), 1--17. [Heavy Ball; Polyak step size]
\item Duchi, J., Hazan, E., \& Singer, Y. (2011). Adaptive subgradient methods for online learning and stochastic optimization. \emph{Journal of Machine Learning Research}, 12, 2121--2159. [Adagrad; regret bounds]
\item Tieleman, T. \& Hinton, G. (2012). Lecture 6.5 -- RMSprop. COURSERA: Neural Networks for Machine Learning. [RMSprop]
\item Nesterov, Y. (1983). A method for unconstrained convex minimization problem with the rate of convergence $O(1/k^2)$. \emph{Doklady AN USSR}, 269, 543--547. [Nesterov acceleration; optimal first-order method]
\item Nesterov, Y. (2004). \emph{Introductory Lectures on Stochastic Optimization}. Kluwer Academic Publishers. [Convergence rate theory; PL condition; Frank--Wolfe]
\item Kingma, D.P. \& Ba, J. (2015). Adam: A method for stochastic optimization. In \emph{Proc.\ ICLR 2015}. arXiv:1412.6980. [Adam optimiser]
\item Reddi, S.J., Kale, S., \& Kumar, S. (2018). On the convergence of Adam and beyond. In \emph{Proc.\ ICLR 2018}. [Adam non-convergence in non-convex settings]
\item Nelder, J.A. \& Mead, R. (1965). A simplex method for function minimization. \emph{The Computer Journal}, 7(4), 308--313. [Nelder--Mead simplex method]
\item Frank, M. \& Wolfe, P. (1956). An algorithm for quadratic programming. \emph{Naval Research Logistics Quarterly}, 3(1--2), 95--110. [Frank--Wolfe conditional gradient algorithm]
\item Rosenbrock, H.H. (1960). An automatic method for finding the greatest or least value of a function. \emph{The Computer Journal}, 3(3), 175--184. [Rosenbrock test function]
\item Nocedal, J. \& Wright, S.J. (2006). \emph{Numerical Optimization} (2nd ed.). Springer. [Newton's method; trust region; line search; KKT conditions; penalty methods]
\item Bottou, L., Curtis, F.E., \& Nocedal, J. (2018). Optimisation methods for large-scale machine learning. \emph{SIAM Review}, 60(2), 223--311. [SGD; mini-batch gradient estimation; variance reduction]
\end{enumerate}

\end{document}
