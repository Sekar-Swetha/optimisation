\documentclass[11pt,a4paper]{article}
\usepackage[margin=2.2cm]{geometry}
\usepackage{graphicx}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{float}
\usepackage{hyperref}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{listings}
\usepackage{fancyhdr}
\usepackage{microtype}

\hypersetup{colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue}

% Code listing style
\lstset{
  language=Python,
  basicstyle=\ttfamily\scriptsize,
  keywordstyle=\color{blue},
  commentstyle=\color{gray},
  stringstyle=\color{red},
  numbers=left,
  numberstyle=\tiny\color{gray},
  breaklines=true,
  frame=single,
  captionpos=b
}

% Theorem-like environments
\newtheorem{proposition}{Proposition}
\newtheorem{remark}{Remark}

\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small CS7DS2 -- Final Assignment}
\fancyhead[R]{\small Swetha Sekar (25336453)}
\fancyfoot[C]{\thepage}

\title{%
  \includegraphics[width=0.25\textwidth]{tcd_logo.jpg}\\[1em]
  \textbf{CS7DS2 -- Optimisation Algorithms for Data Analysis}\\[0.5em]
  \Large Final Assignment Report
}
\author{Swetha Sekar \\ Student ID: 25336453 \\ School of Computer Science and Statistics \\ Trinity College Dublin}
\date{April 2026}

\begin{document}
\maketitle
\thispagestyle{empty}

\begin{abstract}
This report presents a systematic comparative investigation of twenty optimisation algorithms across six conceptually distinct paradigms, evaluated on three benchmark problems of increasing difficulty: a well-conditioned linear regression quadratic loss (condition number $\kappa \approx 1.12$), a mildly anisotropic toy neural-network loss (condition number $\kappa \approx 6.90$), and the ill-conditioned Rosenbrock function (condition number $\kappa \approx 2508$ at the minimum).

The six paradigms studied are: \textbf{(Q1)} adaptive step-size methods (Polyak, Adagrad, RMSprop, Heavy Ball), \textbf{(Q2)} momentum-based and stochastic methods (Nesterov acceleration, Adam, mini-batch SGD), \textbf{(Q3)} second-order Newton's method with local quadratic approximations, \textbf{(Q4)} derivative-free approaches (finite differences, Nesterov random search, Nelder--Mead simplex, grid search), \textbf{(Q5)} constrained optimisation via projected gradient descent and penalty methods, and \textbf{(Q6)} the Frank--Wolfe algorithm for box-constrained problems.

Key findings: the Polyak step size achieves best convergence when $f^{\star}$ is known exactly but diverges catastrophically under misspecification. Newton's method solves the quadratic loss in a single iteration and outperforms all first-order methods on Rosenbrock despite far fewer iterations. Nesterov acceleration achieves the theoretical $O(1/k^2)$ improvement over gradient descent's $O(1/k)$ rate on Rosenbrock, advancing to $f = 0.479$ versus $f = 3.388$ for GD in 150 iterations. Finite-difference gradient approximation with $\delta = 0.05$ closely matches exact gradient descent, while large $\delta = 0.8$ causes substantial truncation error. Projected gradient descent enforces feasibility immediately (zero violation from iteration 1), whereas penalty methods require careful weight tuning. Frank--Wolfe exhibits the characteristic $O(1/k)$ convergence rate without requiring any projection operation.
\end{abstract}

\tableofcontents
\newpage

% ============================================================
\section{Benchmark Problems and Experimental Setup}
\label{sec:benchmarks}
% ============================================================

All experiments use a fixed random seed (\texttt{np.random.seed(42)}) for reproducibility. The three benchmark functions span qualitatively different optimisation landscapes, allowing each algorithm's strengths and weaknesses to be clearly isolated.

\subsection{Benchmark A: Linear Regression Quadratic Loss}
A synthetic dataset with $m = 1000$ samples and $d = 2$ parameters is generated:
\begin{equation}
\theta^{\star} = \begin{bmatrix} 3 \\ 4 \end{bmatrix}, \quad x^{(i)} \in \mathbb{R}^2 \sim \mathcal{N}(0, I), \quad y^{(i)} = \theta^{\star T} x^{(i)} + \varepsilon^{(i)}, \quad \varepsilon^{(i)} \sim \mathcal{N}(0, 1).
\end{equation}
The loss function is the mean squared error:
\begin{equation}
J(\theta) = \frac{1}{2m} \sum_{i=1}^{m} \left(\theta^T x^{(i)} - y^{(i)}\right)^2 = \frac{1}{2}\theta^T H \theta - b^T\theta + c,
\end{equation}
where $H = \frac{1}{m}X^T X$ is the (constant) Hessian and $b = \frac{1}{m}X^T y$. The gradient is $\nabla J(\theta) = H\theta - b = \frac{1}{m}X^T(X\theta - y)$.

The function is strongly convex with $\mu = \lambda_{\min}(H) \approx 0.925$ and Lipschitz gradient constant $L = \lambda_{\max}(H) \approx 1.033$. The condition number $\kappa(H) \approx 1.12$ makes this an extremely well-conditioned problem --- gradient descent is expected to converge quickly. The OLS solution $\theta^{\star}_{\text{OLS}} = H^{-1}b \approx (3.028, 4.017)^T$ defines the true minimiser; due to observation noise, $J^{\star} = J(\theta^{\star}_{\text{OLS}}) \approx 0.4826 \neq 0$. Starting point: $\theta_0 = [0, 0]^T$.

\subsection{Benchmark B: Toy Neural Network Loss}
\begin{equation}
f(x_1, x_2) = (x_1 - 1)^2 + 5(x_2 - 2)^2 + \sin(x_1).
\end{equation}
The gradient is $\nabla f = [2(x_1 - 1) + \cos(x_1),\ 10(x_2 - 2)]^T$ and the Hessian is $H(x) = \text{diag}(2 - \sin(x_1),\ 10)$. The sinusoidal term introduces mild nonlinearity: the Hessian varies with $x_1$, with eigenvalues ranging between $[1, 3]$ and $[10, 10]$. At the minimum $x^{\star} \approx (0.582, 2.0)$ --- where $2(x_1^{\star} - 1) + \cos(x_1^{\star}) = 0$ and $x_2^{\star} = 2$ --- the Hessian has eigenvalues $\approx 1.45$ and $10$, giving condition number $\kappa \approx 6.90$. The minimum objective is $f^{\star} \approx 0.7244$. Starting point: $x_0 = [-1, 4]^T$.

\subsection{Benchmark C: Rosenbrock Function}
\begin{equation}
f(x_1, x_2) = (1 - x_1)^2 + 100(x_2 - x_1^2)^2.
\end{equation}
The global minimum is at $(1, 1)$ with $f^{\star} = 0$. The gradient is:
\begin{align}
\frac{\partial f}{\partial x_1} &= -2(1 - x_1) - 400 x_1(x_2 - x_1^2), \\
\frac{\partial f}{\partial x_2} &= 200(x_2 - x_1^2).
\end{align}
The Hessian at the optimum $(1,1)$ is:
\begin{equation}
H(1,1) = \begin{bmatrix} 802 & -400 \\ -400 & 200 \end{bmatrix},
\end{equation}
with eigenvalues $\lambda_{\min} \approx 0.40$ and $\lambda_{\max} \approx 1002$, giving condition number $\kappa \approx 2508$. This extreme ill-conditioning, combined with the narrow curved valley along $x_2 = x_1^2$, makes Rosenbrock one of the most challenging standard test functions and the primary discriminator between algorithms in this study. Starting point: $x_0 = [-1, 1]^T$.

\subsection{Benchmark D: Constrained Feasible Set}
For constrained problems: $\mathcal{X} = \{x \in \mathbb{R}^2 : 0.5 \leq x_1 \leq 5,\ -5 \leq x_2 \leq 10\}$, a box with diameter $D = \|(5-0.5, 10-(-5))\| \approx 15.66$.

% ============================================================
\section{Question 1: Adaptive Step-Size Methods \hfill [30 marks]}
\label{sec:q1}
% ============================================================

\subsection{Theoretical Background}

Four adaptive step-size methods are compared against a constant step-size gradient descent (GD) baseline. The baseline GD update is $x_{k+1} = x_k - \alpha \nabla f(x_k)$, and for $L$-smooth strongly convex functions converges at the linear rate:
\begin{equation}
f(x_k) - f^{\star} \leq \left(1 - \frac{\mu}{L}\right)^k [f(x_0) - f^{\star}],
\end{equation}
where $\mu$ and $L$ are the strong convexity and smoothness parameters. The adaptive methods attempt to improve convergence by automatically adjusting $\alpha_k$ to local curvature information.

\subsection{Algorithm Formulations}

\begin{algorithm}[H]
\caption{Polyak Step Size}
\label{alg:polyak}
\begin{algorithmic}[1]
\Require Initial point $x_0$, optimal value $f^{\star}$, tolerance $\varepsilon$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $\alpha_k \gets \dfrac{f(x_k) - f^{\star}}{\|g_k\|^2 + \varepsilon}$ \Comment{Normalised progress-to-gradient ratio}
    \State $x_{k+1} \gets x_k - \alpha_k\, g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

The Polyak step size is theoretically optimal for non-smooth convex functions when $f^{\star}$ is known: it achieves convergence in $O(1/\sqrt{k})$ with no tuning required. However, it requires accurate knowledge of $f^{\star}$.

\begin{algorithm}[H]
\caption{Adagrad}
\label{alg:adagrad}
\begin{algorithmic}[1]
\Require Initial point $x_0$, base learning rate $\alpha_0$, tolerance $\varepsilon$, iterations $N$
\State $G_0 \gets \mathbf{0}$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $G_{k+1} \gets G_k + g_k \odot g_k$ \Comment{Per-coordinate cumulative squared gradient}
    \State $x_{k+1} \gets x_k - \dfrac{\alpha_0}{\sqrt{G_{k+1}} + \varepsilon} \odot g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

Adagrad adapts the learning rate per coordinate by dividing by $\sqrt{G_k}$, assigning smaller rates to frequently-updated directions and larger rates to infrequent ones. The per-coordinate effective step size is $\alpha_k^{(i)} = \alpha_0 / (\sqrt{G_k^{(i)}} + \varepsilon)$, which monotonically decreases because $G_k^{(i)}$ is non-decreasing. This guarantees convergence but can cause premature stagnation in non-stationary settings.

\begin{algorithm}[H]
\caption{RMSprop}
\label{alg:rmsprop}
\begin{algorithmic}[1]
\Require Initial point $x_0$, learning rate $\alpha_0$, decay $\beta$, tolerance $\varepsilon$, iterations $N$
\State $v_0 \gets \mathbf{0}$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $v_{k+1} \gets \beta\, v_k + (1 - \beta)\, g_k \odot g_k$ \Comment{Exponential moving average}
    \State $x_{k+1} \gets x_k - \dfrac{\alpha_0}{\sqrt{v_{k+1}} + \varepsilon} \odot g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

RMSprop replaces Adagrad's growing denominator with an exponential moving average of squared gradients, controlled by decay factor $\beta \in (0,1)$. This gives the effective step size a finite memory, preventing monotone decay and allowing adaptation to non-stationary objectives.

\begin{algorithm}[H]
\caption{Polyak Momentum / Heavy Ball}
\label{alg:heavyball}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, momentum $\beta$, iterations $N$
\State $z_0 \gets \mathbf{0}$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_{k+1} \gets \beta\, z_k + \alpha\, \nabla f(x_k)$ \Comment{Exponential average of gradients}
    \State $x_{k+1} \gets x_k - z_{k+1}$
\EndFor
\end{algorithmic}
\end{algorithm}

The Heavy Ball method (Polyak Momentum) accelerates gradient descent by accumulating a velocity vector $z_k$ that averages past gradients. For a quadratic objective with eigenvalues $[\mu, L]$, the optimal parameters are $\alpha^{\star} = 4/(\sqrt{L}+\sqrt{\mu})^2$ and $\beta^{\star} = ((\sqrt{\kappa}-1)/(\sqrt{\kappa}+1))^2$, giving convergence rate $O(((\sqrt{\kappa}-1)/(\sqrt{\kappa}+1))^k)$, a significant improvement over GD's $O(((κ-1)/(κ+1))^k)$ rate when $\kappa$ is large.

\subsection{Hyperparameter Settings}

\begin{table}[H]
\centering
\caption{Q1: Hyperparameter settings for each method and benchmark. All methods run for 120 iterations.}
\label{tab:q1_params}
\begin{tabular}{llccc}
\toprule
\textbf{Method} & \textbf{Parameter} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & $\alpha$ & 0.08 & 0.06 & 0.0012 \\
\midrule
Polyak & $f^{\star}$ & 0 & 0 & 0 \\
 & $\varepsilon$ & $10^{-4}$ & $10^{-4}$ & $10^{-3}$ \\
\midrule
Adagrad & $\alpha_0$ & 1.8 & 1.2 & 0.45 \\
 & $\varepsilon$ & $10^{-5}$ & $10^{-5}$ & $10^{-5}$ \\
\midrule
RMSprop & $\alpha_0$ & 0.22 & 0.14 & 0.0035 \\
 & $\beta$ & 0.9 & 0.9 & 0.9 \\
 & $\varepsilon$ & $10^{-5}$ & $10^{-5}$ & $10^{-5}$ \\
\midrule
Heavy Ball & $\alpha$ & 0.045 & 0.035 & 0.0008 \\
 & $\beta$ & 0.88 & 0.90 & 0.86 \\
\bottomrule
\end{tabular}
\end{table}

The GD step sizes are chosen to be slightly below $2/L$ (a standard stability requirement); Rosenbrock requires a very small $\alpha = 0.0012$ because the Hessian eigenvalue near the starting point $(-1, 1)$ can reach $\lambda_{\max} \approx 1682$, demanding $\alpha < 2/1682 \approx 0.0012$ for stability.

\subsection{Convergence Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_A.pdf}
\caption{Q1: Convergence on Benchmark A (Linear Regression). GD, Adagrad, and Heavy Ball all converge rapidly to the noise floor $J^{\star} \approx 0.4826$, exploiting the near-unit condition number $\kappa \approx 1.12$. RMSprop converges to a slightly elevated value ($\approx 0.506$) due to residual oscillation near the minimum from its adaptive step. Polyak \textbf{diverges} because $f^{\star} = 0$ misspecifies the true minimum: as the iterate approaches $\theta_{\text{OLS}}$, $\|\nabla J\| \to 0$ while $J - 0 \approx 0.48$, causing $\alpha_k \to \infty$ and oscillation.}
\label{fig:q1_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_B.pdf}
\caption{Q1: Convergence on Benchmark B (Toy Neural Network). The same $f^{\star}$ misspecification causes Polyak to diverge. Adagrad and Heavy Ball match GD's final accuracy ($f \approx 0.7244$), with Heavy Ball benefiting from momentum smoothing. The moderate condition number $\kappa \approx 6.9$ means all first-order methods converge acceptably.}
\label{fig:q1_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_C.pdf}
\caption{Q1: Convergence on Benchmark C (Rosenbrock). With $f^{\star} = 0$ specified correctly, Polyak achieves the best convergence ($f \approx 0.009$): the step size $\alpha_k = f(x_k)/\|\nabla f(x_k)\|^2$ decreases naturally as both the numerator and denominator shrink. Heavy Ball ($f = 1.40$) and Adagrad ($f = 2.20$) outperform GD ($f = 3.51$) by exploiting momentum and coordinate-wise adaptation, respectively. RMSprop ($f = 3.12$) is constrained by its very small base rate $\alpha_0 = 0.0035$.}
\label{fig:q1_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q1: Final objective values after 120 iterations. Bold indicates best per benchmark.}
\label{tab:q1_final}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & 0.4826 & 0.7244 & 3.5142 \\
Polyak & $\dagger$ diverges & $\dagger$ diverges & \textbf{0.0094} \\
Adagrad & \textbf{0.4826} & \textbf{0.7244} & 2.2034 \\
RMSprop & 0.5027 & 0.7271 & 3.1204 \\
Heavy Ball & 0.4826 & 0.7244 & 1.4003 \\
\bottomrule
\multicolumn{4}{l}{\footnotesize $\dagger$ Polyak diverges because $f^{\star}=0$ misspecifies the true minimum on A and B.}
\end{tabular}
\end{table}

\textbf{Key finding:} The Polyak step size is highly sensitive to the accuracy of $f^{\star}$. The divergence mechanism is as follows: let $f^{\star}_{\text{true}}$ denote the actual minimum. If $f^{\star} < f^{\star}_{\text{true}}$ (as here with $f^{\star} = 0$), then as $x_k \to x^{\star}_{\text{true}}$ we have $\|g_k\| = \|\nabla f(x_k)\| \to 0$ but $f(x_k) - f^{\star} \to f^{\star}_{\text{true}} - f^{\star} > 0$. The step size therefore grows as $\alpha_k \approx (f^{\star}_{\text{true}} - f^{\star})/\|g_k\|^2 \to \infty$, causing oscillation and divergence. On Benchmark C where $f^{\star} = 0$ is exact, this pathology disappears and Polyak achieves superior convergence.

\subsection{Contour Plots with Optimisation Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q1_contour_B.pdf}
\caption{Q1: Trajectories on Benchmark B. The elliptical contours reflect the $\sim$7:1 curvature ratio between the $x_2$ and $x_1$ directions. GD and Adagrad follow near-direct paths to the minimum. Heavy Ball shows the characteristic momentum overshoot followed by damped return. The Polyak trajectory diverges widely, confirming the $f^{\star}$ misspecification mechanism.}
\label{fig:q1_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q1_contour_C.pdf}
\caption{Q1: Trajectories on Benchmark C (Rosenbrock). The banana-shaped valley $x_2 \approx x_1^2$ is visible in the contour levels. All methods first descend rapidly towards the valley floor, then must advance slowly along it towards $(1,1)$. The Polyak trajectory (with correct $f^{\star}$) shows the most aggressive progress along the valley, while GD with $\alpha = 0.0012$ advances minimally due to its conservative step constrained by the high curvature near the starting point.}
\label{fig:q1_contour_C}
\end{figure}

\subsection{Adaptive Step-Size Evolution}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_A.pdf}
\caption{Q1: Step-size evolution on Benchmark A. The Polyak step $\alpha_k$ grows as $\|g_k\| \to 0$ while $f - f^{\star}$ remains bounded away from zero, confirming the divergence mechanism. Adagrad's per-coordinate step $\alpha_0/\sqrt{G_k}$ decays monotonically as squared gradients accumulate in the denominator --- this decay is irreversible once started. RMSprop stabilises due to its exponential moving average with $\beta = 0.9$: the effective step $\alpha_0/\sqrt{v_k}$ plateaus once $v_k$ reaches steady state.}
\label{fig:q1_stepsize_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_B.pdf}
\caption{Q1: Step-size evolution on Benchmark B. Adagrad decays most rapidly in early iterations when large gradients are encountered (the starting point $(-1, 4)$ is far from the minimum), then settles to a very small effective rate. RMSprop maintains a moderate, slowly-varying effective step because its exponential average forgets old gradients.}
\label{fig:q1_stepsize_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_C.pdf}
\caption{Q1: Step-size evolution on Benchmark C. With correct $f^{\star} = 0$, the Polyak step decreases smoothly as $f(x_k) \to 0$ while $\|\nabla f(x_k)\|$ also shrinks. The ratio $f(x_k)/\|\nabla f(x_k)\|^2$ exhibits a controlled, monotone decay --- characteristic of the method converging correctly. This well-behaved step schedule confirms why Polyak achieves the best Rosenbrock convergence.}
\label{fig:q1_stepsize_C}
\end{figure}

\subsection{Comparative Analysis}

\begin{itemize}[nosep]
\item \textbf{Convergence speed:} When $f^{\star}$ is known exactly, Polyak converges fastest (Rosenbrock: $f = 0.009$). On Benchmarks A and B, where the condition number is small, GD, Adagrad, and Heavy Ball all reach the minimum rapidly and differences between methods are negligible.
\item \textbf{Smoothness:} RMSprop produces the smoothest convergence curves due to exponential gradient averaging. Heavy Ball shows characteristic overshoot (proportional to $\beta/(1-\beta)$ times the gradient step) followed by damped oscillation.
\item \textbf{Stability:} GD baseline is most stable but slowest. Polyak is unstable when $f^{\star}$ is misspecified. Adagrad is uniformly stable due to its monotonically decreasing step, though this may cause premature stagnation in long runs.
\item \textbf{Practical implications:} Exact $f^{\star}$ is rarely known in practice, limiting Polyak's applicability to settings such as support vector machine training or problems with a known lower bound. RMSprop and Adagrad are more robust defaults for non-stationary objectives. Heavy Ball is effective when some estimate of the condition number is available for setting $(\alpha, \beta)$.
\end{itemize}

% ============================================================
\section{Question 2: Momentum and Stochastic Methods \hfill [30 marks]}
\label{sec:q2}
% ============================================================

\subsection{Theoretical Background}

Nesterov's accelerated gradient method achieves the optimal first-order convergence rate for smooth convex functions:
\begin{equation}
f(x_k) - f^{\star} \leq \frac{2L \|x_0 - x^{\star}\|^2}{(k+1)^2} = O(1/k^2),
\end{equation}
compared to $O(1/k)$ for gradient descent. This $O(1/k^2)$ rate is optimal: no first-order black-box method can achieve a better guarantee on smooth convex functions (Nesterov's lower bound). The key innovation is the \emph{lookahead} step: evaluating the gradient at a predicted future position rather than the current iterate.

\subsection{Algorithm Formulations}

\begin{algorithm}[H]
\caption{Nesterov Momentum / Acceleration}
\label{alg:nesterov}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, max momentum $\beta_{\max}$, iterations $N$
\State $z_0 \gets \mathbf{0}$
\For{$k = 1, 2, \ldots, N$}
    \State $\beta_k \gets \min\!\left(\frac{k-1}{k+2},\ \beta_{\max}\right)$ \Comment{Adaptive momentum schedule}
    \State $\tilde{x}_k \gets x_k + \beta_k\, z_k$ \Comment{Lookahead point}
    \State $z_{k+1} \gets \beta_k\, z_k - \alpha\, \nabla f(\tilde{x}_k)$ \Comment{Velocity update at lookahead}
    \State $x_{k+1} \gets x_k + z_{k+1}$
\EndFor
\end{algorithmic}
\end{algorithm}

The critical distinction from Heavy Ball is the \emph{lookahead}: the gradient is evaluated at $\tilde{x}_k = x_k + \beta_k z_k$ rather than at $x_k$. This allows the method to \emph{correct} its trajectory by using a projected estimate of the next iterate before committing to a step, which is what enables the $O(1/k^2)$ guarantee. The adaptive schedule $\beta_k = (k-1)/(k+2)$ builds momentum gradually, with $\beta_k \to 1$ as $k \to \infty$, bounded by $\beta_{\max}$ for stability.

\begin{algorithm}[H]
\caption{Adam Optimiser}
\label{alg:adam}
\begin{algorithmic}[1]
\Require Initial point $x_0$, learning rate $\alpha$, moments $\beta_1, \beta_2$, tolerance $\varepsilon$, iterations $N$
\State $m_0 \gets \mathbf{0},\ v_0 \gets \mathbf{0}$
\For{$t = 1, 2, \ldots, N$}
    \State $g_t \gets \nabla f(x_t)$
    \State $m_t \gets \beta_1\, m_{t-1} + (1 - \beta_1)\, g_t$ \Comment{First moment (momentum)}
    \State $v_t \gets \beta_2\, v_{t-1} + (1 - \beta_2)\, g_t \odot g_t$ \Comment{Second moment (curvature)}
    \State $\hat{m}_t \gets m_t\, /\, (1 - \beta_1^t)$ \Comment{Bias-corrected first moment}
    \State $\hat{v}_t \gets v_t\, /\, (1 - \beta_2^t)$ \Comment{Bias-corrected second moment}
    \State $x_{t+1} \gets x_t - \alpha\, \hat{m}_t\, /\, (\sqrt{\hat{v}_t} + \varepsilon)$
\EndFor
\end{algorithmic}
\end{algorithm}

Adam synthesises momentum (via $m_t$, a first-order gradient exponential average) with per-coordinate curvature adaptation (via $v_t$, a second-order moment estimate), inheriting the advantages of both RMSprop and momentum methods. The bias-correction terms $1/(1-\beta_1^t)$ and $1/(1-\beta_2^t)$ counteract the initialisation at zero: without them, early estimates $\hat{m}_t$ and $\hat{v}_t$ would be biased towards zero, making initial steps too small.

\subsection{Hyperparameter Settings}

\begin{table}[H]
\centering
\caption{Q2: Hyperparameter settings. Nesterov and Adam run for 150 iterations.}
\label{tab:q2_params}
\begin{tabular}{llccc}
\toprule
\textbf{Method} & \textbf{Parameter} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & $\alpha$ & 0.08 & 0.06 & 0.0012 \\
\midrule
Nesterov & $\alpha$ & 0.06 & 0.035 & 0.0007 \\
 & $\beta_{\max}$ & 0.90 & 0.92 & 0.90 \\
\midrule
Adam & $\alpha$ & 0.12 & 0.08 & 0.006 \\
 & $\beta_1$ & 0.82 & 0.80 & 0.80 \\
 & $\beta_2$ & 0.999 & 0.999 & 0.999 \\
 & $\varepsilon$ & $10^{-8}$ & $10^{-8}$ & $10^{-8}$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Empirical Convergence Rate Verification}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/convergence_rate_loglog.pdf}
\caption{Empirical convergence rate verification on log-log axes. Left: Frank--Wolfe with standard schedule $\gamma_k = 2/(k+2)$ closely follows the $O(1/k)$ reference line (dashed), confirming the theoretical rate. Fixed $\beta$ schedules deviate from $O(1/k)$ and exhibit oscillation. Right: on a quadratic with $\kappa = 50$, Gradient Descent follows the $O(1/k)$ slope and Nesterov Acceleration follows the $O(1/k^2)$ slope on log-log axes, providing direct empirical confirmation of both theoretical rates.}
\label{fig:rate_verify}
\end{figure}

\subsection{Convergence Comparison}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_A.pdf}
\caption{Q2: Convergence on Benchmark A. All three methods converge to $J^{\star} \approx 0.4826$. The near-unit condition number means GD already converges in very few iterations, leaving little room for momentum methods to show improvement. On well-conditioned quadratics, the advantage of Nesterov over GD scales with $\sqrt{\kappa}$, which here is only $\approx 1.06$.}
\label{fig:q2_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_B.pdf}
\caption{Q2: Convergence on Benchmark B. Both Nesterov and Adam converge to $f^{\star} \approx 0.7244$. Adam's per-coordinate scaling effectively handles the $\sim$7:1 anisotropy. Nesterov's lookahead suppresses overshoot across the narrow $x_2$ direction, enabling smooth convergence despite the moderate condition number.}
\label{fig:q2_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_C.pdf}
\caption{Q2: Convergence on Benchmark C. The Rosenbrock function most clearly reveals the benefit of acceleration. Nesterov achieves $f = 0.479$ --- a $7\times$ improvement over GD ($f = 3.388$) in the same number of iterations. This is consistent with the $O(1/k^2)$ vs $O(1/k)$ gap: at $k=150$, the squared rate is $(1/150)^2 \approx 4.4\times10^{-5}$ times the initial error versus $(1/150) \approx 6.7\times10^{-3}$ for GD. Adam reaches $f = 1.578$; while its per-coordinate scaling helps, the highly nonlinear Rosenbrock valley limits Adam's effectiveness compared to Nesterov's momentum in the consistent downhill direction.}
\label{fig:q2_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q2: Final objective values after 150 iterations. Bold indicates best.}
\label{tab:q2_final}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & 0.4826 & 0.7244 & 3.3880 \\
Nesterov & 0.4826 & 0.7244 & \textbf{0.4788} \\
Adam & 0.4826 & 0.7244 & 1.5785 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Contour Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q2_contour_B.pdf}
\caption{Q2: Trajectories on Benchmark B. Nesterov's trajectory shows the characteristic ``smooth arc'' of accelerated methods: momentum in the $x_2$ direction (where curvature is 10) is quickly damped since the gradient in that direction is larger, while the lookahead prevents oscillation. Adam takes more direct steps by independently scaling each coordinate.}
\label{fig:q2_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q2_contour_C.pdf}
\caption{Q2: Trajectories on Benchmark C. All methods descend quickly to the valley floor but differ substantially in progress along it. Nesterov builds consistent momentum in the direction of descent along the valley, enabling it to traverse toward $(1,1)$. GD's constant step size requires $\alpha \ll 2/\lambda_{\max}$, severely limiting its velocity along the valley floor.}
\label{fig:q2_contour_C}
\end{figure}

\subsection{Mini-Batch Stochastic Gradient Descent}

Mini-batch SGD on Benchmark A uses learning rate $\alpha = 0.06$, batch sizes $b \in \{5, 40\}$, 50 epochs, with data shuffled at every epoch. The gradient estimator is:
\begin{equation}
\hat{g}(\theta) = \frac{1}{b} \sum_{i \in \mathcal{B}} x^{(i)}\left(\theta^T x^{(i)} - y^{(i)}\right),
\end{equation}
which is an unbiased estimate of the full gradient $\nabla J(\theta)$. The variance of this estimator scales as $\sigma^2_g / b$, where $\sigma^2_g$ is the per-sample gradient variance.

\begin{algorithm}[H]
\caption{Mini-Batch SGD}
\label{alg:sgd}
\begin{algorithmic}[1]
\Require Data $(X, y)$, initial $\theta_0$, learning rate $\alpha$, batch size $b$, epochs $E$
\For{$e = 1, 2, \ldots, E$}
    \State Randomly permute data indices
    \For{each mini-batch $\mathcal{B}$ of size $b$}
        \State $g \gets \frac{1}{b} X_{\mathcal{B}}^T(X_{\mathcal{B}}\theta - y_{\mathcal{B}})$ \Comment{Stochastic gradient}
        \State $\theta \gets \theta - \alpha\, g$
    \EndFor
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_sgd_batch.pdf}
\caption{Q2: Mini-batch SGD with different batch sizes. With $b = 5$, each epoch performs $1000/5 = 200$ updates --- more parameter updates per epoch, leading to faster initial convergence but higher per-update gradient noise $\propto 1/\sqrt{b}$. With $b = 40$, each epoch has $1000/40 = 25$ updates with lower variance. The smaller batch converges faster per epoch (more passes through the optimisation landscape), but exhibits more oscillation near the minimum as individual mini-batch gradients deviate from the true gradient direction.}
\label{fig:q2_sgd_batch}
\end{figure}

\subsection{SGD with Increased Noise}

The experiment is repeated with output noise scaled by factor 6: $y^{(i)} = \theta^{\star T} x^{(i)} + 6\varepsilon^{(i)}$, $\varepsilon^{(i)} \sim \mathcal{N}(0,1)$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q2_sgd_noisy.pdf}
\caption{Q2: Effect of observation noise on SGD convergence. Left: original noise ($\sigma = 1$). Right: high noise ($\sigma = 6$). Increasing the noise by factor 6 raises the irreducible loss floor by factor $\sim 36$ (since $J^{\star} \propto \sigma^2$), lifting the floor from $\approx 0.48$ to $\approx 17.3$. Under high noise, both batch sizes converge to approximately the same elevated floor, and the relative advantage of smaller batches diminishes: the irreducible observation noise $\sigma^2/m$ now dominates the mini-batch sampling noise $\sigma^2_g/b$, making the choice of $b$ less critical.}
\label{fig:q2_sgd_noisy}
\end{figure}

\begin{remark}
The noise floor analysis reveals a fundamental limitation of SGD with fixed learning rate: for any $\alpha > 0$, the iterates converge not to the exact minimum but to a neighbourhood of radius $O(\alpha \sigma^2_g / b)$. To achieve exact convergence, one must either use a decaying learning rate schedule (at the cost of slower convergence) or reduce to full-batch GD. The optimal trade-off between convergence speed and noise floor is problem-dependent. In the high-noise experiment ($\sigma = 6$), the noise floor scales to $J^{\star}_{\text{noisy}} = \sigma^2 J^{\star}_{\text{low}} \approx 36 \times 0.4826 \approx 17.4$, which matches the observed $\approx 17$--$18$.
\end{remark}

\subsection{Comparison: Nesterov vs Adam}

While both Nesterov and Adam achieve fast convergence on smooth, convex problems, they embody fundamentally different design philosophies:

\begin{table}[H]
\centering
\caption{Nesterov vs Adam: Design philosophy and practical trade-offs.}
\label{tab:nesterov_adam}
\begin{tabular}{lll}
\toprule
\textbf{Property} & \textbf{Nesterov} & \textbf{Adam} \\
\midrule
Adaptation & None (global step $\alpha$) & Per-coordinate scaling \\
Momentum & Lookahead (corrective) & First-moment EMA \\
Theory & Optimal $O(1/k^2)$ for smooth convex & Convergence on smooth non-convex \\
Hyperparameters & 2 ($\alpha$, $\beta_{\max}$) & 4 ($\alpha$, $\beta_1$, $\beta_2$, $\varepsilon$) \\
Best for & Well-conditioned convex problems & Anisotropic, non-stationary problems \\
Rosenbrock ($\kappa \approx 2508$) & $f = 0.479$ (better) & $f = 1.578$ (worse) \\
\bottomrule
\end{tabular}
\end{table}

The Rosenbrock result can be explained theoretically: Rosenbrock has \emph{global} ill-conditioning (the condition number along the trajectory changes significantly), but the dominant gradient direction is consistently along the valley floor. Nesterov's momentum in this persistent direction is more effective than Adam's per-coordinate adaptation, which may scale gradient components in directions that are not aligned with the natural coordinate axes. Adam excels in deep learning where different layers or neurons may require very different effective learning rates, a situation that does not arise in the 2D test problems studied here.

% ============================================================
\section{Question 3: Newton's Method and Local Approximation \hfill [30 marks]}
\label{sec:q3}
% ============================================================

\subsection{Local Approximations of $g(x) = x^4$}

To motivate Newton's method, consider the local Taylor approximation of $g(x) = x^4$ around $x_0 = 0.25$:

\textbf{First-order (Taylor) approximation:}
\begin{equation}
g(x) \approx g(x_0) + g'(x_0)(x - x_0) = 0.25^4 + 4 \cdot 0.25^3\,(x - 0.25) = 0.0039 + 0.0625(x - 0.25).
\end{equation}
This linear model captures only the slope and provides no information about curvature. Minimising it yields no finite minimum (it is unbounded below).

\textbf{Second-order approximation:}
\begin{equation}
g(x) \approx g(x_0) + g'(x_0)(x - x_0) + \tfrac{1}{2}g''(x_0)(x - x_0)^2 = 0.0039 + 0.0625(x - 0.25) + 0.375(x - 0.25)^2.
\end{equation}

The Newton update minimises this quadratic model: setting $\frac{d}{dx}[\text{second-order approx}] = 0$ gives:
\begin{equation}
x_{k+1} = x_k - \frac{g'(x_k)}{g''(x_k)} = x_k - \frac{4x_k^3}{12x_k^2} = x_k - \frac{x_k}{3} = \frac{2}{3}x_k.
\end{equation}
Starting from $x_0 = 0.25$: $x_1 = 0.167$, $x_2 = 0.111$, \ldots --- geometric convergence towards $x^{\star} = 0$.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_approximation.pdf}
\caption{Q3: Local approximations of $g(x) = x^4$ at $x_0 = 0.25$. The first-order approximation (tangent line) captures only local slope; it deviates rapidly and provides no useful curvature information for locating the minimum. The second-order (quadratic) approximation fits the function well in a neighbourhood of $x_0$ and its minimum provides a Newton step towards $x^{\star} = 0$. The quality of the second-order approximation degrades further from $x_0$ since $g''(x) = 12x^2$ varies with $x$, motivating the iterative Newton algorithm.}
\label{fig:q3_approx}
\end{figure}

\subsection{Newton's Method}

\begin{algorithm}[H]
\caption{Newton's Method with Damping}
\label{alg:newton}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, damping $\lambda$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $H_k \gets \nabla^2 f(x_k) + \lambda I$ \Comment{Tikhonov-regularised Hessian}
    \State $p_k \gets H_k^{-1}\, g_k$ \Comment{Newton direction (solve linear system)}
    \State $x_{k+1} \gets x_k - \alpha\, p_k$ \Comment{Damped Newton step}
\EndFor
\end{algorithmic}
\end{algorithm}

The Newton step $p_k = H_k^{-1} g_k$ scales the gradient by the inverse Hessian, effectively rescaling each direction by its curvature. This makes Newton's method \emph{affine invariant}: its performance is independent of the problem's conditioning. The damping factor $\lambda = 10^{-8}$ ensures numerical stability when $H_k$ is nearly singular, and the step size $\alpha \leq 1$ ensures stability far from the minimum where the quadratic approximation is inaccurate.

\begin{proposition}[Quadratic Convergence of Newton's Method]
For a twice continuously differentiable function $f$ with $\nabla^2 f$ Lipschitz continuous near $x^{\star}$ (i.e., $\|\nabla^2 f(x) - \nabla^2 f(y)\| \leq M\|x-y\|$), if $x_0$ is sufficiently close to $x^{\star}$ and $\alpha = 1$, the undamped Newton iterates satisfy:
\begin{equation}
\|x_{k+1} - x^{\star}\| \leq \frac{M}{2\mu}\|x_k - x^{\star}\|^2,
\end{equation}
where $\mu = \lambda_{\min}(\nabla^2 f(x^{\star})) > 0$. This \emph{quadratic convergence} means the number of correct digits approximately doubles each iteration.
\end{proposition}

Settings: GD uses 80 iterations; Newton uses 20 iterations with Hessian damping $\lambda = 10^{-8}$.

\begin{table}[H]
\centering
\caption{Q3: Hyperparameters for GD and Newton.}
\label{tab:q3_params}
\begin{tabular}{lcccccc}
\toprule
 & \multicolumn{2}{c}{\textbf{Bench.\ A}} & \multicolumn{2}{c}{\textbf{Bench.\ B}} & \multicolumn{2}{c}{\textbf{Bench.\ C}} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5} \cmidrule(lr){6-7}
 & GD & Newton & GD & Newton & GD & Newton \\
\midrule
$\alpha$ & 0.08 & 1.0 & 0.06 & 0.85 & 0.001 & 0.22 \\
Iterations & 80 & 20 & 80 & 20 & 80 & 20 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Convergence Comparison}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_A.pdf}
\caption{Q3: Newton vs GD on Benchmark A. Newton with $\alpha = 1.0$ converges in \textbf{exactly one iteration} to $J = 0.4826$. This is the theoretical prediction: for a pure quadratic $J(\theta) = \frac{1}{2}\theta^T H\theta - b^T\theta + c$, the Newton step with $\alpha=1$ gives $\theta_1 = \theta_0 - H^{-1}\nabla J(\theta_0) = H^{-1}b = \theta^{\star}_{\text{OLS}}$ regardless of the starting point. GD requires 80 iterations to reach comparable accuracy because it converges at rate $(1 - 1/\kappa)^k \approx (1 - 1/1.12)^k$.}
\label{fig:q3_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_B.pdf}
\caption{Q3: Newton vs GD on Benchmark B. Newton converges in approximately 5 iterations due to the near-quadratic structure. The Hessian $H(x) = \text{diag}(2 - \sin(x_1), 10)$ varies slowly with $x_1$, so Newton's curvature estimate remains accurate across steps, enabling fast convergence. Final value: $f = 0.7244$, matched by GD but in $>10\times$ fewer gradient evaluations. Each Newton step requires $O(d^2)$ work to solve the linear system, versus $O(d)$ for GD; in 2D the overhead is negligible.}
\label{fig:q3_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_C.pdf}
\caption{Q3: Newton vs GD on Benchmark C. Newton with $\alpha = 0.22$ reaches $f = 0.686$ in 20 iterations, versus GD's $f = 3.732$ in 80 iterations. The damped step ($\alpha < 1$) is essential here: the Rosenbrock Hessian changes rapidly along the trajectory, and a full Newton step ($\alpha = 1$) would overshoot significantly. The damping effectively restricts the step to the region where the local quadratic model is accurate (the ``trust region''). Nevertheless, Newton's ability to detect and exploit the dominant $x_1 \leftrightarrow x_2$ coupling via the off-diagonal Hessian term $h_{12} = -400x_1$ allows far more efficient valley traversal.}
\label{fig:q3_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q3: Final objective values and converged positions after respective iteration budgets.}
\label{tab:q3_final}
\begin{tabular}{lccccc}
\toprule
\textbf{Benchmark} & \textbf{GD iters} & \textbf{GD $f$} & \textbf{Newton iters} & \textbf{Newton $f$} & \textbf{Newton final $x$} \\
\midrule
A & 80 & 0.4827 & 20 & 0.4826 & $(3.028, 4.017)$ \\
B & 80 & 0.7244 & 20 & 0.7244 & $(0.582, 2.000)$ \\
C & 80 & 3.7319 & 20 & 0.6864 & $(0.183, 0.020)$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{remark}
For Rosenbrock, Newton does not yet reach the global minimum $(1,1)$ in 20 iterations, as the damped step $\alpha = 0.22$ is conservative. However, the Newton iterate at $(0.183, 0.020)$ already approaches the parabolic valley floor ($x_2 \approx x_1^2 = 0.033$), having traversed a large portion of the curved path from $(-1, 1)$.
\end{remark}

\subsection{Contour Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q3_contour_B.pdf}
\caption{Q3: Newton vs GD trajectories on Benchmark B. Newton takes 5 large, well-directed steps that account for the 7:1 curvature difference between the two coordinate directions, reaching the minimum with minimal zigzag. GD's trajectory reflects the anisotropy: the $x_2$ component (high curvature) converges first, followed by gradual convergence of $x_1$.}
\label{fig:q3_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q3_contour_C.pdf}
\caption{Q3: Newton vs GD trajectories on Rosenbrock. Newton's trajectory traverses the curved valley far more efficiently than GD, which is constrained to tiny steps. The Newton direction $H^{-1}g$ naturally accounts for the strong coupling between $x_1$ and $x_2$ encoded in the off-diagonal Hessian entry $h_{12} = -400x_1$.}
\label{fig:q3_contour_C}
\end{figure}

\subsection{Update Magnitude and Newton Decrement}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q3_update_magnitude.pdf}
\caption{Q3: Update magnitude $\|x_{k+1} - x_k\|$ vs iteration for GD and Newton. Newton's update magnitude decreases rapidly, reflecting the approach to quadratic convergence: once in a neighbourhood of $x^{\star}$, the update norm approximately halves per iteration. For Benchmark A, Newton's update drops to near machine precision after one step (since the loss is exactly quadratic). GD's magnitude decreases linearly (geometric with rate $1-\mu/L$). On Rosenbrock, Newton maintains larger updates than GD throughout, indicating more productive steps despite the conservative step $\alpha=0.22$.}
\label{fig:q3_update}
\end{figure}

A practical stopping criterion for Newton's method is the \emph{Newton decrement} $\lambda(x)$, defined as:
\begin{equation}
\lambda(x)^2 = \nabla f(x)^T H(x)^{-1} \nabla f(x) = \|H(x)^{-1/2} \nabla f(x)\|^2.
\end{equation}
The Newton decrement satisfies $f(x) - f^{\star} \leq \lambda(x)^2 / 2$ (from the quadratic upper bound), making it a reliable, scale-invariant certificate of proximity to the optimum. In practice, Newton iterations are stopped when $\lambda(x)^2/2 < \epsilon$ for some small tolerance $\epsilon$. In this implementation, the stopping criterion is replaced by a fixed iteration budget.

\textbf{Computational cost analysis:} Each Newton iteration costs $O(d^3)$ for Cholesky factorisation of $H_k$ and $O(d^2)$ for back-substitution, compared to $O(d)$ per gradient descent step. For $d = 2$, this overhead is trivial. For large-scale problems with $d = 10^6$ (e.g., neural network weights), the $O(d^3)$ cost makes exact Newton infeasible, motivating quasi-Newton methods (L-BFGS) that approximate $H^{-1}$ using a limited memory rank-$m$ update.

% ============================================================
\section{Question 4: Derivative Approximation and Derivative-Free Optimisation \hfill [30 marks]}
\label{sec:q4}
% ============================================================

\subsection{Part A: Finite Difference and Nesterov Random Search (Benchmark B)}

\subsubsection{Finite Difference Gradient Approximation}

The forward finite-difference approximation replaces each partial derivative with:
\begin{equation}
\frac{\partial f}{\partial x_i}(x) \approx \frac{f(x + \delta e_i) - f(x)}{\delta}, \quad i = 1, \ldots, d.
\end{equation}
By Taylor expansion, the truncation error is:
\begin{equation}
\left|\frac{f(x + \delta e_i) - f(x)}{\delta} - \frac{\partial f}{\partial x_i}(x)\right| \leq \frac{\delta}{2}\left|\frac{\partial^2 f}{\partial x_i^2}(x)\right| + O(\delta^2).
\end{equation}
Additionally, finite-precision arithmetic introduces round-off error of order $\varepsilon_{\text{machine}}/\delta \approx 10^{-16}/\delta$. The total error is minimised at the optimal perturbation $\delta^{\star} \sim \sqrt{\varepsilon_{\text{machine}}} \approx 10^{-8}$.

In this experiment, $\delta = 0.05$ yields good gradient approximations (truncation error $\approx \frac{0.05}{2} \times 10 = 0.25$ for $f_{x_2x_2} = 10$, which is significant but workable), while $\delta = 0.8$ causes severe truncation error as the perturbation spans a region of strong curvature variation.

\subsubsection{Nesterov Random Search}

\begin{algorithm}[H]
\caption{Nesterov Random Search (Zeroth-Order)}
\label{alg:nrs}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, perturbation $\delta$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State Sample $u_k \sim \mathcal{N}(0, I)$, normalise: $u_k \gets u_k / \|u_k\|$
    \State $\hat{g}_k \gets \dfrac{f(x_k + \delta u_k) - f(x_k)}{\delta}\, u_k$ \Comment{Random directional derivative estimate}
    \State $x_{k+1} \gets x_k - \alpha\, \hat{g}_k$
\EndFor
\end{algorithmic}
\end{algorithm}

The expected value of the estimator satisfies $\mathbb{E}[\hat{g}_k] = \nabla f_\delta(x_k)$, where $f_\delta(x) = \mathbb{E}_{u}[f(x + \delta u)]$ is a smoothed version of $f$. The key property is that only \emph{two} function evaluations are needed per iteration regardless of dimension $d$, making it scalable to high-dimensional problems where the $O(d)$ cost of finite differences becomes prohibitive.

\subsubsection{Hyperparameter Settings and Results}

\begin{table}[H]
\centering
\caption{Q4A: Hyperparameter settings for derivative approximation methods on Benchmark B.}
\label{tab:q4a_params}
\begin{tabular}{lccccc}
\toprule
\textbf{Method} & $\alpha$ & $\delta$ & \textbf{Iterations} & \textbf{FE/iter} & \textbf{Total FE} \\
\midrule
Exact GD & 0.06 & --- & 120 & 1 & 120 \\
FD GD (good) & 0.08 & 0.05 & 120 & 3 & 360 \\
FD GD (poor) & 0.08 & 0.8 & 120 & 3 & 360 \\
Nesterov Random & 0.025 & 0.08 & 220 & 2 & 440 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q4_fval_B.pdf}
\caption{Q4A: Convergence comparison on Benchmark B. Exact GD converges fastest. FD GD with $\delta = 0.05$ closely tracks exact GD, confirming that the gradient approximation error is small enough not to significantly affect the optimisation trajectory. FD GD with $\delta = 0.8$ converges poorly: the step spans a region where the curvature of $f_{x_2x_2} = 10$ changes significantly, introducing systematic gradient error $\approx 0.8 \times 10 / 2 = 4$. Nesterov random search converges slowest: each iteration uses a single random directional projection of the gradient, equivalent to 1-coordinate GD on average over $d=2$ dimensions.}
\label{fig:q4_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q4_contour_B.pdf}
\caption{Q4A: Trajectories on Benchmark B. Exact GD and FD GD with $\delta = 0.05$ follow nearly identical paths, confirming gradient approximation fidelity. FD GD with $\delta = 0.8$ deviates significantly from the correct gradient direction due to large truncation error. The Nesterov random search trajectory is noisy and wandering, consistent with gradient estimates that project onto a random direction rather than the full gradient.}
\label{fig:q4_contour_B}
\end{figure}

\subsection{Part B: Derivative-Free Methods (Benchmark C)}

\subsubsection{Nelder--Mead Simplex Method}

The Nelder--Mead algorithm maintains a simplex of $n+1 = 3$ vertices in $\mathbb{R}^2$ and iteratively transforms it through four ordered operations:
\begin{enumerate}[nosep]
\item \textbf{Reflection} ($\alpha_r = 1.0$): Move the worst vertex $x_{n+1}$ through the centroid $\bar{x}$ of the remaining vertices: $x_r = \bar{x} + \alpha_r(\bar{x} - x_{n+1})$.
\item \textbf{Expansion} ($\gamma = 2.0$): If reflection improved significantly, try a further step: $x_e = \bar{x} + \gamma(\bar{x} - x_{n+1})$.
\item \textbf{Contraction} ($\rho = 0.5$): If reflection worsened, contract towards the centroid: $x_c = \bar{x} + \rho(x_r - \bar{x})$ (outside) or $x_c = \bar{x} + \rho(x_{n+1} - \bar{x})$ (inside).
\item \textbf{Shrink} ($\sigma = 0.5$): If contraction fails, contract all vertices towards the best: $x_i \gets x_1 + \sigma(x_i - x_1)$.
\end{enumerate}
No gradient information is required; the method relies entirely on function comparisons to guide the simplex. The initial simplex uses step size $0.35$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q4_nelder_mead_C.pdf}
\caption{Q4B: Nelder--Mead on Rosenbrock over 160 iterations. Left: the simplex centroid trajectory navigates the banana-shaped valley without gradient information. The simplex geometry adapts to the local landscape by shrinking in directions of high curvature and elongating along the valley. Right: the best objective value decreases steadily; the rate decreases near the minimum as the simplex must contract finely around the optimal point.}
\label{fig:q4_nm}
\end{figure}

\subsubsection{Grid Search}

Grid search exhaustively evaluates $f$ on a uniform $55 \times 55 = 3025$-point grid over $x_1 \in [-2, 2]$, $x_2 \in [-1, 3]$, with resolution $\Delta x_1 = \Delta x_2 \approx 0.073$. The best point found is the global minimum within the grid, guaranteed to be within $\Delta x_1^2 \times f_{x_1x_1}/2 + \Delta x_2^2 \times f_{x_2x_2}/2$ of the true minimum.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q4_grid_search_C.pdf}
\caption{Q4B: Grid search on Rosenbrock. Left: 3025 grid points cover the search region uniformly. The optimal grid point is near the true minimum $(1,1)$, limited by grid resolution. Right: the best-so-far objective decreases monotonically as progressively better grid points are scanned. Grid search guarantees coverage of the entire domain (unlike local methods) but requires $O(N^d)$ evaluations, making it intractable for $d > 3$ or $N > 100$.}
\label{fig:q4_grid}
\end{figure}

\subsection{Computational and Algorithmic Comparison}

\begin{table}[H]
\centering
\caption{Q4: Comparison of methods. FE = function evaluations per step, $d$ = dimension.}
\label{tab:q4_comparison}
\begin{tabular}{lccccc}
\toprule
\textbf{Method} & \textbf{Gradient} & \textbf{FE/step} & \textbf{Scalability} & \textbf{Convergence Rate} & \textbf{Accuracy} \\
\midrule
Exact GD & Exact & $1 + d$ & Good ($O(d)$) & $O(1/k)$ & Exact \\
FD GD ($\delta$ small) & Approx. & $d + 1$ & Good ($O(d)$) & $O(1/k)$ & High \\
FD GD ($\delta$ large) & Poor approx. & $d + 1$ & Good ($O(d)$) & $O(1/k)$, shifted & Low \\
Nesterov random & None & 2 & \textbf{Best} ($O(1)$) & $O(1/\sqrt{k})$ & Low \\
Nelder--Mead & None & $\leq 3$ & Low-dim ($d \leq 5$) & Empirical & Moderate \\
Grid search & None & $N^d$ total & Very poor ($O(N^d)$) & Global, $O(\Delta^2)$ & Grid-limited \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Key trade-off:} Methods requiring gradient information (GD, FD-GD) converge at $O(1/k)$ rates and are efficient when gradients are available or cheap to approximate. Derivative-free methods require more function evaluations per iteration but eliminate the need for gradient computation or code differentiation. Nelder--Mead is well-suited for smooth low-dimensional problems; grid search provides global coverage guarantees but at exponential cost; random search scales to high dimensions at the cost of slow, noisy convergence.

% ============================================================
\section{Question 5: Constrained Optimisation \hfill [30 marks]}
\label{sec:q5}
% ============================================================

\subsection{Problem Setup and Optimality Conditions}

Minimise Benchmark B subject to the constraint $x_1 \geq 0.5$, starting from the infeasible point $x_0 = (0.2, 4.0)$:
\begin{equation}
\min_{x \in \mathcal{F}} f(x) = (x_1-1)^2 + 5(x_2-2)^2 + \sin(x_1), \quad \mathcal{F} = \{x \in \mathbb{R}^2 : x_1 \geq 0.5\}.
\end{equation}

\textbf{Important observation:} The unconstrained minimiser of $f$ is at $x^{\star}_{\text{unc}} \approx (0.582, 2.0)$, which satisfies $x_1^{\star} = 0.582 > 0.5$. Therefore, the constraint is \emph{inactive} at the optimum, meaning the constrained minimum $x^{\star}_{\text{con}} = x^{\star}_{\text{unc}}$ and the KKT multiplier $\mu^{\star} = 0$. The constraint influences only the \emph{trajectory} --- methods starting from the infeasible point $(0.2, 4.0)$ must navigate differently to reach the same final point.

The KKT conditions for this problem are:
\begin{equation}
\nabla f(x^{\star}) + \mu^{\star} (-e_1) = 0, \quad \mu^{\star} \geq 0, \quad \mu^{\star}(0.5 - x_1^{\star}) = 0,
\end{equation}
where $e_1 = (1, 0)^T$. Since the constraint is inactive, $\mu^{\star} = 0$ and $\nabla f(x^{\star}) = 0$ --- identical to the unconstrained KKT condition.

\begin{algorithm}[H]
\caption{Projected Gradient Descent}
\label{alg:pgd}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, projection $\Pi_{\mathcal{F}}$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_{k+1} \gets x_k - \alpha\, \nabla f(x_k)$ \Comment{Unconstrained gradient step}
    \State $x_{k+1} \gets \Pi_{\mathcal{F}}(z_{k+1})$ \Comment{Project: $x_1 \leftarrow \max(0.5,\, x_1)$}
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{algorithm}[H]
\caption{Penalty Method}
\label{alg:penalty}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, penalty weight $\lambda$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $F_{\lambda}(x) \gets f(x) + \lambda \max(0,\, 0.5 - x_1)$ \Comment{Penalised objective}
    \State Compute $\nabla F_{\lambda}(x_k) = \nabla f(x_k) - \lambda \mathbf{1}[x_1 < 0.5]\, e_1$
    \State $x_{k+1} \gets x_k - \alpha\, \nabla F_{\lambda}(x_k)$
\EndFor
\end{algorithmic}
\end{algorithm}

The penalty method uses a non-smooth penalty term $\lambda \max(0, 0.5 - x_1)$, whose subgradient with respect to $x_1$ is $-\lambda \cdot \mathbf{1}[x_1 < 0.5]$. A large $\lambda$ strongly penalises infeasibility but requires a smaller step size $\alpha$ for stability (since the penalty gradient can be large near the constraint boundary).

Settings: Projected GD ($\alpha = 0.08$), Penalty ($\alpha = 0.05$ for $\lambda \leq 1.8$; $\alpha = 0.03$ for $\lambda = 4.5$), unconstrained GD ($\alpha = 0.07$). All run for 100 iterations.

\subsection{Results}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q5_fval.pdf}
\caption{Q5: Objective value $f(x_k)$ vs iteration. All methods converge to $f \approx 0.7244$ since the constraint is inactive at the optimum. The key differences are in convergence speed and the trajectory through infeasible space. Projected GD converges most directly (step into $\mathcal{F}$ immediately), while penalty methods with small $\lambda$ waste early iterations in the infeasible region.}
\label{fig:q5_fval}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q5_contour.pdf}
\caption{Q5: Contour plot with constraint boundary $x_1 = 0.5$ (dashed red) and infeasible region $x_1 < 0.5$ (shaded). The starting point $(0.2, 4.0)$ is infeasible. Projected GD immediately clips to $x_1 = 0.5$ and then optimises within $\mathcal{F}$, following a path along the boundary before moving to the interior once $x_1 > 0.5$ becomes natural. Penalty methods are gradually pulled towards feasibility with increasing $\lambda$ causing more aggressive deflection.}
\label{fig:q5_contour}
\end{figure}

\subsection{Constraint Violation Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q5_violation.pdf}
\caption{Q5: Constraint violation $\max(0, 0.5 - x_1)$ on logarithmic scale. Left: full 100 iterations. Right: first 30 iterations (zoomed). Projected GD achieves \textbf{zero violation from iteration 1}: the projection operator $\Pi_{\mathcal{F}}$ enforces $x_1 \geq 0.5$ as a hard constraint at every step. Penalty methods: with $\lambda = 4.5$, feasibility is achieved within $\sim$10 iterations; all methods eventually achieve feasibility since the unconstrained minimum is in the interior of $\mathcal{F}$.}
\label{fig:q5_violation}
\end{figure}

\subsection{Complete Results Summary}

\begin{table}[H]
\centering
\caption{Q5: Final positions, objective values, and constraint violations after 100 iterations. Since the unconstrained minimum $(0.582, 2.0)$ satisfies $x_1 > 0.5$, all methods converge to the same constrained minimum with zero final violation.}
\label{tab:q5_final}
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & $\alpha$ & \textbf{Final $x$} & \textbf{$f(x)$} & \textbf{Violation} \\
\midrule
Unconstrained GD & 0.07 & $(0.582, 2.000)$ & 0.7244 & 0.0000 \\
Projected GD & 0.08 & $(0.582, 2.000)$ & 0.7244 & 0.0000 \\
Penalty $\lambda = 0.15$ & 0.05 & $(0.582, 2.000)$ & 0.7244 & 0.0000 \\
Penalty $\lambda = 1.8$ & 0.05 & $(0.582, 2.000)$ & 0.7244 & 0.0000 \\
Penalty $\lambda = 4.5$ & 0.03 & $(0.583, 2.000)$ & 0.7244 & 0.0000 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Comparative Discussion}

\textbf{Projected GD} is the gold standard for simple constraints: it enforces feasibility as a hard constraint at every iteration, guarantees monotone decrease in $f$ within $\mathcal{F}$, and requires no tuning beyond the step size. For the box constraint $x_1 \geq 0.5$, projection is a trivial $\max$ operation: $\Pi_{\mathcal{F}}(x) = (\max(0.5, x_1), x_2)$. For complex constraints (e.g., $\|x\|_2 \leq 1$), efficient projection algorithms still exist, though at higher computational cost. The convergence rate of projected GD for convex $f$ with $L$-Lipschitz gradient is $O(1/k)$, matching gradient descent.

\textbf{Penalty methods} are simpler to implement (no projection oracle required) but soften the constraint. They impose a trade-off: too-small $\lambda$ leads to persistent infeasibility; too-large $\lambda$ requires a smaller step size for stability and may slow convergence by creating a steep gradient near the boundary. The optimal $\lambda$ depends on the problem scale and is problem-specific. As $\lambda \to \infty$, the penalty method converges to the constrained solution, but numerical conditioning deteriorates.

\textbf{Augmented Lagrangian (beyond this study):} A more sophisticated approach is the Augmented Lagrangian method (also known as the method of multipliers), which adds both a penalty term and a Lagrange multiplier estimate:
\begin{equation}
L_\rho(x, \mu) = f(x) + \mu(0.5 - x_1) + \frac{\rho}{2}\max(0, 0.5 - x_1)^2.
\end{equation}
Alternating between minimising over $x$ and updating $\mu \leftarrow \mu + \rho\max(0, 0.5 - x_k)$ achieves exact feasibility at finite $\rho$ (unlike pure penalty which requires $\rho \to \infty$), making it numerically superior to penalty methods while avoiding the computational cost of second-order projected methods.

\textbf{Key insight:} When the constraint is \emph{inactive} at the optimum (as here), both methods converge to the same final point. The methods differ primarily in their \emph{trajectory}: projected GD respects feasibility throughout, while penalty methods may take an infeasible shortcut before converging. For safety-critical applications requiring \emph{always-feasible} iterates, projected GD is mandatory. For problems where the constraint \emph{is} active at the optimum (e.g., $\min f(x)$ subject to $\|x\| \leq r$ with the unconstrained minimum outside the ball), the differences between methods would be more pronounced in the final solutions as well.

% ============================================================
\section{Question 6: Linear Programmes and Frank--Wolfe Algorithm \hfill [30 marks]}
\label{sec:q6}
% ============================================================

\subsection{Feasible Set and Linear Programme}

Feasible set: $\mathcal{X} = \{x \in \mathbb{R}^2 : 0.5 \leq x_1 \leq 5,\ -5 \leq x_2 \leq 10\}$, a box with diameter $D \approx 15.66$.

\textbf{Linear Programme:} For $f(x) = a^T x$ with $a = [1, 2]^T$, the minimum over $\mathcal{X}$ is obtained by minimising each coordinate independently (since the objective decomposes):
\begin{equation}
\min_{x \in \mathcal{X}} x_1 + 2x_2 = \min_{0.5 \leq x_1 \leq 5} x_1 + \min_{-5 \leq x_2 \leq 10} 2x_2 = 0.5 + 2(-5) = -9.5.
\end{equation}
Both components of $a$ are positive, so both coordinates are minimised at their lower bounds:
\begin{equation}
x^{\star} = (0.5,\, -5), \qquad f^{\star} = -9.5.
\end{equation}
This illustrates the fundamental theorem of linear programming: \emph{the optimum of a linear objective over a bounded polytope is attained at a vertex}. The linear objective has parallel level sets, and the minimum is at the vertex in the direction $-a$.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q6_lp.pdf}
\caption{Q6-I: Feasible region (blue box) and linear objective contours $f(x) = x_1 + 2x_2$. The contour lines are parallel with slope $-1/2$ (since $x_2 = (f - x_1)/2$). The minimum contour that still intersects $\mathcal{X}$ touches the lower-left corner $(0.5, -5)$, confirming the vertex optimum.}
\label{fig:q6_lp}
\end{figure}

\subsection{Frank--Wolfe Algorithm}

\begin{algorithm}[H]
\caption{Frank--Wolfe (Conditional Gradient)}
\label{alg:fw}
\begin{algorithmic}[1]
\Require Initial point $x_0 \in \mathcal{X}$, mixing parameter $\beta \in (0,1)$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_k \gets \arg\min_{x \in \mathcal{X}}\, \nabla f(x_k)^T x$ \Comment{Linear minimisation oracle (LMO)}
    \State $x_{k+1} \gets \beta\, x_k + (1 - \beta)\, z_k$ \Comment{Convex combination, $(1-\beta)$ step towards $z_k$}
\EndFor
\end{algorithmic}
\end{algorithm}

The Frank--Wolfe algorithm replaces the projection step of projected gradient descent with a \emph{linear minimisation} over $\mathcal{X}$. For box constraints, the LMO decomposes per coordinate:
\begin{equation}
(z_k)_i = \begin{cases} l_i & \text{if } (\nabla f(x_k))_i > 0 \\ u_i & \text{if } (\nabla f(x_k))_i \leq 0 \end{cases},
\end{equation}
requiring only $O(d)$ comparisons. Since $x_0 \in \mathcal{X}$ and $z_k \in \mathcal{X}$ at every step, $x_{k+1} = \beta x_k + (1-\beta) z_k \in \mathcal{X}$ by convexity --- \emph{iterates are always feasible without any projection}.

\begin{proposition}[Frank--Wolfe Convergence Rate]
For a convex differentiable function $f$ with $L$-Lipschitz gradient and the standard step size $\gamma_k = 2/(k+2)$ (corresponding to $\beta_k = k/(k+2)$):
\begin{equation}
f(x_k) - f^{\star} \leq \frac{2L D^2}{k+2} = O\!\left(\frac{LD^2}{k}\right),
\end{equation}
where $D = \max_{x, y \in \mathcal{X}} \|x - y\|$ is the diameter of $\mathcal{X}$. This $O(1/k)$ rate is optimal for Frank--Wolfe-type methods on general convex problems. Note that the bound depends on $D^2$: larger feasible sets slow convergence, in contrast to projection methods.
\end{proposition}

In the implementation, a fixed $\beta$ (rather than the decreasing $\gamma_k = 2/(k+2)$) is used, which modifies the convergence properties: the iterates may not achieve the $O(1/k)$ bound with fixed $\beta$, and residual oscillation near the optimum may persist.

\subsection{Interior Optimum: $f(x) = (x_1 - 1)^2 + (x_2 - 5)^2$}

The unconstrained minimum $(1, 5)$ lies inside $\mathcal{X}$ (since $0.5 \leq 1 \leq 5$ and $-5 \leq 5 \leq 10$). Two mixing parameters are tested: $\beta = 0.90$ (10\% step towards LP vertex per iteration) and $\beta = 0.985$ (1.5\% step).

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q6_fw_convergence.pdf}
\caption{Q6-II: Frank--Wolfe convergence for the interior optimum from $x_0 = (1, 1)$. With $\beta = 0.985$ (smaller steps towards $z_k$), the final objective ($f \approx 0.0045$) is lower than with $\beta = 0.90$ ($f \approx 0.0089$) after 180 iterations. Larger steps ($\beta = 0.90$, i.e., $\gamma_k = 0.10$) cause the iterate to jump more aggressively towards the LP vertices of $\mathcal{X}$, and $z_k$ oscillates between extreme points as the gradient direction alternates. The conservative $\beta = 0.985$ suppresses this oscillation, allowing smoother convergence at the cost of slower initial descent.}
\label{fig:q6_fw_conv}
\end{figure}

\subsection{Boundary Optimum: $f(x) = x_1^2 + x_2^2$}

The unconstrained minimum $(0, 0)$ lies outside $\mathcal{X}$ (since $x_1 \geq 0.5$). The constrained minimum is at the boundary point $(0.5, 0)$ with $f^{\star} = 0.25$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q6_fw_contour.pdf}
\caption{Q6: Frank--Wolfe trajectories. Left (interior): the iterate converges towards the interior minimum $(1, 5)$ from $x_0 = (1, 1)$. Both $\beta$ values reach the vicinity of $(1,5)$ but with different oscillation levels. Right (boundary): the iterate converges towards the constrained minimum $(0.5, 0)$. Frank--Wolfe naturally handles this boundary case: when $\nabla f(x_k) = 2x_k$ points in the positive $x_1$ direction, the LMO selects $z_{k,1} = l_1 = 0.5$, and convex combination gradually pulls $x_{k,1}$ towards 0.5.}
\label{fig:q6_fw_contour}
\end{figure}

\subsection{Evolution of $x_k$ and $z_k$}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q6_evolution.pdf}
\caption{Q6: Evolution of iterates $x_k$ and LP vertices $z_k$ vs iteration. Top-left ($\beta = 0.90$, interior): $x_k$ converges quickly but $z_k$ oscillates between extreme vertices of $\mathcal{X}$ as the gradient direction fluctuates --- a characteristic zig-zag pattern of Frank--Wolfe. Top-right ($\beta = 0.985$, interior): slower but smoother convergence with less $z_k$ oscillation. Bottom-left (boundary): $x_1^{(k)}$ converges to 0.5 and $x_2^{(k)}$ approaches 0. Bottom-right: convergence comparison showing the $O(1/k)$ trend for the interior cases.}
\label{fig:q6_evolution}
\end{figure}

\begin{table}[H]
\centering
\caption{Q6: Final Frank--Wolfe positions and objective values after respective iteration counts.}
\label{tab:q6_final}
\begin{tabular}{lcccc}
\toprule
\textbf{Case} & \textbf{Iters} & \textbf{Final $x$} & \textbf{$f(x)$} & \textbf{$f^{\star}$} \\
\midrule
Interior, $\beta = 0.90$ & 180 & $(1.066, 4.932)$ & 0.0089 & 0.0 \\
Interior, $\beta = 0.985$ & 180 & $(0.997, 5.067)$ & 0.0045 & 0.0 \\
Boundary, $\beta = 0.93$ & 140 & $(0.500, -0.337)$ & 0.3637 & 0.25 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Interior vs Boundary Behaviour}

\textbf{Interior optimum:} Frank--Wolfe converges but exhibits a characteristic ``zig-zag'' in the LP vertex sequence $z_k$. The LP vertex $z_k$ jumps between extreme points of $\mathcal{X}$ at each iteration since even small gradient changes can flip the sign of a component and select the opposite bound. The fixed-$\beta$ convex combination gradually averages these oscillations. The $O(1/k)$ convergence rate (confirmed experimentally in Figure~\ref{fig:rate_verify}) is slower than the $O(1/k^2)$ of Nesterov acceleration, but Frank--Wolfe's advantage lies in its \emph{projection-free} nature and the interpretability of iterates as sparse convex combinations of vertices.

\textbf{Boundary optimum:} Frank--Wolfe is particularly well-suited to boundary optima. When $x^{\star}$ lies on the boundary, the LP vertex $z_k$ frequently selects the closest constraint face, and the convex combination pulls the iterate towards the boundary naturally. The final value $f = 0.364$ (vs $f^{\star} = 0.25$) indicates the algorithm has not fully converged in 140 iterations; using the decreasing schedule $\gamma_k = 2/(k+2)$ rather than fixed $\beta = 0.93$ would provide the $O(1/k)$ convergence guarantee and improved terminal accuracy.

\textbf{Frank--Wolfe gap (duality gap):} A key practical advantage of Frank--Wolfe is its built-in optimality certificate, the \emph{Frank--Wolfe gap}:
\begin{equation}
h_k = \nabla f(x_k)^T (x_k - z_k) \geq f(x_k) - f^{\star} \geq 0.
\end{equation}
This follows from the convexity of $f$: $f^{\star} \geq f(x_k) + \nabla f(x_k)^T(x^{\star} - x_k) \geq f(x_k) - h_k$ (using the fact that $z_k$ minimises the linear objective). The FW gap provides an upper bound on the optimality gap without knowing $f^{\star}$, making it a practical stopping criterion. When $h_k < \epsilon$, the current iterate is guaranteed to be $\epsilon$-suboptimal.

\textbf{Frank--Wolfe vs Projected GD:} Projected GD achieves faster convergence (linear rate for strongly convex functions) but requires a projection operation. For simple constraint sets (box, $\ell_2$ ball), projection is $O(d)$ and preferred. For complex constraint sets --- such as the nuclear norm ball in matrix optimisation (projection requires SVD: $O(mn\min(m,n))$) or the flow polytope in network problems (projection requires LP: costly) --- Frank--Wolfe's LMO is substantially cheaper, making it the method of choice in large-scale structured optimisation.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/convergence_rate_loglog.pdf}
\caption{Convergence rate verification on log-log axes. Left: Frank--Wolfe with standard schedule $\gamma_k=2/(k+2)$ follows the $O(1/k)$ slope. Fixed $\beta$ schedules deviate and oscillate. Right: on a quadratic ($\kappa=50$), GD follows the $O(1/k)$ slope and Nesterov follows $O(1/k^2)$, empirically confirming both theoretical rates.}
\label{fig:fw_rate}
\end{figure}

% ============================================================
\section{Cross-Method Comparison}
\label{sec:crossmethod}
% ============================================================

To provide a unified perspective on all methods studied in Questions 1 and 2, the following figure runs all seven first-order methods (GD, Polyak, Adagrad, RMSprop, Heavy Ball, Nesterov, Adam) on all three benchmarks simultaneously for 150 iterations.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/all_methods_comparison.pdf}
\caption{Comprehensive comparison of all Q1+Q2 methods across all benchmarks (150 iterations). The key takeaways are: (i) On Benchmark A ($\kappa \approx 1.12$), all methods converge to the same floor and differences are negligible. (ii) On Benchmark B ($\kappa \approx 6.9$), methods requiring tuning (RMSprop, Adagrad) converge to slightly higher floors if not tuned precisely, while momentum methods (Nesterov, Heavy Ball) converge most smoothly. (iii) On Benchmark C ($\kappa \approx 2508$), the conditioning dominates: Polyak is best (when $f^{\star}$ is known), Nesterov is the best gradient-only method, and constant-step GD is worst. The figure illustrates the central lesson: \emph{algorithm selection should match problem conditioning} --- exotic methods offer little benefit over GD on easy problems but are essential on ill-conditioned ones.}
\label{fig:all_methods}
\end{figure}

\begin{table}[H]
\centering
\caption{Summary ranking of all methods: final objective values after 150 iterations on each benchmark. Lower is better; bold marks the best gradient-based method. Polyak marked $\dagger$ as it requires $f^{\star}$.}
\label{tab:crossmethod}
\begin{tabular}{lcccl}
\toprule
\textbf{Method} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} & \textbf{Key property} \\
\midrule
GD (baseline) & 0.4826 & 0.7244 & 3.39 & Simplest; stable \\
Polyak$^\dagger$ & diverges & diverges & \textbf{0.009} & Optimal with $f^{\star}$; fragile \\
Adagrad & \textbf{0.4826} & \textbf{0.7244} & 2.20 & Coordinate adaptation; monotone decay \\
RMSprop & 0.506 & 0.728 & 3.12 & Adaptive; non-monotone \\
Heavy Ball & \textbf{0.4826} & \textbf{0.7244} & 1.40 & Momentum; needs $\kappa$ estimate \\
Nesterov & \textbf{0.4826} & \textbf{0.7244} & \textbf{0.479} & \textbf{Optimal $O(1/k^2)$; preferred} \\
Adam & \textbf{0.4826} & \textbf{0.7244} & 1.58 & Robust; best for anisotropic NNs \\
\bottomrule
\end{tabular}
\end{table}

% ============================================================
\section{Conclusion}
\label{sec:conclusion}
% ============================================================

This study investigated six families of optimisation algorithms across problems of increasing difficulty, revealing systematic relationships between algorithm design, problem structure, and convergence behaviour.

\textbf{Summary of key findings:}

\begin{enumerate}[nosep]
\item \textbf{Problem conditioning dominates method selection.} On the well-conditioned Benchmark A ($\kappa \approx 1.12$), virtually all methods converge rapidly and differences are minimal. Benchmark C (Rosenbrock, $\kappa \approx 2508$) is the principal discriminator: only methods with access to curvature information (Newton), momentum along the valley direction (Nesterov), or exact knowledge of $f^{\star}$ (Polyak) show substantial improvements over standard gradient descent.

\item \textbf{Adaptive step sizes improve robustness at the cost of sensitivity.} Polyak achieves optimal convergence when $f^{\star}$ is exactly known but fails catastrophically under misspecification. Adagrad and RMSprop are more robust alternatives that require only per-coordinate curvature signals rather than the global minimum value.

\item \textbf{Momentum methods achieve optimal first-order complexity.} Nesterov acceleration realises the $O(1/k^2)$ bound, reducing iterations by a factor of $\approx 7\times$ on Rosenbrock compared to GD. Heavy Ball with appropriate $(\alpha, \beta)$ also achieves significant acceleration, especially when the condition number is known. Adam combines both momentum and per-coordinate adaptation, making it robust for anisotropic problems but less effective on highly nonlinear landscapes.

\item \textbf{Newton's method is transformative when applicable.} Solving the quadratic Benchmark A exactly in one step --- while GD requires $>30$ iterations --- highlights the value of curvature information. On Rosenbrock, damped Newton outperforms all first-order methods with $4\times$ fewer iterations. The $O(d^3)$ cost of solving the Newton system limits applicability to moderate-dimensional problems.

\item \textbf{Derivative-free methods incur a cost in convergence rate.} Finite-difference GD with small $\delta$ closely approximates exact GD at a $d$-fold increase in function evaluations. Nesterov random search reduces evaluations to 2 per step at the cost of $O(1/\sqrt{k})$ convergence. Nelder--Mead and grid search are viable only in low dimensions ($d \leq 5$ and $d \leq 3$, respectively).

\item \textbf{Constraint structure determines the method of choice.} Projected GD is preferred when projection is cheap and feasibility must be maintained at every iterate (e.g., safety-critical applications). Penalty methods are simpler to implement but require careful weight tuning and may temporarily violate constraints. When the unconstrained minimum is interior to the feasible set (as in this problem), all constrained methods converge to the same point and differ only in their convergence trajectory.

\item \textbf{Frank--Wolfe provides a projection-free alternative.} The $O(1/k)$ convergence rate is slower than projected GD but the LMO is often substantially cheaper than projection for structured constraint sets. For problems with sparse solution representations (e.g., low-rank matrices), Frank--Wolfe naturally produces structured iterates.
\end{enumerate}

\textbf{Overall recommendation:} For a new optimisation problem, begin with the simplest applicable method (GD or Adam for unconstrained; projected GD for simple constraints), then graduate to more sophisticated methods (Newton, Nesterov) if convergence is unsatisfactory. The choice should be guided by the problem's conditioning, the availability of gradient information, and the feasibility of second-order computation.

% ============================================================
\newpage
\appendix
\section{Python Code}
\label{app:code}
% ============================================================

The complete implementation is provided in the file \texttt{CS7DS2\_Final\_Code\_25336453.py}. Key algorithm implementations are reproduced below for reference.

\begin{lstlisting}[caption={Gradient Descent}]
def gradient_descent(grad_fn, loss_fn, x0, alpha, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for _ in range(n_iters):
        x = x - alpha * grad_fn(x)
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Polyak Step Size}]
def polyak_step(grad_fn, loss_fn, x0, f_star, eps, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        alpha_k = (loss_fn(x) - f_star) / (np.dot(g, g) + eps)
        alpha_hist.append(alpha_k)
        x = x - alpha_k * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Adagrad}]
def adagrad(grad_fn, loss_fn, x0, alpha0, eps, n_iters):
    x = x0.copy().astype(float)
    G = np.zeros_like(x)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        G += g**2
        eff_alpha = alpha0 / (np.sqrt(G) + eps)
        alpha_hist.append(np.mean(eff_alpha))
        x = x - eff_alpha * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={RMSprop}]
def rmsprop(grad_fn, loss_fn, x0, alpha0, beta, eps, n_iters):
    x = x0.copy().astype(float)
    v = np.zeros_like(x)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        v = beta * v + (1 - beta) * g**2
        eff_alpha = alpha0 / (np.sqrt(v) + eps)
        alpha_hist.append(np.mean(eff_alpha))
        x = x - eff_alpha * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Heavy Ball / Polyak Momentum}]
def heavy_ball(grad_fn, loss_fn, x0, alpha, beta, n_iters):
    x = x0.copy().astype(float)
    z = np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for _ in range(n_iters):
        g = grad_fn(x)
        z = beta * z + alpha * g
        x = x - z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Nesterov Momentum}]
def nesterov_momentum(grad_fn, loss_fn, x0, alpha, beta_max, n_iters):
    x = x0.copy().astype(float)
    z = np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for k in range(1, n_iters + 1):
        beta_k = min((k - 1) / (k + 2), beta_max)
        lookahead = x + beta_k * z
        g = grad_fn(lookahead)
        z = beta_k * z - alpha * g
        x = x + z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Adam Optimiser}]
def adam_optimiser(grad_fn, loss_fn, x0, alpha, beta1, beta2, eps, n_iters):
    x = x0.copy().astype(float)
    m_vec, v_vec = np.zeros_like(x), np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for t in range(1, n_iters + 1):
        g = grad_fn(x)
        m_vec = beta1 * m_vec + (1 - beta1) * g
        v_vec = beta2 * v_vec + (1 - beta2) * g**2
        m_hat = m_vec / (1 - beta1**t)
        v_hat = v_vec / (1 - beta2**t)
        x = x - alpha * m_hat / (np.sqrt(v_hat) + eps)
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Newton's Method with Damping}]
def newtons_method(grad_fn, hess_fn, loss_fn, x0, alpha, n_iters, damping=1e-8):
    x = x0.copy().astype(float)
    x_hist, f_hist, update_norms = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        H = hess_fn(x) if callable(hess_fn) else hess_fn
        H_reg = H + damping * np.eye(len(x))
        try:
            p = np.linalg.solve(H_reg, g)
        except np.linalg.LinAlgError:
            p = g
        update_norms.append(np.linalg.norm(alpha * p))
        x = x - alpha * p
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(update_norms)
\end{lstlisting}

\begin{lstlisting}[caption={Frank--Wolfe Algorithm}]
def frank_wolfe(grad_fn, loss_fn, x0, bounds, beta, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist, z_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        z = np.array([bounds[i][0] if g[i] > 0 else bounds[i][1]
                       for i in range(len(g))])
        z_hist.append(z.copy())
        x = beta * x + (1 - beta) * z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(z_hist)
\end{lstlisting}

\end{document}
