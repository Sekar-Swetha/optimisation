\documentclass[11pt,a4paper]{article}
\usepackage[margin=2.2cm]{geometry}
\usepackage{graphicx}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{float}
\usepackage{hyperref}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{listings}
\usepackage{fancyhdr}
\usepackage{microtype}

\hypersetup{colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue}

% Theorem-style environments
\newtheorem{proposition}{Proposition}
\newtheorem{remark}{Remark}

% Code listing style
\lstset{
  language=Python,
  basicstyle=\ttfamily\scriptsize,
  keywordstyle=\color{blue},
  commentstyle=\color{gray},
  stringstyle=\color{red},
  numbers=left,
  numberstyle=\tiny\color{gray},
  breaklines=true,
  frame=single,
  captionpos=b
}

\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small CS7DS2 -- Final Assignment}
\fancyhead[R]{\small Swetha Sekar (25336453)}
\fancyfoot[C]{\thepage}

\title{%
  \includegraphics[width=0.25\textwidth]{tcd_logo.jpg}\\[1em]
  \textbf{CS7DS2 -- Optimisation Algorithms for Data Analysis}\\[0.5em]
  \Large Final Assignment Report
}
\author{Swetha Sekar \\ Student ID: 25336453 \\ School of Computer Science and Statistics \\ Trinity College Dublin}
\date{April 2026}

\begin{document}
\maketitle
\thispagestyle{empty}

\begin{abstract}
This report presents a comprehensive comparative study of first-order, second-order, derivative-free, and constrained optimisation algorithms applied to three benchmark problems of increasing difficulty: a well-conditioned linear regression quadratic loss (condition number $\kappa \approx 1.12$), a mildly anisotropic toy neural network loss (condition number $\kappa \approx 6.90$), and the highly ill-conditioned Rosenbrock function (condition number $\kappa \approx 2508$). Six categories of methods are investigated across six questions: (Q1) adaptive step-size methods --- Polyak, Adagrad, RMSprop, and Heavy Ball; (Q2) momentum-based methods --- Nesterov acceleration and Adam --- together with mini-batch stochastic gradient descent; (Q3) Newton's method with Hessian-based local quadratic approximations; (Q4) derivative-free approaches including finite differences, Nesterov random search, Nelder--Mead simplex, and grid search; (Q5) constrained optimisation via projected gradient descent and quadratic penalty methods; and (Q6) the Frank--Wolfe conditional gradient algorithm for box-constrained problems. Each method is evaluated in terms of convergence speed, trajectory geometry, sensitivity to hyperparameters, and theoretical convergence guarantees. The central finding is that second-order curvature information is indispensable for ill-conditioned problems such as Rosenbrock, whereas adaptive first-order methods suffice for well-conditioned or mildly anisotropic objectives. The Polyak step demonstrates the critical importance of specifying $f^{\star}$ accurately, and the Frank--Wolfe algorithm is shown to be well-suited for linear-constrained settings where projection is costly but linear subproblems are cheap.
\end{abstract}

\tableofcontents
\newpage

% ============================================================
\section{Benchmark Problems and Experimental Setup}
\label{sec:benchmarks}
% ============================================================

All experiments use a fixed random seed (\texttt{np.random.seed(42)}) for full reproducibility.

\subsection{Benchmark A: Linear Regression Quadratic Loss}
\label{sec:bench_A}

A synthetic dataset with $m = 1000$ samples and $d = 2$ parameters is generated as follows:
\begin{equation}
\theta^{\star} = \begin{bmatrix} 3 \\ 4 \end{bmatrix}, \quad
x^{(i)} \sim \mathcal{N}(0, I_2), \quad
y^{(i)} = (\theta^{\star})^T x^{(i)} + \varepsilon^{(i)}, \quad
\varepsilon^{(i)} \sim \mathcal{N}(0, 1).
\end{equation}
The loss function is the mean squared error:
\begin{equation}
J(\theta) = \frac{1}{2m} \sum_{i=1}^{m} \left(\theta^T x^{(i)} - y^{(i)}\right)^2
= \frac{1}{2m}\|X\theta - y\|_2^2.
\end{equation}
This is a $\mu$-strongly convex and $L$-smooth quadratic with constant Hessian $H = \frac{1}{m}X^T X$. The gradient is $\nabla J(\theta) = \frac{1}{m}X^T(X\theta - y)$. The ordinary least-squares solution is $\theta_{\text{OLS}} = H^{-1}\bigl(\frac{1}{m}X^Ty\bigr) \approx (3.028, 4.017)^T$.

\textbf{Smoothness and conditioning.} Numerically, the Hessian eigenvalues are $\mu = 0.9247$ and $L = 1.0325$, giving a \emph{condition number} $\kappa = L/\mu \approx 1.12$. This is an exceptionally well-conditioned problem: gradient descent with step size $\alpha \leq 1/L \approx 0.969$ converges at rate $((\kappa-1)/(\kappa+1))^k \approx 0.057^k$ under the optimal step. The irreducible loss floor due to observation noise is $J^{\star} = J(\theta_{\text{OLS}}) \approx 0.4826$. Starting point: $\theta_0 = (0, 0)^T$.

\subsection{Benchmark B: Toy Neural Network Loss}
\label{sec:bench_B}
\begin{equation}
\label{eq:bench_B}
f(x_1, x_2) = (x_1 - 1)^2 + 5(x_2 - 2)^2 + \sin(x_1).
\end{equation}
This function combines a quadratic bowl with a sinusoidal perturbation along $x_1$. The gradient and Hessian are:
\begin{align}
\nabla f(x) &= \bigl[2(x_1 - 1) + \cos(x_1),\; 10(x_2 - 2)\bigr]^T, \\
\nabla^2 f(x) &= \operatorname{diag}\!\bigl(2 - \sin(x_1),\; 10\bigr).
\end{align}
The global minimum is at $x^{\star} \approx (0.5824,\, 2.0000)^T$ with $f^{\star} \approx 0.7244$, determined by solving $2(x_1-1)+\cos(x_1) = 0$ numerically. At the optimum, the Hessian has eigenvalues $\mu \approx 1.450$ and $L = 10$, giving $\kappa \approx 6.90$. The sinusoidal perturbation introduces mild nonlinearity, making this a non-quadratic but nearly convex problem. Starting point: $x_0 = (-1, 4)^T$.

\subsection{Benchmark C: Rosenbrock Function}
\label{sec:bench_C}
\begin{equation}
\label{eq:rosenbrock}
f(x_1, x_2) = (1 - x_1)^2 + 100(x_2 - x_1^2)^2.
\end{equation}
The global minimum is at $(1, 1)^T$ with $f^{\star} = 0$. The Hessian is:
\begin{equation}
\nabla^2 f(x) = \begin{bmatrix} 2 + 1200x_1^2 - 400x_2 & -400x_1 \\ -400x_1 & 200 \end{bmatrix}.
\end{equation}
At the optimum $(1,1)^T$, the Hessian eigenvalues are approximately $0.40$ and $1002$, giving $\kappa \approx 2508$. At the starting point $(-1, 1)^T$, the eigenvalues are similarly $0.40$ and $1002$, with maximum stable gradient descent step $\alpha \leq 1/L \approx 0.001$. This extreme ill-conditioning, combined with the narrow curved valley defined by $x_2 = x_1^2$, makes Rosenbrock one of the most widely used test functions for assessing algorithm robustness. Starting point: $x_0 = (-1, 1)^T$.

\subsection{Benchmark D: Feasible Set for Constrained Problems}
\label{sec:bench_D}
For Questions~5 and~6: $\mathcal{X} = \{x \in \mathbb{R}^2 : 0.5 \leq x_1 \leq 5,\; -5 \leq x_2 \leq 10\}$ (axis-aligned box).

\subsection{Summary of Benchmark Properties}
\begin{table}[H]
\centering
\caption{Key properties of the three benchmark functions. $\kappa = L/\mu$ is the condition number evaluated at the optimum (or for the linear case, globally). GD stable rate uses the optimal step $\alpha^* = 2/(L+\mu)$.}
\label{tab:benchmarks}
\begin{tabular}{lcccccc}
\toprule
\textbf{Benchmark} & $\mu$ & $L$ & $\kappa$ & \textbf{GD stable rate} & \textbf{Min.\ $f^{\star}$} & \textbf{Start $x_0$} \\
\midrule
A (Lin.\ Reg.) & 0.925 & 1.033 & 1.12 & 0.057 & 0.4826 & $(0,0)^T$ \\
B (Toy NN) & 1.450 & 10.00 & 6.90 & 0.747 & 0.7244 & $(-1,4)^T$ \\
C (Rosenbrock) & 0.40 & 1002 & 2508 & 0.999 & 0 & $(-1,1)^T$ \\
\bottomrule
\end{tabular}
\end{table}

% ============================================================
\section{Question 1: Adaptive Step-Size Methods \hfill [30 marks]}
\label{sec:q1}
% ============================================================

\subsection{Theoretical Background}

Gradient descent with a fixed step $\alpha$ satisfies, for a $\mu$-strongly convex and $L$-smooth function:
\begin{equation}
\label{eq:gd_rate}
f(x_{k+1}) - f^{\star} \leq \left(1 - \frac{2\alpha\mu L}{\mu + L}\right)^{k+1} (f(x_0) - f^{\star}).
\end{equation}
The challenge is that an appropriate $\alpha$ requires knowing $L$ (or $\mu$), which may not be available. Adaptive methods address this by computing effective step sizes from gradient information.

\subsection{Algorithm Formulations}

Four adaptive methods are compared against a constant step-size gradient descent baseline across all three benchmarks for $N = 120$ iterations.

\begin{algorithm}[H]
\caption{Polyak Step Size}
\label{alg:polyak}
\begin{algorithmic}[1]
\Require Initial point $x_0$, optimal value $f^{\star}$, tolerance $\varepsilon > 0$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $\alpha_k \gets \dfrac{f(x_k) - f^{\star}}{\|g_k\|^2 + \varepsilon}$ \Comment{Optimal step if $f^\star$ known exactly}
    \State $x_{k+1} \gets x_k - \alpha_k\, g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{proposition}[Polyak Convergence]
If $f$ is convex with minimum $f^{\star}$ known exactly, the Polyak step satisfies $f(x_{k+1}) \leq f(x_k)$ and $f(x_k) \to f^{\star}$ at a rate that adapts to the local curvature without requiring knowledge of $L$ or $\mu$. If $f^{\star}$ is misspecified ($\hat{f}^{\star} < f_{\text{true}}^{\star}$), the denominator $\|g_k\|^2$ shrinks as $x_k$ approaches the optimum while the numerator $f(x_k) - \hat{f}^{\star}$ remains bounded away from zero, causing $\alpha_k \to \infty$ and divergence.
\end{proposition}

\begin{algorithm}[H]
\caption{Adagrad}
\label{alg:adagrad}
\begin{algorithmic}[1]
\Require Initial point $x_0$, base learning rate $\alpha_0 > 0$, $\varepsilon > 0$, iterations $N$
\State $G_0 \gets \mathbf{0} \in \mathbb{R}^d$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $G_{k+1} \gets G_k + g_k \odot g_k$ \Comment{Per-coordinate cumulative squared gradient}
    \State $x_{k+1} \gets x_k - \dfrac{\alpha_0}{\sqrt{G_{k+1}} + \varepsilon} \odot g_k$ \Comment{$\odot$ denotes elementwise product}
\EndFor
\end{algorithmic}
\end{algorithm}

Adagrad's effective step in coordinate $i$ decays as $\alpha_0 / \sqrt{\sum_{j=0}^{k} (g_j)_i^2}$, which is $O(1/\sqrt{k})$. This monotone decay prevents the step from ever growing, which ensures stability but can cause premature slowdown on non-stationary or long-horizon problems.

\begin{algorithm}[H]
\caption{RMSprop}
\label{alg:rmsprop}
\begin{algorithmic}[1]
\Require Initial point $x_0$, learning rate $\alpha_0$, decay $\beta \in (0,1)$, $\varepsilon > 0$, iterations $N$
\State $v_0 \gets \mathbf{0}$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $v_{k+1} \gets \beta\, v_k + (1 - \beta)\, g_k \odot g_k$ \Comment{Exponential moving average of $g^2$}
    \State $x_{k+1} \gets x_k - \dfrac{\alpha_0}{\sqrt{v_{k+1}} + \varepsilon} \odot g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

RMSprop replaces Adagrad's cumulative accumulation with an exponential moving average (decay factor $\beta$), retaining only recent curvature information. The effective window length is approximately $1/(1-\beta)$ iterations, so $\beta = 0.9$ averages the last $\sim$10 squared gradients.

\begin{algorithm}[H]
\caption{Polyak Momentum / Heavy Ball}
\label{alg:heavyball}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha > 0$, momentum $\beta \in [0, 1)$, iterations $N$
\State $z_0 \gets \mathbf{0}$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_{k+1} \gets \beta\, z_k + \alpha\, \nabla f(x_k)$ \Comment{Accumulated momentum term}
    \State $x_{k+1} \gets x_k - z_{k+1}$
\EndFor
\end{algorithmic}
\end{algorithm}

For a $\mu$-strongly convex quadratic with optimal parameters $\alpha^* = 4/(\sqrt{L}+\sqrt{\mu})^2$ and $\beta^* = ((\sqrt{L}-\sqrt{\mu})/(\sqrt{L}+\sqrt{\mu}))^2$, the Heavy Ball method achieves a convergence rate of $(({\sqrt{\kappa}-1})/({\sqrt{\kappa}+1}))^k$ per step, matching Nesterov's optimal rate for quadratics. However, this optimality does not extend to nonconvex problems.

\subsection{Hyperparameter Settings}

\begin{table}[H]
\centering
\caption{Q1: Hyperparameter settings for each method and benchmark. All methods run for 120 iterations.}
\label{tab:q1_params}
\begin{tabular}{llccc}
\toprule
\textbf{Method} & \textbf{Parameter} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & $\alpha$ & 0.08 & 0.06 & 0.0012 \\
\midrule
Polyak & $f^{\star}$ & 0 & 0 & 0 \\
 & $\varepsilon$ & $10^{-4}$ & $10^{-4}$ & $10^{-3}$ \\
\midrule
Adagrad & $\alpha_0$ & 1.8 & 1.2 & 0.45 \\
 & $\varepsilon$ & $10^{-5}$ & $10^{-5}$ & $10^{-5}$ \\
\midrule
RMSprop & $\alpha_0$ & 0.22 & 0.14 & 0.0035 \\
 & $\beta$ & 0.9 & 0.9 & 0.9 \\
 & $\varepsilon$ & $10^{-5}$ & $10^{-5}$ & $10^{-5}$ \\
\midrule
Heavy Ball & $\alpha$ & 0.045 & 0.035 & 0.0008 \\
 & $\beta$ & 0.88 & 0.90 & 0.86 \\
\bottomrule
\end{tabular}
\end{table}

\begin{remark}
For Benchmark C, the maximum stable gradient descent step is $1/L \approx 0.001$ (since $L \approx 1002$ at the Rosenbrock optimum). The chosen GD step $\alpha = 0.0012$ slightly exceeds this theoretical bound but remains empirically stable due to the conservative Hessian estimate far from the optimum.
\end{remark}

\subsection{Convergence Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_A.pdf}
\caption{Q1: Convergence on Benchmark A (Linear Regression, $\kappa \approx 1.12$). GD, Adagrad, and Heavy Ball all converge to the noise floor $J^{\star} \approx 0.4826$ within the first few dozen iterations, reflecting the well-conditioned nature of this problem. RMSprop converges to a slightly higher value ($J \approx 0.5027$) because its exponentially averaged step-size estimate oscillates near the minimum rather than decaying monotonically. The Polyak step \textbf{diverges} ($J \approx 2.72$): with $\hat{f}^{\star} = 0$ while the true minimum is $J^{\star} \approx 0.4826$, the numerator $f(x_k) - 0$ does not decrease to zero, so $\alpha_k = (f(x_k) - 0) / (\|\nabla f(x_k)\|^2 + \varepsilon) \to \infty$ as $\|\nabla f\| \to 0$, producing growing oscillations.}
\label{fig:q1_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_B.pdf}
\caption{Q1: Convergence on Benchmark B (Toy Neural Network, $\kappa \approx 6.90$). The same Polyak misspecification causes divergence here ($f \approx 24.07$). Adagrad and Heavy Ball match GD's final accuracy ($f \approx 0.7244$), while RMSprop achieves a very similar result ($f \approx 0.7271$). The moderate anisotropy ($\kappa \approx 6.90$) does not pose severe difficulties for any method with a correctly chosen step size. Heavy Ball's momentum ($\beta = 0.90$) provides smooth, direct convergence.}
\label{fig:q1_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_C.pdf}
\caption{Q1: Convergence on Benchmark C (Rosenbrock, $\kappa \approx 2508$). Here $\hat{f}^{\star} = 0$ coincides with the true minimum, so the Polyak step is well-specified and achieves the best convergence ($f \approx 0.009$): as $f(x_k) \to 0$ and $\|\nabla f(x_k)\| \to 0$, the ratio $\alpha_k = f(x_k)/\|\nabla f(x_k)\|^2$ remains well-behaved. Heavy Ball achieves $f \approx 1.40$ by exploiting momentum to traverse the narrow Rosenbrock valley faster than GD ($f \approx 3.51$). Adagrad reaches $f \approx 2.20$: its per-coordinate adaptive scaling corrects for the large curvature imbalance, but its decaying step slows progress. RMSprop ($f \approx 3.12$) shows limited advantage with the very small base rate $\alpha_0 = 0.0035$ imposed by Rosenbrock's ill-conditioning.}
\label{fig:q1_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q1: Final objective values after 120 iterations. Bold indicates best per benchmark.}
\label{tab:q1_final}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & 0.4826 & 0.7244 & 3.5142 \\
Polyak & 2.7189 & 24.0659 & \textbf{0.0094} \\
Adagrad & \textbf{0.4826} & \textbf{0.7244} & 2.2034 \\
RMSprop & 0.5027 & 0.7271 & 3.1204 \\
Heavy Ball & 0.4826 & 0.7244 & 1.4003 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Key finding:} The Polyak step size is highly sensitive to the accuracy of $f^{\star}$. When the specified $\hat{f}^{\star} = 0$ deviates from the true minimum (Benchmarks A and B), the method diverges. On Benchmark C where $\hat{f}^{\star} = 0$ is exact, Polyak achieves the best convergence among all methods, confirming its theoretical optimality under correct specification.

\subsection{Contour Plots with Optimisation Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q1_contour_B.pdf}
\caption{Q1: Trajectories on Benchmark B. The elliptical contours reflect the 6.9:1 curvature ratio between the $x_2$ and $x_1$ directions ($L = 10$, $\mu \approx 1.45$). GD and Adagrad follow quasi-direct paths towards the minimum because the curvature imbalance is moderate. Heavy Ball shows characteristic momentu\-m overshoot near the minimum but recovers quickly. The Polyak trajectory diverges outward due to $f^{\star}$ misspecification.}
\label{fig:q1_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q1_contour_C.pdf}
\caption{Q1: Trajectories on Benchmark C (Rosenbrock). The characteristic banana-shaped valley $x_2 \approx x_1^2$ is clearly visible. All methods rapidly descend to the valley floor in the first few iterations (exploiting the large gradient normal to the valley), then progress slowly along the floor towards $(1,1)$ (where the gradient is small). The Polyak trajectory (with correct $\hat{f}^{\star} = 0$) advances furthest along the valley due to its adaptive, curvature-sensitive step size. GD with $\alpha = 0.0012$ barely advances because the small step required for transverse stability severely limits longitudinal progress.}
\label{fig:q1_contour_C}
\end{figure}

\subsection{Adaptive Step-Size Evolution}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_A.pdf}
\caption{Q1: Step-size evolution on Benchmark A. The Polyak step exhibits pathological growth: as $\|\nabla f(x_k)\| \to 0$ near the OLS solution while $f(x_k) - 0 \approx 0.483$ remains nonzero, $\alpha_k$ grows without bound. Adagrad decays monotonically as $O(1/\sqrt{k})$ due to cumulative squared-gradient accumulation. RMSprop stabilises at a moderate level because its exponential forgetting (window $\approx 10$ iterations for $\beta=0.9$) tracks recent curvature rather than accumulating historical gradients.}
\label{fig:q1_stepsize_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_B.pdf}
\caption{Q1: Step-size evolution on Benchmark B. The Polyak step diverges as on Benchmark A. Adagrad decays rapidly in early iterations when large gradients are encountered (the starting point $(-1,4)$ is far from the minimum), then settles to a small effective rate once the iterates approach the minimum. RMSprop maintains a moderate, slowly-varying effective step throughout.}
\label{fig:q1_stepsize_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_C.pdf}
\caption{Q1: Step-size evolution on Benchmark C. With correct $\hat{f}^{\star} = 0$, the Polyak step decreases smoothly: both $f(x_k)$ and $\|\nabla f(x_k)\|$ tend to zero, but the ratio $f(x_k)/\|\nabla f(x_k)\|^2$ remains bounded and eventually decays. This well-behaved schedule explains Polyak's superior Rosenbrock convergence. Adagrad and RMSprop exhibit very small effective steps due to the large initial gradients in the Rosenbrock bowl.}
\label{fig:q1_stepsize_C}
\end{figure}

\subsection{Comparative Analysis}

\begin{itemize}[nosep]
\item \textbf{Convergence speed:} When $f^{\star}$ is known exactly, Polyak converges fastest. Otherwise, Heavy Ball and Adagrad match or exceed the GD baseline. The well-conditioned Benchmark A ($\kappa \approx 1.12$) is easy for all methods.
\item \textbf{Smoothness:} RMSprop produces the smoothest convergence curves due to exponential averaging. Heavy Ball exhibits characteristic overshoot followed by damped oscillation.
\item \textbf{Stability:} GD is most stable but slowest on ill-conditioned problems. Polyak is unstable when $f^{\star}$ is misspecified. Adagrad is uniformly stable due to its monotonically decreasing effective step, though premature slowdown limits its use on long-horizon problems.
\item \textbf{Practical implication:} In practice, exact $f^{\star}$ is rarely known, limiting Polyak's utility to specific structured problems. RMSprop and Adagrad are more robust, and Heavy Ball provides a useful middle ground between speed and stability.
\end{itemize}

% ============================================================
\section{Question 2: Momentum and Stochastic Methods \hfill [30 marks]}
\label{sec:q2}
% ============================================================

\subsection{Theoretical Background}

Gradient descent converges at a linear rate bounded by $(\kappa-1)/(\kappa+1)$ per step. For $\kappa \approx 2508$ (Rosenbrock), this gives a rate of $\approx 0.999$, requiring thousands of iterations to reduce the error by a factor of 10. Nesterov's accelerated gradient achieves the optimal rate of $(1 - 1/\sqrt{\kappa})^k$ for smooth strongly convex problems, requiring only $O(\sqrt{\kappa})$ iterations instead of $O(\kappa)$. This is the fundamental motivation for momentum-based methods.

\subsection{Algorithm Formulations}

\begin{algorithm}[H]
\caption{Nesterov Momentum / Acceleration}
\label{alg:nesterov}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha > 0$, max momentum $\beta_{\max} \in [0,1)$, iterations $N$
\State $z_0 \gets \mathbf{0}$
\For{$k = 1, 2, \ldots, N$}
    \State $\beta_k \gets \min\!\left(\dfrac{k-1}{k+2},\ \beta_{\max}\right)$ \Comment{Momentum schedule: $\beta_k \to 1$ as $k \to \infty$}
    \State $\tilde{x}_k \gets x_k + \beta_k\, z_k$ \Comment{Lookahead point (extrapolation)}
    \State $z_{k+1} \gets \beta_k\, z_k - \alpha\, \nabla f(\tilde{x}_k)$ \Comment{Momentum update using lookahead gradient}
    \State $x_{k+1} \gets x_k + z_{k+1}$
\EndFor
\end{algorithmic}
\end{algorithm}

The key distinction from Heavy Ball is the \emph{lookahead step}: the gradient is evaluated at the extrapolated point $\tilde{x}_k = x_k + \beta_k z_k$ rather than at $x_k$ itself. This ``corrective lookahead'' allows the algorithm to anticipate overshoot before it occurs. The schedule $\beta_k = (k-1)/(k+2)$ implements Nesterov's optimal momentum sequence, achieving the $O(1/k^2)$ convergence rate for convex functions and $O(1/\sqrt{\kappa})^k$ for strongly convex functions. This represents a quadratic improvement in iteration count over gradient descent.

\begin{algorithm}[H]
\caption{Adam Optimiser}
\label{alg:adam}
\begin{algorithmic}[1]
\Require Initial point $x_0$, learning rate $\alpha > 0$, moments $\beta_1, \beta_2 \in [0,1)$, $\varepsilon > 0$, iterations $N$
\State $m_0 \gets \mathbf{0},\; v_0 \gets \mathbf{0}$
\For{$t = 1, 2, \ldots, N$}
    \State $g_t \gets \nabla f(x_t)$
    \State $m_t \gets \beta_1\, m_{t-1} + (1 - \beta_1)\, g_t$ \Comment{First moment: EMA of gradients}
    \State $v_t \gets \beta_2\, v_{t-1} + (1 - \beta_2)\, g_t \odot g_t$ \Comment{Second moment: EMA of $g^2$}
    \State $\hat{m}_t \gets m_t / (1 - \beta_1^t)$ \Comment{Bias correction: removes initialisation bias}
    \State $\hat{v}_t \gets v_t / (1 - \beta_2^t)$
    \State $x_{t+1} \gets x_t - \alpha\, \hat{m}_t / (\sqrt{\hat{v}_t} + \varepsilon)$
\EndFor
\end{algorithmic}
\end{algorithm}

Adam combines gradient momentum (via $m_t$, analogous to Heavy Ball) with per-coordinate adaptive scaling (via $v_t$, analogous to RMSprop). The bias correction in $\hat{m}_t$ and $\hat{v}_t$ corrects for the zero initialisation of the moment vectors, which otherwise biases early steps towards zero. Typical parameters are $\beta_1 = 0.9$, $\beta_2 = 0.999$, $\varepsilon = 10^{-8}$. While Adam works well empirically for machine learning, it lacks the $O(1/k^2)$ convergence guarantee of Nesterov; indeed, it can fail to converge on simple convex problems with adversarial step sequences.

\subsection{Hyperparameter Settings}

\begin{table}[H]
\centering
\caption{Q2: Hyperparameter settings. Nesterov and Adam run for 150 iterations.}
\label{tab:q2_params}
\begin{tabular}{llccc}
\toprule
\textbf{Method} & \textbf{Parameter} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & $\alpha$ & 0.08 & 0.06 & 0.0012 \\
\midrule
Nesterov & $\alpha$ & 0.06 & 0.035 & 0.0007 \\
 & $\beta_{\max}$ & 0.90 & 0.92 & 0.90 \\
\midrule
Adam & $\alpha$ & 0.12 & 0.08 & 0.006 \\
 & $\beta_1$ & 0.82 & 0.80 & 0.80 \\
 & $\beta_2$ & 0.999 & 0.999 & 0.999 \\
 & $\varepsilon$ & $10^{-8}$ & $10^{-8}$ & $10^{-8}$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Convergence Comparison}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_A.pdf}
\caption{Q2: Convergence on Benchmark A ($\kappa \approx 1.12$). All three methods converge to $J^{\star} \approx 0.4826$. The near-unit condition number makes this problem trivially easy for all methods: the theoretical GD convergence rate is already $\approx 0.057$ per step, leaving little room for momentum to demonstrate improvement. All methods reach the noise floor within the first $\sim$50 iterations.}
\label{fig:q2_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_B.pdf}
\caption{Q2: Convergence on Benchmark B ($\kappa \approx 6.90$). Both Nesterov and Adam converge to $f^{\star} \approx 0.7244$. Adam's per-coordinate adaptive scaling handles the 6.9:1 curvature ratio effectively. Nesterov's lookahead gradient evaluations prevent overshoot across the narrower $x_1$ direction, resulting in fewer oscillations than GD.}
\label{fig:q2_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_C.pdf}
\caption{Q2: Convergence on Benchmark C ($\kappa \approx 2508$). The Rosenbrock function reveals the starkest differences. Nesterov achieves $f \approx 0.479$ in 150 iterations, a $7\times$ improvement over GD ($f \approx 3.388$), by accumulating momentum along the consistent gradient direction in the valley floor. Adam reaches $f \approx 1.578$: while its per-coordinate normalisation corrects for the curvature imbalance, the nonlinear curvature variation of the Rosenbrock valley (the Hessian changes significantly along the valley) reduces the effectiveness of the fixed-window second-moment estimate, limiting acceleration compared to Nesterov.}
\label{fig:q2_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q2: Final objective values after 150 iterations.}
\label{tab:q2_final}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & 0.4826 & 0.7244 & 3.3880 \\
Nesterov & 0.4826 & 0.7244 & \textbf{0.4788} \\
Adam & 0.4826 & 0.7244 & 1.5785 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Contour Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q2_contour_B.pdf}
\caption{Q2: Trajectories on Benchmark B. Nesterov's trajectory exhibits the characteristic smooth arc of accelerated methods: the lookahead avoids the zig-zag pattern that GD displays on anisotropic surfaces (where the step is too large for the $x_1$ direction but too small for $x_2$). Adam takes more direct, diagonal steps by independently scaling each coordinate according to recent squared-gradient magnitudes.}
\label{fig:q2_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q2_contour_C.pdf}
\caption{Q2: Trajectories on Benchmark C. All methods descend rapidly to the valley floor (the dominant gradient direction is transverse to the valley), but differ dramatically in their progress \emph{along} the floor. Nesterov's momentum builds acceleration in the consistent longitudinal direction toward $(1,1)$, achieving substantially further progress than GD within the same number of iterations. Adam also advances further than GD, but oscillates more due to the rapidly varying Hessian causing instability in the second-moment estimate.}
\label{fig:q2_contour_C}
\end{figure}

\subsection{Mini-Batch Stochastic Gradient Descent}
\label{sec:sgd}

Mini-batch SGD on Benchmark A uses learning rate $\alpha = 0.06$, batch sizes $b \in \{5, 40\}$, 50 epochs, with data indices randomly permuted at each epoch. The gradient estimate for a mini-batch $\mathcal{B}$ is:
\begin{equation}
g_{\mathcal{B}}(\theta) = \frac{1}{|\mathcal{B}|} \sum_{i \in \mathcal{B}} (x^{(i)T}\theta - y^{(i)}) x^{(i)},
\end{equation}
which satisfies $\mathbb{E}[g_{\mathcal{B}}(\theta)] = \nabla J(\theta)$ (unbiased), with variance $\propto \sigma^2 / |\mathcal{B}|$ where $\sigma^2$ is the per-sample gradient variance.

\begin{algorithm}[H]
\caption{Mini-Batch SGD}
\label{alg:sgd}
\begin{algorithmic}[1]
\Require Data $(X, y)$, initial $\theta_0$, learning rate $\alpha$, batch size $b$, epochs $E$
\For{$e = 1, 2, \ldots, E$}
    \State Randomly permute data indices
    \For{each mini-batch $\mathcal{B}$ of size $b$}
        \State $g \gets \frac{1}{b} X_{\mathcal{B}}^T(X_{\mathcal{B}}\theta - y_{\mathcal{B}})$
        \State $\theta \gets \theta - \alpha\, g$
    \EndFor
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_sgd_batch.pdf}
\caption{Q2: Mini-batch SGD with different batch sizes on Benchmark A. With $b = 5$, each epoch performs $1000/5 = 200$ gradient updates and converges faster in terms of epoch count due to the higher update frequency. However, the smaller batch means higher gradient variance, producing loss oscillations near the minimum. With $b = 40$, each epoch has $1000/40 = 25$ updates with lower variance, yielding smoother but slower per-epoch progress. The variance of the stochastic gradient is $\text{Var}[g_{\mathcal{B}}] \propto 1/b$, explaining the $\sqrt{8}$-fold difference in noise level between $b=5$ and $b=40$. Both batch sizes converge to the same noise floor since the fixed step size $\alpha=0.06$ does not decay.}
\label{fig:q2_sgd_batch}
\end{figure}

\subsection{SGD with Increased Observation Noise}

The experiment is repeated with output noise scaled by a factor of 6 ($\varepsilon^{(i)} \sim \mathcal{N}(0, 36)$ instead of $\mathcal{N}(0,1)$). The irreducible loss floor scales as $J^{\star} \propto \sigma^2$, so a $6\times$ noise increase raises the floor by $36\times$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q2_sgd_noisy.pdf}
\caption{Q2: Effect of observation noise on SGD convergence. Left: original noise ($\sigma = 1$). Right: high noise ($\sigma = 6$). Increasing the observation noise by factor 6 raises the irreducible loss floor by factor $\approx 36$ (from $J^{\star} \approx 0.483$ to $J^{\star} \approx 17.4$). Under high noise, both batch sizes converge to approximately the same elevated floor, and the relative advantage of smaller batches (more updates per epoch) diminishes. This occurs because the dominant source of gradient noise shifts from mini-batch sampling to observation noise, i.e.\ $\text{Var}[g_{\mathcal{B}}] \approx \sigma^2/b + \sigma_{\text{obs}}^2$: when $\sigma_{\text{obs}}$ dominates, the batch size $b$ has negligible impact on convergence speed.}
\label{fig:q2_sgd_noisy}
\end{figure}

\subsection{Comparative Analysis}

Nesterov momentum provides the strongest theoretical guarantees and the best empirical performance on Rosenbrock, confirming the $O(\sqrt{\kappa})$ iteration savings relative to gradient descent for ill-conditioned problems. Adam is highly effective for neural network training where the gradient variance across coordinates differs greatly (explaining its near-universal adoption in deep learning), but its adaptive per-coordinate scaling provides less benefit on the relatively smooth Rosenbrock surface. Mini-batch SGD demonstrates the fundamental variance--speed trade-off: smaller batches provide more updates at the cost of noisier gradients, and this trade-off becomes less important when observation noise is large.

% ============================================================
\section{Question 3: Newton's Method and Local Approximation \hfill [30 marks]}
\label{sec:q3}
% ============================================================

\subsection{Local Approximations of $g(x) = x^4$}

At a point $x_0$, the $k$-th order Taylor expansion of a smooth function $g$ is:
\begin{equation}
g(x) \approx \sum_{j=0}^{k} \frac{g^{(j)}(x_0)}{j!} (x - x_0)^j.
\end{equation}

\textbf{First-order (tangent line) approximation} at $x_0 = 0.25$:
\begin{equation}
\hat{g}_1(x) = g(x_0) + g'(x_0)(x - x_0) = (0.25)^4 + 4(0.25)^3(x - 0.25)
\approx 0.0039 + 0.0625(x - 0.25).
\end{equation}

\textbf{Second-order (parabolic) approximation:}
\begin{equation}
\hat{g}_2(x) = \hat{g}_1(x) + \tfrac{1}{2}g''(x_0)(x - x_0)^2
= 0.0039 + 0.0625(x - 0.25) + 0.375(x - 0.25)^2.
\end{equation}
since $g''(x) = 12x^2$ gives $g''(0.25) = 12(0.0625) = 0.75$.

The Newton step minimises $\hat{g}_2$ by setting $\hat{g}_2'(x) = 0$:
\begin{equation}
x_{k+1} = x_k - \frac{g'(x_k)}{g''(x_k)}.
\end{equation}
For $g(x) = x^4$, this gives $x_{k+1} = x_k - \frac{4x_k^3}{12x_k^2} = x_k - \frac{x_k}{3} = \frac{2}{3}x_k$, which converges geometrically to 0. Newton's method applied to minimising $g(x) = x^4$ is equivalent to solving $g'(x) = 4x^3 = 0$, which has a zero of multiplicity 3, leading to linear rather than quadratic convergence for this specific case.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_approximation.pdf}
\caption{Q3: Local approximations of $g(x) = x^4$ at $x_0 = 0.25$. The first-order approximation (blue dashed, tangent line) correctly captures the local slope but deviates rapidly from the true function as $|x - x_0|$ grows. The second-order approximation (red dash-dot, parabola) captures local curvature and fits well in a neighbourhood of radius $\sim|x_0|$ around the expansion point. Crucially, the parabola has a finite minimum at $x \approx 0.167$, which is the Newton step's suggested update. This illustrates the core principle of Newton's method: instead of stepping opposite to the gradient by a fixed amount, it steps to the minimiser of the local quadratic model, adapting the step size to the local curvature.}
\label{fig:q3_approx}
\end{figure}

\subsection{Newton's Method with Damping}

\begin{algorithm}[H]
\caption{Newton's Method with Damping and Hessian Regularisation}
\label{alg:newton}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha > 0$, damping $\lambda \geq 0$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $H_k \gets \nabla^2 f(x_k) + \lambda I$ \Comment{Tikhonov regularisation ensures $H_k \succ 0$}
    \State Solve $H_k p_k = g_k$ for $p_k$ (Newton direction)
    \State $x_{k+1} \gets x_k - \alpha\, p_k$ \Comment{Damped Newton step; $\alpha=1$ is full Newton}
\EndFor
\end{algorithmic}
\end{algorithm}

Newton's method has \emph{local quadratic convergence}: in a neighbourhood of a non-degenerate minimiser where $\nabla^2 f \succ 0$, the iterate satisfies:
\begin{equation}
\|x_{k+1} - x^{\star}\| \leq C\|x_k - x^{\star}\|^2,
\end{equation}
for some constant $C > 0$. This superlinear convergence rate means the number of accurate digits roughly doubles each iteration near the optimum, contrasted with gradient descent's linear convergence rate.

Settings: GD uses $N_{\text{GD}} = 80$ iterations; Newton uses $N_{\text{N}} = 20$ iterations with Hessian regularisation $\lambda = 10^{-8}$ (negligible numerically).

\begin{table}[H]
\centering
\caption{Q3: Hyperparameters for GD and Newton.}
\label{tab:q3_params}
\begin{tabular}{lcccccc}
\toprule
 & \multicolumn{2}{c}{\textbf{Bench.\ A}} & \multicolumn{2}{c}{\textbf{Bench.\ B}} & \multicolumn{2}{c}{\textbf{Bench.\ C}} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5} \cmidrule(lr){6-7}
 & GD & Newton & GD & Newton & GD & Newton \\
\midrule
$\alpha$ & 0.08 & 1.0 & 0.06 & 0.85 & 0.001 & 0.22 \\
Iterations & 80 & 20 & 80 & 20 & 80 & 20 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Convergence Comparison}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_A.pdf}
\caption{Q3: Newton vs GD on Benchmark A. Newton's method with $\alpha = 1.0$ (full Newton step) converges in \textbf{a single iteration} to $J \approx 0.4826$. This is guaranteed by theory: for a quadratic objective $J(\theta) = \frac{1}{2}\theta^T H\theta - b^T\theta + c$, the Newton step is exactly $p = H^{-1}\nabla J(\theta) = \theta - \theta^{\star}$, so one step reaches the minimiser regardless of the starting point. GD requires 80 iterations to converge to comparable accuracy because the step $\alpha = 0.08 \ll 1/L \approx 0.97$ is highly conservative.}
\label{fig:q3_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_B.pdf}
\caption{Q3: Newton vs GD on Benchmark B. Newton converges in approximately 5 iterations. The Hessian $H(x) = \operatorname{diag}(2-\sin(x_1), 10)$ varies slowly near the optimum (since $\sin$ is approximately linear over small regions), so each Newton step remains a good approximation. The damped step $\alpha = 0.85$ prevents potential overshoot due to the non-quadratic $\sin(x_1)$ term. Final value: Newton $f = 0.7244$, matching GD's final value in a fraction of the iterations.}
\label{fig:q3_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_C.pdf}
\caption{Q3: Newton vs GD on Benchmark C. Newton with $\alpha = 0.22$ (heavily damped) reaches $f = 0.686$ in 20 iterations --- a dramatic improvement over GD's $f = 3.732$ in 80 iterations. The damped step is necessary because the Rosenbrock Hessian changes rapidly along the valley: full Newton steps ($\alpha = 1$) would follow the local quadratic model too aggressively and overshoot, since the model is only accurate in a small neighbourhood. Nevertheless, even heavily damped Newton steps exploit curvature far more effectively than gradient descent, which is constrained to $\alpha \leq 0.001$ by the large second eigenvalue $L \approx 1002$.}
\label{fig:q3_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q3: Final objective values and converged positions after respective iteration counts.}
\label{tab:q3_final}
\begin{tabular}{lcccc}
\toprule
\textbf{Benchmark} & \textbf{GD $f$ (80 it.)} & \textbf{Newton $f$ (20 it.)} & \textbf{Newton final $x$} & \textbf{True $x^{\star}$}\\
\midrule
A & 0.4827 & 0.4826 & $(3.028, 4.017)$ & $(3.028, 4.017)$ \\
B & 0.7244 & 0.7244 & $(0.582, 2.000)$ & $(0.582, 2.000)$ \\
C & 3.7319 & 0.6864 & $(0.183, 0.020)$ & $(1.000, 1.000)$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Contour Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q3_contour_B.pdf}
\caption{Q3: Newton vs GD trajectories on Benchmark B. Newton takes large, well-directed steps that account for the curvature difference between coordinates (factor of 10 between $x_2$ and $x_1$ eigenvalues), reaching the minimum in approximately 5 steps. GD takes many small steps in a zig-zag pattern reflecting the anisotropic curvature: the component along the high-curvature $x_2$ direction converges quickly, but the low-curvature $x_1$ direction progresses slowly under the conservative step.}
\label{fig:q3_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q3_contour_C.pdf}
\caption{Q3: Newton vs GD trajectories on Rosenbrock. Newton's trajectory exploits the Hessian's information about the valley geometry, following the floor of the banana-shaped valley more faithfully. GD, constrained to tiny steps ($\alpha = 0.001$) by stability requirements, makes negligible progress along the valley floor in 80 iterations. The dramatic difference in trajectory quality underscores why second-order methods are essential for highly ill-conditioned problems.}
\label{fig:q3_contour_C}
\end{figure}

\subsection{Update Magnitude}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q3_update_magnitude.pdf}
\caption{Q3: Update magnitude $\|x_{k+1} - x_k\|$ vs iteration for GD and Newton across all three benchmarks. Newton on Benchmark A shows a step of finite size followed by immediate machine-precision convergence (superlinear in one step for quadratics). On Benchmarks B and C, Newton's update magnitude decreases rapidly after the first few large steps, consistent with quadratic convergence in the region where the quadratic model is accurate. GD's update magnitude decreases slowly and linearly on all benchmarks, reflecting its linear convergence rate. On Rosenbrock, Newton maintains larger effective steps than GD despite using fewer iterations, indicating more productive movement per step.}
\label{fig:q3_update}
\end{figure}

\subsection{Comparative Analysis}

Newton's method is transformative for quadratic problems (single-step convergence) and highly effective for near-quadratic problems (B: $\sim$5 steps). For Rosenbrock, damping is essential: the rapidly varying Hessian means the quadratic model is accurate only locally, and undamped Newton steps would diverge. The computational cost per step is $O(d^3)$ for the Hessian solve, compared to $O(d)$ for gradient descent, but the superlinear convergence rate means far fewer steps are needed, making Newton the method of choice for low-dimensional, smooth, non-degenerate problems where gradient computation is cheap.

% ============================================================
\section{Question 4: Derivative Approximation and Derivative-Free Optimisation \hfill [30 marks]}
\label{sec:q4}
% ============================================================

\subsection{Part A: Finite Difference and Nesterov Random Search (Benchmark B)}

\subsubsection{Finite Difference Gradient Approximation}

When analytic gradients are unavailable, the partial derivative can be approximated using the forward finite difference:
\begin{equation}
\frac{\partial f}{\partial x_i}(x) \approx \frac{f(x + \delta e_i) - f(x)}{\delta}.
\end{equation}
By Taylor expansion: $f(x + \delta e_i) = f(x) + \delta \partial_i f(x) + \frac{\delta^2}{2}\partial_{ii}^2 f(x) + O(\delta^3)$, so the approximation error is:
\begin{equation}
\left|\frac{\partial f}{\partial x_i}(x) - \frac{f(x + \delta e_i) - f(x)}{\delta}\right| = \frac{\delta}{2}|\partial_{ii}^2 f(x)| + O(\delta^2).
\end{equation}
This $O(\delta)$ truncation error is compounded by floating-point cancellation errors of order $O(\varepsilon_{\text{mach}}/\delta)$ (since $f(x+\delta e_i)$ and $f(x)$ are nearly equal for small $\delta$). Balancing these yields an optimal $\delta^{\star} \sim \sqrt{\varepsilon_{\text{mach}}} \approx 10^{-8}$ for double precision. The experiments test $\delta = 0.05$ (close to optimal for this scale) and $\delta = 0.8$ (too large, dominated by truncation error).

\subsubsection{Nesterov Random Search}

\begin{algorithm}[H]
\caption{Nesterov Random Search (Zeroth-Order)}
\label{alg:nrs}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha > 0$, perturbation $\delta > 0$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State Sample $u_k \sim \mathcal{N}(0, I_d)$; normalise $\hat{u}_k \gets u_k / \|u_k\|$
    \State $\hat{g}_k \gets \dfrac{f(x_k + \delta \hat{u}_k) - f(x_k)}{\delta}\, \hat{u}_k$ \Comment{Random directional gradient estimate}
    \State $x_{k+1} \gets x_k - \alpha\, \hat{g}_k$
\EndFor
\end{algorithmic}
\end{algorithm}

This zeroth-order method estimates a directional derivative along a uniformly random unit direction $\hat{u}_k$, requiring only \emph{two} function evaluations per iteration regardless of dimension $d$. The key identity is $\mathbb{E}_u[\hat{g}_k] = \nabla_\delta f(x_k)$ (a smoothed gradient), where the smoothing radius is $\delta$. Nesterov and Spokoiny (2017) show this method converges at rate $O(d/\sqrt{k})$ for smooth non-convex problems, confirming the dimension-dependence of the convergence rate.

\subsubsection{Results}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q4_fval_B.pdf}
\caption{Q4A: Convergence comparison on Benchmark B. Exact GD converges fastest. FD GD with $\delta = 0.05$ closely tracks exact GD, with the gradient approximation error bounded by $(\delta/2) \cdot \max_i |\partial_{ii}^2 f(x)| \approx 0.025 \times 10 = 0.25$, small relative to the gradient magnitude. FD GD with $\delta = 0.8$ converges poorly: the truncation error ($\delta/2 \cdot L \approx 4$) exceeds the true gradient in many iterations, producing inaccurate gradient directions. Nesterov random search converges slowest: sampling a single random direction per step provides only a noisy one-dimensional projection of the $d=2$ gradient, with high variance even at the correct optimum direction.}
\label{fig:q4_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q4_contour_B.pdf}
\caption{Q4A: Trajectories on Benchmark B. Exact GD and good FD ($\delta = 0.05$) follow nearly identical paths, confirming that the gradient approximation error is negligible relative to the gradient magnitude. Poor FD ($\delta = 0.8$) deviates due to inaccurate gradient directions from truncation error, particularly in the $x_1$ direction where the second derivative $\partial_{11}^2 f = 2-\sin(x_1) \approx 1.84$ is smaller than $L_{x_2} = 10$ but the error $0.4 \times 1.84 \approx 0.74$ is still significant. Nesterov random search follows a noisy, meandering path reflecting the high variance of single-direction gradient estimates.}
\label{fig:q4_contour_B}
\end{figure}

\subsection{Part B: Derivative-Free Methods (Benchmark C)}

\subsubsection{Nelder--Mead Simplex Method}

The Nelder--Mead algorithm maintains a simplex of $n+1 = 3$ vertices in $\mathbb{R}^2$ and iteratively transforms it through four operations:
\begin{enumerate}[nosep]
\item \textbf{Reflection} ($\rho_r = 1.0$): Move worst vertex $x_{n+1}$ through the centroid $\bar{x}$ of the remaining vertices: $x_r = \bar{x} + 1.0(\bar{x} - x_{n+1})$.
\item \textbf{Expansion} ($\gamma = 2.0$): If reflection improved beyond the best, try $x_e = \bar{x} + 2.0(\bar{x} - x_{n+1})$.
\item \textbf{Contraction} ($\rho = 0.5$): If reflection worsened, contract: $x_c = \bar{x} + 0.5(x_r - \bar{x})$ or $x_c = \bar{x} + 0.5(x_{n+1} - \bar{x})$.
\item \textbf{Shrink} ($\sigma = 0.5$): Contract all vertices toward the best: $x_i \gets x_1 + 0.5(x_i - x_1)$.
\end{enumerate}
Settings: initial simplex step $= 0.35$, $N = 160$ iterations. No gradient information is required.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q4_nelder_mead_C.pdf}
\caption{Q4B: Nelder--Mead on Rosenbrock. Left: the simplex centroid trajectory navigates the curved valley without gradient information. The simplex contracts and orients itself along the valley floor, demonstrating the algorithm's ability to exploit geometric function information through the simplex shape. Right: the best objective value decreases steadily (note log scale) as the simplex explores, contracts, and focuses near the minimum. Nelder--Mead is effective for low-dimensional, smooth objectives but lacks theoretical convergence guarantees: it can stagnate on degenerate simplices.}
\label{fig:q4_nm}
\end{figure}

\subsubsection{Grid Search}

Grid search evaluates the objective on a uniform $55 \times 55 = 3025$ point grid over $x_1 \in [-2, 2]$, $x_2 \in [-1, 3]$, with grid spacing $\Delta x \approx 0.073$ in each coordinate.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q4_grid_search_C.pdf}
\caption{Q4B: Grid search on Rosenbrock. Left: the 3025 grid points uniformly cover the search region; the best point is near the true optimum $(1,1)$, with a grid resolution gap of at most $\sqrt{2} \times 0.073 \approx 0.10$ in Euclidean distance. Right: the best-so-far curve shows step-wise improvement as the row-major scan encounters progressively better points. Grid search guarantees finding the global optimum to within grid resolution, but its $O(N^d)$ cost (3025 evaluations for a $55\times 55$ grid in $d=2$) makes it infeasible for $d > 4$: a $55^3$ grid in $d=3$ would require $\sim$166,000 evaluations, and $55^{10}$ in $d=10$ would require $\sim 10^{17}$ evaluations.}
\label{fig:q4_grid}
\end{figure}

\subsection{Computational Comparison}

\begin{table}[H]
\centering
\caption{Q4: Comparison of derivative-free methods. FE = function evaluations per iteration, $d$ = dimension. Gradient info column refers to gradient information used per step.}
\label{tab:q4_comparison}
\begin{tabular}{lccccc}
\toprule
\textbf{Method} & \textbf{Gradient Info} & \textbf{FE/iter} & \textbf{Scalability} & \textbf{Convergence Rate} & \textbf{Accuracy} \\
\midrule
Exact GD & Full gradient & 1 & $O(d)$/iter & $O(1/k)$ linear & High \\
FD GD ($\delta$ small) & Approx.\ gradient & $d + 1$ & $O(d^2)$/iter & $O(1/k)$ & High \\
FD GD ($\delta$ large) & Poor approx. & $d + 1$ & $O(d^2)$/iter & Degraded & Low \\
Nesterov random & None (0th order) & 2 & $O(d/\sqrt{k})$ & $O(d/\sqrt{k})$ & Low \\
Nelder--Mead & None & $\leq n+1 = 3$ & Low-dim only & No guarantee & Moderate \\
Grid search & None & $N^d$ total & $O(N^d)$ & Grid-limited & Grid-limited \\
\bottomrule
\end{tabular}
\end{table}

% ============================================================
\section{Question 5: Constrained Optimisation \hfill [30 marks]}
\label{sec:q5}
% ============================================================

\subsection{Problem Formulation and KKT Conditions}

The constrained problem is:
\begin{equation}
\min_{x \in \mathbb{R}^2} f(x) = (x_1-1)^2 + 5(x_2-2)^2 + \sin(x_1)
\quad \text{subject to} \quad x_1 \geq 0.5.
\end{equation}
This is equivalent to $\min_{x} f(x)$ s.t.\ $g(x) = 0.5 - x_1 \leq 0$. The KKT necessary conditions for a local minimum $x^{\star}$ are:
\begin{align}
\nabla f(x^{\star}) + \lambda^{\star} \nabla g(x^{\star}) &= 0, \label{eq:kkt_stationarity}\\
g(x^{\star}) &\leq 0, \quad \lambda^{\star} \geq 0, \quad \lambda^{\star} g(x^{\star}) = 0. \label{eq:kkt_comp_slack}
\end{align}
The unconstrained minimiser of Benchmark B is $x^{\star}_{\text{unc}} \approx (0.582, 2.0)$, which satisfies $x^{\star}_{1, \text{unc}} = 0.582 \geq 0.5$. Therefore, the constraint $x_1 \geq 0.5$ is \emph{inactive} at the unconstrained solution, and the constrained and unconstrained minima coincide: $\lambda^{\star} = 0$ (complementary slackness), $x^{\star}_{\text{con}} = (0.582, 2.0)$, $f^{\star} = 0.7244$.

The optimisation starts from the infeasible point $x_0 = (0.2, 4.0)$, which violates the constraint ($x_1 = 0.2 < 0.5$).

\begin{algorithm}[H]
\caption{Projected Gradient Descent}
\label{alg:pgd}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha > 0$, projection $\Pi_{\mathcal{X}}$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_{k+1} \gets x_k - \alpha\, \nabla f(x_k)$ \Comment{Unconstrained gradient step}
    \State $x_{k+1} \gets \Pi_{\mathcal{X}}(z_{k+1})$ \Comment{Project onto $\mathcal{X}$: $(x_1, x_2) \mapsto (\max(0.5, x_1), x_2)$}
\EndFor
\end{algorithmic}
\end{algorithm}

Projected gradient descent is guaranteed to reduce the objective at every step when $\mathcal{X}$ is convex and $\alpha \leq 1/L$, satisfying the descent condition $f(x_{k+1}) \leq f(x_k)$ with $\Pi_{\mathcal{X}}$ being the Euclidean projection. For a half-space constraint $x_1 \geq 0.5$, the projection is a simple clipping operation: $\Pi_{\mathcal{X}}(z) = (\max(0.5, z_1), z_2)$.

\begin{algorithm}[H]
\caption{Quadratic Penalty Method}
\label{alg:penalty}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha > 0$, penalty weight $\lambda > 0$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $F_{\lambda}(x) \gets f(x) + \lambda \max(0,\, 0.5 - x_1)$ \Comment{Penalised objective; L1 penalty}
    \State $x_{k+1} \gets x_k - \alpha\, \nabla_x F_{\lambda}(x_k)$
\EndFor
\end{algorithmic}
\end{algorithm}

The gradient of the penalised objective is:
\begin{equation}
\nabla F_{\lambda}(x) = \nabla f(x) + \begin{cases} (-\lambda, 0)^T & \text{if } x_1 < 0.5 \\ (0, 0)^T & \text{if } x_1 \geq 0.5 \end{cases}.
\end{equation}
As $\lambda \to \infty$, the penalty forces $x_1 \to 0.5$ from the infeasible side, and the penalised solution converges to the constrained solution. However, large $\lambda$ increases the condition number of $F_\lambda$, necessitating smaller step sizes.

Settings: Projected GD ($\alpha = 0.08$), Penalty with $\lambda \in \{0.15, 1.8, 4.5\}$ ($\alpha = 0.05$ for $\lambda \leq 1.8$; $\alpha = 0.03$ for $\lambda = 4.5$), unconstrained GD ($\alpha = 0.07$). All run for 100 iterations.

\subsection{Results}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q5_fval.pdf}
\caption{Q5: Objective value $f(x)$ vs iteration. Projected GD converges to the constrained minimum $f \approx 0.7244$ at $(0.582, 2.0)^T$, confirmed by the KKT analysis above (the constraint is inactive at the optimum). The penalty method with $\lambda = 4.5$ achieves the same final value as projected GD, while $\lambda = 1.8$ and $\lambda = 0.15$ converge to slightly higher values due to residual constraint violation that the weaker penalty does not eliminate. Unconstrained GD (dashed) converges to the unconstrained minimum, which coincides with the constrained minimum in this case since the constraint is inactive.}
\label{fig:q5_fval}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q5_contour.pdf}
\caption{Q5: Contour plot with constraint boundary $x_1 = 0.5$ (dashed red) and infeasible region (shaded). The starting point $(0.2, 4.0)$ is infeasible. Projected GD immediately clips $x_1$ to $0.5$ in the first projection step, then descends within the feasible region toward $(0.582, 2.0)$. The unconstrained GD path passes through the infeasible region since it ignores the constraint, eventually converging to the same minimum (which happens to be feasible for this specific problem). Penalty methods with increasing $\lambda$ show progressively tighter adherence to the feasible boundary.}
\label{fig:q5_contour}
\end{figure}

\subsection{Constraint Violation Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q5_violation.pdf}
\caption{Q5: Constraint violation $v(x) = \max(0,\; 0.5 - x_1)$ on logarithmic scale. Left: full 100 iterations. Right: first 30 iterations (zoomed). Projected GD achieves \textbf{zero violation from iteration 1}: the projection enforces feasibility as a hard constraint at every step, regardless of the gradient direction. The penalty method with $\lambda = 4.5$ achieves feasibility within $\sim$10 iterations; its initial violation decreases at a rate governed by the effective penalty gradient $\lambda = 4.5$, which is large enough to quickly overcome the objective gradient near the infeasible starting point. With $\lambda = 0.15$, the penalty is too weak relative to the objective gradient from $(0.2, 4.0)$, and feasibility is achieved slowly or incompletely within 100 iterations.}
\label{fig:q5_violation}
\end{figure}

\subsection{Comparative Discussion}

\begin{table}[H]
\centering
\caption{Q5: Final positions and constraint violations after 100 iterations.}
\label{tab:q5_final}
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{Final $x$} & \textbf{$f(x)$} & \textbf{Violation} & \textbf{Hard Feasibility} \\
\midrule
Projected GD & $(0.582, 2.000)$ & 0.7244 & 0.0000 & Yes \\
Penalty $\lambda = 4.5$ & $(0.583, 2.000)$ & 0.7244 & 0.0000 & No (soft) \\
Penalty $\lambda = 1.8$ & $(0.583, 2.000)$ & 0.7244 & $\sim 0$ & No (soft) \\
Penalty $\lambda = 0.15$ & --- & $>0.7244$ & $> 0$ & No \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Projected GD} enforces feasibility as a \emph{hard constraint}: every iterate $x_k \in \mathcal{X}$. This is the fundamental advantage for safety-critical applications where constraint violation is unacceptable. The cost is requiring a projection oracle, which for a half-space is trivial ($O(1)$) but can be $O(d^3)$ for general polytopes.

\textbf{Penalty methods} enforce feasibility \emph{softly} via the modified objective $F_\lambda = f + \lambda \cdot \text{violation}$. The advantage is simplicity: no projection oracle is needed, only the gradient of $F_\lambda$. The trade-off: small $\lambda$ allows residual violation; large $\lambda$ creates an ill-conditioned objective (the gradient of the penalty term dominates), requiring smaller step sizes (here $\alpha = 0.03$ for $\lambda = 4.5$, vs $\alpha = 0.05$ for smaller $\lambda$). In the augmented Lagrangian method, this ill-conditioning is addressed by updating $\lambda$ adaptively while adding a quadratic penalty term.

\textbf{Connection to KKT:} When the constraint is inactive at the optimum (as in this problem), both methods converge to the same solution as unconstrained gradient descent. The methods differ primarily in their approach to handling the infeasible starting point and their treatment of the constraint boundary.

% ============================================================
\section{Question 6: Linear Programmes and Frank--Wolfe Algorithm \hfill [30 marks]}
\label{sec:q6}
% ============================================================

\subsection{Feasible Set and Linear Programme}

Feasible set: $\mathcal{X} = \{x \in \mathbb{R}^2 : 0.5 \leq x_1 \leq 5,\; -5 \leq x_2 \leq 10\}$ (rectangular box with four vertices).

\textbf{Linear Programme:} Minimise $f(x) = a^T x = x_1 + 2x_2$ over $\mathcal{X}$. Since $f$ is linear, $\nabla f = a = (1, 2)^T$ is constant and strictly positive in both coordinates. By the \emph{Fundamental Theorem of Linear Programming}, the minimum of a linear objective over a bounded convex polytope is attained at a vertex. For a box constraint, the minimum in coordinate $i$ is at the lower bound when $a_i > 0$:
\begin{equation}
x^{\star} = \begin{bmatrix} 0.5 \\ -5 \end{bmatrix}, \qquad
f^{\star} = 1 \times 0.5 + 2 \times (-5) = -9.5.
\end{equation}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q6_lp.pdf}
\caption{Q6-I: Feasible region $\mathcal{X}$ (blue rectangle) and linear objective contours $f(x) = x_1 + 2x_2 = c$. The contour lines are parallel (constant gradient $a$), and the minimum occurs at the lower-left vertex $(0.5, -5)$ where both coordinates are at their lower bounds. This illustrates the fundamental LP property: the optimum of any linear objective over a bounded polytope lies at a vertex. For a box with $d$ coordinates and two bounds per coordinate, there are $2^d$ vertices to check, but the LP oracle used by Frank--Wolfe can identify the optimal vertex in $O(d)$ by inspecting each gradient sign independently.}
\label{fig:q6_lp}
\end{figure}

\subsection{Frank--Wolfe Algorithm (Conditional Gradient)}

\begin{algorithm}[H]
\caption{Frank--Wolfe (Conditional Gradient)}
\label{alg:fw}
\begin{algorithmic}[1]
\Require Initial point $x_0 \in \mathcal{X}$, mixing parameter $\beta \in [0,1)$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_k \gets \arg\min_{x \in \mathcal{X}}\, \nabla f(x_k)^T x$ \Comment{Linear minimisation oracle (LP subproblem)}
    \State $x_{k+1} \gets \beta\, x_k + (1 - \beta)\, z_k$ \Comment{Convex combination stays in $\mathcal{X}$}
\EndFor
\end{algorithmic}
\end{algorithm}

A key property is that since $x_k, z_k \in \mathcal{X}$ and $\mathcal{X}$ is convex, every iterate $x_{k+1}$ is a convex combination of feasible points and thus automatically feasible: \emph{no projection is required}. For box constraints, the LP oracle solves in $O(d)$ by:
\begin{equation}
(z_k)_i = \begin{cases} l_i & \text{if } (\nabla f(x_k))_i > 0 \\ u_i & \text{if } (\nabla f(x_k))_i \leq 0 \end{cases}
\end{equation}

\textbf{Convergence:} For a convex, $L$-smooth objective with diameter $D = \max_{x,y \in \mathcal{X}} \|x-y\|$ and the decay schedule $\gamma_k = 2/(k+2)$, Frank--Wolfe achieves:
\begin{equation}
f(x_k) - f^{\star} \leq \frac{2LD^2}{k+2} = O\left(\frac{1}{k}\right).
\end{equation}
This $O(1/k)$ rate is slower than projected gradient descent's $O(1/k)$ (with better constants) but avoids the expensive projection oracle.

The experiments here use a fixed $\beta$ rather than the decaying $\gamma_k = 1-\beta = 2/(k+2)$ schedule. The fixed $\beta$ is equivalent to a constant step-size Frank--Wolfe variant, which may not converge to the exact optimum but is simpler to tune in practice.

\subsection{Interior Optimum: $f(x) = (x_1 - 1)^2 + (x_2 - 5)^2$}

The unconstrained minimum $(1, 5)^T$ lies strictly inside $\mathcal{X}$ (since $0.5 < 1 < 5$ and $-5 < 5 < 10$). Two mixing parameters are tested: $\beta = 0.90$ (step fraction $1-\beta = 10\%$ towards LP vertex) and $\beta = 0.985$ (1.5\% step).

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q6_fw_convergence.pdf}
\caption{Q6-II: Frank--Wolfe convergence for the interior optimum. With $\beta = 0.985$ (smaller steps toward the LP vertex), the final objective ($f = 0.0046$) is lower than with $\beta = 0.90$ ($f = 0.0089$). This occurs because larger steps ($1-\beta = 10\%$) cause greater oscillation: as $x_k$ approaches the interior optimum $(1,5)$, the gradient $\nabla f(x_k)$ oscillates in direction, causing $z_k$ to jump between different vertices of $\mathcal{X}$, and the large step amplifies these vertex-hops into iterate oscillations. The conservative $\beta = 0.985$ damps these oscillations by taking only 1.5\% steps, achieving smoother convergence at the cost of slower initial progress.}
\label{fig:q6_fw_conv}
\end{figure}

\subsection{Boundary Optimum: $f(x) = x_1^2 + x_2^2$}

The unconstrained minimum $(0, 0)^T$ lies outside $\mathcal{X}$ (since $x_1 \geq 0.5$). The constrained minimum is $x^{\star}_{\text{con}} = (0.5, 0)^T$ with $f^{\star}_{\text{con}} = 0.25$, lying on the boundary $x_1 = 0.5$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q6_fw_contour.pdf}
\caption{Q6: Frank--Wolfe trajectories on both objective functions. Left: interior optimum --- trajectories converge toward $(1, 5)$ from starting point $(1, 1)$. Both $\beta$ values converge, with smaller $\beta = 0.985$ showing smoother approach to the interior. Right: boundary optimum --- the trajectory converges toward $(0.5, 0)$ on the $x_1 = 0.5$ boundary. Frank--Wolfe naturally gravitates toward the constraint boundary when the unconstrained optimum lies outside $\mathcal{X}$, because $z_k$ consistently selects the vertex $(0.5, z_2^{(k)})$ on the lower-$x_1$ face.}
\label{fig:q6_fw_contour}
\end{figure}

\subsection{Evolution of $x_k$ and $z_k$}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q6_evolution.pdf}
\caption{Q6: Evolution of iterates $x_k$ and LP vertices $z_k$. Top-left ($\beta = 0.90$): $x_k$ converges quickly in both coordinates toward the interior optimum $(1,5)$, while $z_k$ oscillates between opposite vertices of $\mathcal{X}$ as the gradient direction fluctuates. Top-right ($\beta = 0.985$): slower, smoother convergence of $x_k$ with same oscillatory $z_k$, but the smaller step size $(1-\beta)$ dampens the impact of vertex oscillations. Bottom-left (boundary case): $x_1$ converges monotonically to 0.5, while $x_2$ converges to near 0. The LP vertex $z_1$ is consistently 0.5 (lower bound), confirming the algorithm correctly identifies the binding constraint. Bottom-right: convergence comparison --- boundary case converges to $f \approx 0.364$ after 140 iterations (above $f^{\star} = 0.25$), indicating that more iterations or a decaying $\gamma_k = 2/(k+2)$ step schedule would be needed to reach the exact constrained optimum.}
\label{fig:q6_evolution}
\end{figure}

\begin{table}[H]
\centering
\caption{Q6: Final Frank--Wolfe positions and objective values.}
\label{tab:q6_final}
\begin{tabular}{lcccc}
\toprule
\textbf{Case} & \textbf{$\beta$} & \textbf{Final $x$} & \textbf{$f(x)$} & \textbf{$f^{\star}$} \\
\midrule
Interior & 0.90 & $(1.066, 4.932)$ & 0.0089 & 0.0 \\
Interior & 0.985 & $(0.997, 5.067)$ & 0.0046 & 0.0 \\
Boundary & 0.93 & $(0.500, -0.337)$ & 0.3637 & 0.25 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Interior vs Boundary Behaviour}

\textbf{Interior optimum:} When the unconstrained minimum lies inside $\mathcal{X}$, Frank--Wolfe converges to it but exhibits a characteristic zig-zag: as $x_k$ approaches the interior, the gradient $\nabla f(x_k) \to 0$, causing small perturbations to determine the LP vertex $z_k$, which then oscillates between different vertices. The convex combination $x_{k+1} = \beta x_k + (1-\beta)z_k$ averages out these oscillations, but this averaging slows convergence. The $O(1/k)$ convergence rate reflects this inefficiency near interior minima.

\textbf{Boundary optimum:} Frank--Wolfe is particularly natural when the constrained minimum lies on the boundary, because the LP oracle $z_k$ consistently selects the vertex closest to the gradient direction, which corresponds to the binding constraint face. The iterates naturally approach the boundary without explicit projection, demonstrating Frank--Wolfe's fundamental advantage: \emph{constraint satisfaction by convex combination rather than projection}. The residual gap between the achieved value ($f = 0.364$) and $f^{\star} = 0.25$ arises because the fixed $\beta = 0.93$ does not satisfy the standard $\gamma_k = 2/(k+2)$ decay schedule required for the $O(1/k)$ convergence guarantee.

% ============================================================
\section{Conclusion}
\label{sec:conclusion}
% ============================================================

This study has demonstrated that no single optimisation algorithm dominates across all problem structures. The central findings are summarised below.

\textbf{Impact of condition number.} Benchmark A ($\kappa \approx 1.12$) is nearly trivially easy for all first-order methods. Benchmark B ($\kappa \approx 6.90$) reveals moderate differences between methods. Benchmark C ($\kappa \approx 2508$) exposes the fundamental limitations of gradient descent and variants: convergence rates of $\approx 0.999$ per step mean thousands of iterations are needed, while Newton's method circumvents this through Hessian-based curvature normalisation.

\textbf{Adaptive step sizes (Q1).} The Polyak step achieves optimal convergence when $f^{\star}$ is known exactly but diverges catastrophically when misspecified. RMSprop and Adagrad are more robust to misspecification but may exhibit premature slowdown (Adagrad) or oscillation near the minimum (RMSprop). Heavy Ball provides a useful blend of speed and stability.

\textbf{Momentum methods (Q2).} Nesterov acceleration achieves theoretically optimal $O(1/\sqrt{\kappa})$ convergence for strongly convex problems and provides the best practical performance on Rosenbrock among first-order methods. Adam is effective for machine learning workloads with per-coordinate curvature heterogeneity, but lacks guaranteed convergence on general objectives.

\textbf{Newton's method (Q3).} Second-order information is transformative: for the quadratic Benchmark A, Newton converges in a single step; for Rosenbrock, damped Newton achieves $4\times$ lower final loss than GD in one-quarter the iterations. The computational overhead ($O(d^3)$ Hessian solve) is justified for low-dimensional or nearly quadratic problems.

\textbf{Derivative-free methods (Q4).} Finite differences with small $\delta$ closely match exact gradient descent at $O(d)$ extra function evaluations per step. Large $\delta$ degrades convergence due to truncation error. Nesterov random search and Nelder--Mead are useful when gradient computation is impossible, but both sacrifice convergence rate in exchange for gradient-free operation.

\textbf{Constrained optimisation (Q5).} Projected gradient descent enforces hard feasibility at every step and converges to the constrained minimum. Penalty methods offer simpler implementation at the cost of soft feasibility; the penalty weight $\lambda$ must be tuned carefully to balance feasibility enforcement against step-size stability.

\textbf{Frank--Wolfe (Q6).} The conditional gradient algorithm avoids projection by maintaining feasibility through convex combinations, achieving $O(1/k)$ convergence. It is best suited to problems where the LP subproblem is cheap (e.g., box constraints: $O(d)$ per step) but projection is expensive (e.g., nuclear norm ball: $O(d^3)$ SVD). For boundary-constrained problems, Frank--Wolfe naturally identifies binding constraints without explicit detection.

% ============================================================
\newpage
\appendix
\section{Python Code}
\label{app:code}
% ============================================================

The complete implementation is provided in \texttt{CS7DS2\_Final\_Code\_25336453.py}. Key algorithm implementations are reproduced below.

\begin{lstlisting}[caption={Gradient Descent}]
def gradient_descent(grad_fn, loss_fn, x0, alpha, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for _ in range(n_iters):
        x = x - alpha * grad_fn(x)
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Polyak Step Size}]
def polyak_step(grad_fn, loss_fn, x0, f_star, eps, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        alpha_k = (loss_fn(x) - f_star) / (np.dot(g, g) + eps)
        alpha_hist.append(alpha_k)
        x = x - alpha_k * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Adagrad}]
def adagrad(grad_fn, loss_fn, x0, alpha0, eps, n_iters):
    x = x0.copy().astype(float)
    G = np.zeros_like(x)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        G += g**2
        eff_alpha = alpha0 / (np.sqrt(G) + eps)
        alpha_hist.append(np.mean(eff_alpha))
        x = x - eff_alpha * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={RMSprop}]
def rmsprop(grad_fn, loss_fn, x0, alpha0, beta, eps, n_iters):
    x = x0.copy().astype(float)
    v = np.zeros_like(x)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        v = beta * v + (1 - beta) * g**2
        eff_alpha = alpha0 / (np.sqrt(v) + eps)
        alpha_hist.append(np.mean(eff_alpha))
        x = x - eff_alpha * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Heavy Ball / Polyak Momentum}]
def heavy_ball(grad_fn, loss_fn, x0, alpha, beta, n_iters):
    x = x0.copy().astype(float)
    z = np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for _ in range(n_iters):
        g = grad_fn(x)
        z = beta * z + alpha * g
        x = x - z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Nesterov Momentum}]
def nesterov_momentum(grad_fn, loss_fn, x0, alpha, beta_max, n_iters):
    x = x0.copy().astype(float)
    z = np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for k in range(1, n_iters + 1):
        beta_k = min((k - 1) / (k + 2), beta_max)
        lookahead = x + beta_k * z
        g = grad_fn(lookahead)
        z = beta_k * z - alpha * g
        x = x + z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Adam Optimiser}]
def adam_optimiser(grad_fn, loss_fn, x0, alpha, beta1, beta2, eps, n_iters):
    x = x0.copy().astype(float)
    m_vec, v_vec = np.zeros_like(x), np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for t in range(1, n_iters + 1):
        g = grad_fn(x)
        m_vec = beta1 * m_vec + (1 - beta1) * g
        v_vec = beta2 * v_vec + (1 - beta2) * g**2
        m_hat = m_vec / (1 - beta1**t)
        v_hat = v_vec / (1 - beta2**t)
        x = x - alpha * m_hat / (np.sqrt(v_hat) + eps)
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Newton's Method with Damping and Regularisation}]
def newtons_method(grad_fn, hess_fn, loss_fn, x0, alpha, n_iters, damping=1e-8):
    x = x0.copy().astype(float)
    x_hist, f_hist, update_norms = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        H = hess_fn(x) if callable(hess_fn) else hess_fn
        H_reg = H + damping * np.eye(len(x))
        try:
            p = np.linalg.solve(H_reg, g)
        except np.linalg.LinAlgError:
            p = g  # fall back to steepest descent
        update_norms.append(np.linalg.norm(alpha * p))
        x = x - alpha * p
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(update_norms)
\end{lstlisting}

\begin{lstlisting}[caption={Frank--Wolfe Algorithm}]
def frank_wolfe(grad_fn, loss_fn, x0, bounds, beta, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist, z_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        z = np.array([bounds[i][0] if g[i] > 0 else bounds[i][1]
                       for i in range(len(g))])
        z_hist.append(z.copy())
        x = beta * x + (1 - beta) * z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(z_hist)
\end{lstlisting}

\end{document}
