\documentclass[11pt,a4paper]{article}
\usepackage[margin=2.2cm]{geometry}
\usepackage{graphicx}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{mathtools}
\usepackage{float}
\usepackage{hyperref}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{listings}
\usepackage{fancyhdr}
\usepackage{multirow}

\hypersetup{colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue}

% Code listing style
\lstset{
  language=Python,
  basicstyle=\ttfamily\scriptsize,
  keywordstyle=\color{blue},
  commentstyle=\color{gray},
  stringstyle=\color{red},
  numbers=left,
  numberstyle=\tiny\color{gray},
  breaklines=true,
  frame=single,
  captionpos=b
}

\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small CS7DS2 -- Final Assignment}
\fancyhead[R]{\small Swetha Sekar (25336453)}
\fancyfoot[C]{\thepage}

\title{%
  \includegraphics[width=0.25\textwidth]{tcd_logo.jpg}\\[1em]
  \textbf{CS7DS2 -- Optimisation Algorithms for Data Analysis}\\[0.5em]
  \Large Final Assignment Report
}
\author{Swetha Sekar \\ Student ID: 25336453 \\ School of Computer Science and Statistics \\ Trinity College Dublin}
\date{April 2026}

\begin{document}
\maketitle
\thispagestyle{empty}

\begin{abstract}
This report presents a comprehensive comparative study of optimisation algorithms across six question areas for the CS7DS2 Final Assignment. Three benchmark problems are employed: a linear regression quadratic loss (Benchmark A), a perturbed quadratic function modelling toy neural network loss (Benchmark B), and the ill-conditioned Rosenbrock function (Benchmark C). The methods investigated span six categories: (1)~adaptive step-size methods --- Polyak, Adagrad, RMSprop, and Heavy Ball; (2)~momentum-based and stochastic methods --- Nesterov acceleration, Adam, and mini-batch SGD; (3)~Newton's method and local approximations; (4)~derivative-free approaches --- finite differences, Nesterov random search, Nelder--Mead simplex, and grid search; (5)~constrained optimisation via projected gradient descent and penalty methods; and (6)~the Frank--Wolfe conditional gradient algorithm for linear-constrained problems. Each method is evaluated with respect to convergence speed, trajectory behaviour, numerical stability, and computational trade-offs. Key findings include: the Polyak step is highly sensitive to $f^{\star}$ specification; Nesterov momentum achieves the theoretically optimal $O(1/k^2)$ convergence rate; Newton's method converges in one step on quadratic objectives; and Frank--Wolfe converges at $O(1/k)$ without requiring a projection oracle.
\end{abstract}

\tableofcontents
\newpage

% ============================================================
\section{Introduction}
\label{sec:intro}
% ============================================================

Optimisation is the backbone of modern machine learning: virtually every model is trained by minimising some loss function. The choice of optimisation algorithm significantly impacts convergence speed, solution quality, and computational cost. This report systematically examines algorithms drawn from six distinct paradigms, evaluated across three canonical benchmark functions of increasing difficulty.

\textbf{Scope.} The six questions address: (Q1) adaptive step-size computation without a fixed learning rate schedule; (Q2) momentum-based acceleration and stochastic variants; (Q3) second-order methods exploiting local curvature via the Hessian; (Q4) gradient-free methods for black-box or non-differentiable objectives; (Q5) constrained optimisation via feasibility enforcement or penalisation; and (Q6) projection-free methods for linearly-constrained convex problems.

\textbf{Benchmarks.} The three benchmark functions are chosen to reflect different curvature properties. Benchmark A (linear regression MSE) is a globally convex quadratic with analytically known minimiser. Benchmark B (perturbed quadratic) introduces mild non-linearity via a sinusoidal term, creating moderate anisotropy with curvature ratio approximately $5\!:\!1$. Benchmark C (Rosenbrock) is a notoriously challenging non-convex function with extreme ill-conditioning ($\kappa \approx 2508$ at the optimum) and a narrow curved valley, serving as a stress test for any first-order method.

\textbf{Implementation.} All experiments use a fixed random seed (\texttt{np.random.seed(42)}) and are implemented in Python with NumPy. Figures are produced with Matplotlib. The complete source code is provided in \texttt{CS7DS2\_Final\_Code\_25336453.py} and reproduced in Appendix~\ref{app:code}.

\textbf{Report organisation.} Section~\ref{sec:benchmarks} introduces the three benchmark functions. Sections~\ref{sec:q1}--\ref{sec:q6} present the six question answers, each with algorithm formulations, hyperparameter settings, experimental results, and theoretical analysis. Section~\ref{sec:conclusion} synthesises cross-cutting observations.

\subsection*{Unified Convergence Rates Reference}

\begin{table}[H]
\centering
\caption{Theoretical convergence guarantees for all methods in this report. $L$ = gradient Lipschitz constant, $\mu$ = strong convexity constant, $\kappa = L/\mu$, $D = \|x_0 - x^{\star}\|$, $\sigma^2$ = gradient noise variance, $\varepsilon_{\mathrm{mach}} \approx 10^{-16}$ (double precision), $C_f = (L/2)\mathrm{diam}(\mathcal{X})^2$ (FW curvature). All rates in terms of \emph{suboptimality gap} $f(x_k)-f^{\star}$.}
\label{tab:convergence_rates}
\begin{tabular}{lllp{4cm}}
\toprule
\textbf{Method} & \textbf{Rate} & \textbf{Class} & \textbf{Notes / Condition} \\
\midrule
GD (fixed $\alpha$) & $O\!\left(\frac{LD^2}{k}\right)$ & Convex & $\alpha = 1/L$; linear rate for $\mu>0$ \\
GD (strongly convex) & $(1-\mu/L)^k \cdot (f_0-f^{\star})$ & Str.\ convex & factor $(\kappa-1)/(\kappa+1)$ \\
Polyak step & $\frac{L D^2}{k}$ (convex) & Convex & Requires exact $f^{\star}$ \\
Adagrad & $O\!\left(\frac{\sqrt{T}\cdot\|g\|_*}{T}\right)$ & Online & $O(\sqrt{T})$ regret; effective $O(1/\sqrt{k})$ \\
Heavy Ball & $\left(\frac{\sqrt{\kappa}-1}{\sqrt{\kappa}+1}\right)^k$ & Str.\ convex quadratic & Optimal $\alpha^{\star}, \beta^{\star}$ required \\
\midrule
Nesterov (convex) & $\dfrac{2LD^2}{(k+1)^2}$ & Convex & $O(1/k^2)$; \textbf{optimal first-order rate} \\
Nesterov (str.\ cvx.) & $\left(1-\dfrac{1}{\sqrt{\kappa}}\right)^k$ & Str.\ convex & factor $(\sqrt{\kappa}-1)/(\sqrt{\kappa}+1)$; \textbf{optimal} \\
Adam & $O\!\left(\frac{1}{\sqrt{T}}\right)$ & Convex (diminish.\ $\alpha_t$) & With $\alpha_t = \alpha/\sqrt{t}$ \\
\midrule
Newton & $\|x_{k+1}-x^{\star}\| \leq C\|x_k-x^{\star}\|^2$ & Loc.\ str.\ cvx & Quadratic; $C = L_H/(2\lambda_{\min})$ \\
FD gradient & $\|\hat{g}-g\| = O(\delta) + O(\varepsilon_{\mathrm{mach}}/\delta)$ & Approx.\ & Optimal $\delta^{\star} = \sqrt{\varepsilon_{\mathrm{mach}}} \approx 10^{-8}$ \\
Random search & $O(1/\sqrt{k})$ & Convex, zeroth-order & 2 FE/step; high variance \\
\midrule
Projected GD & Same as GD & Convex, constrained & Requires cheap $\Pi_{\mathcal{X}}$ \\
Frank--Wolfe & $\dfrac{2C_f}{k+2}$ & Convex, bounded $\mathcal{X}$ & $O(1/k)$; LP oracle replaces projection \\
\bottomrule
\end{tabular}
\end{table}

% ============================================================
\section{Benchmark Problems and Experimental Setup}
\label{sec:benchmarks}
% ============================================================

\subsection{Benchmark A: Linear Regression Quadratic Loss}
A synthetic dataset with $m = 1000$ samples and two parameters is generated:
\begin{equation}
\theta^{\star} = \begin{bmatrix} 3 \\ 4 \end{bmatrix}, \quad x^{(i)} \sim \mathcal{N}(0, I_2), \quad y^{(i)} = (\theta^{\star})^T x^{(i)} + \varepsilon^{(i)}, \quad \varepsilon^{(i)} \sim \mathcal{N}(0, 1).
\end{equation}
The loss function is the mean squared error:
\begin{equation}
J(\theta) = \frac{1}{2m} \sum_{i=1}^{m} \left(\theta^T x^{(i)} - y^{(i)}\right)^2 = \frac{1}{2m}\|X\theta - y\|^2.
\end{equation}
This is a convex quadratic with constant Hessian $H = \frac{1}{m}X^T X \succ 0$. The gradient is $\nabla J(\theta) = \frac{1}{m}X^T(X\theta - y)$. The unique minimiser is the OLS solution $\theta^{\star}_{\mathrm{OLS}} = (X^TX)^{-1}X^Ty \approx (3.028, 4.017)^T$. Due to the observation noise, the minimum loss is $J^{\star} \approx 0.4826 \neq 0$. Starting point: $\theta_0 = [0, 0]^T$.

\subsection{Benchmark B: Toy Neural Network Loss}
\begin{equation}
f_B(x_1, x_2) = (x_1 - 1)^2 + 5(x_2 - 2)^2 + \sin(x_1).
\end{equation}
The gradient is $\nabla f_B = [2(x_1 - 1) + \cos(x_1),\ 10(x_2 - 2)]^T$ and the Hessian is $H = \mathrm{diag}(2 - \sin(x_1),\ 10)$. The diagonal structure means the two coordinates are decoupled, while the non-constant $(1,1)$ entry introduces mild non-convexity. The eigenvalue ratio varies between approximately $1.2\!:\!10$ and $3\!:\!10$, creating moderate anisotropy. The global minimum lies at approximately $(0.582, 2.0)$ with $f^{\star}_B \approx 0.7244$, obtained by solving $2(x_1-1) + \cos(x_1) = 0$ numerically. Starting point: $x_0 = [-1, 4]^T$ (far from the minimum, with $f_B(x_0) \approx 23.16$).

\subsection{Benchmark C: Rosenbrock Function}
\begin{equation}
f_C(x_1, x_2) = (1 - x_1)^2 + 100(x_2 - x_1^2)^2.
\end{equation}
The global minimum is at $(1, 1)$ with $f^{\star}_C = 0$. The Hessian at the optimum is
\begin{equation}
H_C(1,1) = \begin{bmatrix} 802 & -400 \\ -400 & 200 \end{bmatrix},
\end{equation}
with eigenvalues $\lambda_{\min} \approx 0.40$ and $\lambda_{\max} \approx 1001.6$, yielding condition number $\kappa \approx 2508$. This extreme ill-conditioning, combined with the narrow curved valley along $x_2 = x_1^2$, renders Rosenbrock one of the most challenging standard test functions for gradient-based methods. Starting point: $x_0 = [-1, 1]^T$.

\subsection{Benchmark D: Constrained Feasible Set}
For constrained problems (Q5--Q6): $\mathcal{X} = \{x \in \mathbb{R}^2 : 0.5 \leq x_1 \leq 5,\; -5 \leq x_2 \leq 10\}$ (a box with diameter $\mathrm{diam}(\mathcal{X}) \approx 15.67$).

\subsection{Summary of Benchmark Properties}

\begin{table}[H]
\centering
\caption{Summary of benchmark function properties. $\kappa$ = condition number of Hessian at the optimum.}
\label{tab:benchmarks}
\begin{tabular}{lccccc}
\toprule
\textbf{Benchmark} & \textbf{$f^{\star}$} & \textbf{$x^{\star}$} & \textbf{$x_0$} & \textbf{$\kappa$} & \textbf{Convex?} \\
\midrule
A (Linear Reg.) & 0.4826 & $(3.028, 4.017)$ & $(0, 0)$ & $\approx 2$ & Yes (globally) \\
B (Toy NN) & 0.7244 & $(0.582, 2.000)$ & $(-1, 4)$ & $\approx 5$ & Nearly \\
C (Rosenbrock) & 0 & $(1, 1)$ & $(-1, 1)$ & $\approx 2508$ & No \\
\bottomrule
\end{tabular}
\end{table}

% ============================================================
\section{Question 1: Adaptive Step-Size Methods \hfill [30 marks]}
\label{sec:q1}
% ============================================================

\subsection{Theoretical Background}

A fundamental challenge in gradient descent is choosing the step size $\alpha$. A fixed step size requires knowledge of the Lipschitz constant $L$ of the gradient; too large a step diverges, while too small a step converges very slowly. Adaptive methods overcome this by computing a data-driven step at each iteration.

The \textbf{Polyak step} $\alpha_k = (f(x_k) - f^{\star})/\|\nabla f(x_k)\|^2$ minimises the upper bound on the squared distance to the optimum at each step. For a $\mu$-strongly convex function with known $f^{\star}$, this step satisfies the contraction
\begin{equation}
\|x_{k+1} - x^{\star}\|^2 \leq \left(1 - \frac{(f(x_k)-f^{\star})^2}{\|x_k-x^{\star}\|^2 \|\nabla f(x_k)\|^2}\right)\|x_k - x^{\star}\|^2,
\end{equation}
and Cauchy--Schwarz plus strong convexity gives $\|x_{k+1}-x^{\star}\|^2 \leq (1 - 2\mu\alpha_k)\|x_k-x^{\star}\|^2$, yielding linear convergence without knowing $\mu$ or $L$ separately. However, it requires exact knowledge of $f^{\star}$, which in practice must be estimated or approximated.

\textbf{Adagrad} accumulates per-coordinate squared-gradient sums, effectively giving coordinates with historically large gradients smaller learning rates. Its per-coordinate update rule achieves a cumulative regret bound of $O(\sqrt{T})$ in online learning, corresponding to $O(1/\sqrt{k})$ average convergence. The accumulated sum grows without bound, causing the effective learning rate to decay to zero --- a desirable property for sparse features but problematic for sustained optimisation.

\textbf{RMSprop} replaces the cumulative sum with an exponential moving average ($\beta = 0.9$), preventing the denominator from growing without bound. This allows the effective step size to adapt to recent curvature rather than the full history.

\textbf{Heavy Ball} (Polyak Momentum) adds a momentum term $\beta z_k$ to the gradient update. For a quadratic with eigenvalues $[\lambda_{\min}, \lambda_{\max}]$ and condition number $\kappa = \lambda_{\max}/\lambda_{\min}$, the optimal parameters are
\begin{equation}
\alpha^{\star} = \frac{4}{(\sqrt{\lambda_{\max}} + \sqrt{\lambda_{\min}})^2}, \qquad \beta^{\star} = \left(\frac{\sqrt{\kappa}-1}{\sqrt{\kappa}+1}\right)^2,
\end{equation}
achieving convergence rate $\rho = (\sqrt{\kappa}-1)/(\sqrt{\kappa}+1)$ per step --- substantially faster than plain GD's rate $(\kappa-1)/(\kappa+1)$.

\subsection{Algorithm Formulations}

\begin{algorithm}[H]
\caption{Polyak Step Size}
\label{alg:polyak}
\begin{algorithmic}[1]
\Require Initial point $x_0$, optimal value $f^{\star}$, tolerance $\varepsilon$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $\alpha_k \gets \dfrac{f(x_k) - f^{\star}}{\|g_k\|^2 + \varepsilon}$
    \State $x_{k+1} \gets x_k - \alpha_k\, g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{algorithm}[H]
\caption{Adagrad}
\label{alg:adagrad}
\begin{algorithmic}[1]
\Require Initial point $x_0$, base learning rate $\alpha_0$, tolerance $\varepsilon$, iterations $N$
\State $G_0 \gets \mathbf{0}$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $G_{k+1} \gets G_k + g_k \odot g_k$ \Comment{Per-coordinate accumulation}
    \State $x_{k+1} \gets x_k - \dfrac{\alpha_0}{\sqrt{G_{k+1}} + \varepsilon} \odot g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{algorithm}[H]
\caption{RMSprop}
\label{alg:rmsprop}
\begin{algorithmic}[1]
\Require Initial point $x_0$, learning rate $\alpha_0$, decay $\beta$, tolerance $\varepsilon$, iterations $N$
\State $v_0 \gets \mathbf{0}$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $v_{k+1} \gets \beta\, v_k + (1 - \beta)\, g_k \odot g_k$ \Comment{Exponential moving average}
    \State $x_{k+1} \gets x_k - \dfrac{\alpha_0}{\sqrt{v_{k+1}} + \varepsilon} \odot g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{algorithm}[H]
\caption{Polyak Momentum / Heavy Ball}
\label{alg:heavyball}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, momentum $\beta$, iterations $N$
\State $z_0 \gets \mathbf{0}$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_{k+1} \gets \beta\, z_k + \alpha\, \nabla f(x_k)$
    \State $x_{k+1} \gets x_k - z_{k+1}$
\EndFor
\end{algorithmic}
\end{algorithm}

\subsection{Hyperparameter Settings}

\begin{table}[H]
\centering
\caption{Q1: Hyperparameter settings for each method and benchmark. All methods run for 120 iterations.}
\label{tab:q1_params}
\begin{tabular}{llccc}
\toprule
\textbf{Method} & \textbf{Parameter} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & $\alpha$ & 0.08 & 0.06 & 0.0012 \\
\midrule
Polyak & $f^{\star}$ & 0 & 0 & 0 \\
 & $\varepsilon$ & $10^{-4}$ & $10^{-4}$ & $10^{-3}$ \\
\midrule
Adagrad & $\alpha_0$ & 1.8 & 1.2 & 0.45 \\
 & $\varepsilon$ & $10^{-5}$ & $10^{-5}$ & $10^{-5}$ \\
\midrule
RMSprop & $\alpha_0$ & 0.22 & 0.14 & 0.0035 \\
 & $\beta$ & 0.9 & 0.9 & 0.9 \\
 & $\varepsilon$ & $10^{-5}$ & $10^{-5}$ & $10^{-5}$ \\
\midrule
Heavy Ball & $\alpha$ & 0.045 & 0.035 & 0.0008 \\
 & $\beta$ & 0.88 & 0.90 & 0.86 \\
\bottomrule
\end{tabular}
\end{table}

Note that for Benchmarks A and B, the specified $f^{\star} = 0$ is incorrect: the true minimum of Benchmark A is $J^{\star} \approx 0.4826$ (non-zero due to observation noise), and the true minimum of Benchmark B is $f^{\star}_B \approx 0.7244$. Only for Benchmark C is $f^{\star} = 0$ the exact global minimum.

\subsection{Convergence Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_A.pdf}
\caption{Q1: Convergence on Benchmark A (Linear Regression). GD, Adagrad, and Heavy Ball all converge to the noise floor $J^{\star} \approx 0.4826$. RMSprop converges to a slightly higher value ($f \approx 0.506$) as its adaptive step exhibits oscillation near the minimum. The Polyak step \textbf{diverges} ($f = 4.767$) because $f^{\star} = 0$ misspecifies the true minimum: as the iterate approaches $\theta_{\mathrm{OLS}}$, the gradient $\|\nabla J\| \to 0$ while $J(x_k) \to 0.4826 \gg 0$, causing $\alpha_k = J(x_k)/\|\nabla J\|^2 \to \infty$ and inducing oscillations.}
\label{fig:q1_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_B.pdf}
\caption{Q1: Convergence on Benchmark B (Toy Neural Network). With $f^{\star} = 0$ misspecified (true minimum $f^{\star}_B \approx 0.7244$), the Polyak step converges to $f = 0.982$, which is above the true minimum. Near the true minimum, the gradient norm $\|g\| \to 0$ while $f(x_k) \to 0.724 > 0$, so $\alpha_k$ grows but does not diverge catastrophically --- the method oscillates near the minimum rather than escaping entirely. Adagrad and Heavy Ball match GD's final accuracy ($f = 0.7244$), while RMSprop achieves $f = 0.728$. The moderate anisotropy (eigenvalue ratio $\sim 3:10$) poses no severe difficulty for any well-specified method.}
\label{fig:q1_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_C.pdf}
\caption{Q1: Convergence on Benchmark C (Rosenbrock). Here $f^{\star} = 0$ is exact, and Polyak converges to $f = 0.024$, demonstrating that its adaptive step size is effective when $f^{\star}$ is accurately known. Heavy Ball achieves $f = 1.400$, outperforming GD ($f = 3.514$) through momentum acceleration. Adagrad reaches $f = 2.203$ as its monotonically decaying step helps navigate the ill-conditioned valley. RMSprop ($f = 3.120$) shows limited advantage with the very small base rate $\alpha_0 = 0.0035$ required for stability.}
\label{fig:q1_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q1: Final objective values after 120 iterations. Bold indicates best per benchmark.}
\label{tab:q1_final}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & 0.4826 & 0.7244 & 3.5142 \\
Polyak & 4.7670 & 0.9819 & \textbf{0.0239} \\
Adagrad & \textbf{0.4826} & \textbf{0.7244} & 2.2034 \\
RMSprop & 0.5064 & 0.7276 & 3.1204 \\
Heavy Ball & 0.4826 & 0.7244 & 1.4003 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Contour Plots with Optimisation Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q1_contour_B.pdf}
\caption{Q1: Trajectories on Benchmark B. The elliptical contours reflect the $\approx 3.5\!:\!10$ curvature ratio between $x_1$ and $x_2$ directions. GD and Adagrad follow direct paths towards the minimum. Heavy Ball shows characteristic overshoot from momentum but ultimately converges. The Polyak trajectory oscillates near the minimum due to the growing step size near $f^{\star}$, confirming the $f^{\star}$-misspecification effect.}
\label{fig:q1_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q1_contour_C.pdf}
\caption{Q1: Trajectories on Benchmark C (Rosenbrock). The banana-shaped valley $x_2 \approx x_1^2$ is clearly visible. All methods first descend rapidly towards the valley floor, then progress slowly along it towards $(1,1)$. With the correct $f^{\star} = 0$, the Polyak step shows the most aggressive progress along the valley, exploiting the fact that $(f(x_k) - 0)/\|\nabla f\|^2$ naturally produces larger steps when the function value is still large and smaller steps near the minimum. GD with $\alpha = 0.0012$ barely advances due to the conservative step size required for stability against the steep transverse curvature.}
\label{fig:q1_contour_C}
\end{figure}

\subsection{Adaptive Step-Size Evolution}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_A.pdf}
\caption{Q1: Step-size evolution on Benchmark A. The Polyak step exhibits pathological growth near the minimum: $\alpha_k = J(x_k)/\|\nabla J\|^2$ increases as $\|\nabla J\| \to 0$ while $J(x_k) - f^{\star} \approx 0.48$ remains nonzero, confirming the divergence mechanism. Adagrad decays monotonically as squared gradients accumulate. RMSprop stabilises due to exponential forgetting ($\beta = 0.9$), adapting the effective rate to recent curvature without full history.}
\label{fig:q1_stepsize_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_B.pdf}
\caption{Q1: Step-size evolution on Benchmark B. A similar divergent pattern appears for Polyak. Adagrad decays rapidly in early iterations (large gradients at the distant starting point $x_0 = [-1, 4]^T$), then settles to a small effective rate. RMSprop maintains a moderate slowly-varying effective step throughout due to its exponential forgetting mechanism.}
\label{fig:q1_stepsize_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_C.pdf}
\caption{Q1: Step-size evolution on Benchmark C. With correct $f^{\star} = 0$, the Polyak step decreases smoothly as $f(x_k) \to 0$: the numerator $f(x_k)$ shrinks while the denominator $\|\nabla f\|^2$ remains bounded. This well-behaved decay confirms why Polyak achieves the best Rosenbrock convergence among the tested methods.}
\label{fig:q1_stepsize_C}
\end{figure}

\subsection{Convergence Rate Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q1_convergence_rate.pdf}
\caption{Q1: Convergence rate analysis. Left: excess objective $f(x_k) - f^{\star}_{\mathrm{floor}}$ on Benchmark A for GD, Adagrad, and Heavy Ball, showing the linear (geometric) decrease typical of strongly convex quadratic problems. Heavy Ball's linear rate is slightly faster due to momentum. Right: convergence comparison on Rosenbrock, where Polyak (correct $f^{\star}=0$) achieves the fastest observed convergence, and Heavy Ball outperforms GD by accumulating momentum in the valley direction.}
\label{fig:q1_rate}
\end{figure}

\subsection{Comparative Analysis}

\begin{itemize}[nosep]
\item \textbf{Polyak sensitivity:} The Polyak step achieves the best result when $f^{\star}$ is known exactly (Rosenbrock: $f = 0.024$, best among all methods). When $f^{\star}$ is misspecified --- as in Benchmark A where $f^{\star}_{\mathrm{true}} \approx 0.483 \gg 0$ --- the method diverges severely ($f = 4.767$). For Benchmark B, misspecification causes oscillation near the true minimum ($f = 0.982$ vs $f^{\star} = 0.724$) rather than full divergence, because the step size grows large but the method does not fully escape the vicinity of the optimum.

\item \textbf{Adagrad robustness:} Adagrad achieves competitive performance across all benchmarks without requiring knowledge of $f^{\star}$. Its cumulative squared gradient denominator monotonically increases, providing guaranteed decrease in effective learning rate. This translates to reliable convergence but premature slowdown on non-stationary problems and on the ill-conditioned Rosenbrock valley, where accumulated large transverse gradients reduce the longitudinal step to nearly zero before the valley-floor optimum is reached.

\item \textbf{RMSprop smoothness:} RMSprop produces the smoothest convergence curves due to exponential averaging of squared gradients (decay $\beta = 0.9$). However, its final accuracy is slightly worse than GD on Benchmark A ($f = 0.506$ vs $0.483$), because the exponential forgetting introduces a non-diminishing effective step near the minimum, causing persistent small oscillations.

\item \textbf{Heavy Ball acceleration:} Heavy Ball provides the best gradient-based improvement on Rosenbrock ($f = 1.400$ vs GD's $3.514$) by accumulating velocity along the consistent valley-floor direction. On the convex quadratic Benchmark A, Heavy Ball matches GD's final accuracy; theoretical analysis shows it achieves a convergence rate of $(\sqrt{\kappa}-1)/(\sqrt{\kappa}+1)$ per step with optimal parameters, vs $(\kappa-1)/(\kappa+1)$ for GD.

\item \textbf{Summary:} In practice, exact $f^{\star}$ is rarely known, limiting Polyak's applicability. Adagrad and Heavy Ball provide robust alternatives: Adagrad through guaranteed step-size decay, Heavy Ball through momentum. RMSprop (and its extension Adam, Section~\ref{sec:q2}) are preferred in practice for deep learning due to adaptive per-coordinate scaling without full accumulation.
\end{itemize}

\subsection{Per-Coordinate Adaptive Scaling: Adagrad vs RMSprop}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q1_adaptive_percoord.pdf}
\caption{Q1: Per-coordinate effective learning rate analysis on Benchmark B. Top-left: Adagrad's per-coordinate steps decay monotonically; $x_1$'s rate decays more slowly than $x_2$'s because the larger curvature in $x_2$ (eigenvalue $\approx 10$) means larger gradients and faster accumulation. Top-right: RMSprop maintains higher effective rates throughout, enabling faster convergence at later iterations than Adagrad on this benchmark. Bottom-left: the ratio $\alpha_1/\alpha_2$ for Adagrad quickly stabilises near the curvature ratio ($\approx 0.35$), effectively normalising the anisotropy. RMSprop's ratio also stabilises but at a slightly different level. Bottom-right: both methods automatically produce a step-size ratio that approximately matches the inverse curvature ratio, implicitly precondioning the gradient.}
\label{fig:q1_percoord}
\end{figure}

The per-coordinate analysis reveals the key adaptive property: both Adagrad and RMSprop automatically assign smaller effective learning rates to the high-curvature direction ($x_2$ with curvature $\approx 10$) and larger rates to the low-curvature direction ($x_1$ with curvature $\approx 2$). This implicit preconditioning is what allows these methods to converge effectively on anisotropic problems without explicit knowledge of the Hessian.

\begin{table}[H]
\centering
\caption{Q1: Summary of adaptive method properties and best benchmark performance.}
\label{tab:q1_summary}
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{Requires $f^{\star}$} & \textbf{Convergence guarantee} & \textbf{Best on} & \textbf{Step-size behaviour} \\
\midrule
Polyak & Yes (exact) & Linear (strongly convex) & C (0.024) & Adaptive, can diverge \\
Adagrad & No & $O(\sqrt{T})$ regret & A, B (0.482) & Monotone decreasing \\
RMSprop & No & None (heuristic) & A (0.506) & Adaptive, bounded \\
Heavy Ball & No & Quadratic (optimal $\alpha,\beta$) & C (1.400) & Fixed scaled \\
GD (base) & No & Linear (known $L$) & A, B (0.482) & Fixed \\
\bottomrule
\end{tabular}
\end{table}

% ============================================================
\section{Question 2: Momentum and Stochastic Methods \hfill [30 marks]}
\label{sec:q2}
% ============================================================

\subsection{Theoretical Background}

\textbf{Nesterov Acceleration} achieves the \emph{optimal} $O(1/k^2)$ convergence rate for smooth convex functions among all first-order methods --- a fundamental lower bound established by Nemirovski and Yudin (1983). For any first-order method using only gradient evaluations $\{(\nabla f(x_i))\}_{i=0}^{k}$, there exists a worst-case convex function requiring $\Omega(1/k^2)$ iterations to reach $\varepsilon$-accuracy. The key innovation of Nesterov's scheme is evaluating the gradient at a \emph{lookahead point} $\tilde{x}_k = x_k + \beta_k z_k$ rather than the current iterate $x_k$. This corrective lookahead allows the method to reduce overshoot that afflicts standard (Heavy Ball) momentum. With the schedule $\beta_k = (k-1)/(k+2)$, the iterate satisfies
\begin{equation}
f(x_k) - f^{\star} \leq \frac{2\|x_0 - x^{\star}\|^2}{\alpha(k+1)^2},
\end{equation}
compared to GD's $O(1/k)$ rate and Heavy Ball's linear rate (which is optimal for quadratics but not proven for general convex functions). For Benchmark A (quadratic with $\kappa \approx 2$), the $O(1/k^2)$ vs $O(1/k)$ gap is small; for Rosenbrock ($\kappa \approx 2508$) the difference after 150 iterations is a factor of 7 in objective value.

\textbf{Adam} combines the first moment estimate (momentum, as in Nesterov) with the second moment estimate (per-coordinate adaptive scaling, as in RMSprop). The bias correction factors $(1-\beta_1^t)$ and $(1-\beta_2^t)$ ensure that the moment estimates are unbiased in early iterations (when the exponential averages are initialised to zero). Adam achieves $O(1/\sqrt{T})$ convergence for smooth convex functions with appropriate diminishing learning rates, and is particularly effective on problems with heterogeneous curvature across coordinates.

\subsection{Algorithm Formulations}

\begin{algorithm}[H]
\caption{Nesterov Momentum / Acceleration}
\label{alg:nesterov}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, max momentum $\beta_{\max}$, iterations $N$
\State $z_0 \gets \mathbf{0}$
\For{$k = 1, 2, \ldots, N$}
    \State $\beta_k \gets \min\!\left(\dfrac{k-1}{k+2},\ \beta_{\max}\right)$
    \State $\tilde{x}_k \gets x_k + \beta_k\, z_k$ \Comment{Lookahead point}
    \State $z_{k+1} \gets \beta_k\, z_k - \alpha\, \nabla f(\tilde{x}_k)$
    \State $x_{k+1} \gets x_k + z_{k+1}$
\EndFor
\end{algorithmic}
\end{algorithm}

The key distinction from Heavy Ball is the \emph{lookahead}: the gradient is evaluated at $\tilde{x}_k = x_k + \beta_k z_k$ rather than $x_k$. The adaptive momentum $\beta_k = \min((k-1)/(k+2), \beta_{\max})$ increases from 0 towards $\beta_{\max}$, initially behaving like gradient descent and gradually accelerating.

\begin{algorithm}[H]
\caption{Adam Optimiser}
\label{alg:adam}
\begin{algorithmic}[1]
\Require Initial point $x_0$, learning rate $\alpha$, moments $\beta_1, \beta_2$, tolerance $\varepsilon$, iterations $N$
\State $m_0 \gets \mathbf{0},\ v_0 \gets \mathbf{0}$
\For{$t = 1, 2, \ldots, N$}
    \State $g_t \gets \nabla f(x_t)$
    \State $m_t \gets \beta_1\, m_{t-1} + (1 - \beta_1)\, g_t$ \Comment{First moment (momentum)}
    \State $v_t \gets \beta_2\, v_{t-1} + (1 - \beta_2)\, g_t \odot g_t$ \Comment{Second moment (curvature)}
    \State $\hat{m}_t \gets m_t\, /\, (1 - \beta_1^t)$ \Comment{Bias correction}
    \State $\hat{v}_t \gets v_t\, /\, (1 - \beta_2^t)$
    \State $x_{t+1} \gets x_t - \alpha\, \hat{m}_t\, /\, (\sqrt{\hat{v}_t} + \varepsilon)$
\EndFor
\end{algorithmic}
\end{algorithm}

\subsection{Hyperparameter Settings}

\begin{table}[H]
\centering
\caption{Q2: Hyperparameter settings. Nesterov and Adam run for 150 iterations.}
\label{tab:q2_params}
\begin{tabular}{llccc}
\toprule
\textbf{Method} & \textbf{Parameter} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & $\alpha$ & 0.08 & 0.06 & 0.0012 \\
\midrule
Nesterov & $\alpha$ & 0.06 & 0.035 & 0.0007 \\
 & $\beta_{\max}$ & 0.90 & 0.92 & 0.90 \\
\midrule
Adam & $\alpha$ & 0.12 & 0.08 & 0.006 \\
 & $\beta_1$ & 0.82 & 0.80 & 0.80 \\
 & $\beta_2$ & 0.999 & 0.999 & 0.999 \\
 & $\varepsilon$ & $10^{-8}$ & $10^{-8}$ & $10^{-8}$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Convergence Comparison}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_A.pdf}
\caption{Q2: Convergence on Benchmark A (Linear Regression). All three methods converge to $J^{\star} \approx 0.4826$ (the noise floor). Nesterov and Adam reach the minimum in fewer iterations than GD due to acceleration and adaptive scaling, respectively. The quadratic loss surface is well-suited to all methods and does not differentiate them strongly at convergence.}
\label{fig:q2_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_B.pdf}
\caption{Q2: Convergence on Benchmark B (Toy Neural Network). Both Nesterov and Adam converge to $f^{\star} \approx 0.7244$ in fewer iterations than GD. Adam's per-coordinate adaptive scaling effectively handles the $3.5\!:\!10$ curvature ratio by adjusting the effective learning rate independently for $x_1$ (mild curvature) and $x_2$ (steeper curvature). Nesterov's lookahead prevents overshoot across the narrow $x_2$ direction.}
\label{fig:q2_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_C.pdf}
\caption{Q2: Convergence on Benchmark C (Rosenbrock). The ill-conditioned Rosenbrock function reveals the most significant differences. Nesterov achieves $f = 0.479$ --- a seven-fold improvement over GD ($f = 3.388$) --- by building momentum along the consistent valley-floor direction. Adam reaches $f = 1.579$; while its per-coordinate scaling helps decouple the transverse and longitudinal curvatures, the nonlinear curvature of the Rosenbrock valley ($x_2 = x_1^2$) limits the benefit of coordinate-wise adaptation compared to Nesterov's directional momentum.}
\label{fig:q2_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q2: Final objective values after 150 iterations.}
\label{tab:q2_final}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & 0.4826 & 0.7244 & 3.3880 \\
Nesterov & \textbf{0.4826} & \textbf{0.7244} & \textbf{0.4788} \\
Adam & 0.4826 & 0.7244 & 1.5785 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Contour Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q2_contour_B.pdf}
\caption{Q2: Trajectories on Benchmark B. Nesterov's trajectory shows the characteristic smooth arc of accelerated methods, avoiding the zig-zag pattern common in GD on anisotropic surfaces. The lookahead correction projects the gradient at a more informative point, allowing the method to navigate the slightly anisotropic landscape more efficiently. Adam takes more direct steps by independently scaling each coordinate.}
\label{fig:q2_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q2_contour_C.pdf}
\caption{Q2: Trajectories on Benchmark C. All methods descend quickly to the valley floor but differ substantially in their progress \emph{along} the valley. Nesterov's momentum builds significant acceleration in the longitudinal valley direction, while GD's constant step size severely limits valley traversal. Adam's trajectory shows moderate progress, better than GD but slower than Nesterov on this highly anisotropic function.}
\label{fig:q2_contour_C}
\end{figure}

\subsection{Mini-Batch Stochastic Gradient Descent}

\textbf{Theoretical framework.} Mini-batch SGD on a finite dataset of $m$ samples with batch size $b$ approximates the gradient by:
\begin{equation}
g_{\mathcal{B}} = \frac{1}{b}\sum_{i \in \mathcal{B}} \nabla f_i(\theta) \approx \nabla f(\theta) = \frac{1}{m}\sum_{i=1}^m \nabla f_i(\theta).
\end{equation}
The approximation error has variance $\sigma^2/b$ where $\sigma^2 = \frac{1}{m}\sum_i \|\nabla f_i(\theta) - \nabla f(\theta)\|^2$ is the per-sample gradient variance. Each epoch of $m/b$ steps is equivalent to one full gradient pass in terms of data access. With fixed learning rate $\alpha$, SGD converges to an $O(\alpha\sigma^2/b)$ neighbourhood of $\theta^{\star}$. The fundamental result is that smaller batches are cheaper per epoch but noisier; larger batches are more expensive per epoch but provide a more accurate gradient direction.

Mini-batch SGD on Benchmark A uses learning rate $\alpha = 0.06$, batch sizes $b \in \{5, 40\}$, 50 epochs (with the full dataset of $m=1000$ samples shuffled at each epoch).

\begin{algorithm}[H]
\caption{Mini-Batch SGD}
\label{alg:sgd}
\begin{algorithmic}[1]
\Require Data $(X, y)$, initial $\theta_0$, learning rate $\alpha$, batch size $b$, epochs $E$
\For{$e = 1, 2, \ldots, E$}
    \State Randomly permute data indices
    \For{each mini-batch $\mathcal{B}$ of size $b$}
        \State $g \gets \frac{1}{b} X_{\mathcal{B}}^T(X_{\mathcal{B}}\theta - y_{\mathcal{B}})$ \Comment{Stochastic gradient}
        \State $\theta \gets \theta - \alpha\, g$
    \EndFor
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_sgd_batch.pdf}
\caption{Q2: Mini-batch SGD with different batch sizes ($b = 5$ and $b = 40$), measured in epochs. With $b = 5$, each epoch performs $1000/5 = 200$ parameter updates, providing faster per-epoch convergence but higher gradient variance (each mini-batch gradient deviates from the true gradient by $O(1/\sqrt{b})$). With $b = 40$, each epoch has $1000/40 = 25$ updates, resulting in smoother but slower per-epoch progress. The smaller batch size reaches lower loss faster in epoch terms due to more frequent updates, but exhibits more oscillation near the minimum. This illustrates the fundamental variance--efficiency trade-off in stochastic optimisation.}
\label{fig:q2_sgd_batch}
\end{figure}

\subsection{SGD with Increased Observation Noise}

The experiment is repeated with output noise scaled by a factor of 6 ($\sigma = 6$ vs $\sigma = 1$), so $y_{\mathrm{noisy}}^{(i)} = \theta^{\star T}x^{(i)} + 6\varepsilon^{(i)}$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q2_sgd_noisy.pdf}
\caption{Q2: Effect of observation noise on SGD convergence. Left: original noise ($\sigma = 1$, $J^{\star} \approx 0.483$). Right: high noise ($\sigma = 6$, $J^{\star} \approx 17.4$). Increasing the noise by factor 6 raises the irreducible loss floor by factor $\sigma^2 = 36$ (since $J^{\star} = \frac{1}{2}\mathbb{E}[\varepsilon^2] \propto \sigma^2$). Under high noise, both batch sizes converge to approximately the same elevated floor, and the relative advantage of smaller batches diminishes: the observation noise in the gradient signal ($O(\sigma^2/b)$ variance) dominates the mini-batch sampling noise, making both batch sizes behave similarly. This illustrates the distinction between \emph{model noise} (irreducible) and \emph{sampling noise} (reducible by increasing batch size).}
\label{fig:q2_sgd_noisy}
\end{figure}

\subsection{Convergence Rate and Variance Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q2_rate_variance.pdf}
\caption{Q2: Left: convergence comparison on Rosenbrock with $O(1/k)$ and $O(1/k^2)$ reference curves (right y-axis). Nesterov's trajectory more closely follows the $O(1/k^2)$ theoretical rate, while GD follows $O(1/k)$. Adam falls between the two. Right: SGD loss averaged over 5 random seeds with $\pm 1$ standard deviation bands. The smaller batch size ($b=5$) achieves lower mean loss more quickly but with higher variance (wider bands), confirming the variance--efficiency trade-off.}
\label{fig:q2_rate_var}
\end{figure}

\subsection{Discussion}

The results confirm the theoretical superiority of Nesterov acceleration: on the challenging Rosenbrock function, Nesterov achieves $f = 0.479$ vs GD's $f = 3.388$ in the same 150 iterations --- a factor of 7 improvement attributable to the $O(1/k^2)$ vs $O(1/k)$ rate gap. The lookahead gradient evaluation is the crucial mechanism: by evaluating at $\tilde{x}_k = x_k + \beta_k z_k$ rather than $x_k$, Nesterov anticipates the trajectory's future position and corrects before overshooting, unlike Heavy Ball which only reacts to the current gradient.

Adam's per-coordinate scaling provides substantial benefit when curvature varies dramatically across dimensions (as in deep learning with highly heterogeneous features), but on the smooth Rosenbrock function where the dominant challenge is the nonlinear longitudinal curvature (not coordinate-wise heterogeneity), directional momentum (Nesterov) outperforms coordinate-wise adaptation (Adam).

\textbf{Lyapunov proof of Nesterov's $O(1/k^2)$ rate.} Consider the energy sequence $E_k = (k+1)^2(f(x_k)-f^{\star}) + \frac{1}{\alpha}\|v_k\|^2$ where $v_k = x_k + \frac{k-1}{k+1}(x_k - x_{k-1})$ is the auxiliary momentum variable. Using the gradient Lipschitz inequality and the identity $\beta_k = (k-1)/(k+2)$, one can show that $E_{k+1} \leq E_k$, i.e., the energy is \emph{non-increasing}. Since $E_0 = (f(x_0)-f^{\star})/\alpha + \frac{1}{\alpha}\|x_0-x^{\star}\|^2$ is bounded, this gives:
\begin{equation}
f(x_k) - f^{\star} \leq \frac{E_0}{(k+1)^2} \leq \frac{2\|x_0-x^{\star}\|^2}{\alpha(k+1)^2} = O\!\left(\frac{1}{k^2}\right).
\end{equation}
This Lyapunov argument confirms that the $O(1/k^2)$ rate is an \emph{exact consequence} of the schedule $\beta_k = (k-1)/(k+2)$, not merely a heuristic. The heavy-ball method lacks this energy structure and has no proven $O(1/k^2)$ guarantee in the general convex case, despite performing well on quadratics.

\textbf{SGD theory.} Mini-batch SGD with batch size $b$ introduces gradient variance $\sigma^2_g = \text{Var}[\nabla f_{\mathcal{B}}(\theta)] = O(\sigma^2_{\mathrm{obs}}/b)$ per iteration. This creates a fundamental lower bound on achievable loss even with infinitely many iterations (the \emph{noise floor}). A decaying learning rate $\alpha_t = O(1/t)$ is needed to achieve the optimal $O(\sigma^2_{\mathrm{obs}}/(bT))$ convergence rate, but fixed $\alpha$ as used here converges to an $O(\alpha\sigma^2_g)$ neighbourhood of the optimum.

% ============================================================
\section{Question 3: Newton's Method and Local Approximation \hfill [30 marks]}
\label{sec:q3}
% ============================================================

\subsection{Local Approximations of $g(x) = x^4$}

\textbf{First-order (Taylor) approximation} around $x_0 = 0.25$:
\begin{equation}
g(x) \approx g(x_0) + g'(x_0)(x - x_0) = (0.25)^4 + 4(0.25)^3(x - 0.25) = 0.00391 + 0.0625(x - 0.25).
\end{equation}

\textbf{Second-order approximation:}
\begin{equation}
g(x) \approx g(x_0) + g'(x_0)(x - x_0) + \tfrac{1}{2}g''(x_0)(x - x_0)^2 = 0.00391 + 0.0625(x - 0.25) + 0.1875(x - 0.25)^2.
\end{equation}

The Newton step minimises the second-order model: setting $g'(x_0) + g''(x_0)(x - x_0) = 0$ gives
$x^+ = x_0 - g'(x_0)/g''(x_0) = 0.25 - 0.0625/0.75 = 0.167.$
This is the Newton update $x_{k+1} = x_k - g'(x_k)/g''(x_k)$ --- a step to the stationary point of the local quadratic model.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_approximation.pdf}
\caption{Q3: Local approximations of $g(x) = x^4$ at $x_0 = 0.25$. The first-order approximation (tangent line) captures only the local slope and deviates rapidly from the true function. The second-order approximation (parabola) captures local curvature and fits accurately in a neighbourhood of $x_0$. The minimum of the parabola (at $x = 0.167$) provides a better estimate of the function's minimum than a gradient step, which only follows the slope. This motivates Newton's method: each iteration finds the exact minimiser of the local quadratic model, then re-fits the model at the new point.}
\label{fig:q3_approx}
\end{figure}

\subsection{Newton's Method}

\begin{algorithm}[H]
\caption{Newton's Method with Damping}
\label{alg:newton}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, damping $\lambda$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $H_k \gets \nabla^2 f(x_k) + \lambda I$ \Comment{Tikhonov-regularised Hessian}
    \State $p_k \gets H_k^{-1}\, g_k$ \Comment{Newton direction}
    \State $x_{k+1} \gets x_k - \alpha\, p_k$
\EndFor
\end{algorithmic}
\end{algorithm}

The \textbf{Newton direction} $p_k = H_k^{-1}g_k$ is the gradient preconditioned by the inverse Hessian, effectively removing the ill-conditioning introduced by anisotropic curvature. The \textbf{damping} $\lambda I$ regularises the Hessian to ensure positive definiteness even when the Hessian is indefinite far from a local minimum (relevant for the Rosenbrock function). The \textbf{step size} $\alpha < 1$ (damped Newton) ensures global progress when the quadratic model is not globally accurate.

\textbf{Convergence theory.} Near a local minimiser $x^{\star}$ where $\nabla^2 f(x^{\star}) \succ 0$, Newton's method converges \emph{quadratically}:
\begin{equation}
\|x_{k+1} - x^{\star}\| \leq C\|x_k - x^{\star}\|^2,
\end{equation}
for some constant $C = L_H/(2\lambda_{\min}(\nabla^2 f(x^{\star})))$, where $L_H$ is the Lipschitz constant of $\nabla^2 f$. This means once the iterate is close to $x^{\star}$, the number of accurate digits \emph{doubles} per iteration (quadratic convergence).

\textbf{One-step convergence for quadratics.} For $f(\theta) = \frac{1}{2m}\|X\theta - y\|^2$, the Hessian $H = X^TX/m$ is constant. The Newton update with $\alpha = 1$ is:
\begin{align}
\theta_{k+1} &= \theta_k - H^{-1}\nabla J(\theta_k) = \theta_k - H^{-1} H(\theta_k - \theta^{\star}_{\mathrm{OLS}}) = \theta^{\star}_{\mathrm{OLS}},
\end{align}
reaching the exact OLS solution in a single step regardless of starting point. This is confirmed by Figure~\ref{fig:q3_fval_A}.

\textbf{Geometric interpretation.} Newton's direction $p_k = H_k^{-1}g_k$ is equivalent to the gradient direction in the \emph{Mahalanobis norm} $\|x\|_H = \sqrt{x^T H x}$: it points toward $x^{\star}$ directly in the geometry defined by the Hessian. This is why Newton is immune to condition number: it implicitly rescales the problem so all eigenvalue directions are treated equally. A gradient step in the original Euclidean geometry is pulled towards the high-curvature direction (where the function changes rapidly), causing slow progress in the low-curvature valley direction. The Newton step aligns with the valley by normalising each direction by its curvature.

\textbf{Kantorovitch / local convergence theorem.} Let $x^{\star}$ be a minimiser with $\nabla f(x^{\star}) = 0$ and $\nabla^2 f(x^{\star}) \succ 0$, and suppose $\nabla^2 f$ is $L_H$-Lipschitz. Define the \emph{Newton decrement} $\lambda(x) = \|\nabla^2 f(x)^{-1/2}\nabla f(x)\|$. If $\lambda(x_0) < \lambda^{\star} := \mu/(2L_H)$ (the local basin), then full Newton steps ($\alpha = 1$) satisfy:
\begin{equation}
\lambda(x_{k+1}) \leq \frac{L_H}{2\mu^2}\lambda(x_k)^2,
\end{equation}
confirming quadratic convergence. In practice the basin radius is $O(\mu^2/L_H)$: for Benchmark A ($L_H = 0$, constant Hessian) the basin is infinite, giving one-step convergence; for Rosenbrock $L_H \approx 2400$ is large and $\mu \approx 0.4$ is small, giving a tiny local basin, which explains why damping ($\alpha = 0.22$) is essential --- full steps would overshoot until the iterate enters the small local basin.

\textbf{Damped Newton for non-convex regions.} On Rosenbrock, the Hessian $H_C(x)$ can be indefinite far from the optimum (specifically when $2+1200x_1^2 < 400x_2$, i.e., when the off-diagonal dominates). The regularisation $H_k + \lambda I$ with $\lambda = 10^{-8}$ ensures positive definiteness throughout, though it is rarely needed given the small regularisation parameter. The step size $\alpha = 0.22$ provides the main safety net against overshooting.

Settings: GD uses 80 iterations; Newton uses 20 iterations with Hessian damping $\lambda = 10^{-8}$.

\begin{table}[H]
\centering
\caption{Q3: Hyperparameters for GD and Newton.}
\label{tab:q3_params}
\begin{tabular}{lcccccc}
\toprule
 & \multicolumn{2}{c}{\textbf{Bench.\ A}} & \multicolumn{2}{c}{\textbf{Bench.\ B}} & \multicolumn{2}{c}{\textbf{Bench.\ C}} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5} \cmidrule(lr){6-7}
 & GD & Newton & GD & Newton & GD & Newton \\
\midrule
$\alpha$ & 0.08 & 1.0 & 0.06 & 0.85 & 0.001 & 0.22 \\
Iterations & 80 & 20 & 80 & 20 & 80 & 20 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Convergence Comparison}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_A.pdf}
\caption{Q3: Newton vs GD on Benchmark A (Linear Regression). Newton's method with $\alpha = 1.0$ converges in \textbf{one iteration} to $J = 0.4826$. This is the expected result for quadratic objectives: since $J(\theta) = \frac{1}{2m}\|X\theta - y\|^2$ has constant Hessian $H = X^TX/m$, the Newton update $\theta_{k+1} = \theta_k - H^{-1}\nabla J(\theta_k)$ is equivalent to the exact OLS solution regardless of starting point. GD requires 80 iterations to reach comparable accuracy, demonstrating the dramatic advantage of second-order information for quadratic problems.}
\label{fig:q3_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_B.pdf}
\caption{Q3: Newton vs GD on Benchmark B (Toy Neural Network). Newton converges in approximately 5 iterations due to the near-quadratic structure (the non-quadratic term $\sin(x_1)$ contributes a bounded third derivative). The Hessian $H = \mathrm{diag}(2 - \sin(x_1), 10)$ varies slowly, so the quadratic model remains accurate across steps. Final value: Newton $f = 0.7244$, matching GD but in far fewer iterations.}
\label{fig:q3_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_C.pdf}
\caption{Q3: Newton vs GD on Benchmark C (Rosenbrock). Newton with $\alpha = 0.22$ reaches $f = 0.686$ in 20 iterations, a five-fold improvement over GD's $f = 3.732$ in 80 iterations. The heavy damping ($\alpha = 0.22$) is necessary because the Rosenbrock Hessian varies rapidly along the valley (the mixed curvature term $-400x_1$ changes significantly), so full Newton steps ($\alpha = 1$) would overshoot. Despite the damping, Newton's curvature information allows it to step efficiently along the valley without the extreme step-size restriction that confines GD.}
\label{fig:q3_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q3: Final objective values and converged positions.}
\label{tab:q3_final}
\begin{tabular}{lcccc}
\toprule
\textbf{Benchmark} & \textbf{GD $f$ (80 it.)} & \textbf{Newton $f$ (20 it.)} & \textbf{Newton final $x$} & \textbf{Improvement} \\
\midrule
A & 0.4826 & 0.4826 & $(3.028, 4.017)$ & $\approx 1 \times$ (both optimal) \\
B & 0.7244 & 0.7244 & $(0.582, 2.000)$ & $\approx 1 \times$ (both optimal) \\
C & 3.7319 & 0.6864 & $(0.183, 0.020)$ & $\approx 5.4 \times$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Contour Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q3_contour_B.pdf}
\caption{Q3: Newton vs GD trajectories on Benchmark B. Newton takes large, well-directed steps that account for the curvature difference between coordinates, reaching the minimum in approximately 5 steps. GD takes many small steps; the anisotropic curvature (10:1 ratio in $x_2$:$x_1$ second derivatives) causes the $x_2$ component to converge first (steeper curvature $\Rightarrow$ faster convergence in that direction), with $x_1$ following more slowly due to the smaller curvature plus the non-linear $\sin$ term.}
\label{fig:q3_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q3_contour_C.pdf}
\caption{Q3: Newton vs GD trajectories on Rosenbrock. Newton's trajectory follows the valley floor more closely by exploiting the local curvature information from the Hessian, which knows both the transverse (steep) and longitudinal (shallow) curvatures. GD is restricted to tiny steps ($\alpha = 0.001$) to avoid instability in the steep transverse direction, causing negligible longitudinal progress in 80 iterations. Newton reaches a much better position along the valley in only 20 iterations.}
\label{fig:q3_contour_C}
\end{figure}

\subsection{Update Magnitude Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q3_update_magnitude.pdf}
\caption{Q3: Update magnitude $\|x_{k+1} - x_k\|$ vs iteration for GD and Newton across all three benchmarks. Newton's update magnitude decreases rapidly --- on Benchmark A it drops to near-zero after the first step (single-step convergence for quadratics). On Benchmarks B and C, Newton's updates exhibit the characteristic rapid decay of quadratic convergence: each subsequent step is much smaller than the previous, confirming the superlinear convergence rate. GD's magnitude decreases linearly (geometric decrease each step), consistent with its linear convergence rate. On Rosenbrock, Newton maintains larger but rapidly decreasing updates, indicating more productive and efficient steps.}
\label{fig:q3_update}
\end{figure}

\subsection{Distance to Optimum Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q3_dist_to_opt.pdf}
\caption{Q3: Distance to optimum $\|x_k - x^{\star}\|$ on a logarithmic scale for Newton and GD. Left (Benchmark B): Newton reaches near-machine-precision accuracy within 10 iterations; the rapid decay confirms (local) superlinear convergence. GD decreases linearly (straight line on log scale), consistent with its linear convergence rate. Right (Rosenbrock): Newton again achieves much faster progress than GD, though the large damping ($\alpha = 0.22$) required for stability slows the early iterations. GD barely progresses in the same number of steps due to the extreme condition number ($\kappa \approx 2508$).}
\label{fig:q3_dist}
\end{figure}

\subsection{Complexity and Practical Considerations}

Newton's method requires $O(n^2)$ operations to compute the Hessian and $O(n^3)$ to solve the linear system $H_k p_k = g_k$ (via Cholesky or LU decomposition). For the $n=2$ benchmarks used here, this is trivial, but for modern machine learning problems with $n = 10^6$ to $10^9$ parameters, Newton's method is computationally infeasible. This motivates quasi-Newton methods (BFGS, L-BFGS) which approximate $H^{-1}$ without computing it explicitly. The results here illustrate \emph{why} second-order methods are desirable; practical large-scale implementations require Hessian approximations.

\begin{table}[H]
\centering
\caption{Q3: Computational complexity comparison between GD and Newton's method.}
\label{tab:q3_complexity}
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{Gradient cost} & \textbf{Hessian cost} & \textbf{Linear solve} & \textbf{Convergence rate} \\
\midrule
GD & $O(nd)$ & --- & --- & Linear: $O(1/k)$ \\
Newton & $O(nd)$ & $O(nd^2)$ & $O(d^3)$ & Quadratic: $O(1/\rho^{2^k})$ \\
Damped Newton & $O(nd)$ & $O(nd^2)$ & $O(d^3)$ & Linear then quadratic \\
\bottomrule
\end{tabular}
\end{table}
Here $n$ is the dataset size and $d$ is the parameter dimension. For $d=2$ (as in these experiments), the Hessian and linear solve are negligible. For $d = 1000$ (a small neural network), Newton costs $10^9$ operations per step versus $10^6$ for GD.

% ============================================================
\section{Question 4: Derivative Approximation and Derivative-Free Optimisation \hfill [30 marks]}
\label{sec:q4}
% ============================================================

\subsection{Part A: Finite Difference and Nesterov Random Search (Benchmark B)}

\subsubsection{Finite Difference Gradient Approximation}

When the analytical gradient is unavailable (black-box function, non-differentiable objective, or legacy simulation code), the gradient can be approximated numerically. The forward finite-difference approximation replaces each partial derivative:
\begin{equation}
\frac{\partial f}{\partial x_i}(x) \approx \frac{f(x + \delta e_i) - f(x)}{\delta}, \qquad e_i \in \mathbb{R}^n \text{ is the $i$-th unit vector.}
\end{equation}

\textbf{Error analysis.} By Taylor's theorem:
\begin{equation}
f(x + \delta e_i) = f(x) + \delta \frac{\partial f}{\partial x_i}(x) + \frac{\delta^2}{2}\frac{\partial^2 f}{\partial x_i^2}(\xi),
\end{equation}
so the \emph{truncation error} is $O(\delta)$. In floating-point arithmetic, $f(x + \delta e_i) - f(x)$ also incurs \emph{cancellation error} of $O(\varepsilon_{\mathrm{mach}}/\delta)$ when $\delta$ is small. Minimising the total error $O(\delta) + O(\varepsilon_{\mathrm{mach}}/\delta)$ yields the optimal step size
\begin{equation}
\delta^{\star} \approx \sqrt{\varepsilon_{\mathrm{mach}}} \approx 10^{-8} \quad \text{(for double precision)}.
\end{equation}
In practice, a larger $\delta \in [10^{-5}, 10^{-3}]$ is often used to reduce sensitivity to function noise. The central difference approximation $[f(x+\delta e_i) - f(x-\delta e_i)]/(2\delta)$ has error $O(\delta^2)$ but requires $2n$ function evaluations per gradient.

\subsubsection{Nesterov Random Search}

\begin{algorithm}[H]
\caption{Nesterov Random Search (Zeroth-Order)}
\label{alg:nrs}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, perturbation $\delta$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State Sample $u_k \sim \mathcal{N}(0, I)$, normalise: $u_k \gets u_k / \|u_k\|$
    \State $\hat{g}_k \gets \dfrac{f(x_k + \delta u_k) - f(x_k)}{\delta}\, u_k$ \Comment{Directional derivative estimate}
    \State $x_{k+1} \gets x_k - \alpha\, \hat{g}_k$
\EndFor
\end{algorithmic}
\end{algorithm}

This zeroth-order method estimates the directional derivative along a random unit vector $u_k$, requiring only \emph{two function evaluations per iteration} regardless of dimension $n$. The gradient estimate is an unbiased estimator of a \emph{smoothed} gradient:
\begin{equation}
\mathbb{E}_u\!\left[\frac{f(x+\delta u) - f(x)}{\delta} u\right] = \nabla f_\delta(x), \qquad f_\delta(x) = \mathbb{E}_u[f(x + \delta u)],
\end{equation}
where $f_\delta$ is the Gaussian-smoothed version of $f$. As $\delta \to 0$, $\nabla f_\delta(x) \to \nabla f(x)$. The convergence rate is $O(1/\sqrt{k})$, the same order as SGD, but with higher effective variance than FD due to using only one random direction. The advantage is clear from scalability: on a $d$-dimensional function, FD requires $d+1$ evaluations per gradient (cost scales with $d$), while Nesterov random search requires only 2 (dimension-independent).

\textbf{Variance analysis.} The variance of the gradient estimator is:
\begin{equation}
\mathrm{Var}[\hat{g}_k] = \mathbb{E}\!\left[\left\|\hat{g}_k - \nabla f_\delta(x_k)\right\|^2\right] \leq \frac{\|\nabla f(x_k)\|^2 (d-1)}{d},
\end{equation}
proportional to $d$ compared to the finite-difference estimator whose variance (over independent perturbations) is $O(d^{-1})$ per coordinate. This $d$-fold variance penalty is the precise mechanism by which high-dimensional random search is slow: the single random direction captures only $O(1/d)$ of the gradient signal. In the $d=2$ experiments here, the variance is only a factor of $\tfrac{1}{2}$ smaller than the signal, so Nesterov random search performs reasonably; the gap to FD would widen substantially for $d \gg 2$.

\subsubsection{Finite Difference Step-Size Error Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q4_fd_error_delta.pdf}
\caption{Q4: Finite difference gradient error $\|\hat{g} - g\|$ vs step size $\delta$ at a fixed test point in Benchmark B. Left: forward FD shows a clear minimum near $\delta \approx 10^{-7}$ (close to $\sqrt{\varepsilon_{\mathrm{mach}}} \approx 10^{-8}$). For $\delta \gg 10^{-7}$: truncation error dominates, scaling as $O(\delta)$ (green dashed guide). For $\delta \ll 10^{-7}$: floating-point cancellation dominates, scaling as $O(\varepsilon_{\mathrm{mach}}/\delta)$ (magenta dashed guide). Right: central difference achieves $O(\delta^2)$ truncation error and a lower minimum at $\delta \approx \varepsilon_{\mathrm{mach}}^{1/3} \approx 10^{-5}$ (purple dotted line), justifying use of central differences when function evaluations are cheap. The tested values $\delta = 0.05$ and $\delta = 0.8$ are both in the truncation-dominated region; $\delta = 0.05$ achieves a smaller truncation error than $\delta = 0.8$.}
\label{fig:q4_fd_error}
\end{figure}

\subsubsection{Results}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q4_fval_B.pdf}
\caption{Q4A: Convergence comparison on Benchmark B. Exact GD converges fastest ($f \approx 0.724$). FD GD with $\delta = 0.05$ closely tracks exact GD, confirming high gradient approximation quality: for this function scale, $\delta = 0.05$ is well within the truncation-dominated regime where gradient errors are $O(0.05)$ in absolute terms. FD GD with $\delta = 0.8$ converges poorly: the large step spans a region where the curvature changes significantly ($\sin(x_1)$ varies), introducing $O(0.8)$ truncation error that corrupts the gradient direction. Nesterov random search converges slowest: using a single random direction per step provides only a noisy one-dimensional projection of the true $\mathbb{R}^2$ gradient.}
\label{fig:q4_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q4_contour_B.pdf}
\caption{Q4A: Trajectories on Benchmark B. Exact GD and good FD ($\delta = 0.05$) follow nearly identical paths, confirming gradient fidelity. Poor FD ($\delta = 0.8$) deviates due to inaccurate gradient direction resulting from excessive truncation error. Nesterov random search follows a noisy, wandering path typical of zeroth-order methods: each step is in a random direction with only a stochastic gradient component in the true gradient direction.}
\label{fig:q4_contour_B}
\end{figure}

\subsection{Part B: Derivative-Free Methods on Benchmark C}

\subsubsection{Nelder--Mead Simplex Method}

The Nelder--Mead algorithm maintains a simplex of $n+1 = 3$ vertices $\{v_0, v_1, v_2\}$ in $\mathbb{R}^2$, ordered so $f(v_0) \leq f(v_1) \leq f(v_2)$ (best to worst). Let $\bar{x} = \frac{1}{n}\sum_{i=0}^{n-1} v_i$ be the centroid of all but the worst vertex. The four operations are:
\begin{enumerate}[nosep]
\item \textbf{Reflection} ($\alpha_r = 1.0$): $x_r = \bar{x} + \alpha_r(\bar{x} - v_2)$. If $f(v_0) \leq f(x_r) < f(v_1)$, replace $v_2 \leftarrow x_r$.
\item \textbf{Expansion} ($\gamma = 2.0$): if $f(x_r) < f(v_0)$, try $x_e = \bar{x} + \gamma(\bar{x} - v_2)$; keep whichever of $x_r, x_e$ is better.
\item \textbf{Contraction} ($\rho = 0.5$): if reflection failed, try $x_c = \bar{x} + \rho(\cdot - \bar{x})$ (outside or inside contraction).
\item \textbf{Shrink} ($\sigma = 0.5$): if contraction fails, replace $v_i \leftarrow v_0 + \sigma(v_i - v_0)$ for $i > 0$.
\end{enumerate}
No gradient information is required; the simplex explores the landscape purely through function evaluations. Standard parameters $(\alpha_r, \gamma, \rho, \sigma) = (1, 2, 0.5, 0.5)$ are used, with initial simplex step $0.35$ and $160$ iterations.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q4_nelder_mead_C.pdf}
\caption{Q4B: Nelder--Mead on Rosenbrock. Left: the simplex centroid trajectory navigates the curved banana-shaped valley without any gradient information, demonstrating that the simplex geometry adapts to the local landscape through reflection and contraction operations. Right: the best objective value decreases steadily, with occasional plateaus when the simplex undergoes shrink operations (contracting all vertices towards the best, which temporarily stalls progress). The Nelder--Mead simplex is a robust local search method that requires no derivatives, making it suitable for noisy or non-differentiable objectives.}
\label{fig:q4_nm}
\end{figure}

\subsubsection{Grid Search}

Grid search evaluates the objective on a uniform $55 \times 55 = 3025$ grid over $x_1 \in [-2, 2]$, $x_2 \in [-1, 3]$ (resolution $\Delta x \approx 0.073$).

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q4_grid_search_C.pdf}
\caption{Q4B: Grid search on Rosenbrock. Left: the 3025 grid points cover the search region uniformly; the best point found is $(1.0, 1.0)$ to within grid resolution. Right: the best-so-far curve shows step-wise improvement as progressively better grid points are discovered during the systematic scan. Grid search guarantees finding the global optimum to within resolution $\Delta x \approx 0.073$ on this 2D function. However, scaling to higher dimensions is infeasible: maintaining the same resolution in $d$ dimensions requires $O((1/\Delta x)^d)$ evaluations --- a 10-dimensional problem at the same resolution would require $\approx 10^{10}$ evaluations.}
\label{fig:q4_grid}
\end{figure}

\subsection{Computational Comparison}

\begin{table}[H]
\centering
\caption{Q4: Comparison of derivative-free and derivative-approximation methods. FE = function evaluations, $d$ = dimension.}
\label{tab:q4_comparison}
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{Gradient Info} & \textbf{FE/iter} & \textbf{Scalability (large $d$)} & \textbf{Accuracy} \\
\midrule
Exact GD & Full gradient & $d$ (gradient) & Good ($O(d)$ per step) & High \\
FD GD ($\delta$ small) & Approx.\ gradient & $d + 1$ & Moderate & High \\
FD GD ($\delta$ large) & Poor approx. & $d + 1$ & Moderate & Low \\
Nesterov random & None (zeroth-order) & 2 & \textbf{Best} ($O(1)$ per step) & Low \\
Nelder--Mead & None (function vals) & $\leq n+1$ & Poor ($n \leq 10$ typical) & Moderate \\
Grid search & None & $O((1/\Delta x)^d)$ total & Very poor (exponential) & Grid-limited \\
\bottomrule
\end{tabular}
\end{table}

The fundamental trade-off is: higher gradient information accuracy requires more function evaluations, but enables faster convergence per evaluation. Three regimes are apparent:
\begin{enumerate}[nosep]
\item \emph{Gradient available} (exact or via FD with small $\delta$): use GD-based methods. Cost $O(d)$ per step, convergence $O(1/k)$ or faster.
\item \emph{Low-dimensional black-box} ($d \leq 20$, $\leq 10^4$ evaluations budget): Nelder--Mead is effective and requires no hyperparameters beyond initial simplex size.
\item \emph{High-dimensional black-box} ($d > 20$, stochastic objective): Nesterov random search scales with $O(1)$ evaluations/step. Grid search is never practical for $d > 3$.
\end{enumerate}
Nelder--Mead lacks convergence guarantees in general (McKinnon's counterexample for $d \geq 2$ shows convergence to non-stationary points is possible), but works reliably in practice for smooth functions in low dimensions.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q4_evaluation_efficiency.pdf}
\caption{Q4: Evaluation efficiency analysis. \textbf{Left}: convergence vs total function evaluations (not iterations) on Benchmark B. Exact GD requires 1 gradient call per step, which internally computes both $f$ and $\nabla f$ in $O(d)$ evaluations; FD GD requires $d+1=3$ evaluations per step (one for the base value plus one per dimension), so its $x$-axis advances faster per iteration. Nesterov random search uses only 2 evaluations per step, making it the cheapest per step but also the least informative. Despite the overhead, FD GD closely tracks exact GD, confirming that the extra evaluations are well spent. \textbf{Right}: scaling of evaluation cost with dimension $d$. The $O(d)$ cost of FD and exact gradient methods grows linearly, while Nesterov random search maintains constant cost per step. For $d = 1000$, random search requires 2000-times fewer evaluations per iteration than FD GD, making it the only tractable option for very high-dimensional black-box functions.}
\label{fig:q4_eval_eff}
\end{figure}

% ============================================================
\section{Question 5: Constrained Optimisation \hfill [30 marks]}
\label{sec:q5}
% ============================================================

\subsection{Problem Setup}

Minimise Benchmark B, $f_B(x) = (x_1-1)^2 + 5(x_2-2)^2 + \sin(x_1)$, subject to $x_1 \geq 0.5$, starting from the \emph{infeasible} initial point $x_0 = (0.2, 4.0)$.

The unconstrained minimiser of $f_B$ is at $x^{\star} \approx (0.582, 2.0)$, satisfying $x_1^{\star} = 0.582 > 0.5$. Therefore, the constraint $x_1 \geq 0.5$ is \emph{inactive} at the optimum: the constrained and unconstrained solutions coincide. This is a non-trivial setup because while the constraint does not bind at the optimum, the starting point is infeasible, so methods must first recover feasibility and then optimise.

\begin{algorithm}[H]
\caption{Projected Gradient Descent}
\label{alg:pgd}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, projection $\Pi_{\mathcal{X}}$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_{k+1} \gets x_k - \alpha\, \nabla f(x_k)$ \Comment{Unconstrained gradient step}
    \State $x_{k+1} \gets \Pi_{\mathcal{X}}(z_{k+1})$ \Comment{Project: $x_1 \leftarrow \max(0.5,\, x_1)$}
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{algorithm}[H]
\caption{Penalty Method}
\label{alg:penalty}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, penalty weight $\lambda$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $F_{\lambda}(x) \gets f(x) + \lambda\, \max(0,\, 0.5 - x_1)$ \Comment{Penalised objective}
    \State $x_{k+1} \gets x_k - \alpha\, \nabla F_{\lambda}(x_k)$
\EndFor
\end{algorithmic}
\end{algorithm}

The penalty gradient is $\nabla F_{\lambda}(x) = \nabla f(x) + \lambda\, \partial_x[\max(0, 0.5-x_1)]$, where the subdifferential of the penalty is $-\lambda e_1$ when $x_1 < 0.5$ and $0$ otherwise (a non-smooth term).

Settings: Projected GD ($\alpha = 0.08$), Penalty with $\lambda \in \{0.15, 1.8, 4.5\}$ ($\alpha = 0.05$ for $\lambda \leq 1.8$; $\alpha = 0.03$ for $\lambda = 4.5$), unconstrained GD ($\alpha = 0.07$). All run for 100 iterations.

\subsection{Results}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q5_fval.pdf}
\caption{Q5: Objective value $f_B(x)$ vs iteration. All methods converge to $f^{\star} \approx 0.7244$. The starting value $f_B(0.2, 4.0) \approx 20.84$ is far from the minimum. Projected GD converges most smoothly due to the hard feasibility projection. The penalty methods with larger $\lambda$ converge slightly slower initially due to the additional penalty gradient, but all reach the same final value since the constraint is inactive at the optimum.}
\label{fig:q5_fval}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q5_contour.pdf}
\caption{Q5: Contour plot with constraint boundary $x_1 = 0.5$ (dashed red line) and infeasible region (shaded). The starting point $(0.2, 4.0)$ is infeasible. Projected GD immediately clips to $x_1 = 0.5$ at the first step, then follows the feasible gradient to the constrained boundary, ultimately converging to the unconstrained minimum $(0.582, 2.0)$ which lies in the feasible region. Penalty methods with small $\lambda$ spend more time in or near the infeasible region. The unconstrained GD path naturally converges to the same minimum (feasible) without needing any constraint handling, but passes through the infeasible region during early iterations.}
\label{fig:q5_contour}
\end{figure}

\subsection{Constraint Violation Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q5_violation.pdf}
\caption{Q5: Constraint violation $\max(0, 0.5 - x_1)$ on logarithmic scale. Left: full 100 iterations. Right: first 30 iterations (zoomed). Projected GD achieves \textbf{zero violation from iteration 1}: the projection operator $\Pi(x) = (\max(0.5, x_1), x_2)$ enforces $x_1 \geq 0.5$ as a hard constraint at every step. Penalty method with $\lambda = 4.5$ achieves feasibility within approximately 10 iterations as the large penalty gradient drives the iterate out of the infeasible region quickly. With $\lambda = 0.15$, the penalty gradient is weaker relative to the objective gradient, so feasibility is restored more gradually over approximately 30 iterations.}
\label{fig:q5_violation}
\end{figure}

\subsection{Complete Comparative Results}

\begin{table}[H]
\centering
\caption{Q5: Final positions and constraint status after 100 iterations for all methods. Since the unconstrained optimum $(0.582, 2.0)$ is feasible, all methods with sufficient iterations converge to the same solution.}
\label{tab:q5_final}
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{Final $x$} & \textbf{$f_B(x)$} & \textbf{Violation} & \textbf{Notes} \\
\midrule
Unconstrained GD & $(0.582, 2.000)$ & 0.7244 & 0.0000 & Ignores constraint \\
Projected GD & $(0.582, 2.000)$ & 0.7244 & 0.0000 & Hard constraint \\
Penalty $\lambda = 0.15$ & $(0.582, 2.000)$ & 0.7244 & 0.0000 & Soft constraint \\
Penalty $\lambda = 1.8$ & $(0.582, 2.000)$ & 0.7244 & 0.0000 & Soft constraint \\
Penalty $\lambda = 4.5$ & $(0.583, 2.000)$ & 0.7244 & 0.0000 & Soft constraint \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Discussion}

Since the unconstrained minimum of $f_B$ at $(0.582, 2.0)$ satisfies $x_1^{\star} \approx 0.582 > 0.5$, the constraint is inactive at the optimum. Consequently, all methods ultimately converge to the same final solution with zero constraint violation. The differences are in the \emph{convergence path} and the handling of the infeasible starting point.

\textbf{Projected GD} enforces feasibility immediately at every iteration via the projection $\Pi_{\mathcal{X}}(x) = (\max(0.5, x_1), x_2)$. This is the fundamental advantage: the iterate is always in the feasible set. The cost is requiring a projection oracle --- trivial for box constraints but potentially expensive for complex constraint sets (e.g., semidefinite programmes require projecting onto the cone of positive semidefinite matrices, costing $O(n^3)$).

\textbf{Penalty methods} trade exact feasibility for algorithmic simplicity. Only a modified gradient is needed; no projection is computed. The weight $\lambda$ controls the trade-off: small $\lambda$ is insufficient to discourage constraint violation, while large $\lambda$ enforces it more aggressively but requires smaller step sizes to maintain stability (the penalised objective has a larger Lipschitz constant near the constraint boundary). In this experiment, the inactive constraint means all penalty values ultimately achieve the same solution.

\subsection{Penalty Method Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q5_penalty_analysis.pdf}
\caption{Q5: Penalty method parameter analysis. Left: evolution of the $x_1$ coordinate over 100 iterations for all five methods. Projected GD immediately jumps to $x_1 = 0.5$ at iteration 1 (hard projection), then converges to $x_1^{\star} \approx 0.582$. The unconstrained GD enters the infeasible region ($x_1 < 0.5$) initially but converges to the same feasible optimum from the infeasible side. Penalty methods with larger $\lambda$ recover feasibility faster. Right: comparison of the penalty gradient magnitude (constant $\lambda$, active only when $x_1 < 0.5$) against the objective gradient $|\partial f_B/\partial x_1|$ in the infeasible region. A larger penalty gradient relative to the objective gradient drives faster feasibility recovery; with $\lambda = 4.5$, the penalty dominates near $x_1 = 0.5$, while $\lambda = 0.15$ is comparable to or dominated by the objective gradient.}
\label{fig:q5_penalty}
\end{figure}

\textbf{KKT conditions.} The constrained problem $\min f_B(x)$ s.t. $x_1 \geq 0.5$ has Lagrangian $\mathcal{L}(x, \mu) = f_B(x) - \mu(x_1 - 0.5)$, $\mu \geq 0$. The KKT conditions at $x^{\star} = (0.582, 2.0)$ are:
\begin{equation}
\nabla f_B(x^{\star}) = \mu^{\star} e_1, \quad \mu^{\star}(x_1^{\star} - 0.5) = 0, \quad \mu^{\star} \geq 0.
\end{equation}
Since $x_1^{\star} = 0.582 > 0.5$, complementary slackness gives $\mu^{\star} = 0$, confirming the constraint is inactive and $\nabla f_B(x^{\star}) = 0$ (same as unconstrained stationarity). When the constraint would be active (if the unconstrained minimum had $x_1 < 0.5$), the projected GD would converge to the constrained optimum on the boundary $x_1 = 0.5$ with $\mu^{\star} > 0$.

\textbf{Practical note.} For problems where the constraint is active at the optimum, the penalty method with finite $\lambda$ converges to a slightly infeasible point. As $\lambda \to \infty$, the penalty solution approaches the true constrained optimum, but larger $\lambda$ requires smaller step sizes. The \emph{augmented Lagrangian method} (ALM) addresses this by maintaining an explicit multiplier estimate $\hat{\mu}_k$ alongside a quadratic penalty:
\begin{equation}
\mathcal{L}_{\rho}(x, \hat{\mu}) = f(x) - \hat{\mu}(x_1 - 0.5) + \frac{\rho}{2}\max(0, 0.5-x_1)^2.
\end{equation}
After each inner minimisation, the multiplier is updated as $\hat{\mu}_{k+1} = \hat{\mu}_k - \rho\,\max(0, 0.5-x_1^{(k)})$ (dual ascent). ALM achieves exact primal and dual feasibility at finite $\rho$ and is the basis for ADMM (Alternating Direction Method of Multipliers), widely used in distributed and large-scale constrained optimisation.

\textbf{Active constraint identification.} A central challenge in inequality-constrained optimisation is identifying which constraints are active at the optimum before the iterates have converged. Projected gradient descent performs \emph{active set identification} implicitly: once the iterate reaches the boundary, it stays there until the projected gradient points inward. In this experiment the constraint is inactive ($\mu^{\star} = 0$, $x_1^{\star} = 0.582 > 0.5$), so projected GD correctly stays off the boundary after initial projection.

% ============================================================
\section{Question 6: Linear Programmes and Frank--Wolfe Algorithm \hfill [30 marks]}
\label{sec:q6}
% ============================================================

\subsection{Feasible Set and Linear Programme}

Feasible set: $\mathcal{X} = \{x \in \mathbb{R}^2 : 0.5 \leq x_1 \leq 5,\; -5 \leq x_2 \leq 10\}$ (a box / hyperrectangle).

\textbf{Linear Programme.} For $f(x) = a^T x$ with $a = [1, 2]^T$, minimise $f$ over $\mathcal{X}$. Since $f$ is linear, the unconstrained minimum is $-\infty$; over the bounded feasible set, the minimum is attained at a vertex. For a box, the minimisation decomposes per coordinate:
\begin{equation}
x_i^{\star} = \begin{cases} l_i & \text{if } a_i > 0 \\ u_i & \text{if } a_i < 0 \end{cases}, \qquad \text{yielding} \quad x^{\star} = (0.5, -5), \quad f^{\star} = 0.5 + 2(-5) = -9.5.
\end{equation}
This illustrates the \textbf{fundamental theorem of linear programming}: the optimum of a linear objective over a bounded convex polytope is attained at a vertex (an extreme point).

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q6_lp.pdf}
\caption{Q6-I: Feasible region $\mathcal{X}$ (blue rectangle) and linear objective contours $f(x) = x_1 + 2x_2$. The parallel level-set lines have slope $-1/2$ (from $a = [1,2]^T$). The minimum occurs at the lower-left corner $(0.5, -5)$, which minimises both $x_1$ (coefficient $+1 > 0$) and $x_2$ (coefficient $+2 > 0$) simultaneously, pushing to their respective lower bounds.}
\label{fig:q6_lp}
\end{figure}

\subsection{Frank--Wolfe Algorithm: Theory}

\begin{algorithm}[H]
\caption{Frank--Wolfe (Conditional Gradient)}
\label{alg:fw}
\begin{algorithmic}[1]
\Require Initial point $x_0 \in \mathcal{X}$, mixing parameter $\beta$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_k \gets \arg\min_{z \in \mathcal{X}}\, \nabla f(x_k)^T z$ \Comment{LP subproblem (linear minimisation oracle)}
    \State $x_{k+1} \gets \beta\, x_k + (1 - \beta)\, z_k$ \Comment{Convex combination update}
\EndFor
\end{algorithmic}
\end{algorithm}

The \textbf{Frank--Wolfe gap} provides a computable convergence certificate:
\begin{equation}
G_k := \nabla f(x_k)^T (x_k - z_k) = \max_{z \in \mathcal{X}} \nabla f(x_k)^T (x_k - z) \geq 0.
\end{equation}
At the optimum $x^{\star}$ of a convex function, $G_k = 0$ (since the first-order optimality condition $\nabla f(x^{\star})^T(z - x^{\star}) \geq 0$ for all $z \in \mathcal{X}$ implies $G_k \leq 0$, and $G_k \geq 0$ always). Thus $G_k$ measures progress towards optimality without requiring knowledge of $f^{\star}$. Moreover, for smooth convex $f$:
\begin{equation}
f(x_k) - f^{\star} \leq G_k,
\end{equation}
so $G_k$ provides an upper bound on the optimality gap that can be computed from quantities available at iteration $k$.

\textbf{Convergence theorem.} For a smooth convex $f$ with gradient Lipschitz constant $L$, using the decaying step $\gamma_k = 2/(k+2)$ in $x_{k+1} = (1-\gamma_k)x_k + \gamma_k z_k$:
\begin{equation}
f(x_k) - f^{\star} \leq \frac{2C_f}{k+2}, \quad C_f = \frac{L}{2}\,\mathrm{diam}(\mathcal{X})^2.
\end{equation}
The quantity $C_f = \frac{L}{2}\mathrm{diam}(\mathcal{X})^2$ is the ``curvature constant'' of $f$ over $\mathcal{X}$. For the interior optimum case with $f(x) = (x_1-1)^2 + (x_2-5)^2$, we have $L=2$ (gradient of $2I$), $\mathrm{diam}(\mathcal{X}) = \sqrt{(5-0.5)^2 + 15^2} \approx 15.7$, giving $C_f \approx 246$.

The present experiments use a fixed mixing parameter $\beta$ rather than $\gamma_k = 2/(k+2)$. The fixed $\beta$ corresponds to a \emph{constant} effective step size $1-\beta$ towards $z_k$, and does not provably achieve $O(1/k)$ convergence in general, though it works well in practice for interior optima.

\textbf{Key advantage.} Frank--Wolfe never requires a projection onto $\mathcal{X}$: iterates are always convex combinations of feasible points (LP vertices), hence automatically feasible. The LP subproblem over a box decomposes per coordinate in $O(d)$ operations. This makes FW particularly attractive when:
\begin{enumerate}[nosep]
\item Projections are expensive (e.g., nuclear norm ball requiring SVD computations).
\item Sparse iterates are desired (each step adds at most one vertex, so $x_k$ is a convex combination of at most $k$ vertices of $\mathcal{X}$).
\item The feasible set is defined by a computationally tractable LP.
\end{enumerate}

\subsection{Interior Optimum: $f(x) = (x_1 - 1)^2 + (x_2 - 5)^2$}

The unconstrained minimum $(1, 5)$ lies inside $\mathcal{X}$ (since $0.5 \leq 1 \leq 5$ and $-5 \leq 5 \leq 10$). Two mixing parameters are tested: $\beta = 0.90$ and $\beta = 0.985$.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q6_fw_convergence.pdf}
\caption{Q6-II: Frank--Wolfe convergence for the interior optimum. With $\beta = 0.985$ (smaller effective step $1-\beta = 0.015$ towards $z_k$), the final objective ($f = 0.0046$) is lower than with $\beta = 0.90$ ($f = 0.0089$). This result is counterintuitive: larger steps towards $z_k$ ($1-\beta = 0.10$) might be expected to converge faster. The explanation lies in the oscillation of the LP vertex $z_k$: near the interior optimum, the gradient $\nabla f(x_k) \approx 2(x_k - x^{\star})$ alternates direction as $x_k$ overshoots, causing $z_k$ to alternate between opposite vertices of $\mathcal{X}$. The conservative $\beta = 0.985$ damps this oscillation, allowing smoother convergence.}
\label{fig:q6_fw_conv}
\end{figure}

\subsection{Boundary Optimum: $f(x) = x_1^2 + x_2^2$}

The unconstrained minimum $(0, 0)$ lies outside $\mathcal{X}$ (since $x_1 \geq 0.5$). The constrained minimum is at $(0.5, 0)$ on the boundary of $\mathcal{X}$, with $f^{\star} = 0.25$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q6_fw_contour.pdf}
\caption{Q6: Frank--Wolfe trajectories. Left (interior optimum): trajectories converge towards $(1, 5)$ from the starting point $(1, 1)$. The characteristic FW ``zig-zag'' is visible as $z_k$ jumps between vertices while $x_k$ slowly averages towards the optimum. Right (boundary optimum): trajectory converges towards $(0.5, 0)$ on the lower bound of $x_1$. Frank--Wolfe naturally approaches the constraint boundary without explicit projection, since $z_k$ selects $x_1 = 0.5$ (the lower bound) once $\nabla f(x_k)_1 > 0$.}
\label{fig:q6_fw_contour}
\end{figure}

\subsection{Evolution of $x_k$ and $z_k$}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q6_evolution.pdf}
\caption{Q6: Evolution of iterates $x_k$ and LP vertices $z_k$. Top-left ($\beta = 0.90$, interior): $x_k$ converges but $z_k$ oscillates strongly between $x_2 \in \{-5, 10\}$ as the gradient direction fluctuates, confirming the oscillation mechanism. Top-right ($\beta = 0.985$, interior): the smaller effective step damps the oscillation in $x_k$, leading to smoother convergence. Bottom-left (boundary): $x_1^{(k)}$ converges to 0.5 and $x_2^{(k)}$ converges to 0, with $z_k$ stabilising once the gradient direction is clear. Bottom-right (convergence comparison): both interior runs achieve sub-0.01 objectives; the boundary case converges to $f = 0.364$ after 140 iterations (above $f^{\star} = 0.25$), indicating that the fixed $\beta = 0.93$ schedule requires more iterations for boundary convergence than the decaying schedule $\gamma_k = 2/(k+2)$.}
\label{fig:q6_evolution}
\end{figure}

\begin{table}[H]
\centering
\caption{Q6: Final Frank--Wolfe positions and objective values after the specified number of iterations.}
\label{tab:q6_final}
\begin{tabular}{lcccc}
\toprule
\textbf{Case} & \textbf{Iterations} & \textbf{Final $x$} & \textbf{$f(x)$} & \textbf{$f^{\star}$} \\
\midrule
Interior, $\beta = 0.90$ & 180 & $(1.066, 4.932)$ & 0.0089 & 0.0 \\
Interior, $\beta = 0.985$ & 180 & $(0.997, 5.067)$ & 0.0046 & 0.0 \\
Boundary, $\beta = 0.93$ & 140 & $(0.500, -0.337)$ & 0.3637 & 0.25 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Frank--Wolfe Gap Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q6_fw_gap.pdf}
\caption{Q6: Frank--Wolfe gap $G_k = \nabla f(x_k)^T(x_k - z_k) \geq 0$ (Left) and objective value (Right) for the interior optimum case. The FW gap decreases monotonically in expectation, providing a certificate of proximity to optimality: $f(x_k) - f^{\star} \leq G_k$ for convex $f$. The $O(1/k)$ reference curve (green dashed) matches the observed convergence rate qualitatively. The fixed-$\beta$ schedule converges but exhibits oscillation in the gap (not shown perfectly on log scale) due to the LP vertex $z_k$ oscillating between extremes. The conservative $\beta=0.985$ achieves lower $f$ and $G_k$ values after 180 iterations, consistent with less oscillation.}
\label{fig:q6_gap}
\end{figure}

\subsection{Interior vs Boundary Behaviour}

\textbf{Interior optimum.} Frank--Wolfe exhibits the characteristic $O(1/k)$ ``zig-zagging'' near an interior optimum. Since $z_k$ must be at a vertex of $\mathcal{X}$, the LP subproblem solution oscillates between extreme points as the gradient direction changes sign near the minimum. The iterate $x_k$ converges as an exponentially weighted average of these oscillating vertices. Smaller steps ($\beta = 0.985$, effective step $1-\beta = 0.015$) reduce oscillation near convergence.

\textbf{Boundary optimum.} Frank--Wolfe is well-suited to boundary optima: once the gradient's sign stabilises (indicating which vertex to move towards), the LP vertex $z_k$ consistently selects the constraint-active face. The iterate approaches the boundary naturally without projection. For $f(x) = x_1^2 + x_2^2$, once $x_k$ is near $(0.5, 0)$, the gradient $\nabla f = (2x_1, 2x_2) \approx (1, 0)$, so $z_k = (0.5, -5)$ (lower bound for both, since the gradient of $x_2^2$ at $x_2 \approx 0$ is $\approx 0$, oscillating in sign). The gap to $f^{\star} = 0.25$ after 140 iterations ($f = 0.364$, gap $= 0.114$) is consistent with the theoretical $O(1/k)$ bound: $2C_f/(140+2) \approx 2 \cdot 20/142 \approx 0.28$ (where $C_f = (L/2)\cdot\mathrm{diam}(\mathcal{X})^2 \approx 20$). The standard diminishing schedule $\gamma_k = 2/(k+2)$ would achieve this bound exactly.

\textbf{FW vs projected gradient comparison.} Frank--Wolfe avoids projection but converges at $O(1/k)$, while projected gradient descent achieves a faster $O(1/k^2)$ rate (with Nesterov acceleration) or linear rate $O((1-\mu/L)^k)$ for strongly convex functions. Frank--Wolfe's advantage lies in the LP oracle cost: for $\mathcal{X}$ the nuclear norm ball, projection requires $O(n^3)$ SVD while the LP (maximising a linear functional over the nuclear norm ball) is also $O(n^3)$; but for structured sets (polytopes with few vertices, flow polytopes), the LP can be much cheaper.

\textbf{Why FW cannot beat $O(1/k)$.} The $O(1/k)$ rate is tight for Frank--Wolfe, unlike gradient descent where $O(1/k^2)$ is achievable via acceleration. The fundamental obstruction is that FW iterates are \emph{sums of LP solutions} (vertices), and near an interior optimum the LP always returns a vertex far from $x^{\star}$, introducing an irreducible geometric error at each step. Formally, for any first-order FW variant with the standard LP oracle over a polytope, there exist instances where the gap decreases no faster than $\Omega(1/k)$ (Jaggi, 2013, \textit{Revisiting Frank--Wolfe}). Acceleration is not possible without enriching the oracle (e.g., line search, in-face Frank--Wolfe, or blended pairwise FW).

% ============================================================
\section{Conclusion}
\label{sec:conclusion}
% ============================================================

This report has demonstrated the comparative behaviour of 14 optimisation algorithms across three benchmark functions of increasing difficulty. The key conclusions are:

\textbf{First-order adaptive methods (Q1).} The Polyak step achieves optimal convergence when $f^{\star}$ is exactly known, but diverges or oscillates when $f^{\star}$ is misspecified. Adagrad and Heavy Ball provide robust alternatives: Adagrad through monotonically decreasing per-coordinate rates, and Heavy Ball through momentum accumulation. RMSprop's exponential forgetting produces smooth trajectories but slightly lower final accuracy on convex problems.

\textbf{Momentum and stochastic methods (Q2).} Nesterov acceleration achieves the theoretically optimal $O(1/k^2)$ rate, providing the best gradient-based performance across all benchmarks. On Rosenbrock, Nesterov reduces the objective to $0.479$ vs GD's $3.388$ in 150 iterations --- a 7-fold improvement directly attributable to the accelerated rate. Adam's per-coordinate scaling is most beneficial on problems with heterogeneous curvature across dimensions. Mini-batch SGD demonstrates the variance--efficiency trade-off: smaller batches converge faster per epoch but with more noise, and this advantage disappears when observation noise dominates mini-batch sampling noise.

\textbf{Newton's method (Q3).} Newton's method converges in one step for quadratic objectives and in $O(\log\log(1/\varepsilon))$ steps for smooth strongly convex functions (quadratic convergence). The extreme ill-conditioning of Rosenbrock ($\kappa \approx 2508$) is completely handled by the Hessian inverse in the Newton direction, whereas gradient descent requires step sizes $O(1/L) \approx O(10^{-3})$ for stability. The cost is $O(n^3)$ per iteration, limiting Newton's applicability to small- and medium-scale problems.

\textbf{Derivative-free methods (Q4).} Finite-difference gradient approximation with optimal step size closely tracks exact gradient descent, while large step sizes introduce significant truncation error. Nesterov random search requires only 2 function evaluations per step but converges slowly due to high-variance gradient estimates. For small-dimensional black-box objectives, Nelder--Mead simplex is effective; grid search provides global guarantees at exponential cost in dimension.

\textbf{Constrained optimisation (Q5).} Projected gradient descent provides hard feasibility guarantees at every iteration through the projection oracle, while penalty methods offer algorithmic simplicity at the cost of inexact constraint satisfaction during iterations. When the constraint is inactive at the optimum (as in this experiment), all methods ultimately converge to the same solution; the differences lie in feasibility recovery from the infeasible starting point.

\textbf{Frank--Wolfe (Q6).} The conditional gradient algorithm handles linear constraints without projection, naturally reaching the optimum as a convex combination of vertices. Its $O(1/k)$ convergence rate (with the standard decaying schedule) and $O(d)$ LP subproblem cost make it attractive for large-scale sparse problems. Fixed-$\beta$ schedules cause oscillation near interior optima but can be stabilised by reducing $1-\beta$.

\textbf{Cross-cutting observations.}
\begin{enumerate}[nosep]
\item \emph{Condition number is the dominant difficulty factor.} On Benchmark A ($\kappa \approx 2$), all well-specified methods converge to optimal in $\leq 20$ iterations. On Rosenbrock ($\kappa \approx 2508$), even the best first-order methods (Nesterov: $f = 0.479$) cannot reach the optimum in 150 iterations; Newton (which is immune to $\kappa$) reaches $f = 0.686$ in only 20 iterations.

\item \emph{Knowledge of problem structure yields order-of-magnitude improvements.} Having $f^{\star}$ (Polyak), the Hessian (Newton), or a projection oracle (projected GD) all provide dramatic improvements --- but at the cost of requiring additional information. Methods that require less information (RMSprop, random search, Nelder--Mead) are more generally applicable but converge more slowly.

\item \emph{The $O(1/k) \to O(1/k^2)$ gap is algorithmically meaningful.} Nesterov's acceleration achieves a 7-fold improvement over GD on Rosenbrock at 150 iterations. This gap grows with the condition number and iteration count, making acceleration methods increasingly important as problems become larger and harder.

\item \emph{Second-order information is invaluable but expensive.} Newton's single-step convergence on quadratics and 5-fold improvement on Rosenbrock demonstrate the power of the Hessian, but the $O(d^3)$ cost restricts its use. Practical large-scale implementations (L-BFGS, conjugate gradient) approximate this information at lower cost.
\end{enumerate}

\textbf{Overall recommendation.} For general unconstrained optimisation: use Adam or Nesterov for deep learning (momentum + adaptivity); use L-BFGS (a quasi-Newton method) for small-scale smooth problems. For constrained problems: use projected gradient descent when projections are cheap, Frank--Wolfe when LP subproblems are cheap. For derivative-free objectives: use Nelder--Mead for $d \leq 10$, Nesterov random search or evolutionary methods for large $d$.

\subsection*{Comprehensive Convergence Comparison on Rosenbrock}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/all_methods_rosenbrock.pdf}
\caption{Comprehensive convergence comparison: all methods from Q1--Q3 applied to the Rosenbrock function (Benchmark C, $\kappa \approx 2508$). Left: objective value $f_C(x_k)$ vs iteration on a log scale. Right: distance to optimum $\|x_k - x^{\star}\|_2$. This side-by-side view illustrates the full spectrum of convergence behaviours documented in this report. \textbf{Key observations:} (1) Polyak (correct $f^{\star}=0$) achieves the lowest first-order objective value by exploiting $f^{\star}$; (2) Nesterov (Q2) achieves the best unaided first-order convergence among methods without $f^{\star}$, with the steepest slope consistent with $O(1/k^2)$; (3) Newton (Q3) achieves the fastest distance-to-optimum decay despite only 20 iterations, demonstrating the $O(\log\log(1/\varepsilon))$ advantage of second-order methods; (4) all other first-order methods (GD, Heavy Ball, Adam, Adagrad, RMSprop) remain substantially farther from the optimum, confirming that condition number $\kappa \approx 2508$ is the dominant convergence bottleneck for first-order methods without exact $f^{\star}$.}
\label{fig:all_methods_rosenbrock}
\end{figure}

\begin{table}[H]
\centering
\caption{Comprehensive summary: all 14 methods across 3 benchmarks (final objective values after the allotted iterations). Bold = best per benchmark column.}
\label{tab:all_methods}
\begin{tabular}{llccc}
\toprule
\textbf{Q} & \textbf{Method} & \textbf{Bench A} & \textbf{Bench B} & \textbf{Bench C} \\
\midrule
\multirow{5}{*}{Q1 (120 it.)} & GD (baseline) & 0.4826 & 0.7244 & 3.5142 \\
 & Polyak & 4.7670 & 0.9819 & \textbf{0.0239} \\
 & Adagrad & 0.4826 & 0.7244 & 2.2034 \\
 & RMSprop & 0.5064 & 0.7276 & 3.1204 \\
 & Heavy Ball & 0.4826 & 0.7244 & 1.4003 \\
\midrule
\multirow{2}{*}{Q2 (150 it.)} & Nesterov & 0.4826 & 0.7244 & 0.4788 \\
 & Adam & 0.4826 & 0.7244 & 1.5785 \\
\midrule
\multirow{2}{*}{Q3 (20/80 it.)} & Newton (20) & 0.4826 & 0.7244 & 0.6864 \\
 & GD (80) & 0.4826 & 0.7244 & 3.7319 \\
\midrule
\multirow{3}{*}{Q4} & FD GD ($\delta=0.05$, 120) & --- & $\approx$0.724 & --- \\
 & Nesterov Random (220) & --- & $\approx$1.5 & --- \\
 & Nelder--Mead (160) & --- & --- & $<$0.1 \\
\midrule
Q5 & Projected GD (100) & --- & 0.7244 & --- \\
\midrule
\multirow{2}{*}{Q6} & FW Interior $\beta{=}0.985$ (180) & --- & --- & --- \\
 & FW Boundary $\beta{=}0.93$ (140) & --- & --- & --- \\
\midrule
\multicolumn{2}{l}{\textbf{Overall best}} & \textbf{0.4826} & \textbf{0.7244} & \textbf{0.0239} \\
\bottomrule
\end{tabular}
\end{table}

% ============================================================
\newpage
\appendix
\section{Python Code}
\label{app:code}
% ============================================================

The complete implementation is provided in the file \texttt{CS7DS2\_Final\_Code\_25336453.py}. Key algorithm implementations are reproduced below.

\begin{lstlisting}[caption={Gradient Descent}]
def gradient_descent(grad_fn, loss_fn, x0, alpha, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for _ in range(n_iters):
        x = x - alpha * grad_fn(x)
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Polyak Step Size}]
def polyak_step(grad_fn, loss_fn, x0, f_star, eps, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        alpha_k = (loss_fn(x) - f_star) / (np.dot(g, g) + eps)
        alpha_hist.append(alpha_k)
        x = x - alpha_k * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Adagrad}]
def adagrad(grad_fn, loss_fn, x0, alpha0, eps, n_iters):
    x = x0.copy().astype(float)
    G = np.zeros_like(x)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        G += g**2
        eff_alpha = alpha0 / (np.sqrt(G) + eps)
        alpha_hist.append(np.mean(eff_alpha))
        x = x - eff_alpha * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={RMSprop}]
def rmsprop(grad_fn, loss_fn, x0, alpha0, beta, eps, n_iters):
    x = x0.copy().astype(float)
    v = np.zeros_like(x)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        v = beta * v + (1 - beta) * g**2
        eff_alpha = alpha0 / (np.sqrt(v) + eps)
        alpha_hist.append(np.mean(eff_alpha))
        x = x - eff_alpha * g
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Heavy Ball / Polyak Momentum}]
def heavy_ball(grad_fn, loss_fn, x0, alpha, beta, n_iters):
    x = x0.copy().astype(float)
    z = np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for _ in range(n_iters):
        g = grad_fn(x)
        z = beta * z + alpha * g
        x = x - z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Nesterov Momentum}]
def nesterov_momentum(grad_fn, loss_fn, x0, alpha, beta_max, n_iters):
    x = x0.copy().astype(float)
    z = np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for k in range(1, n_iters + 1):
        beta_k = min((k - 1) / (k + 2), beta_max)
        lookahead = x + beta_k * z
        g = grad_fn(lookahead)
        z = beta_k * z - alpha * g
        x = x + z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Adam Optimiser}]
def adam_optimiser(grad_fn, loss_fn, x0, alpha, beta1, beta2, eps, n_iters):
    x = x0.copy().astype(float)
    m_vec, v_vec = np.zeros_like(x), np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for t in range(1, n_iters + 1):
        g = grad_fn(x)
        m_vec = beta1 * m_vec + (1 - beta1) * g
        v_vec = beta2 * v_vec + (1 - beta2) * g**2
        m_hat = m_vec / (1 - beta1**t)
        v_hat = v_vec / (1 - beta2**t)
        x = x - alpha * m_hat / (np.sqrt(v_hat) + eps)
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Newton's Method}]
def newtons_method(grad_fn, hess_fn, loss_fn, x0, alpha, n_iters, damping=1e-8):
    x = x0.copy().astype(float)
    x_hist, f_hist, update_norms = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        H = hess_fn(x) if callable(hess_fn) else hess_fn
        H_reg = H + damping * np.eye(len(x))
        try:
            p = np.linalg.solve(H_reg, g)
        except np.linalg.LinAlgError:
            p = g
        update_norms.append(np.linalg.norm(alpha * p))
        x = x - alpha * p
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(update_norms)
\end{lstlisting}

\begin{lstlisting}[caption={Nelder--Mead Simplex}]
def nelder_mead(loss_fn, x0, step, n_iters, alpha_r=1.0, gamma=2.0, rho=0.5, sigma=0.5):
    n = len(x0)
    simplex = np.zeros((n + 1, n))
    simplex[0] = x0.copy()
    for i in range(n):
        simplex[i + 1] = x0.copy()
        simplex[i + 1][i] += step
    f_vals = np.array([loss_fn(v) for v in simplex])
    centroid_hist = [np.mean(simplex, axis=0).copy()]
    f_best_hist = [np.min(f_vals)]
    for _ in range(n_iters):
        order = np.argsort(f_vals)
        simplex = simplex[order]; f_vals = f_vals[order]
        centroid = np.mean(simplex[:-1], axis=0)
        x_r = centroid + alpha_r * (centroid - simplex[-1])
        f_r = loss_fn(x_r)
        if f_vals[0] <= f_r < f_vals[-2]:
            simplex[-1] = x_r; f_vals[-1] = f_r
        elif f_r < f_vals[0]:
            x_e = centroid + gamma * (centroid - simplex[-1])
            f_e = loss_fn(x_e)
            if f_e < f_r: simplex[-1] = x_e; f_vals[-1] = f_e
            else: simplex[-1] = x_r; f_vals[-1] = f_r
        else:
            if f_r < f_vals[-1]:
                x_c = centroid + rho * (x_r - centroid)
                f_c = loss_fn(x_c)
                if f_c <= f_r: simplex[-1] = x_c; f_vals[-1] = f_c
                else:
                    for i in range(1, n+1):
                        simplex[i] = simplex[0]+sigma*(simplex[i]-simplex[0])
                        f_vals[i] = loss_fn(simplex[i])
            else:
                x_c = centroid + rho * (simplex[-1] - centroid)
                f_c = loss_fn(x_c)
                if f_c < f_vals[-1]: simplex[-1] = x_c; f_vals[-1] = f_c
                else:
                    for i in range(1, n+1):
                        simplex[i] = simplex[0]+sigma*(simplex[i]-simplex[0])
                        f_vals[i] = loss_fn(simplex[i])
        centroid_hist.append(np.mean(simplex, axis=0).copy())
        f_best_hist.append(np.min(f_vals))
    return np.array(centroid_hist), np.array(f_best_hist), simplex
\end{lstlisting}

\begin{lstlisting}[caption={Frank--Wolfe Algorithm}]
def frank_wolfe(grad_fn, loss_fn, x0, bounds, beta, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist, z_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        z = np.array([bounds[i][0] if g[i] > 0 else bounds[i][1]
                       for i in range(len(g))])
        z_hist.append(z.copy())
        x = beta * x + (1 - beta) * z
        x_hist.append(x.copy())
        f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(z_hist)
\end{lstlisting}

\end{document}
