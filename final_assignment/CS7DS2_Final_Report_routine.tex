\documentclass[11pt,a4paper]{article}
\usepackage[margin=2.2cm]{geometry}
\usepackage{graphicx}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{float}
\usepackage{hyperref}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{listings}
\usepackage{fancyhdr}
\usepackage{multirow}
\usepackage{array}

\hypersetup{colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue}

\lstset{
  language=Python,
  basicstyle=\ttfamily\scriptsize,
  keywordstyle=\color{blue},
  commentstyle=\color{gray},
  stringstyle=\color{red},
  numbers=left,
  numberstyle=\tiny\color{gray},
  breaklines=true,
  frame=single,
  captionpos=b
}

\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small CS7DS2 -- Final Assignment}
\fancyhead[R]{\small Swetha Sekar (25336453)}
\fancyfoot[C]{\thepage}

\newtheorem{theorem}{Theorem}
\newtheorem{proposition}{Proposition}
\newtheorem{remark}{Remark}

\title{%
  \includegraphics[width=0.25\textwidth]{tcd_logo.jpg}\\[1em]
  \textbf{CS7DS2 -- Optimisation Algorithms for Data Analysis}\\[0.5em]
  \Large Final Assignment Report
}
\author{Swetha Sekar \\ Student ID: 25336453 \\ School of Computer Science and Statistics \\ Trinity College Dublin}
\date{April 2026}

\begin{document}
\maketitle
\thispagestyle{empty}

\begin{abstract}
This report presents a systematic comparative study of first-order and second-order optimisation algorithms applied to three benchmark problems of increasing difficulty: a convex quadratic loss (Benchmark A), a mildly nonlinear loss with sinusoidal perturbation (Benchmark B), and the highly ill-conditioned Rosenbrock function (Benchmark C). Six families of methods are investigated: (1)~adaptive step-size rules including Polyak, Adagrad, RMSprop, and Heavy Ball; (2)~accelerated and adaptive methods including Nesterov acceleration and Adam, together with mini-batch SGD; (3)~Newton's method exploiting second-order curvature; (4)~derivative-free approaches including finite-difference gradient descent, Nesterov random search, Nelder--Mead, and grid search; (5)~constrained optimisation via projected gradient descent and penalty methods; and (6)~the Frank--Wolfe conditional gradient algorithm for linearly-constrained problems. Key findings include: the Polyak step size diverges when $f^\star$ is misspecified, achieving best performance only on Rosenbrock where $f^\star = 0$ exactly; Nesterov acceleration achieves the theoretically optimal $O(1/k^2)$ convergence rate on convex problems; Newton's method solves the quadratic benchmark in a single step demonstrating its affine invariance; and Frank--Wolfe's $O(1/k)$ rate is confirmed with distinct behaviour at interior versus boundary optima.
\end{abstract}

\tableofcontents
\newpage

% ============================================================
\section{Benchmark Problems and Experimental Setup}
\label{sec:benchmarks}
% ============================================================

All experiments use a fixed random seed (\texttt{np.random.seed(42)}) for reproducibility. Table~\ref{tab:benchmark_summary} summarises the key properties of each benchmark.

\begin{table}[H]
\centering
\caption{Summary of benchmark problem properties.}
\label{tab:benchmark_summary}
\begin{tabular}{lllll}
\toprule
\textbf{Benchmark} & \textbf{Function} & \textbf{Convex?} & \textbf{Cond.\ No.\ $\kappa$} & \textbf{Starting Point} \\
\midrule
A: Linear Regression & Quadratic & Yes (strongly) & $\approx 1.8$ & $\theta_0 = [0,\,0]^T$ \\
B: Toy Neural Network & Nonlinear & Approx.\ (locally) & $\approx 5$ & $x_0 = [-1,\,4]^T$ \\
C: Rosenbrock & Nonconvex & No & $\approx 2000$ & $x_0 = [-1,\,1]^T$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Benchmark A: Linear Regression Quadratic Loss}
A synthetic dataset with $m = 1000$ samples and two parameters is generated:
\begin{equation}
\theta^{\star} = \begin{bmatrix} 3 \\ 4 \end{bmatrix}, \quad x^{(i)} \sim \mathcal{N}(0, I_2), \quad y^{(i)} = \theta^{\star T} x^{(i)} + \varepsilon^{(i)}, \quad \varepsilon^{(i)} \sim \mathcal{N}(0, 1).
\end{equation}
The loss function is the mean squared error:
\begin{equation}
J(\theta) = \frac{1}{2m} \sum_{i=1}^{m} \left(\theta^T x^{(i)} - y^{(i)}\right)^2 = \frac{1}{2m}\|X\theta - y\|^2.
\label{eq:loss_A}
\end{equation}
This is a strongly convex quadratic with constant Hessian $H = \frac{1}{m}X^T X \succ 0$. The gradient is $\nabla J(\theta) = \frac{1}{m}X^T(X\theta - y)$. The Hessian's eigenvalues determine the Lipschitz constant $L = \lambda_{\max}(H)$ and strong convexity parameter $\mu = \lambda_{\min}(H)$; here $\kappa = L/\mu \approx 1.8$, making Benchmark~A the most well-conditioned problem. Due to observation noise $\varepsilon^{(i)} \sim \mathcal{N}(0,1)$, the irreducible minimum is $J^\star \approx 0.4826 \neq 0$.

\subsection{Benchmark B: Toy Neural Network Loss}
\begin{equation}
f(x_1, x_2) = (x_1 - 1)^2 + 5(x_2 - 2)^2 + \sin(x_1).
\label{eq:loss_B}
\end{equation}
The gradient is $\nabla f = [2(x_1 - 1) + \cos(x_1),\ 10(x_2 - 2)]^T$ and the Hessian is $H(x) = \operatorname{diag}(2 - \sin(x_1),\ 10)$. The condition number varies with $x_1$: at the minimum it is $\kappa \approx 10/(2-\sin(x_1^\star)) \approx 10/1.455 \approx 6.9$. The minimum satisfies $2(x_1^\star - 1) + \cos(x_1^\star) = 0$ (solved numerically: $x_1^\star \approx 0.5823$) and $x_2^\star = 2$, giving $x^\star \approx (0.582, 2.0)$ and $f^\star \approx 0.7232$. The Lipschitz constant is $L = \max_{x}(\lambda_{\max}(H(x))) = 10$ (from the $x_2$ curvature), giving a stability requirement $\alpha < 1/L = 0.1$ for gradient descent. Starting point: $x_0 = [-1, 4]^T$.

\subsection{Benchmark C: Rosenbrock Function}
\begin{equation}
f(x_1, x_2) = (1 - x_1)^2 + 100(x_2 - x_1^2)^2.
\label{eq:loss_C}
\end{equation}
The global minimum is at $(1, 1)$ with $f^\star = 0$. The Hessian at the optimum is:
\begin{equation}
H(1,1) = \begin{bmatrix} 802 & -400 \\ -400 & 200 \end{bmatrix},
\end{equation}
with eigenvalues $\lambda_1 \approx 0.5$ and $\lambda_2 \approx 1001.5$, giving condition number $\kappa \approx 2003$. This extreme ill-conditioning, combined with a narrow curved valley along $x_2 = x_1^2$, makes Rosenbrock the most challenging benchmark. Two separate difficulties arise: (i)~\emph{descent phase} from $x_0 = (-1,1)$ to the valley floor requires navigating steep transverse gradients ($\partial^2 f/\partial x_2^2 = 200$); (ii)~\emph{valley traversal} along $x_2 \approx x_1^2$ requires small careful steps in the nearly-flat longitudinal direction ($\lambda_1 \approx 0.5$). The maximal stable step for gradient descent is $\alpha < 2/(\lambda_1 + \lambda_2) \approx 2 \times 10^{-3}$ at the optimum. Starting point: $x_0 = [-1, 1]^T$.

\subsection{Benchmark D: Constrained Feasible Set}
For constrained optimisation (Questions 5--6):
\begin{equation}
\mathcal{X} = \{x \in \mathbb{R}^2 : 0.5 \leq x_1 \leq 5,\ -5 \leq x_2 \leq 10\}.
\end{equation}
This is a compact convex polytope (a box), enabling efficient projections and linear minimisation oracles required by projected gradient descent and Frank--Wolfe respectively.

% ============================================================
\section{Question 1: Adaptive Step-Size Methods \hfill [30 marks]}
\label{sec:q1}
% ============================================================

\subsection{Algorithm Formulations and Theoretical Background}

Four adaptive step-size methods are compared against constant-step gradient descent across all three benchmarks for 120 iterations. The fundamental motivation is that a fixed step size $\alpha < 1/L$ is required for convergence but may be far too conservative: adaptive methods adjust $\alpha_k$ to the local geometry, accelerating convergence without requiring knowledge of $L$.

\begin{algorithm}[H]
\caption{Polyak Step Size}
\label{alg:polyak}
\begin{algorithmic}[1]
\Require Initial point $x_0$, optimal value $f^{\star}$, tolerance $\varepsilon$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $\alpha_k \gets \dfrac{f(x_k) - f^{\star}}{\|g_k\|^2 + \varepsilon}$
    \State $x_{k+1} \gets x_k - \alpha_k\, g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

The Polyak step size is theoretically optimal for convex functions when $f^\star$ is known: it minimises the distance to the optimum along the gradient direction, guaranteeing $\|x_{k+1} - x^\star\| \leq \|x_k - x^\star\|$ for convex $f$. The regulariser $\varepsilon$ prevents division by zero when $\|\nabla f\| \approx 0$.

\begin{algorithm}[H]
\caption{Adagrad}
\label{alg:adagrad}
\begin{algorithmic}[1]
\Require Initial point $x_0$, base learning rate $\alpha_0$, tolerance $\varepsilon$, iterations $N$
\State $G_0 \gets \mathbf{0}$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $G_{k+1} \gets G_k + g_k \odot g_k$ \Comment{Per-coordinate squared-gradient accumulation}
    \State $x_{k+1} \gets x_k - \dfrac{\alpha_0}{\sqrt{G_{k+1}} + \varepsilon} \odot g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

Adagrad scales the learning rate by the inverse square root of the sum of squared past gradients. Coordinates with large cumulative gradients receive smaller effective rates (stabilising large-gradient directions) while infrequently-updated coordinates maintain larger rates. The effective rate is monotonically decreasing: $\text{eff\_}\alpha_k \sim \alpha_0 / \sqrt{k}$ in the worst case, which guarantees convergence but causes premature slowdown on stationary problems.

\begin{algorithm}[H]
\caption{RMSprop}
\label{alg:rmsprop}
\begin{algorithmic}[1]
\Require Initial point $x_0$, learning rate $\alpha_0$, decay $\beta$, tolerance $\varepsilon$, iterations $N$
\State $v_0 \gets \mathbf{0}$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $v_{k+1} \gets \beta\, v_k + (1 - \beta)\, g_k \odot g_k$ \Comment{Exponential moving average}
    \State $x_{k+1} \gets x_k - \dfrac{\alpha_0}{\sqrt{v_{k+1}} + \varepsilon} \odot g_k$
\EndFor
\end{algorithmic}
\end{algorithm}

RMSprop corrects Adagrad's vanishing step-size problem by replacing the full history with an exponentially-weighted moving average (parameter $\beta = 0.9$). Recent gradients receive weight $(1-\beta) = 0.1$ while the effective window spans roughly $1/(1-\beta) = 10$ iterations, allowing the step size to recover when curvature decreases.

\begin{algorithm}[H]
\caption{Polyak Momentum / Heavy Ball}
\label{alg:heavyball}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, momentum $\beta$, iterations $N$
\State $z_0 \gets \mathbf{0}$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_{k+1} \gets \beta\, z_k + \alpha\, \nabla f(x_k)$
    \State $x_{k+1} \gets x_k - z_{k+1}$
\EndFor
\end{algorithmic}
\end{algorithm}

Heavy Ball accumulates gradient momentum: $z_k$ is a weighted sum of all past gradients with geometric decay $\beta^j$. For a strongly convex quadratic with condition number $\kappa$, the optimal parameters $\alpha^\star = 4/(\sqrt{L} + \sqrt{\mu})^2$ and $\beta^\star = \left((\sqrt{\kappa}-1)/(\sqrt{\kappa}+1)\right)^2$ give convergence rate $\rho = ((\sqrt{\kappa}-1)/(\sqrt{\kappa}+1))^2$, versus $\rho_{\rm GD} = (\kappa-1)/(\kappa+1)$ for gradient descent --- a significant improvement for large $\kappa$.

\subsection{Hyperparameter Settings}

\begin{table}[H]
\centering
\caption{Q1: Hyperparameter settings for each method and benchmark. All methods run for 120 iterations.}
\label{tab:q1_params}
\begin{tabular}{llccc}
\toprule
\textbf{Method} & \textbf{Parameter} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & $\alpha$ & 0.08 & 0.06 & 0.0012 \\
\midrule
Polyak & $f^{\star}$ & 0 & 0 & 0 \\
 & $\varepsilon$ & $10^{-4}$ & $10^{-4}$ & $10^{-3}$ \\
\midrule
Adagrad & $\alpha_0$ & 1.8 & 1.2 & 0.45 \\
 & $\varepsilon$ & $10^{-5}$ & $10^{-5}$ & $10^{-5}$ \\
\midrule
RMSprop & $\alpha_0$ & 0.22 & 0.14 & 0.0035 \\
 & $\beta$ & 0.9 & 0.9 & 0.9 \\
 & $\varepsilon$ & $10^{-5}$ & $10^{-5}$ & $10^{-5}$ \\
\midrule
Heavy Ball & $\alpha$ & 0.045 & 0.035 & 0.0008 \\
 & $\beta$ & 0.88 & 0.90 & 0.86 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Convergence Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_A.pdf}
\caption{Q1: Convergence on Benchmark~A (Linear Regression). GD, Adagrad, and Heavy Ball all converge to the noise floor $J^\star \approx 0.4826$. The Polyak step \textbf{diverges} to $f \approx 2.72$ because $f^\star = 0$ misspecifies the true minimum --- as $\theta \to \theta_{\rm OLS}$, the gradient norm $\|\nabla J\| \to 0$ while $J(\theta_k) - 0 \to 0.4826 > 0$, causing $\alpha_k = (J(\theta_k) - f^\star)/\|\nabla J\|^2 \to \infty$. RMSprop converges to a slightly elevated value ($\approx 0.5027$) because its adaptive step oscillates near the minimum.}
\label{fig:q1_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_B.pdf}
\caption{Q1: Convergence on Benchmark~B (Toy Neural Network). Polyak again diverges ($f \approx 24.07$) due to $f^\star$ misspecification: the true minimum $f^\star \approx 0.7234 > 0$, not 0. Adagrad and Heavy Ball match GD's final accuracy ($f = 0.7244$). The moderate condition number ($\kappa \approx 7$) is amenable to all first-order methods; the $5:1$ ratio between $x_2$ and $x_1$ curvatures does not pose severe difficulties.}
\label{fig:q1_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_fval_C.pdf}
\caption{Q1: Convergence on Benchmark~C (Rosenbrock). Here $f^\star = 0$ is exactly correct; Polyak converges best ($f = 0.0094$) --- its step size $\alpha_k = f(x_k)/\|\nabla f\|^2$ shrinks as the iterate approaches the minimum, giving stable, accelerated convergence. Heavy Ball achieves $f = 1.40$, outperforming GD ($f = 3.51$) through momentum accumulation along the valley floor. Adagrad ($f = 2.20$) benefits from per-coordinate adaptation to the ill-conditioned valley. RMSprop ($f = 3.12$) is limited by the tiny base rate required for stability ($\alpha_0 = 0.0035$).}
\label{fig:q1_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q1: Final objective values after 120 iterations. Bold indicates best per benchmark.}
\label{tab:q1_final}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & 0.4826 & 0.7244 & 3.5142 \\
Polyak & 2.7189 & 24.0659 & \textbf{0.0094} \\
Adagrad & \textbf{0.4826} & \textbf{0.7244} & 2.2034 \\
RMSprop & 0.5027 & 0.7271 & 3.1204 \\
Heavy Ball & 0.4826 & 0.7244 & 1.4003 \\
\bottomrule
\end{tabular}
\end{table}

\begin{remark}[Polyak Divergence Mechanism]
The Polyak step size satisfies the descent property $\|x_{k+1} - x^\star\|^2 \leq \|x_k - x^\star\|^2\cdot(1 - (f(x_k)-f^\star)^2/(\|g_k\|^2\cdot\|x_k-x^\star\|^2))$ only when $f^\star$ is the true minimum. When $f^\star$ is underspecified (e.g.\ $f^\star = 0 < f_{\rm true}^\star$), the numerator $f(x_k) - f^\star$ remains bounded away from zero as $\|\nabla f(x_k)\| \to 0$, causing $\alpha_k \to \infty$ and divergence.
\end{remark}

\subsection{Contour Plots with Optimisation Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q1_contour_B.pdf}
\caption{Q1: Optimisation trajectories on Benchmark~B overlaid on objective contours. The elliptical contours reflect the 5:1 curvature ratio between $x_2$ and $x_1$ directions. GD and Adagrad follow nearly straight paths towards the minimum. Heavy Ball exhibits characteristic overshoot and return caused by momentum accumulation, then converges. The Polyak trajectory diverges visibly due to $f^\star$ misspecification, spiralling away from the minimum.}
\label{fig:q1_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q1_contour_C.pdf}
\caption{Q1: Optimisation trajectories on Benchmark~C (Rosenbrock). All trajectories descend rapidly to the banana-shaped valley $\{x_2 \approx x_1^2\}$ and then progress along it towards $(1,1)$. The Polyak trajectory (with correct $f^\star = 0$) shows the most aggressive progress along the valley floor. GD with $\alpha = 0.0012$ must use a tiny step to avoid instability, barely advancing along the valley in 120 iterations.}
\label{fig:q1_contour_C}
\end{figure}

\subsection{Adaptive Step-Size Evolution}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_A.pdf}
\caption{Q1: Step-size evolution on Benchmark~A. The Polyak step grows pathologically: $\alpha_k \to \infty$ as $\|\nabla J\| \to 0$ while $J(x_k) - f^\star \to 0.4826$. Adagrad decays monotonically as $\alpha_k \approx \alpha_0/\sqrt{k}$, reflecting the cumulative squared-gradient denominator. RMSprop stabilises due to exponential forgetting ($\beta = 0.9$), settling at an effective rate commensurate with recent curvature.}
\label{fig:q1_stepsize_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_B.pdf}
\caption{Q1: Step-size evolution on Benchmark~B. Similar divergent behaviour for Polyak. Adagrad's rapid early decay reflects large initial gradients from the starting point $x_0 = [-1, 4]^T$ far from the minimum. RMSprop maintains a moderate, slowly-varying effective rate throughout due to its finite-window averaging.}
\label{fig:q1_stepsize_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_stepsize_C.pdf}
\caption{Q1: Step-size evolution on Benchmark~C. With correct $f^\star = 0$, the Polyak step decreases monotonically: as $f(x_k) \to 0$ the numerator shrinks while the denominator remains bounded. This graceful decay is why Polyak achieves the best Rosenbrock convergence: the step size automatically scales with progress towards the true minimum.}
\label{fig:q1_stepsize_C}
\end{figure}

\subsection{Comparative Analysis}

\begin{table}[H]
\centering
\caption{Q1: Theoretical convergence rates and practical behaviour summary.}
\label{tab:q1_theory}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Theoretical rate} & \textbf{Step size} & \textbf{Memory overhead} \\
\midrule
GD & $O\!\left(\left(\tfrac{\kappa-1}{\kappa+1}\right)^k\right)$ linear & Fixed $\alpha \leq 1/L$ & $O(d)$ \\
Polyak & Depends on $f^\star$ accuracy & Adaptive (auto) & $O(d)$ \\
Adagrad & $O(1/\sqrt{k})$ for general convex & Adaptive, monotone $\downarrow$ & $O(d)$ \\
RMSprop & No convergence proof (heuristic) & Adaptive, bounded & $O(d)$ \\
Heavy Ball & $O\!\left(\left(\tfrac{\sqrt{\kappa}-1}{\sqrt{\kappa}+1}\right)^k\right)$ linear & Fixed $\alpha$, $\beta$ & $O(d)$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{itemize}[nosep]
\item \textbf{Convergence speed:} When $f^\star$ is known exactly, Polyak is fastest (Rosenbrock: $f = 0.009$ vs GD $f = 3.51$ after 120 iterations). Without exact $f^\star$, Heavy Ball and Adagrad provide the best improvement over GD baseline.
\item \textbf{Smoothness:} RMSprop produces the smoothest convergence curves due to exponential gradient averaging. Heavy Ball shows characteristic overshoot-then-converge oscillations from momentum accumulation.
\item \textbf{Stability:} Adagrad is the most uniformly stable method across all benchmarks; its monotonically decreasing step ensures $\sum_k \alpha_k = \infty$ (sufficient for convergence) yet $\alpha_k \to 0$ (dampening oscillations). GD baseline is stable but slow due to its conservative constant step.
\item \textbf{Practical implication:} Exact $f^\star$ is rarely available, limiting Polyak's applicability. RMSprop and Adagrad are more robust defaults, with RMSprop preferred for non-stationary problems where recent gradient information is more relevant than historical.
\item \textbf{Effect of ill-conditioning:} All methods slow down on Rosenbrock ($\kappa \approx 2000$). For a strongly convex quadratic, Heavy Ball's optimal rate $\left((\sqrt{\kappa}-1)/(\sqrt{\kappa}+1)\right)^2 \approx 0.957$ gives a 2.3$\times$ improvement in convergence rate over GD's $(\kappa-1)/(\kappa+1) \approx 0.9990$ --- but on non-quadratic problems like Rosenbrock, the advantage depends on effective local curvature and momentum direction.
\end{itemize}

% ============================================================
\section{Question 2: Momentum and Stochastic Methods \hfill [30 marks]}
\label{sec:q2}
% ============================================================

\subsection{Algorithm Formulations and Theoretical Background}

\begin{algorithm}[H]
\caption{Nesterov Momentum / Acceleration}
\label{alg:nesterov}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, max momentum $\beta_{\max}$, iterations $N$
\State $z_0 \gets \mathbf{0}$
\For{$k = 1, 2, \ldots, N$}
    \State $\beta_k \gets \min\!\left(\dfrac{k-1}{k+2},\ \beta_{\max}\right)$
    \State $\tilde{x}_k \gets x_k + \beta_k\, z_k$ \Comment{Lookahead point}
    \State $z_{k+1} \gets \beta_k\, z_k - \alpha\, \nabla f(\tilde{x}_k)$
    \State $x_{k+1} \gets x_k + z_{k+1}$
\EndFor
\end{algorithmic}
\end{algorithm}

The critical distinction from Heavy Ball is the \emph{lookahead}: the gradient is evaluated at the extrapolated point $\tilde{x}_k = x_k + \beta_k z_k$ rather than $x_k$. This allows Nesterov's method to ``look ahead'' in the momentum direction and correct before overshooting. The schedule $\beta_k = (k-1)/(k+2)$ grows from $0$ to $\beta_{\max}$, achieving Nesterov's celebrated $O(1/k^2)$ convergence rate on convex functions (versus $O(1/k)$ for gradient descent):

\begin{proposition}[Nesterov convergence rate]
For $L$-smooth convex $f$ and $\alpha = 1/L$, the Nesterov sequence satisfies $f(x_k) - f^\star \leq \frac{2L\|x_0 - x^\star\|^2}{(k+1)^2}$, achieving the optimal first-order convergence rate (Nesterov 1983).
\end{proposition}

The practical consequence: to achieve $f(x_k) - f^\star \leq \varepsilon$, gradient descent requires $O(\sqrt{L/\varepsilon})$ iterations while Nesterov requires only $O(\sqrt{L/\varepsilon})$ --- but Nesterov's bound is tighter by a factor of $\sqrt{L/\mu}$ (the condition number) in the strongly convex case, reducing to $O(\sqrt{\kappa}\log(1/\varepsilon))$ vs GD's $O(\kappa\log(1/\varepsilon))$.

\begin{algorithm}[H]
\caption{Adam Optimiser}
\label{alg:adam}
\begin{algorithmic}[1]
\Require Initial point $x_0$, learning rate $\alpha$, moments $\beta_1, \beta_2$, tolerance $\varepsilon$, iterations $N$
\State $m_0 \gets \mathbf{0},\ v_0 \gets \mathbf{0}$
\For{$t = 1, 2, \ldots, N$}
    \State $g_t \gets \nabla f(x_t)$
    \State $m_t \gets \beta_1\, m_{t-1} + (1 - \beta_1)\, g_t$ \Comment{First moment (mean)}
    \State $v_t \gets \beta_2\, v_{t-1} + (1 - \beta_2)\, g_t \odot g_t$ \Comment{Second moment (uncentred variance)}
    \State $\hat{m}_t \gets m_t / (1 - \beta_1^t)$ \Comment{Bias correction: counteracts zero initialisation}
    \State $\hat{v}_t \gets v_t / (1 - \beta_2^t)$
    \State $x_{t+1} \gets x_t - \alpha\, \hat{m}_t / (\sqrt{\hat{v}_t} + \varepsilon)$
\EndFor
\end{algorithmic}
\end{algorithm}

Adam combines momentum (via $m_t$, estimating $\mathbb{E}[g_t]$) with per-coordinate adaptive scaling (via $v_t$, estimating $\mathbb{E}[g_t^2]$). The bias corrections ensure that at step $t=1$, $\hat{m}_1 = g_1$ and $\hat{v}_1 = g_1^2$ regardless of the zero initialisation of $m_0, v_0$. The effective step size for coordinate $i$ is $\alpha/\sqrt{\hat{v}_{t,i}}$, which is large when the recent gradient variance is small and vice versa.

\subsection{Hyperparameter Settings}

\begin{table}[H]
\centering
\caption{Q2: Hyperparameter settings. Nesterov and Adam run for 150 iterations; GD baseline for 150 iterations.}
\label{tab:q2_params}
\begin{tabular}{llccc}
\toprule
\textbf{Method} & \textbf{Parameter} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & $\alpha$ & 0.08 & 0.06 & 0.0012 \\
\midrule
Nesterov & $\alpha$ & 0.06 & 0.035 & 0.0007 \\
 & $\beta_{\max}$ & 0.90 & 0.92 & 0.90 \\
\midrule
Adam & $\alpha$ & 0.12 & 0.08 & 0.006 \\
 & $\beta_1$ & 0.82 & 0.80 & 0.80 \\
 & $\beta_2$ & 0.999 & 0.999 & 0.999 \\
 & $\varepsilon$ & $10^{-8}$ & $10^{-8}$ & $10^{-8}$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Convergence Comparison}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_A.pdf}
\caption{Q2: Convergence on Benchmark~A. All three methods converge to $J^\star \approx 0.4826$. Nesterov and Adam reach the noise floor in fewer iterations than GD, with Nesterov slightly faster in the initial phase due to its growing momentum schedule. The quadratic structure is well-suited to acceleration since $O(1/k^2)$ improvement over $O(1/k)$ is most visible in the early-to-middle iterations.}
\label{fig:q2_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_B.pdf}
\caption{Q2: Convergence on Benchmark~B. Both Nesterov and Adam converge to $f^\star \approx 0.7244$, matching GD's final accuracy but arriving there faster. Adam's per-coordinate scaling effectively handles the 7:1 condition number: the $x_2$ direction (higher curvature) receives a smaller effective step while $x_1$ (lower curvature) maintains a larger step, reducing the curvature-induced bottleneck.}
\label{fig:q2_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_fval_C.pdf}
\caption{Q2: Convergence on Benchmark~C (Rosenbrock). This benchmark reveals the most dramatic performance differences. Nesterov achieves $f = 0.479$ versus GD's $f = 3.388$ after 150 iterations --- a 7$\times$ improvement. Nesterov's acceleration builds momentum along the consistent valley-floor direction ($x_2 \approx x_1^2$), compounding over iterations; the adaptive $\beta_k$ schedule prevents premature overshoot during the initial steep descent into the valley. Adam ($f = 1.578$) benefits less: its per-coordinate scaling is effective when curvature directions align with coordinate axes (anisotropic but axis-aligned problems), but the Rosenbrock valley's efficient search direction is diagonal (involving both $x_1$ and $x_2$ simultaneously), which Adam's coordinate-wise normalisation cannot exploit. This is a key limitation of diagonal-preconditioner methods: they do not adapt to off-diagonal curvature.}
\label{fig:q2_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q2: Final objective values after 150 iterations. Bold indicates best per benchmark.}
\label{tab:q2_final}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} \\
\midrule
GD (baseline) & 0.4826 & 0.7244 & 3.3880 \\
Nesterov & \textbf{0.4826} & \textbf{0.7244} & \textbf{0.4788} \\
Adam & 0.4826 & 0.7244 & 1.5785 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Contour Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q2_contour_B.pdf}
\caption{Q2: Trajectories on Benchmark~B. Nesterov's trajectory shows the characteristic smooth arc of accelerated methods, avoiding the zig-zag pattern of GD on anisotropic surfaces. The lookahead step corrects each momentum overshoot before it overshoots the minimum. Adam takes more direct steps by independently normalising each coordinate, effectively rescaling the gradient to an approximately unit-sphere before stepping.}
\label{fig:q2_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q2_contour_C.pdf}
\caption{Q2: Trajectories on Benchmark~C. All methods descend quickly to the valley floor but differ markedly in progress \emph{along} the valley towards $(1,1)$. Nesterov builds momentum in the consistent valley-floor direction, achieving far more progress than GD with the same step size. Adam's coordinate-wise normalisation does not align well with the valley's diagonal curvature structure, limiting its advantage over GD on this benchmark.}
\label{fig:q2_contour_C}
\end{figure}

\subsection{Mini-Batch Stochastic Gradient Descent}

Mini-batch SGD on Benchmark~A uses $\alpha = 0.06$, batch sizes $b \in \{5, 40\}$, 50 epochs, with data shuffled at every epoch. At each step, the mini-batch gradient estimator is:
\begin{equation}
\tilde{g}(\theta; \mathcal{B}) = \frac{1}{b} X_\mathcal{B}^T(X_\mathcal{B}\theta - y_\mathcal{B}) = \nabla J(\theta) + \underbrace{\xi_k}_{\text{stochastic noise}},
\end{equation}
where $\mathbb{E}[\xi_k] = 0$ and $\mathbb{E}[\|\xi_k\|^2] \leq \sigma^2/b$ for some $\sigma^2 < \infty$ (sampling variance). The two key trade-offs are: (1)~\emph{Updates per epoch}: $b=5$ gives $m/b = 200$ gradient steps per epoch, while $b=40$ gives only 25; (2)~\emph{Gradient noise}: $\text{Var}[\tilde{g}] \propto 1/b$, so $b=5$ has $40/5 = 8\times$ higher variance than $b=40$. These effects compete: more noisy updates (small $b$) can converge faster per epoch but oscillate near the minimum.

\begin{algorithm}[H]
\caption{Mini-Batch SGD}
\label{alg:sgd}
\begin{algorithmic}[1]
\Require Data $(X, y)$, initial $\theta_0$, learning rate $\alpha$, batch size $b$, epochs $E$
\For{$e = 1, 2, \ldots, E$}
    \State Randomly permute indices $\{1, \ldots, m\}$
    \For{each mini-batch $\mathcal{B}$ of size $b$}
        \State $g \gets \frac{1}{b} X_\mathcal{B}^T(X_\mathcal{B}\theta - y_\mathcal{B})$
        \State $\theta \gets \theta - \alpha\, g$
    \EndFor
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q2_sgd_batch.pdf}
\caption{Q2: Mini-batch SGD loss per epoch for batch sizes $b=5$ and $b=40$. With $b=5$: each epoch performs $1000/5 = 200$ gradient updates, providing faster per-epoch convergence. With $b=40$: each epoch performs only $1000/40 = 25$ updates, resulting in fewer but less noisy gradient steps per epoch. The smaller batch converges faster in terms of epochs due to more frequent updates, but exhibits more oscillation near the minimum because each mini-batch gradient deviates more from the true full-batch gradient. Both converge to approximately the same final loss $J^\star \approx 0.4826$.}
\label{fig:q2_sgd_batch}
\end{figure}

\subsection{SGD with Increased Observation Noise}

The experiment is repeated with noise scaled by factor 6 ($\varepsilon^{(i)} \sim \mathcal{N}(0, 36)$ versus $\mathcal{N}(0, 1)$). Since $J^\star \propto \sigma_{\rm noise}^2$ for linear regression, the irreducible loss floor increases by factor $\sigma_{\rm noise}^2 = 36$: $J^\star_{\rm noisy} \approx 36 \times 0.4826 \approx 17.4$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q2_sgd_noisy.pdf}
\caption{Q2: Effect of observation noise on mini-batch SGD. Left: original noise ($\sigma = 1$). Right: high noise ($\sigma = 6$). Under high noise, both batch sizes converge to the elevated floor $J^\star \approx 17$--$18$. Two effects are visible: (1)~the convergence \emph{speed} is similar since $\alpha$ and the condition number of $H$ are unchanged; (2)~the relative advantage of small batch ($b=5$) over large batch ($b=40$) diminishes under high observation noise, because the dominant noise source is now the measurement model rather than mini-batch sampling. With $\sigma=6$, both batch sizes exhibit comparable oscillation amplitude near the floor.}
\label{fig:q2_sgd_noisy}
\end{figure}

% ============================================================
\section{Question 3: Newton's Method and Local Approximation \hfill [30 marks]}
\label{sec:q3}
% ============================================================

\subsection{Local Approximations of $g(x) = x^4$}

At $x_0 = 0.25$: $g(x_0) = 0.25^4 = 3.906 \times 10^{-3}$, $g'(x_0) = 4 \times 0.25^3 = 0.0625$, $g''(x_0) = 12 \times 0.25^2 = 0.75$.

\textbf{First-order (linear) approximation:}
\begin{equation}
g(x) \approx g(x_0) + g'(x_0)(x - x_0) = 3.906 \times 10^{-3} + 0.0625\,(x - 0.25).
\end{equation}

\textbf{Second-order (quadratic) approximation:}
\begin{equation}
g(x) \approx g(x_0) + g'(x_0)(x - x_0) + \tfrac{1}{2}g''(x_0)(x - x_0)^2 = 3.906 \times 10^{-3} + 0.0625(x - 0.25) + 0.375(x - 0.25)^2.
\end{equation}

\textbf{Newton update derivation:} Minimising the second-order approximation with respect to $x$:
\begin{equation}
\frac{d}{dx}\left[g(x_0) + g'(x_0)(x - x_0) + \tfrac{1}{2}g''(x_0)(x-x_0)^2\right] = g'(x_0) + g''(x_0)(x - x_0) = 0
\end{equation}
\begin{equation}
\Rightarrow x^+ = x_0 - \frac{g'(x_0)}{g''(x_0)} = 0.25 - \frac{0.0625}{0.75} = 0.25 - 0.0833 = 0.1667.
\end{equation}
This is the Newton step: instead of stepping along the negative gradient with a fixed size, we step to the minimum of the local quadratic model. The step is automatically scaled by $1/g''(x_0)$: when curvature is high (large $g''$), the step is small to avoid overshooting; when curvature is low, the step is larger.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_approximation.pdf}
\caption{Q3: Local approximations of $g(x) = x^4$ at $x_0 = 0.25$. The first-order tangent line captures only local slope and diverges from $g(x)$ outside a tiny neighbourhood. The second-order parabola captures curvature and matches $g(x)$ over a wider region. The minimum of the parabola at $x \approx 0.167$ provides a better estimate of the function's minimum than a gradient step would, motivating Newton's method as an iterative application of this quadratic minimisation.}
\label{fig:q3_approx}
\end{figure}

\subsection{Newton's Method}

For a general differentiable function, the Newton update is:
\begin{equation}
x_{k+1} = x_k - \alpha \left[\nabla^2 f(x_k)\right]^{-1} \nabla f(x_k),
\end{equation}
where the damping parameter $\alpha \in (0, 1]$ is required when the Hessian approximation quality degrades far from the minimum (as on Rosenbrock). The Hessian is regularised as $H_k + \lambda I$ with $\lambda = 10^{-8}$ to ensure positive definiteness.

\begin{algorithm}[H]
\caption{Newton's Method with Hessian Damping}
\label{alg:newton}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, damping $\lambda$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $g_k \gets \nabla f(x_k)$
    \State $H_k \gets \nabla^2 f(x_k) + \lambda I$ \Comment{Regularised Hessian}
    \State $p_k \gets H_k^{-1}\, g_k$ \Comment{Newton direction (solve linear system)}
    \State $x_{k+1} \gets x_k - \alpha\, p_k$
\EndFor
\end{algorithmic}
\end{algorithm}

Settings: GD uses 80 iterations; Newton uses 20 iterations with Hessian damping $\lambda = 10^{-8}$.

\begin{table}[H]
\centering
\caption{Q3: Hyperparameters for GD baseline and Newton's method.}
\label{tab:q3_params}
\begin{tabular}{lcccccc}
\toprule
 & \multicolumn{2}{c}{\textbf{Bench.\ A}} & \multicolumn{2}{c}{\textbf{Bench.\ B}} & \multicolumn{2}{c}{\textbf{Bench.\ C}} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5} \cmidrule(lr){6-7}
 & GD & Newton & GD & Newton & GD & Newton \\
\midrule
$\alpha$ & 0.08 & 1.0 & 0.06 & 0.85 & 0.001 & 0.22 \\
Iterations & 80 & 20 & 80 & 20 & 80 & 20 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Convergence Comparison}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_A.pdf}
\caption{Q3: Newton vs GD on Benchmark~A. Newton with $\alpha = 1.0$ (full step) converges in \textbf{exactly one iteration} to $J = 0.4826$. This is the theoretical expectation: for a quadratic $J(\theta) = \frac{1}{2}\theta^T H\theta - b^T\theta + c$, the Newton step $\theta^+ = \theta - H^{-1}\nabla J(\theta) = \theta - H^{-1}(H\theta - b) = H^{-1}b = \theta^\star$ reaches the global minimiser in one step regardless of $\theta_0$, a property known as \emph{affine invariance}. GD requires 80 iterations to reach comparable accuracy.}
\label{fig:q3_fval_A}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_B.pdf}
\caption{Q3: Newton vs GD on Benchmark~B. Newton converges in approximately 5 iterations due to the near-quadratic structure. The Hessian $H(x) = \operatorname{diag}(2 - \sin(x_1),\ 10)$ is nearly constant near the minimum (since $\sin$ changes slowly), so the quadratic approximation remains accurate across iterations. GD requires 80 iterations but eventually converges to the same value $f = 0.7244$.}
\label{fig:q3_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q3_fval_C.pdf}
\caption{Q3: Newton vs GD on Benchmark~C. Newton with $\alpha = 0.22$ reaches $f = 0.686$ in 20 iterations, substantially outperforming GD ($f = 3.732$ in 80 iterations). The damped Newton step ($\alpha = 0.22 \ll 1$) is necessary because the Rosenbrock Hessian changes rapidly away from the minimum, and the full Newton step would overshoot the curved valley. Even with damping, Newton's curvature-aware updates navigate the valley more efficiently than GD's isotropic steps.}
\label{fig:q3_fval_C}
\end{figure}

\begin{table}[H]
\centering
\caption{Q3: Final objective values and converged positions. Newton achieves comparable or better results in 20 iterations versus GD's 80.}
\label{tab:q3_final}
\begin{tabular}{lcccc}
\toprule
\textbf{Benchmark} & \textbf{GD $f$ (80 it.)} & \textbf{Newton $f$ (20 it.)} & \textbf{Improvement factor} & \textbf{Newton final $x$} \\
\midrule
A & 0.4827 & 0.4826 & $1\times$ & $(3.028,\, 4.017)$ \\
B & 0.7244 & 0.7244 & $1\times$ (in 5 it.) & $(0.582,\, 2.000)$ \\
C & 3.7319 & 0.6864 & $5.4\times$ & $(0.183,\, 0.020)$ \\
\bottomrule
\end{tabular}
\end{table}

Note that Newton's final position on Benchmark~C is $(0.183, 0.020)$, still far from the true optimum $(1, 1)$ --- confirming that 20 damped Newton steps ($\alpha = 0.22$) are insufficient to fully converge on the extremely ill-conditioned Rosenbrock. Full convergence requires either more iterations, an adaptive step-size line search, or switching to the trust-region Newton variant.

\subsection{Contour Trajectories}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q3_contour_B.pdf}
\caption{Q3: Newton vs GD trajectories on Benchmark~B. Newton takes large, curvature-corrected steps that account for the 7:1 eigenvalue ratio between coordinates, reaching the minimum in $\approx 5$ steps. The Newton step $p_k = H^{-1}\nabla f$ rescales each coordinate by its inverse curvature, effectively normalising the elliptical contours to circular ones. GD takes many small steps, with slow progress in the low-curvature $x_1$ direction.}
\label{fig:q3_contour_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q3_contour_C.pdf}
\caption{Q3: Newton vs GD trajectories on Benchmark~C. Newton's trajectory follows the valley floor more closely by using Hessian information. GD with $\alpha = 0.001$ is constrained to tiny steps to avoid instability from the large Hessian eigenvalues ($\lambda_{\max} \approx 1000$), barely advancing in 80 iterations. Newton's step effectively inverts the Hessian, allowing progress along the low-curvature valley direction even when the orthogonal direction has very high curvature.}
\label{fig:q3_contour_C}
\end{figure}

\subsection{Update Magnitude vs Iteration}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q3_update_magnitude.pdf}
\caption{Q3: Update magnitude $\|x_{k+1} - x_k\| = \alpha\|H_k^{-1}g_k\|$ vs iteration for GD (80 steps) and Newton (20 steps). Newton's update magnitude decreases rapidly, consistent with \emph{quadratic convergence}: once inside the basin of attraction, $\|x_k - x^\star\| \leq C\|x_{k-1} - x^\star\|^2$, meaning the number of accurate decimal digits roughly doubles each iteration. For Benchmark~A, Newton's update drops to machine precision after a single step (the quadratic is solved exactly). For Benchmark~B, Newton converges in $\sim$5 steps due to near-constant Hessian. For Benchmark~C, the damped Newton step ($\alpha = 0.22$) takes fewer, larger, more productive steps than GD ($\alpha = 0.001$): Newton's update norms are 10--100$\times$ larger than GD's, yet each step produces much greater function value decrease because the Hessian-inverse preconditioner aligns the step with the low-curvature valley direction.}
\label{fig:q3_update}
\end{figure}

% ============================================================
\section{Question 4: Derivative Approximation and Derivative-Free Optimisation \hfill [30 marks]}
\label{sec:q4}
% ============================================================

\subsection{Part A: Finite Difference and Nesterov Random Search on Benchmark B}

\subsubsection{Finite Difference Gradient Approximation}

The forward finite-difference approximation replaces each partial derivative with:
\begin{equation}
\frac{\partial f}{\partial x_i}(x) \approx \frac{f(x + \delta e_i) - f(x)}{\delta}, \quad i = 1, \ldots, d.
\end{equation}
By Taylor expansion: $f(x + \delta e_i) = f(x) + \delta \frac{\partial f}{\partial x_i} + \frac{\delta^2}{2}\frac{\partial^2 f}{\partial x_i^2} + O(\delta^3)$, so the error is:
\begin{equation}
\left|\frac{f(x + \delta e_i) - f(x)}{\delta} - \frac{\partial f}{\partial x_i}(x)\right| = \frac{\delta}{2}\left|\frac{\partial^2 f}{\partial x_i^2}(x)\right| + O(\delta^2).
\end{equation}
The truncation error is $O(\delta)$ and grows with $\delta$. In finite precision, there is also a \emph{cancellation error} $O(\varepsilon_{\rm machine}/\delta)$ from computing the difference of nearly-equal floating-point numbers. The optimal $\delta$ balances these: $\delta^\star \sim \sqrt{\varepsilon_{\rm machine}} \approx 10^{-8}$. The assignment tests $\delta = 0.05$ (good: truncation $O(\delta)$ is manageable) and $\delta = 0.8$ (poor: truncation error dominates). Both use $\alpha = 0.08$, 120 iterations.

\subsubsection{Nesterov Random Search}

\begin{algorithm}[H]
\caption{Nesterov Random Search}
\label{alg:nrs}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, perturbation $\delta$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State Sample $u_k \sim \mathcal{N}(0, I_d)$; normalise: $u_k \gets u_k / \|u_k\|$
    \State $\hat{g}_k \gets \dfrac{f(x_k + \delta u_k) - f(x_k)}{\delta}\, u_k$ \Comment{Directional derivative estimate}
    \State $x_{k+1} \gets x_k - \alpha\, \hat{g}_k$
\EndFor
\end{algorithmic}
\end{algorithm}

This zeroth-order method estimates a \emph{directional derivative} along a uniformly-random unit vector $u_k$. The estimator satisfies $\mathbb{E}[\hat{g}_k] \approx \nabla f(x_k)$ (up to $O(\delta)$ error): the random projection $\langle \nabla f, u_k \rangle \cdot u_k$ is an unbiased estimator of $\frac{1}{d}\nabla f(x_k)$ in expectation. The method requires only \emph{two} function evaluations per iteration regardless of dimension $d$, making it far more efficient than coordinate-wise FD ($d+1$ evaluations) in high dimensions.

Settings: $\alpha = 0.025$, $\delta = 0.08$, 220 iterations; exact GD baseline with $\alpha = 0.06$.

\subsubsection{Results on Benchmark B}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q4_fval_B.pdf}
\caption{Q4A: Convergence comparison on Benchmark~B. Exact GD converges fastest ($\sim$30 iterations to near-minimum). FD GD with $\delta = 0.05$ closely tracks exact GD, confirming high gradient approximation fidelity ($O(\delta) = O(0.05)$ truncation error is small relative to $\|\nabla f\|$). FD GD with $\delta = 0.8$ converges much more slowly and to a higher final value: the large truncation error ($O(0.8)$) corrupts the gradient direction, especially near the minimum where $\|\nabla f\|$ is small and the error-to-signal ratio is large. Nesterov random search converges slowest and noisiest: using a single random direction per step provides only a one-dimensional projection of the gradient, requiring more iterations to explore the full gradient direction.}
\label{fig:q4_fval_B}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q4_contour_B.pdf}
\caption{Q4A: Optimisation trajectories on Benchmark~B. Exact GD and good FD ($\delta = 0.05$) follow nearly identical smooth paths, confirming gradient fidelity. Poor FD ($\delta = 0.8$) deviates from the true gradient direction, producing an irregular trajectory that still converges but less efficiently. Nesterov random search wanders stochastically: each step is taken in a random direction, relying on the average over many iterations to make progress towards the minimum.}
\label{fig:q4_contour_B}
\end{figure}

\subsection{Part B: Derivative-Free Methods on Benchmark C}

\subsubsection{Nelder--Mead Simplex Method}

The Nelder--Mead algorithm maintains a simplex of $n+1 = 3$ vertices in $\mathbb{R}^2$, iteratively transforming it via four operations applied to the \emph{worst} vertex $x_w$ relative to the centroid $\bar{x}$ of the other $n$ vertices:
\begin{align}
\text{Reflection:}& \quad x_r = \bar{x} + \alpha_r(\bar{x} - x_w), \quad \alpha_r = 1 \\
\text{Expansion:}& \quad x_e = \bar{x} + \gamma(\bar{x} - x_w), \quad \gamma = 2 \\
\text{Contraction:}& \quad x_c = \bar{x} + \rho(\cdot - \bar{x}), \quad \rho = 0.5 \\
\text{Shrink:}& \quad x_i \gets x_{\rm best} + \sigma(x_i - x_{\rm best}), \quad \sigma = 0.5
\end{align}
No gradient information is required. The simplex adapts its shape to the local landscape: elongating in productive directions and contracting when progress stalls.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q4_nelder_mead_C.pdf}
\caption{Q4B: Nelder--Mead on Benchmark~C (Rosenbrock). Left: the simplex centroid trajectory navigates the curved valley without any gradient information, demonstrating that simplex geometry adapts to the local landscape. The centroid path follows the valley floor from $(-1, 1)$ towards $(1, 1)$, avoiding the ill-conditioning issues that plagued gradient-based methods. Right: the best objective value decreases steadily, with periodic plateaus corresponding to simplex contraction phases.}
\label{fig:q4_nm}
\end{figure}

\subsubsection{Grid Search}

Brute-force grid search evaluates the objective at all $55 \times 55 = 3025$ uniformly-spaced points over $x_1 \in [-2, 2]$, $x_2 \in [-1, 3]$, with grid spacing $\Delta x_1 = 4/54 \approx 0.074$ and $\Delta x_2 = 4/54 \approx 0.074$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q4_grid_search_C.pdf}
\caption{Q4B: Grid search on Benchmark~C. Left: the 3025 grid points cover the search region uniformly. The best grid point found is close to the true optimum $(1,1)$ but offset by up to $\Delta x/2 \approx 0.037$ in each coordinate (grid resolution limit). Right: the best-so-far curve decreases in steps as progressively better grid points are encountered during the sequential scan.}
\label{fig:q4_grid}
\end{figure}

\subsection{Computational and Accuracy Comparison}

\begin{table}[H]
\centering
\caption{Q4: Comparison of derivative-free and approximate methods. FE = function evaluations per iteration; $d$ = problem dimension.}
\label{tab:q4_comparison}
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{Gradient Used?} & \textbf{FE/iter} & \textbf{Scales to large $d$?} & \textbf{Convergence type} \\
\midrule
Exact GD & Exact & 1 & Yes & Linear $O(1/k)$ \\
FD GD ($\delta=0.05$) & Approx.\ ($O(\delta)$ err.) & $d+1$ & Moderate & Near-linear \\
FD GD ($\delta=0.8$) & Poor approx. & $d+1$ & Moderate & Degraded \\
Nesterov random search & None (2 FE) & 2 & \textbf{Yes} & Sublinear, noisy \\
Nelder--Mead & None & $\leq n+1=3$ & Low-dim only & Sublinear \\
Grid search & None & $N^d$ total & \textbf{No} ($O(N^d)$) & Grid-limited \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Key insight:} Grid search is the only method guaranteed to find the global optimum (to within grid resolution) but its exponential cost in $d$ makes it impractical for $d > 3$. For our $55\times55 = 3025$ evaluation grid over $[-2,2]\times[-1,3]$, the best grid point achieves $f \approx 0.001$ compared to $f^\star = 0$, a resolution limited by grid spacing $\Delta \approx 0.074$. Nelder--Mead is effective for low-dimensional problems ($d \leq 10$) and can handle non-smooth or noisy objectives, but converges sublinearly and may stagnate in narrow valleys. Finite-difference GD bridges gradient-based and gradient-free methods: choosing $\delta = 0.05$ gives gradient error $\approx (\delta/2)\|H\|_2 \approx 0.025 \times 10 = 0.25$, acceptable relative to $\|\nabla f\|$ away from the minimum, while $\delta = 0.8$ gives error $\approx 4$, corrupting the gradient direction entirely. Nesterov random search is the most scalable: constant 2 FE/iter regardless of $d$, at the cost of high gradient variance ($\mathbb{E}[\|\hat{g}-\nabla f\|^2] = O(\|\nabla f\|^2 d)$ due to single-direction projection).

% ============================================================
\section{Question 5: Constrained Optimisation \hfill [30 marks]}
\label{sec:q5}
% ============================================================

\subsection{Problem Setup}

Minimise Benchmark~B subject to $x_1 \geq 0.5$, starting from the \emph{infeasible} initial point $x_0 = (0.2, 4.0)$:
\begin{equation}
\min_{x \in \mathbb{R}^2}\ f(x) \quad \text{subject to} \quad x_1 \geq 0.5.
\end{equation}
The KKT conditions for this inequality-constrained problem are:
\begin{equation}
\nabla f(x^\star) - \mu^\star e_1 = 0, \quad \mu^\star(x_1^\star - 0.5) = 0, \quad \mu^\star \geq 0, \quad x_1^\star \geq 0.5,
\end{equation}
where $e_1 = [1, 0]^T$ and $\mu^\star$ is the Lagrange multiplier. Since the unconstrained minimum $x^\star \approx (0.582, 2.0)$ already satisfies $x_1^\star = 0.582 > 0.5$, the constraint is \emph{inactive} at the optimum, implying $\mu^\star = 0$. Thus the constrained and unconstrained optima coincide, and the challenge is purely about handling the infeasible starting point.

\begin{algorithm}[H]
\caption{Projected Gradient Descent}
\label{alg:pgd}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_{k+1} \gets x_k - \alpha\, \nabla f(x_k)$ \Comment{Unconstrained gradient step}
    \State $x_{k+1} \gets \Pi_{\mathcal{X}}(z_{k+1}) = \left[\max(0.5,\, z_1^+),\, z_2^+\right]$ \Comment{Project onto $x_1 \geq 0.5$}
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{algorithm}[H]
\caption{Penalty Method}
\label{alg:penalty}
\begin{algorithmic}[1]
\Require Initial point $x_0$, step size $\alpha$, penalty $\lambda$, iterations $N$
\State Define $F_\lambda(x) = f(x) + \lambda\max(0,\, 0.5 - x_1)$ \Comment{Penalised objective}
\For{$k = 0, 1, \ldots, N-1$}
    \State $\nabla F_\lambda(x) = \nabla f(x) - \lambda \mathbf{1}[x_1 < 0.5]\, e_1$ \Comment{Gradient of penalised objective}
    \State $x_{k+1} \gets x_k - \alpha\, \nabla F_\lambda(x_k)$
\EndFor
\end{algorithmic}
\end{algorithm}

Settings: Projected GD ($\alpha = 0.08$); Penalty with $\lambda \in \{0.15, 1.8, 4.5\}$ ($\alpha = 0.05$ for $\lambda \leq 1.8$, $\alpha = 0.03$ for $\lambda = 4.5$); Unconstrained GD ($\alpha = 0.07$). All methods run for 100 iterations.

\subsection{Objective Value Comparison}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q5_fval.pdf}
\caption{Q5: Penalised or plain objective value $f(x_k)$ vs iteration. Projected GD converges monotonically to the constrained minimum $f \approx 0.7244$. Penalty methods with larger $\lambda$ approach the same minimum more tightly, while $\lambda = 0.15$ settles at a slightly higher value where the penalty gradient is insufficient to fully enforce feasibility. Unconstrained GD also converges to $f \approx 0.7244$ because the unconstrained minimum happens to be feasible ($x_1^\star \approx 0.582 > 0.5$), but its trajectory passes through the infeasible region temporarily.}
\label{fig:q5_fval}
\end{figure}

\subsection{Contour Plot with Feasible Region}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/q5_contour.pdf}
\caption{Q5: Objective contours with the constraint boundary $x_1 = 0.5$ (dashed red), infeasible region (shaded), and optimisation trajectories. Starting from the infeasible point $(0.2, 4.0)$, projected GD immediately clips $x_1 \leftarrow \max(0.5, x_1)$ at iteration 1, moving onto the boundary and then converging within the feasible set. The unconstrained GD initially moves into the feasible region and converges to the shared minimum. Penalty methods with increasing $\lambda$ produce trajectories that are progressively deflected away from the infeasible region.}
\label{fig:q5_contour}
\end{figure}

\subsection{Constraint Violation Analysis}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q5_violation.pdf}
\caption{Q5: Constraint violation $\max(0,\, 0.5 - x_1)$ on a logarithmic scale. Left: full 100 iterations. Right: zoomed to first 30 iterations. Projected GD achieves \textbf{zero violation from iteration 1} --- the projection $\max(0.5, x_1)$ is a hard constraint enforced at every step. Penalty with $\lambda = 4.5$ achieves near-feasibility within $\sim$10 iterations; the larger penalty gradient overcomes the objective gradient pulling $x_1$ below 0.5. With $\lambda = 0.15$, the penalty force is too weak to enforce strict feasibility, and the iterates remain slightly infeasible throughout.}
\label{fig:q5_violation}
\end{figure}

\subsection{Comparative Discussion}

\begin{table}[H]
\centering
\caption{Q5: Final positions, objective values, and constraint violations after 100 iterations.}
\label{tab:q5_final}
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{Final $x$} & \textbf{$f(x)$} & \textbf{Violation} & \textbf{Strictly feasible?} \\
\midrule
Unconstrained GD & $(0.582,\, 2.000)$ & 0.7244 & 0.0000 & Yes (by coincidence) \\
Projected GD & $(0.582,\, 2.000)$ & 0.7244 & 0.0000 & Yes (guaranteed) \\
Penalty $\lambda=0.15$ & $(\approx 0.48,\, 2.00)$ & $\approx 0.73$ & $>0$ & No \\
Penalty $\lambda=1.8$ & $(\approx 0.55,\, 2.00)$ & $\approx 0.72$ & $\approx 0$ & Approx. \\
Penalty $\lambda=4.5$ & $(0.583,\, 2.000)$ & 0.7244 & 0.0000 & Yes \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Projected GD} enforces feasibility \emph{exactly} at every iteration via the projection operator. For this problem, the projection $\Pi_{\mathcal{X}}(x) = \max(0.5, x_1)$ is trivial ($O(1)$ operation). The fundamental advantage is that feasibility is a hard constraint: no iterate ever violates it, which is critical in applications where infeasible points are inadmissible (e.g., physical constraints, probability simplex).

\textbf{Penalty methods} trade exact feasibility for algorithmic simplicity: only a modified gradient is needed, with no projection oracle. The weight $\lambda$ controls the trade-off:
\begin{itemize}[nosep]
\item Small $\lambda$ (0.15): weak penalty; the objective gradient dominates, and feasibility is only weakly enforced.
\item Large $\lambda$ (4.5): strong penalty; effectively enforces feasibility but requires a reduced step size ($\alpha = 0.03$ vs $0.05$) to prevent oscillation caused by the non-smooth penalty term's large effective gradient.
\end{itemize}
The penalty method converges to the true constrained optimum as $\lambda \to \infty$, but large $\lambda$ creates ill-conditioning in the penalised objective. The \emph{Augmented Lagrangian} method (not implemented here) addresses this by combining the penalty term with a Lagrange multiplier update $\mu^{t+1} = \mu^t - \lambda \cdot \text{violation}^t$, so the multiplier adapts automatically to enforce feasibility at finite $\lambda$. This achieves exact constraint satisfaction without requiring $\lambda \to \infty$, avoiding the ill-conditioning that comes with large penalty weights.

% ============================================================
\section{Question 6: Linear Programmes and Frank--Wolfe Algorithm \hfill [30 marks]}
\label{sec:q6}
% ============================================================

\subsection{Feasible Set and Linear Programme}

Feasible set: $\mathcal{X} = \{x \in \mathbb{R}^2 : 0.5 \leq x_1 \leq 5,\ -5 \leq x_2 \leq 10\}$, a compact convex box.

\textbf{Linear Programme:} For $f(x) = a^Tx$ with $a = [1, 2]^T$, the minimum over $\mathcal{X}$ is obtained by minimising each coordinate independently (since the feasible set is a box and the objective is linear):
\begin{equation}
x_i^\star = \begin{cases} \ell_i & \text{if } a_i > 0 \\ u_i & \text{if } a_i < 0 \end{cases} \quad \Rightarrow \quad x^\star = (0.5,\, -5), \quad f^\star = 0.5 + 2(-5) = -9.5.
\end{equation}
This illustrates the \textbf{fundamental theorem of linear programming}: the optimum of a linear objective over a bounded convex polytope is always attained at a vertex. The LP subproblem structure is the same oracle used by Frank--Wolfe.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q6_lp.pdf}
\caption{Q6-I: Feasible region $\mathcal{X}$ (blue rectangle) and contours of $f(x) = x_1 + 2x_2$. The parallel contour lines of the linear objective intersect $\mathcal{X}$ at the lower-left vertex $(0.5, -5)$, which is the LP optimum at $f^\star = -9.5$.}
\label{fig:q6_lp}
\end{figure}

\subsection{Frank--Wolfe Algorithm}

\begin{algorithm}[H]
\caption{Frank--Wolfe (Conditional Gradient)}
\label{alg:fw}
\begin{algorithmic}[1]
\Require Initial feasible $x_0 \in \mathcal{X}$, mixing parameter $\beta$, iterations $N$
\For{$k = 0, 1, \ldots, N-1$}
    \State $z_k \gets \arg\min_{x \in \mathcal{X}}\, \nabla f(x_k)^T x$ \Comment{Linear minimisation oracle (LMO)}
    \State $x_{k+1} \gets \beta\, x_k + (1 - \beta)\, z_k$ \Comment{Convex combination: stay in $\mathcal{X}$}
\EndFor
\end{algorithmic}
\end{algorithm}

For the box constraint $\mathcal{X}$, the LMO decomposes per coordinate:
\begin{equation}
(z_k)_i = \begin{cases} \ell_i & \text{if } (\nabla f(x_k))_i > 0 \\ u_i & \text{if } (\nabla f(x_k))_i \leq 0 \end{cases}, \quad i = 1, 2,
\end{equation}
requiring $O(d)$ operations --- much cheaper than a general-purpose projection. Every iterate $x_k$ is a convex combination of $x_0$ and LP vertices in $\mathcal{X}$, so \emph{feasibility is maintained automatically} without any projection.

With step size $\gamma_k = (1-\beta)$ (constant here), the convergence rate for $L$-smooth convex $f$ satisfies:
\begin{equation}
f(x_k) - f^\star \leq \frac{2LD^2}{k+2},
\end{equation}
where $D = \operatorname{diam}(\mathcal{X}) = \|(5-0.5, 10-(-5))\|_2 = \sqrt{4.5^2 + 15^2} \approx 15.66$ is the diameter of $\mathcal{X}$. For the interior objective $f(x) = (x_1-1)^2 + (x_2-5)^2$ with $L = 2$ (Hessian eigenvalue), the bound gives $f(x_k) - f^\star \leq 2 \cdot 2 \cdot 15.66^2/(k+2) \approx 980/(k+2)$. While this bound is loose (the actual convergence is faster due to the interior optimum), it confirms the $O(1/k)$ rate. This $O(1/k)$ rate is slower than projected gradient descent's $O(e^{-\mu k / L})$ linear rate, representing the price paid for replacing the projection with the cheaper LMO.

\subsection{Interior Optimum: $f(x) = (x_1-1)^2 + (x_2-5)^2$}

The unconstrained minimum $(1, 5)$ satisfies $0.5 \leq 1 \leq 5$ and $-5 \leq 5 \leq 10$, so it lies \emph{inside} $\mathcal{X}$. Two mixing parameters are compared: $\beta = 0.90$ (step size $(1-\beta) = 0.10$) and $\beta = 0.985$ (step size $0.015$).

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/q6_fw_convergence.pdf}
\caption{Q6-II: Frank--Wolfe convergence for the interior optimum. With $\beta = 0.985$ (smaller steps), the final objective ($f = 0.0046$) is lower than with $\beta = 0.90$ ($f = 0.0089$) after 180 iterations. Larger steps ($\beta = 0.90$) cause oscillation: $z_k$ jumps between vertices of $\mathcal{X}$ as the gradient direction fluctuates near the optimum, and the $10\%$ mixing weight amplifies these jumps in $x_k$. Smaller steps ($\beta = 0.985$, 1.5\% mixing) lead to smoother, more stable convergence.}
\label{fig:q6_fw_conv}
\end{figure}

\subsection{Boundary Optimum: $f(x) = x_1^2 + x_2^2$}

The unconstrained minimum $(0, 0)$ lies outside $\mathcal{X}$ (requires $x_1 \geq 0.5$). The constrained optimum is $(0.5, 0)$ on the boundary, with $f^\star = 0.25$.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q6_fw_contour.pdf}
\caption{Q6: Frank--Wolfe trajectories. Left: interior optimum --- both $\beta = 0.90$ and $\beta = 0.985$ trajectories converge towards $(1, 5)$ from $(1, 1)$. Right: boundary optimum --- the trajectory from $(3, 3)$ converges towards $(0.5, 0)$ on the constraint boundary. Frank--Wolfe naturally handles constraint boundaries: $z_k$ repeatedly selects the vertex $(0.5, -5)$ (lower left corner) as the gradient points towards the unconstrained minimum outside $\mathcal{X}$, pulling $x_k$ progressively towards the boundary.}
\label{fig:q6_fw_contour}
\end{figure}

\subsection{Evolution of $x_k$ and $z_k$}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/q6_evolution.pdf}
\caption{Q6: Evolution of iterates $x_k$ and LP vertices $z_k$ versus iteration. Top-left ($\beta=0.90$): $x_k$ converges quickly but $z_k$ oscillates between opposing vertices as the gradient direction reverses near the optimum. Top-right ($\beta=0.985$): slower, smoother convergence of both $x_k$ and $z_k$ due to smaller mixing weight. Bottom-left (boundary, $\beta=0.93$): $x_1^{(k)}$ converges monotonically to 0.5 as $z_1^{(k)} = 0.5$ (lower bound) is consistently selected; $x_2^{(k)}$ converges towards 0 with $z_2^{(k)}$ oscillating between $\pm 5$ as the $x_2$ gradient sign alternates. Bottom-right: convergence comparison across all cases.}
\label{fig:q6_evolution}
\end{figure}

\subsection{Interior vs Boundary Behaviour}

\begin{table}[H]
\centering
\caption{Q6: Frank--Wolfe final positions and objective values.}
\label{tab:q6_final}
\begin{tabular}{lcccc}
\toprule
\textbf{Case} & \textbf{$\beta$} & \textbf{Final $x$} & \textbf{$f(x)$} & \textbf{$f^\star$} \\
\midrule
Interior & 0.90 & $(1.066,\, 4.932)$ & 0.0089 & 0.0 \\
Interior & 0.985 & $(0.997,\, 5.067)$ & 0.0046 & 0.0 \\
Boundary & 0.93 & $(0.500,\, -0.337)$ & 0.3637 & 0.25 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Interior optimum ($\beta = 0.90$ and $0.985$):} Frank--Wolfe converges but exhibits the characteristic ``zig-zag'' oscillation: near the interior minimum, $z_k$ alternates between opposing vertices of $\mathcal{X}$ (e.g.\ $(0.5, -5)$ and $(5, 10)$) as the gradient direction oscillates. The convex average damps these jumps at rate $O(1/k)$ rather than $O(1/k^2)$. A useful diagnostic is the \emph{Frank--Wolfe gap} $g_k = \nabla f(x_k)^T(x_k - z_k)$, which satisfies $f(x_k) - f^\star \leq g_k$ and can serve as a stopping criterion; $g_k \to 0$ as $x_k \to x^\star$ since $\nabla f(x^\star) = 0$ at the interior optimum. Comparing $\beta$ values: conservative steps ($\beta = 0.985$) produce less oscillation and a lower final value ($f = 0.0046$) than aggressive steps ($\beta = 0.90$, $f = 0.0089$) after 180 iterations.

\textbf{Boundary optimum ($\beta = 0.93$):} Frank--Wolfe is well-suited to boundary optima. The LMO $z_k$ consistently selects the vertex $(0.5, \cdot)$ nearest the constrained optimum $(0.5, 0)$, pulling $x_k$ towards the boundary without explicit projection. The final value $f = 0.3637$ (after 140 iterations) remains above $f^\star = 0.25$, because the fixed step size $(1-\beta) = 0.07$ does not decay and causes residual oscillation. Using $\gamma_k = 2/(k+2)$ (satisfying $\sum_k \gamma_k = \infty$, $\sum_k \gamma_k^2 < \infty$) would guarantee convergence to $f^\star$; this is a fundamental limitation of the fixed-$\beta$ implementation.

\textbf{Comparison with projected GD (Q5):} Projected GD achieves zero constraint violation at every step and converges linearly. Frank--Wolfe is preferred when: (1)~the projection is expensive to compute (e.g., nuclear norm ball requires singular value decomposition), but (2)~linear minimisation over $\mathcal{X}$ is tractable. For box constraints, projection and LMO have equal cost, but Frank--Wolfe's projection-free nature becomes decisive for complex polytopes arising in machine learning (e.g., flow polytopes in structured prediction).

% ============================================================
\section{Summary and Conclusions}
\label{sec:conclusions}
% ============================================================

\subsection{Final Values Bar Chart}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/summary_final_values_bar.pdf}
\caption{Final objective values for each method and benchmark. Benchmarks A and B show most methods converging to the same minimum (differences are negligible), while Benchmark~C (Rosenbrock) reveals dramatic performance separation. The Polyak step on Benchmark~B diverges ($f=24.1$, shown in red) due to $f^\star$ misspecification. Newton's exceptional performance on Bench.~A (single-iteration convergence) is visible. The green bars for Benchmark~C expose the 500$\times$ performance gap between GD and Polyak/Newton.}
\label{fig:summary_bar}
\end{figure}

\subsection{Cross-Method Comparison}

\begin{table}[H]
\centering
\caption{Overall comparison of all methods across benchmarks. Ratings: $\bullet\bullet\bullet$ = excellent, $\bullet\bullet$ = good, $\bullet$ = poor, $\times$ = fails.}
\label{tab:overall}
\begin{tabular}{lccccc}
\toprule
\textbf{Method} & \textbf{Bench.\ A} & \textbf{Bench.\ B} & \textbf{Bench.\ C} & \textbf{No $f^\star$ needed?} & \textbf{No gradient?} \\
\midrule
GD (baseline) & $\bullet\bullet$ & $\bullet\bullet$ & $\bullet$ & Yes & No \\
Polyak & $\times$ & $\times$ & $\bullet\bullet\bullet$ & No & No \\
Adagrad & $\bullet\bullet\bullet$ & $\bullet\bullet\bullet$ & $\bullet\bullet$ & Yes & No \\
RMSprop & $\bullet\bullet$ & $\bullet\bullet$ & $\bullet$ & Yes & No \\
Heavy Ball & $\bullet\bullet\bullet$ & $\bullet\bullet\bullet$ & $\bullet\bullet$ & Yes & No \\
Nesterov & $\bullet\bullet\bullet$ & $\bullet\bullet\bullet$ & $\bullet\bullet\bullet$ & Yes & No \\
Adam & $\bullet\bullet\bullet$ & $\bullet\bullet\bullet$ & $\bullet\bullet$ & Yes & No \\
Newton & $\bullet\bullet\bullet$ & $\bullet\bullet\bullet$ & $\bullet\bullet\bullet$ & Yes & No \\
FD GD ($\delta$ small) & $\bullet\bullet$ & $\bullet\bullet$ & $\bullet$ & Yes & Yes \\
Nesterov RS & $\bullet$ & $\bullet$ & $\bullet$ & Yes & Yes \\
Nelder--Mead & N/A & N/A & $\bullet\bullet$ & Yes & Yes \\
Grid Search & N/A & N/A & $\bullet\bullet$ & Yes & Yes \\
Projected GD & $\bullet\bullet\bullet$ & $\bullet\bullet\bullet$ & N/A & Yes & No \\
Frank--Wolfe & $\bullet\bullet$ & $\bullet\bullet$ & N/A & Yes & No \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Comprehensive Benchmark C Comparison}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/summary_rosenbrock_all.pdf}
\caption{Comprehensive comparison of all methods on Benchmark~C (Rosenbrock, $\kappa \approx 2000$) over 200 iterations (Newton: 30 iterations only). This figure synthesises results from Questions 1, 2, and 3. The ranking after 200 iterations is: Polyak ($f = 0.0014$, correct $f^\star=0$) $>$ Newton 30 iters ($f = 0.16$) $>$ Nesterov ($f = 0.20$) $>$ Heavy Ball ($f = 0.29$) $>$ Adam ($f = 0.60$) $>$ Adagrad ($f = 1.45$) $>$ GD ($f = 3.17$). The ordering reflects the efficiency of curvature exploitation: Polyak uses functional gap information, Newton uses exact Hessian, Nesterov and Heavy Ball build directed momentum, while GD and Adagrad rely only on first-order local information. The nearly 2000$\times$ spread between best and worst methods underscores why algorithm choice is critical for ill-conditioned problems.}
\label{fig:summary_rosenbrock}
\end{figure}

\subsection{Key Findings}

\begin{enumerate}[nosep]
\item \textbf{Well-conditioned problems (Benchmark A, $\kappa \approx 1.8$):} All gradient-based methods converge efficiently to $J^\star \approx 0.4826$ (the irreducible noise floor). Newton achieves this in exactly one iteration (affine invariance). Mini-batch SGD with $b=5$ reaches $J^\star$ in $\sim$10 epochs vs $\sim$20 for $b=40$, confirming the update-count advantage of smaller batches.

\item \textbf{Moderately ill-conditioned problems (Benchmark B, $\kappa \approx 7$):} Per-coordinate methods (Adagrad, Adam) normalise the 7:1 curvature mismatch between $x_1$ and $x_2$. All methods except Polyak (which diverges due to $f^\star$ misspecification) converge to $f^\star \approx 0.7232$ within 150 iterations. Newton converges in $\sim$5 steps.

\item \textbf{Severely ill-conditioned problems (Benchmark C, $\kappa \approx 2000$):} Performance diverges dramatically. After 120 iterations: GD ($f=3.51$), Heavy Ball ($f=1.40$), Adagrad ($f=2.20$), Polyak ($f=0.009$). After 150 iterations: GD ($f=3.39$), Nesterov ($f=0.48$), Adam ($f=1.58$). Newton in just 20 iterations: $f=0.69$. The lesson: at $\kappa=2000$, first-order methods with constant step sizes can barely progress; curvature exploitation (Newton) or momentum-based acceleration (Nesterov) is essential.

\item \textbf{Constraint handling (Q5):} The constraint $x_1 \geq 0.5$ is non-binding at the unconstrained minimum $(x_1^\star \approx 0.582 > 0.5)$, so all methods ultimately converge to the same point. The difference is \emph{how they handle the infeasible starting point}: projected GD is immediately feasible (hard constraint), penalty methods with large $\lambda$ become feasible within 10 iterations, while $\lambda = 0.15$ never enforces strict feasibility.

\item \textbf{Derivative-free methods (Q4):} Good finite-difference ($\delta = 0.05$) achieves near-identical convergence to exact gradient descent. Nelder--Mead navigates Rosenbrock without gradients, achieving steady progress along the valley. Grid search guarantees global coverage but is limited to the grid resolution ($\Delta \approx 0.074$).
\end{enumerate}

\subsection{Practical Recommendations}

For machine learning applications: (1)~Adam or RMSprop are the practical defaults for neural network training due to robustness to hyperparameter choice and adaptivity to gradient scale changes; (2)~Nesterov momentum is preferred when convergence rate matters and a good learning rate can be tuned; (3)~Newton's method and quasi-Newton methods (L-BFGS) are preferred for small-scale problems where Hessian computation is affordable; (4)~For constrained problems with simple constraints (box, simplex), projected gradient descent is efficient; for complex polytopes, Frank--Wolfe provides a scalable alternative.

% ============================================================
\newpage
\appendix
\section{Python Implementation}
\label{app:code}
% ============================================================

The complete implementation is provided in the file \texttt{CS7DS2\_Final\_Code\_25336453.py}. Key algorithm implementations are reproduced below for reference.

\begin{lstlisting}[caption={Gradient Descent and Polyak Step Size}]
def gradient_descent(grad_fn, loss_fn, x0, alpha, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for _ in range(n_iters):
        x = x - alpha * grad_fn(x)
        x_hist.append(x.copy()); f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)

def polyak_step(grad_fn, loss_fn, x0, f_star, eps, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        alpha_k = (loss_fn(x) - f_star) / (np.dot(g, g) + eps)
        alpha_hist.append(alpha_k)
        x = x - alpha_k * g
        x_hist.append(x.copy()); f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Adagrad and RMSprop}]
def adagrad(grad_fn, loss_fn, x0, alpha0, eps, n_iters):
    x = x0.copy().astype(float)
    G = np.zeros_like(x)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        G += g**2
        eff_alpha = alpha0 / (np.sqrt(G) + eps)
        alpha_hist.append(np.mean(eff_alpha))
        x = x - eff_alpha * g
        x_hist.append(x.copy()); f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)

def rmsprop(grad_fn, loss_fn, x0, alpha0, beta, eps, n_iters):
    x = x0.copy().astype(float)
    v = np.zeros_like(x)
    x_hist, f_hist, alpha_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        v = beta * v + (1 - beta) * g**2
        eff_alpha = alpha0 / (np.sqrt(v) + eps)
        alpha_hist.append(np.mean(eff_alpha))
        x = x - eff_alpha * g
        x_hist.append(x.copy()); f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(alpha_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Heavy Ball and Nesterov Momentum}]
def heavy_ball(grad_fn, loss_fn, x0, alpha, beta, n_iters):
    x = x0.copy().astype(float)
    z = np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for _ in range(n_iters):
        g = grad_fn(x)
        z = beta * z + alpha * g
        x = x - z
        x_hist.append(x.copy()); f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)

def nesterov_momentum(grad_fn, loss_fn, x0, alpha, beta_max, n_iters):
    x = x0.copy().astype(float)
    z = np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for k in range(1, n_iters + 1):
        beta_k = min((k - 1) / (k + 2), beta_max)
        lookahead = x + beta_k * z
        g = grad_fn(lookahead)
        z = beta_k * z - alpha * g
        x = x + z
        x_hist.append(x.copy()); f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Adam Optimiser}]
def adam_optimiser(grad_fn, loss_fn, x0, alpha, beta1, beta2, eps, n_iters):
    x = x0.copy().astype(float)
    m_vec, v_vec = np.zeros_like(x), np.zeros_like(x)
    x_hist, f_hist = [x.copy()], [loss_fn(x)]
    for t in range(1, n_iters + 1):
        g = grad_fn(x)
        m_vec = beta1 * m_vec + (1 - beta1) * g
        v_vec = beta2 * v_vec + (1 - beta2) * g**2
        m_hat = m_vec / (1 - beta1**t)
        v_hat = v_vec / (1 - beta2**t)
        x = x - alpha * m_hat / (np.sqrt(v_hat) + eps)
        x_hist.append(x.copy()); f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist)
\end{lstlisting}

\begin{lstlisting}[caption={Newton's Method with Damping}]
def newtons_method(grad_fn, hess_fn, loss_fn, x0, alpha, n_iters, damping=1e-8):
    x = x0.copy().astype(float)
    x_hist, f_hist, update_norms = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        H = hess_fn(x) if callable(hess_fn) else hess_fn
        H_reg = H + damping * np.eye(len(x))
        try:
            p = np.linalg.solve(H_reg, g)
        except np.linalg.LinAlgError:
            p = g
        update_norms.append(np.linalg.norm(alpha * p))
        x = x - alpha * p
        x_hist.append(x.copy()); f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(update_norms)
\end{lstlisting}

\begin{lstlisting}[caption={Frank--Wolfe (Conditional Gradient)}]
def frank_wolfe(grad_fn, loss_fn, x0, bounds, beta, n_iters):
    x = x0.copy().astype(float)
    x_hist, f_hist, z_hist = [x.copy()], [loss_fn(x)], []
    for _ in range(n_iters):
        g = grad_fn(x)
        # Linear minimisation oracle for box constraints
        z = np.array([bounds[i][0] if g[i] > 0 else bounds[i][1]
                      for i in range(len(g))])
        z_hist.append(z.copy())
        x = beta * x + (1 - beta) * z
        x_hist.append(x.copy()); f_hist.append(loss_fn(x))
    return np.array(x_hist), np.array(f_hist), np.array(z_hist)
\end{lstlisting}

\end{document}
